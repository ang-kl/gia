{
  "name": "soleat",
  "version": "0.62.811",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "test": "vitest run",
    "test:render": "node scripts/render-smoke.mjs",
    "test:watch": "vitest",
    "build": "npm run build:cuisine && npm run build:menu && npm run build:hawker && npm run build:transport && npm run build:oversight && npm run build:about && npm run build:clipboard",
    "build:about": "cd web/about && npm install --no-audit --no-fund && npm run build",
    "build:clipboard": "cd web/clipboard && npm install --no-audit --no-fund && npm run build",
    "build:cuisine": "cd web/cuisine && npm install --no-audit --no-fund && npm run build",
    "build:menu": "cd web/menu && npm install --no-audit --no-fund && npm run build",
    "build:hawker": "cd web/hawker && npm install --no-audit --no-fund && npm run build",
    "build:transport": "cd web/transport && npm install --no-audit --no-fund && npm run build",
    "build:oversight": "cd web/oversight && npm install --no-audit --no-fund && npm run build",
    "build:geo": "node scripts/build-geo-overlays.js",
    "michelin:refresh": "node scripts/build-michelin-meta.mjs",
    "fetch:mrt-osm": "node scripts/fetch-mrt-lines-osm.js",
    "fetch:24hr-places": "node scripts/fetch-24hr-places.js",
    "count:cuisines": "node scripts/recount-cuisine-venues.js",
    "postinstall": "npm run build"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.40.0",
    "@google/generative-ai": "^0.24.1",
    "@sentry/node": "^8.0.0",
    "adm-zip": "^0.5.17",
    "axios": "^1.6.0",
    "cheerio": "^1.2.0",
    "dotenv": "^16.3.1",
    "express": "^5.2.1",
    "node-telegram-bot-api": "^0.64.0",
    "pino": "^9.0.0",
    "redis": "^4.6.10"
  },
  "devDependencies": {
    "pino-pretty": "^11.0.0",
    "playwright-core": "^1.62.1",
    "vitest": "^2.0.0"
  }
}
