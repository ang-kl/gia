node_modules/
.env
.env.local
*.log
.DS_Store
dist/
# Vite build output for the Cuisine Picker TMA — emitted by `npm run build`
# (which runs as a postinstall on Railway). Source lives in web/cuisine/.
public/cuisine/
