node_modules/
.env
.env.local
*.log
.DS_Store
dist/
# Vite build output for the Cuisine Picker TMA — emitted by `npm run build`
# (which runs as a postinstall on Railway). Source lives in web/cuisine/.
public/cuisine/
# Vite build output for the Menu TMA (v0.28.0). Source: web/menu/.
public/menu/
# Vite build output for the Hawker NEA TMA (v0.38.0). Source: web/hawker/.
public/hawker/
