// classics-notes-i18n.generated.js — translated bodies for classics-notes.js.
//
// GENERATED SHAPE, HAND-FILLED CONTENT. `scripts/translate-content.mjs` needs
// GEMINI_API_KEY and the operator's standing instruction is not to spend on
// external APIs, so rows here are written by hand, exactly as the 1,653 French
// dish notes in v0.62.778 were. The file keeps the generated shape so a future
// funded run can extend it instead of replacing it.
//
// KEY: "<scope>::<dish>" where scope is the ISO country code for CLASSIC_NOTES
// ("SG::chilli crab") and the cuisine slug for CUISINE_NOTES
// ("peranakan::ayam buah keluak"). The two namespaces cannot collide: country
// codes are upper-case, cuisine slugs lower-case.
//
// VALUES are note bodies only — never `local` (native script is identity, not
// copy) and never `sources` (a citation is not translated).
//
// classics-notes.js folds these onto each note at load. A HAND-AUTHORED note
// wins: the merge only fills a language the note does not already carry.
module.exports = {
};
