(№ 702 - 12-06 '26 13:00 SGT) v0.62.47

# Journal — soleat v0.62.47 (group the whole city plate — "More local classics" → ascending food-group sections)

> Operator: *"have you complete phase 1 to group cities dishes"* → answered **No**: the food-group
> grouping (`dish-food-group.js`) was built for the CUISINE plate only; the geo CITY plate
> (`platesNear` / `city-plates.js`) showed its country-overlay "More local classics" as one flat
> scrolling chip wall. Decision (AskUserQuestion): **"Group the whole city plate."** Since the
> city-UNIQUE dishes are tiny (21 cities, max 4 dishes — already the rich 📜 headliner rows), the
> grouping lands on the long classics list, matching the cuisine plate's model.

> Commit 719 / journal 702 / build 441 (web/ changed). PATCH 0.62.46 → 0.62.47. PR #1008 (draft).

## New

### [HDR] #702 | 13:00 SGT 12-06-26 | v0.62.47 — group the city plate's "More local classics" | dish-food-group.js + city-plates.js + web/cuisine (ArrivalPlate.jsx) + tests | 1 PR | 1 build |

- **[INTENT]** Apply the cuisine plate's food-group grouping to the CITY plate. The city-unique
  dishes stay as the headliner rows; the country-overlay "More local classics" (SG 164, MY ~40)
  becomes ascending food-group sections instead of a flat chip wall.

- **[DELTA]**
  - **`dish-food-group.js`** — extracted `sortGroupsAscending()` (shared); added
    **`groupDishNames(names)`** → buckets a flat names-only list into ascending food-group sections
    (drinks caught by the keyword rules since names carry no `kind` flag), `'other'` last. Exported.
  - **`city-plates.js`** — `platesForCity` attaches **`plate.classicGroups`** (grouped) alongside
    the existing flat `plate.classics`; lazy-require `dish-food-group`, fail-open. The city-unique
    `dishes` (headliner rows) are untouched.
  - **`web/cuisine/src/v2/components/ArrivalPlate.jsx`** — "More local classics" renders labelled
    ascending sections when `plate.classicGroups` is present; the flat chip wall remains as the
    fallback (overlay-less countries / back-compat).
  - **`__tests__/dish-food-group.test.js`** (+2) — `groupDishNames` (placement, drink detection,
    ascending, `'other'` last, empty/null) + city-plate integration (`classicGroups` partitions
    `classics` with nothing lost, bilingual labels, `dishes` preserved).

- **[VERIFICATION (sandbox)]** `node --check` clean (`dish-food-group.js`, `city-plates.js`); full
  suite **3408/3408** green (+2); `web/cuisine` build green. Singapore groups:
  `soup:6 · veg:6 · snack:8 · grilled:9 · stew-curry:10 · rice:11 · bread-dumpling:12 · seafood:12 ·
  noodles:15 · sweet:16 · drink:39 · other:18` (other last).

- **[STATUS]** **DRAFT** — PR #1008 (rides `claude/refine-local-plan-5vh1jm`). Railway on merge.

- **[TEST]** Set location near a covered city (Singapore) with no cuisine selected → the geo plate
  "What to try here"; expand → city-unique dishes as rich 📜 rows; open "More Singapore classics" →
  ascending food-group sections (Soups, Veg, …, Drinks, More). Tap any chip → dish search.

- **[KNOWN GAPS]**
  - City-UNIQUE dishes (1–4 per city) are intentionally NOT sub-grouped — they're the headliner
    rows with rich 📜 cards; grouping a 3-item list adds nothing.
  - Overlay-less countries (no `classicsForCountry`) keep the no-classics plate (unchanged).
  - Places per-SKU telemetry remains the other open follow-up.

- **Confirmation Gates**

  | Gate | Resolution | Date |
  |---|---|---|
  | G1 — version bump | PATCH `0.62.46 → 0.62.47`. | 12-06 '26 |
  | G2 — destructive action | None — additive; flat `classics` path kept as fallback. | 12-06 '26 |
  | G3 — new decision rule | City plate's "More local classics" grouped by food group (ascending) server-side; city-unique dishes stay headliner rows. | 12-06 '26 |
  | G4 — paid external API call | None — pure in-memory from curated overlays. | 12-06 '26 |

## Amendments

- **`dish-food-group.js`** (`sortGroupsAscending` extract + `groupDishNames`) · **`city-plates.js`**
  (`platesForCity` attaches `classicGroups`) · **`web/cuisine/src/v2/components/ArrivalPlate.jsx`**
  (grouped classics render) · **`__tests__/dish-food-group.test.js`** (+2) · **`package.json`**
  `0.62.46 → 0.62.47` · **`doc/.serial-state.yml`** counters + anchor.

## Deleted

- (none) — additive; the flat `plate.classics` chip-wall render is preserved as the fallback path.
