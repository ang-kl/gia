(№ 916 - 17-07 '26 07:30 SGT) v0.62.559

# Journal — soleat v0.62.559 (Hawker: InfoWindow bus stop shows on the EMBEDDED map, not external Google Maps)

> Operator (IMG_0699, the centre-pin InfoWindow): "Why [does] click on the bus stop of the hawker
> centre pin open Google Map instead of showing me where the bus stop is in the embedded map?"

## New

### [HDR] #916 | 07:30 SGT 17-07-26 | v0.62.559 — InfoWindow bus stop → embedded-map pin | 1 file | PR #(pending) |

- **[FIX].** `web/hawker/src/components/HawkerMapPanel.jsx` `buildInfoHtml`: the centre-pin InfoWindow's
  bus-stop rows were external links (`<a href="https://maps.google.com/?q=lat,lng" target="_blank">`).
  They now call **`window.__giaHawkerShowBusStop(code, lat, lng, description)`** via an inline `onclick`
  — the SAME handler the card's bus-stop pill uses — so a tap **drops the labelled bus-stop pin + pans
  + flashes on THIS embedded map** (with its live-arrivals bubble) instead of opening external Google
  Maps. (The station rows already used `__giaHawkerFocusStation`; now the bus stops match.)
  - New `jsStr()` helper safely embeds the bus-stop description in the single-quoted JS string inside
    the double-quoted `onclick` attribute (JS-escape the quote/backslash, then HTML-escape so the
    browser decodes it back to valid JS). Removed the now-unused `gmaps()` local.

- **[DELTA — housekeeping].** `package.json` 0.62.558 → **0.62.559**; Journal #916 + serial-state
  (commit 1033→**1034**, build 658→**659**, journal 915→**916**; re-anchor 17-07 '26 07:30 SGT,
  prior 915 demoted verbatim AU-7).

- **[VERIFICATION (sandbox)].** **Hawker vite build green** (42 modules); **full `npm test` 3719/3719
  across 145 files**. ⚠️ Live-only (iPad) — shipping live.

- **[STATUS]** STAGED → PROD on merge (operator "make it live").

- **[KNOWN GAPS].**
  1. 👀 **Device recheck** — tapping a bus stop in the centre-pin InfoWindow now drops the pin +
     arrivals bubble on the embedded map (no external Google Maps)?
  2. 🅺🅸🆅 Hawker CLEANING-closure still shelved (needs a data.gov.sg sample; sandbox egress-blocked).
  3. ❓ Q4 carpark-closes-card repro still awaited.

## Confirmation Gates
| Gate | Status | Note |
|------|--------|------|
| G1 | ✅ | PATCH bump 0.62.558→0.62.559; behaviour fix; build + 3719 tests green |
| G2 | ➖ | no destructive action |
| G3 | ➖ | no new decision rule |
| G4 | ➖ | no external paid API call |
