(№ 909 - 17-07 '26 06:03 SGT) v0.62.552

# Journal — soleat v0.62.552 (Hawker carousel: glass peeking cards + iPad-mini 2-in-focus · map toggle folded into FooterNav)

> Operator (IMG_0686, iPad Pro landscape, v0.62.551):
> 1. The carousel's THREE cards stay in focus, but the two "half-seen" cards peeking at each end
>    should read as a **glass effect** — and show **two in focus on iPad-mini** width.
> 2. The map 🗺 pill at the bottom-right should be **part of the "down / end"** cluster (not a
>    separate floating pill above it).

## New

### [HDR] #909 | 06:03 SGT 17-07-26 | v0.62.552 — carousel glass peek + iPad-mini count + toggle into FooterNav | 2 files | PR #(pending) |

- **[DELTA 1 — glass peeking cards + responsive focus count].** `web/hawker/src/App.jsx`:
  - New **`CentreCarousel`** component: an `IntersectionObserver` (root = the scroll track, threshold
    0.92) marks a card **focused (opaque)** once it is ≥ 92 % visible; anything less peeks and renders
    **glass**. Falls back to all-opaque when `IntersectionObserver` is unavailable.
  - `renderCentreCard(c, i, glass)` gained a **`glass`** flag → the card surface switches between opaque
    `bg-tg-card` and `bg-tg-card/60 backdrop-blur-md`. The portrait/mobile grids pass `glass=false`
    (unchanged); only the carousel's peeking cards are glass.
  - **Responsive focus count** via `basisClass="basis-[82%] md:basis-[44%] min-[1180px]:basis-[30%]"`:
    3 cards in focus on wide tablets/desktop (≥ 1180 px), **2 on an iPad-mini-width** screen, 1 on a
    phone aspect.

- **[DELTA 2 — map toggle folded into FooterNav].**
  - `web/_shared/components/FooterNav.jsx` — new optional **`leading`** prop: renders an app-supplied
    control as the FIRST item inside the same rounded cluster, with a divider (other TMAs omit it and
    are unaffected).
  - `App.jsx` — the standalone fixed `MapToggle` pill is replaced by **`MapToggleButton`** passed as
    `FooterNav`'s `leading`, so 🗺 Map / 📋 List now sits in the `[🗺] | ⇣ down | 🔚 end` row (operator).
    Removed the dead `isWide && …` MapToggle line from the phones stacked layout.

- **[DELTA — housekeeping].** `package.json` 0.62.551 → **0.62.552**; Journal #909 + serial-state
  (commit 1026→**1027**, build 651→**652**, journal 908→**909**; re-anchor 17-07 '26 06:03 SGT,
  prior 908 demoted verbatim AU-7).

- **[VERIFICATION (sandbox)].** **Hawker vite build green** (42 modules); **full `npm test` 3719/3719
  across 145 files**. ⚠️ The glass peek, the IntersectionObserver focus flip, the iPad-mini 2-count,
  and the folded toggle are live-only (iPad) — shipping live.

- **[STATUS]** STAGED → PROD on merge (operator "make it live").

- **[TEST]** Suite excludes `web/`; JSX validated by the vite build. No new unit test.

- **[KNOWN GAPS].**
  1. 👀 **iPad recheck** — peeking cards read glass, 3 in focus on Pro / 2 on mini? The 🗺/📋 toggle
     reads as part of the down/end cluster? The `min-[1180px]` mini/Pro breakpoint may need tuning.
  2. 🧭 **Still pending — hawker CLEANING closure + BIB GOURMAND** (separate feature). Sandbox proxy
     blocks data.gov.sg (403) and the retired parser is below the shallow-clone graft, so the cleaning
     dataset (d_bda4baa634dd1cc7a6c7cad5f19e2d68) column names must come from an operator-pasted sample.
     Bib Gourmand is buildable now (join `SG-michelin.js` `BIB_GOURMAND[]` → centres; pin `#C6282D`;
     `✳️ Bib Gourmand · <stall>` card row) pending the operator's label confirm.

## Confirmation Gates
| Gate | Status | Note |
|------|--------|------|
| G1 | ✅ | PATCH bump 0.62.551→0.62.552; additive/restyle; build + 3719 tests green |
| G2 | ➖ | no destructive action (MapToggle → MapToggleButton rename; dead line removed) |
| G3 | ➖ | no new decision rule |
| G4 | ➖ | no external paid API call |
