(№ 753 - 24-06 '26 04:10 SGT) v0.62.301

# Journal — soleat v0.62.274 → v0.62.301 (Cuisine/Hawker/Train UI + search-confidence arc: catalogue reorg · result-card depth · exact/alternate "Nearby Flavours" + combos · map fixes · open-hours timezone · /ztest confidence · forex-in-strip · Set Meal Beta · FR fit)

> Operator: a long single-session arc — 31 PRs (#1242–#1272), v0.62.274→v0.62.301 — across
> the Cuisine catalogue + result cards, the three map TMAs, the bot search/open-hours, and
> a French-fit audit. Built + shipped via the GitHub Contents API (iCloud `.git`
> lock-jammed; per-PR squash + auto-merge). Grouped by theme below, PR refs inline.
> Commit 812→843 / journal 752→753 / build 467→498.

## New

### [HDR] #753 | 04:10 SGT 24-06-26 | v0.62.274→v0.62.301 — 31-PR Cuisine/Hawker/Train UI + search-confidence arc (#1242–#1272) | ~25 files | 31 PRs |

- **[INTENT]** Operator's rolling polish + feature pass after v0.62.273: reorganise the
  Cuisine cuisine-catalogue by world region, deepen the result-card hierarchy, mark results
  that aren't an exact cuisine hit, fix several map regressions, make open/closed status
  timezone- + holiday-correct, tighten /ztest + search messaging, surface local-currency
  conversion, add a confirmed-from-website "Set Meal" probe, and audit French for fit.

- **[DELTA]** (grouped by theme)
  - **Cuisine catalogue reorg** (#1242–#1245, v0.62.284–285) — SG-rooted cuisines fold into
    **Southeast Asian** (Singaporean + Peranakan lead; "Common in Singapore" retired);
    Eurasian → 🌏, **Peranakan → phoenix-and-peony motif image**; **East Asian** national/SAR
    cuisines (incl. HK/Macau row) then a hollow divider then regional China; **Americas &
    Oceania** Aus/NZ last; **European** split N/S/Central/E/W by hollow dividers. New
    `CUISINE_ORDER` + `DIVIDER_BEFORE` overlays in `cuisines-vault.js`; drawer renders the
    hollow `col-span-2` hairline.
  - **Result-card depth** (#1256, #1258, #1259, #1263) — in-view card **opaque white**, side
    cards **liquid-glass**; 💬/🔍 FABs lifted **z-40** above cards; composer-expand **lifts
    the centred card** (headroom fix so its top isn't chopped); **opaque/lift now track the
    scroll-centred card** (validated against the live list — fixed "opaque sometimes didn't
    apply" + zero-result "**tap 🔍 again**" when criteria changed since the search).
  - **Exact vs "Nearby Flavours"** (#1260, #1264) — single-cuisine results tag
    `matchTier` exact/alternate; alternates get a **"{cuisine} & Nearby Flavours"** strip
    (CVD-safe accent). **Combos** (e.g. Korean+Japanese): both-cuisine = exact, one-cuisine
    cards get a per-cuisine strip + vertical grouping. Added Middle-Eastern + East-Asian
    **dish keywords** so review-only venues classify. Root cause was Moroccan→Lebanese/Turkish
    (middle-eastern un-gated).
  - **Map fixes** (#1257, #1261, #1265) — **removed auto-fullscreen** on Train + Hawker (all
    platforms; desktop/notebook takeover); **hawker pins no longer radius-clipped** (fixed
    show/disappear on pan/zoom — `name==='hawker' → radius Infinity`); **minZoom 5→7** on all
    three maps (z5 hung the app).
  - **Open-hours** (#1262) — read **`currentOpeningHours.periods`** (holiday-aware) + venue
    **`utcOffsetMinutes`** (`open-hours.js localNow`); split-shift / midnight math preserved.
  - **/ztest + places links** (#1265, #1268) — result links resolve to the **actual place**
    (`googleMapsUrl` + placeId, not an empty coords pin); the probe shows a **confidence
    ladder ✅ on-site · 📷 from photo · ◌ unconfirmed** instead of raw "no-match/scrape-failed".
  - **City "near {landmark}"** (#1266) — an OTHER-region city pick reverse-geocodes the
    centroid to the nearest prominent POI → "**{city} — near {landmark}**" (new
    `/api/reverse-geocode?near=1` + `nearestLandmark` searchNearby).
  - **NEA hawker names** (#1267) — join the official NEA GeoJSON `NAME` by postal (98% of 123)
    → `centre.displayName` on all three TMA cards ("Blk 1 Jalan Kukoh" → "Jalan Kukoh Blk 1
    (Kukoh 21 Food Centre)"); `name` unchanged (coords/stalls joins intact).
  - **Forex in the strip** (#1269) — the horizontal card shows the home-currency conversion on
    a muted 2nd line ("≈ S$…") for ¥/₫/Rp/₩ venues (data already on `priceRangeDisplay`).
  - **Set Meal (Beta)** (#1270, #1272) — opt-in chip beside Michelin; the top venues get a
    **confirmed set-lunch/dinner price scraped from their OWN website** (`scoutVenueMulti`,
    bounded 6 + cached 24h + fail-soft; website-text only). Trimmed to **price-only** (#1272)
    — signature/popular dishes removed (they stay on the card "Try" line).
  - **French fit audit** (#1271) — completeness clean (no missing FR across 4 TMAs); shortened
    6 over-long control labels (Ouvert · Valider · Validé · Modifier ▾ · Oublier · "Voir {n}
    centres sur la carte") so pills don't truncate/distort.

- **[VERIFICATION (sandbox)]** No local web build (iCloud `.git` lock → GitHub Contents API).
  Per PR: `node --check` on changed `.js`, `esbuild` parse on changed `.jsx`, and targeted
  Node assertion replays (cuisine-vault groupings, open-hours offsets/split-shifts, exact/
  alternate + combo classification, `scoutVenueMulti` fixture, `googleMapsUrl` place links).
  Full Vitest + TMA build run in CI on each PR.

- **[STATUS]** PROD — #1242–#1272 squash-merged to `main` (v0.62.274→v0.62.301). Host
  redeploys per merge. This doc-catch-up PR carries v0.62.302.

- **[TEST]** Operator eyeball on the live TMAs: catalogue groups + dividers + Peranakan chip;
  result-card opaque-centre/glass-sides + composer lift (no chop); Moroccan → "& Nearby
  Flavours" strips, Korean+Japanese per-cuisine strips; Train/Hawker no auto-fullscreen +
  hawker pins persist on pan; JP/VN/ID/KR open/closed + "≈ S$" line; city pick "— near …";
  hawker NEA names; Set Meal (Beta) confirmed prices; 🇫🇷 control labels fit.

- **[KNOWN GAPS]**
  - **Vault snapshot STILL DUE** — `vault/v0.62.30x/` is a ~1574-file full-tree copy created
    server-side via `/admin/sync-vault` (owner-triggered); it cannot be produced through the
    Contents-API workflow. Operator to run the vault sync (last snapshot: `vault/v0.62.153`).
  - **gh-API stale-base regression (fixed)** — mid-arc, two PRs (#1262, #1263) were built from
    a stale local `origin/main` (no `git fetch` first) and their PUTs clobbered #1260's
    server tagging + client wiring; restored in #1264. Lesson saved: always `git fetch` before
    `git show origin/main`; sanity-grep a prior-PR marker after multi-file PUTs.
  - **Set Meal (Beta) coverage** — partial (PDF/JS/image menus + no-website venues can't
    confirm → no line). Gemini-vision fallback stays in the `/ztest` probe, not the live card.
  - **Dishes confidence (Track A/B)** — planned (`/ztest` ladder shipped as #1268); the card
    "Try"-line evidence-tagging + popular-vs-signature split is still queued.

- **Confirmation Gates**

  | Gate | Resolution | Date |
  |---|---|---|
  | G1 — version bump | PATCH ×31 (`0.62.274`→`0.62.301`), one per PR; this doc PR → `0.62.302`. | 24-06 '26 |
  | G2 — destructive action | _n/a_ — additive/edits; removed UI (auto-fullscreen, hawker radius clip, "Common in SG" bucket, Set-Meal signature) has rationale preserved in code/this journal (AU-7). | 24-06 '26 |
  | G3 — new decision rule | exact/alternate `matchTier` + "Nearby Flavours"; hawker pins unclipped; minZoom 7; open-hours uses currentOpeningHours + utcOffset; Set Meal = price-only. | 24-06 '26 |
  | G4 — paid external API call | Set Meal Beta scrapes venue sites (no paid API); `/ztest` vision (Gemini) is probe-only + bounded; `nearestLandmark` = Places searchNearby (already-budgeted key). | 24-06 '26 |

## Amendments

- **`doc/.serial-state.yml`** — commit 812→843, journal 752→753, build 467→498; re-anchored
  04:10 SGT 24-06-26.
- **VibeCodingRecord regenerated** — `data/prs.ndjson` + `data/pr-files.tsv` appended #1202–#1272
  (66 PRs; the journal was current to #1240, the VCR to #1201); `GEN_DATE` → 2026-06-24; ran
  `generate.mjs` (records.tsv 1249 rows, `vibe-coding-record.md`, `public/doc/vibe-journal.html`,
  `public/doc/vibe-journal.json`).
- **"Set Meal · Signature (Beta)" → "Set Meal (Beta)"** (#1272): signature/popular dishes
  removed from the feature (kept separate, on the card "Try" line).

## Deleted

_None._ Removed UI (auto-fullscreen, hawker radius clip, "Common in Singapore" bucket, the
Set-Meal signature line) has its rationale preserved in code comments + this entry.
