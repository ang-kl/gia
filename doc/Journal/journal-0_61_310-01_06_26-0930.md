(№ 511 - 01-06 '26 09:30 SGT) v0.61.310

# Journal — soleat v0.61.310 (Cuisine + Hawker TMA maps read MAP_ID env)

> Operator (01-06 '26): *"is there a DEMO_MAP_ID"* → after the audit showed Cuisine + Hawker TMAs hard-coded `'DEMO_MAP_ID'` while Transport + the static `/app/map` already read `process.env.MAP_ID` via `/maps-key`, operator picked option **A**: wire Cuisine + Hawker to the same pattern; keep `DEMO_MAP_ID` as the unset-env fallback.
>
> Result: all five TMA maps + the legacy `/app/map` now respect the operator's `MAP_ID` env (currently `db1201f307409c29…`). Custom vector styling (e.g. the Register O-9 transit-station-label hide) finally applies to Cuisine + Hawker too.
>
> Commit 524 / journal 511 / build 313, on branch `claude/v0.61.310-mapid-wiring` off `main` at v0.61.309.

## New

### [HDR] #511 | 09:30 SGT 01-06-26 | v0.61.310 — MAP_ID env wired to Cuisine + Hawker | 4 files | 1 PR | 1 build |

- **[INTENT]** Eliminate the styling inconsistency where Cuisine + Hawker TMA maps rendered against Google's public `DEMO_MAP_ID` (shared demo quota, default Google styling) while Transport + the static map honoured the operator's registered Map ID.

- **[DELTA]**

  **1. `web/cuisine/src/v2/components/MapPanel.jsx`** — mirrors Transport's v0.60.87 pattern:
  - New `mapIdRef = useRef('DEMO_MAP_ID')` declared alongside the other refs.
  - In the existing `/maps-key` fetch handler (line ~119), after the script tag is built, override `mapIdRef.current = d.mapId` when `d.mapIdSource === 'env:MAP_ID' && d.mapId`. Server side, the `/maps-key` endpoint at `index.js:16141–16147` already returns `mapId` + `mapIdSource: 'env:MAP_ID'` when `process.env.MAP_ID` is set, and `mapId: 'GIA_SANCTUARY'` + `mapIdSource: 'placeholder'` when it isn't.
  - Map constructor's `mapId: 'DEMO_MAP_ID'` (line 191) replaced with `mapId: mapIdRef.current`.

  **2. `web/hawker/src/components/HawkerMapPanel.jsx`** — same pattern. The Hawker MapPanel has its own `/maps-key` fetch (line 130) so the override slots into the existing block. Map constructor at line 179 updated.

  **3. `package.json`** — `0.61.309 → 0.61.310`.

  **4. `doc/.serial-state.yml`** — commit 523→524, build 312→313, journal 510→511; re-anchored 01-06 '26 09:30 SGT.

- **[VERIFICATION (sandbox)]**

  - `node --check` doesn't apply to `.jsx`; Vite build is the equivalent. Cuisine + Hawker TMA builds expected clean.
  - `npm test -- --run` — no test changes; pre-existing suites stay green (the TMA components are not unit-tested at the React level, but the `/maps-key` server endpoint is unchanged).

- **[STATUS]** PR-OPEN on push. Railway redeploys on merge; first Cuisine / Hawker TMA load post-deploy hits the new code path.

- **[TEST]** Operator (post-merge):
  1. Open Cuisine TMA on a deployed build → DevTools / network panel: confirm Google Maps tile requests carry the operator's Map ID (not `DEMO_MAP_ID`).
  2. Cuisine map renders with the operator's styled Map ID — should match what Transport TMA already shows (transit labels hidden per Register O-9, if that style was applied).
  3. Hawker TMA — same.
  4. Static `/app/map` unchanged (already on the right path).
  5. **Fallback test**: temporarily unset `MAP_ID` on Railway → all five TMAs fall back to `DEMO_MAP_ID` cleanly (markers still render). Re-set after verification.

- **[KNOWN GAPS]**
  - **Map style is whatever the operator configured in Google Cloud Console** for the Map ID `db1201f307409c29…`. If Cuisine + Hawker styling looks different than expected, the Map ID itself may need tweaking in Cloud Console (Maps Platform → Map Styles → POI / Transit / Stations) — not a code change.
  - **`DEMO_MAP_ID` retained as the literal fallback string** in three places: cuisine MapPanel, hawker HawkerMapPanel, transport MrtMapPanel. Required: AdvancedMarkerElement refuses to render without *some* registered Map ID, and Google's demo ID is the safest portable default for dev / accidental-unset.
  - **No client-side log of which Map ID was used** in the Cuisine / Hawker TMAs. Transport's MrtMapPanel doesn't log either. Easy follow-up if the operator wants visibility per-mount.
  - **`/maps-key` is fetched twice on cuisine + hawker pages** when both happen to mount (sharing the global `__giaMapsReady`). The override race-condition is benign: `mapIdRef.current` is set before `initMap()` is called in each component, and the map constructor reads the ref at that moment.

- **Confirmation Gates**

  | Gate | Resolution | Date |
  |---|---|---|
  | G1 — version bump | PATCH `0.61.309 → 0.61.310`. Wiring change, no API contract break. | 01-06 '26 |
  | G2 — destructive action | _n/a_. Two-line change (add ref + override) + one-line constructor edit per file, in two files. Authenticity trace: DEMO_MAP_ID is *not* removed; it's now only the fallback when `mapIdSource !== 'env:MAP_ID'`. | 01-06 '26 |
  | G3 — new decision rule | _n/a_. Restates the v0.60.87 / Transport convention. | 01-06 '26 |
  | G4 — paid external API call | _n/a_. Same `/maps-key` round-trip the components already made; just reads two additional fields from the existing response. | 01-06 '26 |

## Amendments

- **`web/cuisine/src/v2/components/MapPanel.jsx`** — gains `mapIdRef`; `/maps-key` fetch handler overrides on `mapIdSource === 'env:MAP_ID'`; map constructor reads `mapIdRef.current`.
- **`web/hawker/src/components/HawkerMapPanel.jsx`** — same three changes.
- **`package.json`** — version `0.61.309 → 0.61.310`.
- **`doc/.serial-state.yml`** — counters + last_anchor_time re-anchored 01-06 '26 09:30 SGT.

## Deleted

_None._ `DEMO_MAP_ID` literal retained as the safe fallback string in all three TMA MapPanel files.
