(№ 910 - 17-07 '26 06:14 SGT) v0.62.553

# Journal — soleat v0.62.553 (Hawker: Michelin Bib Gourmand pin + stall names on card/InfoWindow/chat · cleaning-closure feature K.I.V.)

> Operator (on the cleaning + Bib feature): "1. Put on K.I.V." (the hawker CLEANING-closure half —
> shelved) and "2. Follow house style across the app" (the Bib Gourmand card label). So: build the
> Bib Gourmand half now in house style, and hold the cleaning feature.

## New

### [HDR] #910 | 06:14 SGT 17-07-26 | v0.62.553 — Bib Gourmand hawker pins + stall names | 4 files | PR #(pending) |

- **[DELTA — data join].** `hawker-vault.js` — new `loadBibIndex()` + a join in `loadAll()` that tags
  each centre with `bibStalls` (the Michelin Bib Gourmand stall names it contains). Source is the
  existing `SG-michelin.js` `BIB_GOURMAND[]` (each entry's `address` IS the hawker-centre name). Match
  is a **deterministic number-stripped token key** (`_bibKey` = `_normaliseHawkerName` + strip block
  numbers) so "Old Airport Road Food Centre" ↔ "Blk 51 Old Airport Road", **plus a digit-consistency
  guard** (a bib address carrying a block number only tags a centre whose name contains it). Verified
  in Node: **19/122 centres tagged**, all flagships present (Old Airport Road, Chinatown Complex,
  Maxwell, Amoy, Hong Lim, Tiong Bahru, Newton, …). Deliberately NOT using `findByName` (its
  edit-distance produced false positives: Jurong West 505 → Jurong West Hawker Centre; both Redhill
  entries → one Blk 79). The number-strip + digit guard kills the Clementi 448-vs-353 false positive.

- **[DELTA — plumb-through].** `index.js` — `/api/hawker/centres-by-region` slim record gains
  `bibStalls`; `/api/hawker/save-pick` chat card gains a `✳️ Bib Gourmand · <stall(s)>` line
  (HTML-escaped, house style matching `SG-michelin.formatMichelinLine`).

- **[DELTA — TMA].**
  - `web/hawker/src/components/HawkerMapPanel.jsx` — `hawkerPinNode(isNew, number, hasBib)` gives a Bib
    centre the **Cuisine macaron-red pin `#C6282D`** PLUS a small **✳️ marker** (the operator is
    red/green colour-blind — the pin must not be colour-only; matches the app's CVD-safe convention).
    `syncMarkers` passes the flag; `buildInfoHtml` adds a `✳️ Bib Gourmand · <stall>` row.
  - `web/hawker/src/App.jsx` — `renderCentreCard` adds a `✳️ Bib Gourmand · <stall>` row (house
    style) below the stall/status chips, in every layout.

- **[DELTA — housekeeping].** `package.json` 0.62.552 → **0.62.553**; Journal #910 + serial-state
  (commit 1027→**1028**, build 652→**653**, journal 909→**910**; re-anchor 17-07 '26 06:14 SGT,
  prior 909 demoted verbatim AU-7).

- **[VERIFICATION (sandbox)].** `node --check` index.js + hawker-vault.js OK; **Bib join run in Node —
  19 centres tagged, spot-checked correct**; **Hawker vite build green** (42 modules); **full `npm test`
  3719/3719 across 145 files**. ⚠️ The pin colour/glyph + card rows are live-only (iPad) — shipping live.

- **[STATUS]** STAGED → PROD on merge (operator "make it live").

- **[TEST]** Bib join verified by a standalone Node harness (count + spot-check). No new vitest (the
  join reads committed data; DOM is build-validated).

- **[KNOWN GAPS].**
  1. 🅺🅸🆅 **Hawker CLEANING-closure feature — SHELVED per operator ("Put on K.I.V.").** Blocked anyway:
     sandbox proxy egress-blocks data.gov.sg (403) and the retired parser is below the shallow-clone
     graft, so the dataset (d_bda4baa634dd1cc7a6c7cad5f19e2d68) column names need an operator sample
     before it can be built (grey pin + closed tab/strip). Resume when un-K.I.V.'d + sample provided.
  2. 🧭 **Bib match residual** — "Redhill Market" + "Redhill Food Centre" (Bib data has no block number)
     both tag Blk 79 AND Blk 85 Redhill Lane with the same 2 stalls; a mild honest over-tag (both ARE
     Redhill Bib centres). ~19 hawker-centre bib stalls covered; standalone-restaurant bib entries
     (street/area address) are correctly excluded.
  3. 👀 iPad recheck — Bib pin reads macaron-red + ✳️, and the `✳️ Bib Gourmand · <stall>` row shows on
     the card + map InfoWindow + save-to-chat card?
  4. 🧭 Feature/Technical/Register doc entries for the Bib feature still due (log-only this pass).

## Confirmation Gates
| Gate | Status | Note |
|------|--------|------|
| G1 | ✅ | PATCH bump 0.62.552→0.62.553 (feature under the ongoing hawker arc); build + 3719 tests green |
| G2 | ➖ | no destructive action |
| G3 | ➖ | no new decision rule |
| G4 | ➖ | no external paid API call (Bib data is the committed SG-michelin.js) |
