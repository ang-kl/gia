(№ 876 - 08-07 '26 10:15 SGT) v0.62.520

# Journal — soleat v0.62.520 (Transport: CCL6 station-card data readied pre-launch, no fabrication)

> Operator: *"station exit, bus stop near by ensure the card details are ready"* + reply **"1"**
> (prep the safe half now). Made the 3 CCL6 station cards launch-ready **as far as trustworthy
> sources allow** — added each station to `data/stations.json` (OneMap coords + SMRT more-info
> link + CCL/CC30–32 badge) while **leaving exits and first/last-train empty**, because no
> authoritative exit source exists yet and we do not invent data. Bus stops already compute
> **live** server-side. Everything is future-gated, so nothing shows until the 12 Jul flip.

## New

### [HDR] #876 | 10:15 SGT 08-07-26 | v0.62.520 — CCL6 station-card data readied | 4 files | PR #(pending) |

- **[INTENT].** The operator asked to ensure the Transport-TMA **station card** for the 3 new
  CCL6 stations carries **station exits** and **nearby bus stops**, ready for the 12 Jul launch.
  Then picked **option 1**: prep the safe half now (link + badge), track exits as a gap. The goal:
  the card lights up correctly on launch day with **no fabricated data**.

- **[INVESTIGATION] (read-only, first).** Traced the card end-to-end:
  - `web/transport/src/components/MrtMapPanel.jsx` `contextHtml()` renders name / codes /
    line-status / crowd / **exits** / **bus stops** / taxis / opening-date / map-link — but
    **future stations skip context**: `if (!ctx || isFuture) return ''`.
  - `index.js` `/api/transport/station-context` computes **bus stops LIVE**
    (`transport.nearestStops(redis, lat, lng, 600, 5)` — nearest 5 within 600 m of the station
    coords, from the LTA Redis bus catalogue). **Nothing to store; self-populates on render.**
  - Exits come from `data/station-exits.json` (`exitsForStation`); the station-name **more-info
    link** + **line badge** come from `data/stations.json` (`stationMoreInfoUrl` + `lines[]`).
  - **FINDING:** all 3 CCL6 stations were **absent** from `data/stations.json` **and**
    `data/station-exits.json`.

- **[SOURCING].**
  - **OneMap** POI search for the exits of all 3 → **0 results** (stations too new to be
    geocoded at exit granularity). **OSM** empty. The LTA exit GeoJSON predates CCL6.
  - ⇒ Exit letters/coordinates have **no authoritative source yet** and were **NOT invented**
    (honours the "never invent; mark gaps" rule).
  - **SMRT** `journey.smrt.com.sg/journey/station_info/<slug>/` returns HTTP 200 for the 3 slugs
    — but the SMRT SPA returns 200 for *any* slug, so page **content is unverified**. The
    lowercase-hyphen slug convention is already documented in `_meta.schemaNotes` as
    "unverified per-station", so the added URLs match existing data hygiene.

- **[DELTA].**
  - `data/stations.json` — added 3 entries (Keppel `CC30`/`keppel`, Cantonment `CC31`/`cantonment`,
    Prince Edward Road `CC32`/`prince-edward-road`):
    - `station_name`, **OneMap** `lat`/`lng` (building-accurate, mirrored from `data/mrt-coords.json`),
      `exit_centroid: null`.
    - `lines[]` = `SMRT` / `CCL` / `Circle Line` / station_code / slug + `more_info_url`
      (`journey.smrt.com.sg/journey/station_info/<slug>/`).
    - `exits: []` and `first_last_train: []` left **empty** — pending LTA publication at opening.
    - per-entry `data_quality_notes` spelling out provenance + gaps.
    - `_meta`: `stationCount` 180→**183**, `futureStationsSkipped` 29→**26**, + a `schemaNote`
      documenting the manual pre-launch addition.
  - `package.json` — 0.62.519 → **0.62.520** (triggers the Railway redeploy).
  - `doc/Journal/journal-0_62_520-08_07_26-1015.md` — this entry.
  - `doc/.serial-state.yml` — commit 993→**994**, build 619→**620**, journal 875→**876**;
    re-anchor 08-07 '26 10:15 SGT (prior 875 anchor demoted verbatim to
    `last_anchor_source_superseded_875`).

- **[STATUS]** PROD — merges to main; the per-PR bot redeploys. The card render is **future-gated**
  (`contextHtml: if(!ctx||isFuture) return ''`), so **none of this shows to users until the 12 Jul
  flip**. Added now so the launch-flip PR **#1525** (`future`→`operational`) lights up the
  name-link + CCL badge with **no extra work** — only `exits[]` + timings remain to fill on/after
  opening day.

- **[VERIFICATION (sandbox)].** `JSON.parse(data/stations.json)` OK; `node` confirms the 3 entries
  resolve (`lat`/`lng`/`station_code`/`more_info_url`); clean **79-insertion** diff (verified
  against a byte-identical `indent=2` re-dump baseline — no incidental reformatting).

- **[TEST]** Data-file-only change (no `index.js` / handler / search / matcher / `i18n` surface) →
  outside the `gia-preflight` trigger set; **no web build** (`stations.json` is server-read via
  `fs`, not bundled into any TMA). Full `npx vitest run` in CI.

- **[KNOWN GAPS].**
  1. 🚪 **Exits** — `station-exits.json` still has no CCL6 data; **no authoritative source yet**
     (OneMap 0 / OSM 0 / LTA exit GeoJSON predates CCL6). Fill `stations.json` `exits[]` +
     `station-exits.json` **on/after 12 Jul 2026** once LTA publishes the exit GeoJSON.
  2. 🕐 **First/last train** — `first_last_train: []`; timings not published until opening.
  3. 🔗 **SMRT slug content** — `[UNVERIFIED]` per-station (SPA 200s any slug); pattern is standard,
     reconcile if a station page 404s in-app after launch.
  4. 🗓️ **Launch flip** — held PR **#1525** (`future`→`operational`), merge **12 Jul 2026**.
  5. 🗺️ **Google Maps** coord cross-check still deferred (Chrome extension offline); OneMap is
     SG-authoritative.

## Confirmation Gates
| Gate | Status | Note |
|------|--------|------|
| G1 | ✅ | JSON.parse OK; 3 entries resolve via node; clean 79-insertion diff; version 0.62.519→0.62.520 |
| G2 | ✅ | operator asked exits + nearby bus stops ready; bus stops already live server-side; added stations.json link+badge; exits left as a sourced GAP (not invented) per option 1 |
| G3 | ✅ | serial commit 993→994, build 619→620, journal 875→876; anchor 08-07 '26 10:15 SGT (prior 875 demoted verbatim) |
| G4 | ➖ | operator eyeball on the live card at the 12 Jul flip (name-link + CCL badge render; bus stops populate); fill exits + timings once LTA publishes |
