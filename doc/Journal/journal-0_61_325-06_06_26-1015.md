(№ 526 - 06-06 '26 10:15 SGT) v0.61.325

# Journal — soleat v0.61.325 (Cuisine TMA anchor-mismatch modal: name BOTH places specifically)

> Operator (05-06 '26): *"if ask if I want JP or change, you have to be specific which part of JP, it should be Fukuoka, Japan or even the street name + City + Country or current location which is street/building + city + country."*
>
> The v0.61.321 anchor-coherence modal said "Your saved spot is "{name}", but your device is somewhere else (Singapore)" with buttons "Use my current location" / `Keep "{name}"`. Too vague: it named neither place's country/city, and "somewhere else" gave the user nothing to recognise. This patch makes the modal name BOTH places specifically — the saved anchor and the device location — each as at least "City, Country" and, when resolvable, "{street/area}, {city}, {country}".
>
> Logged in the v0.61.324 journal `[KNOWN GAPS]` as the deferred next patch. Commit 539 / journal 526 / build 326, branch `claude/v0.61.325-...` off main @ v0.61.324 (sha 422aa8f).

## New

### [HDR] #526 | 10:15 SGT 06-06-26 | v0.61.325 — anchor-mismatch modal names both places | 3 files | 1 PR | 1 build |

- **[INTENT]** When the anchor/device coherence check fires, the modal must name the saved spot AND the current location specifically — at minimum "City, Country" (e.g. "Fukuoka, Japan"), upgrading the device option to "{street/building}, {city}, {country}" once a reverse-geocode lands. Labels only; no change to the apply* / gate logic.

- **[DELTA]**

  **1. `web/cuisine/src/v2/App.jsx`**

  - **New module-level helper `placeLabel({ lat, lng, name })`** (after `fabBgFg`): builds a specific human label from coords + optional name. Uses `nearestIataCity(lat,lng)` (synchronous haversine over the frozen IATA table — returns `{ city: { name, country, countryCode, ... }, distanceKm }`) for City + full Country name. Composes `"{name}, {city}, {country}"`, de-duping when `name === city`, and gracefully falls back to `"{name}, {country}"` / `"{city}, {country}"` / `name` / a coarse `"{lat}, {lng}"`. No API call.

  - **Anchor-coherence effect (~line 844):** on mismatch, resolve BOTH labels synchronously up-front — `anchorLabel = placeLabel({...anchor, name: anchor.name})` and `deviceLabel = placeLabel(userLoc)` — and store them in `anchorMismatch` alongside the existing `anchorName` / `deviceCountry`. Then fire ONE `reverseGeocode(userLoc)` (same call class the locationName banner already makes at App.jsx:388) to upgrade `deviceLabel` to a street/area-prefixed label when it lands; on failure the IATA-only "City, Country" device label is kept (never blank, never loading-forever).

  - **Modal (~line 2099):** body now reads EN `Your saved spot is {anchorLabel}, but you appear to be at {deviceLabel}.` / FR `Votre lieu enregistré est {anchorLabel}, mais vous semblez vous trouver à {deviceLabel}.` Primary button `Use {deviceLabel}` / `Utiliser {deviceLabel}`; secondary `Keep {anchorLabel}` / `Garder {anchorLabel}`. Body uses `break-words`; buttons use `break-words line-clamp-2` so a long label wraps to two lines instead of overflowing the 420px modal.

  **2. `package.json`** — `0.61.324 → 0.61.325`.

  **3. `doc/.serial-state.yml`** — commit 539 / build 326 / journal 526; re-anchored 10:15 SGT 06-06-26.

- **[VERIFICATION (sandbox)]**
  - `web/cuisine/src/v2/App.jsx` esbuild JSX parse (`esbuild --loader:.jsx=jsx --bundle=false --format=esm`): **clean**.
  - Helper shapes confirmed by reading source: `reverseGeocode({lat,lng})` → `{ name, formatted, cached? }` (`name` = neighbourhood/street/area). `nearestIataCity(lat,lng)` → `{ city: { iata, name, country, countryCode, lat, lng }, distanceKm }` (the IATA table carries a full `country` name, so no separate code→name lookup was needed; `OTHER_COUNTRIES`/`findCountry` reserved as fallback but unused).
  - Sample render — JP-anchor (anchor.name "Naka Ward", coords near Fukuoka) / SG-device: anchorLabel `Naka Ward, Fukuoka, Japan`; deviceLabel (pre-geocode) `Singapore`, (post-geocode, e.g. street "Telok Blangah") `Telok Blangah, Singapore`. Body: *"Your saved spot is Naka Ward, Fukuoka, Japan, but you appear to be at Telok Blangah, Singapore."* Buttons: `Use Telok Blangah, Singapore` / `Keep Naka Ward, Fukuoka, Japan`.
  - Local vite build = known iCloud `vite/dist` corruption (env artifact); CI TMA-build authoritative.

- **[STATUS]** PR-OPEN; auto-merge squash armed. Railway redeploys on merge.

- **[TEST]** Operator (post-merge): with a stale JP anchor on an SG device, open the Cuisine TMA → mismatch modal now reads "saved spot is Naka Ward, Fukuoka, Japan … you appear to be at {SG street}, Singapore" with matching specific buttons (no "somewhere else", no bare name).

- **[KNOWN GAPS]**
  - **Device street name depends on reverse-geocode.** If the one `/api/reverse-geocode` call fails or returns only "Singapore", the device label stays at the IATA "City, Country" form (acceptable per the operator's "at least City, Country" floor).
  - **Anchor label has no street unless the anchor's own `name` already carries one.** The anchor label uses `anchor.name` verbatim (whatever was saved) + IATA city/country; it is NOT separately reverse-geocoded (would be a second paid call for a spot the user already named). City+Country is always present.
  - **Out-of-IATA-coverage coords** (no nearby table entry) → label falls back to name-only or a coarse coord string; rare given the 348-entry table.
  - **Sibling modals (`coherenceMismatch` v0.61.274 / `regionMismatch` v0.61.276)** left unchanged — they already name SG/MY concretely and the operator hit the ANCHOR modal specifically. Not over-reached.

- **Confirmation Gates**

  | Gate | Resolution | Date |
  |---|---|---|
  | G1 — version bump | PATCH `0.61.324 → 0.61.325`. Copy/label change only. | 06-06 '26 |
  | G2 — destructive action | _n/a_. Additive helper + copy edits. | 06-06 '26 |
  | G3 — new decision rule | Mismatch modals name BOTH places via a shared `placeLabel` helper (IATA-synchronous floor, reverse-geocode upgrade for the device side). | 06-06 '26 |
  | G4 — paid external API call | ONE `/api/reverse-geocode` per modal show — same call class the locationName banner already makes; no NEW API surface. Approved as in-class. | 06-06 '26 |

## Amendments

- **`web/cuisine/src/v2/App.jsx`** — added `placeLabel` helper; anchor-coherence effect now stores `anchorLabel`/`deviceLabel` (IATA-synchronous) and upgrades the device label via one reverse-geocode; modal copy + both buttons now name both places specifically (EN + FR).
- **`package.json`** — `0.61.324 → 0.61.325`.
- **`doc/.serial-state.yml`** — counters + anchor re-set 10:15 SGT 06-06-26.

## Deleted

_None._
