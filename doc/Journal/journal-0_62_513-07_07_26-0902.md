(№ 869 - 07-07 '26 09:02 SGT) v0.62.513

# Journal — soleat v0.62.513 (Sketchbook: drop "Local food pick")

> Operator picked **C, D, E** — three Sketchbook (clipboard) TMA workstreams — to build
> in sequence. This entry is **D**: drop the "Local food pick" feature, which the operator
> judged not useful. A numbered scope question resolved it to **"Whole chip"** — remove the
> entire second header chip, not just the city-classics list inside it.

## New

### [HDR] #869 | 09:02 SGT 07-07-26 | v0.62.513 — Sketchbook: drop "Local food pick" chip | 4 files (1 deleted) | PR #(pending) |

- **[INTENT].** Operator: *"drop the 'Local food pick' in Sketchbook TMA as it isnt useful"*
  (part of the C/D/E pick). The Sketchbook header carried two folio chips — *🍜 Cuisine &
  Filter* and *📍 Pick Local Dish*. The second is the "Local food pick". Because workstream
  E will add a free-text search FAB (ported from the Cuisine TMA `TellMePanel`) that also
  searches saved-card dishes, I asked whether D should remove the whole chip or only the
  city-classics list inside it. Operator chose **"Whole chip"**.

- **[DELTA].**
  - `web/clipboard/src/App.jsx` — removed: `plate` state + the `/api/cuisine/plate` fetch
    `useEffect`; the `dishFilter` state; the `dishOptions` / `cityGuess` / `savedDishSet`
    derivations; the `dishFilter` branch inside `cardMatches()`; `dishFilter` from
    `filterActive`; and the dish-related props passed to `<Shell>` (`dishFilter`,
    `dishOptions`, `plate`, `savedDishSet`, `onSetDish`).
  - `web/clipboard/src/components/Shell.jsx` — removed: the `LocalClassicPicker` import; the
    *📍 Pick Local Dish* chip button; the `drop === 'dish'` dropdown panel (incl. the
    saved-dish-chip fallback); the vestigial `dishDraft` state; and the dish props from the
    component signature. The cuisine chip (`flex-1`) now fills the row full-width.
  - `web/clipboard/src/components/LocalClassicPicker.jsx` — **deleted** (its only consumer was
    the removed dish panel).
  - `web/clipboard/src/lib/i18n.js` — removed 4 now-orphaned keys: `chrome.pickLocal`,
    `classic.tapHint`, `classic.more`, `filter.noDishes`. Kept `filter.all` / `filter.none`
    (still used by the cuisine picker).
  - `package.json` — 0.62.512 → **0.62.513**.
  - `doc/Journal/journal-0_62_513-07_07_26-0902.md` — this entry.
  - `doc/.serial-state.yml` — commit 986→**987**, journal 868→**869**; re-anchor 09:02 SGT
    (prior 0802 anchor demoted to `last_anchor_source_superseded_0802`).

- **[VERIFICATION (sandbox)]** `npm run build` in `web/clipboard/` → **BUILD_OK** (vite, **53**
  modules — one fewer than before, `LocalClassicPicker` gone). `node --check` on
  `src/lib/i18n.js` → OK (`.jsx` parsed by the vite build). Residual-reference grep across
  `web/clipboard/src/` for every removed symbol (`LocalClassicPicker`, `dishFilter`,
  `dishOptions`, `savedDishSet`, `onSetDish`, `dishDraft`, `chrome.pickLocal`,
  `/api/cuisine/plate`, `cityGuess`) → **NONE**.

- **[STATUS]** PROD — merges to main; Sketchbook (clipboard) TMA redeploys via the per-PR bot.

- **[TEST]** No unit test for TMA presentation; the vite build is the parse gate. Full
  `npm test` runs in CI (change is TMA-only — `web/clipboard/src` — outside the gia-preflight
  trigger set: no `index.js` / bot-handler / search / matcher surface touched). 4 CI checks gate
  the PR.

- **[KNOWN GAPS].** Pre-existing dead const `cuisineOptions` in App.jsx left untouched (it was
  already unused on `main` before D — out of D's scope). **C** (Cuisine Filter → match the
  Cuisine TMA `CuisineDrawer` layout) and **E** (free-text search FAB) are the next two PRs in
  this pick. MRT stations (Keppel / Cantonment / Prince Edward Road) still due for the 12 Jul
  2026 launch. Header liquid-glass (Cuisine) still due.

## Confirmation Gates
| Gate | Status | Note |
|------|--------|------|
| G1 | ✅ | clipboard vite build green (53 modules); version 0.62.512→0.62.513 |
| G2 | ✅ | whole-chip removal per operator's numbered pick; residual-ref grep clean |
| G3 | ✅ | serial commit 986→987, journal 868→869; anchor 07-07 '26 09:02 SGT (prior demoted) |
| G4 | ➖ | UI-only removal on the operator-owned Sketchbook TMA; no data/route/bot surface touched |
