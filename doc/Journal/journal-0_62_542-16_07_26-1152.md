(№ 898 - 16-07 '26 11:52 SGT) v0.62.542

# Journal — soleat v0.62.542 (Result-pin colour-coding + non-SG ring-3 auto-frame)

> Operator, from the live screenshot IMG_3463 (Brisbane, v0.62.541): **(1)** *"Where is the third
> ring?"* and **(2)** colour-code the result pins — closed grey, Michelin red, closing-soon pink.
> Picks: ring 3 → *"keep at farthest result + auto-frame it"*; pins → the 4-state scheme below.

## New

### [HDR] #898 | 11:52 SGT 16-07-26 | v0.62.542 — pin colours + ring-3 auto-frame | 3 files | PR #(pending) |

- **[DIAGNOSIS — the "missing" third ring].** It *was* being drawn. Outside SG, ring 3 = the farthest
  result and is **uncapped** (vs SG's reach ring, capped ~6 stops), so in a spread-out "Cities" search
  it lands **beyond the zoomed-in (z13) viewport** → off-screen. Fix chosen: keep it at the farthest
  result, but auto-frame the map to show it.

- **[DELTA]** (`web/cuisine/src/v2/components/MapPanel.jsx` only — the shared ring helper is unchanged).
  - **Pin colour-coding.** NEW `pinColor(v)` + colour consts, precedence **availability/urgency before
    prestige**:
    - closed (`openNow === false`) → **grey** `#8E8E93`
    - else closing within the hour (`closingSoonMinutes` 0–60) → **pink** `#DB2777` (matches the card's
      pink "Closing in N min" tab)
    - else Michelin star / Bib (`michelinCategory`) → **red** `#C6282D` (Michelin macaron red)
    - else open → **green** `#34C759`
    `cuisinePinNode(number, background=green)` now takes a colour; the `syncMarkers` pin loop passes
    `pinColor(v)`.
  - **Ring-3 auto-frame.** On a NEW result set, for non-SG with a 3rd ring
    (`farthestResultDist(anchor, venues) > 2000`), `fitBounds` to the ring's extent (anchor ± farthest in
    the 4 cardinal directions, 40 px padding) instead of the centroid+z12 frame. SG and the
    no-ring-3 case keep the existing centroid+z12. Imported `farthestResultDist` from the shared helper.
  - `package.json` 0.62.541 → **0.62.542**; Journal #898 + serial-state (commit 1015→**1016**, build
    641→**642**, journal 897→**898**; re-anchor 16-07 '26 11:52 SGT, prior 897 demoted verbatim AU-7).

- **[VERIFICATION (sandbox)].**
  - **FULL `npm test` → 3718/3718 across 145 files** (suite excludes `web/`; `pinColor`/framing are DOM
    logic → the build is the JSX validation).
  - Cuisine vite build **green**. `web/` changed (cuisine only) → ran the cuisine build per gia-preflight
    §0; no `.js` server change; no hawker build (`MapPanel` is cuisine-only, `distance-rings` untouched).

- **[STATUS]** STAGED → PROD on merge. Front-end change; the version bump + built `public/cuisine/*`
  (git-ignored; CI rebuilds) redeploy the Cuisine TMA.

- **[KNOWN GAPS].**
  1. 🎨 **Pin-colour precedence** — grey > pink > red > green (availability/urgency first), so a closed
     or closing-soon Michelin shows grey/pink, not red. Flippable if the operator wants Michelin red to
     win — **awaiting confirm**.
  2. 🔭 **Non-SG ring-3 auto-frame** can zoom **out** a lot for a far outlier (accepted trade-off of
     "keep at farthest result").
  3. 🃏 **ResultDrawer left-peek/overlap** — separate layout follow-up.
  4. 🧩 **Consolidation PR-5 / PR-6**, PR-3 (gemini), country-pref redis-kv — deferred.
  5. 🚪 **CCL6 + Punggol Coast exit pins** — need the LTA exit file.

## Confirmation Gates
| Gate | Status | Note |
|------|--------|------|
| G1 | ✅ | PATCH bump 0.62.541→0.62.542; additive front-end change, full suite + cuisine build green |
| G2 | ➖ | no destructive action |
| G3 | ➖ | no new decision rule (feature choices captured from the operator's message) |
| G4 | ➖ | no external paid API call |
