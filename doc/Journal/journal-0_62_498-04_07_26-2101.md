(№ 852 - 04-07 '26 21:01 SGT) v0.62.498

# Journal — soleat v0.62.498 (Menu TMA: auto-detect gates on anchorLoading, not a fixed 800 ms timer) — first-launch location race

> Operator: "2 tighten" — from the Menu first-launch location trace, chose to tighten the residual 800 ms boot race (option 2).
> Read as: remove the fixed-delay guess before auto-detect; wait for the real "cache fetch settled" signal instead.

## New

### [HDR] #852 | 21:01 SGT 04-07-26 | v0.62.498 — Menu auto-detect waits on anchorLoading | 1 web file + 3 doc/meta | PR #(pending) |

- **[INTENT].** The Menu first-launch location trace confirmed there is **no browser location cache**
  (Menu keeps location in React state only; zero `localStorage`; server Redis is the SSOT via
  `/api/cuisine/user-location`) and **no boot gate**. The one residual was a **race**: the auto-detect
  `useEffect` (`web/menu/src/App.jsx` ~346) waited a **fixed 800 ms `setTimeout`** for the initial
  user-location fetch to resolve before firing `runAutoDetect`. On a slow server-cache fetch (>800 ms)
  auto-detect could run while `anchor` was still `null`; the timer could also fire before `setAnchor`
  committed. The historical first-launch "Allow Once" symptom itself was already fixed by
  **#1370 / v0.62.393** (native LocationManager) — this only closes the timing window.

- **[DELTA] (1 code file).** `web/menu/src/App.jsx`
  - Dropped the `setTimeout(…, 800)` wakeup + its `clearTimeout` cleanup.
  - Gate the effect on the existing **`anchorLoading`** state, which flips `false` in **every** terminal
    branch of the `/api/cuisine/user-location` fetch (`.then` / `.catch` / no-Telegram). Auto-detect now
    fires the instant the fetch settles — never later, and never before the anchor commits (`setAnchor`
    batches with `setAnchorLoading(false)`, so `runAutoDetect` always sees the fetched anchor).
  - Added `anchorLoading` to the dep array (was `[anchor?.lat, anchor?.lng]`).
  - **No timer added to any boot gate** — Menu has none; this only sequences auto-detect, never gates render.

- **[VERIFICATION (sandbox)]** esbuild esm JSX parse **clean** (152 kB, only react/tg.js resolve-warnings);
  full vitest; `web/menu` production build **clean** (245 kB bundle, 44 modules).

- **[STATUS]** PROD — Menu TMA redeploys on merge (Railway). G4: eyeball first-launch on a **slow**
  connection (anchor resolves correctly before auto-detect; no jump-to-default).

- **[TEST]** Full vitest + `web/menu` build via CI (behavioural timing change; no test file touched —
  the effect has no unit harness).

- **[KNOWN GAPS]** Menu still lacks Cuisine's `initialOverrides` hash-seed + splash gate — a **deliberate**
  divergence (Menu is a tile hub with no venue-load race to gate), not a missing fix. Standing: MRT CCL6
  go-live flip (task #17, 12 Jul); set-location TZ bot line (`index.js:5739` hard-coded SGT); G4 CJK/ru/de
  live eyeball; FR Michelin BBOX geo-guard.

## Confirmation Gates
| Gate | Status | Note |
|------|--------|------|
| G1 | ✅ | esbuild parse clean; single-file behavioural change; anchorLoading flips false in all fetch branches |
| G2 | ✅ | vitest + web/menu build both green in sandbox |
| G3 | ✅ | serial commit 969→970, build 608→609, journal 851→852; anchor re-set 04-07 '26 21:01 SGT |
| G4 | ⏳ | redeploy + first-launch eyeball on a slow connection (no default-location jump) |
