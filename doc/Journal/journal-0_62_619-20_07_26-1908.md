# Journal — v0.62.619 (prototype: Inter — the "in-flight-map" humanist sans — on the TMA headers)

_(№ 963 — 20-07 '26 19:08 SGT)_

## New

### [HDR] v0.62.619 — header-font prototype: load Inter and apply it to the Cuisine / Hawker / Train headers

- **[INTENT]** Operator (on a **BNE→SIN flight**, admiring the in-flight moving-map typography — a
  humanist sans in the Frutiger / Segoe-UI lineage, likely FlightPath3D IFE): *"prototype on the
  headers"* — try that pleasing font on the TMA headers. Chose **Inter** as the closest free match.

- **[DELTA — prototype]**
  1. `web/{cuisine,hawker,transport}/index.html` — added `<link rel="preconnect">` for
     googleapis/gstatic + a `<link href="…Inter:wght@400;500;600;700&display=swap">`.
  2. `web/{cuisine,hawker,transport}/src/styles.css` — appended a **plain-CSS** class
     `.font-inter { font-family: 'Inter', ui-sans-serif, system-ui, -apple-system, 'Segoe UI', Roboto, …;
     letter-spacing: -0.01em; }` (plain CSS ⇒ **not** Tailwind-purged).
  3. Applied **`font-inter`** to the header containers — Cuisine's sticky `<header>`, Transport's
     `<header>` (`headerEl`), and **all three** Hawker headers (portrait two-panel, drawer, landscape
     top-bar). **Only headers** changed; body text is untouched.

- **[VERIFICATION (sandbox)]** **cuisine + hawker + transport** `vite build` green; the Inter `<link>` is
  present in all three built `public/*/index.html`; full `npm test` **3738/3738 across 147 files**;
  **Playwright** confirmed the Hawker header's computed `font-family` = `Inter, ui-sans-serif, system-ui,
  …` (Inter first). **CANNOT** render Inter in-sandbox (Google Fonts is stubbed → falls back to
  system-ui); on the operator's real device Google Fonts loads Inter. `package.json` +
  `package-lock.json` 0.62.618 → 0.62.619; `.serial-state.yml` commit 1100→1101, build 720→721, journal
  962→963, re-anchor **20-07 '26 19:08 SGT**.

- **[TIME ANCHOR]** data.gov.sg blocked by the environment network policy (`403` `CONNECT`); anchored to
  the container system clock `2026-07-20T11:08Z` = **20-07 '26 19:08 SGT** (TF-9 terminal-runtime source).

- **[STATUS]** DRAFT → squash-merge to `main` on green CI (auto-merge SQUASH). New PR (branch restarted
  from `origin/main` `86f5cf9` = `#1626` merged). **Prototype** — for the operator to eyeball before
  committing to it app-wide.

- **[TEST]** No new unit test — a font-load + CSS class; verified via build + the computed-font-family
  probe.

- **[KNOWN GAPS / caveats]** This loads Inter from **fonts.googleapis.com at runtime** — if the soleat
  Express **CSP** blocks external styles/fonts, it silently falls back to the system stack (Segoe UI /
  system-ui — still pleasing, just not Inter). If the operator keeps it, the robust follow-up is to
  **self-host Inter** (a `woff2` in the repo + `@font-face`) and drop the CDN — for offline, privacy, and
  no external dependency. Applies to **headers only** so far; extending to body/cards is a follow-up if
  wanted. Carried: v0.62.610 `disableVerticalSwipes` + v0.62.617 desktop-fullscreen still await
  real-device eyeballs.

## Confirmation Gates
| Gate | Status | Note |
|------|--------|------|
| G1 | ✅ | PATCH bump 0.62.618 → 0.62.619; all three builds + 3738 tests green; header font-family stack verified. |
| G2 | ➖ | additive — a font `<link>` + a CSS class + a class on each header; nothing removed. |
| G3 | ➖ | no new decision rule (a UI prototype for review). |
| G4 | ➖ | no external paid API call (Google Fonts is free; runtime, not build-time). |
