(№ 709 - 13-06 '26 06:10 SGT) v0.62.53

# Journal — soleat v0.62.53 (default-on location-drift telemetry; per-ChatID isolation confirmed)

> Operator: "Errors when another ChatID uses this cuisine TMA, the location didn't set and move out.
> Is my location code changes in past 2 days … for everyone … or just my ChatID?" + "turn on more debug
> log for 'location drift' every ChatID and Cuisine TMA … turn on log by default … impact on
> performance and cost?"

> Commit 727 / journal 709 / build (no web change). PATCH 0.62.52 → 0.62.53. PR #1012 (draft).

## New

### [HDR] #709 | 06:10 SGT 13-06-26 | v0.62.53 — [LocDrift] telemetry + per-chat isolation answer | loc-drift-log.js (new) + index.js | 1 PR | 0 build |

- **[INTENT]** Make every location set + every cuisine-search anchor decision visible in the Railway
  logs (default ON) so a "location didn't set / drifted" report from ANY ChatID can be reconstructed.
  Also answer: is location state per-ChatID or global, and did the last 2 days of PR work change it
  for everyone?

- **[DELTA]**
  - **`loc-drift-log.js`** (new) — `logLocDrift(event, fields)`: ONE compact single-line JSON record
    per event. `ENABLED = process.env.LOC_DRIFT_LOG !== '0'` (default ON; set `LOC_DRIFT_LOG=0` to
    silence). `r5()` rounds coords to 5 dp (~1 m).
  - **`index.js` `/api/cuisine/set-location`** — `[LocDrift] set-location {chat,dev,from,to,moved_m,
    label,region,country,cap_m,ambient,kept}` per write (reuses the `prevLoc` already read for the
    area-change notify; `moved_m` via `location-mode.haversineMeters`).
  - **`index.js` `/api/cuisine/search`** — `[LocDrift] search-anchor {chat,center,region,radius_m,
    cap_m,cap_src}`. New `capSource` tracks the cap origin: `explicit` (persisted radiusCapM) /
    `city-default` (nearest-city radiusM) / `other-default` (40 km OTHER ceiling) / `none`.
    `cap_src='city-default'` on an OTHER free-text pick is the fingerprint of the 12-06 "Putrajaya pick,
    durian results spilled to KL" (45 km nearest-city default from a ~12 km city).
  - **`__tests__/loc-drift-log.test.js`** (new, 5).

- **[per-ChatID answer]** Location is stored **per-user**: `loc:${hashChatId(chatId)}` (+ a per-device
  `:dev:<id>` sub-key) in Redis (`location-cache.js`), written only after `verifyInitData(initData,
  TELEGRAM_BOT_TOKEN)` resolves the CALLER's own Telegram `userId` (`index.js` set-location). No
  module-level/global location variable exists in `index.js`. ⇒ one ChatID **cannot** leak into
  another; the last 2 days' changes (plate-clear = UI; autocomplete `radiusM` = soft bias) never touch
  the set-location write path or its keying. The log the operator linked
  (`log/logs.1781301427251.json`) is the **Next.js `gia-web`** landing service (`gia-web@0.1.160`,
  service `b3c56e8d`), NOT the Cuisine/Express service (`f9a39516`/`678921e6`); it only carries
  "Failed to find Server Action … older or newer deployment" (Next.js bundle version-skew, unrelated to
  cuisine location). Diagnosing another ChatID's failure needs the Express/cuisine service log.

- **[VERIFICATION (sandbox)]** `node --check` clean (index.js + loc-drift-log.js); full suite
  **3443/3443** (+5). No `web/` change → no TMA build.

- **[STATUS]** **DRAFT** — PR #1012 (rides `claude/refine-local-plan-5vh1jm`). Railway on merge.

- **[TEST]** After deploy, every Cuisine set-location + search emits a `[LocDrift]` line. Grep Railway:
  `[LocDrift] search-anchor` with `cap_src":"city-default"` + a large `radius_m` ⇒ wide-radius drift.

- **[KNOWN GAPS]**
  - `search-anchor` is emitted before the JB focus-point tightening (D784), so a JB-focus search logs
    the pre-tighten metro radius. OTHER (the drift case) is accurate. Can move later if JB drift matters.
  - Cost/perf: 2 `console.log` lines per location event; CPU negligible. Impact is Railway LOG VOLUME
    (egress/retention), not API spend — no new paid calls. `LOC_DRIFT_LOG=0` is the kill-switch.

- **Confirmation Gates**

  | Gate | Resolution | Date |
  |---|---|---|
  | G1 — version bump | PATCH `0.62.52 → 0.62.53`. | 13-06 '26 |
  | G2 — destructive action | None — additive logging only. | 13-06 '26 |
  | G3 — new decision rule | Location-drift telemetry default ON (`LOC_DRIFT_LOG=0` to disable). | 13-06 '26 |
  | G4 — paid external API call | None — `console.log` only; no new API spend. | 13-06 '26 |

### Amends self — log-collection workflow (№ 710)

**Amendment (№ 710, 13-06 '26 06:30 SGT):** Operator pasted a GitHub Actions template and asked to add
it "using RAILWAY_API_KEY, Save into /log, runs at one hour intervals". Added
**`.github/workflows/fetch-railway-logs.yml`**: hourly cron (`0 * * * *`; the template's `*/15` was
overridden per the operator's "one hour intervals"), `workflow_dispatch` for manual test, saves to
**`log/log-<ISO>.json`**, uses **`secrets.RAILWAY_API_KEY`** against the Railway public GraphQL API
(`backboard.railway.com/graphql/v2`). Two-step: resolve the Cuisine/Express service's latest
deployment (`deployments(first:1,input:{projectId,environmentId,serviceId})`) → fetch its
`deploymentLogs(startDate,endDate,limit:5000)`. IDs taken from the prod-log tags (project
`5fc70f51…`, env `cdfa2ad0…`, app service `f9a39516…`). 65-min window absorbs cron jitter. Soft-skips
if `RAILWAY_API_KEY` is unset or 0 rows; commits+pushes to main with `--rebase --autostash` under a
concurrency group. **Operator TODO:** add the `RAILWAY_API_KEY` repo secret, then run the workflow once
via `workflow_dispatch` to confirm the GraphQL schema matches (the response prints on error).
Scheduled runs only fire from `main`, so it's inert until this PR merges.

### Amends self — v0.62.54 rating rework + bug + 15-min workflow (№ 711)

**Amendment (№ 711, 13-06 '26 06:50 SGT):** Operator: relabel the rating filter + "I select 'no
ratings' but two restaurants with ratings appear." **`venue-filters.js`** `applyRatingFloor` `'unrated'`
**bugfix** — keep ONLY venues with no Google star rating; the prior logic also kept barely-reviewed
RATED venues (<10 reviews) and fell back to the FULL rated list when none were unrated, so rated places
leaked into "Unrated". Now strict (any star > 0 excluded; honest-empty, no fallback); removed the unused
`UNRATED_MODE_MAX_REVIEWS`. **`QuickFilters.jsx`** — 4 stacked options: Unrated / "New or no reviews
yet" (italic) · Any rating / "No minimum" (italic) · Good+ ≥ 3.7 · Set rating [Choose 1.0 to 5.0] field
box. **`i18n.js`** relabels + `rating.goodPlus`/`rating.setRating`; pill "No rating" → "Unrated".
**`__tests__/venue-filters.test.js`** — 3 unrated tests rewritten to the strict spec. Also
**`.github/workflows/fetch-logs.yml`** (new, every 15 min → `logs/`) replaces the hourly
`fetch-railway-logs.yml` (removed) per the operator's revised "/15 → logs/" spec. Suite **3443/3443**;
web build green. PATCH 0.62.53 → 0.62.54. Gates: G1 PATCH · G2 removed an unused const + a superseded
unmerged workflow file (additive net) · G3 Unrated = strictly no-star · G4 none.

### Amends self — `[STATUS]` post-merge (№ 712)

**Amendment (№ 712, 13-06 '26 06:55 SGT):** Operator: *"Merge."* PR **#1012** squash-merged to `main`
at **`9035933`** (all 4 checks green). STATUS: **MERGED / PROD**. Live: rating filter rework + the
"Unrated shows rated venues" bugfix (strict no-star), default-on `[LocDrift]` telemetry, and the
15-min `fetch-logs.yml` collector. **Operator TODO:** add repo secret `RAILWAY_API_KEY`, then run
*Auto Fetch Railway Logs* once via `workflow_dispatch` to confirm the Railway GraphQL schema (prints on
error); scheduled runs fire only from `main`.

### Amends self — v0.62.55 collector cadence (№ 713)

**Amendment (№ 713, 13-06 '26 06:58 SGT):** Operator: *"Change from 15 minutes to 1 hour, saved into
/log in JSON format."* `.github/workflows/fetch-logs.yml` — cron `*/15` → `0 * * * *` (hourly); output
`logs/` → `log/` (the existing log dir); fetch window 20 → 65 min. Response already JSON. No app code.
PATCH 0.62.54 → 0.62.55. PR #1013 (draft).

### Amends self — v0.62.56 Stop button + 10s rotation (№ 714)

**Amendment (№ 714, 13-06 '26 07:05 SGT):** Operator: "Have a '🛑 Stop loading' in the random loading
pop-up window … people find the loading random eateries annoying" + "Change the fun-fact to 10 second
changes." **`App.jsx`** — per-search `AbortController` (`searchAbortRef`); 🛑 Stop aborts the in-flight
NDJSON stream + drops the overlay, keeping already-streamed venues (AbortError caught as non-error);
fun-fact interval 15000 → 10000 ms. **`api.js`** — `postJsonStream`/`searchCuisine` accept optional
`signal`. **`FunFactModal.jsx`** — `onStop` prop renders the 🛑 button; rotating overlay shows it for
the abortable 'rotating' search (not the boot warm-start). **`i18n.js`** — funfact.stop / loading.stop.
Suite 3443/3443; build green. PATCH 0.62.55 → 0.62.56. **Open question to operator (task 3):** the
loading pop-up's fun facts draw ONLY from `data/fun-facts.js` (NLB SG food history, cuisine-tagged) —
the city-plate / nation-overlay 📜 DISH explanations are a SEPARATE curated set and are NOT in the
rotation. Pending operator decision on whether to merge them in.

### Amends self — v0.62.57 dish explanations in fun facts (№ 715)

**Amendment (№ 715, 13-06 '26 07:20 SGT):** Operator (AskUserQuestion: "Only cuisine/city matches")
chose to surface the 📜 dish explanations in the loading pop-up, scoped to the current cuisine/city.
**`lib/fun-facts.js`** — `dishFactsFromPlate(plate)` shapes the active plate's headliner + group dish
`note.{en,fr}` into fun-fact records (`id:'dish:<name>'`, `source:'Soleat'`, dish name baked into the
body, deduped); `pickFunFact(ctx, extraFacts)` mixes them in tagged with the live ctx so they rank as
Tier-1/2 (cuisine/city) matches. **`App.jsx`** — the rotation `pick()` feeds
`dishFactsFromPlate(cuisinePlate || arrivalPlate)` via `activePlateRef` (the plate states are declared
below the fun-fact effect → ref avoids a TDZ reference). **`__tests__/fun-facts.test.js`** +3. Suite
**3446/3446**; build green. PATCH 0.62.56 → 0.62.57. Answer to "did you include all the dish
explanations": they were a SEPARATE curated set (city-plates.js / nation-overlay.js, server-sent on the
plate) — now mixed into the wait-pop-up rotation, but only for the searched cuisine/city per the
operator's pick.

## Amendments

- **`loc-drift-log.js`** (new) · **`index.js`** (2 emit points + `capSource`) ·
  **`__tests__/loc-drift-log.test.js`** (new) · **`package.json`** `0.62.52 → 0.62.53` ·
  **`doc/.serial-state.yml`**.

## Deleted

- (none) — additive.
