(№ 709 - 13-06 '26 06:10 SGT) v0.62.53

# Journal — soleat v0.62.53 (default-on location-drift telemetry; per-ChatID isolation confirmed)

> Operator: "Errors when another ChatID uses this cuisine TMA, the location didn't set and move out.
> Is my location code changes in past 2 days … for everyone … or just my ChatID?" + "turn on more debug
> log for 'location drift' every ChatID and Cuisine TMA … turn on log by default … impact on
> performance and cost?"

> Commit 727 / journal 709 / build (no web change). PATCH 0.62.52 → 0.62.53. PR #1012 (draft).

## New

### [HDR] #709 | 06:10 SGT 13-06-26 | v0.62.53 — [LocDrift] telemetry + per-chat isolation answer | loc-drift-log.js (new) + index.js | 1 PR | 0 build |

- **[INTENT]** Make every location set + every cuisine-search anchor decision visible in the Railway
  logs (default ON) so a "location didn't set / drifted" report from ANY ChatID can be reconstructed.
  Also answer: is location state per-ChatID or global, and did the last 2 days of PR work change it
  for everyone?

- **[DELTA]**
  - **`loc-drift-log.js`** (new) — `logLocDrift(event, fields)`: ONE compact single-line JSON record
    per event. `ENABLED = process.env.LOC_DRIFT_LOG !== '0'` (default ON; set `LOC_DRIFT_LOG=0` to
    silence). `r5()` rounds coords to 5 dp (~1 m).
  - **`index.js` `/api/cuisine/set-location`** — `[LocDrift] set-location {chat,dev,from,to,moved_m,
    label,region,country,cap_m,ambient,kept}` per write (reuses the `prevLoc` already read for the
    area-change notify; `moved_m` via `location-mode.haversineMeters`).
  - **`index.js` `/api/cuisine/search`** — `[LocDrift] search-anchor {chat,center,region,radius_m,
    cap_m,cap_src}`. New `capSource` tracks the cap origin: `explicit` (persisted radiusCapM) /
    `city-default` (nearest-city radiusM) / `other-default` (40 km OTHER ceiling) / `none`.
    `cap_src='city-default'` on an OTHER free-text pick is the fingerprint of the 12-06 "Putrajaya pick,
    durian results spilled to KL" (45 km nearest-city default from a ~12 km city).
  - **`__tests__/loc-drift-log.test.js`** (new, 5).

- **[per-ChatID answer]** Location is stored **per-user**: `loc:${hashChatId(chatId)}` (+ a per-device
  `:dev:<id>` sub-key) in Redis (`location-cache.js`), written only after `verifyInitData(initData,
  TELEGRAM_BOT_TOKEN)` resolves the CALLER's own Telegram `userId` (`index.js` set-location). No
  module-level/global location variable exists in `index.js`. ⇒ one ChatID **cannot** leak into
  another; the last 2 days' changes (plate-clear = UI; autocomplete `radiusM` = soft bias) never touch
  the set-location write path or its keying. The log the operator linked
  (`log/logs.1781301427251.json`) is the **Next.js `gia-web`** landing service (`gia-web@0.1.160`,
  service `b3c56e8d`), NOT the Cuisine/Express service (`f9a39516`/`678921e6`); it only carries
  "Failed to find Server Action … older or newer deployment" (Next.js bundle version-skew, unrelated to
  cuisine location). Diagnosing another ChatID's failure needs the Express/cuisine service log.

- **[VERIFICATION (sandbox)]** `node --check` clean (index.js + loc-drift-log.js); full suite
  **3443/3443** (+5). No `web/` change → no TMA build.

- **[STATUS]** **DRAFT** — PR #1012 (rides `claude/refine-local-plan-5vh1jm`). Railway on merge.

- **[TEST]** After deploy, every Cuisine set-location + search emits a `[LocDrift]` line. Grep Railway:
  `[LocDrift] search-anchor` with `cap_src":"city-default"` + a large `radius_m` ⇒ wide-radius drift.

- **[KNOWN GAPS]**
  - `search-anchor` is emitted before the JB focus-point tightening (D784), so a JB-focus search logs
    the pre-tighten metro radius. OTHER (the drift case) is accurate. Can move later if JB drift matters.
  - Cost/perf: 2 `console.log` lines per location event; CPU negligible. Impact is Railway LOG VOLUME
    (egress/retention), not API spend — no new paid calls. `LOC_DRIFT_LOG=0` is the kill-switch.

- **Confirmation Gates**

  | Gate | Resolution | Date |
  |---|---|---|
  | G1 — version bump | PATCH `0.62.52 → 0.62.53`. | 13-06 '26 |
  | G2 — destructive action | None — additive logging only. | 13-06 '26 |
  | G3 — new decision rule | Location-drift telemetry default ON (`LOC_DRIFT_LOG=0` to disable). | 13-06 '26 |
  | G4 — paid external API call | None — `console.log` only; no new API spend. | 13-06 '26 |

### Amends self — log-collection workflow (№ 710)

**Amendment (№ 710, 13-06 '26 06:30 SGT):** Operator pasted a GitHub Actions template and asked to add
it "using RAILWAY_API_KEY, Save into /log, runs at one hour intervals". Added
**`.github/workflows/fetch-railway-logs.yml`**: hourly cron (`0 * * * *`; the template's `*/15` was
overridden per the operator's "one hour intervals"), `workflow_dispatch` for manual test, saves to
**`log/log-<ISO>.json`**, uses **`secrets.RAILWAY_API_KEY`** against the Railway public GraphQL API
(`backboard.railway.com/graphql/v2`). Two-step: resolve the Cuisine/Express service's latest
deployment (`deployments(first:1,input:{projectId,environmentId,serviceId})`) → fetch its
`deploymentLogs(startDate,endDate,limit:5000)`. IDs taken from the prod-log tags (project
`5fc70f51…`, env `cdfa2ad0…`, app service `f9a39516…`). 65-min window absorbs cron jitter. Soft-skips
if `RAILWAY_API_KEY` is unset or 0 rows; commits+pushes to main with `--rebase --autostash` under a
concurrency group. **Operator TODO:** add the `RAILWAY_API_KEY` repo secret, then run the workflow once
via `workflow_dispatch` to confirm the GraphQL schema matches (the response prints on error).
Scheduled runs only fire from `main`, so it's inert until this PR merges.

### Amends self — v0.62.54 rating rework + bug + 15-min workflow (№ 711)

**Amendment (№ 711, 13-06 '26 06:50 SGT):** Operator: relabel the rating filter + "I select 'no
ratings' but two restaurants with ratings appear." **`venue-filters.js`** `applyRatingFloor` `'unrated'`
**bugfix** — keep ONLY venues with no Google star rating; the prior logic also kept barely-reviewed
RATED venues (<10 reviews) and fell back to the FULL rated list when none were unrated, so rated places
leaked into "Unrated". Now strict (any star > 0 excluded; honest-empty, no fallback); removed the unused
`UNRATED_MODE_MAX_REVIEWS`. **`QuickFilters.jsx`** — 4 stacked options: Unrated / "New or no reviews
yet" (italic) · Any rating / "No minimum" (italic) · Good+ ≥ 3.7 · Set rating [Choose 1.0 to 5.0] field
box. **`i18n.js`** relabels + `rating.goodPlus`/`rating.setRating`; pill "No rating" → "Unrated".
**`__tests__/venue-filters.test.js`** — 3 unrated tests rewritten to the strict spec. Also
**`.github/workflows/fetch-logs.yml`** (new, every 15 min → `logs/`) replaces the hourly
`fetch-railway-logs.yml` (removed) per the operator's revised "/15 → logs/" spec. Suite **3443/3443**;
web build green. PATCH 0.62.53 → 0.62.54. Gates: G1 PATCH · G2 removed an unused const + a superseded
unmerged workflow file (additive net) · G3 Unrated = strictly no-star · G4 none.

### Amends self — `[STATUS]` post-merge (№ 712)

**Amendment (№ 712, 13-06 '26 06:55 SGT):** Operator: *"Merge."* PR **#1012** squash-merged to `main`
at **`9035933`** (all 4 checks green). STATUS: **MERGED / PROD**. Live: rating filter rework + the
"Unrated shows rated venues" bugfix (strict no-star), default-on `[LocDrift]` telemetry, and the
15-min `fetch-logs.yml` collector. **Operator TODO:** add repo secret `RAILWAY_API_KEY`, then run
*Auto Fetch Railway Logs* once via `workflow_dispatch` to confirm the Railway GraphQL schema (prints on
error); scheduled runs fire only from `main`.

### Amends self — v0.62.55 collector cadence (№ 713)

**Amendment (№ 713, 13-06 '26 06:58 SGT):** Operator: *"Change from 15 minutes to 1 hour, saved into
/log in JSON format."* `.github/workflows/fetch-logs.yml` — cron `*/15` → `0 * * * *` (hourly); output
`logs/` → `log/` (the existing log dir); fetch window 20 → 65 min. Response already JSON. No app code.
PATCH 0.62.54 → 0.62.55. PR #1013 (draft).

### Amends self — v0.62.56 Stop button + 10s rotation (№ 714)

**Amendment (№ 714, 13-06 '26 07:05 SGT):** Operator: "Have a '🛑 Stop loading' in the random loading
pop-up window … people find the loading random eateries annoying" + "Change the fun-fact to 10 second
changes." **`App.jsx`** — per-search `AbortController` (`searchAbortRef`); 🛑 Stop aborts the in-flight
NDJSON stream + drops the overlay, keeping already-streamed venues (AbortError caught as non-error);
fun-fact interval 15000 → 10000 ms. **`api.js`** — `postJsonStream`/`searchCuisine` accept optional
`signal`. **`FunFactModal.jsx`** — `onStop` prop renders the 🛑 button; rotating overlay shows it for
the abortable 'rotating' search (not the boot warm-start). **`i18n.js`** — funfact.stop / loading.stop.
Suite 3443/3443; build green. PATCH 0.62.55 → 0.62.56. **Open question to operator (task 3):** the
loading pop-up's fun facts draw ONLY from `data/fun-facts.js` (NLB SG food history, cuisine-tagged) —
the city-plate / nation-overlay 📜 DISH explanations are a SEPARATE curated set and are NOT in the
rotation. Pending operator decision on whether to merge them in.

### Amends self — v0.62.57 dish explanations in fun facts (№ 715)

**Amendment (№ 715, 13-06 '26 07:20 SGT):** Operator (AskUserQuestion: "Only cuisine/city matches")
chose to surface the 📜 dish explanations in the loading pop-up, scoped to the current cuisine/city.
**`lib/fun-facts.js`** — `dishFactsFromPlate(plate)` shapes the active plate's headliner + group dish
`note.{en,fr}` into fun-fact records (`id:'dish:<name>'`, `source:'Soleat'`, dish name baked into the
body, deduped); `pickFunFact(ctx, extraFacts)` mixes them in tagged with the live ctx so they rank as
Tier-1/2 (cuisine/city) matches. **`App.jsx`** — the rotation `pick()` feeds
`dishFactsFromPlate(cuisinePlate || arrivalPlate)` via `activePlateRef` (the plate states are declared
below the fun-fact effect → ref avoids a TDZ reference). **`__tests__/fun-facts.test.js`** +3. Suite
**3446/3446**; build green. PATCH 0.62.56 → 0.62.57. Answer to "did you include all the dish
explanations": they were a SEPARATE curated set (city-plates.js / nation-overlay.js, server-sent on the
plate) — now mixed into the wait-pop-up rotation, but only for the searched cuisine/city per the
operator's pick.

### Amends self — v0.62.58 rating panel header + 2×2 (№ 716)

**Amendment (№ 716, 13-06 '26 07:10 SGT):** Operator (post-#1013 merge): combine Unrated + Any rating on
one row; Good+ ≥ 3.7 + Set rating [Choose 1.0 to 5.0] on one row; add a header "Refine Google Rating".
**`QuickFilters.jsx`** — header line + two `grid-cols-2` rows; the Set-rating cell stacks its label over
the 1.0–5.0 field so the box keeps full half-cell width. **`i18n.js`** — `rating.refineHeader`. Suite
**3446/3446**; build green. PATCH 0.62.57 → 0.62.58. PR #1014 (draft). Also confirmed to operator: only
#1013 was open (no stale PRs); the log workflow auto-runs hourly once on main + `RAILWAY_API_KEY` set —
no manual run needed for routine logging (one `workflow_dispatch` advised to verify the Railway query).

### Amends self — `[STATUS]` #1014 merge (№ 717)

**Amendment (№ 717, 13-06 '26 07:12 SGT):** Operator: *"Merge."* PR **#1014** squash-merged to `main`
at **`29360e0`** (all 4 checks green). STATUS: **MERGED / PROD**. The "Refine Google Rating" header +
2×2 rating layout (Unrated | Any rating ; Good+ ≥ 3.7 | Set rating [Choose 1.0 to 5.0]) is live.

### Amends self — v0.62.59 actions Node-24 bump (№ 718)

**Amendment (№ 718, 13-06 '26 07:30 SGT):** Operator: *"Bump / Push."* Run #1 of *Auto Fetch Railway
Logs* surfaced GitHub's Node-20 deprecation warning (actions forced to Node 24 from 16-06 '26). Bumped
`actions/checkout@v4 → v5` (6×) and `actions/setup-node@v4 → v5` (4×) across `ci.yml`,
`durian-variance.yml`, `fetch-logs.yml`; `cron.yml` uses no actions; third-party
`stefanzweifel/git-auto-commit-action@v5` left as-is (not flagged). Also recorded: run #1 was a green
SOFT-SKIP — job log shows `RAILWAY_API_KEY:` empty → "not set — skipping"; no Railway call was made,
no log committed. Operator still to add the `RAILWAY_API_KEY` secret + re-run. PATCH 0.62.58 → 0.62.59.

### Amends self — v0.62.60 rating-panel polish + open/idle reminder (№ 719)

**Amendment (№ 719, 13-06 '26 07:35 SGT):** Operator (panel screenshot): vertical-align the Good+
radio; shorten "Set rating" → **"Set as"** with a one-line `[1.0 to 5.0]` field (goal: smaller box);
and "whenever I return from idle or runs the Cuisine, reminds user that 'Google Rating for Eateries
are set Good+ ≥ 3.7⭐'". **`QuickFilters.jsx`** — `RatingOption` centres dot+label when hint-less
(hinted cells keep top-align); the Set-as cell is back to ONE inline row (dot + "Set as" + field).
**`i18n.js`** — `rating.setRating` "Set as"/"Définir", `rating.customHint` "1.0 to 5.0", new
`rating.reminder` ("⭐ Google Rating for eateries is set to '{label}'"). **`App.jsx`** —
`ratingReminder` bottom toast (mirrors locMoveNote, bottom-14 so they never overlap, 7 s auto-dismiss,
tap-dismiss): fired on app open (after the rating-pref fetch; failed fetch announces the effective
'3.7' default) and on return from ≥2 min hidden (visibilitychange; the floor keeps quick app-flips
quiet); `ratingReminderLabel()` renders '3.7' → "Good+ ≥ 3.7 ⭐", 'unrated'/'any' → pill labels,
other floors → "≥ X ⭐". Suite **3446/3446**; build green. PATCH 0.62.59 → 0.62.60. Rides PR #1015.

### Amends self — v0.62.61 amended rating pop-ups + session-scoped rating (№ 720)

**Amendment (№ 720, 13-06 '26 07:50 SGT):** Operator amended the v0.62.60 pop-up copy into THREE
messages and (AskUserQuestion, **G3**) confirmed idle-return ACTUALLY RESETS the rating — a custom
rating now lasts for the session only. **Cuisine `App.jsx`** — every fresh entry + every ≥2 min
idle-return resets the pref to '3.7' (persisting via saveRatingPref only when the saved value
differed, so chat /rating agrees); pop-ups: entry/idle `reset` ("Rating reset: Good+ ≥ 3.7⭐ /
Showing eateries with generally good Google ratings."), first device run `intro` ("Rating set to
Good+ ≥ 3.7⭐ / Change it anytime if you want more or fewer eateries.", localStorage
`gia.rating.introSeen`), after Save `saved` ("Search rating updated"); dropped the v0.62.60
'{label}' reminder + helper. **`i18n.js`** — 5 new keys replace `rating.reminder`. **Menu TMA**
(`App.jsx`, `api.js` + `i18n.js`) — same ≥2 min idle-return reset (POST `/api/cuisine/rating-pref`
'3.7', fire-and-forget) + the same reset pop-up; new exported `saveRatingPref`. Suite **3446/3446**;
cuisine + menu builds green. PATCH 0.62.60 → 0.62.61. Rides PR #1015.

### Amends self — `[STATUS]` #1015 merge (№ 721)

**Amendment (№ 721, 13-06 '26 08:00 SGT):** Operator: *"merge."* PR **#1015** squash-merged to `main`
at **`3c13d6d`** (all 4 checks green). STATUS: **MERGED / PROD**. Live: actions→v5 (Node-24 ready),
rating-panel polish ("Set as" one-line + Good+ centring), and the amended session-scoped rating
pop-ups — entry/idle-return RESET (Cuisine + Menu), Save toast, first-run intro. Outstanding (operator
side): add `RAILWAY_API_KEY` secret + re-run *Auto Fetch Railway Logs* (run #1 soft-skipped).

### Amends self — v0.62.62 log-fetch → dedicated branch (№ 722)

**Amendment (№ 722, 13-06 '26 09:05 SGT):** Operator added the `RAILWAY_API_KEY` secret and re-ran
*Auto Fetch Railway Logs* — the Railway GraphQL query WORKED (fetched + created
`log/log-2026-06-13T01-01-53Z.json`), but the push to **`main` was rejected** by branch protection
(`GH006: Protected branch update failed — 4 of 4 required status checks expected`). Fix: the
"Commit and push logs" step now targets a dedicated, unprotected **`railway-logs`** branch (untracked
log file survives the branch switch; `checkout -B` from `origin/railway-logs` or fork off main on the
first run; rebase-then-push). main stays clean; CI never blocks the hourly commit. The Railway schema +
token are validated end-to-end. PATCH 0.62.61 → 0.62.62.

### Amends self — `[STATUS]` #1016 merge (№ 723)

**Amendment (№ 723, 13-06 '26 09:06 SGT):** Operator: *"Go."* PR **#1016** squash-merged to `main`
at **`221537e`** (all 4 checks green). STATUS: **MERGED / PROD**. The hourly *Auto Fetch Railway Logs*
now commits `log/log-*.json` to the dedicated **`railway-logs`** branch. One more run is needed to
create/populate that branch (the integration can't trigger it — 403); operator to re-run or wait for
the top-of-hour cron, then drift analysis reads `[LocDrift]` lines off `railway-logs`.

### Amends self — v0.62.63 reset copy in boot overlay + Stop on all pop-ups (№ 724)

**Amendment (№ 724, 13-06 '26 09:12 SGT):** Operator: on relaunch show "Rating reset: Good+ ≥ 3.7⭐ /
Showing eateries with generally good Google ratings." on the NEXT LINE under "Please wait while loading
random eateries…"; and put the 🛑 Stop pill on EVERY loading pop-up (small font, 0.5px red/yellow
border). **`App.jsx`** — the initial loading overlay's boot branch now renders the reset/intro copy
below the "Please wait…" line (kind!=='saved'); the bottom rating toast is suppressed while that overlay
shows it (no dup). The 🛑 Stop button now renders for ALL loadingReasons (was 'rotating' only), restyled
`rounded-full border-[0.5px] border-amber-500 text-[10px]`; on the boot warm-start (no AbortController)
it just dismisses the wait. **`FunFactModal.jsx`** — same pill restyle. Answer to "why a Stop button":
it lets users bail out of ANY wait; now consistent across the fun-fact modal, the rotating overlay, and
the boot overlay. Suite **3446/3446**; build green. PATCH 0.62.62 → 0.62.63.

### Amends self — `[STATUS]` #1017 merge (№ 725)

**Amendment (№ 725, 13-06 '26 09:17 SGT):** Operator: *"Merge."* PR **#1017** squash-merged to `main`
at **`e3972b1`** (all 4 checks green). STATUS: **MERGED / PROD**. Live: rating-reset/intro copy inside
the boot loading overlay (below "Please wait while loading random eateries…"), and the 🛑 Stop pill
(small font, 0.5px amber border) on every loading pop-up. Still pending operator: one more
*Auto Fetch Railway Logs* run to create the `railway-logs` branch, then `[LocDrift]` drift analysis.

### Amends self — v0.62.64 log fetch merges recent deployments (№ 726)

**Amendment (№ 726, 13-06 '26 09:25 SGT):** Operator: *"Done"* (re-ran the workflow). Runs #4/#5
succeeded but the commit step was SKIPPED — job log: `Latest deployment: bb7f2ca6… / Fetched 0 log
rows`. ROOT CAUSE: `deploymentLogs` is scoped to ONE deployment, and the merge burst (#1015–#1017)
triggered fresh Railway redeploys, so the LATEST deployment is seconds old and has no logs in the
65-min-back window — they belong to the PREVIOUS deployment. FIX (`fetch-logs.yml`): resolve the latest
**5** deployments (`deployments(first:5,…)`) and fetch+merge each one's `deploymentLogs` for the window
(jq concat + sort_by timestamp), wrapped as `{since,until,count,logs}`. Reuses the proven deploymentLogs
query (no schema risk). Verified jq merge/sort locally; YAML valid. PATCH 0.62.63 → 0.62.64. Next run
should land a non-empty file on `railway-logs`.

### Amends self — `[STATUS]` #1018 merge (№ 727)

**Amendment (№ 727, 13-06 '26 09:26 SGT):** Operator: *"Merge."* PR **#1018** squash-merged to `main`
at **`240aca2`** (all 4 checks green). STATUS: **MERGED / PROD**. The log fetch now merges the latest 5
deployments' logs, so a fresh-redeploy empty window no longer yields rows=0. Next *Auto Fetch Railway
Logs* run should commit a non-empty file to `railway-logs`; then `[LocDrift]` drift analysis.

### Amends self — v0.62.65 logs back to main/log/ (№ 728)

**Amendment (№ 728, 13-06 '26 09:52 SGT):** Operator (looking at `main/log/`, empty) + AskUserQuestion:
*"Put on main/log/ (needs bot bypass)."* Reverted the "Commit and push logs" step from the dedicated
`railway-logs` branch (v0.62.62) back to **`main`** (`git add log/… ; commit ; pull --rebase ; push
origin main`), KEEPING the v0.62.64 multi-deployment merge fetch. **Operator TODO (one-time):** add
`github-actions[bot]` to the branch ruleset's BYPASS list (Settings → Rules/Branches → allow Actions to
bypass required status checks) — otherwise the bot push is rejected (`GH006`). YAML valid. PATCH
0.62.64 → 0.62.65. Once the bypass is set + one run fires, `log/log-*.json` lands on `main`; then
`[LocDrift]` drift analysis.

### Amends self — v0.62.66 Stop curating + pop-up polish (№ 729)

**Amendment (№ 729, 13-06 '26 09:55 SGT):** Operator: "Stop loading" → **"Stop curating"** (same 🛑),
on its own LAST line flush RIGHT; the pop-up "looks 90s". Also answered: the 10 s timer is the fun-fact
ROTATION cadence only — it does NOT touch the search/result/location, so it is not a drift cause.
**`i18n.js`** — `funfact.stop`/`loading.stop` → "🛑 Stop curating" / "🛑 Arrêter la sélection".
**`App.jsx`** loading overlay — modernised (blurred `bg-black/30` scrim, `rounded-3xl` card,
`ring-1 ring-tg-accent/10`, softer `shadow-2xl`, roomier padding, max-w-320); the 🛑 pill moved to its
own row `flex justify-end` (last line, right-flush). **`FunFactModal.jsx`** — same pill →
`justify-end`. Suite **3446/3446**; build green. PATCH 0.62.65 → 0.62.66. Rides PR #1019.

### Amends self — v0.62.67 back to railway-logs branch (№ 730)

**Amendment (№ 730, 13-06 '26 10:05 SGT):** Operator hit the bot-bypass friction on protected `main`
(two screenshots were both NEW-rule screens, neither the bypass) and (AskUserQuestion) chose **"Switch
to railway-logs branch"**. Reverted the "Commit and push logs" step from `main` (v0.62.65) BACK to the
unprotected **`railway-logs`** branch — zero GitHub settings needed. Keeps the v0.62.64 multi-deployment
merge fetch + v0.62.66 "Stop curating" UI. YAML valid. PATCH 0.62.66 → 0.62.67. Once a run fires it
creates `railway-logs` with the log file; browse at `…/tree/railway-logs/log`; `[LocDrift]` analysis
reads from there.

### Amends self — `[STATUS]` #1019 merge (№ 731)

**Amendment (№ 731, 13-06 '26 10:09 SGT):** Operator: *"merge"* (after confirming "anything to change
here" → nothing; railway-logs needs no branch-protection rule). PR **#1019** squash-merged to `main` at
**`d0c2db6`** (all 4 checks green). STATUS: **MERGED / PROD**. Live: "Stop curating" pill (last line,
right-flush) + modernised loading pop-up; log fetch commits to the unprotected `railway-logs` branch
(no settings). NEXT: re-run *Auto Fetch Railway Logs* → creates `railway-logs` + commits the file →
`[LocDrift]` drift analysis off that branch.

## Amendments

- **`loc-drift-log.js`** (new) · **`index.js`** (2 emit points + `capSource`) ·
  **`__tests__/loc-drift-log.test.js`** (new) · **`package.json`** `0.62.52 → 0.62.53` ·
  **`doc/.serial-state.yml`**.

## Deleted

- (none) — additive.
