(№ 709 - 13-06 '26 06:10 SGT) v0.62.53

# Journal — soleat v0.62.53 (default-on location-drift telemetry; per-ChatID isolation confirmed)

> Operator: "Errors when another ChatID uses this cuisine TMA, the location didn't set and move out.
> Is my location code changes in past 2 days … for everyone … or just my ChatID?" + "turn on more debug
> log for 'location drift' every ChatID and Cuisine TMA … turn on log by default … impact on
> performance and cost?"

> Commit 727 / journal 709 / build (no web change). PATCH 0.62.52 → 0.62.53. PR #1012 (draft).

## New

### [HDR] #709 | 06:10 SGT 13-06-26 | v0.62.53 — [LocDrift] telemetry + per-chat isolation answer | loc-drift-log.js (new) + index.js | 1 PR | 0 build |

- **[INTENT]** Make every location set + every cuisine-search anchor decision visible in the Railway
  logs (default ON) so a "location didn't set / drifted" report from ANY ChatID can be reconstructed.
  Also answer: is location state per-ChatID or global, and did the last 2 days of PR work change it
  for everyone?

- **[DELTA]**
  - **`loc-drift-log.js`** (new) — `logLocDrift(event, fields)`: ONE compact single-line JSON record
    per event. `ENABLED = process.env.LOC_DRIFT_LOG !== '0'` (default ON; set `LOC_DRIFT_LOG=0` to
    silence). `r5()` rounds coords to 5 dp (~1 m).
  - **`index.js` `/api/cuisine/set-location`** — `[LocDrift] set-location {chat,dev,from,to,moved_m,
    label,region,country,cap_m,ambient,kept}` per write (reuses the `prevLoc` already read for the
    area-change notify; `moved_m` via `location-mode.haversineMeters`).
  - **`index.js` `/api/cuisine/search`** — `[LocDrift] search-anchor {chat,center,region,radius_m,
    cap_m,cap_src}`. New `capSource` tracks the cap origin: `explicit` (persisted radiusCapM) /
    `city-default` (nearest-city radiusM) / `other-default` (40 km OTHER ceiling) / `none`.
    `cap_src='city-default'` on an OTHER free-text pick is the fingerprint of the 12-06 "Putrajaya pick,
    durian results spilled to KL" (45 km nearest-city default from a ~12 km city).
  - **`__tests__/loc-drift-log.test.js`** (new, 5).

- **[per-ChatID answer]** Location is stored **per-user**: `loc:${hashChatId(chatId)}` (+ a per-device
  `:dev:<id>` sub-key) in Redis (`location-cache.js`), written only after `verifyInitData(initData,
  TELEGRAM_BOT_TOKEN)` resolves the CALLER's own Telegram `userId` (`index.js` set-location). No
  module-level/global location variable exists in `index.js`. ⇒ one ChatID **cannot** leak into
  another; the last 2 days' changes (plate-clear = UI; autocomplete `radiusM` = soft bias) never touch
  the set-location write path or its keying. The log the operator linked
  (`log/logs.1781301427251.json`) is the **Next.js `gia-web`** landing service (`gia-web@0.1.160`,
  service `b3c56e8d`), NOT the Cuisine/Express service (`f9a39516`/`678921e6`); it only carries
  "Failed to find Server Action … older or newer deployment" (Next.js bundle version-skew, unrelated to
  cuisine location). Diagnosing another ChatID's failure needs the Express/cuisine service log.

- **[VERIFICATION (sandbox)]** `node --check` clean (index.js + loc-drift-log.js); full suite
  **3443/3443** (+5). No `web/` change → no TMA build.

- **[STATUS]** **DRAFT** — PR #1012 (rides `claude/refine-local-plan-5vh1jm`). Railway on merge.

- **[TEST]** After deploy, every Cuisine set-location + search emits a `[LocDrift]` line. Grep Railway:
  `[LocDrift] search-anchor` with `cap_src":"city-default"` + a large `radius_m` ⇒ wide-radius drift.

- **[KNOWN GAPS]**
  - `search-anchor` is emitted before the JB focus-point tightening (D784), so a JB-focus search logs
    the pre-tighten metro radius. OTHER (the drift case) is accurate. Can move later if JB drift matters.
  - Cost/perf: 2 `console.log` lines per location event; CPU negligible. Impact is Railway LOG VOLUME
    (egress/retention), not API spend — no new paid calls. `LOC_DRIFT_LOG=0` is the kill-switch.

- **Confirmation Gates**

  | Gate | Resolution | Date |
  |---|---|---|
  | G1 — version bump | PATCH `0.62.52 → 0.62.53`. | 13-06 '26 |
  | G2 — destructive action | None — additive logging only. | 13-06 '26 |
  | G3 — new decision rule | Location-drift telemetry default ON (`LOC_DRIFT_LOG=0` to disable). | 13-06 '26 |
  | G4 — paid external API call | None — `console.log` only; no new API spend. | 13-06 '26 |

### Amends self — log-collection workflow (№ 710)

**Amendment (№ 710, 13-06 '26 06:30 SGT):** Operator pasted a GitHub Actions template and asked to add
it "using RAILWAY_API_KEY, Save into /log, runs at one hour intervals". Added
**`.github/workflows/fetch-railway-logs.yml`**: hourly cron (`0 * * * *`; the template's `*/15` was
overridden per the operator's "one hour intervals"), `workflow_dispatch` for manual test, saves to
**`log/log-<ISO>.json`**, uses **`secrets.RAILWAY_API_KEY`** against the Railway public GraphQL API
(`backboard.railway.com/graphql/v2`). Two-step: resolve the Cuisine/Express service's latest
deployment (`deployments(first:1,input:{projectId,environmentId,serviceId})`) → fetch its
`deploymentLogs(startDate,endDate,limit:5000)`. IDs taken from the prod-log tags (project
`5fc70f51…`, env `cdfa2ad0…`, app service `f9a39516…`). 65-min window absorbs cron jitter. Soft-skips
if `RAILWAY_API_KEY` is unset or 0 rows; commits+pushes to main with `--rebase --autostash` under a
concurrency group. **Operator TODO:** add the `RAILWAY_API_KEY` repo secret, then run the workflow once
via `workflow_dispatch` to confirm the GraphQL schema matches (the response prints on error).
Scheduled runs only fire from `main`, so it's inert until this PR merges.

### Amends self — v0.62.54 rating rework + bug + 15-min workflow (№ 711)

**Amendment (№ 711, 13-06 '26 06:50 SGT):** Operator: relabel the rating filter + "I select 'no
ratings' but two restaurants with ratings appear." **`venue-filters.js`** `applyRatingFloor` `'unrated'`
**bugfix** — keep ONLY venues with no Google star rating; the prior logic also kept barely-reviewed
RATED venues (<10 reviews) and fell back to the FULL rated list when none were unrated, so rated places
leaked into "Unrated". Now strict (any star > 0 excluded; honest-empty, no fallback); removed the unused
`UNRATED_MODE_MAX_REVIEWS`. **`QuickFilters.jsx`** — 4 stacked options: Unrated / "New or no reviews
yet" (italic) · Any rating / "No minimum" (italic) · Good+ ≥ 3.7 · Set rating [Choose 1.0 to 5.0] field
box. **`i18n.js`** relabels + `rating.goodPlus`/`rating.setRating`; pill "No rating" → "Unrated".
**`__tests__/venue-filters.test.js`** — 3 unrated tests rewritten to the strict spec. Also
**`.github/workflows/fetch-logs.yml`** (new, every 15 min → `logs/`) replaces the hourly
`fetch-railway-logs.yml` (removed) per the operator's revised "/15 → logs/" spec. Suite **3443/3443**;
web build green. PATCH 0.62.53 → 0.62.54. Gates: G1 PATCH · G2 removed an unused const + a superseded
unmerged workflow file (additive net) · G3 Unrated = strictly no-star · G4 none.

### Amends self — `[STATUS]` post-merge (№ 712)

**Amendment (№ 712, 13-06 '26 06:55 SGT):** Operator: *"Merge."* PR **#1012** squash-merged to `main`
at **`9035933`** (all 4 checks green). STATUS: **MERGED / PROD**. Live: rating filter rework + the
"Unrated shows rated venues" bugfix (strict no-star), default-on `[LocDrift]` telemetry, and the
15-min `fetch-logs.yml` collector. **Operator TODO:** add repo secret `RAILWAY_API_KEY`, then run
*Auto Fetch Railway Logs* once via `workflow_dispatch` to confirm the Railway GraphQL schema (prints on
error); scheduled runs fire only from `main`.

### Amends self — v0.62.55 collector cadence (№ 713)

**Amendment (№ 713, 13-06 '26 06:58 SGT):** Operator: *"Change from 15 minutes to 1 hour, saved into
/log in JSON format."* `.github/workflows/fetch-logs.yml` — cron `*/15` → `0 * * * *` (hourly); output
`logs/` → `log/` (the existing log dir); fetch window 20 → 65 min. Response already JSON. No app code.
PATCH 0.62.54 → 0.62.55. PR #1013 (draft).

### Amends self — v0.62.56 Stop button + 10s rotation (№ 714)

**Amendment (№ 714, 13-06 '26 07:05 SGT):** Operator: "Have a '🛑 Stop loading' in the random loading
pop-up window … people find the loading random eateries annoying" + "Change the fun-fact to 10 second
changes." **`App.jsx`** — per-search `AbortController` (`searchAbortRef`); 🛑 Stop aborts the in-flight
NDJSON stream + drops the overlay, keeping already-streamed venues (AbortError caught as non-error);
fun-fact interval 15000 → 10000 ms. **`api.js`** — `postJsonStream`/`searchCuisine` accept optional
`signal`. **`FunFactModal.jsx`** — `onStop` prop renders the 🛑 button; rotating overlay shows it for
the abortable 'rotating' search (not the boot warm-start). **`i18n.js`** — funfact.stop / loading.stop.
Suite 3443/3443; build green. PATCH 0.62.55 → 0.62.56. **Open question to operator (task 3):** the
loading pop-up's fun facts draw ONLY from `data/fun-facts.js` (NLB SG food history, cuisine-tagged) —
the city-plate / nation-overlay 📜 DISH explanations are a SEPARATE curated set and are NOT in the
rotation. Pending operator decision on whether to merge them in.

### Amends self — v0.62.57 dish explanations in fun facts (№ 715)

**Amendment (№ 715, 13-06 '26 07:20 SGT):** Operator (AskUserQuestion: "Only cuisine/city matches")
chose to surface the 📜 dish explanations in the loading pop-up, scoped to the current cuisine/city.
**`lib/fun-facts.js`** — `dishFactsFromPlate(plate)` shapes the active plate's headliner + group dish
`note.{en,fr}` into fun-fact records (`id:'dish:<name>'`, `source:'Soleat'`, dish name baked into the
body, deduped); `pickFunFact(ctx, extraFacts)` mixes them in tagged with the live ctx so they rank as
Tier-1/2 (cuisine/city) matches. **`App.jsx`** — the rotation `pick()` feeds
`dishFactsFromPlate(cuisinePlate || arrivalPlate)` via `activePlateRef` (the plate states are declared
below the fun-fact effect → ref avoids a TDZ reference). **`__tests__/fun-facts.test.js`** +3. Suite
**3446/3446**; build green. PATCH 0.62.56 → 0.62.57. Answer to "did you include all the dish
explanations": they were a SEPARATE curated set (city-plates.js / nation-overlay.js, server-sent on the
plate) — now mixed into the wait-pop-up rotation, but only for the searched cuisine/city per the
operator's pick.

### Amends self — v0.62.58 rating panel header + 2×2 (№ 716)

**Amendment (№ 716, 13-06 '26 07:10 SGT):** Operator (post-#1013 merge): combine Unrated + Any rating on
one row; Good+ ≥ 3.7 + Set rating [Choose 1.0 to 5.0] on one row; add a header "Refine Google Rating".
**`QuickFilters.jsx`** — header line + two `grid-cols-2` rows; the Set-rating cell stacks its label over
the 1.0–5.0 field so the box keeps full half-cell width. **`i18n.js`** — `rating.refineHeader`. Suite
**3446/3446**; build green. PATCH 0.62.57 → 0.62.58. PR #1014 (draft). Also confirmed to operator: only
#1013 was open (no stale PRs); the log workflow auto-runs hourly once on main + `RAILWAY_API_KEY` set —
no manual run needed for routine logging (one `workflow_dispatch` advised to verify the Railway query).

### Amends self — `[STATUS]` #1014 merge (№ 717)

**Amendment (№ 717, 13-06 '26 07:12 SGT):** Operator: *"Merge."* PR **#1014** squash-merged to `main`
at **`29360e0`** (all 4 checks green). STATUS: **MERGED / PROD**. The "Refine Google Rating" header +
2×2 rating layout (Unrated | Any rating ; Good+ ≥ 3.7 | Set rating [Choose 1.0 to 5.0]) is live.

### Amends self — v0.62.59 actions Node-24 bump (№ 718)

**Amendment (№ 718, 13-06 '26 07:30 SGT):** Operator: *"Bump / Push."* Run #1 of *Auto Fetch Railway
Logs* surfaced GitHub's Node-20 deprecation warning (actions forced to Node 24 from 16-06 '26). Bumped
`actions/checkout@v4 → v5` (6×) and `actions/setup-node@v4 → v5` (4×) across `ci.yml`,
`durian-variance.yml`, `fetch-logs.yml`; `cron.yml` uses no actions; third-party
`stefanzweifel/git-auto-commit-action@v5` left as-is (not flagged). Also recorded: run #1 was a green
SOFT-SKIP — job log shows `RAILWAY_API_KEY:` empty → "not set — skipping"; no Railway call was made,
no log committed. Operator still to add the `RAILWAY_API_KEY` secret + re-run. PATCH 0.62.58 → 0.62.59.

### Amends self — v0.62.60 rating-panel polish + open/idle reminder (№ 719)

**Amendment (№ 719, 13-06 '26 07:35 SGT):** Operator (panel screenshot): vertical-align the Good+
radio; shorten "Set rating" → **"Set as"** with a one-line `[1.0 to 5.0]` field (goal: smaller box);
and "whenever I return from idle or runs the Cuisine, reminds user that 'Google Rating for Eateries
are set Good+ ≥ 3.7⭐'". **`QuickFilters.jsx`** — `RatingOption` centres dot+label when hint-less
(hinted cells keep top-align); the Set-as cell is back to ONE inline row (dot + "Set as" + field).
**`i18n.js`** — `rating.setRating` "Set as"/"Définir", `rating.customHint` "1.0 to 5.0", new
`rating.reminder` ("⭐ Google Rating for eateries is set to '{label}'"). **`App.jsx`** —
`ratingReminder` bottom toast (mirrors locMoveNote, bottom-14 so they never overlap, 7 s auto-dismiss,
tap-dismiss): fired on app open (after the rating-pref fetch; failed fetch announces the effective
'3.7' default) and on return from ≥2 min hidden (visibilitychange; the floor keeps quick app-flips
quiet); `ratingReminderLabel()` renders '3.7' → "Good+ ≥ 3.7 ⭐", 'unrated'/'any' → pill labels,
other floors → "≥ X ⭐". Suite **3446/3446**; build green. PATCH 0.62.59 → 0.62.60. Rides PR #1015.

### Amends self — v0.62.61 amended rating pop-ups + session-scoped rating (№ 720)

**Amendment (№ 720, 13-06 '26 07:50 SGT):** Operator amended the v0.62.60 pop-up copy into THREE
messages and (AskUserQuestion, **G3**) confirmed idle-return ACTUALLY RESETS the rating — a custom
rating now lasts for the session only. **Cuisine `App.jsx`** — every fresh entry + every ≥2 min
idle-return resets the pref to '3.7' (persisting via saveRatingPref only when the saved value
differed, so chat /rating agrees); pop-ups: entry/idle `reset` ("Rating reset: Good+ ≥ 3.7⭐ /
Showing eateries with generally good Google ratings."), first device run `intro` ("Rating set to
Good+ ≥ 3.7⭐ / Change it anytime if you want more or fewer eateries.", localStorage
`gia.rating.introSeen`), after Save `saved` ("Search rating updated"); dropped the v0.62.60
'{label}' reminder + helper. **`i18n.js`** — 5 new keys replace `rating.reminder`. **Menu TMA**
(`App.jsx`, `api.js` + `i18n.js`) — same ≥2 min idle-return reset (POST `/api/cuisine/rating-pref`
'3.7', fire-and-forget) + the same reset pop-up; new exported `saveRatingPref`. Suite **3446/3446**;
cuisine + menu builds green. PATCH 0.62.60 → 0.62.61. Rides PR #1015.

### Amends self — `[STATUS]` #1015 merge (№ 721)

**Amendment (№ 721, 13-06 '26 08:00 SGT):** Operator: *"merge."* PR **#1015** squash-merged to `main`
at **`3c13d6d`** (all 4 checks green). STATUS: **MERGED / PROD**. Live: actions→v5 (Node-24 ready),
rating-panel polish ("Set as" one-line + Good+ centring), and the amended session-scoped rating
pop-ups — entry/idle-return RESET (Cuisine + Menu), Save toast, first-run intro. Outstanding (operator
side): add `RAILWAY_API_KEY` secret + re-run *Auto Fetch Railway Logs* (run #1 soft-skipped).

### Amends self — v0.62.62 log-fetch → dedicated branch (№ 722)

**Amendment (№ 722, 13-06 '26 09:05 SGT):** Operator added the `RAILWAY_API_KEY` secret and re-ran
*Auto Fetch Railway Logs* — the Railway GraphQL query WORKED (fetched + created
`log/log-2026-06-13T01-01-53Z.json`), but the push to **`main` was rejected** by branch protection
(`GH006: Protected branch update failed — 4 of 4 required status checks expected`). Fix: the
"Commit and push logs" step now targets a dedicated, unprotected **`railway-logs`** branch (untracked
log file survives the branch switch; `checkout -B` from `origin/railway-logs` or fork off main on the
first run; rebase-then-push). main stays clean; CI never blocks the hourly commit. The Railway schema +
token are validated end-to-end. PATCH 0.62.61 → 0.62.62.

### Amends self — `[STATUS]` #1016 merge (№ 723)

**Amendment (№ 723, 13-06 '26 09:06 SGT):** Operator: *"Go."* PR **#1016** squash-merged to `main`
at **`221537e`** (all 4 checks green). STATUS: **MERGED / PROD**. The hourly *Auto Fetch Railway Logs*
now commits `log/log-*.json` to the dedicated **`railway-logs`** branch. One more run is needed to
create/populate that branch (the integration can't trigger it — 403); operator to re-run or wait for
the top-of-hour cron, then drift analysis reads `[LocDrift]` lines off `railway-logs`.

### Amends self — v0.62.63 reset copy in boot overlay + Stop on all pop-ups (№ 724)

**Amendment (№ 724, 13-06 '26 09:12 SGT):** Operator: on relaunch show "Rating reset: Good+ ≥ 3.7⭐ /
Showing eateries with generally good Google ratings." on the NEXT LINE under "Please wait while loading
random eateries…"; and put the 🛑 Stop pill on EVERY loading pop-up (small font, 0.5px red/yellow
border). **`App.jsx`** — the initial loading overlay's boot branch now renders the reset/intro copy
below the "Please wait…" line (kind!=='saved'); the bottom rating toast is suppressed while that overlay
shows it (no dup). The 🛑 Stop button now renders for ALL loadingReasons (was 'rotating' only), restyled
`rounded-full border-[0.5px] border-amber-500 text-[10px]`; on the boot warm-start (no AbortController)
it just dismisses the wait. **`FunFactModal.jsx`** — same pill restyle. Answer to "why a Stop button":
it lets users bail out of ANY wait; now consistent across the fun-fact modal, the rotating overlay, and
the boot overlay. Suite **3446/3446**; build green. PATCH 0.62.62 → 0.62.63.

### Amends self — `[STATUS]` #1017 merge (№ 725)

**Amendment (№ 725, 13-06 '26 09:17 SGT):** Operator: *"Merge."* PR **#1017** squash-merged to `main`
at **`e3972b1`** (all 4 checks green). STATUS: **MERGED / PROD**. Live: rating-reset/intro copy inside
the boot loading overlay (below "Please wait while loading random eateries…"), and the 🛑 Stop pill
(small font, 0.5px amber border) on every loading pop-up. Still pending operator: one more
*Auto Fetch Railway Logs* run to create the `railway-logs` branch, then `[LocDrift]` drift analysis.

### Amends self — v0.62.64 log fetch merges recent deployments (№ 726)

**Amendment (№ 726, 13-06 '26 09:25 SGT):** Operator: *"Done"* (re-ran the workflow). Runs #4/#5
succeeded but the commit step was SKIPPED — job log: `Latest deployment: bb7f2ca6… / Fetched 0 log
rows`. ROOT CAUSE: `deploymentLogs` is scoped to ONE deployment, and the merge burst (#1015–#1017)
triggered fresh Railway redeploys, so the LATEST deployment is seconds old and has no logs in the
65-min-back window — they belong to the PREVIOUS deployment. FIX (`fetch-logs.yml`): resolve the latest
**5** deployments (`deployments(first:5,…)`) and fetch+merge each one's `deploymentLogs` for the window
(jq concat + sort_by timestamp), wrapped as `{since,until,count,logs}`. Reuses the proven deploymentLogs
query (no schema risk). Verified jq merge/sort locally; YAML valid. PATCH 0.62.63 → 0.62.64. Next run
should land a non-empty file on `railway-logs`.

### Amends self — `[STATUS]` #1018 merge (№ 727)

**Amendment (№ 727, 13-06 '26 09:26 SGT):** Operator: *"Merge."* PR **#1018** squash-merged to `main`
at **`240aca2`** (all 4 checks green). STATUS: **MERGED / PROD**. The log fetch now merges the latest 5
deployments' logs, so a fresh-redeploy empty window no longer yields rows=0. Next *Auto Fetch Railway
Logs* run should commit a non-empty file to `railway-logs`; then `[LocDrift]` drift analysis.

### Amends self — v0.62.65 logs back to main/log/ (№ 728)

**Amendment (№ 728, 13-06 '26 09:52 SGT):** Operator (looking at `main/log/`, empty) + AskUserQuestion:
*"Put on main/log/ (needs bot bypass)."* Reverted the "Commit and push logs" step from the dedicated
`railway-logs` branch (v0.62.62) back to **`main`** (`git add log/… ; commit ; pull --rebase ; push
origin main`), KEEPING the v0.62.64 multi-deployment merge fetch. **Operator TODO (one-time):** add
`github-actions[bot]` to the branch ruleset's BYPASS list (Settings → Rules/Branches → allow Actions to
bypass required status checks) — otherwise the bot push is rejected (`GH006`). YAML valid. PATCH
0.62.64 → 0.62.65. Once the bypass is set + one run fires, `log/log-*.json` lands on `main`; then
`[LocDrift]` drift analysis.

### Amends self — v0.62.66 Stop curating + pop-up polish (№ 729)

**Amendment (№ 729, 13-06 '26 09:55 SGT):** Operator: "Stop loading" → **"Stop curating"** (same 🛑),
on its own LAST line flush RIGHT; the pop-up "looks 90s". Also answered: the 10 s timer is the fun-fact
ROTATION cadence only — it does NOT touch the search/result/location, so it is not a drift cause.
**`i18n.js`** — `funfact.stop`/`loading.stop` → "🛑 Stop curating" / "🛑 Arrêter la sélection".
**`App.jsx`** loading overlay — modernised (blurred `bg-black/30` scrim, `rounded-3xl` card,
`ring-1 ring-tg-accent/10`, softer `shadow-2xl`, roomier padding, max-w-320); the 🛑 pill moved to its
own row `flex justify-end` (last line, right-flush). **`FunFactModal.jsx`** — same pill →
`justify-end`. Suite **3446/3446**; build green. PATCH 0.62.65 → 0.62.66. Rides PR #1019.

### Amends self — v0.62.67 back to railway-logs branch (№ 730)

**Amendment (№ 730, 13-06 '26 10:05 SGT):** Operator hit the bot-bypass friction on protected `main`
(two screenshots were both NEW-rule screens, neither the bypass) and (AskUserQuestion) chose **"Switch
to railway-logs branch"**. Reverted the "Commit and push logs" step from `main` (v0.62.65) BACK to the
unprotected **`railway-logs`** branch — zero GitHub settings needed. Keeps the v0.62.64 multi-deployment
merge fetch + v0.62.66 "Stop curating" UI. YAML valid. PATCH 0.62.66 → 0.62.67. Once a run fires it
creates `railway-logs` with the log file; browse at `…/tree/railway-logs/log`; `[LocDrift]` analysis
reads from there.

### Amends self — `[STATUS]` #1019 merge (№ 731)

**Amendment (№ 731, 13-06 '26 10:09 SGT):** Operator: *"merge"* (after confirming "anything to change
here" → nothing; railway-logs needs no branch-protection rule). PR **#1019** squash-merged to `main` at
**`d0c2db6`** (all 4 checks green). STATUS: **MERGED / PROD**. Live: "Stop curating" pill (last line,
right-flush) + modernised loading pop-up; log fetch commits to the unprotected `railway-logs` branch
(no settings). NEXT: re-run *Auto Fetch Railway Logs* → creates `railway-logs` + commits the file →
`[LocDrift]` drift analysis off that branch.

### Amends self — `[STATUS]` #1021 Putrajaya cap fix (№ 732)

**Amendment (№ 732, 13-06 '26 13:49 SGT):** Operator: *"Build"* (the location-drift cap fix, after
topping up Gemini + Anthropic credits). Acting on the merged log `log/logs.1781315803421.json`
(`chat 100000001`): the Putrajaya pick persisted `cap_m:null` → fell back to the **MAJOR 45 km**
`city-default`, and `[Cuisine-Search] D703d` scoped results "≤45000m of set location", reaching
central KL (22.6 km) / Petaling Jaya (20.9 km) / Shah Alam (22.8 km) — the logged drift.

`[DELTA]`: new **`TIGHT_15KM`** tier in `city-centroids.js` (`_radiusForCity`) for `Putrajaya` +
`Cyberjaya` (compact cluster ~20 km south of the KL core; 15 km keeps Cyberjaya at 2.5 km, excludes
the KL core). City-default fill only — an explicit `radiusCapM` still wins; `searchRadius` unchanged.
`__tests__/city-centroids.test.js`: +TIGHT-tier assertion, +exact-drift-pin regression
(`nearestCityRadiusM(2.96957,101.71218)===15000`), allowed-radius set += 15000. `package.json`
0.62.67 → **0.62.68**.

`[VERIFICATION (sandbox)]`: `node --check city-centroids.js` OK; `npm test -- --run` **3448 passed**
(136 files); `web/` untouched (no build). `[STATUS]`: **DRAFT** PR **#1021** (draft, CI running) →
Railway auto-deploys on squash-merge to `main`. `[KNOWN GAPS]`: AI-credit depletion (Gemini 429 /
Anthropic 400 — operator top-up done, out of code scope); `durian` `ZERO reason=all-seen-criteria`
session-seen exhaustion still open.

### Amends self — `[STATUS]` #1021 MERGED (№ 733)

**Amendment (№ 733, 13-06 '26 13:56 SGT):** Operator: *"Merge"*. PR **#1021** squash-merged to
`main` at **`b34793d`** (all 4 checks green). `[STATUS]`: **MERGED / PROD** (v0.62.68) — Railway
auto-deploys. Live: Putrajaya/Cyberjaya now cap at 15 km (TIGHT tier) instead of the 45 km KL-bleed
default. Verify next log shows `cap_m:15000 cap_src:city-default` for a Putrajaya pin.

### Amends self — `[STATUS]` #1022 opaque first-load overlay (№ 734)

**Amendment (№ 734, 13-06 '26 14:20 SGT):** Operator (two screenshots IMG_0612 translucent first-load
vs IMG_0613 opaque "Did you know?"): *"Why the first load is translucent pop-up"* → diagnosed a
styling mismatch (loading overlay `bg-tg-card/95` + card `backdrop-blur` + faint 60% border vs
FunFactModal solid `bg-tg-card` + 2px accent frame); operator: *"yes make it opaque"*.

`[DELTA]`: `web/cuisine/src/v2/App.jsx` loading-overlay card → `bg-tg-card` (opaque),
`border-2 border-tg-accent`, `ring-tg-accent/30`, dropped card-level `backdrop-blur` (wrapper scrim
still blurs the page). Applies to all loading reasons (initial / rotating-early / refresh).
`package.json` 0.62.68 → **0.62.69**. Display-only; no logic touched.

`[VERIFICATION (sandbox)]`: `npm test -- --run` **3448 passed**; `web/cuisine` `npm run build` ✓ built
(pre-existing import-chunking warnings only). `[STATUS]`: **DRAFT** PR **#1022** (CI running) →
Railway auto-deploys + rebuilds the TMA on squash-merge. `[KNOWN GAPS]`: AI-credit verification +
`durian` session-seen exhaustion still open (carried from № 732/733).

### Amends self — `[STATUS]` #1022 MERGED (№ 735)

**Amendment (№ 735, 13-06 '26 14:49 SGT):** Operator: *"merge"*. PR **#1022** squash-merged to
`main` at **`a3429c0`** (all 4 checks green incl. TMA build). `[STATUS]`: **MERGED / PROD**
(v0.62.69) — Railway redeploys + rebuilds the cuisine TMA. Live: the first-load loading pop-up is
now opaque (solid `bg-tg-card`, 2px accent frame), matching the "Did you know?" fact card.

### Amends self — doc-system catch-up (VibeCodingRecord + Register + Technical) (№ 736)

**Amendment (№ 736, 13-06 '26 15:45 SGT):** Operator: *"update vibe jounal, documentation, vault"*.
`[DELTA]`:
- **VibeCodingRecord** — appended PRs **#1004 → #1022** (19 rows) to `data/prs.ndjson` + their squash-commit
  file lists to `data/pr-files.tsv` (sourced from main's squash commits), bumped `GEN_DATE` → 2026-06-13,
  ran `node doc/VibeCodingRecord/generate.mjs` → `records.tsv` (1009 rows), `vibe-coding-record.md`, and the
  tracked hosted page `public/doc/vibe-journal.{html,json}`.
- **Register** — new incremental snapshot `register-0_62_69-13_06_26-1545.md` (№ 127): D-62.53 → D-62.69,
  Open items O-42 (durian `all-seen-criteria` zero), O-43 (AI-credit depletion has no alerting),
  O-44 (v0.61.301 → v0.62.52 Register/Technical/Feature backfill still due — logged, not papered over).
- **Technical** — new incremental snapshot `technical-0_62_69-13_06_26-1545.md` (№ 52): `loc-drift-log.js`,
  the `capSource` thread-point, the `TIGHT_15KM` tier, the `railway-logs` build constraint, the loading-overlay
  styling contract, `[LocDrift]`/D777b/D781 diagnostic codes, and a Deprecated table (DT-1 45 km Putrajaya cap,
  DT-2 frosted overlay).
- **Feature** — intentionally NOT created; the user-facing feature backfill is folded into Register O-44 (AU-1:
  record the absence rather than fabricate a thin file).
- **Vault** — `vault/v0.62.69/` snapshot handled as a separate step (see next amendment / KNOWN GAPS).

`[VERIFICATION (sandbox)]`: generator ran clean (categories feature=666 fix=134 …; rework-flagged=87); ledger
tail now ends at #1022. `[STATUS]`: docs committed to `claude/refine-local-plan-5vh1jm`. Serial-state:
register 126→127, technical 51→52, journal 734→735, commit 751→752; re-anchored 12-06 13:00 → 13-06 15:45 SGT
(>5 min cross-session drift per TF-7, source: harness message stamp 13-06 07:40 UTC = 15:40 SGT, no net for the
sovereign air-temperature anchor).

### Amends self — vault/v0.62.69 snapshot (№ 737)

**Amendment (№ 737, 13-06 '26 15:50 SGT):** completes the *"vault"* part of the doc/vault catch-up.
`[DELTA]`: `vault/v0.62.69/` — snapshot of merged `main` at **`a3429c0`** (v0.62.69, PR #1022), built via
`git archive origin/main` over all top-level entries EXCEPT `vault/` (so it doesn't recursively swallow the
prior snapshots). **187 MB, 169 top-level entries**, no `node_modules`, no nested `vault/`. Mirrors the
`vault/v0.62.37` pattern (§16.1 vault-prior-known-good). `[VERIFICATION]`: snapshot `package.json` version =
0.62.69; `city-centroids.js` carries `TIGHT_15KM` (the #1021 fix). `[STATUS]`: committed to
`claude/refine-local-plan-5vh1jm`. commit 752→753.

### Amends self — v0.62.70 first-load overlay copy + blinking ellipsis (№ 738)

**Amendment (№ 738, 13-06 '26 17:24 SGT):** Operator (screenshot IMG_0615, confirming the #1022 opaque
card is live) — first-load overlay copy spec: *"Finding eateries… / Rating reset to Good+ ≥ 3.7⭐ /
[Stop search]"* + *"'…' blink the Horizontal Ellipsis. Char U+2026"*.

`[DELTA]` (`web/cuisine`):
- `lib/i18n.js` — `loading.initial` "Please wait while loading random eateries…" → **"Finding eateries"**
  (en) / "Recherche de restaurants" (fr); ellipsis removed from the string (now a separate blinking span).
  `rating.resetTitle` "Rating reset: …" → **"Rating reset to Good+ ≥ 3.7⭐"** (fr "réinitialisée à").
  `loading.stop` **and** `funfact.stop` "🛑 Stop curating" → **"🛑 Stop search"** (fr "Arrêter la recherche")
  — both stop buttons renamed in step for consistency; 🛑 kept.
- `v2/App.jsx` — render a trailing `<span className="animate-blink">…</span>` (U+2026) after the loading text.
- `styles.css` — `@keyframes blink {0,100%:1; 50%:0}` + `.animate-blink` (1.1s ease-in-out), plain class so JIT
  never purges it.
- **Kept** `rating.resetBody` ("Showing eateries with generally good Google ratings.") — operator's spec
  didn't list it; retained as non-destructive (flagged for confirmation).
- `package.json` 0.62.69 → **0.62.70**.

`[VERIFICATION (sandbox)]`: `npm test -- --run` **3448 passed**; `web/cuisine` build ✓. `[STATUS]`: committed
to `claude/refine-local-plan-5vh1jm` — rides the open docs PR **#1023** (now docs/vault + this UX patch).
commit 753→754. `[KNOWN GAPS]`: rating.resetBody retention pending operator confirm; blink scoped to the
first-load ellipsis only (rotating/refresh "…" unchanged).

### Amends self — `[STATUS]` #1023 MERGED (№ 741)

**Amendment (№ 741, 13-06 '26 17:32 SGT):** Operator: *"Merge"*. PR **#1023** squash-merged to `main` at
**`95381c1`** (all 4 checks green). `[STATUS]`: **MERGED / PROD** (v0.62.72). One PR carried: the doc/vault
catch-up (VibeCodingRecord #1004–#1022, Register/Technical v0.62.69, vault/v0.62.69) + the loading-overlay
copy (v0.62.70 "Finding eateries…" + blinking ellipsis + "Stop search") + the fun-fact card layout (v0.62.71
"Food fact" + single footer row) + the rating-reset body-line drop (v0.62.72). Railway redeploys + rebuilds
the cuisine TMA.

### Amends self — `[STATUS]` #1024 MERGED (№ 743)

**Amendment (№ 743, 13-06 '26 17:55 SGT):** Operator: *"Merge"*. PR **#1024** squash-merged to `main` at
**`7bb1e66`** (all 4 checks green). `[STATUS]`: **MERGED / PROD** (v0.62.73). Live: first-load overlay text
left-justified, 🛑 Stop search right-justified. Railway redeploys + rebuilds the cuisine TMA.

### Amends self — `[STATUS]` #1025 MERGED (№ 745)

**Amendment (№ 745, 13-06 '26 18:52 SGT):** Operator: *"merge"*. PR **#1025** squash-merged to `main` at
**`164bc56`** (all 4 checks green). `[STATUS]`: **MERGED / PROD** (docs only — no version bump). O-43 dropped;
O-44 doc backfill (register/technical/feature -0_62_52) resolved. Only **O-42** (durian all-seen-criteria
zero-result) remains open.

### Amends self — O-42 closed won't-fix (№ 746)

**Amendment (№ 746, 13-06 '26 19:07 SGT):** Operator: *"close o-42"* (after I explained the durian
all-seen-criteria zero-result). `[DELTA]`: `register-0_62_69-…` moved **O-42** from Open Items to
*Dropped / Won't-do* with rationale — the state self-heals (`resetSeen` fires, next tap recovers) and the
empty tap shows the v0.62.13 `zeroReasonKey` "tap 🔍 again" copy, which is more honest than silently
re-serving the same venues; edge case only on narrow queries in compact cities. **No open items remain in
the v0.62.69 arc snapshot.** Docs only. `[STATUS]`: committed to `claude/refine-local-plan-5vh1jm`; PR to
follow. commit 761→762.

### Amends self — `[STATUS]` #1026 MERGED (№ 747)

**Amendment (№ 747, 13-06 '26 19:19 SGT):** Operator: *"Merge"*. PR **#1026** squash-merged to `main` at
**`b93ae8c`** (all 4 checks green; docs only). `[STATUS]`: **MERGED**. O-42 closed won't-fix. **Register now
has zero open items.** All session work shipped or resolved/dropped with a trail.

### Amends self — `[STATUS]` #1027 MERGED (№ 749)

**Amendment (№ 749, 13-06 '26 19:55 SGT):** Operator: *"merge"*. PR **#1027** squash-merged to `main` at
**`3a8bddb`** (all 4 checks green). `[STATUS]`: **MERGED / PROD** (v0.62.74). Live: `australian` + `new-zealand`
now run the D703e/D703f Gemini relevance gate — the Spanish/Iberico "Southern European Deli" false positive
gets dropped. Railway redeploys + rebuilds the cuisine TMA.

### Amends self — `[STATUS]` #1028 MERGED (№ 751)

**Amendment (№ 751, 13-06 '26 20:56 SGT):** Operator: *"merge"*. PR **#1028** squash-merged to `main` at
**`e37e4a7`** (all 4 checks green). `[STATUS]`: **MERGED / PROD** (v0.62.75). Live: the OTHER geofence is
fail-closed + floor-free — a coordless / out-of-range venue can no longer leak into a foreign-city search
(the Kapitan-SG-in-Putrajaya cross-country leak is closed). Railway redeploys + rebuilds the cuisine TMA.

### Amends self — `[STATUS]` #1029 MERGED (№ 753)

**Amendment (№ 753, 13-06 '26 22:09 SGT):** Operator: *"merge"*. PR **#1029** squash-merged to `main` at
**`3803a6c`** (all 4 checks green). `[STATUS]`: **MERGED / PROD** (v0.62.76). Live: the `[Cuisine-Enrich]
D707 enrichSlow timings …` per-phase line. NEXT: once Railway redeploys, one search + a log fetch will show
the D707 breakdown → then the targeted timeout fix.

### Amends self — `[STATUS]` #1030 MERGED (№ 755)

**Amendment (№ 755, 14-06 '26 10:27 SGT):** Operator: *"Merge"*. PR **#1030** squash-merged to `main` at
**`f655e95`** (all 4 checks green; docs/vault only). `[STATUS]`: **MERGED**. VibeCodingRecord → #1029,
Register/Technical v0.62.76, vault/v0.62.76 snapshot all on main. Live Open items: O-45 (Load-failed timeout,
high), O-46 (BestTime non-SG), O-47 (stale Results count), O-48 (97 Durian curated pick) — O-45/O-48 await
operator input (post-deploy D707 log; venue place_id+coords).

### Amends self — `[STATUS]` #1031 MERGED (№ 757)

**Amendment (№ 757, 14-06 '26 13:53 SGT):** Operator: *"merge"*. PR **#1031** squash-merged to `main` at
**`d961d7a`** (all 4 checks green). `[STATUS]`: **MERGED / PROD** (v0.62.77). Live: wait pop-up shows the
first streamed result's name (bold navy) + ⏳ static / … blinks; BestTime skipped for non-SG (O-46 done);
O-45 closed. Railway redeploys + rebuilds the cuisine TMA. Open: O-47, O-48.

### Amends self — `[STATUS]` #1032 MERGED (№ 760)

**Amendment (№ 760, 14-06 '26 14:23 SGT):** Operator: *"merge"*. PR **#1032** squash-merged to `main` at
**`057e934`** (all 4 checks green). `[STATUS]`: **MERGED / PROD** (v0.62.78–79). Live: progressive first-result
name in the wait card (FunFactModal + refresh overlay); durian zero-results root fix (soft-rating on the plain
durian cuisine + D793 small-pool recycle). O-42 resolved, O-48 superseded. Open: O-47. Railway redeploys.
NEXT: durian re-test from Putrajaya + log → expect `durian soft-rating: floor skipped` + `D793 recycle`.

### Amends self — `[STATUS]` #1033 MERGED (№ 763)

**Amendment (№ 763, 14-06 '26 14:50 SGT):** Operator: *"Merge"*. PR **#1033** squash-merged to `main` at
**`f36533d`** (all 4 checks green). `[STATUS]`: **MERGED / PROD** (v0.62.80–81). Live: durian all-seen recycle
(any non-empty pool, no size gate → no more durian zeros); progressive first-result name only on the
"Finding eateries…" overlay (off the fun-fact card); per-card **📍 ~X.X km away** prominent distance row.
Railway redeploys + rebuilds the TMA. Open: O-47.

### Amends self — `[STATUS]` #1034 MERGED (№ 765)

**Amendment (№ 765, 14-06 '26 15:37 SGT):** Operator: *"Merge"*. PR **#1034** squash-merged to `main` at
**`e4ef468`** (all 4 checks green). `[STATUS]`: **MERGED / PROD** (v0.62.82). Live: wait-card Stop pill on the
text row; dots blink one-at-a-time; rating ⭐ twinkles ✨→🌟→⭐→💫; MY/KUL picker pills butt together.
Railway redeploys + rebuilds the TMA. Open: O-47.

### Amends self — `[STATUS]` #1035 MERGED (№ 770)

**Amendment (№ 770, 14-06 '26 16:14 SGT):** Operator: *"Merge PR #1035"*. PR **#1035** squash-merged to `main`
at **`2738011`** (all 4 checks green). `[STATUS]`: **MERGED / PROD** (v0.62.83–86). Live: location-pick prompt
renders HTML (no literal `<b>`, "top" dropped); wait card (no ⏳, Stop on rating row, smaller, hugs border);
Tourist+Regions one row; prompt "📍 Choose a city below…"; restructured `/location` card; dynamic
"{flag} {street} · current" quick-pick. Railway redeploys + rebuilds the TMA. Open: O-47 + doc/vault catch-up
(v0.62.77–86 still undocumented).

### Amends self — `[STATUS]` #1036 MERGED (№ 773)

**Amendment (№ 773, 14-06 '26 16:39 SGT):** Operator: *"Merge"*. PR **#1036** squash-merged to `main` at
**`400e201`** (all 4 checks green). `[STATUS]`: **MERGED / PROD** (v0.62.87–88). Live: wait-card Stop pill
vertically centred / refresh one-row + Stop below; durian-repeat honest "all N within ~Xkm → 🔭 Widen" prompt
(widen lifts the OTHER cap to 40 km). Railway redeploys + rebuilds the TMA. Open: O-47 + doc/vault catch-up
(v0.62.77–88).

### Amends self — `[STATUS]` #1037 MERGED (№ 776)

**Amendment (№ 776, 14-06 '26 17:11 SGT):** Operator: *"Merge"*. PR **#1037** squash-merged to `main` at
**`dd83cf4`** (all 4 checks green). `[STATUS]`: **MERGED / PROD** (v0.62.89–90). Live: wait-card Stop pill in
the bottom-right corner; **sticky Widen switch** (per-cuisine, OFF ~15 km / ON ~40 km, resets on cuisine
change) replacing the one-shot pill; **liquid-glass** Stop + switch styling. Railway redeploys + rebuilds the
TMA. Open: O-47 + doc/vault catch-up (v0.62.77–90).

### Amends self — v0.62.91 widen tap lifts the DISCOVERY radius + #1038 MERGED (№ 777)

**Amendment (№ 777, 14-06 '26 18:23 SGT):** Operator (with log `logs.1781430757811.json`): *"The widen keeps
going back to same criteria and same list of durian. Why, why"* + *"Are there compressed criteria for widen
that kill the overall results and look into saved or cache locations? Should it be a separate search branch"*
+ *"'97 durian' doesn't appear after widen"* + *"When I reopen the Cuisine TMA, widen should switch off and
being saved"*.

`[INTENT]`: explain why the sticky Widen switch (№ 775) returned the *same* durian list on re-tap and fix it.

`[DELTA]` — root cause from the log: the widen override fired `D777c widen → anchorCap 40000m` but
`radius=15000` stayed and `D781 OTHER radius ceiling 20000m → 15000m` clamped it back. The widen lifted only
the **post-fetch distance cap** (`anchorCap`), never the **discovery radius** (`searchRadius`):
- `index.js` (~14664) — on `wantWiden && isOther`, now also lift `searchRadius` to the 40 km `WIDEN_CAP_M`
  (was: anchorCap only). The primary `pipeline.discover()` now queries the wider ring; the criteria hash
  (`computeCriteriaHash`, which includes `radius`) changes → the 20-min pool cache (`cuisine:pool:…`) + the
  per-criteria seen-set are tracked **separately** from the tight pool (no stale 15 km reuse — answering the
  operator's "cache locations" question); `D781` no longer clamps it; the geo-scope already opens to 40 km via
  `resolveCap → anchorCap`. Gated to `isOther` + the 40 km OTHER ceiling.
- **No FE change**: `widenActive` is `useState(false)` → reopening the TMA already starts OFF (operator's "switch
  off + saved" requirement); sticky ON across re-taps within a session; resets on cuisine change. The switch
  *felt* unsaved only because the server returned no new venues.
- **"97 Durian"** is a Places **recall/ranking** ceiling (low-prominence stall past the ~30–60 fetched results),
  NOT a radius limit — left as-is pending an operator decision on a widen-only pagination bump (logged below).
- **No separate search branch** added — the existing path handles the radius once `searchRadius` is lifted; a
  named-place branch would be the curated/hardcoded route the operator rejected.
- `package.json` 0.62.90 → **0.62.91**.

`[VERIFICATION (sandbox)]`: `node --check index.js` ✓; `npm test -- --run` **3450 passed** (136 files); no
`web/` changes. `[STATUS]`: **MERGED / PROD** — PR **#1038** squash-merged to `main` at **`2a3e725`** (all 4
checks green). `[KNOWN GAPS]`: 97-Durian recall (widen-only deeper pagination — operator decision pending);
O-47 stale "Results: N"; doc/vault catch-up v0.62.77–91. commit 793→794.

### Amends self — v0.62.92 widen-repeat trio (B + rotation + honest signal) + #1039 MERGED (№ 778)

**Amendment (№ 778, 14-06 '26 21:39 SGT):** Operator (with log `logs.1781442738719.json`): *"B."* +
*"before you build, check the log and ask why keeps repeating same eateries … strategise and code-audit the
search. report first and ask permission before build … i need to know how the impact to other cuisines other
than durian as well"* → after audit + *"build the trio (B + rotation + honest signal)"* + *"merge"*.

`[INTENT]`: stop the widen "keeps repeating same eateries". The v0.62.91 radius fix worked (`D777c
searchRadius 20000→40000m`, pool 26) but the repeat persisted.

`[AUDIT — why it repeated]`: the per-session seen-set exhausts the pool, then `D793` re-served it
**nearest-first** and the slice took the **same top 12 every tap** (`index.js:16313`, `:16353`) — the farther
14 of 26 never surfaced. A recycle-ORDERING bug, not a recall shortage; deeper recall alone wouldn't fix it.

`[DELTA]` — three additive changes (all `v0.62.92`):
- **B — deeper recall on widen** (`index.js` base discover ×2 paths): `maxResults` 30→**60** (single/escalation,
  2→3 Places pages) and 15→**30** + a 2nd page (multi-cuisine) when `wantWiden`. More genuinely-unique venues
  from a dense OTHER pool (KL durians at 40 km) before recycle. **Widen + OTHER only** — SG/JB + normal
  searches pay nothing.
- **Recycle rotation** (`index.js` D793): advance a per-session, per-criteria Redis cursor
  (`cuisine:recycle:<chat>:<hash>`, 30-min TTL) and rotate the pool by `offset=((cursor-1)*sliceCap)%len`, so
  consecutive recycles serve the NEXT window and cycle the whole pool. **Generic** (all cuisines/regions);
  no-Redis fallback = the prior nearest-first behaviour (no regression). New `allSeenRecycledCount`.
- **Honest signal** (`index.js` payload + `web/cuisine/src/v2/App.jsx`): `allSeenInRange` now sent after a
  widen too (`capKm`=real cap, `count`=full recycled-pool size, `widened` flag); the note reads **"You've seen
  all N within ~40 km"** instead of silently repeating. FE copy reworked (drops the pre-widen "rest are further
  out" line).
- `package.json` 0.62.91 → **0.62.92**.

`[CROSS-CUISINE IMPACT]` (operator asked): B = widen+OTHER only (durian + any dense OTHER cuisine; SG/JB
untouched). Rotation + honest signal = generic improvements to EVERY exhausted search (all cuisines/regions) —
strictly more variety + truth; counter/`finalBatch`/`exhausted` semantics + durian ordering preserved.

`[VERIFICATION (sandbox)]`: `node --check index.js` ✓; `npm test -- --run` **3450 passed** (136 files);
`web/cuisine` build ✓. `[STATUS]`: **MERGED / PROD** — PR **#1039** squash-merged to `main` at **`189c96c`**
(all 4 checks green). `[KNOWN GAPS]`: progressive 40→60 km widen (offered, operator decision pending); O-47;
doc/vault catch-up v0.62.77–92. commit 794→795.

### Amends self — v0.62.93 special-mode seed FAN-OUT (97 Durian root cause) + #1041 MERGED (№ 779)

**Amendment (№ 779, 15-06 '26 08:16 SGT):** Operator: *"Still doesn't show '97 durian' establishments, do a
google map query of 'durians' from IOI City Mall by distance and all the durian terms specified"* →
*"try this google map key first: …"* (operator-supplied key for a live diagnostic) → after the probe +
*"Build per-seed fan-out"* + *"merge"*.

`[INTENT]`: find why "97 Durian" (and most near durians) never surfaced, with EVIDENCE, then fix.

`[DIAGNOSIS — live Places probe from IOI City Mall, 2.9686,101.7120]`: the sandbox has no
`GOOGLE_MAPS_API_KEY`, so the operator supplied one. A probe (per-seed Text Search + dedup) proved the app
was under-recalling ~10×:
- "97 Durian (Sri Petaling)" **exists** in Places — 11.8 km away (inside the 15 km cap), `primaryType=wholesaler`
  (NOT in `NON_FOOD_TYPES`, so it passes the filter), 4.8★. Per-seed it ranks **#29 within 15 km**.
- Google has **39 durian venues ≤15 km / 76 ≤40 km**; the prod log showed only **4 ≤15 km**.
- ROOT CAUSE: `pipeline.js:1030` **OR-joined every synonym seed into one query**
  (`durian shop OR durian stall OR … OR gerai durian`), so Google returned ONE prominence-ranked set and the
  near pool was lost. NOT coverage, NOT distance, NOT the type gate, NOT page depth.

`[DELTA]` (`v0.62.93`, server-only):
- `pipeline.js` — new `fanOutSeeds` option on `discover()`. When set, run EACH seed as its own Text Search
  and **merge + dedup by placeId** (the union of per-term nearest results) instead of the OR-join. One page
  per seed → cost bounded (~N seeds vs 1 call). New diag `D710b`. Single-query path preserved verbatim under
  an `else`.
- `index.js` — pass `fanOutSeeds: !!specialMode` on the durian/fruits discover. Regular cuisine searches keep
  the single OR-query (multi-cuisine already had its own per-cuisine fan-out).
- `package.json` 0.62.92 → **0.62.93**.

`[CROSS-CUISINE IMPACT]`: gated to special modes (durian/fruits — synonym seeds). Regular cuisine searches,
all regions incl. SG/JB, are UNTOUCHED. Cost: a durian/fruits search now issues ~6–9 Text Searches vs 1
(low-frequency; result caches under the existing per-criteria pool key).

`[VERIFICATION (sandbox)]`: `node --check index.js`+`pipeline.js` ✓; `npm test -- --run` **3450 passed**;
empirically validated against live Places (operator key — NOT committed; guarded the diff). `[STATUS]`:
**MERGED / PROD** — PR **#1041** squash-merged to `main` at **`5235062`** (all 4 checks green).
`[KNOWN GAPS]`: progressive 40→60 km widen (still offered); O-47; doc/vault catch-up v0.62.77–93. commit
795→796.

### Amends self — v0.62.94 widen NL/Tell-me path + colloquial dish-alias + Places-API limit on record (№ 780)

**Amendment (№ 780, 15-06 '26 10:48 SGT):** Operator on the Tell-me (NL free-text) box, three threads —
*"dai lok mee kl … so narrowed compared to google map"* → *"how about: Dai Lok"* → *"Ipoh Tuck Kee … also
sells Dai Lok mee / So is Ying Xian Canton Cuisine 赢鲜海鲜大排档"* → *"note this Places-API-vs-consumer-Maps
limitation in the PR/journal so the reasoning is on record"*.

`[INTENT]`: the NL path (`/api/cuisine/nl-query`, renders its venues directly) returned far fewer results
than Google Maps for KL dishes; find why with EVIDENCE, fix, and record the residual data-surface limit.

`[DIAGNOSIS — live Places probe from IOI City Mall, 2.9686,101.7120; operator key, NOT committed]`:
- Places itself is fine — `dai lok mee` returns ~39 correct KL Hokkien Mee spots (Tong Lian Kee, Mun Wah,
  Kim Lian Kee…), same as Maps. The narrowing was entirely in our NL handler.
- Three under-fetch bugs: (a) **no `regionCode`** passed → discover defaulted to `'SG'` for a Malaysia user;
  (b) **no pagination** (1 page, ~20); (c) **slice 12** with no across-tap paging (one-shot path → that
  slice is all the user sees).
- `dai lok` ALONE → ~9 results and the WRONG dish (lok-lok 淥淥 steamboat + Tai Ka Lok 大家乐 seafood): the
  "mee" token did all the disambiguation; 大碌麵 is the Cantonese nickname for KL Hokkien Mee and the repo
  had no alias.
- Per-seed coverage probe: dedicated noodle shops reachable via `KL Hokkien mee`/`福建面`; tai-chow seller
  **Ipoh Tuck Kee** reachable ONLY via `dai lok mee`; **Ying Xian 赢鲜海鲜大排档** reachable via NONE of the
  four seeds — only its own name.

`[DELTA]` (`v0.62.94`, server-only — no `web/` change):
- `index.js` `/api/cuisine/nl-query` — derive + pass `nlRegionCode` from the forwarded region/country
  (mirrors main-search `searchRegionCode`: `JB`/`MY-PUT`→`MY`, `OTHER`→`countryCode`, `SG`→`SG`); discover
  now `maxResults:60` + `maxPages:3` (probe: 20→50 unique); display slice **12 → 24**. New diag `D773`.
- **`dish-aliases.js`** (new) — colloquial nickname → canonical dish UNION. `dai lok` / 大碌 → `['KL Hokkien
  mee','dai lok mee','福建面','大碌麵']`. The NL handler (`D774`) drops the ambiguous verbatim query (bare
  "dai lok" pulls lok-lok noise) and prepends the union. The union is deliberate: noodle-shop seeds +
  tai-chow seed so BOTH seller types survive (a "Hokkien mee"-only expansion would have dropped Ipoh
  Tuck Kee).
- **`__tests__/dish-aliases.test.js`** (new, 6) — expansion, spacing/Chinese variants, no-false-positive
  on `hokkien mee`/`lok lok`/`tai ka lok`, non-string guards.
- `package.json` 0.62.93 → **0.62.94**.

`[PLACES-API vs CONSUMER-MAPS — on the record, operator's explicit ask]`: Ying Xian is **unreachable by
any dish query — a data-surface limit, not a bug**. We query the **Places API** (`searchText`), which
matches mostly on NAME/TYPE/ADDRESS; the dish string is in none of Ying Xian's indexed fields (generic
seafood/Canton listing). The **consumer Maps app** ranks over a richer corpus we do NOT receive — review
text, menu items, popular-dish tags, photo captions, Q&A — so it *can* surface a venue mentioned only in a
review. (Self-correction: earlier in-thread "Google Maps wouldn't find it either" was an overclaim — only
the API door was tested.) **Product decision (operator):** do NOT curate one-off niche venues — the app's
value is *enhanced quality* of results, not coverage parity; the durable lever is this dish-intelligence
layer (helps every query), not a per-venue patch (doesn't scale, rots). Future curation, if ever, = a
general "known-for" dish→venue dataset. Reasoning also captured in `dish-aliases.js` header.

`[VERIFICATION (sandbox)]`: `node --check index.js` + `dish-aliases.js` ✓; `npm test -- --run` **3456
passed** (+6); empirically validated against live Places (operator key — NOT committed; guarded the diff):
regionCode+pagination 20→50, alias union 80 unique with Ipoh Tuck Kee retained, zero lok-lok noise.
`[STATUS]`: **DRAFT** — PR **#1042** (rides `claude/refine-local-plan-5vh1jm`); Railway on merge.
`[TEST]`: in the TMA Tell-me box from a MY location, "dai lok" now returns KL Hokkien Mee (incl. Ipoh
Tuck Kee), not steamboat. `[KNOWN GAPS]`: Ying Xian-class venues (no dish text) stay unreachable by
design; progressive widen + doc/vault catch-up v0.62.77–94 still due. journal 779→780, commit 796→798.

## Amendments

### Amends self — v0.62.90 sticky Widen SWITCH + liquid-glass buttons (№ 775)

**Amendment (№ 775, 14-06 '26 17:02 SGT):** Operator (plan-approved): *"after widen, second tap goes back to
search criteria … widen isn't lock in … switch UI only for the current search … once the cuisine is close, off"*
+ *"liquid glass 3D button … flush lower bottom + right … standardised"*.

`[LOG VERDICT]` (`logs.1781427226529.json`): confirmed the revert — 08:50:25 `D777c widen → 40000m` (12 venues),
08:50:33 next 🔍 tap `D777b 15000m / D781 → 15000m` → D793 recycle 9. Root: `widen` was a **one-shot** request
flag; the normal re-tap didn't carry it.

`[DELTA]` (`web/cuisine`): **D-62.90** —
- `App.jsx`: new sticky **`widenActive`** state; `runSearch` carries `widen: opts.widen ?? widenActive` (explicit
  toggle wins, re-taps inherit the sticky state); **reset effect** on `state.cuisines` change → OFF; the all-seen
  note's one-shot 🔭 pill replaced by a **glass Switch** (OFF ~15 km / ON ~40 km), shown while exhausted OR while
  widen is on; Stop pill restyled `.glass-pill`.
- `styles.css`: new **`.glass-pill`** (frosted translucent + backdrop-blur + 3D highlight/shadow).
- `package.json` 0.62.89 → **0.62.90**.

`[VERIFICATION]`: `npm test -- --run` **3450 passed**; `web/cuisine` build ✓. `[STATUS]`: committed to
`claude/refine-local-plan-5vh1jm` (rides PR #1037). commit 791→792.

### Amends self — v0.62.89 Stop pill → bottom-right corner (№ 774)

**Amendment (№ 774, 14-06 '26 16:52 SGT):** Operator (IMG_0632): the centred Stop pill "looks horrible" —
*"Stop search must be flushed nearer to bottom border and right"*. `[DELTA]` (`web/cuisine/src/v2/App.jsx`
loading overlay): unified ALL wait states — text block, then the 🛑 Stop pill in its **own row flush to the
bottom-right** (`mt-1 -mb-0.5 flex justify-end`), card padding `pr-3→pr-2.5` + `pb-3→pb-2` so the pill hugs the
bottom + right borders. Dropped the items-center side-by-side flex (v0.62.87) and the refresh-only special
case (now the same layout for rotating/refresh/initial). `package.json` 0.62.88 → **0.62.89**. `[VERIFICATION]`:
`npm test -- --run` **3450 passed**; `web/cuisine` build ✓. `[STATUS]`: committed to
`claude/refine-local-plan-5vh1jm`; PR to follow. commit 790→791.

### Amends self — v0.62.88 durian-repeat root + honest "widen?" prompt (№ 772)

**Amendment (№ 772, 14-06 '26 16:28 SGT):** Operator + log `logs.1781425612075.json`: *"Why the last four
results for durian > Putrajaya keeps repeating / Why 97 durians doesn't appear"*. `[LOG VERDICT]`: durian at
Putrajaya — `D702 18 → D703 17 → D703d 17 → 4 (≤15000m)` → **only 4 durian within the 15 km cap** (13 are
>15 km in KL/Selangor); `D793 all-seen recycle: re-serving 4` on every tap → the repeat. 97 Durian (~11 km)
is **not in Google's 18 candidates** for "Durian Fruits" at Putrajaya (Places coverage/relevance, or coordless)
→ never reaches our filters. Operator chose *"Keep 15 km, add an honest 'widen?' prompt"*.

`[DELTA]` (no hardcode): **D-62.88** —
- `index.js`: accept `req.body.widen` → on the OTHER path lift the tight per-city cap to **40 km** (`D777c`,
  capSource `widen`); flag the D793 recycle; payload `allSeenInRange:{count,capKm}` (only at the tight cap,
  not after a widen).
- `web/cuisine` `lib/api.js`: `searchCuisine({widen})` → `body.widen`. `App.jsx`: `allSeenInRange` state
  (reset per search, set from `r`), threaded `widen` into `runSearch` opts, and a note **"That's all N within
  ~X km. The rest are further out." + 🔭 Widen** button (`runSearch(state,null,{widen:true})`).
- `package.json` 0.62.87 → **0.62.88**.

`[VERIFICATION]`: `node --check` OK; `npm test -- --run` **3450 passed**; `web/cuisine` build ✓. `[STATUS]`:
committed to `claude/refine-local-plan-5vh1jm` (rides PR #1036). commit 788→789.

### Amends self — v0.62.87 wait-card Stop alignment + refresh one-row (№ 771)

**Amendment (№ 771, 14-06 '26 16:25 SGT):** Operator (IMG_0628/0631/0629). `[DELTA]`
(`web/cuisine/src/v2/App.jsx` loading overlay):
- **Stop vertically centred** — Finding-eateries / Rating-reset card: outer flex `items-end` → **`items-center`**
  (operator: bottom-aligned looked off; centre it against the text).
- **Refresh card one row + Stop below** — split the `refresh` branch out: the "Refreshing results with the same
  filters…" copy is a **full-width row** (no longer wraps around the pill), with the 🛑 Stop pill **below it,
  flush right**.
- **Tighter pill** — `px-2`→`px-1.5` + `whitespace-nowrap` on both Stop buttons.
- `package.json` 0.62.86 → **0.62.87**.

`[VERIFICATION]`: `npm test -- --run` **3450 passed**; `web/cuisine` build ✓. `[STATUS]`: committed to
`claude/refine-local-plan-5vh1jm`; PR to follow. commit 787→788.

### Amends self — v0.62.86 dynamic "current location" quick-pick button (№ 769)

**Amendment (№ 769, 14-06 '26 16:10 SGT):** Operator (AskUserQuestion) chose *"Button: 📍 {street} · current"*.
`[DELTA]` (`index.js`): `_buildTopLevelKeyboard(curLoc)` — the second MY slot becomes a **dynamic** button
`{flag} {label} · current` (from the most-recent saved location), callback **`lr:0`** (reuses the
most-recent-recent re-anchor handler); falls back to the IOI City Mall precinct when no saved location exists.
New `_currentLocForQuickPick(chatId)` helper (most-recent `listRecentLocations`). Threaded into
`sendLocationQuickPicks` + the `menu_root` re-render. (Telegram inline buttons are plain single-line text, so
"current" rides inline — no italic/2nd-row.) `package.json` 0.62.85 → **0.62.86**. `[VERIFICATION]`:
`node --check` OK; `npm test -- --run` **3450 passed**; server-only. `[STATUS]`: committed to
`claude/refine-local-plan-5vh1jm` (rides PR #1035). commit 785→786. Closes the A1 follow-up.

### Amends self — v0.62.85 location quick-pick + stale-pin card restructure (№ 768)

**Amendment (№ 768, 14-06 '26 16:01 SGT):** Operator (IMG_0626/0627). `[DELTA]`:
- **A2** — `index.js _buildTopLevelKeyboard`: 🇸🇬 Tourist Areas → + 🇸🇬 Regions → now share **one row** (were
  two rows).
- **B** — `i18n.js loc.precinct.prompt`: "🗺 Quick-pick anchor — tap a precinct or Malaysia city below…" →
  **"📍 Choose a city below, or saved location"** (en + fr).
- **C** — `index.js` `/location` card rebuilt to the operator's template: `📍 *Current Location*` / name /
  "<locality>, <state>, <country>" (new `locName`/`locArea` from the reverse-geocode components) / divider /
  `⚠️ Shared over 30 minutes ago. Please choose a new location.` / `📌 Pick a city below` / `⌨️ Type a place`
  (`/location <place>`, `/l <place>`) / `📍 Use current GPS` (`/location current`, `/l current`). Non-stale
  branch mirrors it without the ⚠️.
- `package.json` 0.62.84 → **0.62.85**.
- **A1 (NOT built — Telegram constraint)**: the dynamic current-location button can carry a `{flag} {street}`
  label, but inline-keyboard buttons are **plain single-line text** — no italic / flush-right / 2nd-row / font
  size. Flagged to operator for a doable alternative.

`[VERIFICATION]`: `node --check` index.js + i18n.js OK; `npm test -- --run` **3450 passed**; server-only.
`[STATUS]`: committed to `claude/refine-local-plan-5vh1jm` (rides PR #1035). commit 784→785.

### Amends self — v0.62.84 wait-card: remove ⏳, Stop on rating row, smaller+tighter (№ 767)

**Amendment (№ 767, 14-06 '26 15:47 SGT):** Operator (IMG_0625). `[DELTA]` (`web/cuisine/src/v2/App.jsx`
loading overlay): removed the **⏳ hourglass** (blinking dots are the only motion); outer flex
`items-center`→**`items-end`** so the 🛑 Stop pill aligns to the LAST text row (the "Rating reset…" row when
present); Stop pill **text-[10px]→text-[8px]** (two sizes smaller), padding `px-2.5 py-1`→`px-2 py-0.5`; card
**`px-5`→`pl-5 pr-3`** + gap `gap-3`→`gap-2` so the pill hugs the right border (no wide gap). `package.json`
0.62.83 → **0.62.84**. `[VERIFICATION]`: `npm test -- --run` **3450 passed**; `web/cuisine` build ✓.
`[STATUS]`: committed to `claude/refine-local-plan-5vh1jm` (rides PR #1035). commit 783→784.

### Amends self — v0.62.83 location-pick prompt HTML + drop "top" (№ 766)

**Amendment (№ 766, 14-06 '26 15:41 SGT):** Operator (IMG_0624): *"Why are the html codes not address 'Want
to see top eateries at <b>IOI Resort City, Putrajaya</b>?'"* + *"Are we sure of the 'top eateries'…?"*.
`[ROOT CAUSE]`: `loc.searchPick.prompt` mixed Markdown `_italic_` + HTML `<b>` but was sent with
`parse_mode:'Markdown'` (line ~3434) → italic rendered, `<b>` literal. (The sibling `loc.set.success` is sent
HTML, so its `<b>` worked.) `[DELTA]`: `i18n.js` — prompt → HTML `<i>…<b>{place}</b>…</i>`, and dropped the
over-claiming "top"/"meilleurs" (the search returns rating-floored NEARBY eateries; the button already says
just "See eateries here"). `index.js` — that `sendMessage` `parse_mode:'Markdown'` → `'HTML'`
(+ disable_web_page_preview). `package.json` 0.62.82 → **0.62.83**. `[VERIFICATION]`: `node --check` OK;
`npm test -- --run` **3450 passed**; server-only. `[STATUS]`: committed to `claude/refine-local-plan-5vh1jm`;
PR to follow. commit 782→783.

### Amends self — v0.62.82 wait-card polish + MY/KUL gap fix (№ 764)

**Amendment (№ 764, 14-06 '26 15:34 SGT):** Operator (3 screenshots + follow-up). `[DELTA]` (`web/cuisine`):
- **Stop pill same row** — the loading overlay restructured: text block (Finding eateries… / Rating reset… /
  first-result name) in a left `flex-1` column with the 🛑 Stop pill vertically-centred on the right of the
  SAME row (was its own bottom line).
- **Dots one-at-a-time** — the trailing `…` replaced by three `.` spans with staggered `animate-blink`
  (0 / 0.25 / 0.5 s) so they light in sequence instead of the whole U+2026 glyph blinking.
- **Animated star** — new `AnimatedStar.jsx` cycles **✨ → 🌟 → ⭐ → 💫** (420 ms); the ⭐ is stripped from the
  `rating.resetTitle`/`introTitle` i18n string and rendered by the component so only the star twinkles.
- **MY/KUL gap** — `CountryDropdown` button had `minWidth:4.5rem` leaving ~1.5 rem empty to the right of
  "🇲🇾 MY ▾", pushing the KUL pill away despite the gap-0 group. Dropped the min-width → KUL butts against it.
- `package.json` 0.62.81 → **0.62.82**.

`[VERIFICATION]`: `npm test -- --run` **3450 passed**; `web/cuisine` build ✓. `[STATUS]`: committed to
`claude/refine-local-plan-5vh1jm`; PR to follow. commit 780→781.

### Amends self — v0.62.81 progressive name off fun-fact card + prominent distance (№ 762)

**Amendment (№ 762, 14-06 '26 14:50 SGT):** Operator (AskUserQuestion): *"Keep fun-fact, drop name from it"*
+ *"Make distance prominent"*. `[DELTA]` (`web/cuisine`):
- `FunFactModal.jsx` — removed the v0.62.78 `firstResultName` line + prop; `App.jsx` drops the prop pass. The
  progressive first-result name now lives ONLY on the "Finding eateries…" overlay (refresh/boot), per operator
  ("only under Finding eateries…, not the fun-fact card"). `streamFirstName` + the overlay line stay.
- `ResultCard.jsx` — distance moved OUT of the meta line into its own prominent row **📍 ~X.X km away**
  (semibold); walk-minutes kept in the meta only when <2 km (genuinely walkable). Answers "scope gets further
  … but didn't tell me how far away".
- `package.json` 0.62.80 → **0.62.81**.

`[VERIFICATION]`: `npm test -- --run` **3450 passed**; `web/cuisine` build ✓. `[STATUS]`: committed to
`claude/refine-local-plan-5vh1jm` (rides PR #1033). commit 778→779.

### Amends self — v0.62.80 all-seen recycle gate widened (durian zeros) (№ 761)

**Amendment (№ 761, 14-06 '26 14:37 SGT):** Operator + log `logs.1781418900751.json`: still getting durian
zeros. `[LOG VERDICT]`: set location had moved to **KL (Sri Petaling, ~3.072,101.690)** → cap is now **45 km**
(`D703d ≤45000m`), so the durian pool is **18** (not Putrajaya's 7). The v0.62.79 fixes ARE live
(`durian soft-rating: floor skipped, kept all 18` ✓) but the **D793 recycle was gated to pool ≤12**, so an
18-venue pool kept dead-zeroing (`ZERO all-seen-session pool=18 trulyUnseen=0`). `[DELTA]` (`index.js`):
**v0.62.80** — drop the `≤FOLLOW_UP_SLICE` gate; recycle ANY non-empty all-seen pool (re-serve `venues`
nearest-first). `package.json` 0.62.79 → **0.62.80**. `[VERIFICATION]`: `node --check` OK; `npm test -- --run`
**3450 passed**; server-only. `[STATUS]`: committed to `claude/refine-local-plan-5vh1jm`; PR to follow.
commit 777→778. `[STILL PENDING OPERATOR]`: wait-UI ("progressive under Finding eateries, not fun-fact card"
— remove the fun-fact card?) + distance prominence.

### Amends self — v0.62.79 durian zero-results root fix (№ 759)

**Amendment (№ 759, 14-06 '26 14:12 SGT):** Operator + log `logs.1781417230306.json`: *"issues with finding
durians at IOI City Mall, Putrajaya. keep getting 1 or zero / why 97 Durians doesn't appear / dont hardcode.
find why the zero results can appear"*.

`[LOG VERDICT]`: durian (plain cuisine, cuisineQueries=["Durian Fruits"]) near Putrajaya: `D703d 30→9`
(15 km cap drops 21 — durian stalls are spread across Selangor), `rating-floor ≥3.7 9→7`, pool=7. Repeat taps:
`ZERO reason=all-seen-session` (sessionSeen=13→26) — the per-session seen-set exhausts the 7, and the
client's `resetSeen` retry only clears the per-CRITERIA seen, never session-seen → repeated dead zeros. No
`resetSeen for chat` recovery fired (the earlier O-42 "self-heal" assumption was wrong).

`[ROOT CAUSE]`: (1) small pool exhausted by session-seen → no recovery; (2) the durian soft-rating
(v0.62.14) was special-mode-only, so the plain durian **cuisine** still floored sub-3.7 stalls (97 Durian).

`[DELTA]` (`index.js`, no hardcode): **D-62.79** — `durianSoftRating` extended to `cuisines` includes 'durian';
**D793 small-pool recycle** (`trulyUnseen` → `let`; when a ≤FOLLOW_UP_SLICE pool is all-seen, re-serve
nearest-first instead of zero). `package.json` 0.62.78 → **0.62.79**. Resolves **O-42** + supersedes **O-48**
(root fix, not a hardcoded pick).

`[VERIFICATION]`: `node --check` OK; `npm test -- --run` **3450 passed**; server-only. `[STATUS]`: committed to
`claude/refine-local-plan-5vh1jm` (rides PR #1032). commit 775→776.

### Amends self — v0.62.78 progressive first-result name in the wait card (№ 758)

**Amendment (№ 758, 14-06 '26 14:10 SGT):** Operator: *"'Finding eateries…' doesn't show what it had find
progressively"*. Root: v0.62.77 put the name in the loading-overlay **'initial'** branch — which is the
**boot, single-shot (non-streaming)** load, so `venues` never populates during it; the normal re-search wait UI
is the **FunFactModal** ('rotating'), which had no name line. `[DELTA]` (`web/cuisine`):
- New `streamFirstName` state, cleared per search, set only by the NDJSON `onBase`/`onPatch` (never flashes the
  previous result; blank on the non-streaming boot).
- `FunFactModal` gains a `firstResultName` prop → bold-navy line under the fact body.
- Loading overlay name line moved OUT of the 'initial' branch to after the loadingReason ternary (so the
  streaming **'refresh'** overlay shows it too).
- `package.json` 0.62.77 → **0.62.78**.

`[VERIFICATION]`: `npm test -- --run` **3450 passed**; `web/cuisine` build ✓. `[STATUS]`: committed to
`claude/refine-local-plan-5vh1jm`; PR to follow. commit 774→775. `[NOTE]`: the single-shot boot load stays
blank (anti-flash design); progressive shows on 'rotating' (FunFactModal) + 'refresh'.

### Amends self — v0.62.77 wait pop-up first-result name + BestTime SG-only (№ 756)

**Amendment (№ 756, 14-06 '26 10:42 SGT):** Operator (plan approved, *"Go / build"*). `[DELTA]`:
- **Pop-up** (`App.jsx` loading overlay): ⏳ `animate-spin` removed (static glyph; the U+2026 `…` blink is the
  only motion); new **bold-navy** (`font-bold text-blue-900`) line under "Finding eateries…" rendering
  `venues[0].name` — the first NDJSON `onBase` card, available while `loading` is still true (verified).
- **O-46 done** — `cuisine-enrich.js` skips `attachFootfallSignals` when `ctx.isSG === false`; `index.js`
  enrichCtx now carries `isSG: !isOther && !isJB`. Non-SG searches no longer pay the BestTime `resolved=0/N`
  latency. New log `[Cuisine-Search] footfall skipped (non-SG region …)`.
- **O-45 closed** — heavy enrichSlow parallelization NOT pursued; BestTime-skip is the pragmatic cut. Register
  `register-0_62_76` Open Items updated (O-45 closed, O-46 done); D-62.77 added.
- `package.json` 0.62.76 → **0.62.77**.

`[VERIFICATION]`: `node --check` index.js + cuisine-enrich OK; `npm test -- --run` **3450 passed**;
`web/cuisine` build ✓. `[STATUS]`: committed to `claude/refine-local-plan-5vh1jm`; PR to follow.
commit 772→773. `[STILL OPEN]`: O-47 (stale Results count), O-48 (curated-picks / 97 Durian, blocked on data).

### Amends self — doc + vault catch-up to v0.62.76 (№ 754)

**Amendment (№ 754, 14-06 '26 10:05 SGT):** Operator: *"update the documentation and vault"*.
`[DELTA]`:
- **VibeCodingRecord** — appended PRs **#1023 → #1029** (7 rows) to `data/prs.ndjson` + squash-commit file
  lists to `data/pr-files.tsv`, bumped `GEN_DATE` → 2026-06-14, regenerated → `records.tsv` (**1016 rows**),
  `vibe-coding-record.md`, hosted `public/doc/vibe-journal.{html,json}`. Ledger ends at #1029.
- **Register** — `register-0_62_76-14_06_26-1005.md` (№ 129): D-62.70 → D-62.76; new Open items O-45
  (enrichSlow 20s "Load failed", high), O-46 (BestTime 0/N for non-SG), O-47 (stale "Results: N" knownTotal),
  O-48 (curated-picks for 97 Durian, blocked on placeId+coords).
- **Technical** — `technical-0_62_76-14_06_26-1005.md` (№ 54): D703d coordless-excluded, D703e/f australian/nz,
  the new **D707** enrichSlow-timings code, the sequential-enrichSlow perf note, Deprecated DT-10 (withinCap
  fail-open) + DT-11 (OTHER never-empty floor).
- **Feature** — not re-cut this arc (the v0.62.70→.76 changes are overlay-polish + search-hardening already in
  Register/Technical; logged here rather than a thin Feature file).
- **Vault** — `vault/v0.62.76/` snapshot of merged main at `3803a6c` (v0.62.76): 187 MB, 169 entries, no
  node_modules/nested-vault; verified the fail-closed geofence + D707 are present.

`[VERIFICATION]`: generator clean (feature=667 fix=137 …); docs only. Serial-state: register 128→129,
technical 53→54, journal 735→736, commit 769→770; re-anchored 13-06 15:45 → 14-06 10:05 SGT. `[STATUS]`:
committed to `claude/refine-local-plan-5vh1jm`; PR to follow.

### Amends self — v0.62.76 enrichSlow timing instrumentation (№ 752)

**Amendment (№ 752, 13-06 '26 22:05 SGT):** Operator: *"Load failed"* + log `logs.1781359269477.json`.
`[LOG VERDICT]`: the geo fix is live (`D703d 57→33 … coordless/out-of-range excluded`), but the search hit
**`D706 deadline-degraded (>20000ms)`** — `cuisine-search-exit ms=22562` (22.5 s) → the client gave up =
"Load failed". Root: `enrichSlow()` runs a **sequential** chain (translate → review-cache → Gemini dishes →
travel → sanctuary/Claude → footfall/BestTime) that sums past 20 s; BestTime also `resolved=0/6` (no MY
coverage). Operator chose *"Add timing logs first"*.

`[DELTA]`: `cuisine-enrich.js` `enrichSlow` — per-phase delta timers + one summary line
**`[Cuisine-Enrich] D707 enrichSlow timings (Nv): crowd=… translate=… reviewCache=… dishes=… finalise=…
travel=… sanctuary=… footfall=… total=…ms`**. No behaviour change. `package.json` 0.62.75 → **0.62.76**.

`[VERIFICATION]`: `node --check` OK; `npm test -- --run` **3450 passed**; server-only (no web build).
`[STATUS]`: committed to `claude/refine-local-plan-5vh1jm`; PR to follow. commit 767→768. `[NEXT]`: deploy →
re-run a search → fetch log → the D707 line pinpoints the dominant cost; then build the targeted fix
(parallelize the worst offenders / gate BestTime to SG).

### Amends self — v0.62.75 OTHER geofence fail-closed (Kapitan SG leak) (№ 750)

**Amendment (№ 750, 13-06 '26 20:15 SGT):** Operator: *"i selected Russian | Malaysia > Putrajaya / result is
Singapore's version of Russian eatery / why / can you check log"* + uploaded `logs.1781352823772.json`.

`[LOG VERDICT]` (window 09:10–12:11): **good news** — #1021's 15 km cap is **deployed & working** (every search
`cap_m:15000 cap_src:city-default`, `D703d ≤15000m`); **AI credits restored** (0 billing failures); the Russian
**relevance gate worked** (`D703f dropped 6 unrelated 7→1`). The leaked venue = **Kapitan Restaurant, 32 Maxwell
Rd, Singapore** (placeId ChIJV1jY_msZ2jER2Nm55hFNYxo, 🚊620min/🚘235min ≈330 km).

`[ROOT CAUSE]`: the OTHER geofence `withinCap = (v) => v.distanceM == null || v.distanceM <= cap` **failed OPEN**
on a null distance — Kapitan reached the cap coordless (a strong far Places text-match for "Russian" that
arrived without a usable distance) → passed; and `demoteNeverEmpty` would resurface it anyway.

`[DELTA]`: `cuisine-geo-scope.js` OTHER branch (also JB-fallback) → **fail CLOSED + floor-free**:
`out.filter(v => Number.isFinite(v.distanceM) && v.distanceM <= cap)`; honest empty (→ zeroReason) beats a
cross-country result. **SG/JB unchanged** (coordless curated venues there are genuinely local).
`__tests__/cuisine-geo-scope.test.js` — flipped the 2 tests that encoded the old fail-open/never-empty for
OTHER, added an SG-keeps-null guard. `package.json` 0.62.74 → **0.62.75**.

`[VERIFICATION]`: `node --check` OK; `npm test -- --run` **3450 passed**; no `web/` change. `[STATUS]`:
committed to `claude/refine-local-plan-5vh1jm`; PR to follow. commit 765→766. `[NOTE]`: behaviour change —
OTHER searches can now return empty (honest "none nearby") rather than ever-empty flooring; deliberate, matches
the operator's earlier Australian-gate choice.

### Amends self — v0.62.74 Australian/NZ relevance gate (№ 748)

**Amendment (№ 748, 13-06 '26 19:33 SGT):** Operator live report — *"I look for Australian food, where I am
IOI city mall, Putrajaya"* → got **ANNE LOURDES Southern European Deli** (Iberico pork / lamb rack) in
**Cheras, KL** (~12.6 km). Diagnosis: (a) the venue is within the new 15 km cap (so #1021 admits it
legitimately — distance is by-design), (b) the real bug = **no cuisine-relevance gate for "australian"**, so
Places loose-matched a generic Western/European deli. Operator chose *"Add relevance gate (recommended)"*.

`[DELTA]`: extend the D703e/D703f Gemini-label gate to Australasian —
- `durian-gemini-verifier.js` `PROMPT_CONFIG` += **`australian`** + **`new-zealand`** (each `sells` prompt
  explicitly rejects the generic-Western / steakhouse / Iberico-deli false positive).
- `index.js` `GEMINI_CACHE_KEYS` += `'australian', 'new-zealand'` (so a single-cuisine Australian search
  triggers the gate; the online verify-then-cache path then drops `unrelated` venues, and an all-unrelated
  pool returns empty → honest "none nearby" rather than the wrong result).
- `__tests__/durian-gemini-verifier.test.js` — guard test: `_buildPrompt('australian'|'new-zealand')` builds
  and the Australian prompt carries the "NOT a generic Western" + Iberico/deli reject caveat.
- `package.json` 0.62.73 → **0.62.74**.

`[VERIFICATION (sandbox)]`: `node --check` index.js + verifier OK; `npm test -- --run` **3449 passed** (+1);
no `web/` change (no build). `[STATUS]`: committed to `claude/refine-local-plan-5vh1jm`; PR to follow.
commit 763→764. `[KNOWN GAPS]`: gate needs working Gemini credits for the online-verify path (topped up
13-06); deploy of #1021's 15 km cap still unconfirmed in logs (no post-deploy search captured yet).

### Amends self — O-43 dropped + O-44 doc backfill (№ 744)

**Amendment (№ 744, 13-06 '26 18:30 SGT):** two operator instructions — *"drop o-43"* then *"doc backfill"*.

`[DELTA]`:
- **O-43 dropped** — `register-0_62_69-…` moved O-43 (AI-credit alerting) from Open Items to a new
  *Dropped / Won't-do (operator decision)* section, rationale preserved (§4.1: items move, never deleted).
- **O-44 doc backfill (RESOLVED)** — three arc-level rollups for the **v0.61.301 → v0.62.52** gap
  (PRs #807–#1011, 205 PRs; minor bumped 0.61.300 → 0.62.0 directly):
  - `doc/Register/register-0_62_52-13_06_26-1830.md` (№ 128) — decisions D-62.A…D-62.N + resolved O-41/O-39/O-36/O-35.
  - `doc/Technical/technical-0_62_52-13_06_26-1830.md` (№ 53) — Michelin loader system, 139-city centroid table,
    FX USD-pivot, progressive NDJSON, per-device location keys, diagnostic codes D786–D791, Deprecated DT-3…DT-9.
  - `doc/Feature/feature-0_62_52-13_06_26-1830.md` (№ 54) — features F-A…F-J + Removed RF-17…RF-23.
  Sourced from the VibeCodingRecord records.tsv (#807–#1011) + the journals; arc-level, referencing them for
  per-PR detail. `register-0_62_69` Open Items updated (O-44 removed → resolved; O-43 → dropped).

`[VERIFICATION]`: docs only — no code touched; no tests/build needed. `[STATUS]`: committed to
`claude/refine-local-plan-5vh1jm`; new PR to follow. Serial-state: register 127→128, technical 52→53,
feature 53→54, commit 759→760.

### Amends self — v0.62.73 loading overlay left-justify (№ 742)

**Amendment (№ 742, 13-06 '26 17:33 SGT):** Operator: *"It has to be left justified / The stop search is
right justified"*. `[DELTA]`: `web/cuisine/src/v2/App.jsx` loading-overlay card `text-center` → **`text-left`**
and the "Finding eateries…" inner flex `justify-center` → **`justify-start`**; the 🛑 Stop pill row keeps its
own `justify-end` (right). Applies to all loading reasons (initial / rotating / refresh) for consistency.
`package.json` 0.62.72 → **0.62.73**. `[VERIFICATION]`: 3448 passed; `web/cuisine` build ✓. `[STATUS]`:
committed to `claude/refine-local-plan-5vh1jm`; new PR to follow. commit 757→758.

### Amends self — v0.62.72 drop rating-reset body line on the loading overlay (№ 740)

**Amendment (№ 740, 13-06 '26 17:31 SGT):** Operator: *"Want it removed"* — the grey body sub-line
"Showing eateries with generally good Google ratings." (and its first-run-intro equivalent) on the
**first-load overlay**. `[DELTA]`: `web/cuisine/src/v2/App.jsx` (~3603) — removed the `text-tg-hint`
body `<div>`; the rating line is now title-only (`rating.resetTitle` / `rating.introTitle`). The
standalone bottom rating-reminder TOAST (~line 2966, a different surface) keeps its body line
unchanged. `rating.resetBody` / `rating.introBody` strings retained (still used by that toast).
`package.json` 0.62.71 → **0.62.72**. `[VERIFICATION]`: 3448 passed; `web/cuisine` build ✓. `[STATUS]`:
committed to `claude/refine-local-plan-5vh1jm` — rides PR **#1023**. commit 755→756.

### Amends self — v0.62.71 fun-fact card layout (№ 739)

**Amendment (№ 739, 13-06 '26 17:27 SGT):** Operator (screenshot IMG_0614) — fact-card layout spec:
header *"💡 Food fact"*, body unchanged, divider, then a **single footer row** spread three ways:
*"••• Finding eateries…   Source: Wikipedia   [Stop]"*; *"don't change the logic"*.

`[DELTA]` (`web/cuisine`):
- `lib/i18n.js` — `funfact.header` "Did you know?" → **"Food fact"** (fr "Info gourmande");
  `funfact.curating` "Still curating…" → **"Finding eateries…"** (fr "Recherche de restaurants…");
  `funfact.stop` "🛑 Stop search" → **"🛑 Stop"** (fr "🛑 Arrêter") — per-screen: the first-load overlay
  keeps "🛑 Stop search", the compact fact-card footer reads "🛑 Stop". (Reverts the № 738 unify.)
- `components/FunFactModal.jsx` — moved the Stop button UP from its own last line INTO the footer row;
  the row is now `justify-between` three columns: `••• Finding eateries…` (truncate) · `Source: …` (shrink-0)
  · `🛑 Stop` (shrink-0). No change to fact selection / min-display / anti-repeat logic.
- `package.json` 0.62.70 → **0.62.71**.

`[VERIFICATION (sandbox)]`: `npm test -- --run` **3448 passed**; `web/cuisine` build ✓. `[STATUS]`: committed
to `claude/refine-local-plan-5vh1jm` — rides PR **#1023**. commit 754→755.

- **`loc-drift-log.js`** (new) · **`index.js`** (2 emit points + `capSource`) ·
  **`__tests__/loc-drift-log.test.js`** (new) · **`package.json`** `0.62.52 → 0.62.53` ·
  **`doc/.serial-state.yml`**.

## Deleted

- (none) — additive.
