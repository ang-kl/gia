(№ 847 - 04-07 '26 11:55 SGT) v0.62.493

# Journal — soleat v0.62.493 (Clipboard TMA: extend UI locales from 5 → 8, adding zh · ja · es) — task 2 COMPLETE

> Operator (task 2 — TMA language-set audit):
> *"audit the language set across all TMA, Menu TMA doesn't have the full language listing like Cuisine."*
> Read as: bring every TMA up to Cuisine's 8-language UI set (en/fr/id/ru/de/**zh/ja/es**), one PR per TMA.
> **This is PR 4/4 — Clipboard — the FINAL one.** (Menu #1493 · Hawker #1494 · Transport #1495, all merged.)

## New

### [HDR] #847 | 11:55 SGT 04-07-26 | v0.62.493 — Clipboard TMA gains zh · ja · es | 1 web file + 3 doc/meta | PR #(pending) |

- **[INTENT].** Close the last locale gap. Add **Chinese (zh)**, **Japanese (ja)**, **Spanish (es)**
  to the Clipboard / Sketchbook TMA so the meal-planning hub — chrome/nav, filter chips, card actions,
  cabinet/drawer forms, sharing/fork flow, meal time-segments, settings — renders in all 8 languages.

- **Structural note.** Clipboard is **unlike** the other three TMAs: strings live as **inline per-key
  `{ en, fr, id, ru, de }`** objects (no `ID/RU/DE_STRINGS` overlay pattern), and it has **no
  LocaleToggle** — it follows the shared `gia.locale` preference via a `SUPPORTED` `Set` +
  `SUPPORTED_LOCALES` array. So the change is **1 code file** and injects the three new fields into
  every entry rather than appending overlay blocks.

- **[DELTA] (1 code file).**
  - `web/clipboard/src/lib/i18n.js` — injected `zh` / `ja` / `es` into **all 170 `STRINGS` entries**;
    `SUPPORTED` Set and `SUPPORTED_LOCALES` array `['en','fr','id','ru','de']` → **`+['zh','ja','es']`**.

- **Translation provenance.** Agent-drafted per language — **170 keys each**. Singapore-standard terms
  (`小贩中心`, `地铁`). All emoji/glyphs (`＋`, `✕`, `✓`, `↩`, `‹`, `⧉`, `🗑`, `⭐`, `🔗`, `📲`, `🟢`, `✳️`,
  `📋`, `✏️`, `📍`, `🍜`) and interpolation tokens (`{n}`, `{city}`, `{cap}`, `{d}`, `{e}`) preserved in
  place. Brand/proper nouns (`Sketchbook`, `Telegram`, `MRT`, `Michelin`, `Bib Gourmand`, `Beta`)
  verbatim; Singapore → 新加坡 / シンガポール / Singapur. ja verified all-Japanese (**programmatic Hangul
  scan clean — 0 hits**); es written apostrophe-free (`¿` is fine in single-quoted JS).

- **[VERIFICATION (sandbox)]** `esbuild` bundle-parse **clean** (only `react` + `./tg.js` module-resolve
  warnings — not syntax). A node harness (imports stripped, `t()` exercised) confirmed **zh/ja/es are
  170/170 complete** and `t()` resolves all 8 locales for `chrome.title`, `set.whatsStored`,
  `cabinet.drawers` (token order preserved), `seg.wholeDay`. Full vitest + `web/clipboard` build in CI.

- **[STATUS]** PROD — Clipboard TMA redeploys on merge (Railway). Verify on Telegram: switching device/
  shared locale to zh/ja/es re-renders the hub. **CJK render eyeball** (font + pill fit) is the G4 gate.

- **[TEST]** Full vitest via CI.

- **[KNOWN GAPS]** (1) **Pre-existing, not introduced by this PR:** 7 `cat.*` category keys
  (`cat.commonHere`, `cat.southeastAsian`, `cat.eastAsian`, `cat.southAsian`, `cat.middleEastern`,
  `cat.european`, `cat.americas`) carry only en/fr/id in `main` — they lack **ru/de** and now also
  degrade those two to the en fallback. Filling ru/de there is a separate cleanup (out of task-2 scope).
  (2) CJK live-render eyeball outstanding. (3) Search auto-fire investigation (A) still separate.
  **Task 2 is now COMPLETE** — all four TMAs (Menu/Hawker/Transport/Clipboard) at 8 locales, matching
  Cuisine. G4 gates for v0.62.483–493 remain open.

## Confirmation Gates
| Gate | Status | Note |
|------|--------|------|
| G1 | ✅ | zh/ja/es 170/170 complete (node harness); esbuild parse clean; ja Hangul-scan 0 hits; es apostrophe-free |
| G2 | ⏳ | full vitest via CI |
| G3 | ✅ | serial 846→847, commit 964→965, build 603→604; anchor re-set 04-07 '26 11:55 SGT |
| G4 | ⏳ | redeploy + operator eyeball (zh/ja/es render + CJK pill fit; task-2 completion sweep) |
