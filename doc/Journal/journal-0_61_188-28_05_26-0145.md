(№ 389 - 28-05 '26 01:45 SGT) v0.61.188

# Journal — soleat v0.61.188 (LocationField OTHER: typing restored, dropdowns still suppressed)

> Operator after v0.61.187 deploy: *"the location field suppressed
> cannot type anymore. you didn't check"*. v0.61.187 made the
> location field fully read-only for OTHER (intent: hide noisy
> dropdown), but also stripped the ability to TYPE a free-text
> address. Operator's actual ask was "no DROPDOWN, but typing
> stays". v0.61.188 splits the difference.
>
> Commit 401 / journal 389 / build 191, on branch
> `claude/v0.61.188-other-locationfield-keep-typing` off `main` at
> v0.61.187.

## New

### [HDR] #389 | 01:45 SGT 28-05-28 | v0.61.188 — OTHER LocationField keeps typing | 5 files | 1 PR | 1 build |

- **[INTENT]** Restore the operator's ability to TYPE a free-text
  address in the OTHER region (Putrajaya / KL / Penang / Batam),
  while keeping v0.61.187's dropdown suppression in place
  ("too many to list").

- **[DELTA]**
  - **`web/cuisine/src/v2/components/LocationField.jsx`** (2 reverts
    of v0.61.187 logic + 1 gate kept):
    - **Autocomplete useEffect**: early-return on `region === 'OTHER'`
      REMOVED. The fetch now runs so Enter-to-anchor still works
      (picks the top hidden suggestion).
    - **Render**: the static "locked" label REMOVED. The
      editable input/button renders the same as SG/JB for OTHER
      too.
    - **KEPT**: the suggestions + no-match dropdown render gates
      `{open && region !== 'OTHER' && …}`. So the dropdown
      stays hidden, but typing + Enter works.
  - **`web/menu/src/components/LocationFieldMenu.jsx`** (1 restructure):
    - Precinct `<select>`: kept hidden for OTHER (v0.61.187
      behaviour preserved — too many possibilities to list).
    - Free-text input + submit button: RESTORED for OTHER (was
      hidden in v0.61.187; the operator's regression).
    - Autocomplete suggestions dropdown: kept hidden for OTHER
      (gate now lives on the suggestions render itself).
    - The "🔒 Anchor locked" hint from v0.61.187 is gone —
      replaced by the visible text input + submit (operator
      learns from doing, not from a hint).
  - **`package.json`** — `0.61.187 → 0.61.188`.
  - **`doc/.serial-state.yml`** — commit 400 → 401, build 190 →
    191, journal 388 → 389; re-anchored 28-05 '26 01:45 SGT.
  - **`doc/Journal/journal-0_61_188-28_05_26-0145.md`** — this entry.

- **[VERIFICATION (sandbox)]**

  ```
  $ cd web/cuisine && npm run build   (✓ built in 3.09s)
  $ cd web/menu && npm run build      (✓ built in 2.75s)
  ```

- **[STATUS]** PR-OPEN on push. Operator merges manually.

- **[TEST]** Operator (post-merge + Railway redeploy + browser cache bust):
  - With Putrajaya anchor active:
    - Cuisine TMA: tap the location field → input opens → type
      e.g. "Cyberjaya" → Enter. Anchor moves to Cyberjaya. NO
      visible dropdown of suggestions during typing.
    - Menu TMA: free-text input is visible with the [Search]
      submit button. Type → submit. Anchor changes. NO precinct
      dropdown, NO autocomplete suggestions panel.
  - With SG / JB anchor: full picker UI (precinct dropdown +
    text input + autocomplete suggestions) — unchanged from
    pre-v0.61.187.

- **[KNOWN GAPS]**
  - **Enter-to-anchor on OTHER requires a successful Places
    autocomplete fetch.** If Places returns no suggestions for
    the typed text, Enter is a no-op. The operator might want a
    fallback "submit raw text" path that geocodes server-side
    (like Menu TMA's submit button does). Filed as
    v0.61.189 candidate if the operator hits this case.
  - **The 'locked' hint is gone**. Some operators might
    appreciate a reminder that the dropdown is intentionally
    hidden ("typing only"). Could re-add as a small placeholder
    text update — easy follow-up.

- **Confirmation Gates**

  | Gate | Resolution | Date |
  |------|-----------|------|
  | G1 — version bump | PATCH `0.61.187 → 0.61.188`. Partial revert of v0.61.187 + UI restructure. | 28-05 '26 |
  | G2 — destructive action | _n/a_. | 28-05 '26 |
  | G3 — new decision rule | YES — OTHER LocationField policy is now "type yes, dropdown no" (was "fully read-only" in v0.61.187). | 28-05 '26 |
  | G4 — paid external API call | Restores ~1 Places autocomplete call per OTHER-anchor keystroke (was suppressed in v0.61.187). Still debounced at 250 ms; negligible cost. | 28-05 '26 |

## Amendments

- **Cuisine TMA `LocationField` policy for OTHER** — v0.61.187 was
  "static read-only label; no autocomplete fetch". v0.61.188 is
  "editable input + Enter-to-anchor; autocomplete fetch runs
  silently in background; dropdown UI stays hidden".
- **Menu TMA `LocationFieldMenu` policy for OTHER** — v0.61.187
  was "everything hidden except the 🔒 Anchor locked hint".
  v0.61.188 is "precinct dropdown hidden; free-text input + submit
  visible; autocomplete dropdown hidden".

## Deleted

- **Cuisine TMA "locked (chat /location)" label** — added in
  v0.61.187, removed in v0.61.188 (the field is no longer
  locked).
- **Menu TMA "🔒 Anchor locked. To change, use /location in chat."
  hint** — added in v0.61.187, removed in v0.61.188 (the text
  input is back; the hint is no longer accurate).
