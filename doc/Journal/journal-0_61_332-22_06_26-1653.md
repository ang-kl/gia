(№ 533 - 22-06 '26 16:53 SGT) v0.61.332

# Journal — soleat v0.61.332 (TF-7 self-correction: bulk year-stamp typo 2027–2031 → 2026 across journal/register filenames + all references)

> Operator (22-06 '26): *"address the journal file mis-named."* Surfaced while
> fixing the CLI status line: its "since journal" count read **0**, because the
> newest journal filename parsed to a **future** date. Root cause — during the
> vibe-journal-regen arc the YEAR field of the late-May 2026 entries was stamped
> as the **day** (`27_05_27`, `28_05_28`, `29_05_29`, `30_05_30`, `31_05_31`)
> instead of `_26`. Day + month were correct; only the 2-digit year was wrong,
> so the files parsed as **2027–2031**.
>
> Doc-only self-correction on `claude/v0.61.316-vibe-journal-regen`. Commit 546 /
> journal 533 / build 332. The status-line repair itself lives OUTSIDE this repo
> (`~/.claude/statusline-command.sh`) and is noted here for trace only.

## New

### [HDR] #533 | 16:53 SGT 22-06-26 | v0.61.332 — TF-7 self-correction: year-stamp typo (2027–2031 → 2026) corrected across 225 journal/register filenames + every stale reference (serial-state, VibeCodingRecord, in-file prose) | ~230 files | 0 PR (doc) | 1 build |

- **[INTENT]** Correct a pervasive, mechanical year-stamp typo introduced during the journal-regen arc: entries roughly #165–#307 (late-May 2026) had their filename YEAR field set equal to the DAY (`dd_05_dd`) rather than `dd_05_26`, so they read as 2027–2031. The bad anchor poisoned the status line's "since journal" count and corrupts chronological sorting + every reference that points at those names. Fix the names AND all references in one self-correction, preserving genuine future-year content (transit roadmap dates, 24-hour `hhmm` time fields, file counts).

- **[MECHANISM]** Date-anchored, surgical — never a blind year sweep:
  - **Filenames:** pure `mv` (`-([0-9]{2})_05_(2[7-9]|3[01])-` → `-\1_05_26-`), so the rename never touched `.git/index.lock` (the iCloud auto-commit tool kept re-creating it); the tool snaps the renames into a commit on its own. Run from the operator's shell — the agent sandbox denies `mv`/`rm`.
  - **References:** in-place `sed` over the same `_05_(2[7-9]|3[01])-` filename pattern in the data layer, plus three date-shaped patterns in prose (`-05 '2[7-9]`/`'3[01]` serial stamps; `(202[789]|203[01])-05` ISO). Every pattern is anchored to a date context, so real `2030`/`2027` mentions and `…-26-2030.md` (= 20:30) survive untouched.

- **[DELTA]**
  - **`doc/Journal/journal-*.md` + `doc/Register/register-*.md`** — **225** files renamed (`dd_05_{27,28,29,30,31}` → `dd_05_26`, incl. iCloud conflict-copies). All 706 journal+register filenames now read year `26`; 0 collisions.
  - **`doc/.serial-state.yml`** — 53 stale `# - doc/Journal/journal-…-dd_05_dd-….md` reference comments corrected to the renamed paths.
  - **`doc/VibeCodingRecord/data/pr-files.tsv`** — 143 PR file-list references corrected to the renamed journal/register paths.
  - **`doc/VibeCodingRecord/data/prs.ndjson` · `records.tsv` · `vibe-coding-record.md`** — 1 each (a `register-0_61_300-31_05_31-…` mention in PR #807's body/title). The real `2026-05-31` mergedAt timestamps were already correct and were left intact.
  - **`doc/Journal` + `doc/Register` prose** — 60 in-file occurrences across 42 files: filename self-references, `dd-05 'YY` serial stamps, and ISO date stamps, all year-corrected to 2026.
  - **`package.json`** — `0.61.331 → 0.61.332`.
  - **`doc/.serial-state.yml`** — commit 545 → 546, journal 532 → 533, build 331 → 332; re-anchored 16:53 SGT 22-06-26.
  - _(Out of repo, trace only):_ `~/.claude/statusline-command.sh` — PR total switched from a `--limit 1000`-capped list to the Search-API `.total_count` (was pinned at 1000, now 1204), and the journal anchor now drops any future-dated file before choosing the newest.

- **[VERIFICATION (sandbox)]**
  - `grep -rohE "_05_(2[7-9]|3[01])-|-05 '(2[7-9]|3[01])|(202[789]|203[01])-05"` over `doc/Journal`, `doc/Register`, `doc/.serial-state.yml`, `doc/VibeCodingRecord` → **0** residual wrong-year stamps.
  - Filename year-field histogram across journal + register → **706 × `26`**, nothing else.
  - Broad `20(2[789]|3[01])` sweep confirmed the survivors are all legitimate: `…-03_05_26-2030.md` (20:30 hhmm), `CRL Phase 1 opens 2030, JRL 2027`, `RTS Link (Dec 2026 / Jun 2027)`, `syntax-check scope (2028 → 115 files)`.

- **[STATUS]** DRAFT / doc-only on the vibe-journal-regen branch. No code path changed; no auto-merge armed. The iCloud auto-commit tool stages the renames + edits.

- **[TEST]** Status-line "since journal" now anchors to the newest *valid* journal date (06-06 '26) and reports a real count (391 at fix time) instead of 0; the future-date guard prevents recurrence even if a bad name reappears.

- **[KNOWN GAPS]**
  - **VCR intentionally NOT regenerated.** `pr-files.tsv` is derived from squash-commit file lists, and the historical commits still hold the OLD names — a regen would re-introduce them. The data layer was therefore corrected in place; future PRs commit the corrected names.
  - **Root cause unaddressed in the generator.** Whatever regen step stamped year=day should be fixed so the typo can't recur; logged in `doc/Register/`.
  - **iCloud conflict-copies** (`… 2.md`, `… 3.md`) were renamed for consistency but remain duplicates — a separate cleanup.

- **Confirmation Gates**

  | Gate | Resolution | Date |
  |---|---|---|
  | G1 — version bump | PATCH `0.61.331 → 0.61.332` (doc-only self-correction, per the every-change-bumps rule). | 22-06 '26 |
  | G2 — destructive action | **APPROVED by operator** ("1"). Bulk `mv` rename (225 files) + `sed` cascade over references. No content deleted; only the wrong year corrected. Date-anchored patterns verified to spare legitimate future-year mentions. | 22-06 '26 |
  | G3 — new decision rule | _n/a_ — applies the existing TF-7 self-correction precedent (correct a wrong time anchor, log it, re-anchor). | 22-06 '26 |
  | G4 — paid external API call | _n/a_. | 22-06 '26 |

## Amendments

- **Filename convention (prior, verbatim):** entries ~#165–#307 were stored as
  `journal-<v>-dd_05_dd-hhmm.md` / `register-<v>-dd_05_dd-hhmm.md` with the year
  field reading **27 / 28 / 29 / 30 / 31**.
  **Correction (№ 533):** year field set to **26** (2026) in all 225 files; day,
  month, and `hhmm` unchanged. Rationale: typo — the project's entire timeline is
  May–June 2026; no entry was authored in 2027–2031.
- **`doc/.serial-state.yml`** — 53 reference comments + counters (commit 546 / journal 533 / build 332) + anchor re-set 16:53 SGT 22-06-26.
- **`doc/VibeCodingRecord/` data + outputs** — 144 stale filename references corrected in place (not regenerated; see [KNOWN GAPS]).
- **`package.json`** — `0.61.331 → 0.61.332`.

## Deleted

_None._ No content removed — only the mis-stamped year corrected.
