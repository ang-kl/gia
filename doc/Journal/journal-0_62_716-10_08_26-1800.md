# Journal — v0.62.716 (Cost-audit Phase D: per-user caps reach the bot commands — plan complete)

_(№ 1,055 — 10-08 '26 18:00 SGT)_

## New

### [HDR] v0.62.716 — extend the rate limiter from 12 HTTP endpoints to the Telegram bot commands

- **[INTENT]** Fourth and final phase of the operator-approved cost-audit plan. The original audit found
  that `rate-limit.js`'s per-chatId limiter (v0.60.173 / DF-54) covered **only** the 12 HTTP
  `/api/cuisine/*` endpoints — every Telegram bot command (`/s`, free-text search, `/eat`, `/drink`,
  `/hidden`) was **completely uncapped per user**. Phase C caps what the *system* spends in a day; this
  phase caps what any *one user* can trigger. Neither substitutes for the other: without D, a single
  user can exhaust the daily budget under C and degrade the product for everyone else. That gap is
  precisely what this closes.

- **[THE EXTRACTION — SHARE THE MECHANISM, DON'T COPY IT]** The bot handlers have no `req`/`res`/`next`,
  so the Express middleware couldn't be reused as-is. Rather than hand-roll a second Redis counter (which
  would drift from the original the first time either side was touched), the counter core was extracted
  **verbatim** out of `makeRateLimiter`'s body into a bare `checkRateLimit(redis, { endpoint, key, cap,
  windowSec, now })`, and `makeRateLimiter` became a thin wrapper that delegates to it. Same
  `gia:rl:<endpoint>:<key>:<bucket>` key shape, same fixed-window semantics, same 429 body.
  **The strongest evidence the extraction was behaviour-preserving: all 12 pre-existing middleware tests
  — written against the pre-extraction implementation — pass completely unchanged.** No test was edited
  to accommodate the refactor.

- **[THE CAPS, AND WHY EACH NUMBER]** All on a 15-minute fixed window, matching the HTTP limiter's
  existing default:
  | endpoint | cap | reasoning |
  |---|---|---|
  | `bot-search` | 40 | Shared by `/s` turns AND free-text search — they cost the same (Places + Gemini) and a user can freely alternate, so a shared budget is the honest accounting. 40/15min is far above any real conversational pace. |
  | `bot-flow` | 40 | `/eat` + `/drink`. Its own counter so an `/eat` spree doesn't consume `/s`'s budget. |
  | `bot-hidden` | 8 | The priciest single command by a wide margin (grounded Gemini + up to 5 Places verifier lookups, uncached, with a retry ladder). Deliberately much tighter. |

- **[ONE ORDERING DETAIL THAT MATTERS]** In `runFlow` the rate-limit check is placed **after** the
  `isProcessing` lock check but **before** `setProcessing`. Putting it after `setProcessing` would leave
  a rate-limited user holding the processing lock with no work running to release it — they'd be told to
  wait, then told "still working on your last request" until the lock expired. Cheap to get wrong,
  annoying to debug from a user report.

- **[FAIL-OPEN THROUGHOUT]** `checkRateLimit` returns `{ limited: false }` on every abnormal path — dev
  bypass, missing key, Redis down, Redis error — with a `reason` field distinguishing "allowed because
  under cap" from "allowed because the check couldn't run". `botRateLimited` (the index.js helper that
  sends the refusal) wraps the whole thing in its own try/catch too. A broken limiter must never block a
  real user; that's the same posture as the pre-existing middleware and as Phase C's `readSpend`.

- **[DELTA]** (3 files)
  1. `rate-limit.js` — `checkRateLimit()` extracted from `makeRateLimiter`'s body; `makeRateLimiter`
     rewritten as a wrapper over it; both exported.
  2. `index.js` — `botRateLimited(chatId, endpoint, cap, lang)` helper (bilingual EN/FR refusal naming
     the cap and the minutes remaining, fail-open on any error), applied at 4 call sites:
     `handleSearchTurn` (`/s`), `runFreeTextSearch`, `runFlow` (`/eat` + `/drink`), and both `/hidden`
     entry points.
  3. `__tests__/rate-limit.test.js` — 10 new tests for the bare `checkRateLimit` (cap boundary, key
     shape, per-endpoint/per-key isolation, window rollover, all four fail-open paths, arg validation),
     appended below the 12 untouched middleware tests.

- **[VERIFICATION (sandbox)]**
  - `node --check` clean on both changed `.js` source files.
  - `npx vitest run __tests__/rate-limit.test.js` — **22/22** (12 pre-existing, unedited + 10 new).
  - Full `npm test -- --run` — **169 files / 4,144 tests** green (4,134 baseline + 10 new).
  - Verified `lang` is genuinely in scope at both `/hidden` call sites (it's a parameter of each
    enclosing function) rather than assuming it from the surrounding style.
  - No `web/` files touched — no TMA build needed.

- **[MUTATION-CHECKED]**
  | mutation | result |
  |---|---|
  | cap boundary `count > cap` → `count >= cap` | ❌ 4 tests, across BOTH the new bare-function suite and the pre-existing middleware suite |
  | `redis-down` fails CLOSED instead of OPEN | ❌ 2 tests, one from each suite |
  | both restored | ✅ 169 files / 4,144 tests green |
  Both mutations failing tests in the *pre-existing* suite as well as the new one is the useful signal
  here: it confirms the extracted core is genuinely the same code path the middleware still runs.

- **[TIME ANCHOR]** Sovereign fetch, `api.data.gov.sg/v1/environment/air-temperature`:
  `items[0].timestamp = 2026-08-10T18:00:00+08:00` = **10-08 '26 18:00 SGT** (TF-6/TF-9).

- **[STATUS]** DRAFT — branch `claude/handover-july-11-49uzvf`, PR #1705, which now carries **all four
  phases** (v0.62.713–716). The cost-audit remediation plan is complete as scoped.

- **[TEST]** Post-deploy: send 41 free-text searches inside 15 minutes and confirm the 41st returns the
  bilingual "hit the limit" refusal naming the remaining minutes, then confirm it clears after the
  window rolls. Run `/hidden` 9 times in a window and confirm the 9th refuses. Confirm `/eat` has its own
  budget (an exhausted `/s` budget must not block `/eat`). Confirm a French-locale user gets the FR copy.

- **[KNOWN GAPS]**
  - **The caps are per-chatId, not per-account or per-IP.** Consistent with the existing HTTP limiter,
    but it means the bound is on a Telegram chat, not a person.
  - **Cap values are reasoned, not measured.** 40/40/8 come from an argument about relative cost and
    plausible human pace, not from real usage percentiles — `/ftlog` and the Oversight usage log now have
    the data to tune them, which hasn't been done.
  - **Fixed window, not sliding** (inherited): a user can spend the full cap at the end of one window and
    again at the start of the next, briefly doubling the nominal rate. Same trade-off the HTTP limiter
    already accepted.
  - **Not exercised against a live bot** — no Telegram runtime in this sandbox; every path here is
    unit-tested against fake Redis, not observed end-to-end.
  - **Other bot commands remain uncapped** — `/carpark`, `/weather`, `/transport` and friends. Deliberate:
    they hit free government APIs (LTA / data.gov.sg), so they carry no per-call spend and were out of
    this plan's cost-driven scope.

## Confirmation Gates
| Gate | Status | Note |
|------|--------|------|
| G1 | ✅ | PATCH bump 0.62.715 → 0.62.716; server-only (no `web/` build); 169 files / 4,144 tests green; two mutations run, both red across old + new suites. |
| G2 | ➖ | no deletion — `makeRateLimiter`'s public contract is unchanged (same signature, same 429 body, same key shape), proven by its 12 pre-existing tests passing without edits; the new gates are additive and fail open. |
| G3 | ➖ | no new decision rule — this follows the approved plan's Phase D scope. The one judgement call (sharing `bot-search` between `/s` and free-text, separate `bot-flow` for `/eat`) is documented above and in the Register. |
| G4 | ➖ | no NEW external paid API call — this entry only ever PREVENTS calls. TF-6 anchor is the free sovereign `data.gov.sg` source. |

## Amendments

### [AMD-1] v0.62.713–716 — STATUS: DRAFT → PROD (post-merge, AU-7)

**Prior content, quoted verbatim (from `[STATUS]` above):**

> - **[STATUS]** DRAFT — branch `claude/handover-july-11-49uzvf`, PR #1705, which now carries **all four
>   phases** (v0.62.713–716). The cost-audit remediation plan is complete as scoped.

**Amended to:**

- **[STATUS]** **PROD** — Railway builds from `main`. PR **#1705** squash-merged as **`79e3d50`**,
  merge timestamp `2026-08-10T19:15:22+08:00` = **10-08 '26 19:15 SGT**, merged by the operator
  (`ang-kl`) after marking the PR ready for review. Final diff: **36 files, +2,030 / −110, 4 commits**
  (v0.62.713 Phase A, .714 Phase B, .715 Phase C, .716 Phase D).

  This single amendment covers all four versions — they shipped as one squash commit, so four separate
  merge records would imply four merge events that did not happen. The sibling entries
  (`journal-0_62_713`, `-714`, `-715`) each carry a short `[AMD-1]` pointing here rather than
  duplicating this block.

  **Verification state at merge:** the sandbox gate was green — 169 test files / 4,144 tests,
  `node --check` clean on every changed `.js`, and every new behaviour mutation-checked. This repo has
  no CI workflow reporting into PR checks (`total_count: 0` on the head commit throughout), so the
  sandbox run *is* the gate here; there is no runner-side confirmation to cite, unlike the v0.62.704
  precedent which had four green checks to quote.

  **Rationale for the amendment (AU-7):** the `[STATUS]` line was written while the PR was open and is
  now false. Per §4.1 the prior text is preserved above rather than overwritten.

  **What this does NOT change — and this matters more than usual for this arc.** Merging is not
  verification, and the honest gap here is wider than the code gate suggests. Every one of
  O-175 / O-176 / O-179 / O-182 still stands, and together they say the same thing from four angles:
  **not one line of this work has been observed against production.** Specifically, still unproven:

  | claim | status at merge |
  |---|---|
  | `/cost` now reports the ~35 newly-instrumented call sites | **unproven** — no live `/cost` read (O-175) |
  | Phase B removes a Places Details call per cold-sanctuary venue | **unproven** — unit-tested in isolation, never observed end-to-end against a real Places response (O-176) |
  | The spend guard sheds enrichment and DMs the owner at threshold | **unproven** — every level transition driven by synthetic Redis fixtures (O-179) |
  | Bot rate limits refuse at the cap with the right copy | **unproven** — no Telegram runtime in the sandbox (O-182) |

  The cap values (40/40/8, O-180) and the Routes `$0.005`/element price (O-172) also remain reasoned
  rather than measured. The first deploy is where the real numbers arrive; tuning those before seeing
  them would be guessing twice.

- **[TIME ANCHOR — AMENDMENT]** `2026-08-10T19:15:22+08:00`, PR #1705's own squash-merge timestamp
  (TF-9 merge evidence, same source class as the `pr_1682` / `pr_1684` / `pr_1687` / `pr_1697`
  precedents). Drift against this entry's `18:00 SGT` sovereign anchor is +75 min and is elapsed
  review-and-merge time, not clock error — no TF-7 correction is due. A fresh sovereign fetch at
  amendment time (`api.data.gov.sg`, `items[0].timestamp = 2026-08-10T19:30:00+08:00` = 19:30 SGT)
  corroborates the merge stamp as a lower bound (TF-10).
