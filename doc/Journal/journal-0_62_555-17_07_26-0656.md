(№ 912 - 17-07 '26 06:56 SGT) v0.62.555

# Journal — soleat v0.62.555 (Hawker tablet map: restore quick-toggle row · station card wider + pan-to-clear-top · tiny card padding)

> Operator (IMG_0692/0693/0694, iPad Pro landscape, v0.62.554), three points:
> 1. Lost ALL the quick buttons in the embedded map on tablet/desktop, every orientation.
> 2. In landscape, the station card clips over the top when the MRT station is near the top edge —
>    expand it horizontally (fewer wrapped lines) and/or move the map so the card is fully visible.
> 3. The bus-stop/station card has FAT white spacing between the border and the content; all cards in
>    landscape should have very tiny spacing.

## New

### [HDR] #912 | 06:56 SGT 17-07-26 | v0.62.555 — quick-toggle row + station-card fit + tiny padding | 4 files | PR #(pending) |

- **[FIX 1 — restore the quick-toggle row].** `web/hawker/src/components/MapControls.jsx` — the
  layer-toggle row (Monochrome/Train/Carpark/Bus Stop + ⋯) sat at `top-2`, **behind the carousel top
  bar** in the tablet/desktop full-bleed map, so it was invisible. New **`fill`** prop drops it below
  the top bar (`top: calc(--tg-content-safe-area-inset-top + 4.25rem)`). `HawkerMapPanel.jsx` passes
  `fill`, and the **left nav cluster moves down to `+ 7.5rem`** so it sits under the toggle row (was
  6rem — would have overlapped).

- **[FIX 2 — station card fits / doesn't clip the top].** `web/hawker/src/lib/mapOverlays.js`:
  - `infoCard` **max-width 248 → 340 px** so the tall station card wraps into fewer lines (the
    "expand horizontal" ask).
  - New `ensureInfoVisible(map, marker)` — an InfoWindow opens ABOVE its marker, so a marker in the
    top half of the viewport clips over the top; this pans the map DOWN (to ~0.6 viewport height) so
    the card drops into view. Called after the **station** card and **bus-stop** card open.

- **[FIX 3 — tiny card padding].**
  - `infoCard` padding **`9px 30px 9px 12px` → `5px 22px 5px 8px`** (the InfoWindow station/bus cards).
  - `web/hawker/src/App.jsx` — `renderCentreCard` gains a **`compact`** flag (`p-1.5 gap-0.5` vs
    `p-2.5 gap-1`); the carousel passes `compact` so the landscape/portrait-expanded centre cards get
    very tiny spacing. The portrait-panel + phone grids keep the roomier padding.

- **[DELTA — housekeeping].** `package.json` 0.62.554 → **0.62.555**; Journal #912 + serial-state
  (commit 1029→**1030**, build 654→**655**, journal 911→**912**; re-anchor 17-07 '26 06:56 SGT,
  prior 911 demoted verbatim AU-7).

- **[VERIFICATION (sandbox)].** `node --check mapOverlays.js` OK; **Hawker vite build green** (42
  modules); **full `npm test` 3719/3719 across 145 files**. ⚠️ Live-only (iPad) — shipping live.

- **[STATUS]** STAGED → PROD on merge (operator "make it live").

- **[KNOWN GAPS].**
  1. 👀 **iPad recheck** — the Monochrome/Train/Carpark/Bus Stop row is back (below the top bar, not
     overlapping the nav cluster)? Station card near the top now pans into view + wraps less? Card
     padding is tiny? The nav-cluster / toggle-row offsets (4.25rem / 7.5rem) may need tuning if the
     top bar wraps to a taller height.
  2. 🅺🅸🆅 Hawker CLEANING-closure still shelved (needs a data.gov.sg sample; sandbox egress-blocked).

## Confirmation Gates
| Gate | Status | Note |
|------|--------|------|
| G1 | ✅ | PATCH bump 0.62.554→0.62.555; UI fixes; build + 3719 tests green |
| G2 | ➖ | no destructive action |
| G3 | ➖ | no new decision rule |
| G4 | ➖ | no external paid API call |
