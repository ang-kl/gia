(№ 797 - 29-06 '26 14:15 SGT) v0.62.443

# Journal — soleat v0.62.443 (Sketchbook — professional filter dropdown, no bleed)

> Operator (2 screenshots): the filter dropdowns look unprofessional and bleed over the border.

## New

### [HDR] #797 | 14:15 SGT 29-06-26 | v0.62.443 — dropdown redesign | 2 files | PR #(pending) |
- **[ROOT CAUSE]** The full-width panel was "flush-attached" to a half-width chip (squared corners
  + header border removed) and had no inset → it read as broken and **bled to the screen edge**;
  the Cuisine panel also showed Rating/Price/Crowd facets with **zero cards** (clutter).
- **[DELTA]** `Shell.jsx` — chips are **always `rounded-xl`** (no corner-squaring); the header
  border is restored. Each filter panel is now a **detached, inset, fully-rounded card** (`mt-2
  rounded-xl border border-tg-border bg-tg-card shadow-lg`, `box-border w-full overflow-x-hidden`)
  that drops below the chip row — no bleed. When there are **no cards** (`cuisineOptions` empty),
  the Cuisine panel shows **only** "No saved cards to filter yet" (grid + facets hidden).
- **[VERIFICATION (sandbox)]** real `vite build` OK (50 modules).
- **[STATUS]** PROD — clipboard TMA rebuild.
- **[NEXT]** Operator rule restated: the Clipboard can create a **blank** ("sketchbook") card with
  full add/edit/delete/move — to build next.

## Confirmation Gates
| Gate | Status | Note |
|------|--------|------|
| G1 | ✅ | vite build |
| G2 | ⏳ | CI |
| G3 | ✅ | serial 796→797 |
| G4 | ⏳ | redeploy + eyeball |
