# Journal — v0.62.600 (Transport header/footer polish + live blinking SGT clock)

_(№ 944 — 19-07 '26 05:06 SGT)_

## New

### [HDR] v0.62.600 — Transport TMA: title trim, status-row live clock (blinking colon), tip removed, view toggle → footer

- **[INTENT]** Operator screenshot (IMG_0760) of the deployed v0.62.599 Train TMA, six concrete
  header/footer tweaks:
  1. Title → just **"🇸🇬 Train System"** (was "🇸🇬 Train Map & Status").
  2. Move the date/time onto the **same row as the "✓ All lines normal" status**.
  3. Date format → **"DD MMM HH:MM"** (24-hour).
  4. **Blink the ":"** so it reads as a running **Singapore-time** clock.
  5. Remove the third row — *"Tip: zoom in to read the pins…"*.
  6. Move the **Schematic / Google-Map view toggle** out of the header, **down to the footer bar**
     (the row with *down* / *end*).

- **[DELTA — client]** `web/transport/src/App.jsx`:
  - New **`LiveClock`** component: `Intl.DateTimeFormat` (`timeZone: 'Asia/Singapore'`,
    `hourCycle: 'h23'`, `formatToParts`) renders **"DD MMM HH:MM"**; a **500 ms** interval re-reads the
    time (minute stays current) and toggles the colon's visibility → a **once-a-second blink**. Renders
    in the row-1 right cluster: `status · clock · locale`.
  - **Removed** the standalone `data.timestampSGT` row, the zoom-tip row, and the header's
    Schematic/Google-Map **pill pair**.
  - The fixed **footer bar** gains a **single view-toggle pill** (left, beside the version): its label
    is the *other* view, and tapping flips `mapView` `png ↔ gmap`; *down* / *end* stay on the right.
  - i18n `header.title` updated in **all 8 locale blocks** (en "Train System", fr "Réseau
    ferroviaire", + id/ru/de/zh/ja/es). The now-unused `view.tipToGmap` / `view.tipZoomIn` strings are
    left in place (harmless; may be reused).

- **[VERIFICATION (sandbox)]** Transport `vite build` green (`index-C6onRdIP.js 262.21 kB │ gzip 85.46
  kB`); the `LiveClock` Intl format was checked in Node → **"19 Jul 05:06"** (exactly DD MMM HH:MM,
  SGT); full `npm test` **3738/3738 across 147 files**; `package.json` + `package-lock.json` 0.62.599 →
  0.62.600; `.serial-state.yml` commit 1079→1080, build 701→702, journal 943→944, re-anchor **19-07 '26
  05:06 SGT**. ⚠️ Live render is UI-only.

- **[TIME ANCHOR]** data.gov.sg blocked by the environment network policy (`403` `CONNECT`); anchored
  to the agent-proxy status timestamp `2026-07-18T21:06Z` = **19-07 '26 05:06 SGT** (TF-9 fallback;
  TF-10 lower bound) — **corroborated** by the `LiveClock` Intl render "19 Jul 05:06".

- **[STATUS]** DRAFT → squash-merge to `main` on green CI (auto-merge SQUASH). New PR.

- **[TEST]** No unit test added (a timer/`Date`-driven display component); the Intl format was
  spot-checked in Node. Existing suite unchanged and green.

- **[KNOWN GAPS]** The responsive scaffold (task #9 — `useViewport` two-panel / carousel + unified tap
  flow) is still pending an operator decision on how Transport's extra panels behave in the wide modes.
  Carried: the card's `mrt.*` strings + the closure tab are en/fr-lean; Cuisine `tg.js` safe-area gap;
  folio-tab folder connection.

## Confirmation Gates
| Gate | Status | Note |
|------|--------|------|
| G1 | ✅ | PATCH bump 0.62.599 → 0.62.600; build + 3738 tests green. |
| G2 | ⚠️→ok | UI elements removed per the operator's explicit instruction (timestamp row, tip row, header toggle pills) — relocated/replaced, not lost (the toggle moved to the footer; the clock supersedes the timestamp). |
| G3 | ➖ | no new decision rule. |
| G4 | ➖ | no external paid API call. |
