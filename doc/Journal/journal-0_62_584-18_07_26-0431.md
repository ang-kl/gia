# Journal — v0.62.584 (Cuisine: tighter map InfoWindow + folio-tab line/alignment)

_(№ 928 — 18-07 '26 04:31 SGT)_

## New

### [HDR] v0.62.584 — Cuisine: thin the map InfoWindow white ring + drop the tab/detail line + level the tabs

- **[INTENT]** Operator (IMG_3518/3519/3520, phone, deployed v0.62.583) clarified three items:
  1. *"The vertical alignment issues."*
  2. *"line drawn between tab header and tab detail are to remove."*
  3. *"The eatery card margins in between white spacing and card details border is too big, make it much thin white spacing."* — IMG_3520 is the **map InfoWindow popup** ("Aji Spice House"), with a thick **white** ring around our grey skeuomorphic `infoCard`.

- **[DELTA — all in `web/cuisine/src/styles.css`]**
  - **#3 (the repeatedly-flagged "awful card" — it was the InfoWindow, not the result card).** The thick
    white ring is Google's **default `.gm-style-iw` chrome** (`.gm-style-iw-c` ~12 px padding + the
    `.gm-style-iw-d` scroll wrapper). Added `.gm-style .gm-style-iw-c { padding: 2px; border-radius:
    15px }` and `.gm-style-iw-d { overflow: visible; padding: 0; max-width: none }` so our card nearly
    fills the bubble (`headerDisabled` already removed Google's top bar, so nothing else depends on it).
  - **#2.** Removed the folio-tab `border-bottom` (was `1px #d1cec9` on inactive tabs) — with the picker
    panel now a **detached fixed overlay**, that bottom border read as a stray horizontal line between
    the tab and its detail.
  - **#1 (best-guess).** The inactive tab's `transform: translateY(1px)` (a folder-pop offset) made it
    sit a hair lower than the active tab; flattened both to `translateY(0)` so the tab row is level.
    (If the operator means a different "vertical" issue, a tight crop of the two tab headers will pin it.)

- **[VERIFICATION (sandbox)]** Cuisine `vite build` green; full `npm test` **3719/3719 across 145
  files**; `package.json` 0.62.583 → 0.62.584; `.serial-state.yml` commit 1060→1061, build 683→684,
  journal 927→928, re-anchor 18-07 04:31 SGT. ⚠️ Live-only: the InfoWindow chrome + tab render need an
  on-device check.

- **[STATUS]** DRAFT → squash-merge to `main` on green CI (auto-merge SQUASH).

- **[TEST]** Existing suite 3719/3719 (CSS-only).

- **[KNOWN GAPS]** Dark-mode carousel-card glass (the v0.62.209 readability tradeoff — awaiting the
  operator's call); Transport TMA port; if #1 is a different "vertical" issue, awaiting a tab close-up.

## Confirmation Gates
| Gate | Status | Note |
|------|--------|------|
| G1 | ✅ | PATCH bump 0.62.583 → 0.62.584; Cuisine build + 3719 tests green. |
| G2 | ➖ | no destructive action (CSS padding/border/transform). |
| G3 | ➖ | no new decision rule. |
| G4 | ➖ | no external paid API call. |
