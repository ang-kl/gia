(№ 391 - 28-05 '26 02:15 SGT) v0.61.190

# Journal — soleat v0.61.190 (Cuisine TMA: two-line result-panel title)

> Operator: title `Results: 24 · Showing 1-12` was crowding the
> right-side `[Copy all]` + `[Copy syntax]` buttons. Wants:
>   Line 1: `Results: 24`
>   Line 2: `· Showing first 12`
> (with buttons on the right unchanged).
>
> Commit 403 / journal 391 / build 193, on branch
> `claude/v0.61.190-two-line-title` off `main` at v0.61.189.

## New

### [HDR] #391 | 02:15 SGT 28-05-28 | v0.61.190 — Two-line title | 4 files | 1 PR | 1 build |

- **[DELTA]**
  - **`web/cuisine/src/v2/lib/i18n.js`** — single-line
    `panel.range / panel.all / panel.limit` keys retired; new
    keys (EN + FR):
    - `panel.line1` = `Results: {known}` (the top line)
    - `panel.line2.first` = `· Showing first {first}` (first
      batch — matches operator's screenshot: `Showing 1-12` →
      `Showing first 12`)
    - `panel.line2.range` = `· Showing {start}-{end}`
      (subsequent pages)
    - `panel.line2.all` = `· Showing all` (single-page known)
    - `panel.line2.limit` = `· Limit reached` (SEEN_CAP)
    - `panel.discovering` unchanged (single-line fallback when
      total unknown).
  - **`web/cuisine/src/v2/components/ResultPanel.jsx`** — title
    render now returns either a string (discovering fallback)
    or a JSX fragment with two stacked `<div>`s. Line 1 uses
    the existing `text-xs font-semibold`; line 2 adds
    `text-tg-hint font-normal` so it reads as subordinate.
    `leading-tight` keeps the two lines compact.
  - **`package.json`** — `0.61.189 → 0.61.190`.
  - **`doc/.serial-state.yml`** — commit 402 → 403, build 192 →
    193, journal 390 → 391; re-anchored 28-05 '26 02:15 SGT.
  - **`doc/Journal/journal-0_61_190-28_05_26-0215.md`** — this entry.

- **[VERIFICATION (sandbox)]**

  ```
  $ cd web/cuisine && npm run build   (✓ built in 2.49s)
  ```

- **[STATUS]** PR-OPEN on push. Operator merges manually.

- **[TEST]** Operator (post-merge + Railway redeploy + TMA cache bust):
  - Run any cuisine search. Title now reads as two lines:
    ```
    Results: 24
    · Showing first 12
    ```
    Buttons stay on the right edge.
  - Page ▶ to page 2 → `Results: 24 / · Showing 13-24`.
  - Tap 🔍 to fetch more → `Results: 36 / · Showing 25-36`.
  - Tight search (≤12 venues) → `Results: 6 / · Showing all`.
  - SEEN_CAP reached → `Results: 100+ / · Limit reached`.

- **[KNOWN GAPS]**
  - **JSX fragment vs string returned from the IIFE**. React
    handles both cases inside the outer `<div>`. If you ever
    wrap the title in a different layout primitive, double-check
    both code paths still render.

- **Confirmation Gates**

  | Gate | Resolution | Date |
  |------|-----------|------|
  | G1 — version bump | PATCH `0.61.189 → 0.61.190`. Pure cosmetic title rewrite. | 28-05 '26 |
  | G2 — destructive action | _n/a_ — three i18n keys retired, but they were only referenced inside this file. | 28-05 '26 |
  | G3 — new decision rule | _n/a_ — same states as v0.61.174, just split across two lines. | 28-05 '26 |
  | G4 — paid external API call | _n/a_. | 28-05 '26 |

## Amendments

- **`panel.range` / `panel.all` / `panel.limit`** — single-line
  format retired (v0.61.174). Replaced by the two-line `line1`
  + `line2.*` scheme.
- **`text-xs font-semibold` single line** — now two stacked
  divs inside the same wrapper, with line 2 styled as
  subordinate (`text-tg-hint font-normal`).

## Deleted

- **i18n keys** `panel.range`, `panel.all`, `panel.limit` —
  superseded by the new `line1` + `line2.*` keys.
