(№ 677 - 11-06 '26 15:35 SGT) v0.62.26

# Journal — soleat v0.62.26 (country pick → capital, not nearest curated city to a stale pin)

> Operator: *"Malaysia and all countries when selecting the country should select capital — I am
> seeing Johor for Malaysia and Fukuoka for Japan. Is it my ChatID?"* Diagnosis (this session): YES
> — chat-state-driven, not random. On country switch, LocationField resolved the pre-selected city
> as (a) anchor-name match → (b) NEAREST curated city within 500 km → (c) capital. A stale
> cross-country anchor made (b) win: a Singapore set-location → Malaysia picked **Johor** (~15 km,
> beats KL ~316 km); a leftover near-Fukuoka pin (from the earlier Fukuoka Michelin testing) →
> Japan picked **Fukuoka** over Tokyo. The 500 km guard (v0.61.418) only catches anchors FAR from
> every city; it can't tell "I'm standing in Johor" from "I have a pin next door." Operator picked
> **option 1**: an explicit country-dropdown pick ALWAYS anchors to the capital; the nearest-city
> restore is for GPS / boot hydration only.

> Commit 694 / journal 677 / build 426 (web/ changed). PATCH 0.62.25 → 0.62.26.

## New

### [HDR] #677 | 15:35 SGT 11-06-26 | v0.62.26 — explicit country pick → capital | web/cuisine/src/v2/components/LocationField.jsx | 1 PR | 1 build |

- **[INTENT]** Operator (verbatim above) + the option-1 decision: selecting a country must land on
  the capital, regardless of a stale persisted anchor near a neighbouring non-capital city.

- **[DELTA]**
  - **`LocationField.jsx`** (country-change effect ~980) — capture
    `explicitCountryPick = userChangedCountryRef.current` and gate the (a) anchor-name match + (b)
    nearest-within-500 km restore behind `!explicitCountryPick`. On an explicit dropdown pick
    `picked` stays null → falls straight to `list[0]` (the capital, verified first in cities.js for
    every country). GPS / boot hydration (ref false) keeps the detected-city restore unchanged, so
    a user physically in-country still sees their real city on auto-detect.

- **[VERIFICATION (sandbox)]** Full suite **3361/3361** green (no test delta — view-layer
  resolution change). `web/cuisine` build green. Capital-first confirmed in cities.js (MY→Kuala
  Lumpur, JP→Tokyo, etc.).

- **[STATUS]** PR (rides the session branch). Railway on merge.

- **[KNOWN GAPS]**
  - Accepted tradeoff (operator's option 1): a user PHYSICALLY in a non-capital city who picks the
    country from the DROPDOWN (not GPS) now lands on the capital, not their city — they re-pick
    their city from the city dropdown. GPS auto-detect is unaffected.
  - Live WebView smoke owed: confirm MY→Kuala Lumpur and JP→Tokyo on an explicit pick with a
    pre-existing cross-country anchor, and that GPS in-country still restores the local city.

- **Confirmation Gates**

  | Gate | Resolution | Date |
  |---|---|---|
  | G1 — version bump | PATCH `0.62.25 → 0.62.26`. | 11-06 '26 |
  | G2 — destructive action | None — one resolution branch gated; nearest-city path retained for GPS/boot. | 11-06 '26 |
  | G3 — new decision rule | Explicit country-dropdown pick → capital; nearest-city restore is GPS/boot-only (operator option 1). | 11-06 '26 |
  | G4 — paid external API call | None. | 11-06 '26 |

## Amendments

- **`web/cuisine/src/v2/components/LocationField.jsx`** (explicit-pick → capital gate) ·
  **`package.json`** `0.62.25 → 0.62.26` · **`doc/.serial-state.yml`** counters + anchor.

## Deleted

- (none) — additive gate; the (a)/(b) nearest-city restore is preserved for GPS / boot hydration.
