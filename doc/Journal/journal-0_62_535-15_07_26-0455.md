(№ 891 - 15-07 '26 04:55 SGT) v0.62.535

# Journal — soleat v0.62.535 (Consolidation PR-2: web/_shared byte-identical components)

> Operator: *"Continue to finish these: Consolidation queue PR-2…PR-6."* PR-2 from
> `audit/consolidation-scope-2026-07-12.md` §5 — extract byte-identical TMA components to a new
> `web/_shared/` package. Verify-first (the scope's own "diff -q first" rule) trimmed the target list.

## New

### [HDR] #891 | 04:55 SGT 15-07-26 | v0.62.535 — web/_shared byte-identical components | 9 files | PR #(pending) |

- **[INTENT].** First consolidation extraction: de-duplicate genuinely byte-identical, self-contained
  TMA components into `web/_shared/`.

- **[VERIFY-FIRST — trimmed the scope's list].**
  - `md5sum`: **device-id.js diverged** (cuisine vs menu), **LocaleToggle.jsx** all 4 differ, cuisine's
    **WeatherBadge differs** → all excluded.
  - A build **spike** caught that **MapControls.jsx**, though byte-identical, `import`s
    `../lib/mapOverlays.js` — a **per-TMA** file (the PR-6 target) → not self-contained → **excluded**
    (folds into PR-6).
  - Net PR-2 = the **3** genuinely self-contained + byte-identical files.

- **[DELTA].**
  - **NEW `web/_shared/{components,lib}/`**:
    - `FooterNav.jsx` (react-only) — consumers hawker + menu; transport's copy was byte-identical but
      **unimported dead code** → deleted.
    - `WeatherBadge.jsx` (react-only) — consumers hawker + transport; **cuisine keeps its own divergent
      copy**.
    - `location-sync.js` (no imports) — consumers cuisine·v2 + menu.
  - 3 files `git mv`'d into `web/_shared`; 4 import sites repointed (`hawker`/`menu`/`transport`/
    `cuisine·v2` App.jsx, relative depths differ — cuisine·v2 is one level deeper); menu + transport
    duplicate/dead copies deleted.

- **[CROSS-ROOT BUILD — confirmed].** Each TMA is a **standalone Vite app** (no workspace), but Rollup
  bundles the `../../_shared/...` relative import at build time. Validated by building **all 4** affected
  TMAs.

- **[VERIFICATION (sandbox)].**
  - All 4 affected TMA `vite` builds green (cuisine / hawker / transport / menu).
  - `node --check web/_shared/lib/location-sync.js` OK.
  - **Full `npm test` → 3699/3699 across 144 files.**
  - Cuisine's own `WeatherBadge.jsx` intact (correctly not shared). Built `public/*` is git-ignored →
    diff is source moves + imports only.
  - `web/` changed → ran the 4 builds per `gia-preflight` §0.

- **[STATUS]** PROD — the version bump redeploys; the TMAs load the shared components (bundled per-app,
  identical output).

- **[TEST]** `web/` change → 4 TMA builds + full `vitest`. No new unit test (byte-identical relocation).

- **[KNOWN GAPS].**
  1. 🧩 **PR-3** (`lib/gemini` — 3 outside call sites), **PR-4** (`lib/redis-kv` — off-hot-path), **PR-5**
     (`web/_shared` build-config + `api.js` + i18n infra) — next in the queue.
  2. 🗺️ **PR-6** (`mapOverlays.js`) — **pause for the §4 decision** (high blast radius, 3 live maps);
     MapControls' extraction folds into it.
  3. 🚪 **CCL6 + Punggol Coast exit pins** — need the LTA exit file.

## Confirmation Gates
| Gate | Status | Note |
|------|--------|------|
| G1 | ✅ | PATCH bump 0.62.534→0.62.535; behaviour-preserving refactor |
| G2 | ➖ | deletions were byte-identical duplicates / unimported dead code (git-tracked, recoverable) |
| G3 | ➖ | no new decision rule |
| G4 | ➖ | no external paid API call |
