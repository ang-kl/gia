# Journal — v0.62.609 (Hawker drawer pills fix + Train phone drawer; BottomSheet shared)

_(№ 953 — 19-07 '26 13:26 SGT)_

## New

### [HDR] v0.62.609 — Hawker: seat the drawer's zone pills on a solid card · Train: fix the tall carousel card + give the phone the same draggable drawer

- **[INTENT]** Operator (IMG_3595 / IMG_3596), three asks in one message:
  1. *"hawker tma looks horrible"* — the v0.62.608 phone drawer's five **zone pills** floated
     **translucent** directly over the busy map (`bg-tg-bg/90 backdrop-blur … border`), reading as a
     messy doubled row.
  2. *"the train TMA card takes too much space"* — a Train station card in the carousel was stretched
     tall with a large empty white tail.
  3. *"merge when you set up the drawer"* — **set up the Hawker drawer for the Train TMA** (the
     green-lit follow-up from v0.62.608: *"if this is good, then Train TMA can take effect as well"*).

- **[DELTA — BottomSheet is now shared]** `git mv web/hawker/src/components/BottomSheet.jsx →
  web/_shared/components/BottomSheet.jsx` so **both** TMAs use one component. Hawker `App.jsx`'s import
  updated to `../../_shared/components/BottomSheet.jsx`; Transport `App.jsx` imports the same shared path.

- **[DELTA — Hawker "horrible" pills]** `web/hawker/src/App.jsx` `drawerLayout()`: the floating pills row
  moved **onto a solid `skeuo-card`** (`rounded-2xl px-2 py-1.5 pointer-events-auto`), just like the
  title card, so the pills no longer float translucent over the map. Unselected pills switched to the
  two-panel's clean **`bg-tg-bg/90 liquid-glass text-tg-text`** (no stray border).

- **[DELTA — Train tall card]** `web/transport/src/App.jsx` `StationCarousel`: the track was
  `flex items-stretch …`, which stretched **every** card to the tallest sibling's height (the empty
  white tail the operator flagged). Changed to **`items-start`** so each card sizes to its own content.

- **[DELTA — Train phone drawer]** `web/transport/src/App.jsx`: a new **phone-portrait drawer branch**
  (`vp.deviceClass === 'mobile' && vp.orientation === 'portrait'`, placed **before** the carousel
  default return) — a **full-bleed `fill` map** behind (`absolute inset-0 z-0`), a **floating header**
  (the existing `headerEl` on a `pointer-events-auto` card, `absolute top z-20`), and the shared
  **`BottomSheet`** holding the **vertical station list** (`lineStations → renderStationCard`, or the
  `singleFocusedCard`; `contentRef={listScrollRef}` / `onContentScroll={onListScroll}`). It's **empty on
  first load** (map only) until a line/station is chosen — matching the carousel. The footer **List
  toggle** (`viewMode === 'list'`) still wins; **tablets / landscape keep the bottom carousel**.

- **[VERIFICATION (sandbox)]** **Playwright** (phone 390) confirmed the Hawker pills now sit on a solid
  card (no translucent-over-map), and the Train drawer renders the floating header + full-bleed map +
  the draggable station-list sheet (HarbourFront / Outram Park / Chinatown … rich cards) — no page
  errors. **Hawker** `vite build` green (`index-lMeAUFYG.js 260.40 kB │ gzip 84.59 kB`); **Transport**
  `vite build` green (`index-DUfyu-Wl.js 270.87 kB │ gzip 88.23 kB`); full `npm test` **3738/3738 across
  147 files**; `package.json` + `package-lock.json` 0.62.608 → 0.62.609; `.serial-state.yml` commit
  1088→1089, build 710→711, journal 952→953, re-anchor **19-07 '26 13:26 SGT**.

- **[TIME ANCHOR]** data.gov.sg blocked by the environment network policy (`403` `CONNECT`); anchored
  to the agent-proxy status timestamp `2026-07-19T05:26Z` = **19-07 '26 13:26 SGT** (TF-9 fallback;
  TF-10 lower bound).

- **[STATUS]** DRAFT → squash-merge to `main` on green CI (auto-merge SQUASH). New PR (branch restarted
  from `origin/main` `f848401`, #1616 merged).

- **[TEST]** No new unit test (CSS-class / markup + a layout-branch change; verified visually via
  Playwright). Existing suite unchanged and green.

- **[KNOWN GAPS]** The Train drawer, like Hawker's, does **not** auto-expand when a map **pin** is
  tapped while the sheet is collapsed (the pin→snap-open refinement is still a shared follow-up). The
  Train footer's **⇡/⇣** scroll button still renders only in `list` mode (the drawer scrolls its own
  inner list); wiring it for the drawer is deferred. Landscape/tablet Train still use the bottom
  carousel (unchanged). Carried: the card's `mrt.*` / closure strings are en/fr-lean; Cuisine `tg.js`
  safe-area gap.

## Confirmation Gates
| Gate | Status | Note |
|------|--------|------|
| G1 | ✅ | PATCH bump 0.62.608 → 0.62.609; Hawker + Transport builds + 3738 tests green; both TMAs verified via Playwright. |
| G2 | ➖ | additive — `git mv` relocates BottomSheet to `_shared` (still tracked, imports updated); pills reseated, `items-stretch`→`items-start`; nothing removed. |
| G3 | ➖ | no new decision rule (the operator's greenlit follow-up + two direct fixes). |
| G4 | ➖ | no external paid API call. |
