(№ 542 - 06-06 '26 19:48 SGT) v0.61.342

# Journal — soleat v0.61.342 (Hong Kong Michelin — load the curated `instruction/HongKong.js` into `HK-michelin.js`; 147 venues; +1 CITY_IATA key; HK manifest)

> Operator: *"hongkong/macau"* → `instruction/HongKong.js`. **FLAG:** despite the "Hong Kong / Macau" guide name, the curated file has **no Macau venues** — Hong Kong only (147 venues, 1 city). JP/KR shape: **2025 partial** (19, upper tiers, no Bib) + **2026 full** (147).
>
> **Left OPEN for review — auto-merge NOT armed.** Commit 555 / journal 542 / build 341, landed via the GitHub Git Data API on origin/main @ v0.61.341.

## New

### [HDR] #542 | 19:48 SGT 06-06-26 | v0.61.342 — Hong Kong Michelin live: `HK-michelin.js` = 147 curated venues; +1 loader CITY_IATA key; HK manifest (19/147) | 2 code files | 1 PR | 1 build |

- **[INTENT]** v0.61.333 shipped `HK-michelin.js` as the empty stub. The operator's curated `instruction/HongKong.js` holds the full venue-centric Hong Kong dataset (**147 venues**, single city "Hong Kong"; 2025 + 2026 editions). This PR loads it **verbatim**, adds the missing city key, registers the HK manifest, leaves `instruction/HongKong.js` as source-of-record.

- **[MECHANISM]** Bare array literal of 147 venue objects (venue-award-schema.v0_1; no `postal`, no locale fields). Copied **byte-for-byte** into `HK-michelin.js`. The loader validates each `city` resolves in `CITY_IATA`; the HK section had **districts** (Central, Tsim Sha Tsui, …) but **not** the whole-territory name "Hong Kong" the data uses — so the loader would have thrown. Added `'hong kong':'HKG'` to `CITY_IATA`. `cities.js` / `iata-cities.js` untouched (CITY_IATA is the loader gate; the picker keeps its district rows; `HKG` already exists in `iata-cities.js`). **Macau is absent** from the dataset — flagged, and asserted absent in tests.

- **[DELTA]**

  **1. `HK-michelin.js`** (root) — empty v0.61.333 stub → `const COUNTRY='HK'; const ENTRIES=[ …147 venues verbatim… ]; module.exports={COUNTRY,ENTRIES}`. Header → v0.61.342 (source = `instruction/HongKong.js`; single "Hong Kong" city; explicit no-Macau note; 2025-partial manifest). 147 unique ids, 166 awards.

  **2. `michelin-data.js`** —
  - `CITY_IATA`: added `'hong kong':'HKG'` (territory) ahead of the HK district keys; comment relabelled "Hong Kong (territory + districts)".
  - `COUNTRY_MANIFEST` + `HK: { 2025:{three:7,two:11,one:1,total:19}, 2026:{three:7,two:13,one:57,bib:70,total:147} }`. 2025 omits Bib (none in source).

  **3. `__tests__/michelin-data.test.js`** — removed `HK` from the empty-tables loop (only `CN` remains); **flipped the false-gate** `'Hong Kong'` → `'Macau'` + `'MO'` (Macau genuinely not loaded); added a full HK load suite (147 venues / 166 awards, 19@2025 / 147@2026, per-tier manifest `{7,11,1}` + `{7,13,57,70}`, unique ids, every `city`==='Hong Kong' resolving to HKG, 8 1/2 Otto e Mezzo - Bombana 3★ both editions, **an explicit "NO Macau venues" test** — every HK venue city is "Hong Kong" and `venuesForCountry('MO')===0`), a `HK-michelin.js is venue-centric with 147 rows` require test, an `hasMichelinData` HK-true case, and a `hasMichelinData` Macau/MO-false case.

  **4. `package.json`** — `0.61.341 → 0.61.342`.

  **`doc/.serial-state.yml`** — commit 554 → 555, journal 541 → 542, build 340 → 341; re-anchored 19:48 SGT 06-06-26.

- **[VERIFICATION (sandbox)]**
  - `node --check` clean: `HK-michelin.js`, `michelin-data.js`, `__tests__/michelin-data.test.js`.
  - Real-loader temp-harness (all 8 `{CC}-michelin.js` + patched `michelin-data.js` in `/tmp/hktest`): loads **without throwing**. `venuesForCountry('HK')===147`; global unique ids **1382/1382** (MY 70 + TH 180 + JP 589 + KR 117 + TW 196 + VN 83 + HK 147, no dup); HK 2025=19 / 2026=147; tiers `{three:7,two:11,one:1}` (2025) + `{three:7,two:13,one:57,bib:70}` (2026); `sum(HK awards)===166`. HK manifest passes. `hasMichelinData` true for HK/Hong Kong; **false** for Macau/MO (and `venuesForCountry('MO')===0`); false for Shanghai/CN.
  - **14/14** explicit assertions green.
  - Full `npm test` runs in CI on merge.

- **[STATUS]** PR-**OPEN**, **auto-merge NOT armed**. Landed via the GitHub Git Data API on a branch off origin/main @ v0.61.341. Railway redeploys on merge.

- **[TEST]** Operator: Hong Kong is now in the venue loader — 147 venues, the full 2026 edition (147) + the partial 2025 upper tiers (19). **Macau is NOT included** (the curated file is Hong Kong only). With HK loaded, only **China** remains an empty stub. `instruction/HongKong.js` stays source-of-record.

- **[KNOWN GAPS]**
  - **Macau missing** — despite the "Hong Kong / Macau" guide name, the source has no Macau venues. A Macau dataset is **still due** (would be its own ISO-2 `MO` table + city key).
  - **2025 is PARTIAL** — 19 venues, upper tiers only (no Bib). A fuller 2025 HK list is **still due**.
  - **No native-name / postal fields** — none stored (source has none).
  - **Unified loader still off the live search path** — MY/TH/JP/KR/TW/VN/HK via `michelin-data.js` not yet wired into the bot's Michelin search (SG standalone).
  - A `doc/Feature/` / `doc/Technical/` / `doc/Register/` update + a `vault/v0.61.342/` snapshot are still due.

- **Confirmation Gates**

  | Gate | Resolution | Date |
  |---|---|---|
  | G1 — version bump | PATCH `0.61.341 → 0.61.342`. | 06-06 '26 |
  | G2 — destructive action | None. Empty `HK-michelin.js` stub filled; append-only CITY_IATA + manifest + test edits. Curated `instruction/HongKong.js` untouched. Loader clean (1382/1382 unique). | 06-06 '26 |
  | G3 — new decision rule | Hong Kong Michelin loads from `instruction/HongKong.js` (147 venues) VERBATIM; `'hong kong'→HKG` added to loader CITY_IATA; HK manifest 2025 19 (partial) / 2026 147. Macau is NOT in this dataset (separate `MO` table still due). | 06-06 '26 |
  | G4 — paid external API call | _n/a_. Data load + CITY_IATA/manifest registration only. | 06-06 '26 |

## Amendments

- **`HK-michelin.js`** — empty stub → 147 curated venues from `instruction/HongKong.js` (verbatim); header → v0.61.342.
- **`michelin-data.js`** — `CITY_IATA` +`'hong kong':'HKG'`; `COUNTRY_MANIFEST` +`HK` (2025 19 / 2026 147).
- **`__tests__/michelin-data.test.js`** — HK removed from empty-tables loop; false-gate flipped 'Hong Kong' → 'Macau'/'MO'; +HK load suite (incl. explicit no-Macau test) + `HK-michelin.js` 147-row require test + `hasMichelinData` HK-true + Macau-false cases.
- **`package.json`** — `0.61.341 → 0.61.342`.
- **`doc/.serial-state.yml`** — counters (commit 555 / journal 542 / build 341) + anchor re-set 19:48 SGT 06-06-26.

## Deleted

- _None._
