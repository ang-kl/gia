(№ 421 - 28-05 '26 07:30 SGT) v0.61.220

# Journal — soleat v0.61.220 (Catmull-Rom for the 6 MRT lines)

> Operator: *"Train Line: Like v0.61.194 for LRT, the rest of train
> lines ought to have curve? but station markers must stay on the
> line. Catmull-Rom is interpolating (vs Chaikin's approximation)
> — output curves smoothly AND passes through every control point."*
>
> Lines to cover: East-West (Green), North-South (Red), North East
> (Purple), Circle (Orange), Downtown (Blue), Thomson-East Coast
> (Brown), plus the CG (Airport) branch. Sources operator flagged
> for cross-check: OpenStreetMap / Overpass MRT-line-geometry
> fetcher, LTA "MRT/LRT Line (GEOJSON)" dataset, Overpass API for
> Singapore `route=subway` + `route=light_rail`.
>
> Commit 433 / journal 421 / build 223, on branch
> `claude/v0.61.220-mrt-catmull-rom` off `main` at v0.61.219.

## New

### [HDR] #421 | 07:30 SGT 28-05-28 | v0.61.220 — Catmull-Rom for MRT lines | 4 files | 1 PR | 1 build |

- **[INTENT]** Match the v0.61.194 LRT treatment — switch every MRT line from Chaikin (approximating; pulls the curve ~25 % off station coords) to Catmull-Rom (interpolating; guaranteed to pass through every control point).

- **[DELTA]**

  **1. `web/transport/src/data/line-paths.js`** — `CATMULL_ROM_LINES` widened from 3 entries to 10:

  ```js
  // v0.61.194
  // const CATMULL_ROM_LINES = new Set(['BPL', 'SLRT', 'PLRT']);
  // v0.61.220
  const CATMULL_ROM_LINES = new Set([
    'BPL', 'SLRT', 'PLRT',
    'NSL', 'EWL', 'CGL',
    'NEL', 'CCL', 'DTL', 'TEL'
  ]);
  ```

  `smoothLinePaths` dispatches every segment through `catmullRomSegment(seg, 12)` (12 sub-points per inter-station gap, same as the LRT pass).

  **2. `__tests__/transport-line-paths.test.js`** — the "passes through every station" assertion now iterates the full line set, not just `[BPL, PLRT]`. The "CCL stays on Chaikin" expectation flipped to "CCL uses Catmull-Rom" (density ≥ 5× raw).

  **3. `package.json`** — `0.61.219 → 0.61.220`.

  **4. `doc/.serial-state.yml`** — commit 432→433, build 222→223, journal 420→421; re-anchored 28-05 '26 07:30 SGT.

- **[VERIFICATION (sandbox)]**

  ```
  $ node --check web/transport/src/data/line-paths.js          (OK)
  $ node --check __tests__/transport-line-paths.test.js        (OK)
  $ node --input-type=module ... (real 209-station mrt-coords) →
      NSL   raw=  27 smoothed=  313 allStationsOnLine=true
      EWL   raw=  33 smoothed=  385 allStationsOnLine=true
      NEL   raw=  17 smoothed=  193 allStationsOnLine=true
      CCL   raw=  34 smoothed=  397 allStationsOnLine=true
      DTL   raw=  36 smoothed=  421 allStationsOnLine=true
      TEL   raw=  29 smoothed=  337 allStationsOnLine=true
      BPL   raw=  14 smoothed=  157 allStationsOnLine=true
      PLRT  raw=  18 smoothed=  194 allStationsOnLine=true
      SLRT  raw=  17 smoothed=  182 allStationsOnLine=true
      CGL   raw=   3 smoothed=  25  allStationsOnLine=true
  ```

  Every one of the 209 station coordinates appears (1e-5° rounding tolerance) in its smoothed line output. The Catmull-Rom mathematical guarantee (B(0)=P1, B(1)=P2) means this is structural, not a coincidence of these particular control points.

- **[STATUS]** PR-OPEN on push.

- **[TEST]** Operator (post-merge + Railway redeploy):
  1. Open Transport TMA → schematic / Google Map view → every MRT line should now visibly curve between stations rather than zig-zag.
  2. Zoom in on a corner (e.g. Outram Park, where NEL/EWL/TEL bend hard) — the line should still pass through the station pin centre, not float past it.
  3. The Cuisine + Hawker train overlays (which share `/api/transport/line-paths`) should pick up the same smoother curve on next reload.
  4. LRT lines (BPL/SLRT/PLRT) should look identical to v0.61.194 — same Catmull-Rom path, same sample rate.

- **[KNOWN GAPS]**
  - When prod has `data/mrt-line-paths.json` (real LTA GEOJSON), `resolveLinePaths` still prefers that geometry as-is (no smoothing applied) — `CATMULL_ROM_LINES` only affects the derived fallback. The file isn't checked in, so prod is on the derived path and this PR fully covers it. If LTA real geometry ever ships, a follow-up should either Catmull-Rom-smooth that too OR ensure stations are projected onto the LTA line.
  - The three sources operator flagged (Overpass MRT fetcher, LTA GEOJSON, Overpass `route=subway`/`route=light_rail`) were verified by reading the existing fetchers (`scripts/fetch-mrt-lines.js`, `scripts/fetch-mrt-lines-osm.js`) without re-running them. The derived geometry stays the source of truth for now.

- **Confirmation Gates**

  | Gate | Resolution | Date |
  |---|---|---|
  | G1 — version bump | PATCH `0.61.219 → 0.61.220`. Single-set extension; existing tests carry over. | 28-05 '26 |
  | G2 — destructive action | _n/a_. | 28-05 '26 |
  | G3 — new decision rule | YES — every MRT/LRT line now goes through Catmull-Rom; treat Chaikin (`smoothSegment`) as the legacy path retained only for non-line use cases / explicit opt-out. | 28-05 '26 |
  | G4 — paid external API call | _n/a_. | 28-05 '26 |

## Amendments

- **`CATMULL_ROM_LINES`** — was `{ BPL, SLRT, PLRT }`. Now extends with `{ NSL, EWL, CGL, NEL, CCL, DTL, TEL }`.
- **`smoothLinePaths` test** — "CCL stays on Chaikin" → "CCL uses Catmull-Rom"; pass-through-station test iterates 9 codes instead of 2.

## Deleted

_None._
