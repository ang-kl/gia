(№ 707 - 12-06 '26 15:39 SGT) v0.62.52

# Journal — soleat v0.62.52 (clear city plate on country/city change + tighten place-autocomplete bias; BP LRT diagnosed)

> Operator (Putrajaya screenshots): "the city dishes should clear once country and city is change";
> "i entered ioi city mall, keeping malaysia > putrajaya. it drift to KL". Plus item 1 (BP LRT) closed
> as a diagnosis (the loop already draws complete in code).

> Commit 724 / journal 707 / build 445 (web/ changed). PATCH 0.62.51 → 0.62.52. PR #1009 (draft).

## New

### [HDR] #707 | 15:39 SGT 12-06-26 | v0.62.52 — clear stale plate + autocomplete bias + BP LRT diagnosis | web/cuisine (App.jsx, LocationField.jsx, lib/api.js) + index.js | 1 PR | 1 build |

- **[INTENT]** (11) Wipe the city/cuisine plate the moment the country or city changes, so a prior
  city's dishes never linger. (12) Stop "IOI City Mall" (Putrajaya) resolving to KL. (1) Diagnose the
  BP LRT "incomplete" report.

- **[DELTA]**
  - **(11) `web/cuisine/src/v2/App.jsx`** — `onCountryChange` now wipes BOTH `arrivalPlate` and
    `cuisinePlate` (it previously cleared neither); the city-pick wipe (`onLocationSelect`, v0.62.33)
    also clears `cuisinePlate` now. Root cause: switching VN→MY via the country dropdown left the
    Hanoi plate over Putrajaya because only the city-PICK path wiped, and only `arrivalPlate`.
  - **(12) `index.js` `/api/cuisine/place-autocomplete`** — accept `radiusM`; use it as the
    `locationBias.circle.radius` (clamped 3–50 km, default 50 km) + fold into the cache key. The flat
    50 km bias reached the more-prominent "IOI Mall Puchong" (~15 km, near KL) and outranked "IOI City
    Mall Putrajaya". **`lib/api.js`** `placeAutocomplete` forwards `radiusM`; **`LocationField.jsx`**
    passes the picked city's `cityRadiusCapM(...)` (Putrajaya ~12 km) so same-city places rank higher.
  - **(1) BP LRT** — NO code change. Traced data→buildLinePaths→smoothLinePaths→`/api/transport/
    line-paths`→client `resolveLinePaths`→`renderPolylines`; reproduced the EXACT served geometry and
    rendered it to PNG (shared with operator): one continuous polyline through all 13 stations, closing
    on BP6 Bukit Panjang (~1 m smoothing overshoot). All BP stations lack an `exit_centroid` so the map
    uses the correct mrt-coords; `data/mrt-line-paths.json` (LTA override) is absent in repo/prod;
    `LINE_SEQUENCES.BPL` unchanged since v0.61.393. Conclusion: not reproducible from code — almost
    certainly a STALE cached `web/transport` bundle in the Telegram WebView; the #1008 deploy's fresh
    asset hashes bust it. Awaiting operator re-check / live `/api/transport/line-paths`.

- **[VERIFICATION (sandbox)]** `node --check` clean (index.js); full suite **3438/3438** green;
  `web/cuisine` build green.

- **[STATUS]** **DRAFT** — PR #1009 (rides `claude/refine-local-plan-5vh1jm`). Railway on merge.

- **[TEST]** Others → MY → Putrajaya after a VN/Hanoi search → the plate is EMPTY (no Hanoi dishes)
  until a Putrajaya search fills it. Type "IOI City Mall" with Putrajaya picked → IOI City Mall
  Putrajaya ranks above IOI Mall Puchong (needs live Places to confirm ranking).

- **[KNOWN GAPS]**
  - (12) is a soft `locationBias` tune — Google bias is a hint, not a hard filter. If it still drifts,
    the next lever is a hard `locationRestriction` circle at the city radius.
  - (1) pending operator verification on the fresh deploy; if still broken, need the live API response.

- **Confirmation Gates**

  | Gate | Resolution | Date |
  |---|---|---|
  | G1 — version bump | PATCH `0.62.51 → 0.62.52`. | 12-06 '26 |
  | G2 — destructive action | None — plate clears are additive UI state resets. | 12-06 '26 |
  | G3 — new decision rule | Plate wipes on country change (not just city pick); autocomplete bias = picked city's radius. | 12-06 '26 |
  | G4 — paid external API call | None new — same Places autocomplete SKU, only a tighter bias radius. | 12-06 '26 |

## Amendments

- **`web/cuisine/src/v2/App.jsx`** (plate wipes) · **`web/cuisine/src/v2/components/LocationField.jsx`**
  (radiusM) · **`web/cuisine/src/v2/lib/api.js`** (radiusM passthrough) · **`index.js`** (autocomplete
  bias radius + cache key) · **`package.json`** `0.62.51 → 0.62.52` · **`doc/.serial-state.yml`**.

## Deleted

- (none) — additive.
