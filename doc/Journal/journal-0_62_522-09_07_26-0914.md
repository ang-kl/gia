(№ 878 - 09-07 '26 09:14 SGT) v0.62.522

# Journal — soleat v0.62.522 (Language & Locale Specification — the missing doc)

> Operator: *"i thought there is a spec on language"*. There was not. The strategy was real but
> **unwritten** — carried in code-file header comments and five `SUPPORTED` arrays. This entry records
> authoring the normative spec, and the two false alarms that proved it was needed.

## New

### [HDR] #878 | 09:14 SGT 09-07-26 | v0.62.522 — Language & Locale Specification | 4 files | PR #(pending) |

- **[INTENT].** The operator asked to spec the locale program "so we can continue after reboot".
  Investigation showed the program is **already complete** (Phases 1–3 shipped), and that the real
  missing artifact is not a resume plan but a **normative language specification**. Author it.

- **[DELTA].**
  - `doc/Technical/technical-0_62_522-09_07_26-0914.md` — **NEW.** The spec. §1 the authoritative
    8-locale set + the 7 declaration sites; §2 the two-tier model (hand-authored master, generated
    overlay, merged at load); §3 what is/isn't translated (Proper-Noun Policy; `cooking-methods.js`
    excluded; translation deliberately **ungrounded**); §4 pipeline mechanics (`MODEL_CHAIN`,
    `BATCH_SIZE=14`, 429-is-not-a-cascade-trigger); §5 the 11-step add-a-language procedure;
    §6 related records. Introduces rules **L-1 … L-11**. Deprecated table entry #1 retires
    "strategy lives in a code header" in favour of this document.
  - `doc/Journal/journal-0_62_522-09_07_26-0914.md` — this entry.
  - `doc/.serial-state.yml` — commit 995→**996**, build 621→**622**, journal 877→**878**,
    technical 56→**57**; re-anchor 09-07 '26 09:14 SGT (prior 877 anchor demoted verbatim, AU-7).
  - `package.json` — 0.62.521 → **0.62.522** (standing per-PR bump; doc-only still bumps).

- **[TWO FALSE ALARMS — why this doc exists].** Both were produced by reading the **stale iCloud tree**:
  1. A stale plan file (`~/.claude/plans/hazy-riding-pinwheel.md`, since deleted with operator
     approval, gate G2) asserted *"1,274 dishes, only 33 carry a note — ~97 % bare"*. `grep -c "note:"`
     on `nation-overlay.js` returns exactly **33**, which *appeared* to confirm it. It does not: 33 is
     the **hand-authored** count. The other ~1,241 live in `nation-overlay-dishnotes.generated.js`
     (~347 KB) and are merged at load. The backfill has been complete since v0.62.449. Codified as **Rule L-4**.
  2. The stale tree also showed Menu/Hawker/Transport/Clipboard capped at 5 locales, implying task #16
     had been reverted. `origin/main` shows all five surfaces at the full 8. Codified as **Rule L-2**.

- **[EXCLUSIONS / HONESTY].** §1.2 records that **no market or reach rationale for the specific eight
  languages exists anywhere in the repo**, and marks it `[UNVERIFIED]` rather than reconstructing a
  plausible-sounding justification. Closing that gap requires an operator decision record, not inference.

- **[VERIFICATION (sandbox)].** Every claim in the spec is cited to `origin/main` at `069cb7c9`
  (v0.62.521), read via `gh api …?ref=main`, not the working tree:
  - `i18n.js:19` `SUPPORTED = ['en','fr','id','ru','de','zh','ja','es']`
  - `web/cuisine/src/v2/lib/i18n.js:1645`, `web/menu/src/i18n.js:15`, `web/hawker/src/i18n.js:17`,
    `web/transport/src/i18n.js:15`, `web/clipboard/src/lib/i18n.js:246,:261` — all 8 locales
  - `scripts/translate-content.mjs:40` `TARGET_LANGS = ['id','ru','de','zh','ja','es']`
  - Proper-Noun Policy, `cooking-methods.js` exclusion, and "no googleSearch … must not be grounded"
    quoted verbatim from the `translate-content.mjs` header
  - `nation-overlay-dishnotes.generated.js` and `-i18n.generated.js` both present

- **[STATUS]** PROD — merges to main; the per-PR bot redeploys. Doc-only; no runtime surface touched.

- **[TEST]** No `index.js` / handler / search / render / `i18n.js` **code** change → outside the
  `gia-preflight` trigger set; no web build. Full `npx vitest run` in CI.

- **[KNOWN GAPS].**
  1. 🌐 **Language rationale unrecorded** — §1.2 is `[UNVERIFIED]`. Needs an operator decision record
     stating why these eight (and which come next).
  2. 📘 **User-Manual translations** — `docs/UserManual/translation-status.json` shows every non-EN
     locale `not_started`. Out of scope here; the manual is a separate surface from the app.
  3. 🗺️ **#1525** — CCL6 launch flip still HELD for 12 Jul 2026.

## Confirmation Gates
| Gate | Status | Note |
|------|--------|------|
| G1 | ✅ | PATCH bump 0.62.521→0.62.522; doc-only change, per the standing per-PR rule |
| G2 | ✅ | destructive action = deletion of the stale local plan file; operator chose option 1 explicitly. No repo file deleted |
| G3 | ✅ | new rules L-1…L-11 proposed in a Technical doc; Deprecated table entry #1 retires the code-header-as-spec convention |
| G4 | ➖ | no external paid API call. Reach/MAU rationale deliberately left `[UNVERIFIED]` rather than researched-and-asserted |
