(№ 806 - 01-07 '26 11:00 SGT) v0.62.452

# Journal — soleat v0.62.452 (Sketchbook — Pick local classic mirrors Cuisine TMA)

> Operator: "Pick Local Dish … identical to Cuisine TMA … grey out those not in the eatery cards"
> (delivery: dishes = this PR; city derived from saved cards).

## New

### [HDR] #806 | 11:00 SGT 01-07-26 | v0.62.452 — Pick local classic + plate endpoint | 6 files | PR #(pending) |
- **`index.js`** — new **`GET /api/cuisine/plate?city=`** → `city-plates.platesForCity()`.
  Forgiving match: exact `CITY_PLATES` key → case-insensitive → query-contains-key (so
  "Katong, Singapore" resolves to Singapore). Read-only, echoes no user text → no auth.
- **`web/clipboard/src/components/LocalClassicPicker.jsx`** (NEW) — mirrors the Cuisine TMA
  ArrivalPlate geo mode: `📍 {city}` header, a 2-col dish grid (English name + native local
  script), and a **"More {city} classics ({N})"** expander. Dishes **not** in the saved cards are
  greyed + non-tappable; tapping a saved dish sets the dish filter.
- **`App.jsx`** — derives the city from saved cards (most-common trailing comma-segment of
  `venue.area`), fetches the plate for it, builds `savedDishSet` from the v0.62.450 `dishOptions`.
- **`Shell.jsx`** — the "Pick local classic" dropdown renders `LocalClassicPicker` when a plate is
  available, else falls back to the v0.62.450 saved-dish chips (unknown city).
- **`i18n.js`** — `classic.tapHint`, `classic.more` (en/fr/id/ru/de).
- **[VERIFICATION (sandbox)]** `esbuild` parse (App, Shell, LocalClassicPicker); `node --check`
  index.js + i18n; real `vite build` OK (52 modules). Full vitest gated by CI.
- **[STATUS]** PROD — server + clipboard TMA rebuild on merge.
- **[KNOWN GAPS]** City derivation is a heuristic over `venue.area`; unknown cities fall back to
  saved-dish chips. Dropdown-✕ pass (LocaleToggle ×3 + MapControls) still staged, not shipped.

## Confirmation Gates
| Gate | Status | Note |
|------|--------|------|
| G1 | ✅ | vite build 52 modules + node --check |
| G2 | ⏳ | full vitest via CI |
| G3 | ✅ | serial 805→806 |
| G4 | ⏳ | redeploy + operator eyeball |
