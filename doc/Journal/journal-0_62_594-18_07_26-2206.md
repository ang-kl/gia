# Journal — v0.62.594 (Cuisine portrait tablet: frozen listing header + independently-scrolling columns)

_(№ 938 — 18-07 '26 22:06 SGT)_

## New

### [HDR] v0.62.594 — Cuisine portrait-tablet listing: freeze the header, scroll the two columns independently ("map stays")

- **[INTENT]** Operator (iPad Pro portrait, Cuisine): *"in listing style, freeze the header of the
  listing … the two columns are scroll independent, and independently expand when user tap."* Chose
  **"map stays"** — keep the ~40 vh map pinned; the frozen header + independent columns fill the rest.

- **[DELTA — App]** `web/cuisine/src/v2/App.jsx`. `boundList = portraitWide && drawerMode === 'vertical'`.
  A `useLayoutEffect` measures the results wrapper's own top and bounds its height to
  `calc(var(--tg-viewport-stable-height, 100dvh) - <top>px - 3rem)` with `overflow:hidden`, re-measured
  on `headerBottom` / `venues` / orientation / picker-open. The wrapper gains `flex flex-col min-h-0`, and
  `boundedColumns={boundList}` is passed to `ResultPanel`. This bounds only the panel — the shared page
  container is untouched, so phones + landscape are unchanged.

- **[DELTA — ResultPanel]** `web/cuisine/src/v2/components/ResultPanel.jsx`. `bounded = boundedColumns &&
  columns === 2`. Root → `flex flex-col flex-1 min-h-0 overflow-hidden`; the **"Showing N" header row →
  `shrink-0`** (freezes); the list container → `grid grid-cols-2 grid-rows-[1fr_auto] flex-1 min-h-0`
  (dropping `items-start` so the columns stretch into the `1fr` row); the two odd/even split columns get
  `overflow-y-auto min-h-0 no-scrollbar` (**independent scroll**); the **✔ Confirmed / ? Ask-first dish
  titles get `sticky top-0 z-10 bg-tg-bg`** (freeze per column); the loading/pagination strips
  (`col-span-2`) drop into the `auto` row **beneath** the scrolling columns. Independent-expand already
  worked (each column is its own flex flow).

- **[VERIFICATION (sandbox)]** Cuisine `vite build` green; full `npm test` **3719/3719 across 145 files**;
  `package.json` + `package-lock.json` 0.62.593 → 0.62.594; `.serial-state.yml` commit 1072→1073, build
  695→696, journal 937→938, re-anchor 18-07 '26 22:06 SGT. ⚠️ **LIVE-ONLY and needs an on-device
  screenshot** — the bounded-height + sticky + independent-scroll layout can't be verified in the
  sandbox (no browser render). Branch rebased onto `origin/main` after #1601 merged.

- **[STATUS]** DRAFT → squash-merge to `main` on green CI (auto-merge SQUASH).

- **[TEST]** Existing suite 3719/3719. The layout is CSS/measurement behaviour (verified by build only).

- **[KNOWN GAPS]** The Michelin `awardCity` grouped branch (rare in portrait-vertical) returns a single
  `cardNodes` flow, not two scroll columns, so its bounded layout is untested — likely wants a follow-up
  if the operator hits a Michelin search in portrait tablet. Cuisine `tg.js` safe-area gap (mirror of
  v0.62.591); folio-tab folder connection; dark-mode carousel glass; Transport port.

## Confirmation Gates
| Gate | Status | Note |
|------|--------|------|
| G1 | ✅ | PATCH bump 0.62.593 → 0.62.594; Cuisine build + 3719 tests green. |
| G2 | ➖ | no destructive action. |
| G3 | ➖ | no new decision rule (applies the operator's confirmed "map stays" layout). |
| G4 | ➖ | no external paid API call. |
