(№ 865 - 06-07 '26 22:34 SGT) v0.62.509

# Journal — soleat v0.62.509 (delete the dead legacy v1 Cuisine TMA — audit change #2, workspace step 1)
<!-- 17 files removed total: App.jsx + api/search.js + components/ ×11 + state/ ×4 -->


> Second operator-approved audit change ("drop 1. do 2, Expand 3 …" → the workspace/consolidation
> track, step 1: retire dead code before consolidating shared TMA modules). The map-first v2
> Cuisine app has been the validated production default since v0.53.0; the legacy chip-grid v1
> was reachable only through a `?v=1` query-param gate with no live deep-links.

## New

### [HDR] #865 | 22:34 SGT 06-07-26 | v0.62.509 — remove dead v1 Cuisine TMA (17 files) + drop the `?v=1` gate | 4 changed / 17 deleted | PR #(pending) |

- **[INTENT].** The 06-07 4-auditor audit flagged `web/cuisine/src/App.jsx` (the legacy chip-grid
  v1 app) as dead code: the only entry point is `main.jsx`'s `?v=1` branch, and a search found
  **no live deep-links** that pass `?v=1` (only stale mentions in old journal entries). v2
  (map-first flip-card) has shipped as the default since v0.53.0. Operator approved the workspace
  track ("Expand 3"); this is its first, lowest-risk step — delete the dead app so the later
  `packages/tma-core` consolidation isn't done against two copies.

- **[DELTA].**
  - `web/cuisine/src/main.jsx` — dropped the `requestedVersion` / `?v=1` gate; `boot()` now always
    `import('./v2/App.jsx')`. Stale-chunk self-heal + theme apply unchanged.
  - **DELETED (17 v1-only files):** `App.jsx`, `api/search.js`, `components/` ×11 (Header,
    RangeSlider, ModeDropdown, TimeDropdown, CuisineAccordion, OtherCuisineInput, PresetCombos,
    PromptPreview, ResultsGrid, Diagnostics, VenueCard), `state/` ×4 (cuisine-i18n.js, cuisines.js,
    diagnostics.js, useCuisineState.js). v2 has its own `ResultCard.jsx`; nothing in `v2/` imports
    the root `components/` or `state/` (build stayed green after removing both dirs).
  - **KEPT (shared with v2):** `api/tg.js`, `styles.css`, `main.jsx`, the whole `v2/` tree.
  - `doc/Journal/journal-0_62_509-06_07_26-2234.md` — this entry.
  - `doc/.serial-state.yml` — commit 982→**983**, journal 864→**865**; build unchanged 615;
    anchor re-set 22:34 SGT.
  - `package.json` — 0.62.508 → **0.62.509**.

- **[REACHABILITY — verified this PR].** `main.jsx` was the sole importer of `./App.jsx`.
  Traced v2's imports: it pulls `api/tg.js` only — **not** the root `components/` or `state/`
  directories. `git grep` over `__tests__` found **no test** referencing the deleted paths. No
  live surface passes `?v=1`.

- **[VERIFICATION (sandbox)]** `npm install && npm run build` inside `web/cuisine/` → **BUILD_OK**
  after the delete + `main.jsx` edit (v2 bundle emits clean). `node --check` N/A (JSX). CI's TMA
  build check re-gates on the PR.

- **[STATUS]** PROD — merges to main; redeploys the Cuisine TMA bundle (v2 unchanged; only the dead
  v1 branch removed, so no user-facing behavior change).

- **[TEST]** Full `npm test` unaffected (no backend `.js` touched; no frontend tests exist). 4
  required CI checks gate the PR.

- **[KNOWN GAPS]** Workspace track step 2 — `packages/tma-core` npm workspace (extract the
  byte-identical MapControls / FooterNav, unify BackFab keys, then tg.js ×6 / i18n.js ×5 /
  mapOverlays ×3) — **still due**; it changes the build system for all 7 TMAs (Railway postinstall),
  so it warrants its own deliberate PR/confirmation, not an autonomous bundle. Also still-due:
  gitignore `vibe-journal.html` behind a build hook; Redis job lock on the 11 schedulers.
  O-49/O-50/O-51, DF-1 (12-Jul MRT flip) unchanged.

## Confirmation Gates
| Gate | Status | Note |
|------|--------|------|
| G1 | ✅ | build green post-delete; version 0.62.508→0.62.509 |
| G2 | ✅ | dead code only — v1 unreachable (no live `?v=1`); git-recoverable |
| G3 | ✅ | serial commit 982→983, journal 864→865; anchor 06-07 '26 22:34 SGT |
| G4 | ➖ | no user-facing surface change (v2 was already the default) |
