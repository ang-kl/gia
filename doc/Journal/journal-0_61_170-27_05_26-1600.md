(№ 371 - 27-05 '26 16:00 SGT) v0.61.170

# Journal — soleat v0.61.170 (TMA counter rewrite: 24/12 split, cap 100, range-based copy, Michelin reformat)

> Operator UX review of the v0.61.163 `Results (12/19)` counter
> uncovered three concerns: (a) the `/` format implies a known
> total when none exists for cuisine search, (b) the v0.61.163
> unified 19-cap loses the v0.60.191 first-tap-richer / follow-up-
> cheaper split, (c) Michelin's `(12/130)` could read more
> actionably.
>
> Operator's proposal: 24 first / 12 follow-up; cap 80 → 100;
> range labels like "Showing first 24 · More available, click 🔍"
> and "Result 25-36 · click 🔍 for next more"; explicit
> "Change criteria or tap ↺" recycle.
>
> I asked 4 clarifying questions; operator picked:
> 1. Cap 80 → 100. ✓
> 2. Tight-combo first-tap: "Showing first {n} (only) · Change
>    criteria or tap ↺" (with count). ✓
> 3. Range labels adjust to actual range on tight tails (e.g.
>    "Result 37-41" not "37-48" when only 5 returned). ✓
> 4. Chat free-text "Search 🔍 for more" button: build full
>    chat-side seen-set + button + recycle. Ships separately as
>    v0.61.171 (next PR).
>
> Commit 383 / journal 371 / build 173, on branch
> `claude/v0.61.170-tma-counter-rewrite` off `main` at v0.61.168
> (commit `03555ce`, PR #669). Version skips `.169` because PR
> #670 (v0.61.169 live-count-templates) was still open at branch
> time; sequence resolves cleanly on the operator's merge order.

## New

### [HDR] #371 | 16:00 SGT 27-05-27 | v0.61.170 — TMA counter rewrite (24/12 split, cap 100, range labels) | 7 files | 1 PR | 1 build |

- **[INTENT]** Replace the v0.61.163 `Results (X/Y)` slash format
  with honest range-based labels keyed to the new 24/12 session
  model. Restore the v0.60.191 split (richer first tap, cheaper
  follow-ups) with new values. Make recycle an explicit operator
  action ("Change criteria or tap ↺") instead of a silent
  autoReset.

- **[DELTA]**
  - **`cuisine-session.js`**: `SEEN_CAP` 80 → 100. The 24 + 8×12
    arithmetic gives ~120 max if every tap is full; cap 100
    means recycle prompt lands around tap 7-8. PAGES_CAP retained
    at 10 with a slightly larger buffer.
  - **`index.js`** (`/api/cuisine/search`):
    - Slice: `const FIRST_TAP_SLICE = 24; const FOLLOW_UP_SLICE = 12;`
      replaces the v0.61.163 `RESULT_SLICE = 19`. Returns to the
      v0.60.191 `isFirstBatch ? FIRST : FOLLOW` ternary with new
      values.
    - Response payload adds three fields:
      - `cumulativeStart` (1-based; `seen.size + 1`)
      - `cumulativeEnd` (`seen.size + top.length`)
      - `finalBatch` (`top.length > 0 && trulyUnseen.length <=
        sliceCap && atLastVariant`) — true when this tap drains
        the unseen pool for the current criteria-variant.
    - `D700` log line surfaces the new fields.
  - **`web/cuisine/src/v2/components/ResultPanel.jsx`**:
    - 4 new props: `cumulativeStart, cumulativeEnd, finalBatch,
      firstBatch`.
    - Header IIFE rewritten with 3-mode logic:
      - **Michelin** (`totalCount` set):
        `Results (130) · Showing first 12` (first batch)
        `Results (130) · Showing 13-24` (range)
        `Results (130) · Final 9 shown` (finalBatch / exhausted)
      - **Cuisine first tap** (`firstBatch=true`):
        more available → `Showing first 24 · More available,
          click 🔍`
        tight-combo (exhausted) → `Showing first 15 (only) ·
          Change criteria or tap ↺`
      - **Cuisine subsequent tap** (`firstBatch=false`):
        more available → `Result 25-36 · click 🔍 for next more`
        final → `Result 37-41 · No more matching · Change criteria
          or tap ↺`
  - **`web/cuisine/src/v2/App.jsx`**:
    - 3 new state vars: `cumulativeStart`, `cumulativeEnd`,
      `finalBatch`.
    - Setters wired in both the live-response handler and the
      snapshot replay path.
    - `<ResultPanel>` receives `firstBatch / finalBatch /
      cumulativeStart / cumulativeEnd` props.
    - `autoResetOnLowCount = false` — the v0.60.188 / v0.61.167
      logic is fully retired. Recycle is now an explicit operator
      action via the ↺ button in the header copy.
    - Orphaned "N results for these criteria. Tap 🔍 to refresh."
      hint indicator (line ~1484 in v0.61.168) removed — double-
      messages the new range header.
  - **`web/cuisine/src/v2/lib/i18n.js`** — 8 new keys covering
    the 4 cuisine states + 3 Michelin states + 1 generic
    `panel.noMore` for callsite reuse. EN + FR both ship.
  - **`package.json`** — `0.61.168 → 0.61.170` (skips `.169`
    while PR #670 open).
  - **`doc/.serial-state.yml`** — `commit 381 → 383`, `build
    171 → 173`, `journal 369 → 371`; re-anchored 27-05 '26
    16:00 SGT.
  - **`doc/Journal/journal-0_61_170-27_05_26-1600.md`** — this entry.

- **[VERIFICATION (sandbox)]**

  ```
  $ node --check cuisine-session.js                          (OK)
  $ node --check index.js                                    (OK)
  $ node --check web/cuisine/src/v2/lib/i18n.js              (OK)
  $ esbuild --loader:.jsx=jsx --log-level=error \
      web/cuisine/src/v2/App.jsx > /dev/null                  (OK)
  $ esbuild --loader:.jsx=jsx --log-level=error \
      web/cuisine/src/v2/components/ResultPanel.jsx > /dev/null (OK)
  ```

  No new tests — the changes are constants + render-template
  shape. The v0.61.161 widening / v0.61.162 anchored / v0.61.167
  autoReset (now retired) tests are still valid for their respective
  modules.

- **[STATUS]** PR-OPEN on push. Operator merges manually.

- **[TEST]** Operator (post-merge):
  - **Cuisine first tap** with full pool: header reads `Showing
    first 24 · More available, click 🔍`. Card list shows 12
    (page 1 of 2 via the existing PAGE_SIZE strip).
  - **Tap 🔍 again**: header reads `Result 25-36 · click 🔍 for
    next more` (assuming pool has 36+ matches).
  - **Continue tapping**: each tap advances the range by 12. The
    100-cap session limit is reached around tap 7-8; sessionFull
    triggers the existing terminal note.
  - **Tight combo** (e.g. Italian + Halal + $ + walking): first
    tap returns 15 with `finalBatch=true`. Header reads `Showing
    first 15 (only) · Change criteria or tap ↺`.
  - **Mid-session final** (tap 4 returns 5 of 12): header reads
    `Result 37-41 · No more matching · Change criteria or tap ↺`.
  - **Michelin** (cuisine = 'michelin'): first tap reads
    `Results (130) · Showing first 12`; subsequent `Showing
    13-24`; final tap `Final 9 shown`.
  - **No autoReset**: tapping 🔍 on a tight-combo result no
    longer silently resets the seen-set. The operator's explicit
    ↺ recycle is the only path to fresh rotation.

- **[KNOWN GAPS]**
  - **Chat free-text dedup + button** (v0.61.171) — operator
    picked the full build (chat seen-set + "Search 🔍 for more"
    button + recycle copy mirror). Filed as the next PR.
  - **Cost impact** — per-session arithmetic for an average
    5-tap session:
      24 (first) + 4 × 12 (follow-ups) = 72 venues.
      Was 5 × 19 (v0.61.163) = 95 venues.
      Routes Distance Matrix saves ~23 venues × $0.005 = $0.115/session.
      Sessions to recycle: 5-6 (was 4-5 at the 80 cap).
  - **The `📄 X/Y` pagination strip inside ResultPanel** (◀ / ▶
    page nav) still shows page-math like `📄 12/24`. That's
    accurate (loaded venues, not session) and untouched.
  - **v0.61.169 PR #670 live-count-templates** is still open at
    branch time. Both PRs touch `index.js` + `package.json` +
    serial. Whichever merges first, the other rebases. No content
    conflicts (PR #670 changes setMyCommands + /start + new
    endpoint; this PR changes cuisine-search slice).
  - **No automated test for the counter render** — would need
    React Testing Library + i18n mocks. The four render branches
    are simple enough to inspect manually; manual smoke covers
    the happy path. Filed as future tooling work.

- **Confirmation Gates**

  | Gate | Resolution | Date |
  |------|-----------|------|
  | G1 — version bump | PATCH `0.61.168 → 0.61.170` (skips .169 while PR #670 open). New backend response fields + counter copy + cap bump. | 27-05 '26 |
  | G2 — destructive action | _n/a_ — pure additions + a slice ratio change (19 → 24/12) that is behaviourally compatible (cumulative venues delivered remain ≤ 100). The `autoResetOnLowCount` retirement removes a silent-reset behaviour that surprised users in sparse areas (v0.61.167 already softened); this PR fully retires it. | 27-05 '26 |
  | G3 — new decision rule | YES — the counter copy + 24/12 split + cap 100 are now operator-confirmed contracts. The v0.61.163 slash format is retired. | 27-05 '26 |
  | G4 — paid external API call | Net SAVING — 5-tap session goes from 95 venues enriched to 72. Per-session Routes cost drops ~$0.115. | 27-05 '26 |

## Amendments

- **`RESULT_SLICE = 19`** (v0.61.163) — split back into `FIRST_TAP_SLICE
  = 24` + `FOLLOW_UP_SLICE = 12` per operator's preference for
  richer first impression + cheaper follow-ups.
- **`SEEN_CAP = 80`** (v0.60.146) — bumped to 100.
- **`autoResetOnLowCount` gate** (v0.60.188 → v0.61.167) — fully
  retired. The v0.61.167 `!firstBatch` carve-out was a stopgap;
  the new range counter + explicit ↺ recycle make the auto-reset
  redundant.
- **`Results (X/Y)` header format** (v0.61.163) — retired. Replaced
  by range labels for cuisine, repurposed for Michelin's curated-
  pool framing.
- **"N results for these criteria. Tap 🔍 to refresh." hint** —
  removed. The new range header carries the equivalent signal
  explicitly.

## Deleted

- v0.61.167 `FOLLOW_UP_THIN_THRESHOLD = 19` constant (now
  unreferenced after `autoResetOnLowCount = false`).
- v0.60.188 hint indicator block (lines 1517-1528 in
  v0.61.168).
