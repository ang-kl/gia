(№ 919 - 17-07 '26 16:34 SGT) v0.62.567

# Journal — soleat v0.62.567 (Cuisine responsive tablet/desktop port — O-54 first half — + doc/VCR catch-up)

> Operator, across this session: "Auto-build 0-54" → then a rapid on-device (iPad) iteration loop
> making the **Cuisine TMA** seamless across phone / tablet / tablet-mini / desktop, mirroring the
> Hawker responsive pattern ("follow hawker codes"). Closed with **"Vault first, vibe code, journal.
> Then go ahead with approach"** → this doc pass (vault re-affirm + VCR regen + this Journal), before
> resuming the next build (the header/tabs-float-over-interactive-map rework).

## New

### [HDR] #919 | 16:34 SGT 17-07-26 | v0.62.561→0.62.567 — Cuisine responsive port (O-54) | 7 code PRs + this doc pass | PRs #1571–#1577 |

- **[INTENT].** Port the Hawker responsive tablet/desktop pattern to the Cuisine TMA (O-54, "Cuisine
  first — highest reuse"). Cuisine already had the hard parts from its own v0.62.190 redesign (a
  full-bleed `fill` map, the `ResultDrawer` swipe-carousel, clean `focusedPlaceId` pin↔card sync), so
  the work was a **targeted enhancement, not a rewrite** — plus a live iteration loop from the
  operator's iPad screenshots.

- **[DELTA] — one PR per operator turn (all tablet/desktop changes gated to `isWide`; phones unchanged):**
  - **#1571 · v0.62.561** — `web/cuisine/src/api/tg.js` fullscreen gate widened from `ipados`-only to
    the Hawker Rule N-2 touch-tablet gate (`ipados/ios/android` + coarse pointer + short screen edge
    ≥ 768; Desktop/macOS still excluded). `ResultDrawer.jsx` + `App.jsx`: `basisClass` → 2 cards in
    focus on iPad-mini / 3 on iPad-Pro+desktop; `useViewport`/`viewportTag` wired; footer device cue.
  - **#1572 · v0.62.562** — landscape carousel drops `blinkOnly` on wide so a card tap zooms 17 + POPS
    the pin InfoWindow (Hawker `activateCentre` parity); `ResultDrawer` IntersectionObserver → the
    in-focus band renders OPAQUE, the half-peeking end cards GLASS (`ResultCard` new `glass` prop);
    the "Cuisine & filters"/"Pick local classic" folio tabs + panels capped to a phone-width column.
  - **#1573 · v0.62.563** — the sticky `<header>` pads its top by `--tg-content-safe-area-inset-top`
    on wide so the title + location row clear Telegram's ⌄/··· system buttons in fullscreen.
  - **#1574 · v0.62.564** — results follow ORIENTATION on wide (landscape → carousel, portrait →
    list) via an effect; the manual list/map toggle hidden in wide-landscape; map stays full-bleed
    while a picker is open on wide.
  - **#1575 · v0.62.565** — the portrait list renders in TWO columns (`ResultPanel` new `columns`
    prop → `grid grid-cols-2`; section headers `col-span-2`).
  - **#1576 · v0.62.566** — the two columns are now INDEPENDENT: `ResultPanel` builds its card nodes
    once then splits them into two `flex flex-col` columns (1,3,5… / 2,4,6…) so an expanding card only
    pushes its OWN column (the grid had aligned rows → gap under a short row-mate).
  - **#1577 · v0.62.567** — the Hawker two-panel: in portrait a static ~40 vh map is pinned
    (`sticky top-[headerBottom]`, `display:contents` off-portrait) with the two-column list scrolling
    beneath it; the map's ⇲ expands to the full carousel and ⇱ collapses back (`MapPanel` new
    `frameHeight`/`onExpandFull`/`onCollapse`); the footer map/list toggle shown in portrait.
  - **This doc pass (v0.62.567, doc-only):**
    - **Vault** — RE-AFFIRMED RETIRED (Register D-134; `vault/` git-ignored since v0.62.508 / #1514).
      The effective "v0.62.567 snapshot" is the merged `main` sha **`3c398e7`** (recoverable via
      `git show 3c398e7:<path>`); no committed `vault/v<N>/` dir (would re-introduce the #1514
      redundancy).
    - **Vibe Coding Record** — REGENERATED: appended PRs **#1552–#1577** (26 rows: the Hawker arc
      #1553–#1569, the doc PR #1570, and this Cuisine arc #1571–#1577) to
      `doc/VibeCodingRecord/data/prs.ndjson` + `data/pr-files.tsv` from the squash-merge commits, bumped
      `GEN_DATE` comment, ran `generate.mjs` → `records.tsv` (1548 rows), `vibe-coding-record.md`, and
      the hosted `public/doc/vibe-journal.{html,json}`.

- **[VERIFICATION (sandbox)] — per code PR:** `node --check` on each changed `.js`; Cuisine `vite build`
  green; full `npm test` (`vitest run`) **3719/3719 across 145 files** on every PR; `package.json`
  0.62.560 → 0.62.567. ⚠️ Live-only: the tablet layouts can't be driven in-sandbox (no Telegram
  webview) — every PR shipped for on-device iPad verification, which drove the iteration loop.
  This doc pass: doc/generated only — **no `.js` TMA change → no node --check / vitest / web build**;
  version UNCHANGED (0.62.567, doc-only per the #1552/#1570 precedent).

- **[STATUS]** PROD — #1571–#1577 all squash-merged to `main` (HEAD `3c398e7`). This doc pass STAGED →
  PROD on merge.

- **[TEST]** Code PRs: existing suite 3719/3719 (no new unit tests — the changes are layout/CSS +
  device-gated behaviour, verified live). Doc pass: n/a.

- **[KNOWN GAPS].**
  1. 👀 **The "map as a layer behind everything, all devices, always"** request (operator, from
     IMG_0722/0723): a half-built fixed-background attempt was **reverted** (it either covered the
     results or made the map un-tappable — a true interactive map-background needs an app-wide
     pointer-events overlay pass). NEXT: the safe approach — make the header + tabs + location + picker
     **float over an interactive full-bleed map** in all modes (header↔map layering rework), operator
     approved ("go ahead with approach").
  2. 🧭 **Result-card border / inner-margin** defect (operator: "margin left+top, none right+bottom") —
     still awaiting a single-card close-up to diagnose the exact CSS.
  3. 🧭 **Transport TMA** port — O-54 second half, not started.
  4. 🅳🅾🅲 Feature/Technical/Register templates for this Cuisine arc not updated this pass (operator
     chose Vault + VCR + Journal); still available on request.

## Confirmation Gates
| Gate | Status | Note |
|------|--------|------|
| G1 | ✅ | across the arc: PATCH bumps 0.62.560→0.62.567 per code PR; Cuisine build + 3719 tests green each time. This doc pass: version UNCHANGED (doc-only). |
| G2 | ➖ | no destructive action (VCR regen is append + regenerate; the reverted fixed-background attempt was never committed). |
| G3 | ⚠️ | vault-retirement RE-AFFIRMED (D-134) despite the "vault" request — a committed vault would re-add the #1514 redundancy; recorded the git sha as the snapshot instead. |
| G4 | ➖ | no external paid API call. |
