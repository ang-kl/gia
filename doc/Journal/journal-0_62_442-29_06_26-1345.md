(№ 796 - 29-06 '26 13:45 SGT) v0.62.442

# Journal — soleat v0.62.442 (Sketchbook — Cuisine-TMA folio doctrine + edit-zoom + footer default)

> Operator (stacked): chips should replicate the Cuisine TMA folio drawer; dimensions bleed over
> the border; editing fields zooms in; effects should match Cuisine; close other overlays when
> editing; footer tab 2 ({default cabinet}) didn't follow spec.

## New

### [HDR] #796 | 13:45 SGT 29-06-26 | v0.62.442 — filter dropdown doctrine + fixes | 3 files | PR #(pending) |
- **No bleed** `Shell.jsx` — the header drops its bottom border + padding when a chip-drawer is
  open, so the panel ends flush (the exact trick the Cuisine TMA uses).
- **Cuisine look** — the Cuisine & Filter panel is a **contained 2-col chip grid**
  (`box-border w-full overflow-x-hidden`) of the cuisines present in the clips, with the facet
  rows (rating/price/crowd/open/Michelin) below; the dish panel is contained too.
- **Slide effect** `styles.css` `.sk-drop` keyframe — slide-down + fade, mirroring the Cuisine
  folio drawer.
- **No edit-zoom** — global `input, textarea, select { font-size: 16px }` kills the iOS
  focus-zoom (drawer title, dish filter, etc.).
- **One overlay at a time** — opening the hamburger closes the drawer; footer nav closes it; the
  drawer state is single-valued (cuisine ↔ dish already mutually exclusive).
- **Footer tab 2** `clipboard-routes.js` — `/state` `defaultCabinetId` falls back to the most-recent
  cabinet when none is set, so the tab always shows a real cabinet name (item 1).
- **[VERIFICATION (sandbox)]** real `vite build` OK (50 modules); `node --check` cr.js.
- **[STATUS]** PROD — server + clipboard TMA rebuild.

## Confirmation Gates
| Gate | Status | Note |
|------|--------|------|
| G1 | ✅ | vite + node --check |
| G2 | ⏳ | CI |
| G3 | ✅ | serial 795→796 |
| G4 | ⏳ | redeploy + eyeball |
