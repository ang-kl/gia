(№ 190 - 16-05 '26 11:00 SGT)

# Journal — soleat v0.60.215

> Operator, three items in one turn: (1) change the `freetext.questionDeclined` copy; (2) question — why did PR #464's TMA footer show `v0.60.4` instead of the real version; (3) frame each TMA footer in a bottom border showing `Experimental · Singapore · v0.60.215`.

## New

### [HDR] #183 | 11:00:00 SGT 16-05-26 | v0.60.215 — TMA footer border + build-version fix + freetext-decline copy | 9 files | 1 PR | 4 TMA builds |

- **[INTENT]**
  1. The `/s` / free-text question-decline message needed the operator's exact new copy.
  2. **Build-number bug** — PR #464's footer showed `v0.60.4` on the Cuisine / Train / Hawker TMAs. Root cause: their `vite.config.js` injected `__BUILD_VERSION__` from the **local** `web/<tma>/package.json`, whose `version` had rotted at `0.60.4`. Only `web/menu/vite.config.js` read the **root** `package.json` (the v0.60.50 fix was never propagated to the other three).
  3. The footers were borderless centred text — the operator wants a framed bottom box for clearer UI.

- **[DELTA]**

  **(1) `i18n.js`** — `freetext.questionDeclined` replaced with the operator-supplied copy (EN verbatim; FR translated, dish names kept verbatim):
  - EN: `🍛 Please try a dish name, cooking method, or food term - e.g. Mee Soto, char kway teow, or goulash dumpling`
  - FR: `🍛 Essayez un nom de plat, une méthode de cuisson ou un terme culinaire - par ex. Mee Soto, char kway teow ou goulash dumpling`
  - The prior copy carried `<i>` markup + a `/cuisine` pointer; the new copy is plain (no markup) — still safe under `parse_mode:'HTML'` (no `<`/`>`/`&`).

  **(2) Build-version fix** — `web/cuisine/vite.config.js`, `web/transport/vite.config.js`, `web/hawker/vite.config.js`: `import pkg from './package.json'` → `import rootPkg from '../../package.json'`; `__BUILD_VERSION__` now `JSON.stringify(rootPkg.version)`. Mirrors the v0.60.50 fix already in `web/menu/`. All four TMA footers now display the deployed app version. Verified — the rebuilt cuisine bundle contains `0.60.215`.

  **(3) Bordered footer** — `web/cuisine/src/v2/App.jsx`, `web/transport/src/App.jsx`, `web/hawker/src/App.jsx`, `web/menu/src/App.jsx`: each footer element gains `mx-2 mb-2 mt-… border border-tg-border rounded-lg px-3 py-2` — a framed box. The text is unchanged (`{footer.tag}` / `{footer.experimental}` + `· v{BUILD_VERSION}`); with the (2) fix it now reads `Experimental · Singapore · v0.60.215`.

  **`package.json`** — `0.60.214 → 0.60.215`. **`doc/Journal/journal-0_60_215-16_05_26-1100.md`** — this entry (#183). **`doc/.serial-state.yml`** — `journal 182→183`, `commit 189→190`, `build 2→3`; re-anchored.

- **[VERIFICATION (sandbox)]**

  ```bash
  $ node --check i18n.js                                   → OK
  $ (cd web/{cuisine,transport,hawker,menu} && npm run build)  → all 4 built clean
  $ grep -ro "0.60.215" public/cuisine/assets/*.js          → present (build-version fix confirmed)
  $ npm test -- --run                                       → 1865 passed (1865)
  ```

- **[STATUS]** STAGED → PROD on next Railway deploy. The `public/<tma>/` bundles are gitignored and rebuilt on deploy; the build-version fix takes effect on that deploy build (which reads the root `package.json` at `0.60.215`).

- **[TEST]** Post-deploy:
  1. `/s` or free-text a question-shaped query → the new `🍛 Please try a dish name…` line.
  2. Open each TMA → the footer is now a bordered box reading `Experimental · Singapore · v0.60.215` (Cuisine keeps its how-to line above; Menu keeps its `Soleat … · 2026` line).
  3. The Cuisine / Train / Hawker footers show `v0.60.215`, not `v0.60.4`.

- **[KNOWN GAPS]**
  - The local `web/cuisine|transport|hawker/package.json` `version: "0.60.4"` fields are now unused for the footer (the vite configs read the root). Left as-is — harmless; a future cleanup could delete or sync them.
  - DF-110-class follow-ups and the free-text food-relatedness gate (reviewed in the prior turn) remain not-built — awaiting the operator's detection-approach decision.

## Confirmation Gates

| Gate | Authority | Stamp |
|---|---|---|
| G1 — version bump | _PATCH 0.60.214 → 0.60.215._ | auto |
| G2 — destructive action | _None._ | n/a |
| G3 — new decision rule | _Operator: TMA footers are framed in a bordered box; the build version displayed must be the deployed (root) version, not the stale local TMA `package.json`._ | operator |
| G4 — paid external API call | _None._ | n/a |

## Amendments

| # | Item | Prior (verbatim) | Amended to | Date | Rationale | Rule | Serial № |
|---|---|---|---|---|---|---|---|
| F-1 | `i18n.js` `freetext.questionDeclined` EN | `"🙂 <i>I can't answer questions in free text yet.</i> Type a dish or cuisine name (e.g. <i>chiffon cake</i>, <i>laksa</i>, <i>ramen</i>), or use /cuisine to pick from a list."` | `"🍛 Please try a dish name, cooking method, or food term - e.g. Mee Soto, char kway teow, or goulash dumpling"` | 16-05 '26 | Operator-supplied replacement copy. | AU-7 | (№ 190 — 16-05 '26 11:00 SGT) |

## Deleted

(Nothing deleted. The `freetext.questionDeclined` prior copy is preserved verbatim in the Amendments table above.)
