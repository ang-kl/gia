(№ 842 - 04-07 '26 09:57 SGT) v0.62.488

# Journal — soleat v0.62.488 (result card: folder-tab status label + full-bleed nearby band, per the approved mockup)

> Operator (IMG_8 — Taliwang Restaurant + North Miznon cards, red folder-tab "Closed" labels and a flat brown "Lebanese and nearby flavours" band):
> *"change the eatery card looks like folder_tab_restaurant_cards_bold_tab.html"* (chose option **1** — hard edge-to-edge flat band + red folder-tab status label).
> Read as: the status pill and the "…& Nearby Flavours" strip should stop reading as pills; adopt the mockup's protruding folder-tab + margin-to-margin flat band.

## New

### [HDR] #842 | 09:57 SGT 04-07-26 | v0.62.488 — folder-tab status + full-bleed nearby band | 1 code file + 3 doc/meta | PR #(pending) |

- **[INTENT].** Make the eatery card match the approved mockup
  `folder_tab_restaurant_cards_bold_tab.html`: the open/closed status reads as a **folder tab**
  protruding above the card's top edge, and the cuisine "…& Nearby Flavours" strip is a **flat
  band flush to the card margins**, never a rounded pill.

- **[DELTA] (1 code file — `web/cuisine/src/v2/components/ResultCard.jsx`).**
  - **Status label → folder tab.** The `openNow === false` (Closed) and
    `closingSoonMinutes >= 0` (Closing-soon) labels now render as a tab that sits above the card:
    `ml-3 -mb-0.5 self-start relative z-10 px-3 py-0.5 rounded-t-lg text-[10px] font-bold`,
    `bg-red-600` (Closed) / `bg-pink-600` (Closing-soon). Word carries the state
    (`tr('card.closed')` / `tn('card.closingSoon')`) — **CVD-safe**, not colour-only. Kept as app
    emoji/i18n rather than porting the mockup's Tabler SVG.
  - **Nearby strip → flat full-width band.** Replaced the old `hasStatusTab ? 'rounded-lg'`
    (all-corners pill) branch with a flat band flush to the card edges:
    `-mx-2.5 mb-1 px-4 py-1 rounded-t-lg text-[11px] font-medium truncate`,
    `backgroundColor: s.accent || '#854F0B'`. The card's `overflow-hidden` clips the top corners
    so the band reads margin-to-margin. `hasStatusTab` gates the band's negative top-margin so the
    tab and band stack cleanly when both are present.

- **[VERIFICATION (sandbox)]** `esbuild` JSX parse clean on `ResultCard.jsx`. Web build in CI
  (`web/cuisine` changed).

- **[STATUS]** PROD — TMA redeploys on merge (Railway). Verify on Telegram: a Closed venue shows a
  red folder tab protruding above the card; a cuisine-alternate venue shows a flat brown band
  flush to the card margins (no pill); both stack cleanly when co-present.

- **[TEST]** Full vitest via CI.

- **[KNOWN GAPS]** (1) Mockup's curved-SVG tab shape is approximated with a `rounded-t-lg`
  rectangle (emoji/i18n kept; SVG not ported). (2) G4 operator eyeball on the deployed card is the
  final gate. (3) Wellington today-vs-tomorrow wording (1/2/3 pick), search auto-fire investigation
  (A), and the Menu/Hawker/Transport/Clipboard zh·ja·es locale extension remain separate open
  items. G4 gates for v0.62.483–488 remain open.

## Confirmation Gates
| Gate | Status | Note |
|------|--------|------|
| G1 | ✅ | esbuild JSX parse clean (ResultCard.jsx) |
| G2 | ⏳ | full vitest via CI |
| G3 | ✅ | serial 841→842, commit 959→960, build 598→599; anchor re-set 04-07 '26 09:57 SGT |
| G4 | ⏳ | redeploy + operator eyeball (protruding red tab + flat full-bleed brown band; no pill) |
