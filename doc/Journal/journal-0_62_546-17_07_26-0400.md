(№ 903 - 17-07 '26 04:00 SGT) v0.62.546

# Journal — soleat v0.62.546 (Hawker tablet: fullscreen re-read + landscape 4-col / portrait stacked)

> Operator (IMG_0672 landscape + IMG_0673 portrait, iPad Pro, v0.62.545): landscape is **missing the
> four columns**, and **portrait should list BELOW the map**. The portrait shot revealed the cause:
> portrait worked (it was reached by *rotating*, which fires `orientationchange`), but the initial
> landscape open showed the mobile layout — the hook read the small modal at mount and never updated
> after Telegram fullscreen resized the webview.

## New

### [HDR] #903 | 04:00 SGT 17-07-26 | v0.62.546 — fullscreen re-read + orientation-split layout | 3 files | PR #(pending) |

- **[FIX 1 — the hook didn't update after fullscreen].** `web/_shared/lib/use-viewport.js` now also
  listens on `window.visualViewport` `resize` **and** re-reads a few times after mount
  (250/700/1500 ms) — Telegram's `requestFullscreen` resizes the webview shortly after mount but doesn't
  reliably fire a window `resize`, so the first-open read (the small centered modal) used to stick as
  `mobile`. Now landscape flips to the tablet layout on first open, not only after a rotate.

- **[FIX 2 — orientation-split layout].** `web/hawker/src/App.jsx`:
  - **Landscape tablet** (`landscapeTablet`) → map fills the screen as the background + a left glass panel
    with the list as **`grid-cols-4`** (operator: four columns).
  - **Portrait tablet** → **stacked** (the inline map on top, list **below** as **`grid-cols-2`**) — per
    "portrait should list below the map." (Was the left-panel overlay; now stacks.)
  - **Phones** → unchanged single column.
  - The map-fills / left-panel / fullscreen-outer structure is now gated on **`landscapeTablet`**, not
    `isWide`; only the list column count keys off `isWide`. Dropped the auto-fit grid style for fixed
    `grid-cols-4` / `grid-cols-2`.
  - `package.json` 0.62.545 → **0.62.546**; Journal #903 + serial-state (commit 1020→**1021**, build
    645→**646**, journal 902→**903**; re-anchor 17-07 '26 04:00 SGT, prior 902 demoted verbatim AU-7).

- **[VERIFICATION (sandbox)].** `node --check use-viewport.js` OK; **Hawker vite build green**. ⚠️ The
  fullscreen re-read + the two layouts can only be confirmed on the real iPad — shipping live.

- **[STATUS]** STAGED → PROD on merge (operator "make it live").

- **[KNOWN GAPS].**
  1. 👀 **iPad recheck** — landscape now 4 cols on first open? portrait map-on-top + 2-col list on rotate?
     Panel width / map height still tunable.
  2. 🧭 Port the confirmed pattern (incl. the fullscreen re-read + `ipados` gate) to **Cuisine + Transport**.

## Confirmation Gates
| Gate | Status | Note |
|------|--------|------|
| G1 | ✅ | PATCH bump 0.62.545→0.62.546; additive, gated (phones unchanged); build green |
| G2 | ➖ | no destructive action |
| G3 | ➖ | no new decision rule |
| G4 | ➖ | no external paid API call |
