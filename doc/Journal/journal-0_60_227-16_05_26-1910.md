(№ 202 - 16-05 '26 19:10 SGT) v0.60.227

# Journal — soleat v0.60.227

> Operator, on the Hawker TMA Google Map: *"The hawker pins in Google map, hawker TMA is too tiny, what is the size? Also the colour of the pins are not obvious. How to make the hawker centre that is new pop?"* — and, after a review + AskUserQuestion: pin size **25px**, new centres carry a **"NEW" label badge**.

## New

### [HDR] #195 | 19:10:00 SGT 16-05-26 | v0.60.227 — Hawker TMA map pins enlarged to 25px + "NEW" badge for new centres | 2 files | 1 PR | 1 build |

- **[INTENT]** The Hawker TMA map markers — added in v0.60.224 as deliberately small 13px dots (function literally `tinyPinNode`) — were, in the operator's use, too tiny to see or tap, and the gold/red fill didn't read against Google Maps' own red/orange POI icons. Operator asked for the current size, a choice of larger sizes, and a way to make the 16 *new* hawker centres pop. Reviewed the marker code, presented size/treatment options; operator chose **25px** pins and a **"NEW" badge** on new centres.

- **[DELTA]**
  - **`web/hawker/src/components/HawkerMapPanel.jsx` — `tinyPinNode()` → `hawkerPinNode()`.** The pin div grows `13px → 25px` (circular, 2px white border, soft shadow — unchanged otherwise). For a new centre (`isNew`), an absolutely-positioned `NEW` badge pill is appended above the dot: 9px bold white text on gold (`#f5a623`), white border, own shadow. `position:absolute` keeps the badge out of the marker's layout box so the `AdvancedMarkerElement` anchor is unaffected. The gold/red palette is unchanged (gold = new, red `#e53935` = established); the InfoWindow `🆕` is unchanged. The sole caller (line 172) updated to `hawkerPinNode(c.isNew)`.
  - `package.json` `0.60.226 → 0.60.227`; this Journal entry (#195); `doc/.serial-state.yml` (`journal 194→195`, `commit 201→202`, `build 10→11`, `chat_counter 20→21`; re-anchored 19:10 SGT).

- **[VERIFICATION (sandbox)]**

  ```bash
  $ npm run build:hawker     → vite build OK, 36 modules, built in 1.78s
  $ npm test -- --run        → 1889 passed (1889)
  ```

  No `node --check` target — the only code file changed is a `.jsx` (covered by the Vite build). `public/hawker/` is git-ignored and rebuilt by CI / Railway.

- **[STATUS]** STAGED → PROD on next Railway deploy (the hawker TMA bundle is rebuilt server-side from `web/hawker/`).

- **[TEST]** Post-deploy: open the Hawker TMA → map. Confirm pins are visibly larger (25px) and tappable; confirm the 16 new centres each show a "NEW" badge above the dot and the 107 established centres show a plain red dot; confirm the badge does not shift the pin off its centre coordinate at any zoom level.

- **[KNOWN GAPS]**
  - Colour palette unchanged — if gold/red still don't read well against the map at 25px, a higher-contrast palette is a follow-up.
  - At city-wide zoom the 25px pins (and "NEW" badges) will overlap in dense regions; marker clustering remains a deferred item.
  - PR #478 (v0.60.226 — Cuisine TMA regex-first dish sourcing) **merged** to main (squash `de364b1`) — recorded here per the standing per-PR Journal rule; its own entry is `journal-0_60_226-16_05_26-1830.md`.
  - Operator's separate same-message request — free-text ambiguity/declined-search response not rendering, allowing transportation queries, and capping Gemini to a Singapore/Johor radius — is **still due** and tracked as a follow-up.

## Confirmation Gates

| Gate | Authority | Stamp |
|---|---|---|
| G1 — version bump | _PATCH 0.60.226 → 0.60.227._ | auto |
| G2 — destructive action | _None._ | n/a |
| G3 — new decision rule | _None — UI sizing/treatment choice, not a policy rule._ | n/a |
| G4 — paid external API call | _None._ | n/a |

## Amendments

| # | Item | Prior (verbatim) | Amended to | Date | Rule | Serial № |
|---|---|---|---|---|---|---|
| O-12 | Hawker TMA map pin | _(v0.60.224)_ `tinyPinNode()` — 13px circular dot, gold = new / red = established | `hawkerPinNode()` — 25px circular dot; new centres carry a "NEW" badge pill above the dot | 16-05 '26 | AU-7 | (№ 202 — 16-05 '26 19:10 SGT) |

## Deleted

(Nothing deleted.)
