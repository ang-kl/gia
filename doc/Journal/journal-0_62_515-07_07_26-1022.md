(№ 871 - 07-07 '26 10:22 SGT) v0.62.515

# Journal — soleat v0.62.515 (Sketchbook: filters-on-top layout parity)

> Operator, after v0.62.514 deployed: *"still ... didn't follow layout from Cuisine TMA
> 'Cuisine & Filter' tab folio. what's wrong with you to follow exactly the Cuisine TMA"* and,
> pinpointed: *"why your sketchbook filter is at the bottom??"*. The cache-bust fixed the
> stale-bundle symptom but NOT the real complaint. This PR reorders the Sketchbook cuisine
> panel so the **filters sit on top** and the cuisine grid below — matching the Cuisine TMA's
> `QuickFilters → CuisineDrawer` folio order.

## New

### [HDR] #871 | 10:22 SGT 07-07-26 | v0.62.515 — Sketchbook filters-on-top layout parity | 4 files | PR #(pending) |

- **[INTENT].** Operator: *"why your sketchbook filter is at the bottom??"* (+ *"follow exactly
  the Cuisine TMA 'Cuisine & Filter' tab folio"*). Workstream **C** — make the Sketchbook cuisine
  picker *follow* the Cuisine TMA layout. Bug-report protocol: traced to source before editing.

- **[ROOT CAUSE].** Layout **order**, not a data/cache bug.
  - `web/clipboard/src/components/Shell.jsx` — the `.sk-drop` panel rendered `<CuisineGroupPicker>`
    (the cuisine grid) **first**, then the rating/price/crowd/open-now/michelin facets block
    **second** → **filters at the bottom**.
  - The mirror source `web/cuisine/src/v2/App.jsx` `cuisinePickOpen` folder-drawer renders
    `<QuickFilters>` (App.jsx:4138) **before** `<CuisineDrawer>` (App.jsx:4161) → **filters at
    the top**, cuisine grid below, 🔍 Search last. The Sketchbook had the two blocks reversed.

- **[DELTA].**
  - `web/clipboard/src/components/Shell.jsx` — reordered the `.sk-drop` panel to **filters-first**:
    moved the facets `<div>` **above** `<CuisineGroupPicker>`; moved the `border-t` divider to sit
    **between** the facets and the grid (it used to sit above the facets, since facets were last);
    the grid now takes the bottom slot where the Cuisine TMA's 🔍 Search button sits (Sketchbook
    has **no** Search button — it filters saved cards live, so this is the closest faithful map).
  - `package.json` — 0.62.514 → **0.62.515**. Re-hashes the bundle
    (`index-CP2cu7U-.js` → **`index-Byi1IQ5K.js`**), so it also doubles as a cache-bust.
  - `web/clipboard/index.html` — diagnostic-boot comment marker **514 → 515**.
  - `doc/Journal/journal-0_62_515-07_07_26-1022.md` — this entry.
  - `doc/.serial-state.yml` — commit 988→**989**, journal 870→**871**; re-anchor 10:22 SGT
    (prior 0952 anchor demoted to `last_anchor_source_superseded_0952`).

- **[VERIFICATION (sandbox)]** `npm run build` in `web/clipboard/` → **BUILD_OK** (vite, **53**
  modules). New bundle `public/clipboard/assets/index-Byi1IQ5K.js`. `public/` is git-ignored → no
  built assets in the diff.

- **[STATUS]** PROD — merges to main; the per-PR bot redeploys the Sketchbook (clipboard) TMA. The
  version bump re-hashes the bundle, so Telegram refetches (no stale-cache re-run of #1520).

- **[TEST]** No unit test for TMA presentation; the vite build is the parse gate. Full `npm test`
  runs in CI (TMA-only change — `web/clipboard` + `package.json` + docs — outside the
  gia-preflight trigger set: no `index.js` / bot-handler / search / matcher surface).

- **[KNOWN GAPS].** Full folio parity, if the operator wants it, still has three pieces NOT taken
  here: (1) **folder-drawer chrome** — the Cuisine TMA uses an attached white `folder-drawer`
  folio under a tab; the Sketchbook uses a detached `.sk-drop` card with a shadow. (2) **greying**
  — Sketchbook greys groups with no saved card (the operator's own rule); the Cuisine TMA never
  greys. (3) **drill-in modal** — the Sketchbook `CategoryModal` lacks the Cuisine TMA's per-chip
  **Dishes ›** reveal + taxonomy badges + `imgFlag` photos (v0.62.463/469). **E** (free-text
  search FAB) still pending. MRT stations (Keppel / Cantonment / Prince Edward Road) still due for
  the 12 Jul 2026 launch.

## Confirmation Gates
| Gate | Status | Note |
|------|--------|------|
| G1 | ✅ | clipboard vite build green (53 modules); version 0.62.514→0.62.515; new bundle hash index-Byi1IQ5K.js |
| G2 | ✅ | root-caused to reversed panel order [Shell.jsx CuisineGroupPicker-then-facets vs Cuisine TMA QuickFilters-then-CuisineDrawer]; reordered filters-first |
| G3 | ✅ | serial commit 988→989, journal 870→871; anchor 07-07 '26 10:22 SGT (prior demoted) |
| G4 | ➖ | layout reorder on the operator-owned Sketchbook TMA; no data/route/bot/logic surface touched — operator eyeball on the live TMA is the final gate |
