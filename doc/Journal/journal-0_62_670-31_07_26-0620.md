# Journal — v0.62.670 (Material-alignment Phase 2b: the five O-85 decisions, answered and applied)

_(№ 1,193 — 31-07 '26 06:20 SGT)_

## New

### [HDR] v0.62.670 — operator ruled on the visible-colour decision list; the approved subset shipped

- **[INTENT]** Operator answered the Phase 2b decision round (31-07 '26, morning): *"BackFab: Keep the
  teal"* · *"Dark one-offs: Fix all three"* · *"Status pairs: Move to blue/amber"* · confirmations:
  *"Map chrome stays light"* + *"Fixed-light surfaces stand"*. This entry applies exactly that subset —
  the first VISIBLE changes of the Material-alignment programme, each individually operator-approved.

- **[DELTA]** (single commit `P2-c` + docs)
  1. **BackFab — no rendering change.** The 4-app teal `#7FDBDB` stands; cuisine `BackFab.jsx`'s stale
     v0.60.91 "inverse theme colours" comment gains an AU-7 supersession note (prior text kept verbatim).
     Closes O-85 item 1 → decision D-42a below.
  2. **Dark one-offs (cuisine, O-85 item 2 — "fix all three"):**
     - streamed first-result name `text-blue-900` → `text-tg-accent` (was illegible on dark themes);
     - the 12 dish-group badges `bg-*-100 text-*-800` → same-hue `bg-*-500/20 text-tg-text` (near-identical
       tint in light mode, readable wash in dark; hue stays decorative, label carries meaning);
     - selected category card `bg-[#DCEBFF] text-[#0c2540]` → `bg-tg-accent/15 text-tg-text`.
  3. **Status pairs → blue/amber (O-85 item 4, operator chose the visible recolour):** transport's
     all-normal/affected header + popup, StationCard's open-now/closed-now, and hawker's open pill:
     `text-green-500`→`text-blue-500`, `text-orange-500`→`text-amber-500`,
     `bg-green-600/20 text-green-500`→`bg-blue-600/20 text-blue-500` (hawker's closed state was already
     amber). Green leaves the status vocabulary in these two apps.
  4. **Confirmations recorded, nothing changed:** D-42 (map chrome stays light — `.gia-map-btn` is the
     standing rule) and D-43 (fixed-light surfaces stand: focused ResultCard, folio/manila, insight
     banner, map InfoWindows).

- **[VERIFICATION (sandbox)]** cuisine/hawker/transport builds green; full `npm test` 3840/3840 ×152.
  Visual deltas are exactly the approved ones: dark-mode readability fixes + the blue/amber recolour;
  light-mode badge tint shift is the approved near-identical wash. **Device check rides O-79** (both
  themes; the badges and status colours are the items to eyeball).

- **[TIME ANCHOR]** Container clock `2026-07-30T22:20:28Z` = **31-07 '26 06:20 SGT** (TF-9).

- **[STATUS]** PR pending (branch restarted from `origin/main` after #1672 merged). Auto-merge on green
  CI per the standing authorization.

- **[KNOWN GAPS]**
  - Two red/green sites were OUTSIDE this decision round and remain: cuisine's rating Save→Saved button
    (`bg-green-500`/`bg-red-600`) and ResultCard's "Closed"/"Closing soon" tabs (`bg-red-600`/
    `bg-pink-600`). Flagged to the operator in the same reply; awaiting a call → Register O-87.
  - Remaining B-visible inventory (≈150 sites) stays as-is: now either confirmed-standing (D-42/D-43),
    brand data, or documented deliberate design. Phase 2 is COMPLETE with this entry unless O-87 reopens
    a slice.

## Confirmation Gates
| Gate | Status | Note |
|------|--------|------|
| G1 | ✅ | PATCH bump 0.62.669 → 0.62.670; three builds + 3840 tests green. |
| G2 | ➖ | no deletions (the superseded BackFab comment is kept verbatim per AU-7). |
| G3 | ✅ | five operator decisions recorded (D-42a teal, O-85.2 fix-all-three, O-85.4 blue/amber, D-42, D-43). |
| G4 | ➖ | no external paid API call. |
