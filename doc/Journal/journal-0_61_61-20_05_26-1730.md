(№ 272 - 20-05 '26 17:30 SGT) v0.61.61

# Journal — soleat v0.61.60 → v0.61.61 (/transport train reply — layout tweak)

> Operator: *"The train information layout requires tweaking, follow this example"* —
> supplied a target layout for the `/transport` Train bot reply (a Telegram text
> message, not the Mini-App). Plus: *"↗ : open in Google map"* and *"The station
> name can be hyperlink to the station info."*
>
> Sequenced after PR #559 — this entry is commit 272 / journal 265 / build 72.

## New

### [HDR] #265 | 17:30:00 SGT 20-05-26 | v0.61.61 — /transport train reply layout | ~2 kb | 3 files | 1 PR | 1 build |

- **[INTENT]** Reflow the `/transport` Train reply to the operator's example —
  nearest-3-stations + weather header first, then the service-status block, then
  engineering; restyle the station rows; hyperlink each station name.

- **[DELTA]**
  - **`index.js`** `runTransportTrain` —
    - **Header** is now `🚇 Nearest 3 Train stations · <weather>` — the weather
      string (`emoji · condition · temp°C`) comes from the existing
      `weatherChatFooter()`; best-effort, the ` · <weather>` suffix is omitted on
      any failure.
    - **Reordered:** the nearest-3-stations block leads; the service-status block
      (`Status:` / `Notes:` / `Refreshed:`) moved **below** the station rows; the
      separate "Train" heading line and the est-wait line are dropped.
    - **Station name → hyperlink.** New module helper `stationMoreInfoUrl(name)`
      reads `data/stations.json` (cached) and returns the station's SMRT/SBS
      `more_info_url`; the row renders the name as `<a href="…">name</a>` (plain
      text when the station isn't in the dataset).
    - The static **frequency/headway footer was dropped** (not in the operator
      layout).
  - **`i18n.js`** —
    - `transport.train.nearestHeader` → `🚇 Nearest 3 Train stations{wx}` (was the
      est-wait header).
    - `transport.train.stationRow` → `{name} · {dist}{crowd} <a …>↗</a>` — leading
      `· ` dropped; the Google-Maps glyph `🗺️` → `↗` per the operator.
  - **`package.json`** — `0.61.60` → `0.61.61` (PATCH).

- **[VERIFICATION (sandbox)]**

  ```bash
  $ node --check index.js  → OK
  $ node --check i18n.js   → OK
  $ npm test  → 65 files / 1928 tests green
  ```

- **[STATUS]** DRAFT → PR (base `main`, branch `claude/review-gia4lunch-readme-3s798`).
  Deploys via the Railway auto-deploy on `main` once merged.

- **[TEST]** `/transport` → Train. The reply leads with `🚇 Nearest 3 Train
  stations · ⛅ <condition> · <temp>°C`, then 3 rows `<emoji> <code> <name> ·
  <dist> · <crowd> ↗` (name links to the SMRT/SBS station-info page, `↗` opens
  Google Maps), then a blank line + `Status:` / `Notes:` / `Refreshed:`, then
  `⚠️ Affected lines` (only during a disruption) and `🔧 Upcoming engineering`.

- **[KNOWN GAPS]**
  - `weatherChatFooter()` is chat-wide (Singapore-centroid / cached `weather:tma-
    summary`), not per-user-location — acceptable for a header weather glyph.
  - The `⚠️ Affected lines` detail block is kept (it renders only during a
    disruption, so the operator's all-normal example doesn't show it); the
    frequency/headway footer was removed as it is absent from the example.
  - `transport.train.heading` i18n key is now unused (left in place — removing i18n
    keys is avoided).

- **Confirmation Gates**

  | Gate | Resolution | Date |
  |------|-----------|------|
  | G1 — version bump | PATCH `0.61.60` → `0.61.61`, established 0.61.x cadence | 20-05 '26 |
  | G2 — destructive action | the static frequency/headway footer block was removed from the Train reply — absent from the operator's target layout; the `mrt-lines.networkHeadwayRange()` helper itself is retained | 20-05 '26 |
  | G3 — new decision rule | the Train reply leads with the nearest-stations + weather header; station names hyperlink to their SMRT/SBS station-info page; `↗` is the Google-Maps affordance | 20-05 '26 |
  | G4 — paid external API call | _n/a_ | — |

## Amendments

_None._

## Deleted

### v0.60.75 — static network frequency / headway footer (Train reply)

Removed from `runTransportTrain` (`index.js`) — the block that appended a published-
headway footer to the `/transport` Train reply:

> ```js
> try {
>   const mrtLines = require('./mrt-lines');
>   const r = mrtLines.networkHeadwayRange();
>   if (r && Number.isFinite(r.peakMin)) {
>     lines.push('', tn('transport.train.headway', lang, {
>       peakMin: r.peakMin, peakMax: r.peakMax,
>       offMin:  r.offMin,  offMax:  r.offMax
>     }));
>   }
> } catch (err) {
>   console.warn('[Transport] headway footer render failed:', err.message);
> }
> ```

Rationale: not present in the operator's supplied target layout (2026-05-20). The
`mrt-lines.networkHeadwayRange()` helper and the `transport.train.headway` i18n key
are kept in case a future surface wants them.
