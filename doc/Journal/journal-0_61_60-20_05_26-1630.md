(№ 271 - 20-05 '26 16:30 SGT) v0.61.60

# Journal — soleat v0.61.59 → v0.61.60 (CR6 station info card — template redesign)

> Operator screenshot review: *"The train station information looks ugly, follow
> this template and ask."* — supplied a full redesign template for the station info
> card. Clarified via AskUserQuestion: interchange = stacked per-line blocks;
> first/last-train row dropped; exit rows stay tappable. The operator believed the
> exit street name was in the LTA exit GeoJSON — it is **not** (see Known Gaps).
>
> Sequenced after PR #558 — this entry is commit 271 / journal 264 / build 71.

## New

### [HDR] #264 | 16:30:00 SGT 20-05-26 | v0.61.60 — station info card template redesign | ~2 kb | 4 files | 1 PR | 1 build |

- **[INTENT]** Rebuild the CR6 station info card body to the operator's supplied
  template — restyled header, per-line stacked blocks, redesigned Exits list,
  operator + Google-Maps footer.

- **[DELTA]**
  - **`mapOverlays.js`** (byte-identical ×3) — `stationInfoCardHtml(rec)` fully
    rewritten:
    - **Per-line block** (stacked, divider between blocks for interchanges): a
      **big station-code pill** (`codePill` — white bold text on the line colour)
      · **"<Name> Station"** bold in the line colour; the **"<line_code> ·
      <line_name>"** line; a per-line **"More Info ↗"** link.
    - **Exits** — station-level, label-sorted (`localeCompare` numeric). Each row:
      `Exit # · <street?> · Bus Stop № <code>`. `Exit #` and `Bus Stop № <code>`
      are tappable map-focus links (`window.__giaStationFocus` → pan + zoom);
      `<street>` (an **optional `exit.street` field**) renders bold/dark only when
      present. The bus-stop **code is now shown** (`Bus Stop № 13121`) — was a
      bare `Bus №` link.
    - **Footer** — `Operator: <operator(s)>` (interchange operators joined with
      ` · `) then **`Google Map ↗`** (links the station coords).
    - **first/last train rows dropped** — not in the operator template.
  - **`package.json`** — `0.61.59` → `0.61.60` (PATCH).
  - No change to `data/stations.json` or `build-station-info.js` — the card reads
    the existing record shape; `exit.street` is a new optional field the renderer
    is ready for.

- **[VERIFICATION (sandbox)]**

  ```bash
  $ node --check web/transport/src/lib/mapOverlays.js  → OK
  $ npm run build:cuisine / build:hawker / build:transport  → all ✓ built
  $ npm test  → 65 files / 1928 tests green
  ```
  `mapOverlays.js` confirmed byte-identical ×3.

- **[STATUS]** DRAFT → PR (base `main`, branch `claude/review-gia4lunch-readme-3s798`).
  Deploys via the Railway auto-deploy on `main` once merged.

- **[TEST]** Toggle 🚉 Train Line, tap a station: the card shows the code pill +
  bold line-coloured "<Name> Station", the line, a per-line More Info link, then a
  label-sorted Exits list (`Exit 1 · Bus Stop № …`), then `Operator:` + `Google
  Map ↗`. An interchange (e.g. Newton) stacks one header/line/More-Info block per
  line, then the shared Exits + a joined-operator footer.

- **[KNOWN GAPS]**
  - **Exit street name not available.** The operator's AskUserQuestion answer said
    it is "in the geojson for train station exit" — verified it is **not**:
    `geoloc/LTAMRTStationExitGEOJSON.geojson` (597 features) carries only
    `STATION_NA`, `EXIT_CODE`, `OBJECTID`, `INC_CRC`, `FMEL_UPD_D`. The card renders
    `exit.street` the moment that field exists in `stations.json`; sourcing it
    (OneMap reverse-geocode of each exit's coords, or an operator-supplied dataset)
    is a follow-up — surfaced to the operator.
  - Bus-service numbers no longer shown on the card (the template replaced the
    services list with the bus-stop code); `nearest_bus_stop.services` still lives
    in the data.
  - Sandbox-unverified UI change. CR6 Phase 2b (first/last train) remains blocked.

- **Confirmation Gates**

  | Gate | Resolution | Date |
  |------|-----------|------|
  | G1 — version bump | PATCH `0.61.59` → `0.61.60`, established 0.61.x cadence | 20-05 '26 |
  | G2 — destructive action | _n/a_ | — |
  | G3 — new decision rule | station card follows the operator's redesign template; interchanges stack per-line blocks; first/last-train row dropped; exit street name is an optional field pending a data source | 20-05 '26 |
  | G4 — paid external API call | _n/a_ | — |

## Amendments

### [HDR] #261 — CR6 station info card body superseded by the template redesign

The v0.61.57 card body (journal #261) — described there as:

> `stationInfoCardHtml(rec)` — renders the card: `"<name> station"` title; one
> block per line (`Operator: …`, `"<line_code> · <line_name>"`, a station-code
> pill in the line colour via `codePill`/`codeHex`, any `first_last_train` rows,
> a `More info ↗` link); then a station-level **Exits** list where each
> `Exit <label>` and `Bus №` is a tappable link, followed by the bus services.

Appended change with rationale and serial (20-05 '26 16:30 SGT, № 271):

Superseded by the v0.61.60 operator-template redesign (this entry, #264). The
header is now a code pill + bold line-coloured "<Name> Station"; `Operator:` and a
new `Google Map ↗` move to a station-level footer; first/last-train rows are
dropped; the Exits list shows `Bus Stop № <code>` instead of a bare `Bus №` link +
services list, and gains an optional `<street>` slot. The tappable map-focus links
on `Exit #` / bus stop are retained.

## Deleted

_None._
