(№ 509 - 01-06 '26 07:45 SGT) v0.61.308

# Journal — soleat v0.61.308 (vault snapshot @ v0.61.307 tip)

> Operator (31-05 '26 → 01-06 '26): *"vault, journal, update features"* (kicked off post v0.61.304 + v0.61.305 merges), then *"resume"* after PR A (v0.61.306 doc upkeep) and the v0.61.307 `/cost` ship landed. PR B (this PR) closes the doc-upkeep arc with the frozen reproducible snapshot at the current main tip.
>
> Vault scope: ~178 MB, **1296 files**. Captures the v0.61.209 → v0.61.307 delta arc (99 versions, ~24 squash-merged PRs + many internal patches). Prior vault `vault/v0.61.208/` is **kept** alongside, not superseded (Rule V-1).
>
> Commit 522 / journal 509 / build 311, on branch `claude/v0.61.308-vault-snapshot` off `main` at v0.61.307 tip.

## New

### [HDR] #509 | 07:45 SGT 01-06-26 | v0.61.308 — vault snapshot (1296 files, 178 MB) | 1298 files | 1 PR | 0 build |

- **[INTENT]** Snapshot the production-tip codebase + docs into `vault/v0.61.308/` so the v0.61.209 → v0.61.307 arc has a frozen reproducible mirror, matching the `vault/v0.61.116/` and `vault/v0.61.208/` precedents (PRs #610 + #711).

- **[DELTA]**

  **1. `vault/v0.61.308/`** *(new, 1296 files, 178 MB)* — rsync mirror of the working tree at v0.61.308, with exclusions per CLAUDE-FULL §16.1:
  - Excluded: `node_modules/` (root + nested), `.git/`, `vault/` (recursion), `dist/` + `web/*/dist/`, `public/{cuisine,menu,hawker,transport,oversight}/` (Vite build outputs), `*.log`, `.env`, `.claude/settings.local.json`, `tmp/`. Also excluded local-only iCloud sync artefacts: `*.mhtml`, `* 2.*`-suffixed sync-duplicates.
  - Kept: `public/app.js`, `public/index.html`, `public/doc/`, `geoloc/*.geojson` (open data), `.env.example`, `.claude/skills/`.
  - Largest tree: `geoloc/` (118 MB — LTA TrafficSign + URA AmendmenttoMP2014Building + NParks + SPF NPC boundaries + STB + NLB + HPB geojson), `data/` (32 MB incl. `durian-variance/`), `doc/` (12 MB).

  **2. `vault/v0.61.308/VAULT_README.md`** — provenance + boot instructions + the v0.61.209 → v0.61.307 sub-arc table (cuisine-venue-counts wrapup → Gemini-cache generalisation → fun-facts → placeId fix class → JB polish → recents drawer → /cost tracker). Cross-references the prior `vault/v0.61.116/` and `vault/v0.61.208/`.

  **3. `package.json`** — `0.61.307 → 0.61.308`. The vault contents reflect the post-bump state (mirrors the v0.61.208 / PR #711 precedent where the bumped version is included in its own vault).

  **4. `doc/.serial-state.yml`** — commit 521→522, build 310→311, journal 508→509; re-anchored 01-06 '26 07:45 SGT (first session-counter entry for chat 2026-06-01).

- **[VERIFICATION (sandbox)]**

  - Sanity post-rsync: zero `node_modules/` directories anywhere in `vault/v0.61.308/`, zero `.env` files, no `public/{cuisine,menu,hawker,transport,oversight}/` build artefacts, no nested `vault/`.
  - File count: 1296 (vs 1114 for v0.61.208 — a ~16% growth across 99 patches, dominated by new tests + new journal entries + new web/ components).
  - Total size: 178 MB (vs 163 MB for v0.61.208 — same growth ratio).
  - `npm test -- --run` not re-run on the branch (no JS changes outside the vault tree).

- **[STATUS]** PR-OPEN on push. Railway does NOT redeploy on a docs-only / vault-only merge (no app-server code change). CI runs all four required checks; empty-blob guardrail catches if any file in the vault was rsynced as 0 bytes from iCloud-stale-checkout (Register O-37) — none expected since the working-tree files were all readable.

- **[TEST]** Operator (post-merge):
  1. Browse `vault/v0.61.308/` on GitHub — confirm `index.js`, `api-cost.js`, `web/cuisine/src/v2/`, `doc/Journal/journal-0_61_307-*.md` all present.
  2. Optional clean-reproduce: `cp -a vault/v0.61.308 /tmp/soleat-v0.61.308 && cd /tmp/soleat-v0.61.308 && npm install && npm test -- --run` → expect 2879 / 2879 tests green.
  3. Confirm prior `vault/v0.61.116/` + `vault/v0.61.208/` are untouched (Rule V-1).

- **[KNOWN GAPS]**
  - **Self-referential omission** — this journal entry IS in the rsync's source tree at write-time, BUT the rsync ran before this journal was written. So `vault/v0.61.308/doc/Journal/journal-0_61_308-*.md` does NOT exist inside the vault. The vault captures up through journal-0_61_307 (the /cost ship). Mirrors the v0.61.208 Rule V-3 precedent.
  - **PR diff will be large** — ~1300 files in one commit. Past vault PRs (#610, #711) handled this fine; GitHub UI is slow but functional.
  - **Register not updated in this PR** — `register-0_61_300-31_05_26-0915.md` is the last register entry. Items O-37 (iCloud-stale) and others may have additional status changes after v0.61.305 / .306 / .307; flagged for a follow-up register refresh.
  - **No CI churn risk** — the vault is purely additive content; no module load paths affected.

- **Confirmation Gates**

  | Gate | Resolution | Date |
  |---|---|---|
  | G1 — version bump | PATCH `0.61.307 → 0.61.308`. Vault-only; no code change. | 01-06 '26 |
  | G2 — destructive action | _n/a_. Additive: new `vault/v0.61.308/` tree, no deletions or rewrites. Prior vaults untouched (Rule V-1). | 01-06 '26 |
  | G3 — new decision rule | _n/a_. Follows the v0.61.116 / v0.61.208 vault-pass precedent (D-1028 / D-1076 / V-1 / V-2 / V-3). | 01-06 '26 |
  | G4 — paid external API call | _n/a_. | 01-06 '26 |

## Amendments

- **`vault/v0.61.308/`** — new tree, 1296 files / 178 MB.
- **`vault/v0.61.308/VAULT_README.md`** — provenance + exclusions + boot instructions + sub-arc table.
- **`package.json`** — version `0.61.307 → 0.61.308`.
- **`doc/.serial-state.yml`** — counters + last_anchor_time re-anchored 01-06 '26 07:45 SGT.

## Deleted

_None._
