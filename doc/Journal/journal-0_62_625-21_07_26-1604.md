# Journal — v0.62.625 (revert the Transport desktop side panel — "revert the side drawer it is aweful")

_(№ 969 — 21-07 '26 16:04 SGT)_

## New

### [HDR] v0.62.625 — remove the v0.62.621 side-panel layout; fall back to the carousel

- **[INTENT]** Operator (Transport TMA on desktop — now correctly fullscreen-wide via v0.62.624 —
  screenshot): *"revert the side drawer it is aweful."*

- **[CONTEXT]** With desktop fullscreen restored (v0.62.624), the v0.62.621 side panel finally rendered on
  a genuinely wide desktop — and the fixed left column stacked all **35 EWL station cards** into an
  unreadable run of coloured name strips (a wall of thin green bars). It looked awful, as the operator
  said.

- **[DELTA]** Removed the `wideLandscape` **side-panel branch** (`deviceClass === 'desktop' || (tablet &&
  landscape)`) from **`web/transport/src/App.jsx`**. Wide / landscape / desktop now **fall through to the
  CAROUSEL layout** (full-bleed map + a bottom station carousel) — the pre-v0.62.621 behaviour. Phone
  portrait keeps the draggable bottom sheet; the LIST toggle still wins.
  - **KEPT:** the v0.62.621 Slice-B station-**card** enhancements (action row Directions / Save / Share,
    category + interchange chip, distance + walking time, collapsible "Train times"). Only the side-panel
    **layout** was reverted — the enriched cards still render in the carousel / list / drawer.

- **[VERIFICATION (sandbox)]** No dangling `wideLandscape` / `<aside>` references. `transport` `vite build`
  green; full `npm test` **3756/3756 across 148 files**. **Playwright** (desktop 1280×800) confirmed
  `noAside = true` (side panel gone), the enhanced cards still render (action row + category chips + walk
  text + collapsible hours expand on click), **zero** page errors. `package.json` + lockfile 0.62.624 →
  0.62.625; `.serial-state.yml` commit 1106→1107, build 726→727, journal 968→969, re-anchor **21-07 '26
  16:04 SGT**.

- **[TIME ANCHOR]** data.gov.sg blocked by the environment network policy (`403` `CONNECT`); anchored to
  the container system clock `2026-07-21T08:04Z` = **21-07 '26 16:04 SGT** (TF-9 terminal-runtime source).

- **[STATUS]** DRAFT → squash-merge to `main` on green CI (auto-merge SQUASH). New PR (branch restarted
  from `origin/main` `0d8b735` = `#1632` merged).

- **[TEST]** No new unit test — a layout-branch removal; verified via build + the Playwright
  computed-state probe (absence of the `<aside>`, cards still present + interactive).

- **[KNOWN GAPS / caveats]** The desktop/wide Transport view is back to **full-bleed map + bottom
  carousel**. The Google-Maps "side panel on desktop" idea from v0.62.621 is shelved — if a wide
  two-column view is wanted later, it needs a proper card treatment for the wide column (the naive full-
  card stack was the problem), not the phone card reused vertically. The card place-details work
  (v0.62.621 Slice B) stands.

## Removed Features

| # | Item | Original Version | Removed in Version | Removal Date (DD-MM 'YY) | Reason | Replacement | Rule Reference | Serial № |
|---|------|------------------|--------------------|--------------------------|--------|-------------|----------------|----------|
| 1 | Transport desktop/landscape-tablet side panel (left station-list column + right map) | 0.62.621 | 0.62.625 | 21-07 '26 | Operator: "revert the side drawer it is aweful" — the narrow left column stacked all 35 station cards into an unreadable run of coloured name strips | Fall through to the full-bleed map + bottom station carousel (pre-v0.62.621 wide behaviour) | AU-2 | (№ 969 — 21-07 '26 16:04 SGT) |

## Confirmation Gates
| Gate | Status | Note |
|------|--------|------|
| G1 | ✅ | PATCH bump 0.62.624 → 0.62.625; transport build + 3756 tests green; Playwright confirmed the side panel is gone and the cards still render. |
| G2 | ✅ | a UI-layout removal at operator's explicit request ("revert … it is aweful"); the removed branch is preserved verbatim in the comment + logged in the Removed Features table. |
| G3 | ➖ | no new decision rule. |
| G4 | ➖ | no external paid API call. |
