(№ 630 - 09-06 '26 22:06 SGT) v0.61.430

# Journal — soleat v0.61.430 (Explicit pick wins: kills country-drift + the MY Michelin grey-out)

> Operator: *"i have michelin drift and location (country) drift . check log:"* → log `log/logs.1781012660221.json`. Investigated; both confirmed; operator chose **"Explicit pick wins."**

> Commit 643 / journal 630 / **build 408 (web/cuisine)**. location-follow.js + App.jsx + test. Revert-ready.

## New

### [HDR] #630 | 22:06 SGT 09-06-26 | v0.61.430 — explicit pick wins over the 20s device-follow + countryPref backfill | web/cuisine | 1 PR | 1 build |

- **[INVESTIGATION — log-confirmed]**
  - **Country drift:** at `23:25:22` the operator set **Kuala Lumpur** → search ran `region=OTHER center=KL`. At `23:25:25` `[set-location] → <redacted>` (the **SG device GPS**) fired on its own and the next search came back **`region=SG center=KL`** — incoherent; the SG post-filter (drops venues > 30 km from SG) then **zeroed durian-pastry**. Root cause: the 20 s "follow device" sync (`App.jsx` `startLocationSync`, ~173-218) overrode the explicit KL pick. The guard meant to stop it (`location-follow.js:51`, `explicitAnchorName`) missed because the inherited pick carried **no anchor name**.
  - **Michelin greyed for MY:** log `23:25:26` `D703c OTHER-region: **no country-pref**`. `CuisineDrawer.jsx` computes `currentMichCountry = region==='SG'?'SG': … : (countryPref||'')`; with `countryPref` empty → `''` → not in `michelinCountries` → **chip disabled**. But MY genuinely has Michelin (KL 33 + George Town/Penang 37 in `michelin-data.js`). JKT/Indonesia greys **correctly** (no Indonesian guide; ID not in the set).
  - **Shared weak spot:** a foreign pick doesn't *lock in* `country = MY` — so device-follow drags region→SG **and** the drawer can't tell the country.

- **[DELTA]** (Cuisine TMA only; operator chose "Explicit pick wins")
  - **`lib/location-follow.js`** — `shouldFollowDevice` gains an **`explicitPick`** param: returns `false` when set. Robust backstop for the no-anchor-name case rule 3 missed.
  - **`App.jsx`** — new **`explicitPickRef`** + a region-keyed effect: whenever `state.region` is **OTHER / JB** (a deliberately chosen foreign region), (a) set `explicitPickRef = true` so the 20 s device-follow **yields** (no more SG drag), and (b) **backfill `state.countryPref`** from the committed coords when it's empty (`coordsToCountry → 'MY'`, else `nearestIataCity → countryCode`) + `saveCountryPref` — re-enabling the Michelin chip and scoping the server country-text-filter. Only fills when EMPTY, so it never overrides an explicit country pick. `explicitPick` is threaded into the `shouldFollowDevice` call.
  - Device-follow **still works on a plain GPS start** (no pick → `explicitPickRef` stays false). Resets on reload.
  - **`__tests__/location-follow.test.js`** — +2 cases (explicitPick blocks follow even with a blank name; no-pick still follows).
  - **`package.json`** `0.61.429 → 0.61.430`; **`doc/.serial-state.yml`** commit 642 → 643, journal 629 → 630, **build 407 → 408**; re-anchored 22:06 SGT.

- **[VERIFICATION (sandbox)]** `node --check location-follow.js` clean. Full suite **3170/3170 green** (3168 + 2). `npm run build:cuisine` green.

- **[STATUS]** PR-**OPEN**, **auto-merge armed (squash)**. Railway redeploys (rebuilt cuisine bundle) on merge.

- **[TEST — operator, deployed]** From SG, pick **Kuala Lumpur** (or any MY city) → the country stays MY across the next searches (no silent flip back to SG), durian/durian-pastry return results, and the **✳️ Michelin chip is enabled** (KL/Penang). Pick a plain GPS start with no manual pick → device-follow still tracks your country in the first 20 s. Jakarta still correctly greys Michelin.

- **[KNOWN GAPS]**
  - "New" review-time signal isn't used here — the foreign-country fallback is `nearestIataCity` (coords-based), same detector the mount auto-detect uses.
  - After ANY foreign pick, device-follow is OFF for the rest of that session (by design — "explicit pick wins"); a reload re-enables it for a fresh GPS start. A user who picks KL then physically travels won't be auto-followed until reload — acceptable per the operator's choice.
  - If the deployed catalogue is ever served WITHOUT `michelinCountries` (stale cache), `CuisineDrawer.jsx:109` falls back to the legacy `regionScope:'SG'` gate and would grey Michelin for OTHER regardless — not changed here (the live catalogue builds the list); flag if it recurs.
  - VibeCodingRecord regen still due — log/do post-merge.

- **Confirmation Gates**

  | Gate | Resolution | Date |
  |---|---|---|
  | G1 — version bump | PATCH `0.61.429 → 0.61.430`. | 09-06 '26 |
  | G2 — destructive action | Adds a guard + a backfill (fills only when empty; never overrides a pick). Device-follow preserved for GPS starts. Reversible. | 09-06 '26 |
  | G3 — new decision rule | Explicit location/country picks win over the 20 s device-follow; OTHER always carries a derived countryPref. | 09-06 '26 |
  | G4 — paid external API call | None. | 09-06 '26 |

## Amendments

- **`web/cuisine/src/v2/lib/location-follow.js`** — `shouldFollowDevice` `explicitPick` guard.
- **`web/cuisine/src/v2/App.jsx`** — `explicitPickRef` + region-keyed lock/backfill effect; `explicitPick` threaded into the follow guard.
- **`__tests__/location-follow.test.js`** — +2 cases.
- **`package.json`** — `0.61.429 → 0.61.430`.
- **`doc/.serial-state.yml`** — counters (commit 643 / journal 630 / build 408) + anchor 22:06 SGT 09-06-26 + `last_anchor_source` prepend.

## Deleted

- The behaviour where the 20 s device-follow could override an explicit foreign pick (the country-drift) and where an OTHER region could carry an empty `countryPref` (the MY Michelin grey-out) — both superseded.
