(№ 484 - 30-05 '26 17:55 SGT) v0.61.283

# Journal — soleat v0.61.283 (durian-variance placeId-loss fix — Plan B cache now actually populated)

> Operator (30-05 '26 ~17:53 SGT, post-v0.61.282 merge):
> *"/Users/akla/Downloads/durian-pastry-gemini-verified-2026-05-30_1752.json /Users/akla/Downloads/durian-gemini-verified-2026-05-30_1750.json"*
>
> Inspection of the four Gemini-verified JSONs in `~/Downloads/` (16:57 / 16:59 / 17:50 / 17:52) revealed a critical regression: **every venue had `placeId: ""` (empty)**. Following the chain:
>
> 1. `scripts/durian-variance.js:180` — the per-query `kept[]` row was `{name, primaryType, formattedAddress}`. No placeId.
> 2. `scripts/durian-variance.js:147` — `placeToVenue` mapped the Places (New) API response without surfacing `p.id`.
> 3. `durian-gemini-verifier.js:_flattenKeptVenues:54` — read `v?.placeId` from the kept row → empty string.
> 4. `durian-gemini-verifier.js:_processBatch:180` — carried the empty placeId through to the labelled output.
> 5. `index.js:ownerDurianGeminiRun:4541-4542` — `if (!v.placeId) continue;` skipped every venue.
> 6. **`dgv:labels:<mode>` Redis hash got zero entries** across every `/ver` run from v0.61.275 onward.
>
> Net: the v0.61.275 Plan B cache "optimization" has been silently a no-op in production for 8 patches (v0.61.275 → v0.61.282). The cuisine-search D703e post-filter saw an empty `labelsMap` and short-circuited via `if (labelsMap && Object.keys(labelsMap).length > 0)` — every search fell through to the v0.61.262 heuristic post-filter alone.
>
> The v0.61.282 inline D703f path is **unaffected** because it reads placeIds directly from the cuisine-search route's venue array (which DOES carry placeId from the Places API response). So the v0.61.282 inline verify-then-cache shipped this morning is the *first* mechanism that actually populates `dgv:labels:<mode>` in production.
>
> v0.61.283 fixes the upstream `/ver` pipeline so future bulk runs populate the cache too:
>
> 1. `scripts/durian-variance.js:147` — `placeToVenue` now includes `placeId: p?.id || ''`.
> 2. `scripts/durian-variance.js:180` — the kept[] row now includes `placeId: v.placeId || ''`.
>
> Existing four JSONs in `~/Downloads/` are permanently placeId-less; re-run `/ver → 🥥 Durian variance → <mode>` followed by `/ver → 🤖 Gemini verify → <mode>` on the v0.61.283 deploy to produce a fresh report with placeIds, then either let `ownerDurianGeminiRun` write to Redis directly OR export the JSON and use the v0.61.282 seed script.
>
> Commit 497 / journal 484 / build 286, on branch
> `claude/v0.61.283-durian-variance-placeid-loss-fix` off `main` at v0.61.282.

## New

### [HDR] #484 | 17:55 SGT 30-05-30 | v0.61.283 — durian-variance placeId-loss fix | 3 files | 1 PR | 1 build |

- **[INTENT]** Restore the v0.61.275 Plan B cache to working order. Every step downstream of `durian-variance.js` was correctly threaded for placeId — only the source itself was dropping it. One-line fix to `placeToVenue` + one-line fix to the `kept.push(row)` shape.

- **[DELTA]**

  **1. `scripts/durian-variance.js:147` (`placeToVenue`).**
  Added `placeId: p?.id || ''` as the first field. The Places (New) API returns the resource id at `p.id`; the FieldMask at line 134 already includes `places.id`, so this lights up without an API change.

  **2. `scripts/durian-variance.js:180` (per-query `kept[]` row).**
  Added `placeId: v.placeId || ''` to the row shape. Inline comment explains the chain to `ownerDurianGeminiRun`'s `if (!v.placeId) continue;` guard so a future reader sees the load-bearing field.

  **3. `package.json`** — `0.61.282 → 0.61.283`.

  **4. `doc/.serial-state.yml`** — commit 496→497, build 285→286, journal 483→484; re-anchored 30-05 '26 17:55 SGT.

- **[VERIFICATION (sandbox)]**

  - `node --check scripts/durian-variance.js` — clean.
  - Existing four JSONs in `~/Downloads/` confirmed placeId-less via `node -e "JSON.parse(...).venues[0].placeId"` → empty string for all 1,689 + 3,527 entries.
  - Vitest still BLOCKED locally by the recurring iCloud `ERR_INVALID_PACKAGE_CONFIG` throttle (Register O-22). CI will run authoritatively.

- **[STATUS]** PR-OPEN on push. Railway redeploys the bot + the /ver pipeline on merge. **The v0.61.282 inline D703f path keeps working unchanged** — this fix only matters for future `/ver` runs.

- **[TEST]** Operator (post-merge):
  1. **Re-run `/ver`.** On the v0.61.283 deploy: `/ver → 🥥 Durian variance → durian-pastry` (or `durian`), wait for completion, then `/ver → 🤖 Gemini verify → durian-pastry`. The post-verify summary message should still read normally, but Railway logs now show `[Cuisine-TMA] dgv:labels:durian-pastry populated N placeId → label entries (30d TTL)` with N > 0 (previously always 0).
  2. **Confirm Redis hash populated.** `redis-cli HLEN dgv:labels:durian-pastry` returns the count from #1.
  3. **Cuisine-search uses cached labels.** Durian-pastry search at any covered location — Railway log shows `D703e Gemini-label post-filter (durian-pastry): N → N-K (dropped=K unrelated, unlabeled=M passed through)` with K > 0 when there are unrelated venues.
  4. **Inline D703f still works for OTHER.** Durian-pastry search at Surabaya (the original failing case) — D703e drops cached-known unrelateds; D703f fires for remaining unlabeled venues and writes them to the cache.

- **[KNOWN GAPS]**
  - **Existing four JSONs are unrecoverable.** Re-running `/ver` is the only path to a placeId-bearing report. The seed script (v0.61.282) still works — it just rejects all entries from the four placeId-less files via its `if (!v.placeId) { skipped++; continue; }` guard.
  - **No automated regression test** for the upstream Places API shape. If Google renames `id` to something else, this breaks silently again. Could add a smoke test in `__tests__/durian-variance.test.js` that asserts `placeToVenue({ id: 'abc' }).placeId === 'abc'`.
  - **The Removed-Features / amendments table** in `doc/Feature/cuisines_js.MD` should gain a row for the v0.61.275 → v0.61.282 "Plan B cache was no-op" period per AU-2. Flagged here; not in this PR.

- **Confirmation Gates**

  | Gate | Resolution | Date |
  |---|---|---|
  | G1 — version bump | PATCH `0.61.282 → 0.61.283`. Single bug fix; no API contract change; no new feature. | 30-05 '26 |
  | G2 — destructive action | _n/a_. Two additive field assignments. | 30-05 '26 |
  | G3 — new decision rule | _n/a_. Restores the v0.61.275 contract that was always intended. | 30-05 '26 |
  | G4 — paid external API call | _n/a_. | 30-05 '26 |

## Amendments

- **`scripts/durian-variance.js`** — `placeToVenue` returns `placeId` from `p.id`; per-query `kept[]` rows include `placeId`. The downstream Plan B chain (v0.61.275: `_flattenKeptVenues` → `_processBatch` → `ownerDurianGeminiRun` → Redis hash) was already threaded correctly; only the source dropped the field.

## Deleted

_None._
