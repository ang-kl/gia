# Journal — v0.62.592 (Cuisine: a pin tap scrolls its card into the vertical list)

_(№ 936 — 18-07 '26 17:46 SGT)_

## New

### [HDR] v0.62.592 — Cuisine: tapping a map pin brings the matching card into view in the list

- **[INTENT]** Operator (iPad Pro portrait, Cuisine TMA): *"when i tap on the pin, you didn't bring up
  the card in listing style."* (Plus a larger ask — freeze the listing header, two columns scroll
  independently, independent expand — held for a layout decision, see Known Gaps.)

- **[DELTA]** In the vertical list (`ResultPanel`) a pin tap sets `focusedPlaceId`, which only **ringed**
  the matching card — it never scrolled it into view (that behaviour lived only in the `ResultDrawer`
  carousel). Fixed:
  - `web/cuisine/src/v2/components/ResultCard.jsx` — the card's root `<button>` now emits
    `data-pid={venue.placeId}` so the list can locate it.
  - `web/cuisine/src/v2/components/ResultPanel.jsx` — added a `panelRef` + a `focusedPlaceId` effect that
    `scrollIntoView({ behavior:'smooth', block:'center' })` the matching card, **scoped to the panel**
    (so it never grabs a `ResultDrawer` card) and **skipping the first render** (a fresh result set
    doesn't yank the page).

- **[VERIFICATION (sandbox)]** Cuisine `vite build` green; full `npm test` **3719/3719 across 145 files**;
  `package.json` + `package-lock.json` 0.62.591 → 0.62.592; `.serial-state.yml` commit 1070→1071, build
  693→694, journal 935→936, re-anchor 18-07 '26 17:46 SGT. ⚠️ Live-only. Branch rebased onto `origin/main`
  to drop the already-merged #1599 commit.

- **[STATUS]** DRAFT → squash-merge to `main` on green CI (auto-merge SQUASH).

- **[TEST]** Existing suite 3719/3719. The scroll is DOM behaviour (verified by build).

- **[KNOWN GAPS — deferred, asked operator]** The operator also wants, in the portrait-tablet listing:
  (a) the **listing header frozen** (Showing N + the ✔ Confirmed / ? Ask-first column titles), (b) the
  **two columns independently scrollable**, (c) per-column independent expand (already per-card). That's
  a portrait-tablet restructure with a genuine **map-placement fork** (keep the ~40vh map sticky above,
  vs collapse it so the two columns get full height) — confirming the intent before building rather than
  guessing a layout I can't verify renders correctly in the sandbox. Also: Cuisine `tg.js` safe-area gap
  (mirror of v0.62.591); folio-tab folder connection; dark-mode carousel glass; Transport port.

## Confirmation Gates
| Gate | Status | Note |
|------|--------|------|
| G1 | ✅ | PATCH bump 0.62.591 → 0.62.592; Cuisine build + 3719 tests green. |
| G2 | ➖ | no destructive action. |
| G3 | ➖ | no new decision rule. |
| G4 | ➖ | no external paid API call. |
