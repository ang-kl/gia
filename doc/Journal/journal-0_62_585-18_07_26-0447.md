# Journal — v0.62.585 (Cuisine: social-media card glyphs were invisible)

_(№ 929 — 18-07 '26 04:47 SGT)_

## New

### [HDR] v0.62.585 — Cuisine: the result card's social buttons show their glyphs again

- **[INTENT]** Operator (IMG_3522/3523/3524, phone) — five items on the result card + folio picker:
  1. *"Card information has severe missing information that was canonical into it"* (IMG_3522: the
     SocialButtons after Maps/Copy were **empty bordered boxes**).
  2. *"alignment still a problem to seamless stitch between the tab header and tab details."*
  3. *"colour of tab details should follow the tab header."*
  4. *"tab detail should have curved corner edges except the wedge need to stitch to the tab header."*
  5. *"The line still there. We had solve this problem before, check past PR."*

- **[DELTA — item 1 (fixed)]** `web/cuisine/src/v2/components/SocialButtons.jsx`. The brand glyphs
  render `fill="currentColor"`, but the button set **no** text colour — so on the focused card
  (`bg-white` + a local `--tg-text` override) it inherited the **app's raw `color`** (light in dark
  mode) → a white glyph on a white button = an **empty box**. Added `text-tg-text` to the button so
  the glyph binds to the card's resolved ink (dark on the focused white card) in both themes.

- **[NOT done — items 2–5, the folio-tab folder connection]** These broke when the picker panel became
  a **fixed overlay** (v0.62.575): the seamless in-flow folder (past **v0.62.264** — `-mt-2` butts the
  panel under the active tab, the header drops its bottom border, tab + panel read as **one folder**)
  no longer applies; the panel now floats at `top: headerBottom`, detached. The **line** (item 5) is
  the inactive tab's bottom border, **just removed in v0.62.584** — the operator is likely on an older
  build, so asked them to re-check on the new one. The **seamless stitch + wedge + rounded-except-
  wedge** (items 2–4) need the panel to visually reconnect to the active tab (an absolute-stitch or a
  return to in-flow), which trades against the floating overlay that fixed the push-down/blackout —
  flagged for a focused pass + an operator call on the approach rather than a blind pixel guess.

- **[VERIFICATION (sandbox)]** Cuisine `vite build` green; full `npm test` **3719/3719 across 145
  files**; `package.json` 0.62.584 → 0.62.585; `.serial-state.yml` commit 1061→1062, build 684→685,
  journal 928→929, re-anchor 18-07 04:47 SGT. ⚠️ Live-only.

- **[STATUS]** DRAFT → squash-merge to `main` on green CI (auto-merge SQUASH).

- **[TEST]** Existing suite 3719/3719 (a `text-tg-text` colour binding).

- **[KNOWN GAPS]** Folio-tab folder connection (items 2–5) pending a focused pass + operator confirm;
  dark-mode carousel glass (v0.62.209 tradeoff); Transport TMA port.

## Confirmation Gates
| Gate | Status | Note |
|------|--------|------|
| G1 | ✅ | PATCH bump 0.62.584 → 0.62.585; Cuisine build + 3719 tests green. |
| G2 | ➖ | no destructive action. |
| G3 | ➖ | no new decision rule. |
| G4 | ➖ | no external paid API call. |
