# Journal — v0.62.656 (doc-system catch-up: the first vault, three documents, and a 30-PR Vibe Journal refresh)

_(№ 1,180 — 29-07 '26 18:12 SGT)_

## New

### [HDR] v0.62.656 — the three artefacts owed after the convergence arc

- **[INTENT]** Operator: *"vault + Documentation + vibe journal"* — the catch-up owed after the
  v0.62.647 → v0.62.655 arc (PRs #1657–#1665).

- **[1 — THE FIRST VAULT]** `vault/v0.62.655/`. This repository had **never** had one, despite the
  protocol existing since contract v0.0.4.

  A full mirror per `CLAUDE-FULL.md` §16.1 is **2,100 files, ~239 MB**, dominated by `geoloc/`
  (119 MB of raw GEOJSON) and `data/` (51 MB). Because committing that permanently bloats every future
  clone, I put the choice to the operator with a ~20 MB reduced-mirror alternative. **The operator
  chose the full mirror, exactly per protocol** — maximum reproducibility, accepting the size cost.
  The choice and its reasoning are recorded verbatim in `VAULT_README.md` so the decision is auditable
  rather than inferred.

  All eight protocol exclusions **verified absent after the copy**: `node_modules/`, `.git/`, nested
  `vault/`, `public/<tma>/assets/`, `.claude/settings.local.json`, `.env`/`*.log`, `tmp/`,
  `migration_audit.log`. The README carries every required section — headline, captured timestamp,
  exclusion list verbatim, file count + size, boot instructions, and the nine-version arc table.

  **Rule V-2 satisfied:** no prior vault exists to supersede, and the README says so explicitly.

- **[2 — DOCUMENTATION]** Three targeted-addition documents, following the convention already
  established in this repo (reference the prior, append the new, never restate):
  - `doc/Technical/technical-0_62_655-29_07_26-1812.md` (№ 61) — seven decisions, including the three
    **silent-drop** Tailwind failures of this arc and the durable lesson: *all three were caught by
    something measuring computed style or built output; none by a unit test asserting on a class
    string.* Plus a 5-row Deprecated table.
  - `doc/Feature/feature-0_62_655-29_07_26-1812.md` (№ 61) — seven feature blocks and a 7-row Removed
    table.
  - `doc/Register/register-0_62_655-29_07_26-1812.md` (№ 135) — 10 Completed, 8 Open, 3 Deferred, and
    six new Decisions **D-22 … D-27**.

- **[3 — VIBE JOURNAL]** Rebuilt from the **squash-merge history** rather than 30 API calls — the
  generator's own header documents that route (`git log --name-only origin/main`), and it is both
  cheaper and exactly reproducible.

  **30 PRs appended**: #1638–#1665, *plus a backfill of #1456 and #1487* which had been missed in an
  earlier catch-up and would otherwise have stayed permanently absent. `GEN_DATE` bumped
  `2026-07-21 → 2026-07-29`. Generator run rewrote `records.tsv` (**1,608 → 1,638 rows**),
  `vibe-coding-record.md`, `public/doc/vibe-journal.html` and `.json`.

  Categories after the run: `feature=1117 fix=225 docs=99 copy=62 refactor=49 prompt-tune=45 test=21
  infra=20`, rework-flagged 149.

- **[VERIFICATION (sandbox)]** Generator ran clean. FULL `npm test` **3821/3821 across 152 files** —
  unchanged from v0.62.655, as expected: **no application source was touched this entry.** The five
  TMA builds are likewise unchanged and were not re-run for that reason.
  `package.json` + lockfile 0.62.655 → 0.62.656; `.serial-state.yml` commit 1138→1139, build 757→758,
  journal 999→1000, feature 60→61, technical 60→61, register 134→135, re-anchor
  **29-07 '26 18:12 SGT**.

- **[TIME ANCHOR]** data.gov.sg blocked (`403` `CONNECT`); container clock `2026-07-29T10:12Z` =
  **29-07 '26 18:12 SGT** (TF-9).

- **[STATUS]** DRAFT → squash-merge to `main` on green CI (auto-merge SQUASH). New PR — branch
  restarted from `origin/main` (#1665 merged).

- **[TEST]** No new tests — this entry adds documentation and a snapshot; the generator is exercised
  by running it.

- **[KNOWN GAPS / caveats]**
  - **Rule V-3 applies:** the vault's filesystem state cannot contain this journal entry, which
    describes the vault's own creation. The omission is recorded here and in the commit message.
  - The vault is a **~239 MB, 2,100-file** addition to git history. That is the operator's explicit,
    informed choice, but it is irreversible in practice and worth restating.
  - `doc/Builder/`, `doc/Persona/`, `doc/Legal/` and `doc/Chat/` were **not** updated this pass — the
    operator asked for vault, Documentation and Vibe Journal specifically, and nothing in this arc
    changed the builder framework, capability profile, legal surface, or requires a chat transcript
    append.
  - **O-56 remains the highest-priority open item:** the Cuisine drawer is unverified on a real
    client.

## Confirmation Gates
| Gate | Status | Note |
|------|--------|------|
| G1 | ✅ | PATCH bump 0.62.655 → 0.62.656; 3821 tests green; generator ran clean. Docs-and-snapshot only, no application source touched. |
| G2 | ➖ | nothing removed. The vault is purely additive; the three documents are targeted additions that restate nothing. |
| G3 | ➖ | no new decision rule proposed here — D-22…D-27 are *recorded* in the Register, having been decided during the v0.62.647–655 arc. |
| G4 | ➖ | no external paid API call (the Vibe Journal was built from local git history rather than the GitHub API). |
