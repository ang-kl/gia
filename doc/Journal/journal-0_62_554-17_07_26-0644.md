(№ 911 - 17-07 '26 06:44 SGT) v0.62.554

# Journal — soleat v0.62.554 (Hawker carousel: kill the horizontal band · grey the unwired expand · toggle label "Cards")

> Operator (IMG_0687 + IMG_0688, iPad Pro landscape, v0.62.553), three points:
> 1. Why is there a horizontal "boundary line" across the screen for the cards in landscape?
> 2. The map's ⇲ expand button in landscape should be greyed off — it isn't wired there.
> 3. The map/cards toggle should say "Cards", not "List" — the collapsed content is the card
>    carousel (landscape AND portrait-expanded).

## New

### [HDR] #911 | 06:44 SGT 17-07-26 | v0.62.554 — carousel band fix + unwired-expand greyed + "Cards" label | 3 files | PR #(pending) |

- **[FIX 1 — the horizontal band].** `web/hawker/src/App.jsx` `CentreCarousel`: the flex track defaulted
  to **`align-items:stretch`**, so every card grew to the tallest card's height and the shorter cards
  filled their extra space with card-background — that filled band, plus the heavy `shadow-xl` bridging
  the 8 px gaps, read as one horizontal line across the width. Now **`items-end`** (natural card heights
  on a common baseline; the map shows through the uneven areas) + **`shadow-lg`** (lighter; the drop
  shadows no longer bridge the gaps). `snap-center` unchanged (the 3-in-focus centering).

- **[FIX 2 — grey the unwired expand].** `web/hawker/src/components/HawkerMapPanel.jsx`: in the LANDSCAPE
  full-bleed carousel the map is already full and there is no in-place expand, so the ⇲ button did
  nothing (`fill` + no `onToggleExpand`). New `expandWired = !fill || !!onToggleExpand`; when false the
  button is **`disabled` + greyed** (`bg-gray-200 text-gray-400 cursor-not-allowed`). It stays live
  everywhere it works (phone grows the map in place; the portrait-tablet panel + portrait-expanded
  carousel drive App's `mapExpanded`).

- **[FIX 3 — "Cards" not "List"].** `web/hawker/src/i18n.js`: `btn.showList` "List" → **"Cards"** (fr
  "Cartes"). The map toggle only appears over the card carousel (landscape + portrait-expanded), so
  `🗺 Map` / `📋 Cards` now names the content correctly in both.

- **[DELTA — housekeeping].** `package.json` 0.62.553 → **0.62.554**; Journal #911 + serial-state
  (commit 1028→**1029**, build 653→**654**, journal 910→**911**; re-anchor 17-07 '26 06:44 SGT,
  prior 910 demoted verbatim AU-7).

- **[VERIFICATION (sandbox)].** `node --check i18n.js` OK; **Hawker vite build green** (42 modules);
  **full `npm test` 3719/3719 across 145 files**. ⚠️ The band removal, greyed expand, and label are
  live-only (iPad) — shipping live.

- **[STATUS]** STAGED → PROD on merge (operator "make it live").

- **[KNOWN GAPS].**
  1. 👀 **iPad recheck** — the horizontal band is gone (cards float cleanly over the map)? The ⇲ expand
     reads greyed/disabled in landscape? The toggle says 🗺 Map / 📋 Cards? If a faint line persists it
     may be a different element (e.g. the top-bar border or a screen-recording seam) — flag with a shot.
  2. 🅺🅸🆅 Hawker CLEANING-closure feature still shelved (needs a data.gov.sg sample; sandbox egress-blocked).

## Confirmation Gates
| Gate | Status | Note |
|------|--------|------|
| G1 | ✅ | PATCH bump 0.62.553→0.62.554; UI polish/bugfix; build + 3719 tests green |
| G2 | ➖ | no destructive action |
| G3 | ➖ | no new decision rule |
| G4 | ➖ | no external paid API call |
