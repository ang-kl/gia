(№ 907 - 17-07 '26 05:39 SGT) v0.62.550

# Journal — soleat v0.62.550 (Hawker portrait-tablet: anchored map + scrollable list panel, expand→carousel · standardised left nav cluster)

> Operator (point 4, queued after points 1–3 shipped in #1558):
> 4a. In PORTRAIT tablet/desktop, the list should have a **separate scrollable panel**, with the
>     Google map **anchored at the top** of the panel. When the user **taps expand** on the map (the
>     ⇲ with the zoom/nav side buttons), the **list turns into the carousel** style from point 2.
> 4b. Show the **same standardised left-side map navigation buttons** as the phone's embedded map.

## New

### [HDR] #907 | 05:39 SGT 17-07-26 | v0.62.550 — portrait-tablet anchored-map panel + expand→carousel + left nav cluster | 2 files | PR #(pending) |

- **[DELTA 4a — portrait-tablet anchored map + scrollable list panel + expand→carousel].**
  - `web/hawker/src/components/HawkerMapPanel.jsx` — the ⇲/⇱ expand is now **controllable**: new
    props `expanded` + `onToggleExpand`; when `onToggleExpand` is passed the button is controlled
    (App owns the state), else it stays local (phone, grows the map in place). `toggleExpand()`
    routes accordingly.
  - `web/hawker/src/App.jsx` — new `mapExpanded` state + `portraitTablet = isWide && portrait`.
    The Cuisine full-carousel block is refactored into a reusable **`carouselLayout(collapsible)`**;
    landscape calls `carouselLayout(false)` (always full), portrait-expanded calls
    `carouselLayout(true)` (the map's ⇱ collapses back via `setMapExpanded(false)`). New
    **`portraitTabletPanel()`**: header + region chips + the **map anchored at the top** (`shrink-0`)
    + a **separate `flex-1 overflow-y-auto` list panel** (2-col grid) below — the map does not
    scroll away. The map's ⇲ calls `setMapExpanded(true)` → the layout switches to the carousel.
    Dispatch: `landscapeTablet → carousel(false)`; `portraitTablet && mapExpanded → carousel(true)`;
    `portraitTablet → panel`; else phones = the existing stacked scroll layout (unchanged).

- **[DELTA 4b — standardised left nav cluster].** `HawkerMapPanel.jsx` — the left nav cluster
  (zoom readout/reset · ＋ · ↹ · － · ⇲), the same set as the phone map, now **drops below the top
  bar in the full-bleed (`fill`) layout** (`top: calc(--tg-content-safe-area-inset-top + 6rem)`) so
  all five buttons stay visible instead of being clipped behind the carousel top bar.

- **[DELTA — housekeeping].** `package.json` 0.62.549 → **0.62.550**; Journal #907 + serial-state
  (commit 1024→**1025**, build 649→**650**, journal 906→**907**; re-anchor 17-07 '26 05:39 SGT,
  prior 906 demoted verbatim AU-7).

- **[VERIFICATION (sandbox)].** `node --check mapOverlays.js` OK; **Hawker vite build green** (42
  modules); **full `npm test` 3719/3719 across 145 files**. ⚠️ The portrait-tablet panel, the
  expand→carousel switch, and the nav-cluster position are live-only (iPad) — shipping live.

- **[STATUS]** STAGED → PROD on merge (operator "make it live").

- **[TEST]** Suite excludes `web/`; JSX validated by the vite build. No new unit test (layout).

- **[KNOWN GAPS].**
  1. 👀 **iPad recheck** — portrait tablet: map anchored on top, list scrolls in its own panel below?
     ⇲ expand flips to the carousel + ⇱ collapses back? All five left nav buttons visible in the
     carousel? Default map height in the panel (≈55vh) still tunable.
  2. 🧭 The FooterNav ⇡ top / ⇣ down FABs scroll the WINDOW, but the portrait-tablet panel + carousel
     are fixed layouts (the list scrolls an inner div) — the FABs are inert there (native scroll
     works). Wire them to the inner panel if the operator wants the FABs functional.
  3. 🧭 Port the anchored-map + expand→carousel pattern to **Cuisine + Transport** if desired.

## Confirmation Gates
| Gate | Status | Note |
|------|--------|------|
| G1 | ✅ | PATCH bump 0.62.549→0.62.550; additive/layout, gated to portrait tablet/desktop; build + 3719 tests green |
| G2 | ➖ | no destructive action (carousel block refactored into a shared fn, behaviour preserved) |
| G3 | ➖ | no new decision rule |
| G4 | ➖ | no external paid API call |
