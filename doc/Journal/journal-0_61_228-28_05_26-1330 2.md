(№ 429 - 28-05 '26 13:30 SGT) v0.61.228

# Journal — soleat v0.61.228 (Cuisine TMA: cascading city dropdown mirrors Menu TMA)

> Operator (28-05 '26): *"proceed"* after merging v0.61.227. Mirrors
> the Menu TMA cascading city dropdown into the Cuisine TMA's OTHER
> picker, completing the second half of the operator's *"another
> field for common cities of the country, child dropdown button
> related to the country dropdown button"* arc.
>
> `/s` search wiring (third item of the arc) is tracked as v0.61.229+.
>
> Commit 441 / journal 429 / build 231, on branch
> `claude/v0.61.228-cuisine-city-cascade` off `main` at v0.61.227.

## New

### [HDR] #429 | 13:30 SGT 28-05-28 | v0.61.228 — Cuisine TMA city cascade | 4 files | 1 PR | 1 build |

- **[INTENT]** Match v0.61.227's UX in the Cuisine TMA so the user
  has the same one-tap city pick whether they're picking a
  destination from the Menu TMA or the Cuisine TMA.

- **[DELTA]**

  **1. NEW `web/cuisine/src/v2/lib/cities.js`** — byte-identical
  copy of `web/menu/src/cities.js` (16 countries, 133 cities, MY
  exception of 15 capitals). Header rewritten to call out the
  intentional duplication, same convention as the existing
  `countries.js` mirror.

  **2. `web/cuisine/src/v2/components/LocationField.jsx`** —
  `OtherLocationPicker` grows:
  - import `citiesForCountry` from `'../lib/cities.js'`.
  - new `cityPick` state.
  - `useEffect(() => setCityPick(''), [country.code])` so the
    selection clears every time the country flips.
  - `onCityPick(name)` handler — looks the city up in the
    catalogue, calls `onSelect({ lat, lng, label })` (the
    existing prop that propagates to the Cuisine TMA's anchor
    handling — single source of truth), then clears local state.
  - A `<select>` rendered between the country flag and the
    search input, gated on `citiesForCountry(country.code).length > 0`.

  **3. `web/cuisine/src/v2/lib/i18n.js`** — adds `'loc.other.city'`
  → `'City'` / `'Ville'`, paralleling the Menu TMA entry.

  **4. `package.json`** — `0.61.227 → 0.61.228`.

  **5. `doc/.serial-state.yml`** — commit 440→441, build 230→231,
  journal 428→429; re-anchored 28-05 '26 13:30 SGT.

- **[VERIFICATION (sandbox)]**

  Local Vite build is bitten by the recurring iCloud / Node-24
  PostCSS quirk; CI's TMA-build job on Linux validates cleanly.
  The data file was smoke-tested at v0.61.227 (same content):
  16 countries, 133 cities. The Cuisine TMA copy is a `/bin/cp`
  of that verified file plus a header rewrite, so the data is
  identical.

- **[STATUS]** PR-OPEN on push.

- **[TEST]** Operator (post-merge + Railway redeploy):
  1. In Cuisine TMA OTHER mode, pick country = 🇲🇾 — new city
     `<select>` should appear between the flag and the input,
     listing all 15 capitals.
  2. Pick "Kuching" → Cuisine TMA's anchor jumps to Kuching's
     centroid; the result list re-fetches around those coords.
  3. Flip country to 🇯🇵 → city select swaps to 8 JP cities; the
     prior MY pick is cleared.
  4. Flip to 🇸🇬 → city select disappears (SG has no catalogue;
     the existing SG flow stays unchanged).

- **[KNOWN GAPS]**
  - `/s` chat search doesn't yet consume the per-country city
    list — operator's stated intent "It should affect Menu TMA
    and Cuisine TMA" is satisfied by v0.61.227 + this PR. The
    third item ("When I select a country in Menu TMA, it doesn't
    tie to the cuisine TMA or /s search") is tracked as v0.61.229.
  - Cuisine TMA's `cities.js` and Menu TMA's `cities.js` are
    intentionally duplicated. Any future expansion must edit
    BOTH files (a CI lint could enforce this — out of scope here).

- **Confirmation Gates**

  | Gate | Resolution | Date |
  |---|---|---|
  | G1 — version bump | PATCH `0.61.227 → 0.61.228`. Mirrors prior PR's UX into a sibling TMA. | 28-05 '26 |
  | G2 — destructive action | _n/a_. | 28-05 '26 |
  | G3 — new decision rule | YES — Cuisine TMA's `lib/cities.js` and Menu TMA's `src/cities.js` are intentionally duplicated and must stay in sync. | 28-05 '26 |
  | G4 — paid external API call | _n/a_. | 28-05 '26 |

## Amendments

- **`OtherLocationPicker`** in `web/cuisine/src/v2/components/LocationField.jsx` — adds the cascading city select between the country flag and the search input.

## Deleted

_None._
