(№ 784 - 29-06 '26 08:30 SGT) v0.62.430

# Journal — soleat v0.62.430 (Sketchbook operator batch — items 1, 2, 5, 6)

> Operator batch (14 items). This PR = the quick header/footer/filter text + layout wins.

## New

### [HDR] #784 | 08:30 SGT 29-06-26 | v0.62.430 — items 1,2,5,6 | 3 files | PR #(pending) |
- **1** footer tab 2 already shows the **{default cabinet}** label (confirmed; from P2).
- **2** header title now follows the **DEFAULT** cabinet — `App.jsx` `activeCabinetName` =
  default cabinet name (falls back to the open cabinet).
- **5a** Cuisine chip → "🍜 **Cuisine & Filter**". **5b/5c** dish chip → "📍 **Pick Local Dish**";
  `filter.dishTitle` → "**Filter Local Dish**" (sheet + tab).
- **6** FilterSheet text-mode **Save** is now a small pill. **6a** removed the "All" button
  (clearing the box + Save already drops the filter; the chip ✕ also clears).
- `lib/i18n.js`, `App.jsx`, `FilterSheet.jsx`.
- **[VERIFICATION (sandbox)]** real `vite build` OK (50 modules).
- **[STATUS]** PROD — clipboard TMA rebuild; redeploys per PR.
- **[REMAINING operator items]** 3 (duplicate indicator), 4 (cuisine filter = Criteria-Search
  layout + smaller margins), 7+11 (catch-all row under tabs + sort), 8 (location list grouped by
  country/city), 9 (drawer 3-buttons top-right + drawer-cards wiring), 10 (filed-card label +
  colour strip), 12a (date of copy), 12b (drawer description), 13 (free-text note), 14 (bot
  /clipboard change).

## Confirmation Gates

| Gate | Status | Note |
|------|--------|------|
| G1 build/parse | ✅ | real vite build |
| G2 tests | ⏳ | CI |
| G3 journal/serial | ✅ | serial 783→784 |
| G4 live deploy | ⏳ | redeploy |
