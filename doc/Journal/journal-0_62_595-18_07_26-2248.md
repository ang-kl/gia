# Journal — v0.62.595 (Hawker: cleaning/renovation closure tab + 2026 vault refresh + 2025→2026 text)

_(№ 939 — 18-07 '26 22:48 SGT)_

## New

### [HDR] v0.62.595 — Hawker centres show a cleaning/renovation closure tab; vault refreshed to 2026

- **[INTENT]** Operator uploaded NEA's *"Dates of Hawker Centres Closure"* CSV (18 Jul 2026) and approved
  three tasks:
  1. A **closure tab** above each Hawker card (mirroring the Cuisine TMA's "Closed" tab): *"Closed for
     cleaning till DDth"* (or *"till DD MMMM"* if it runs into a later month), **red**; renovation →
     *"Under Renovation till DDth / DD MMMM"*, **grey**. Both carousel + list, all devices.
  2. **Refresh the vault** stall counts / status / "(new)" from the fresher 2026 data.
  3. **2025 → 2026** across Hawker display / command / Telegram text.

- **[DELTA — data pipeline (Task 1 + 2)]**
  - `data/hawker-cleaning-closures-2026.csv` — the source CSV, committed.
  - `scripts/build-hawker-closures.js` — transforms it into `data/hawker-closures.json` (123 centres:
    Q1–Q4 cleaning windows + `other_works` renovation windows as ISO date ranges, plus postal, 2026 food/
    market stall counts, status, isNew). Renovation excludes "commence operations" new-opening notes.
  - `hawker-vault.js` — `loadClosures()` + a merge in `loadAll()` attaches `c.closures =
    { cleaning, renovation }`, matching **by postal first** (`c.postal` → `byPostal`, **113/122**, up from
    37 by name-fold alone) then exact/normalised name. The same merge **overrides `c.stalls` / `c.status`
    / `c.isNew`** from the 2026 CSV (Task 2 — stall coverage 37 → 113).
  - `index.js` — the `/api/hawker/centres-by-region` slim record now carries `closures`.

- **[DELTA — client tab (Task 1)]** `web/hawker/src/App.jsx`. `_activeClosure(closures)` returns the
  window TODAY falls inside (renovation takes precedence over a cleaning day); `_closureTill(endISO)`
  formats *"8th"* (same month, ordinal) / *"8 December"* (later month) / *"31 December 2029"* (later year).
  `renderCentreCard` wraps the card in a **protruding rounded-top tab** only when currently closed —
  `bg-red-600` "Closed for cleaning till …" / `bg-gray-500` "Under Renovation till …" (EN + FR; other
  langs fall back to EN). Because every layout renders through `renderCentreCard`, the tab shows on the
  **carousel + the list on phone / tablet / desktop**. No closure → the card is byte-unchanged.

- **[DELTA — text (Task 3)]** `web/hawker/src/i18n.js` `header.title` ×7 languages "(2025)" → "(2026)";
  `index.js` `/hawker` command + Telegram-info descriptions ×4 "hawker centres (2025)" → "(2026)".

- **[VERIFICATION (sandbox)]** `node --check` vault + index + generator OK; Hawker `vite build` green;
  full `npm test` **3719/3719 across 145 files** (`hawker-vault.test.js` 36 green); `package.json` +
  `package-lock.json` 0.62.594 → 0.62.595; `.serial-state.yml` commit 1073→1074, build 696→697, journal
  938→939, re-anchor 18-07 '26 22:48 SGT. On 2026-07-18 exactly **1** centre shows a tab (Blk 409 AMK
  Ave 10 — reno "till 7 August"); cleaning reds land on the Sep/Dec windows. Branch rebased onto
  `origin/main` after #1603 merged.

- **[STATUS]** DRAFT → squash-merge to `main` on green CI (auto-merge SQUASH).

- **[TEST]** 3719/3719. Closure date logic (window match, ordinal/month "till" format) is client DOM +
  server merge, verified by build + the by-date probe above.

- **[KNOWN GAPS]** **9/122** centres don't match the closure CSV (edge multi-block names with no postal
  hit — no tab, fail-open); closure-tab labels are **EN/FR only** (other languages degrade to EN). The
  vault MD (`list-of-hawker-centres.md`) stays the Jul-2025 snapshot for names/addresses/regions — the
  2026 refresh layers on via the closures merge (stalls/status), so the MD provenance is left accurate.
  Cuisine `tg.js` safe-area gap; folio-tab folder connection; dark-mode carousel glass; Transport port.

## Confirmation Gates
| Gate | Status | Note |
|------|--------|------|
| G1 | ✅ | PATCH bump 0.62.594 → 0.62.595; Hawker build + 3719 tests green. |
| G2 | ➖ | no destructive action (data added, not removed). |
| G3 | ➖ | no new decision rule. |
| G4 | ➖ | no external paid API call (operator-supplied CSV, committed offline). |
