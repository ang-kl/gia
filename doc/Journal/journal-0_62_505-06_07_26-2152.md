(№ 861 - 06-07 '26 21:52 SGT) v0.62.505

# Journal — soleat v0.62.505 (vault/v0.62.504 snapshot — the overdue backup, taken before structural cleanup)

> Operator: "pause. do a vault first" then "update journal, vibe-coding first, documents then change changes".
> Read as: before any audit-driven structural change (the vault/ cleanup proposed in the code-audit),
> bring the documentation current in this order — (1) take the overdue vault snapshot, (2) Journal,
> (3) VibeCodingRecord regen, (4) any due documents — and only then start the changes.

## New

### [HDR] #861 | 21:52 SGT 06-07-26 | v0.62.505 — vault/v0.62.504 snapshot (27th) | snapshot + 3 meta files | PR #(pending) |

- **[INTENT].** The Register (№ 130) and recent journals had "vault snapshot still due" logged since
  v0.62.502. A full-repo code audit (4 parallel auditors, 06-07) recommended deleting `vault/` as
  redundant with git; the operator paused that and ordered a **final snapshot first** — safety
  archive before any cleanup decision lands.

- **[METHOD].** Snapshot built via the gh Git Data API by **tree-sha reference**: the new
  `vault/v0.62.504/` entry points at a tree composed of HEAD `d844b928`'s root entries **minus
  `vault/` itself** (matching the composition of every prior snapshot, verified against
  `vault/v0.62.386/`). No blobs were re-uploaded — git's content-addressing means the snapshot
  costs one tree object, not 23k file copies.

- **[DELTA].**
  - `vault/v0.62.504/` — **NEW (27th snapshot)**: full source tree at v0.62.504 (HEAD `d844b928`,
    post PR #1509). Includes `index.js`, all root modules, `web/`, `__tests__/`, `doc/`, `data/`.
  - `doc/Journal/journal-0_62_505-06_07_26-2152.md` — this entry.
  - `doc/.serial-state.yml` — commit 978→**979**, journal 860→**861**; build unchanged at 615
    (docs/snapshot-only, no code build); anchor re-set `06-07 '26 21:52 SGT`.
  - `package.json` — 0.62.504 → **0.62.505**.

- **[VERIFICATION (sandbox)]** Snapshot tree verified = HEAD root minus `vault` (entry-count and
  sha comparison). No `.js` touched (`node --check` N/A). Runtime-dependency check for the coming
  cleanup: `vault-index.js` / `vault.js` / cron "vault sync" all operate on **Redis** keys
  (`gia:vault`), never on the `vault/` directory — deletion, when approved, is runtime-safe.

- **[STATUS]** PROD — merges to `main` (repo-content only; no deploy surface change).

- **[TEST]** N/A (no code change). CI's 4 required checks still gate the PR.

- **[KNOWN GAPS]** Ordered by the operator, still due after this PR: **VibeCodingRecord regen**
  (ledger last at ~PR #1503; main is at #1509+), companion `doc/Technical/` arc entry. Then the
  audit-driven changes (vault/ cleanup, workspace consolidation) — each gated on operator's go.
  O-49/O-50/O-51 and DF-1 (MRT flip 12-Jul) unchanged.

## Confirmation Gates
| Gate | Status | Note |
|------|--------|------|
| G1 | ➖ | no code — snapshot + docs; version bumped 0.62.504→0.62.505 |
| G2 | ✅ | vault protocol followed: snapshot is additive (27th), prior 26 untouched |
| G3 | ✅ | serial commit 978→979, journal 860→861; anchor re-set 06-07 '26 21:52 SGT |
| G4 | ➖ | no user-facing surface |
