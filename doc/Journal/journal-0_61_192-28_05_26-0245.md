(№ 393 - 28-05 '26 02:45 SGT) v0.61.192

# Journal — soleat v0.61.192 (Menu TMA OTHER picker mirrors v0.61.191)

> Operator: *"do note that the design change happens if user select
> Others. If others is select, change to: [🇲🇾 ▾] [Times Square
> Kuala Lumpur________] [🔍] or else stay as current location text
> box."* v0.61.191 shipped the Cuisine TMA half; v0.61.192 ships
> the same flow for the Menu TMA.
>
> Commit 405 / journal 393 / build 195, on branch
> `claude/v0.61.192-menu-other-picker` off `main` at v0.61.191.

## New

### [HDR] #393 | 02:45 SGT 28-05-28 | v0.61.192 — Menu TMA OTHER picker | 6 files | 1 PR | 1 build |

- **[DELTA]**
  - **NEW `web/menu/src/countries.js`** — duplicate of
    `web/cuisine/src/v2/lib/countries.js` (Menu TMA has a separate
    Vite build → no shared package). 16-entry frozen array, ASEAN-9
    + Oceania-2 + N-Asia-5. Default 'MY'. Same `findCountry`.
  - **`web/menu/src/i18n.js`** — adds 7 `loc.other.*` keys mirroring
    the Cuisine TMA's v0.61.191. EN + FR.
  - **`web/menu/src/components/LocationFieldMenu.jsx`** (4 edits):
    1. Imports the new countries module + new i18n keys.
    2. 4 new state hooks: `countryPref` (default 'MY'),
       `otherResults`, `otherSearching`, `otherNoMatch`.
    3. New handlers:
       - `onOtherSearch` — POST to `/api/cuisine/place-search-by-country`
         (reuses v0.61.191's endpoint) with `body.initData`.
       - `pickOtherResult(r)` — calls `postSetLocation({ lat, lng,
         label, country })` to set the anchor directly.
       - `cancelOtherSearch` — clears the results panel.
    4. JSX branches on `currentAnchor.region === 'OTHER'` (legacy
       'MY-PUT' also routes here):
       - Hides the precinct `<select>` (no SG/JB precincts apply).
       - Hides the autocomplete suggestions panel.
       - Renders: country flag dropdown + free-text input + 🔍
         Search button + 5-row confirmation list + cancel row.
       SG/JB anchors keep the existing UI exactly.
  - **`index.js`** — extends `/api/menu/set-location` to accept a
    direct `{ lat, lng, label, country }` body shape. New branch
    early-returns before the precinctId / text branches; sets
    `region: 'OTHER'`, `radiusCapM: 20000`. `country` is optional
    (ISO 3166-1 alpha-2) and stored alongside the anchor for
    telemetry / future per-country caps.
  - **`package.json`** — `0.61.191 → 0.61.192`.
  - **`doc/.serial-state.yml`** — commit 404 → 405, build 194 →
    195, journal 392 → 393.
  - **`doc/Journal/journal-0_61_192-28_05_26-0245.md`** — this entry.

- **[VERIFICATION (sandbox)]**

  ```
  $ node --check index.js                          (OK)
  $ cd web/menu && npm run build                   (✓ built in 1.86s)
  ```

- **[STATUS]** PR-OPEN on push. Operator merges manually.

- **[TEST]** Operator (post-merge + Railway redeploy + Menu TMA
  cache bust):
  - With Putrajaya / OTHER anchor active:
    - Open Menu TMA → location section reads `📍 Search anchor`,
      then `Anchored at <X> · 20 km cap`, then the OTHER picker:
      `[🇲🇾 MY ▾] [Type a place + Search________] [🔍]`.
      No precinct dropdown, no autocomplete suggestions.
    - Pick 🇲🇾 + type "Times Square Kuala Lumpur" + tap 🔍 →
      confirmation panel `Found in 🇲🇾 Malaysia:` with up to 5
      results, all in Malaysia.
    - Tap a result → anchor moves; subsequent tile launches use it.
    - Switch country to 🇯🇵 + type "Shibuya Crossing" + 🔍 → JP
      results in the confirmation panel.
  - Switch anchor back to SG (via chat `/location` or any SG
    precinct from another path) → Menu TMA reverts to the existing
    precinct `<select>` + free-text + autocomplete UI.

- **[KNOWN GAPS]**
  - **`countryPref` is component-local** — not persisted in Redis
    yet. Refresh / cross-device opens lose the preference. Future
    work could mirror this to `userlocale:countryPref:<chatId>`.
  - **No recent-locations drawer** — operator's "up to 10 like
    clipboard" idea is deferred to v0.61.193+.
  - **/location chat command rework** — deferred.
  - **SG-only chip gating** (Healthier Choice, Durian) — separate
    PR.

- **Confirmation Gates**

  | Gate | Resolution | Date |
  |------|-----------|------|
  | G1 — version bump | PATCH `0.61.191 → 0.61.192`. New TMA branch + new endpoint shape. | 28-05 '26 |
  | G2 — destructive action | _n/a_ — pure additions. SG/JB path untouched. Existing `/api/menu/set-location` body shapes (`precinctId`, `text`) still work. | 28-05 '26 |
  | G3 — new decision rule | YES — Menu TMA OTHER anchor flow is now flag picker → free-text → Search → confirmation list → direct set-location. | 28-05 '26 |
  | G4 — paid external API call | Same as v0.61.191 — one Places `searchText` per Search-button tap, rate-limited 500/hr/chat. | 28-05 '26 |

## Amendments

- **Menu TMA `LocationFieldMenu` OTHER policy** — v0.61.189 fully
  restored the picker for OTHER (revert of v0.61.187/188's
  hide-everything experiments). v0.61.192 now selectively replaces
  the picker UI with the country-flag flow for OTHER anchors —
  same intent as v0.61.187 but with a working typing experience
  (the country dropdown narrows Places to one country, so the
  confirmation list stays short + relevant).
- **`/api/menu/set-location` request shape** — adds a third path
  alongside `{ precinctId }` and `{ text }`: `{ lat, lng, label,
  country? }` for the OTHER picker's direct-coords commit.

## Deleted

_None._
