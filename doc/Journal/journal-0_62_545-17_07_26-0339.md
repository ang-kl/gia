(№ 902 - 17-07 '26 03:39 SGT) v0.62.545

# Journal — soleat v0.62.545 (Hawker tablet layout: request fullscreen so it fills the iPad screen)

> Operator (IMG_0669 landscape + IMG_0670 portrait, iPad Pro, v0.62.544): both modes **don't fill the
> screen**. Root cause: on iPad, Telegram opens the Mini App as a **centered modal** — the v0.62.544
> full-bleed layout only filled that small modal, and the modal's tiny webview never even triggered the
> tablet layout (it showed the *mobile* layout boxed in the middle).

## New

### [HDR] #902 | 03:39 SGT 17-07-26 | v0.62.545 — iPad fullscreen for Hawker | 3 files | PR #(pending) |

- **[FIX].** Re-introduce Telegram **auto-fullscreen for touch tablets only** (Bot API 8.0
  `requestFullscreen`) so the iPad webview covers the whole screen; then the v0.62.544 full-bleed layout
  fills it and the resize flips `useViewport` to the tablet layout. This **reverses v0.62.286** (which had
  removed auto-fullscreen entirely) **for tablets only** — Telegram Desktop / notebook (the v0.62.286
  concern) stays excluded.

- **[DELTA].**
  - **`web/hawker/src/tg.js`** — new `safe('tablet-fullscreen', …)` after `expand()`: calls
    `requestFullscreen()` iff `requestFullscreen` exists + `isVersionAtLeast('8.0')` + **platform is
    `ipados`/`ios`/`android`** (not `tdesktop`/`macos`/web) + **coarse pointer** + `min(screen.width,
    screen.height) ≥ 768`. Gated on the physical **`window.screen`** size (not the modal webview's
    `innerWidth`) to avoid a chicken-and-egg — the modal reports a small width pre-fullscreen.
    - **NB (review catch, Codex bot on #1554, P1):** Telegram reports iPad as **`ipados`**, not `ios` — the
      existing iPad gates (`web/menu/src/tg.js:54`, `web/cuisine/src/api/tg.js:125`) key on `ipados`. My
      first pass had `ios`/`android` only → fullscreen would have been **skipped on the very device this
      fixes**. Corrected to include `ipados` before merge (verified against those two files).
  - **`web/hawker/src/App.jsx`** — the tablet panel gets `paddingTop: var(--tg-content-safe-area-inset-top,
    env(safe-area-inset-top, 0px))` so its content clears Telegram's fullscreen top controls.
  - `package.json` 0.62.544 → **0.62.545**; Journal #902 + serial-state (commit 1019→**1020**, build
    644→**645**, journal 901→**902**; re-anchor 17-07 '26 03:39 SGT, prior 901 demoted verbatim AU-7).

- **[VERIFICATION (sandbox)].**
  - `node --check web/hawker/src/tg.js` OK; **Hawker vite build green**.
  - ⚠️ **Cannot drive the live TMA in-sandbox** — the fullscreen behaviour + the resulting layout can only
    be confirmed on the real iPad. Merging to deploy so the operator can verify.

- **[STATUS]** STAGED → PROD on merge (operator "make it live").

- **[KNOWN GAPS].**
  1. 👀 **iPad verification** — does it now fill the screen (both orientations)? Is the top inset right, and
     does the map fill behind the panel? Panel width/opacity/columns still first-pass.
  2. 🧭 If good, apply the same tablet-fullscreen to **Cuisine + Transport** when porting the layout.
  3. Reversal of v0.62.286 is **tablet-scoped**; desktop auto-fullscreen stays off.

## Confirmation Gates
| Gate | Status | Note |
|------|--------|------|
| G1 | ✅ | PATCH bump 0.62.544→0.62.545; tablet-gated, desktop excluded; build green |
| G2 | ➖ | no destructive action |
| G3 | ⚠️ | reverses the v0.62.286 "no auto-fullscreen" rule for TABLETS only, per explicit operator request (desktop unchanged) |
| G4 | ➖ | no external paid API call |
