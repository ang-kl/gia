(№ 882 - 10-07 '26 13:45 SGT) v0.62.526

# Journal — soleat v0.62.526 (Register: file the ms/vi deferred-locale entries)

> Closes a `[KNOWN GAP]` carried across three consecutive journals (#879, #880, #881): the `ms`/`vi`
> locale candidates from the Language & Locale Specification work were repeatedly flagged as "still
> due" for `doc/Register/` but never actually filed. Filed here.

## New

### [HDR] #882 | 13:45 SGT 10-07-26 | v0.62.526 — Register DF-4/DF-5 (ms, vi) | 4 files | PR #(pending) |

- **[INTENT].** File the deferred-locale candidates raised by `doc/Technical/technical-0_62_522-…md`
  Rules L-12/L-13 (v0.62.522–523) as proper Register items, so they're tracked as a decision record
  rather than a recurring journal footnote.

- **[DELTA].**
  - `doc/Register/register-0_62_526-10_07_26-1345.md` — **NEW**, continues № 130
    (`register-0_62_502-…md`). Adds:
    - **DF-4** — Malay (`ms`): Singapore official language, ~9.2% resident home-language share,
      Malaysia a top-5 SG tourist source; outranks `de` on SG-relevance but not the ratified
      reach criterion. Reactivates on the L-13 trigger.
    - **DF-5** — Vietnamese (`vi`): ~Telegram's 7th-largest national base; stronger reach case than
      several shipped locales, still deferred under L-12. Reactivates on L-13 or a standalone
      reach-only decision.
    - **D-132** — records the L-12/L-13 ratification itself (PR #1530) as a Register decision, not
      just a Technical-doc rule.
    This is a **targeted addition** (AU-1) — does not restate № 130's Open/Decisions/Completed
    tables, which remain live as recorded there.
  - `doc/Journal/journal-0_62_526-10_07_26-1345.md` — this entry.
  - `doc/.serial-state.yml` — commit 999→**1000**, build 625→**626**, journal 881→**882**,
    register 130→**131**; re-anchor 10-07 '26 13:45 SGT (prior 881 anchor demoted verbatim, AU-7).
  - `package.json` — 0.62.525 → **0.62.526**.

- **[VERIFICATION (sandbox)].** Base `origin/main` @ v0.62.525 (PR #1532), read via `gh api …?ref=main`
  per Rule L-2. Register figures (DF-4/DF-5 rationale) cross-checked against the merged
  `technical-0_62_522-…md` §1.2 text, not re-derived. No runtime file touched.

- **[STATUS]** PROD — doc-only; merges to main, per-PR bot redeploys (no served surface changes).

- **[TEST]** No `index.js` / handler / search / render / `i18n.js` code change → outside the
  `gia-preflight` trigger set; no web build. Full `npx vitest run` in CI.

- **[KNOWN GAPS].**
  1. 🗺️ **#1525** — CCL6 launch flip, HELD open for 12 Jul 2026. Only remaining dated item.
  2. 🗄️ **Local iCloud git tree** — still ~70 PRs behind `origin/main` (flagged during #1531).
     This PR, like the three before it, was built from a fresh scratch state, not the local tree.

## Confirmation Gates
| Gate | Status | Note |
|------|--------|------|
| G1 | ✅ | PATCH bump 0.62.525→0.62.526; doc-only, standard per-PR rule |
| G2 | ➖ | no destructive action; № 130 untouched, only new content added |
| G3 | ➖ | no new decision rule — files an existing one (L-12/L-13) as a Register item |
| G4 | ➖ | no external paid API call |
