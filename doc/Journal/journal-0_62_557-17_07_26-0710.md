(№ 914 - 17-07 '26 07:10 SGT) v0.62.557

# Journal — soleat v0.62.557 (Hawker: card↔pin 3 s highlight both ways · Bib stall names are hidden Google-Maps links)

> Operator: "When I tap the card in landscape mode, you should [wire] to highlight 3 seconds the pin,
> and vice versa. And the card should contain Bib Gourmand information … but now with hidden hyperlink
> to the Google Map of the stall in the hawker centre that achieved Bib Gourmand."

## New

### [HDR] #914 | 07:10 SGT 17-07-26 | v0.62.557 — card↔pin 3 s highlight (both ways) + Bib stall hidden links | 2 files | PR #(pending) |

- **[DELTA 1 — card ↔ pin highlight, both directions].**
  - `web/hawker/src/components/HawkerMapPanel.jsx` — `activateCentre` (the shared centre-tap flow, run
    by both a pin tap and a card tap) now **pulses the pin for 3 s** (`highlightLoc(lat, lng, 3000)` —
    the map is already at the point + zoom, so this only adds the halo). New **`onCentreTap`** prop
    (via `onCentreTapRef`, stale-safe) fires with the centre name on every activation.
  - `web/hawker/src/App.jsx` — `onCentreTap = (name) => setActivePill('<name>|card')` is passed to all
    three `HawkerMapPanel` instances, so **tapping the PIN highlights the matching card** (accent ring)
    — the "vice versa" of card-tap → pin. (Card tap already runs `activateCentre` via
    `__giaHawkerFocusCentre`, so it now gets the 3 s pin pulse too.)

- **[DELTA 2 — Bib stall names are hidden hyperlinks].** `web/hawker/src/App.jsx` — the
  `✳️ Bib Gourmand · <stalls>` row renders each stall as a **hidden hyperlink** (styled like the hint
  text, `no-underline`) to that stall's Google Maps location within the centre
  (`?q=<stall> <centre> Singapore`). `stopPropagation` so tapping a stall link opens Maps without
  firing the whole-card tap.

- **[DELTA — housekeeping].** `package.json` 0.62.556 → **0.62.557**; Journal #914 + serial-state
  (commit 1031→**1032**, build 656→**657**, journal 913→**914**; re-anchor 17-07 '26 07:10 SGT,
  prior 913 demoted verbatim AU-7).

- **[VERIFICATION (sandbox)].** **Hawker vite build green** (42 modules); **full `npm test` 3719/3719
  across 145 files**. ⚠️ The 3 s pulse, the pin→card ring, and the Bib links are live-only (iPad) —
  shipping live.

- **[STATUS]** STAGED → PROD on merge (operator "make it live").

- **[KNOWN GAPS].**
  1. 👀 **Device recheck** — card tap pulses the pin 3 s; pin tap rings the matching card; each Bib
     stall name opens its Google Maps (and doesn't fire the card tap)?
  2. 🧭 A pin tap sets the card active via `activePill`, so a subsequent tap on that same card counts
     as the "2nd tap" (opens Maps) — flag if that two-tap coupling feels off; can decouple the
     pin-sync ring from the card's own tap counter if wanted.
  3. 🅺🅸🆅 Hawker CLEANING-closure still shelved (needs a data.gov.sg sample; sandbox egress-blocked).

## Confirmation Gates
| Gate | Status | Note |
|------|--------|------|
| G1 | ✅ | PATCH bump 0.62.556→0.62.557; additive interaction; build + 3719 tests green |
| G2 | ➖ | no destructive action |
| G3 | ➖ | no new decision rule |
| G4 | ➖ | no external paid API call |
