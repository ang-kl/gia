# Journal — v0.62.602 (Transport TMA: header status & source pop-ups)

_(№ 946 — 19-07 '26 07:06 SGT)_

## New

### [HDR] v0.62.602 — Transport TMA: the status chip and the title open pop-ups (engineering closures + data source)

- **[INTENT]** Operator on the deployed v0.62.601 Train TMA, three asks:
  1. Make the header **status** a **pop-up** (Cuisine first-load style) carrying the
     engineering-closures info ("No scheduled engineering closures in the next 7 days" / the upcoming
     list) — *linked to the "all lines normal" status*.
  2. Make the footer **source** line ("LTA TrainServiceAlerts (live) + curated engineering schedule") a
     **pop-up** opened by hyperlinking the header **title** "Train System".
  3. *"where is the carousel"* — a question, **answered** (no code change this slice).

- **[DELTA — client]** `web/transport/src/App.jsx`:
  - New **`Modal`** component — a centred, `skeuo-card`, click-outside / ✕ dismiss popup at `z-[60]`
    (over the fixed footer bar), mirroring the Cuisine first-load popup style.
  - `popup` state (`'status' | 'source' | null`) + `firstPopupRef`. The status fetch **auto-opens the
    status popup once on first load** (operator: "like first load in Cuisine TMA").
  - The header **title** is now a `<button>` → opens the **source** popup; the **status chip** is a
    `<button>` → opens the **status** popup.
  - The inline `EngineeringList` (removed from `secondaryPanels`) and the inline source `<footer>`
    (removed from every layout) now live **in the popups**: the status popup shows the affected-lines
    summary + per-line status swatches + the `EngineeringList`; the source popup shows the attribution
    + the build version.
  - i18n: `+status.popupTitle` / `+src.title` / `+src.body` (en/fr).

- **[DELTA — carousel Q]** *"where is the carousel"* → it renders in **wide + landscape** once a line is
  focused (tap a line pill); **wide + portrait** is the two-panel list; **phone** is the stacked column
  (no carousel). No code change — offered a full-map toggle (to reveal the carousel in portrait) as a
  follow-up if wanted.

- **[VERIFICATION (sandbox)]** **Playwright** (stubbed status: NEL delay + one EWL engineering closure)
  confirmed the **status popup auto-shows on first load** ("Service status → ⚠️ 1 line affected →
  NEL · Delay → Upcoming engineering: EWL Sun 20 Jul …") and the **title opens the source popup**
  ("About the data → LTA TrainServiceAlerts (live) + curated engineering schedule → v0.62.601"), with
  **no page errors**. Transport `vite build` green (`index-BiNaBNVz.js 268.18 kB │ gzip 87.45 kB`); full
  `npm test` **3738/3738 across 147 files**; `package.json` + `package-lock.json` 0.62.601 → 0.62.602;
  `.serial-state.yml` commit 1081→1082, build 703→704, journal 945→946, re-anchor **19-07 '26 07:06
  SGT**.

- **[TIME ANCHOR]** data.gov.sg blocked by the environment network policy (`403` `CONNECT`); anchored
  to the agent-proxy status timestamp `2026-07-18T23:06Z` = **19-07 '26 07:06 SGT** (TF-9 fallback;
  TF-10 lower bound).

- **[STATUS]** DRAFT → squash-merge to `main` on green CI (auto-merge SQUASH). New PR.

- **[TEST]** No new unit test (a popup/interaction change; verified visually via Playwright). Existing
  suite unchanged and green.

- **[KNOWN GAPS]** The status popup auto-opens on **every** first load (not persisted) — matches the
  operator's Cuisine reference; a "seen" flag could suppress repeats if wanted. The full-map toggle to
  reveal the carousel in portrait is a proposed follow-up. Carried: unified card↔pin flash flow; the
  card's `mrt.*` + closure strings are en/fr-lean; Cuisine `tg.js` safe-area gap.

## Confirmation Gates
| Gate | Status | Note |
|------|--------|------|
| G1 | ✅ | PATCH bump 0.62.601 → 0.62.602; build + 3738 tests green; both popups verified via Playwright. |
| G2 | ⚠️→ok | the inline EngineeringList + source footer were **relocated** into the popups per the operator's explicit request, not deleted. |
| G3 | ➖ | no new decision rule. |
| G4 | ➖ | no external paid API call. |
