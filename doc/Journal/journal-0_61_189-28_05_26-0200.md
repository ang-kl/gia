(№ 390 - 28-05 '26 02:00 SGT) v0.61.189

# Journal — soleat v0.61.189 (full revert: LocationField OTHER-suppression rolled back to pre-v0.61.187)

> Operator after v0.61.188 deploy: *"still doesn't work, turn back
> the dropdown. I try type in cuisine TMA."* v0.61.188's
> "type but no dropdown" compromise didn't deliver a working
> typing experience for OTHER. Operator now wants the picker UI
> back for OTHER too — same dropdown + autocomplete as SG/JB.
>
> Commit 402 / journal 390 / build 192, on branch
> `claude/v0.61.189-revert-locationfield-other` off `main` at
> v0.61.188.

## New

### [HDR] #390 | 02:00 SGT 28-05-28 | v0.61.189 — Revert OTHER LocationField suppression | 4 files | 1 PR | 1 build |

- **[INTENT]** Restore the original SG/JB picker UI behaviour for
  all regions, including OTHER. Drops the v0.61.187 +
  v0.61.188 experimental suppression in favour of a unified
  picker.

- **[DELTA]**
  - **`web/cuisine/src/v2/components/LocationField.jsx`** —
    restored to commit `c07eae4` (v0.61.185 baseline). Removes:
    - The v0.61.187 autocomplete-skip early-return for OTHER.
    - The v0.61.187 static "locked" read-only label render.
    - The v0.61.187 / v0.61.188 dropdown render gates
      `region !== 'OTHER'`.
    File reverted to 267 lines (unchanged from v0.61.185).
  - **`web/menu/src/components/LocationFieldMenu.jsx`** —
    restored to commit `c07eae4` (v0.61.185 baseline). Removes:
    - The v0.61.187 outer ternary wrapping the precinct
      `<select>` + free-text input + autocomplete dropdown.
    - The v0.61.187 "🔒 Anchor locked" hint.
    - The v0.61.188 restructure of the picker UI.
    File reverted to 294 lines (unchanged from v0.61.185 — which
    DID add the OTHER region to the `isMy` check + autocomplete
    region picking; both kept).
  - **`package.json`** — `0.61.188 → 0.61.189`.
  - **`doc/.serial-state.yml`** — commit 401 → 402, build 191 →
    192, journal 389 → 390; re-anchored 28-05 '26 02:00 SGT.
  - **`doc/Journal/journal-0_61_189-28_05_26-0200.md`** — this entry.

- **[VERIFICATION (sandbox)]**

  ```
  $ cd web/cuisine && npm run build   (✓ built in 2.23s)
  $ cd web/menu && npm run build      (✓ built in 1.81s)
  ```

- **[STATUS]** PR-OPEN on push. Operator merges manually.

- **[TEST]** Operator (post-merge + Railway redeploy + browser
  cache bust):
  - With Putrajaya anchor active:
    - Cuisine TMA: tap location field → input opens → type
      "Cyberjaya" → autocomplete dropdown shows suggestions →
      pick or Enter → anchor moves.
    - Menu TMA: precinct `<select>` dropdown visible (still
      showing SG + JB precincts — the Putrajaya entry shows
      as 🇲🇾 IOI Resort City). Free-text input visible. Type
      → autocomplete shows → pick or submit.
  - With SG / JB anchor: identical behaviour (unchanged from
    pre-v0.61.187).

- **[KNOWN GAPS]**
  - **Operator's earlier "too many to list" concern remains
    unaddressed**. The autocomplete dropdown for OTHER will
    show however-many Places candidates the user's typed text
    matches. If this is genuinely noisy, a future iteration
    could:
    - Cap suggestions at 5 (currently 5 already in Menu TMA;
      Cuisine TMA unbounded).
    - Add a "did you mean to use /location for a Singapore
      precinct?" hint when the typed query looks SG-like
      under an OTHER anchor.
    But not until the operator actually hits a problematic
    case again — the v0.61.187 + .188 attempts to pre-empt it
    didn't land well.
  - **v0.61.185's search-side fix (region-aware regionCode +
    20km cap) is unaffected by this revert** — Putrajaya
    searches still use locationBias.circle correctly and return
    venues (the 0-result regression that started this thread
    is solved at the search layer, not the LocationField UI).

- **Confirmation Gates**

  | Gate | Resolution | Date |
  |------|-----------|------|
  | G1 — version bump | PATCH `0.61.188 → 0.61.189`. Pure revert of two UI experiments. | 28-05 '26 |
  | G2 — destructive action | _n/a_ — both files restored to a known-good baseline (v0.61.185 squash-merge `c07eae4`). | 28-05 '26 |
  | G3 — new decision rule | YES (negative) — the operator's earlier "should not have dropdown for OTHER" guidance is RETRACTED. All regions get the same picker UI. | 28-05 '26 |
  | G4 — paid external API call | _n/a_ — restores normal autocomplete behaviour for OTHER (~1 Places call per keystroke, debounced 250 ms). | 28-05 '26 |

## Amendments

- **Cuisine TMA `LocationField` policy** — was "OTHER read-only"
  (v0.61.187) → "OTHER editable, dropdown hidden" (v0.61.188).
  Now: same picker behaviour as SG / JB. Full revert.
- **Menu TMA `LocationFieldMenu` policy** — was "OTHER picker
  entirely hidden" (v0.61.187) → "OTHER precinct hidden, text
  input visible, autocomplete hidden" (v0.61.188). Now: same
  picker behaviour as SG / JB. Full revert.

## Deleted

- **All v0.61.187 + v0.61.188 LocationField conditional rendering
  branches** — restored to a pre-experimental baseline.
