(№ 864 - 06-07 '26 22:23 SGT) v0.62.508

# Journal — soleat v0.62.508 (delete vault/ from tracking — audit change #1)

> First of the operator-approved audit changes ("drop 1. do 2 …" → do #2 = delete vault/ +
> gitignore generated artifacts). Safe now that the final snapshot (vault/v0.62.504) is archived
> in git history via #1511 — the directory's whole purpose (full-tree snapshots) is already
> served by commit shas.

## New

### [HDR] #864 | 22:23 SGT 06-07-26 | v0.62.508 — remove vault/ (27 snapshots) from tracking | delete dir + .gitignore + 3 meta | PR #(pending) |

- **[INTENT].** The 06-07 4-auditor code audit found `vault/` (27 snapshots, ~25k files) is
  100% redundant with git history — every snapshot is a full source tree already reachable by
  commit sha. Operator approved deletion (audit rec #2), gated on taking the final snapshot
  first (done: #1511, vault/v0.62.504). This PR removes the directory from tracking.

- **[RUNTIME-SAFETY — re-verified this PR].** `git grep` over `*.js`/`*.mjs`/`*.yml` on main:
  **no runtime code reads the `vault/` directory as files.** `vault-index.js` / `vault.js` /
  cron `sync-vault` operate on **Redis** keys (`gia:vault`, `place-reviews:*`, `vibe-summary:*`).
  The only `vault/…` string hits are self-referential text inside the snapshots themselves and
  in serial-state / vibe-journal metadata — no `require`/`readFile`/`readdir`/`__dirname` join.

- **[DELTA].**
  - `vault/` — **DELETED** (all 27 snapshots). Recoverable forever via
    `git show <sha>:vault/<version>/<path>`; the v0.62.504 archive commit is `#1511`.
  - `.gitignore` — added a `vault/` block so future ad-hoc snapshots are never re-committed.
  - `doc/Journal/journal-0_62_508-06_07_26-2223.md` — this entry.
  - `doc/.serial-state.yml` — commit 981→**982**, journal 863→**864**; build unchanged 615.
  - `package.json` — 0.62.507 → **0.62.508**.

- **[SCOPE NOTE — what this PR does NOT do].** The audit also flagged gitignoring the large
  generated artifacts (`public/doc/vibe-journal.html` ~1 MB). **Deferred, on purpose**: Express
  serves that file from `public/` at runtime, so gitignoring it without a build-time generation
  hook would 404 the hosted Vibe Journal. Logged as still-due; needs the generate.mjs step wired
  into the Railway build first. `.serial-state.yml` (~950 KB) stays tracked — it's the SSOT
  counter, not a generated artifact.

- **[VERIFICATION (sandbox)]** New tree fetched recursively pre-commit and confirmed to contain
  **zero** `vault/` paths; `.gitignore` `vault/` line present. No `.js` touched (`node --check`
  N/A). CI's Empty-blob guardrail + TMA build still gate the PR.

- **[STATUS]** PROD — merges to main; repo-content only, no deploy-surface change.

- **[TEST]** N/A (no code). 4 required checks gate.

- **[KNOWN GAPS]** Still-due: gitignore `vibe-journal.html` behind a build hook (above);
  workspace step 1–2 (delete dead v1 cuisine + `packages/tma-core`) — next PR; Redis job lock
  on the 11 schedulers. O-49/O-50/O-51, DF-1 (12-Jul) unchanged.

## Confirmation Gates
| Gate | Status | Note |
|------|--------|------|
| G1 | ➖ | no code; version 0.62.507→0.62.508 |
| G2 | ✅ | vault snapshot archived (#1511) before deletion; git-recoverable |
| G3 | ✅ | serial commit 981→982, journal 863→864; anchor 06-07 '26 22:23 SGT |
| G4 | ➖ | no user-facing surface (runtime reads Redis, not the dir) |
