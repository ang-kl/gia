(№ 783 - 29-06 '26 08:15 SGT) v0.62.429

# Journal — soleat v0.62.429 (Sketchbook B — Clipboard cards = Cuisine ResultCard look)

> Operator: "the copy card should be the same as the search result's card." Root cause (#779): the
> clip stored only the copied HTML body, no structured venue. B stores the venue + renders the card.

## New

### [HDR] #783 | 08:15 SGT 29-06-26 | v0.62.429 — store-venue-on-Copy + ResultCard-parity card | 5 files | PR #(pending) |
- **[DELTA]** `clip-store.js` normalise/denormalise +`venue` (JSON, 6000-cap; absent on copy-all +
  pre-v0.62.429 → text-only). `index.js` copy-one `pushClip` passes the venue. `VenueCard.jsx` —
  `card.venue` → ResultCard-parity (rank·name·type · ★·open·📍dist · price(≈conv)·🌐·crowd dot+word
  · 🍲Try · ✳️Michelin·year; expand 📍area·💬review·vibe); else HTML-stripped fallback.
  `CatchAllStrip`/`DrawerRow` thread number+lang. i18n card.try/open/closed/distAway/crowd*.
- **[VERIFICATION]** clip-store 42 + clipboard-store 74 = 116 green; vite build OK (50 modules);
  node --check index.js + clip-store.js.
- **[STATUS]** PROD. **[GAP]** only NEW copies carry venue → full card; old clips text fallback.

## Confirmation Gates
| Gate | Status | Note |
|------|--------|------|
| G1 | ✅ | vite + node --check |
| G2 | ✅ | 116 tests |
| G3 | ✅ | serial 782→783 |
| G4 | ⏳ | redeploy; eyeball fresh Copy |
