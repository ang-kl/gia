# Journal — v0.62.605 (Hawker: single Maps pin, map bottom margin, Cuisine-style footer dock)

_(№ 949 — 19-07 '26 10:34 SGT)_

## New

### [HDR] v0.62.605 — Hawker: de-dupe the card's Maps pin, give the fixed map a bottom margin, and match the Cuisine footer

- **[INTENT]** Operator (IMG_3588/3589/3590), three Hawker fixes:
  1. *"the card has 2 (📍📍) pin instead of one (📍)."*
  2. *"the fix map should have a bottom tiny white margin so the scroll of the listing below the fix
     map should not look like an afterthought."*
  3. *"the footer should be like cuisine small height, and the end and down follow cuisine."*

- **[DELTA — card pin]** `web/hawker/src/App.jsx` `renderCentreCard`: the Maps link rendered
  `📍 {t('btn.maps')}` while `btn.maps` **already** carries a 📍 ("📍 Maps") → two pins. Dropped the
  JSX prefix so it's a single **📍 Maps**.

- **[DELTA — map margin]** the two-panel fixed (top) map wrapper `px-2 shrink-0` → **`px-2 pb-1.5
  shrink-0`** — a tiny bottom gap so the scrolling list beneath doesn't butt against the map.

- **[DELTA — footer]** the footer now **matches the Cuisine TMA**. New **`FooterDock`** component: a
  compact, full-width **`liquid-glass-dock`** at `fixed bottom-0 inset-x-0` — a `text-[11px]
  text-tg-link` control row with the map/list toggle (left) + **⇡ top / ⇣ down** and **↩ back / 🔚 end**
  (right; Cuisine's `↩` glyph, was `⇠`), and a tiny **integrated version line** below. It replaces the
  floating `FooterNav` pill **and** the separate centred version `<footer>` in **both** the carousel and
  two-panel layouts; the two-panel list gets `paddingBottom` to clear the fixed dock. The now-unused
  `FooterNav` import was removed (the shared component and Menu/Train are untouched).

- **[VERIFICATION (sandbox)]** **Playwright** (stubbed, `mapsUrl` set) confirmed: a single **📍 Maps**,
  bus stops two-per-row, the map's bottom gap, and the **Cuisine-style dock** (`🗺 Map … ↑ top ↩ back`
  + `Experimental · Singapore · v… · (tablet · landscape)`) in both layouts — no page errors. Hawker
  `vite build` green (`index-DnFq4Dyn.js 256.43 kB │ gzip 83.94 kB`); full `npm test` **3738/3738 across
  147 files**; `package.json` + `package-lock.json` 0.62.604 → 0.62.605; `.serial-state.yml` commit
  1084→1085, build 706→707, journal 948→949, re-anchor **19-07 '26 10:34 SGT**.

- **[TIME ANCHOR]** data.gov.sg blocked by the environment network policy (`403` `CONNECT`); anchored
  to the agent-proxy status timestamp `2026-07-19T02:34Z` = **19-07 '26 10:34 SGT** (TF-9 fallback;
  TF-10 lower bound).

- **[STATUS]** DRAFT → squash-merge to `main` on green CI (auto-merge SQUASH). New PR (branch restarted
  from `origin/main` `0dcd5bf`, #1612 merged — the standing restart-after-squash rule).

- **[TEST]** No new unit test (pin glyph + margin + footer layout; verified visually via Playwright).
  Existing suite unchanged and green.

- **[KNOWN GAPS]** The Menu TMA still uses the shared floating `FooterNav` (only Hawker moved to the
  Cuisine dock, per the request). Carried: Transport full-map toggle + unified flash; the card's
  `mrt.*` / closure strings are en/fr-lean; Cuisine `tg.js` safe-area gap.

## Confirmation Gates
| Gate | Status | Note |
|------|--------|------|
| G1 | ✅ | PATCH bump 0.62.604 → 0.62.605; Hawker build + 3738 tests green; all three fixes verified via Playwright. |
| G2 | ➖ | no destructive removal — the FooterNav pill + separate version footer were **replaced** by the dock (the version line lives on inside it); the unused import cleanup is inert. |
| G3 | ➖ | no new decision rule (three direct operator tweaks). |
| G4 | ➖ | no external paid API call. |
