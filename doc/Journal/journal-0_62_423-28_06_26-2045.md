(№ 777 - 28-06 '26 20:45 SGT) v0.62.423

# Journal — soleat v0.62.423 (Sketchbook chrome → Cuisine parity: logo, hamburger icons, footer version)

> Operator: "Sketchbook logo = the Soleat logo in all TMAs; header/footer follow Cuisine TMA;
> Hawker + Train icons in the hamburger follow the Menu TMA." This PR does the contained chrome
> assets; the larger header-component reuse + ResultCard card pipeline follow.

## New

### [HDR] #777 | 20:45 SGT 28-06-26 | v0.62.423 — chrome assets parity | 6 files | PR #(pending) |
- **[DELTA]**
  - **Logo** — header + hamburger brand now render the real **`soleat-icon.png`** (was the ☼
    glyph), matching every other TMA.
  - **Hamburger Switch-App icons** — now the **Menu TMA assets**: `cuisine-icon-v3.png`,
    **`hawker-icon-v3.png`**, **`train-logo.png`** (operator: match Menu TMA). Reused from the
    Menu TMA's existing git blobs into `web/clipboard/public/` (no binary re-upload).
  - **Footer** — added Cuisine's **"Experimental · Singapore · v{version}"** strip below the 4
    tabs, using the `__BUILD_VERSION__` define already in clipboard's vite config.
  - `components/Shell.jsx`, `lib/i18n.js` (`chrome.experimental`).
- **[VERIFICATION (sandbox)]** Real `vite build` OK (50 modules; public assets copied).
- **[STATUS]** PROD — clipboard TMA rebuild; redeploys per PR.
- **[KNOWN GAPS — the remaining Cuisine-parity work, operator-confirmed]**
  1. **Header component reuse** — port/reuse Cuisine's `WeatherBadge` (real temp), `LocaleToggle`
     (flag), and `LocationField` (which is also the 🧭 Recent N/20 LRU), so the header matches
     Cuisine exactly.
  2. **Cards** — operator chose **"store structured venue on Copy"**: extend the Cuisine Copy flow
     + clip-store schema to persist the venue object, then render Sketchbook cards with Cuisine's
     `ResultCard`. Old text-only clips keep a basic card. (Larger cross-cutting change — next.)

## Confirmation Gates

| Gate | Status | Note |
|------|--------|------|
| G1 build/parse | ✅ | real vite build (50 modules) |
| G2 tests | ⏳ | CI |
| G3 journal/serial | ✅ | serial 776→777 |
| G4 live deploy | ⏳ | redeploy + eyeball |
