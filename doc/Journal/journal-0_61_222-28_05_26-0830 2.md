(№ 423 - 28-05 '26 08:30 SGT) v0.61.222

# Journal — soleat v0.61.222 (OTHER picker: KR "LG"-null + KR Japanese 4-result)

> Operator (28-05 '26, KR session): two bugs.
> 1. *"I selected KR, selected japanese cuisine, first time it has 4 return, which is puzzling"*
> 2. *"When I change to Others, select KR, type 'LG' returns one result which is 'null'."*
>
> Diagnosed: (A) per-country keyword filter (`country-text-match.js`)
> couldn't match Hangul/Kanji venue names against its all-Latin
> keyword set, dropping ~80% of valid venues. (B) `place-search-by-country`
> Places-path map at `index.js:11923` lacked the `|| 'Unnamed'` fallback
> the Geocoding path has at line 11966, so a Places result with empty
> displayName shipped `primaryText: null` → JSX rendered "null".
>
> Commit 435 / journal 423 / build 225, on branch
> `claude/v0.61.222-other-picker-fixes` off `main` at v0.61.221.

## New

### [HDR] #423 | 08:30 SGT 28-05-28 | v0.61.222 — OTHER picker non-Latin fail-open + Places-path null fix | 4 files | 1 PR | 1 build |

- **[INTENT]** Make the OTHER cuisine flow return realistic counts for non-Latin-script countries (KR/JP/CN/TH/HK/TW/etc.) and stop the picker from rendering a literal "null" label.

- **[DELTA]**

  **1. `country-text-match.js`** — added `_isNonLatinScript(text)` helper + fail-open branch in `filterVenuesByCountry`.

  ```js
  // v0.61.222
  const _NON_LATIN_RE = /[Ѐ-ӿ฀-໿က-႟ក-៿぀-ヿㇰ-ㇿ㄰-㆏ᄀ-ᇿ一-鿿가-힣]/;
  function _isNonLatinScript(text) {
    return _NON_LATIN_RE.test(String(text || ''));
  }
  // …
  if (_isNonLatinScript(hay)) return true;
  return _venueMatches(hay, words);
  ```

  Unicode blocks covered: Hangul Syllables + Jamo + Compatibility Jamo, CJK Unified, Hiragana, Katakana + Phonetic Extensions + Half-width, CJK Strokes, Thai, Lao, Khmer, Burmese, Cyrillic. Failing open trusts the upstream `locationBias.circle` to keep the geographic constraint — verified by a regression test that confirms cross-border SG-in-MY Latin-script venues still drop.

  **2. `index.js:11923`** — chained fallbacks:

  ```js
  // v0.61.221:
  primaryText: smartPlaceLabel(p?.displayName?.text || '', p?.formattedAddress || ''),
  // v0.61.222:
  primaryText: smartPlaceLabel(p?.displayName?.text || '', p?.formattedAddress || '')
    || p?.formattedAddress || text || 'Unnamed',
  ```

  Now mirrors the Geocoding-path fallback at line 11966.

  **3. `__tests__/country-text-match.test.js`** — 6 new cases (KR Hangul / JP Kanji+Kana / TH Thai / CN Han / HK traditional / regression guard for SG-in-MY drop).

  **4. `package.json`** — `0.61.221 → 0.61.222`.

  **5. `doc/.serial-state.yml`** — commit 434→435, build 224→225, journal 422→423; re-anchored 28-05 '26 08:30 SGT.

- **[VERIFICATION (sandbox)]**

  ```
  $ node --check country-text-match.js      (OK)
  $ node --check index.js                   (OK)
  $ node -e ... smoke →
      KR Hangul → 1 ✓
      JP Kanji  → 1 ✓
      MY drop SG → 1 ✓
      MY Pahang → 1 ✓
  ```

  Vitest hit the recurring iCloud / Node-24 ESM resolver quirk on this local; CI runs cleanly on Linux. The added `country-text-match.test.js` cases will validate via CI.

- **[STATUS]** PR-OPEN on push.

- **[TEST]** Operator (post-merge + Railway redeploy):
  1. Reproduce Bug A: anchor in Seoul, pick Japanese cuisine → expect ~20-30 results (was 4).
  2. Reproduce Bug B: `/location` then OTHER-search with country=KR + text "LG" → confirmation card should NOT show "null" — at minimum will show the formatted address, ideally the building name.
  3. Sanity-check JP: anchor in Tokyo, pick Korean cuisine → expect a healthy count, no drop-off from the filter.

- **[KNOWN GAPS]**
  - The Latin-script-only keyword set still has the asymmetry: for MY/TH/PH/AU/NZ/etc. (Latin-script countries) the filter still does its cross-border-defense job; for non-Latin countries it now degrades gracefully to "trust locationBias". A future PR could extend the keyword set with native-script tokens (`서울`, `東京`, `กรุงเทพ`, `北京`) for tighter cross-border defense in Asia.
  - The "only 4 results" symptom may also reflect upstream Places returning few candidates for the textQuery "Japanese restaurant" in KR — we'd need fresh variance data to confirm whether the filter was the dominant cause vs. just amplification.

- **Confirmation Gates**

  | Gate | Resolution | Date |
  |---|---|---|
  | G1 — version bump | PATCH `0.61.221 → 0.61.222`. Two narrow bug fixes; no schema. | 28-05 '26 |
  | G2 — destructive action | _n/a_. | 28-05 '26 |
  | G3 — new decision rule | YES — for non-Latin-script countries the per-country keyword filter degrades to fail-open; locationBias is the sole geographic guarantee. Any future addition of native-script tokens must NOT remove this fail-open. | 28-05 '26 |
  | G4 — paid external API call | _n/a in this PR_. | 28-05 '26 |

## Amendments

- **`filterVenuesByCountry`** — was Latin-only substring match. Now: non-Latin-script `hay` short-circuits to keep.
- **`index.js:11923`** Places-path `primaryText` — was `smartPlaceLabel(...)` only. Now: `smartPlaceLabel(...) || formattedAddress || query || 'Unnamed'`.

## Deleted

_None._
