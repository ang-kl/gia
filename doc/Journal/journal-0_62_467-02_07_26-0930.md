(№ 821 - 02-07 '26 09:30 SGT) v0.62.467

# Journal — soleat v0.62.467 (Cuisine — closed-now-but-reopens-today strip)

> Operator: closed today but reopens within 99 min → "Closed Now" (red) · "Open in ## minutes"
> (blue); reopens later today → "Closed Now" (red) · "Opens hh:mm ~ hh:mm" (blue). Black middle dot.

## New

### [HDR] #822 | 09:30 SGT 02-07-26 | v0.62.467 — reopen-today strip | 5 files | PR #(pending) |
- **`open-hours.js`** — new `reopenTodayInfo(periods, now, offset, lang)` → `{ minutesUntilOpen,
  openStart, openEnd }` for a currently-closed venue's next SAME-DAY open period (null if it
  doesn't reopen again today → caller falls back to plain "Closed").
- **`cuisine-enrich.js`** + **`index.js`** (NL mirror) — on `openNow === false` venues, set
  `v.reopenMinutes / v.reopenStart / v.reopenEnd`.
- **`ResultCard.jsx`** — closed strip now renders, when the venue reopens today: **"Closed Now"**
  (red) · (black middle dot) · **"Open in {n} min"** (blue, ≤99 min) or **"Opens {start} ~ {end}"**
  (blue, later today). Falls back to plain red **"Closed"** when it doesn't reopen today. Standard
  12px font retained.
- **`web/cuisine/src/v2/lib/i18n.js`** — `card.closedNow / openInMin / opensRange / opensAt`
  (base en/fr + id/zh/ja/es; ru/de EN-fallback).
- **[VERIFICATION (sandbox)]** `node --check` open-hours/cuisine-enrich/index/i18n; `esbuild`
  ResultCard; CI cuisine build + vitest.
- **[STATUS]** PROD — server + cuisine TMA rebuild on merge.

## Confirmation Gates
| Gate | Status | Note |
|------|--------|------|
| G1 | ✅ | node --check + esbuild; CI builds cuisine |
| G2 | ⏳ | full vitest via CI |
| G3 | ✅ | serial 820→821 |
| G4 | ⏳ | redeploy + operator eyeball |
