(№ 850 - 04-07 '26 14:20 SGT) v0.62.496

# Journal — soleat v0.62.496 (Cuisine card: status folder-tab moved OUTSIDE the card so the border stops slicing it) — IMG_2685/IMG_2686 + reference mock

> Operator (IMG_2685 pink "Closing" + IMG_2686 red "Closed", then a reference mock): the folder-tab overlapped/was cut by the card border. Chose **"move tab fully above card"**.
> Read as: render the tab above the card, tucking into the top edge like the reference — the card's rounded border must never cross it.

## New

### [HDR] #850 | 14:20 SGT 04-07-26 | v0.62.496 — status tab rendered outside the bordered card <button> | 1 web file + 3 doc/meta | PR #(pending) |

- **[INTENT].** The Closed / Closing-soon **folder-tab** looked correct on **alternate-match** cards
  (a top "& nearby flavours" band gives it something to tuck into — see the reference North Miznon
  mock). But on **exact-match** cards (Odette, Kuan Zhai) there is **no top band**, so the v0.62.488
  in-card protruding tab (negative top-margin *inside* the bordered `<button>`) collided with the
  card's rounded blue border — the border **sliced through** the tab.

- **[DELTA] (1 code file).** `web/cuisine/src/v2/components/ResultCard.jsx`
  - The return root is now a `w-full flex flex-col` **wrapper `<div>`**; the status tab is its **first
    child**, rendered **OUTSIDE** the card `<button>`, so the card border can never cross it.
  - Tab classes: dropped the `-mt-1.5/-mt-2.5` protrusion; added **`-mb-1`** to tuck the tab's flat
    bottom a few px under the card top (folder-tab overlap); kept `ml-3 self-start relative z-10
    rounded-t-lg` + `bg-red-600` / `bg-pink-600`, `text-white text-[10px] font-bold`.
  - Removed the v0.62.475 **`hasStatusTab`** const + its two usages: with the tab no longer inside the
    card, the in-card cuisine band + first dish-hint again pull **flush to the card top**
    unconditionally (`-mt-1.5/-mt-2.5`).

- **[VERIFICATION (sandbox)]** esbuild esm JSX parse **clean**; `grep hasStatusTab` shows no code refs
  left (only a superseding comment). Matches the reference mock — tab protrudes above and tucks into
  the top edge, **with or without** a top band.

- **[STATUS]** PROD — Cuisine TMA redeploys on merge (Railway). Verify on Telegram: Closed/Closing cards
  (exact-match, no band) show the tab cleanly above the card, no border cutting through.

- **[TEST]** Full vitest + `web/cuisine` build via CI (pure layout/markup change; no test file touched).

- **[KNOWN GAPS]** None for this fix. Standing: G4 CJK/ru/de live-render eyeball; VibeCodingRecord regen.

## Confirmation Gates
| Gate | Status | Note |
|------|--------|------|
| G1 | ✅ | esbuild esm JSX parse clean; no lingering `hasStatusTab` refs; matches reference mock |
| G2 | ⏳ | full vitest + web/cuisine build via CI |
| G3 | ✅ | serial 849→850, commit 967→968, build 606→607; anchor re-set 04-07 '26 14:20 SGT |
| G4 | ⏳ | redeploy + operator eyeball (tab above card, border no longer slices it) |
