(№ 817 - 01-07 '26 16:20 SGT) v0.62.463

# Journal — soleat v0.62.463 (Cuisine — dish-type badge + full-explanation pop-up)

> Operator: "categorise the type of dishes/desserts/drinks … whether vegetarian/meat/soup/
> appetiser/main course/starter/bites, etc." + "click can pop-up to see the whole explanation,
> you can lengthen."

## Investigation before implementation

Checked for existing taxonomy data before building anything:
- **Meal-time** (breakfast/lunch/dinner/snack), **dietary** (vegetarian/meat/seafood), and
  **course** (starter/main/soup/bites) — **do not exist anywhere** in the codebase as curated
  data. Not fabricated (no-invented-data rule); scoped as a **separate follow-up**: a grounded
  Gemini curation pass across ~1,900 dishes, same process as the earlier dish-explanation
  backfill (batched, web-grounded, verify pass per batch).
- **Dish TYPE** (noodles/rice/soup/grilled/stew-curry/seafood/veg/snack/**sweet**=dessert/
  **drink**/other) **already exists** — `dish-food-group.js`'s `foodGroupFor(name, kind)` +
  `GROUP_LABEL` (en/fr). Zero new data needed for this part.

## New

### [HDR] #818 | 16:20 SGT 01-07-26 | v0.62.463 — group badge + full-explanation pop-up | 3 files | PR #(pending) |
- **`index.js`** — `/api/cuisine/dishes` now tags each dish with `{ group, groupLabel }` via the
  existing `foodGroupFor` + `GROUP_LABEL`.
- **`web/cuisine/src/v2/components/CuisineCategoryDrawer.jsx`**
  1. Each dish in the list now shows a small coloured **dish-type badge** (12 groups, decorative
     colour + label text) above the name/local/description preview.
  2. **Tapping a dish no longer fires search immediately.** It opens a **nested full-explanation
     sub-view** (un-truncated `note[lang] ?? note.en`, badge, native script) with a **"🔍 Find
     eateries"** button that runs the actual search — mirroring the existing Pick-local-classic
     "tap to learn more, then search" pattern.
- **[VERIFICATION (sandbox)]** `esbuild` parse + `node --check` index.js; cuisine build + vitest
  via CI.
- **[STATUS]** PROD — server + cuisine TMA rebuild on merge.
- **[KNOWN GAPS]** Meal-time / dietary / course taxonomy — scoped as a separate grounded-Gemini
  curation pass, not yet started.

## Confirmation Gates
| Gate | Status | Note |
|------|--------|------|
| G1 | ✅ | esbuild + node --check; CI builds cuisine |
| G2 | ⏳ | full vitest via CI |
| G3 | ✅ | serial 816→817 |
| G4 | ⏳ | redeploy + operator eyeball |
