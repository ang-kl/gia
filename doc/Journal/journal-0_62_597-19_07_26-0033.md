# Journal — v0.62.597 (Transport TMA: overview + operating-line pills move into the header)

_(№ 941 — 19-07 '26 00:33 SGT)_

## New

### [HDR] v0.62.597 — Transport TMA: the overview + operating-line ticker moves UP into the header (Hawker zone-pill parity) — Slice 1 of the responsive port

- **[INTENT]** Operator kicked off the Transport-TMA responsive port with a first, self-contained
  slice: *"Move the row of overview + train operating line to the header like Hawker TMA five zones
  pill."* The Hawker TMA carries its zone pills inside the frosted header card; the Transport TMA had
  its equivalent control — the `AffectedTicker` ("All Lines" overview reset + one operating-status
  chip per line) — pinned down in the **bottom** fixed bar. This slice lifts it into the header to
  match, freeing the bottom bar and setting up the later responsive/carousel/rich-station-card slices.

- **[DELTA — client]** `web/transport/src/App.jsx`:
  - The `<AffectedTicker compact … />` moves **out of** the bottom fixed bar and **into** the
    `<header>` skeuo-card, placed immediately after the Schematic / Google-Map view-toggle pills
    (comment tag `v0.62.597`). It keeps its full props: `affectedCodes` (the live affected set, else
    every non-future line code), `focusedCode`, `statusByLine`, and the `onFocus` handler.
  - The **first line-chip tap auto-switches** Schematic → Google Map (one-shot, the `v0.60.99`
    behaviour) is preserved verbatim; the "All Lines" reset (`null`) is still ignored by the switch.
  - The **bottom fixed bar** is simplified to just the version tag (left) + the top/down & back/end
    nav buttons (right): the old `<div className="px-2 pt-1.5 pb-0.5">` ticker wrapper is removed and
    the controls row goes `px-3 pb-1` → `px-3 py-1.5` so it centres on its own.

- **[VERIFICATION (sandbox)]** Transport `vite build` green
  (`index-ClyMHcw6.js 250.99 kB │ gzip 82.19 kB`); full `npm test` **3719/3719 across 145 files**;
  `package.json` + `package-lock.json` 0.62.596 → 0.62.597; `.serial-state.yml` commit 1075→1076,
  build 698→699, journal 940→941, re-anchor **19-07 '26 00:33 SGT**. ⚠️ Live-only (renders on the
  deployed Transport Mini App — the pills now sit in the header, the bottom bar is version + nav only).

- **[TIME ANCHOR]** The data.gov.sg sovereign source is **blocked by the environment network policy**
  (agent proxy answered `403` to `CONNECT api.data.gov.sg:443` / `api-open.data.gov.sg:443`). Anchored
  instead to the **agent-proxy status response timestamp** `2026-07-18T16:33Z` = **19-07 '26 00:33
  SGT** (TF-9 assistant-runtime fallback; TF-10 — a network-sourced lower bound on *now*).

- **[STATUS]** DRAFT → squash-merge to `main` on green CI (auto-merge SQUASH).

- **[TEST]** Existing suite 3719/3719 (no Transport-specific unit test touches this JSX-only move).

- **[KNOWN GAPS]** This is **Slice 1** only. Still to come in the port (operator-confirmed spec):
  responsive scaffold (`useViewport` — phone stacked, tablet/desktop two-panel with fixed map,
  landscape/full-map **carousel**); rich Google-Maps-style **StationCard** (stacked per-line-code
  sub-cards with first/last train + operating status + terminus code hyperlink; exits, bus stops,
  taxi stands, nearest hawker centre as hyperlinks without underline; **crowd status**; name-strip =
  line colour for a single line / white for an interchange); Hawker unified tap card↔pin flow;
  Hawker-style card listing. Carried gaps from prior arcs: Cuisine `tg.js` safe-area gap; folio-tab
  folder connection; the 9 unmatched Hawker multi-block centres; closure tab EN/FR only.

## Confirmation Gates
| Gate | Status | Note |
|------|--------|------|
| G1 | ✅ | PATCH bump 0.62.596 → 0.62.597; Transport build + 3719 tests green. |
| G2 | ➖ | no destructive removal — the ticker is relocated, not deleted (same component, same props). |
| G3 | ➖ | no new decision rule (executes the operator's explicit "move to the header" instruction). |
| G4 | ➖ | no external paid API call. |
