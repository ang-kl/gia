# Journal — v0.62.588 (Cuisine: address on the collapsed card + drop obscure "barramundi pie")

_(№ 932 — 18-07 '26 06:04 SGT)_

## New

### [HDR] v0.62.588 — Cuisine: street address shows on the collapsed card; "barramundi pie" removed

- **[INTENT]** Operator, deciding the two items flagged after PR #1595 plus one placement:
  1. *"keep as 'flat white'"* — leave the flat-white / flat-white-australian pair as-is.
  2. *"obscure 'barramundi pie'"* — remove it.
  3. *"Address on the collapsed card"* — surface the address without needing to expand.

- **[DELTA — item 2 (content, removed)]** `nation-overlay.js`. Removed `F('barramundi pie')` from the
  **australian** `iconicDishes` (obscure + redundant next to plain `F('barramundi')`, which stays).
  `NATION_OVERLAY` is **read-only by standing rule** (`city-plates.js:5330`); the rule is **waived here
  by the operator's explicit call**, noted inline. The curated `classics-notes.js` "barramundi pie" note
  is left in place but is now an **orphan** — harmless: `_overlayDishMeta` only reads a note when its
  dish is in the overlay list, and the note is keyed by the full name so it never attaches to plain
  "barramundi". Item 1 (flat white) → **no edit** (operator: keep).

- **[DELTA — item 3 (address, moved)]** `web/cuisine/src/v2/components/ResultCard.jsx`. The address row
  moved **out of** the `expanded` "details" block to be **always visible**, sitting just above the
  collapse boundary with the 🍲 Try / Michelin rows. Prefixed a **📍** pin; `truncate` on the compact
  horizontal carousel strip, `break-words` in the vertical list. The country name stays **dropped**
  (`dropCountry`, v0.62.587). This reverses the v0.62.124 placement (address-in-collapsible) at the
  operator's request while keeping the v0.62.587 drop-country behaviour.

- **[VERIFICATION (sandbox)]** `node --check` nation-overlay OK; full `npm test` **3719/3719 across 145
  files**; Cuisine `vite build` green; `package.json` + `package-lock.json` 0.62.587 → 0.62.588;
  `.serial-state.yml` commit 1065→1066, build 688→689, journal 931→932, re-anchor 18-07 '26 06:04 SGT.
  ⚠️ Live-only. Branch restarted from `origin/main` after PR #1595 merged (merged-branch rule).

- **[STATUS]** DRAFT → squash-merge to `main` on green CI (auto-merge SQUASH).

- **[TEST]** Existing suite 3719/3719. The address relocation is a render-position change; the overlay
  removal drops one names-only classic (no test asserts the AU count).

- **[KNOWN GAPS]** Folio-tab folder connection (v0.62.585 items 2–5); dark-mode carousel glass
  (v0.62.209 tradeoff); Transport TMA responsive port. Orphan `classics-notes.js` "barramundi pie" note
  left in place (harmless) — prune opportunistically on a future classics-notes pass.

## Removed (overlay content)

| # | Item | Original Version | Removed in Version | Removal Date (DD-MM 'YY) | Reason | Replacement | Rule Reference | Serial № |
|---|------|------------------|--------------------|--------------------------|--------|-------------|----------------|----------|
| 1 | `barramundi pie` (australian `iconicDishes`) | v0.62.37 era | v0.62.588 | 18-07 '26 | operator: obscure + redundant next to `barramundi` | none (plain `barramundi` remains) | operator call (NATION_OVERLAY read-only waived) | (№ 1,066 - 18-07 '26 06:04 SGT) |

## Confirmation Gates
| Gate | Status | Note |
|------|--------|------|
| G1 | ✅ | PATCH bump 0.62.587 → 0.62.588; Cuisine build + 3719 tests green. |
| G2 | ✅ | destructive: removed one overlay dish (`barramundi pie`) — **operator-authorised**; recorded in the Removed table above. |
| G3 | ➖ | no new decision rule. |
| G4 | ➖ | no external paid API call. |
