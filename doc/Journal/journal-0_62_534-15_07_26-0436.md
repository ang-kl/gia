(№ 890 - 15-07 '26 04:36 SGT) v0.62.534

# Journal — soleat v0.62.534 (Regenerate station-info: 9 TEL stations' stale coordinates realigned)

> Operator: *"Proceed - one-shot 'regenerate station-info' PR fixes it"* — the TEL drift flagged in
> #1544's `[KNOWN GAPS]`. Investigating the regen revealed the drift was **stale coordinates**, not
> timings: `stations.json` carried older lat/lng for 9 TEL stations than the canonical `mrt-coords.json`.

## New

### [HDR] #890 | 04:36 SGT 15-07-26 | v0.62.534 — regenerate station-info (TEL coords) | 3 files | PR #(pending) |

- **[INTENT].** Reconcile the drift between committed `data/stations.json` and the canonical
  `data/mrt-coords.json` for the 9 TEL stations flagged in #1544.

- **[FINDING].** The builder reads `mrt-coords.json` for lat/lng. `stations.json` had been left
  un-regenerated after an `mrt-coords` accuracy pass, so 9 TEL stations carried **stale coordinates**
  (up to ~700 m off). `first_last_train` for these 9 was **byte-identical** old vs new — the drift was
  purely coordinates.

- **[CORRECTIONS]** (OLD → NEW, all = canonical `mrt-coords`):
  | Station | OLD | NEW | Δ |
  |---|---|---|---|
  | Great World | 1.2939, 103.8316 | 1.295, 103.8333 | ~220 m |
  | Havelock | 1.2884, 103.8347 | 1.2878, 103.8336 | ~140 m |
  | Tanjong Rhu | 1.2911, 103.8746 | 1.2972, 103.8733 | **~690 m** |
  | Katong Park | 1.2987, 103.8843 | 1.2978, 103.8853 | ~150 m |
  | Tanjong Katong | 1.3036, 103.8951 | 1.2992, 103.8975 | **~550 m** |
  | Marine Parade | 1.3023, 103.9056 | 1.3025, 103.905 | ~70 m |
  | Marine Terrace | 1.3057, 103.9156 | 1.3067, 103.915 | ~130 m |
  | Siglap | 1.3144, 103.9281 | 1.3097, 103.9289 | **~530 m** |
  | Bayshore | 1.3175, 103.9377 | 1.3147, 103.9436 | **~700 m** |

  These feed the **server-side station-context** (`exit_centroid` + nearest-bus-stop are computed from
  `stations.json` lat/lng), so the stale coords were mis-anchoring those TEL East-Coast station cards.

- **[DELTA].**
  - `data/stations.json` — the 9 TEL stations' lat/lng realigned to canonical `mrt-coords`.
  - `package.json` — 0.62.533 → **0.62.534**.
  - `doc/Journal/journal-0_62_534-15_07_26-0436.md` — this entry.
  - `doc/.serial-state.yml` — commit 1007→**1008**, build 633→**634**, journal 889→**890**; re-anchor
    15-07 '26 04:36 SGT (prior 889 anchor demoted verbatim, AU-7).

- **[SCOPING].** A raw regen also bumps every `last_updated_at` **and** would revert #1543's CCL6
  custom `data_quality_notes` to the builder's "no exits in source". I collapsed the 171
  timestamp-only stations to their committed timestamps and restored the 3 CCL6 stations to committed —
  net diff = **only the 9 TEL stations' lat/lng** (+ their `last_updated_at`). NOTE: the builder always
  stamps `last_updated_at = now()`, so committed `stations.json` can never byte-match a fresh regen; a
  future builder enhancement could stabilise unchanged rows (out of scope here).

- **[VERIFICATION (sandbox)].**
  - NEW coords == `mrt-coords.json` by construction (the builder reads it).
  - Per-station deep-compare confirms **only** the 9 TEL stations changed; JSON valid.
  - **Full `npm test` → 3699/3699 across 144 files.**
  - No `.js` changed (`build-station-info.js` unchanged since #1544 is on `main`) → **no web build**
    (`stations.json` is server-read).

- **[STATUS]** PROD — the version bump redeploys the bot; the 9 TEL station cards now compute their
  context from the corrected coordinates.

- **[TEST]** Server-read data change; full `vitest`. (The `transport-line-paths` ring test uses its own
  inline fixtures, so it's unaffected.)

- **[KNOWN GAPS].**
  1. 🚪 **CCL6 + Punggol Coast exit pins** — need the LTA exit file (`d_b39d3a0871985372d7e1637193335da5`);
     `api-open.data.gov.sg` proxy-denied, download failing operator-side. Awaits repo-drop or allowlist.
  2. 🕒 **Builder `last_updated_at` churn** — every regen restamps all rows; a stabilise-unchanged-rows
     enhancement would make future regens diff-clean.
  3. 🧱 **Consolidation queue** — PR-2…PR-6 remain (paused on the merge-model choice).

## Confirmation Gates
| Gate | Status | Note |
|------|--------|------|
| G1 | ✅ | PATCH bump 0.62.533→0.62.534; data reconciliation, standard per-PR rule |
| G2 | ➖ | no destructive action |
| G3 | ➖ | no new decision rule |
| G4 | ➖ | no external paid API call |
