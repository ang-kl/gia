(№ 394 - 28-05 '26 03:00 SGT) v0.61.193

# Journal — soleat v0.61.193 (SG-only cuisine chip gating)

> Operator reminder from v0.61.191's design conversation:
> *"Remember to turn off quick toggle buttons … healther choice in
> the quick filter, cuisine like Durian, Durian Pastry are turn
> off"* when region != SG. v0.61.193 implements the chip half
> (fruits / durian / durian-pastry).
>
> Healthier Choice on inspection is a venue card BADGE (🥗 set on
> v.healthierChoice) — not a quick filter. So no quick-filter
> gating is added in this PR.
>
> Commit 406 / journal 394 / build 196, on branch
> `claude/v0.61.193-sg-only-chip-gating` off `main` at v0.61.192.

## New

### [HDR] #394 | 03:00 SGT 28-05-28 | v0.61.193 — SG-only chip gating | 5 files | 1 PR | 1 build |

- **[DELTA]**
  - **NEW `web/cuisine/src/v2/lib/sg-only-slugs.js`** — frozen
    Set `{ fruits, durian, durian-pastry }` + helpers
    `isSgOnlySlug(slug)` + `stripSgOnly(slugs[])`.
  - **`web/cuisine/src/v2/components/CuisineCategoryDrawer.jsx`** —
    imports SG_ONLY_SLUGS; accepts new `region` prop (default
    'SG'); chip render gates `regionLocked = sgOnly && region !==
    'SG'` → applies `opacity-40` + `disabled` + a localized
    `title` tooltip + appends `🇸🇬` to the locked chip's name as
    a visual cue.
  - **`web/cuisine/src/v2/components/CuisineDrawer.jsx`** — passes
    `region` prop through to `<CuisineCategoryDrawer>`.
  - **`web/cuisine/src/v2/App.jsx`** — imports `stripSgOnly`;
    new useEffect that strips SG-only slugs from `state.cuisines`
    whenever `state.region !== 'SG'`. Without this, an operator
    who had Durian selected, then switched to JB/OTHER, would
    see the chip selected but locked — confusing.
  - **`package.json`** — `0.61.192 → 0.61.193`.
  - **`doc/.serial-state.yml`** — commit 405 → 406, build 195 →
    196, journal 393 → 394.
  - **`doc/Journal/journal-0_61_193-28_05_26-0300.md`** — this entry.

- **[VERIFICATION (sandbox)]**

  ```
  $ cd web/cuisine && npm run build   (✓ built in 2.61s)
  ```

- **[STATUS]** PR-OPEN on push. Operator merges manually.

- **[TEST]** Operator (post-merge + Railway redeploy + TMA cache
  bust):
  - SG anchor: open the Dessert/Fruits category → all chips
    (including Durian, Durian Pastry, Fruits) are interactive,
    unchanged from pre-v0.61.193.
  - JB / OTHER anchor: open the same category → Durian,
    Durian Pastry, Fruits chips render at opacity-40 + non-
    tappable + show 🇸🇬 suffix on the label. Hover/long-press
    shows "Singapore-only — switch region to SG."
  - With one of them PRE-selected on SG, then toggle region to
    JB/OTHER → the selection silently drops (`state.cuisines`
    has the slug stripped).
  - Toggle back to SG → the chip is interactive again (no
    auto-restore; operator picks it again deliberately).

- **[KNOWN GAPS]**
  - **Healthier Choice** is a venue badge, not a filter — no
    operator-facing toggle to gate. If the operator clarifies a
    different surface they meant ("turn off Healthier Choice in
    the quick filter"), we'll wire that in a follow-up. For now,
    the badge keeps rendering on venue cards regardless of region
    (it's data carried per-venue, not a user-set filter).
  - **No tests** — React component without rig. Visual + manual.
  - **SG_ONLY_SLUGS scope** is hardcoded to the three slugs the
    operator named. If we later add (say) "kueh" or
    "kopitiam" SG-specific chips, they'd need to be added to
    the Set.

- **Confirmation Gates**

  | Gate | Resolution | Date |
  |------|-----------|------|
  | G1 — version bump | PATCH `0.61.192 → 0.61.193`. Pure UI gate. No API change. | 28-05 '26 |
  | G2 — destructive action | The useEffect strips SG-only slugs from `state.cuisines` on region change — this IS a state mutation but matches operator intent ("turn them off"). Operator can re-select on returning to SG. | 28-05 '26 |
  | G3 — new decision rule | YES — `SG_ONLY_SLUGS` is canonical for "chips that don't apply outside SG". Future SG-specific cuisines should be added to the Set. | 28-05 '26 |
  | G4 — paid external API call | _n/a_. | 28-05 '26 |

## Amendments

- **CuisineCategoryDrawer chip render** — was: `dim = !sel &&
  selected.length >= maxSelected`. Now: `dim = (!sel && >= max) ||
  regionLocked`, where `regionLocked = sgOnly && region !== 'SG'`.
- **Cuisine TMA `state.cuisines`** — new invariant: when
  `state.region !== 'SG'`, no SG_ONLY_SLUGS member is ever
  present. Enforced by the App.jsx useEffect.

## Deleted

_None._
