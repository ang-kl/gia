(№ 913 - 17-07 '26 07:04 SGT) v0.62.556

# Journal — soleat v0.62.556 (Hawker: tap the whole centre card to highlight its pin + toggle active — like the station/bus pills)

> Operator: "How do I tap on the hawker centre card in list style in portrait mode? When I tap anywhere
> within the card except the pill, you should [wire] the same as the station pills and bus-stop pills
> within the card to highlight the hawker centre pin and change the hawker centre card status."

## New

### [HDR] #913 | 07:04 SGT 17-07-26 | v0.62.556 — tappable centre card → highlight pin + active toggle | 2 files | PR #(pending) |

- **[DELTA — map: reusable centre focus].** `web/hawker/src/components/HawkerMapPanel.jsx`: extracted the
  centre-pin tap body into **`activateCentre(c, marker, key)`** (open InfoWindow, zoom to 17, draw the
  transit + walk/2-stop rings, lazy-fetch transit); the marker's click listener now calls it. Centre
  markers are stored **by name** (`markersByNameRef`), and a new global **`window.__giaHawkerFocusCentre(name)`**
  fires the marker's own `click` (via `google.maps.event.trigger`) so a card tap runs the *identical*
  flow as tapping the pin (freshest binding, no stale-locale capture). `revealMap()` scrolls the map
  into view first (phone/stacked).

- **[DELTA — card: tappable + active state].** `web/hawker/src/App.jsx`:
  - New `handleCardTap(c)` reuses the two-tap `handlePillTap` with id `<name>|card`: **1st tap** →
    `__giaHawkerFocusCentre(name)` (highlight the pin) + toggle the card active; **2nd tap** → open the
    centre's Google Maps (`c.mapsUrl`) + clear — exactly like the station/bus pills.
  - `renderCentreCard` root is now `role="button"` + `cursor-pointer` + `onClick={handleCardTap}`, and
    shows an **accent ring + border** (`border-tg-accent ring-1 ring-tg-accent`) when active ("change
    the card status").
  - The inner interactive elements (station pill, bus-stop pills, 📍 Maps link, Save-to-chat button)
    now **`stopPropagation`** so a tap on a pill does its own thing and never fires the card tap
    ("anywhere within the card EXCEPT the pill").

- **[DELTA — housekeeping].** `package.json` 0.62.555 → **0.62.556**; Journal #913 + serial-state
  (commit 1030→**1031**, build 655→**656**, journal 912→**913**; re-anchor 17-07 '26 07:04 SGT,
  prior 912 demoted verbatim AU-7).

- **[VERIFICATION (sandbox)].** **Hawker vite build green** (42 modules); **full `npm test` 3719/3719
  across 145 files**. ⚠️ The card tap / pin highlight / active ring are live-only (iPad) — shipping live.

- **[STATUS]** STAGED → PROD on merge (operator "make it live").

- **[KNOWN GAPS].**
  1. 👀 **Device recheck** — tapping a centre card (not a pill) highlights the pin (InfoWindow + zoom +
     rings) + rings the card accent; a 2nd tap opens Maps + clears; the pills still work independently?
     Applies in every layout (portrait list, portrait panel, landscape carousel), not just portrait.
  2. 🅺🅸🆅 Hawker CLEANING-closure still shelved (needs a data.gov.sg sample; sandbox egress-blocked).

## Confirmation Gates
| Gate | Status | Note |
|------|--------|------|
| G1 | ✅ | PATCH bump 0.62.555→0.62.556; additive interaction; build + 3719 tests green |
| G2 | ➖ | no destructive action (centre-tap body extracted, behaviour preserved) |
| G3 | ➖ | no new decision rule |
| G4 | ➖ | no external paid API call |
