# Journal — v0.62.637 (Hawker UI professionalisation: roll the Transport C1–C3 layer to Hawker)

_(№ 1,161 — 25-07 '26 08:39 SGT)_

## New

### [HDR] v0.62.637 — Hawker TMA motion/polish (reduced-motion + AutoAnimate + Motion springs + View Transitions)

- **[INTENT]** Operator (after the v0.62.636 Transport pilot merged): *"continue"* → *"layer for Hawker"* —
  roll the same proven C1–C3 UI-polish layer to the Hawker TMA.

- **[DELTA]** (Hawker; new deps `@formkit/auto-animate ^0.10` + `motion ^12.42`; mirrors the Transport pilot)
  - **C1 — foundation:** `web/hawker/src/styles.css` — appended the `@media (prefers-reduced-motion: reduce)`
    reset (collapses transitions/scroll; Hawker has no infinite decorative animations) + the `.gia-skeleton`
    shimmer + `::view-transition-old/new(root)` 260 ms crossfade timing. **No** full-screen `LoadingSkeleton` —
    Hawker renders the map + header immediately and streams centres, so there is no blank load state to
    replace (the `busy` text pill stays).
  - **C2 — AutoAnimate:** one `useAutoAnimate()` `listParent` ref on **both** centre-list containers (the
    portrait-tablet panel grid + the phone bottom-sheet drawer list; only one mounts at a time) — cards
    fade/slide as the region (and its centre set) changes. Honours reduced-motion.
  - **C3 — Motion + View Transitions:**
    - **NEW** `web/hawker/src/lib/view-transition.js` (copied from Transport) — `withViewTransition()` wraps
      the 3 view-swap toggles: the map/list `MapToggleButton` (`setListHidden`), the portrait-panel map's
      `setMapExpanded(true)`, and the carousel map's `setMapExpanded(false)`.
    - Motion spring on the **selected centre-card "pop"**: `renderCentreCard`'s root `<div>` → `m.div`,
      `animate={{ scale: cardOn ? 1.02 : 1 }}` spring `420/30/0.7`, `useReducedMotion`-gated (+ `shadow-xl` /
      `relative z-10` on active). Loaded via `LazyMotion` + `domAnimation` in `main.jsx` (the ~⅓-size Motion
      bundle).

- **[BUNDLE COST — flagged, same shape as Transport]** Hawker gzip **~+30 KB** (Motion `domAnimation` +
  AutoAnimate). Same fallback options if the operator declines: CSS-spring (drop Motion) or AutoAnimate-only.

- **[VERIFICATION (sandbox)]** `hawker` `vite build` green (451 modules); FULL `npm test` **3764/3764 across
  148 files**; **Playwright runtime smoke** (stubbed Telegram + `centres-by-region`) — app mounts
  (`rootChildren=1`), content renders, **zero** JS / Motion / AutoAnimate runtime errors. **CANNOT** drive the
  live webview / iPad mini in-sandbox → operator device-check the *feel*: region-change card fade, map/list
  crossfade, centre-select spring pop, reduced-motion stillness. `package.json` + lockfile 0.62.636 →
  0.62.637; `.serial-state.yml` commit 1119→1120, build 738→739, journal 980→981, re-anchor **25-07 '26 08:39
  SGT**.

- **[TIME ANCHOR]** data.gov.sg blocked (`403` `CONNECT`); container clock `2026-07-25T00:39Z` = **25-07 '26
  08:39 SGT** (TF-9).

- **[STATUS]** DRAFT → squash-merge to `main` on green CI (auto-merge SQUASH). New PR — branch restarted from
  `origin/main` `bd1b388` (#1646 merged).

- **[TEST]** No new unit test — motion/DOM behaviour needs the live webview; covered by build + the full suite
  + the Playwright mount smoke.

- **[KNOWN GAPS / caveats]**
  - Live *feel* UNVERIFIED in-sandbox — operator device eyeball is the point.
  - Bundle +30 KB gzip (see above) — same decision point as Transport.
  - Operator's device verdict on the Transport pilot (v0.62.636) **and** this Hawker rollout is still pending.
  - **Cuisine rollout NOT done** — awaiting the go-ahead. Phase C4 (Sonner/NumberFlow) still deferred.
  - `view-transition.js` is duplicated per-app (Transport + Hawker copies); could be consolidated into
    `web/_shared/lib` on the Cuisine pass.

## Confirmation Gates
| Gate | Status | Note |
|------|--------|------|
| G1 | ✅ | PATCH bump 0.62.636 → 0.62.637; hawker build + 3764 tests + Playwright mount smoke green. |
| G2 | ➖ | additive (2 new deps, 1 new file, motion wrappers); nothing removed. |
| G3 | ➖ | no new decision rule (rollout of an approved pilot). |
| G4 | ➖ | no external paid API call. |
