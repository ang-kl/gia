(№ 377 - 27-05 '26 22:45 SGT) v0.61.176

# Journal — soleat v0.61.176 (visible running-version chip on /ver + Periodical menu)

> Operator after PR #676 merged: *"still v0.61.174"* — Railway took
> ~15+ min to roll the deploy and the operator had no in-chat way
> to confirm which version was live. Filed as a 5-min follow-up.
>
> Commit 389 / journal 377 / build 179, on branch
> `claude/v0.61.176-running-version-chip` off `main` at v0.61.175
> (PR #676).

## New

### [HDR] #377 | 22:45 SGT 27-05-27 | v0.61.176 — Running-version chip on /ver + Periodical menu | 3 files | 1 PR | 1 build |

- **[INTENT]** Give the operator a one-glance answer to "what
  version is running right now?" on the two owner surfaces they
  use most: the `/ver` admin keyboard message + the Periodical
  menu header.

- **[DELTA]**
  - **`index.js`** (2 edits):
    - **`/ver` `bot.onText` handler** (~line 3412): the follow-up
      "🛠 Admin commands" inline-keyboard message gains the
      version: `🛠 Admin commands  ·  v0.61.176`. Uses
      `pkgJson.version` (already imported at line 7).
    - **`ownerPeriodicalMenu`** (~line 3306): header changes from
      `📊 Periodical — cadence per item` to `📊 Periodical —
      cadence per item  ·  v0.61.176`. Italics on the version
      keep it visually subordinate to the menu title.
  - **`package.json`** — `0.61.175 → 0.61.176`.
  - **`doc/.serial-state.yml`** — `commit 388 → 389`, `build 178
    → 179`, `journal 376 → 377`; re-anchored 27-05 '26 22:45 SGT.
  - **`doc/Journal/journal-0_61_176-27_05_26-2245.md`** — this
    entry.

- **[VERIFICATION (sandbox)]**

  ```
  $ node --check index.js                        (OK)
  ```

  No tests for the chat-UI layer; visual verification only.
  pkgJson.version is read at module load and cached in the
  Node.js require cache for the lifetime of the container —
  any redeploy implicitly refreshes it.

- **[STATUS]** PR-OPEN on push. Operator merges manually.

- **[TEST]** Operator (post-merge):
  - Send `/ver` — the health-check output already shows
    `soleat vX.X.X` at line 1 (ver.js:199, unchanged). The
    FOLLOW-UP "🛠 Admin commands" message now also shows
    `· v0.61.176`.
  - Tap **📊 Periodical (counts)** — header reads
    `📊 Periodical — cadence per item · v0.61.176`.
  - After the next merge, watch for the version chip to flip
    once Railway's redeploy lands — confirms when the new
    container is live without guessing.

- **[KNOWN GAPS]**
  - **TMA bundle version is separate** from the bot's
    pkgJson.version. The TMA build is bundled into static assets
    served from `/public/cuisine/`; browser caching can keep an
    old bundle in the Telegram webview even after the bot
    container is updated. If operator wants a TMA-side "running
    bundle build" chip too, that's a small follow-up — Vite
    can inject the build timestamp into `import.meta.env.VITE_BUILD_TIME`
    or similar, surfaced in the TMA footer.
  - **Build commit ref** (`detectBuildRef()` in ver.js) is
    already printed on /v / /ver health check output but NOT in
    the Admin keyboard / Periodical menu. If a 12-char SHA chip
    would help diagnose "is this the right commit", easy to
    add: `v0.61.176 · abc123def456`.

- **Confirmation Gates**

  | Gate | Resolution | Date |
  |------|-----------|------|
  | G1 — version bump | PATCH `0.61.175 → 0.61.176`. Pure UX hint; no behaviour change, no API surface change. | 27-05 '26 |
  | G2 — destructive action | _n/a_ — string edits to existing message text only. | 27-05 '26 |
  | G3 — new decision rule | _n/a_. | 27-05 '26 |
  | G4 — paid external API call | _n/a_. | 27-05 '26 |

## Amendments

- **`/ver` "🛠 Admin commands" message** — was
  `🛠 <b>Admin commands</b>` (v0.61.34). Now
  `🛠 <b>Admin commands</b>  ·  <i>v0.61.176</i>`.
- **Periodical menu header** — was
  `📊 <b>Periodical — cadence per item</b>\nButton shows ...`
  (v0.61.175). Now
  `📊 <b>Periodical — cadence per item</b>  ·  <i>v0.61.176</i>\nButton shows ...`.

## Deleted

_None._
