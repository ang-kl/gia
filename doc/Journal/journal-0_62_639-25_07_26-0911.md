# Journal — v0.62.639 (Train iPad mini: squeeze + tighten the carousel cards; list becomes a drawer over the map with named uniform-grid tiles)

_(№ 1,163 — 25-07 '26 09:11 SGT)_

## New

### [HDR] v0.62.639 — Train carousel narrower/tighter + LIST mode = bottom-sheet drawer over the map + grid tiles show the station name

- **[INTENT]** Operator (iPad mini; `IMG_1195` Train carousel + `IMG_1196` Train LIST + `IMG_1197` Hawker
  carousel reference, all v0.62.638): *"You said same height as hawker's carousel cards, where? And this is
  iPad mini screenshot, have at least four can be seem and half at each side, therefore squeeze the width.
  Reduce spacing above and below the body text. The listing — where are the train station name, and why the
  card isn't uniformly a square, use triangle collapse/expand inside the card for details. Drawer effect for
  listing means is another layer on top of the map. I thought we had that in other TMA."*

- **[DELTA]** (Train TMA, 2 files)
  1. **Carousel too WIDE + TALL vs Hawker** — `web/transport/src/App.jsx` `StationCarousel` basis narrowed
     `basis-[82%]/md44%/1180px30%` → **`basis-[60%] sm:basis-[31%] md:basis-[22%]`**, so ~**4** cards + a
     half-peek at each side show on an iPad-mini width. `web/transport/src/components/StationCard.jsx` compact
     body `gap-2 p-2` → **`gap-1 p-1.5`** and `LineSubCard` `p-2 gap-1.5` → **`p-1.5 gap-1`** ("reduce spacing
     above and below the body text"), so a card reads nearer Hawker height.
  2. **LIST "where are the station names"** — the v0.62.632 4/6 CSS-columns truncated the name to "EW1 P..".
     Switched to a **real grid**: `grid grid-cols-2` (portrait) / `grid-cols-3` (landscape) / `1` (phone) +
     `items-start`, so the full name shows and the tiles read as a uniform grid; the collapsible tile keeps
     its **▾** (triangle collapse/expand per card).
  3. **LIST is now a DRAWER OVER THE MAP** (operator: "another layer on top of the map … other TMA") — the
     list `viewMode` return was rebuilt from the two-panel split (fixed map `h-40vh` + scrolling grid) into a
     **full-bleed map** (`absolute inset-0 z-0`, `navInset`) + a floating header + the station grid inside the
     shared **`BottomSheet`** (Hawker / Cuisine parity), on every device.

- **[VERIFICATION (sandbox)]** `transport` `vite build` green (`grid-cols-1/2/3` + `items-start` in the built
  CSS); FULL `npm test` **3764/3764 across 148 files**; transport Playwright smoke — mounts
  (`rootChildren=3`), zero JS / Motion runtime errors. **CANNOT** drive the live iPad in-sandbox → operator
  device-check: carousel ~4-up + tighter, list names visible + uniform grid + the ▾ triangle, and the list
  drawer floating over the map. `package.json` + lockfile 0.62.638 → 0.62.639; `.serial-state.yml` commit
  1121→1122, build 740→741, journal 982→983, re-anchor **25-07 '26 09:11 SGT**.

- **[TIME ANCHOR]** data.gov.sg blocked (`403` `CONNECT`); container clock `2026-07-25T01:11Z` = **25-07 '26
  09:11 SGT** (TF-9).

- **[STATUS]** DRAFT → squash-merge to `main` on green CI (auto-merge SQUASH). New PR — branch restarted from
  `origin/main` `f5d1bf5` (#1648 merged).

- **[TEST]** No new unit test — carousel dims / drawer / grid are layout behaviour needing the live webview;
  covered by the build + the full suite + the mount smoke.

- **[KNOWN GAPS / caveats]**
  - **"Uniformly a square"** — the collapsed tiles are uniform-**width** short blocks, not literal squares. If
    the operator wants exact squares, a `min-height` / `aspect` on the collapsed tile is a one-line follow-up.
  - Carousel "same height as Hawker" is now much closer (tighter spacing), but the Train card is inherently
    richer (per-line status), so a multi-line interchange can still run a touch taller — natural-height, like
    Hawker's own ragged carousel.
  - Live iPad behaviour UNVERIFIED in-sandbox.
  - The Motion select-pop + AutoAnimate (v0.62.636) still apply in both the carousel and the new drawer grid.

## Confirmation Gates
| Gate | Status | Note |
|------|--------|------|
| G1 | ✅ | PATCH bump 0.62.638 → 0.62.639; transport build + 3764 tests + smoke green. |
| G2 | ➖ | layout change (carousel dims, grid, list→drawer); nothing removed from data/behaviour. |
| G3 | ➖ | no new decision rule. |
| G4 | ➖ | no external paid API call. |
