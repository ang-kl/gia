(№ 818 - 01-07 '26 17:10 SGT) v0.62.464

# Journal — soleat v0.62.464 (dish taxonomy pipeline — step 1: script + workflow)

> Operator: "why the dishes in Filipino cuisine are classified as 'more'? 'more' seems
> meaningless" → audit found 52 of ~70 cuisines mostly "other" (many 100%). Operator: fold
> dish-TYPE reclassification into the already-planned Gemini taxonomy pass, rather than
> hand-expanding the brittle regex classifier.

## Audit finding (before implementation)

`dish-food-group.js`'s `foodGroupFor()` classifies dish type by regex over **SG/Malaysian hawker
vocabulary only**. Checked every cuisine: **52 of ~70 are over 50% "other"**, many at **100%**
(French, Korean, Spanish, Mexican, Lebanese, Portuguese, Brazilian, Argentinian, and more) — dishes
like "adobo", "paella", "kimchi", "tacos" match none of the rules. Not a Filipino-specific bug; a
structural gap.

## New

### [HDR] #819 | 17:10 SGT 01-07-26 | v0.62.464 — dish-taxonomy script + workflow (step 1) | 3 files | PR #(pending) |
- **`scripts/draft-dish-taxonomy.mjs`** (NEW) — mirrors `draft-dish-notes.mjs`'s grounded, batched,
  idempotent pattern. Classifies every `iconicDish` into **4 closed-enum fields** via Gemini +
  Google Search grounding: `type` (12 values, replacing the brittle regex classifier),
  `mealTime[]`, `dietary`, `course` (**omitted for `kind:'drink'`** — drinks have no course, per
  operator decision). Strict enum validation rejects any invalid/partial model output (retried next
  run) — closed enums avoid the free-text length/hallucination risk the dish-note drafting has to
  work around.
- **`.github/workflows/draft-dish-taxonomy.yml`** (NEW) — `workflow_dispatch` only,
  `GEMINI_API_KEY` from secrets (never logged), pushes `bot/draft-dish-taxonomy` — Actions can't
  self-open PRs (repo policy), so the PR is opened from that branch afterward.
- **`nation-overlay.js`** — new merge block folds `nation-overlay-taxonomy.generated.js` onto
  each dish's `type`/`mealTime`/`dietary`/`course` at load. `dish-food-group.js`'s regex classifier
  remains the **fallback only** for dishes the generated overlay hasn't reached yet — same
  "generated fills gaps" precedent as the dish-notes/dish-notes-i18n/dish-local overlays.
- **[VERIFICATION (sandbox)]** `node --check` on the new script + edited `nation-overlay.js`;
  structural YAML sanity check on the workflow file.
- **[STATUS]** DRAFT — no data generated yet; this PR is pipeline plumbing only.
- **[KNOWN GAPS / NEXT]** Dispatch the workflow in batches (~1,689 dishes; default batch/max caps
  ~100 dishes per dispatch) and review English + enum output per batch; then extend
  `translate-content.mjs` with a 4th target for the ~15-20 enum labels; then wire the client badges.

## Confirmation Gates
| Gate | Status | Note |
|------|--------|------|
| G1 | ✅ | node --check all 3 files |
| G2 | ➖ | no unit tests touch generator scripts |
| G3 | ✅ | serial 817→818 |
| G4 | ➖ | pipeline plumbing only — no user-facing change yet |

### [HDR] #820 | 18:17 SGT 01-07-26 | v0.62.464 — merge PR #1454 (Soleat User Manual sections 1–6) | 4 files | PR #1454 |

> Operator: "Merge PR #1454"

- **PR #1454** (`docs/soleat-user-manual-sections-1-6` → `main`) was opened as **draft**; marked
  ready-for-review and **squash-merged** to `main` (merge commit `872626c`).
- **`docs/UserManual/en/README.md`** (NEW), **`docs/UserManual/en/00-sections-1-to-6.md`** (NEW) —
  first English master scaffold for the Soleat User Manual, sections 1–6 (Welcome, Who It's For,
  Why It's Different, Where It Works Best, What It Helps You Do, Getting Started).
- **`docs/UserManual/glossary/terms.en.json`** (NEW), **`docs/UserManual/translation-status.json`**
  (NEW) — terminology glossary + the 8-language translation plan (English drafted only; other
  languages tracked as not-started).
- Docs-only change; no application code (`index.js`, bot handlers, `i18n.js`, fuzzy matchers)
  touched. No `package.json` version bump — `gia-preflight` node --check / test gates don't apply
  to this PR's file set.
- **[VERIFICATION (sandbox)]** N/A — no code changed; PR reviewed for scope (docs-only, no
  admin/owner-only commands leaked into the user-facing manual per the PR's own review notes).
- **[STATUS]** PROD — merged to `main`.
- **[TEST]** N/A (docs-only).
- **[KNOWN GAPS / NEXT]** Per PR body: begin translation only after the English master,
  terminology, food-name rules, and help IDs are stable; still need a pass on whether any wording
  overstates feature availability outside Singapore. `doc/Register/` (last touched 14-06-26) and
  `doc/VibeCodingRecord/` (last regenerated 30-06-26) are both still due a catch-up pass — logged
  here rather than done in this pass, per the Journal standing rule §3.

## Confirmation Gates (PR #1454 merge)
| Gate | Status | Note |
|------|--------|------|
| G1 | ➖ | no version bump — docs-only, no code changed |
| G2 | ➖ | no destructive action |
| G3 | ✅ | serial 818→819; commit 936→937 |
| G4 | ➖ | no paid external API call |

### [HDR-amend] counter re-anchor | 10:00 SGT 02-07-26 | supersedes the 818→819/936→937 note above

The 818→819 / 936→937 bump recorded above (this branch's own snapshot, still unmerged as PR
#1456 at the time) was overtaken on `main` by concurrent, unrelated work merged in the interim
(PR #1459 v0.62.466 cuisine closed-strip, PR #1460 dish-source re-curation, PR #1461 docs food-
search sections) which advanced the shared counters to journal 820 / commit 938 / build 577
before this branch could land. Per AU-1 (add, never compress) the prior note is left as-is above;
this branch's actual serial numbers are re-anchored here to the first free slots after that
concurrent work: **journal 820→821, commit 938→939** for the #1454 record, continuing to
**821→822 / 939→940** for the #1462 record immediately below. `doc/.serial-state.yml`'s
counters/anchor block reflects the final 822/940 state directly (not the superseded 819/937).

### [HDR] #822 | 10:00 SGT 02-07-26 | v0.62.464 — merge PR #1462 (Soleat User Manual decision-support sections 15–18) | 3 files | PR #1462 |

> Operator: "Merge PR #1462"

- **PR #1462** (`docs/soleat-user-manual-decision-support` → `main`) was opened as **draft**;
  marked ready-for-review and **squash-merged** to `main` (merge commit `6541f38`).
- **`docs/UserManual/en/03-sections-15-to-18.md`** (NEW) — next English master block: section 15
  (Google Map View), section 16 (Recognised Venues and Curated Food Sources), section 17 (Hidden
  Gems), section 18 (Set Meals and Practical Meal Options).
- **`docs/UserManual/help-registry.seed.json`** — contextual help IDs added for map, recognised
  venues, hidden gems, and practical meal options.
- Docs-only; no application code touched, no `package.json` version bump.
- **[VERIFICATION (sandbox)]** N/A — no code changed; PR's own review notes confirm wording keeps
  set-meal/map-layer/recognised-venue/hidden-gems claims as signals or suggestions, not guarantees.
- **[STATUS]** PROD — merged to `main`.
- **[TEST]** N/A (docs-only).
- **[KNOWN GAPS / NEXT]** Same standing gaps as the #1454 record above: `doc/Register/` and
  `doc/VibeCodingRecord/` catch-up passes still due, not done in this pass.

## Confirmation Gates (PR #1462 merge)
| Gate | Status | Note |
|------|--------|------|
| G1 | ➖ | no version bump — docs-only, no code changed |
| G2 | ➖ | no destructive action |
| G3 | ✅ | serial 821→822; commit 939→940 |
| G4 | ➖ | no paid external API call |

### [HDR] #823 | 10:44 SGT 02-07-26 | v0.62.464 — merge PR #1463 (Soleat User Manual Singapore-context sections 19–24) | 3 files | PR #1463 |

> Operator: "merge PR #1463"

- **PR #1463** (`docs/soleat-user-manual-singapore-context` → `main`) was opened as **draft**;
  marked ready-for-review and **squash-merged** to `main` (merge commit `87147af`).
- **`docs/UserManual/en/04-sections-19-to-24.md`** (NEW) — Singapore practical-context block:
  section 19 (Hawker Centres & Food Centres), section 20 (Train Network/Transport), section 21
  (Train Station Exits), section 22 (Bus Information), section 23 (Car Parks), section 24
  (Weather).
- **`docs/UserManual/help-registry.seed.json`** — contextual help IDs added for hawker, transport,
  train station exits, bus information, carpark, and weather.
- Docs-only; no application code touched, no `package.json` version bump.
- **[VERIFICATION (sandbox)]** N/A — no code changed; PR's own review notes confirm station-
  timing/bus-arrival/carpark-availability/weather wording is framed as decision support, not a
  guarantee.
- **[STATUS]** PROD — merged to `main`.
- **[TEST]** N/A (docs-only).
- **[KNOWN GAPS / NEXT]** Same standing gaps as the #1454/#1462 records above: `doc/Register/`
  and `doc/VibeCodingRecord/` catch-up passes still due, not done in this pass.

## Confirmation Gates (PR #1463 merge)
| Gate | Status | Note |
|------|--------|------|
| G1 | ➖ | no version bump — docs-only, no code changed |
| G2 | ➖ | no destructive action |
| G3 | ✅ | serial 822→823; commit 940→941 |
| G4 | ➖ | no paid external API call |

### [HDR] #824 | 12:20 SGT 02-07-26 | v0.62.464 — merge PR #1464 (Soleat User Manual planning/support sections 25–29) | 3 files | PR #1464 |

> Operator: "merge PR #1464"

- **PR #1464** (`docs/soleat-user-manual-planning-support` → `main`) was opened as **draft**;
  marked ready-for-review and **squash-merged** to `main` (merge commit `6ee057b`).
- **`docs/UserManual/en/05-sections-25-to-29.md`** (NEW) — final core English master block:
  section 25 (Sketchbook/Clipboard), section 26 (Search Tips), section 27 (Common Problems),
  section 28 (Privacy, Data, and User Control), section 29 (Support and Limitations).
- **`docs/UserManual/help-registry.seed.json`** — contextual help IDs added for Sketchbook,
  search tips, troubleshooting, privacy, and limitations.
- Docs-only; no application code touched, no `package.json` version bump.
- **This completes the first full English manual pass, Sections 1–29** (PRs #1454, #1461, #1462,
  #1463, #1464).
- **[VERIFICATION (sandbox)]** N/A — no code changed; PR's own review notes confirm Sketchbook
  wording follows the approved Catch-all → Cabinet → Drawer → Card model, Common Problems is
  framed as recovery guidance not blame, and Privacy/Data wording avoids storage promises beyond
  user-facing commands/concepts.
- **[STATUS]** PROD — merged to `main`.
- **[TEST]** N/A (docs-only).
- **[KNOWN GAPS / NEXT]** English master (Sections 1–29) is now complete; per PR #1454's original
  scope note, translation to the other 7 languages should begin now that the master, terminology,
  and help IDs are stable. `doc/Register/` and `doc/VibeCodingRecord/` catch-up passes remain due,
  not done in this pass.

## Confirmation Gates (PR #1464 merge)
| Gate | Status | Note |
|------|--------|------|
| G1 | ➖ | no version bump — docs-only, no code changed |
| G2 | ➖ | no destructive action |
| G3 | ✅ | serial 823→824; commit 941→942 |
| G4 | ➖ | no paid external API call |

### [HDR-amend] counter re-anchor | 13:31 SGT 02-07-26 | supersedes the 823→824/941→942 note above

The 823→824 / 941→942 bump recorded above (this branch's own snapshot, still unmerged at the
time) was overtaken on `main` by further concurrent, unrelated work merged in the interim (PR
#1465 v0.62.467 cuisine reopens-today strip, PR #1467 v0.62.468 dish-taxonomy 429 cascade fix)
which advanced the shared counters to journal 822 / commit 940 / build 579 before this branch
could land. Per AU-1 (add, never compress) the prior note is left as-is above; this branch's
actual serial number for the record immediately below is re-anchored to the first free slot
after that concurrent work: **journal 822→823, commit 940→941**. `doc/.serial-state.yml`'s
counters/anchor block reflects this final 823/941 state directly.

### [HDR] #823 | 13:31 SGT 02-07-26 | v0.62.464 — merge PR #1466 (Soleat User Manual consolidation) | 5 files | PR #1466 |

> Operator: "merge PR #1466"

- **PR #1466** (`docs/soleat-user-manual-consolidation` → `main`) was opened as **draft**; marked
  ready-for-review and **squash-merged** to `main` (merge commit `9681ee0`).
- **`docs/UserManual/en/00-table-of-contents.md`** (NEW) — full linked table of contents for
  Sections 1–29 + manual file map.
- **`docs/UserManual/MANUAL-STYLE-GUIDE.md`** (NEW) — user-facing writing rules, overclaiming
  rules, translation and help-integration rules.
- **`docs/UserManual/FOOD-TERM-TRANSLATION-POLICY.md`** (NEW) — meaning-aware food-term
  translation policy; cuisine/dish/native-script review rules.
- **`docs/UserManual/TMA-HELP-INTEGRATION.md`** (NEW) — contextual help pattern using stable
  `helpId`s; future image registry guidance.
- **`docs/UserManual/en/README.md`** — now points to the table of contents and related standards;
  status updated to first full English master draft.
- Docs-only; no application code touched, no `package.json` version bump.
- **[VERIFICATION (sandbox)]** N/A — no code changed; PR's own review notes confirm the help
  registry remains a seed (not a stable app contract yet), translation is gated on this
  consolidation being reviewed, and food terms are to be reviewed for meaning, not translated
  blindly.
- **[STATUS]** PROD — merged to `main`.
- **[TEST]** N/A (docs-only).
- **[KNOWN GAPS / NEXT]** Translation to the other 7 languages can now begin per the style guide +
  food-term policy landed here. TMA help buttons still need to be wired to the stable `helpId`s
  (currently a seed registry only). `doc/Register/` and `doc/VibeCodingRecord/` catch-up passes
  remain due, not done in this pass.

## Confirmation Gates (PR #1466 merge)
| Gate | Status | Note |
|------|--------|------|
| G1 | ➖ | no version bump — docs-only, no code changed |
| G2 | ➖ | no destructive action |
| G3 | ✅ | serial 822→823; commit 940→941 |
| G4 | ➖ | no paid external API call |

### [HDR] #824 | 14:56 SGT 02-07-26 | v0.62.464 — merge PR #1468 (Soleat English Master QA checklist) | 1 file | PR #1468 |

> Operator: "merge PR: #1468"

- **PR #1468** (`docs/soleat-user-manual-qa-checklist` → `main`) was opened as **draft**; marked
  ready-for-review and **squash-merged** to `main` (merge commit `0993d2e`).
- **`docs/UserManual/ENGLISH-MASTER-QA-CHECKLIST.md`** (NEW) — QA checklist covering user-facing
  clarity, feature accuracy, command accuracy, terminology consistency, food-term/translation
  readiness, help-integration readiness, privacy/safety, and final editorial polish.
- Per the PR's own note: this was intended as part of the #1466 consolidation but missed that
  merge window, so it landed as a clean follow-up.
- Docs-only; no application code touched, no `package.json` version bump.
- **[VERIFICATION (sandbox)]** N/A — no code changed.
- **[STATUS]** PROD — merged to `main`.
- **[TEST]** N/A (docs-only).
- **[KNOWN GAPS / NEXT]** Same standing gaps as prior manual-arc records: `doc/Register/` and
  `doc/VibeCodingRecord/` catch-up passes remain due, not done in this pass. This checklist should
  be run against the Sections 1–29 master before translation work begins.

## Confirmation Gates (PR #1468 merge)
| Gate | Status | Note |
|------|--------|------|
| G1 | ➖ | no version bump — docs-only, no code changed |
| G2 | ➖ | no destructive action |
| G3 | ✅ | serial 823→824; commit 941→942 |
| G4 | ➖ | no paid external API call |

### [HDR-amend] counter re-anchor | 19:54 SGT 03-07-26 | supersedes the 823→824/941→942 note above

The 823→824 / 941→942 bump recorded above (this branch's own snapshot, still unmerged at the
time) was overtaken on `main` by a full day of further, entirely unrelated code work (Michelin
card fixes, French cuisine i18n, open-hours field/cache fixes, and more — v0.62.469 through
v0.62.484) which advanced the shared counters to journal 838 / commit 956 / build 595 before this
branch could land. Per AU-1 (add, never compress) the prior note is left as-is above; this
branch's actual serial number for the record immediately below is re-anchored to the first free
slot after that concurrent work: **journal 838→839, commit 956→957**. `doc/.serial-state.yml`'s
counters/anchor block reflects this final 839/957 state directly.

### [HDR] #839 | 19:54 SGT 03-07-26 | v0.62.464 — merge PR #1469 (Soleat English Master QA patches) | 5 files | PR #1469 |

> Operator: "merge #1469"

- **PR #1469** (`docs/soleat-user-manual-qa-patches` → `main`) was opened as **draft**; marked
  ready-for-review and **squash-merged** to `main` (merge commit `b1d3ade`).
- **`docs/UserManual/QA-RUN-2026-07-02.md`** (NEW) — QA run summary against the #1468 checklist:
  pass notes, patches applied, remaining review notes.
- **`docs/UserManual/en/02-sections-11-to-14.md`** — internal-sounding "should use" wording →
  user-facing "uses" wording.
- **`docs/UserManual/en/03-sections-15-to-18.md`** — "should support" practical-search wording →
  "can help" wording.
- **`docs/UserManual/en/04-sections-19-to-24.md`** — station timing clarified as first-train,
  last-train, or service-timing context.
- **`docs/UserManual/en/05-sections-25-to-29.md`** — `/forgetme` wording tightened from absolute
  "erase" language to removal-**request** language (notable since it's a user-facing data/privacy
  claim, even though this PR is docs-only and doesn't touch the actual `/forgetme` handler code).
- Docs-only; no application code touched, no `package.json` version bump.
- **[VERIFICATION (sandbox)]** N/A — no code changed; PR's own QA-run doc records the pass notes.
- **[STATUS]** PROD — merged to `main`.
- **[TEST]** N/A (docs-only).
- **[KNOWN GAPS / NEXT]** Per PR's QA result: manual is broadly ready as first English master
  draft; remaining work is glossary expansion, help-ID contract review, screenshot/image planning,
  command alias validation, and food-term review before translation. `doc/Register/` and
  `doc/VibeCodingRecord/` catch-up passes remain due, not done in this pass.

## Confirmation Gates (PR #1469 merge)
| Gate | Status | Note |
|------|--------|------|
| G1 | ➖ | no version bump — docs-only, no code changed |
| G2 | ➖ | no destructive action |
| G3 | ✅ | serial 838→839; commit 956→957 |
| G4 | ➖ | no paid external API call |

### [HDR-amend] counter re-anchor | 09:59 SGT 04-07-26 | supersedes the 838→839/956→957 note above

The 838→839 / 956→957 bump recorded above (this branch's own snapshot, still unmerged at the
time) was overtaken on `main` by further concurrent code work in the interim (PR #1486 v0.62.485
Michelin decouple-from-narrate, PR #1487 v0.62.486 open-hours empty-currentPeriods shadowing fix,
PR #1489 v0.62.487 derive open-hours label when openNow null) which advanced the shared counters
to journal 841 / commit 959 / build 598 before this branch could land. Per AU-1 (add, never
compress) the prior note is left as-is above; this branch's actual serial number for the record
immediately below is re-anchored to the first free slot after that concurrent work: **journal
841→842, commit 959→960**. `doc/.serial-state.yml`'s counters/anchor block reflects this final
842/960 state directly.

### [HDR] #842 | 09:59 SGT 04-07-26 | v0.62.464 — merge PR #1488 (Soleat glossary expansion + help-ID contract review + translation-readiness doc) | 3 files | PR #1488 |

> Operator: "merge PR #1488"

- **PR #1488** (`docs/soleat-user-manual-glossary-help-contract` → `main`) was opened as
  **draft**; marked ready-for-review and **squash-merged** to `main` (merge commit `a99af2e`).
- **`docs/UserManual/glossary/terms.en.json`** — expanded from a small draft glossary into a
  broader translation-ready glossary covering product names, surfaces, features, planning objects,
  food-search concepts, caution phrases, and public commands. Product names and commands are
  marked non-translatable where appropriate; Singapore-specific terms kept explicit.
- **`docs/UserManual/HELP-ID-CONTRACT-REVIEW.md`** (NEW) — reviews current help-ID families,
  records which IDs are acceptable as seed IDs, proposes future granular help IDs for TMA wiring.
  Help registry remains a **seed**, not a stable app contract yet.
- **`docs/UserManual/TRANSLATION-READINESS.md`** (NEW) — records translation gates, links
  glossary + food-term policy + help-ID readiness. Explicitly marks status as
  **`not-ready-for-translation`** until glossary, command aliases, help IDs, and food-term
  examples are reviewed. Concretely closes out the "when can translation start?" question that's
  been carried in prior journal entries' KNOWN GAPS.
- Docs-only; no application code touched, no `package.json` version bump.
- **[VERIFICATION (sandbox)]** N/A — no code changed; PR's review notes confirm the readiness
  gates and the non-translatable markings.
- **[STATUS]** PROD — merged to `main`.
- **[TEST]** N/A (docs-only).
- **[KNOWN GAPS / NEXT]** Translation still gated on: command-alias validation, food-term
  example review, screenshot/image planning — none of which land in this PR. `doc/Register/` and
  `doc/VibeCodingRecord/` catch-up passes remain due, not done in this pass. TMA help buttons
  still need to be wired to the stable `helpId`s (seed registry only).

## Confirmation Gates (PR #1488 merge)
| Gate | Status | Note |
|------|--------|------|
| G1 | ➖ | no version bump — docs-only, no code changed |
| G2 | ➖ | no destructive action |
| G3 | ✅ | serial 841→842; commit 959→960 |
| G4 | ➖ | no paid external API call |
