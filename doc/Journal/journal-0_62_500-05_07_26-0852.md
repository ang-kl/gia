(№ 854 - 05-07 '26 08:52 SGT) v0.62.500

# Journal — soleat v0.62.500 (rebase + land stale PR #1456 journal-record append) — doc housekeeping

> Operator: "I rebase #1456, resolve the journal conflict, then merge."
> Read as: PR #1456 went stale/conflicting — rebase it onto current main, keep the journal records, drop the stale serial edits, merge.

## New

### [HDR] #854 | 08:52 SGT 05-07-26 | v0.62.500 — land PR #1456 journal records onto current main | 3 files | PR #(pending) |

- **[INTENT].** PR #1456 (branch `claude/merge-pr-1454-xswn9p`, opened 01-07) recorded the merges of
  PR #1454 / #1462–1464 / #1466 / #1468 / #1469 / #1488 into
  `doc/Journal/journal-0_62_464-01_07_26-1710.md`, but had gone **DIRTY / CONFLICTING**: its 4-day-stale
  `doc/.serial-state.yml` (counters 818→822) collided with main's counters (now 971+).

- **[DELTA] (3 files).**
  - `doc/Journal/journal-0_62_464-01_07_26-1710.md` — **kept verbatim from the PR branch** (+304 lines).
    The journal file is a **clean append** onto main (main's blob `8f0dbefc` == the PR's base blob), so no
    conflict there; the added HDR records for #1454/#1462… slot into already-passed journal numbers 820–822
    per the AU append-only Recipe (historical back-fill, not a counter re-advance).
  - `doc/.serial-state.yml` — the PR's **stale serial edits were dropped**; re-based off main's live counters
    for this landing only (commit 971→972, build 610→611, journal 853→854) + re-anchor.
  - `package.json` — 0.62.499 → **0.62.500** (even docs-only PRs get a PATCH bump).

- **[VERIFICATION (sandbox)]** journal append clean (no conflict markers); no `.js` touched so `node --check`
  N/A; no web build (docs-only).

- **[STATUS]** PROD — merges to `main`.

- **[TEST]** N/A (docs-only).

- **[KNOWN GAPS]** `doc/Register/` + `doc/VibeCodingRecord/` catch-up passes referenced in the appended
  #1454/#1462 records — VCR was since caught up (v0.62.497 / PR #1500); Register still due. Standing: MRT
  CCL6 go-live flip (task #17, 12 Jul); `index.js:5739` hard-coded-SGT bot line.

## Confirmation Gates
| Gate | Status | Note |
|------|--------|------|
| G1 | ➖ | no code — docs-only; version bumped 0.62.499→0.62.500 |
| G2 | ✅ | no destructive action; stale serial edits dropped, main serial preserved |
| G3 | ✅ | serial commit 971→972, build 610→611, journal 853→854; anchor re-set 05-07 '26 08:52 SGT |
| G4 | ➖ | no user-facing surface / no paid API |
