(№ 837 - 03-07 '26 17:30 SGT) v0.62.483

# Journal — soleat v0.62.483 (Michelin cards regain Closed corner-tab + 🕛 timing row; normal follow-up slice 12→10)

> Operator: *"missing Closed strip and 🕛row"* (Michelin cards) + *"suggest cap at 10 eatery"*
> Read as: (1) restore the Closed corner-tab and clock/opening-hours row on Michelin result
> cards, and (2) trim the normal cuisine follow-up walk to 10 per tap for deadline headroom.

## New

### [HDR] #837 | 17:30 SGT 03-07-26 | v0.62.483 — Michelin open-hours fields + follow-up slice 10 | 1 code file + 3 doc/meta | PR #(pending) |
- **[INTENT].** Two operator-flagged result-list defects, both about quality under the 20s
  route deadline: Michelin cards silently dropped the **Closed** corner-tab and the **🕛
  timing row**, and the normal cuisine follow-up walk was paging 12 at a time right against
  the deadline.
- **Root cause — Michelin cards (traced).** `ResultCard.jsx` is the ONE shared card for both
  Michelin and normal venues; the Closed tab and the 🕛 row both gate on a **strict boolean
  `venue.openNow`** (plus `openClosingLabel` / `closedTodayLabel` / `closingSoonMinutes`). The
  Michelin path (`handleMichelinSearch`, `index.js` ~9605) requested only
  `regularOpeningHours.weekdayDescriptions` in its FIELD_MASK and **skips `enrichSlow`**, so
  `venue.openNow` stayed **`undefined`** → both UI elements collapsed. The normal path
  (`pipeline.js` FIELD_MASK + `cuisine-enrich.js:100-116` label pass) sets those fields, which
  is why normal cards render them fine.
- **[DELTA] (1 code file — `index.js`).**
  - **FIELD_MASK** (~9943): Michelin request gains `places.currentOpeningHours.openNow` /
    `.periods`, `places.utcOffsetMinutes`, `places.regularOpeningHours.periods`.
  - **Venue objects** (success + failure branches): carry `openNow` / `currentPeriods` /
    `regularPeriods` / `utcOffsetMinutes` (failure branch = nulls for parity).
  - **Open-hours label pass** (~10198, after the venue push loop): mirrors
    `cuisine-enrich.js:100-116` — `closedTodayString` / `currentOpenString` /
    `minutesUntilClose` from `open-hours.js` compute `closedTodayLabel` / `openClosingLabel` /
    `closingSoonMinutes`; scratch period fields deleted after. **Field names verified against
    `ResultCard.jsx` on `main`** — exact match, so no web build needed (server-side only).
  - **`FOLLOW_UP_SLICE`** (~16922): **12 → 10** for the NORMAL cuisine follow-up walk. The log
    showed `sliceCap=12` with `enrichSlow` at 16s / 12s right against the 20s deadline; 10 buys
    headroom (mirrors the v0.62.482 Michelin walk trim).
- **Scope note.** Two distinct fixes bundled into one PR for stale-base ship efficiency — both
  are "result-list quality under the 20s deadline". Offered to split if the operator prefers.
- **[VERIFICATION (sandbox)]** `node --check` clean on the edited `index.js`. No web build
  (ResultCard unchanged; the fix only supplies the fields it already reads).
- **[STATUS]** PROD — bot redeploys on merge (Railway). Verify on Telegram: a **closed**
  Michelin venue shows the Closed corner-tab + the 🕛 opening-hours row; the normal cuisine
  walk pages in 10s.
- **[TEST]** Full vitest via CI.
- **[KNOWN GAPS]** The Michelin path still skips `enrichSlow` (crowd/dishes/footfall) by
  design — only the open-hours fields are back-filled, which is all the two UI elements need.
  If a Michelin venue returns no periods from Places, the labels are simply absent (no crash).

## Confirmation Gates
| Gate | Status | Note |
|------|--------|------|
| G1 | ✅ | node --check clean (index.js) |
| G2 | ⏳ | full vitest via CI |
| G3 | ✅ | serial 836→837, commit 954→955, build 593→594; anchor re-set 17:30 SGT |
| G4 | ⏳ | redeploy + operator eyeball (Michelin closed card shows Closed tab + 🕛 row; normal walk pages 10) |
