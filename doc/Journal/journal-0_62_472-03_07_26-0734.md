(№ 826 - 03-07 '26 07:34 SGT) v0.62.472

# Journal — soleat v0.62.472 (Cuisine card: "Closed" corner-tab strip + de-duped clock row)

> Operator (3 messages, latest wins): the blue **"Closed Now · Opens…"** strip on the result
> card should **state only "Closed"**, shrunk to a small pill hugging the word, with its
> **font-size and curve matching the "… & Nearby Flavours" banner**; and the **🕛 clock row
> should drop the now-redundant "Closed now" prefix** since the strip already conveys the state.

## New

### [HDR] #826 | 07:34 SGT 03-07-26 | v0.62.472 — "Closed" corner-tab + clock-row de-dup | 3 files | PR #(pending) |
- **`web/cuisine/src/v2/components/ResultCard.jsx` — top status strip.** The v0.62.467
  three-span strip (`"Closed Now"` red · `"Opens hh:mm ~ hh:mm"` blue) is replaced by a single
  **`card.closed`** span. Restyled from a full-bleed `-mx-2.5 rounded-t-lg text-[12px]` block to a
  **corner tab**: `self-start` + auto width **hugs the word**; `-mt-2.5 -ml-2.5` bleed it flush
  into the card's **top-left** corner; `rounded-br-lg` rounds the one exposed corner; **`text-[10px]`**
  and the `lg` (0.5rem) curve **match the "… & Nearby Flavours" banner** directly below it. Red word
  retained — the WORD carries the state (CVD-safe; not colour alone).
- **`ResultCard.jsx` — 🕛 clock row.** `open` is now `let`; when the venue is closed **and reopens
  today** (`openNow === false && reopenMinutes >= 0`) and the label contains a ` · ` separator, the
  **leading "Closed now · " prefix is sliced off in-card**, leaving just the reopen info
  ("Opens today 11:30 AM"). **Locale-safe** — splits on the server-built ` · ` separator, never
  matches the English words.
- **Right altitude — `open-hours.js` untouched.** The label is server-built by
  `closedTodayString()` and shared by the card, the per-card **Copy clip**, and **bot chat
  messages** (no strip there to compensate). Editing the formatter would wipe "Closed now" from the
  bot too; the de-dup therefore lives at the card render site, guarded to exactly the strip case.
- **[VERIFICATION (sandbox)]** `esbuild --loader=jsx` parse of the edited `ResultCard.jsx` is clean
  (exit 0). No other file touched.
- **[STATUS]** PROD — Cuisine TMA rebuilds on merge; verify on the live TMA (closed venue card:
  small "Closed" corner tab, clock row reads "🕙 Opens today …" with no "Closed now").
- **[TEST]** Full vitest via CI (no ResultCard unit test; render is presentational).
- **[KNOWN GAPS]** The pink **"Closing in N min"** strip is left full-width (operator scoped only the
  Closed strip). France 1★/Bib follow-up still parked on the operator's browser-scrape go/no-go.

## Confirmation Gates
| Gate | Status | Note |
|------|--------|------|
| G1 | ✅ | esbuild jsx parse clean |
| G2 | ⏳ | full vitest via CI |
| G3 | ✅ | serial 825→826, commit 943→944, build 582→583; anchor re-set 07:34 SGT |
| G4 | ⏳ | redeploy + operator eyeball on the live Cuisine TMA |
