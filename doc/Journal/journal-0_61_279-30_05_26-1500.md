(№ 480 - 30-05 '26 15:00 SGT) v0.61.279

# Journal — soleat v0.61.279 (O-26 threshold extract + O-27 Menu __NONE__ parity + O-28 dead-route delete)

> Operator (30-05 '26 14:55 SGT, post-v0.61.278 PR #784 merge): backlog selection — *"3, 4, 5"*.
>
> Three Register items, one patch release. All three close out long-running deferrals that were waiting for the right window.
>
> 1. **O-26 — 150 km JB-distance threshold extraction.** `index.js` had the same 150 km haversine inlined in two places (set-location sanity guard + cuisine-search graceful exit). v0.61.279 hoists `JB_CBD`, `JB_FALLBACK_THRESHOLD_M`, and the `isFarFromJB(lat, lng)` predicate into `location-mode.js`; both callsites now import from there. No behaviour change — same constants, same threshold, same log lines.
>
> 2. **O-27 — Menu TMA `__NONE__` parity.** v0.61.273 added the first-paint `__NONE__` sentinel to the Cuisine TMA's `state.region` so a user in Bangkok wouldn't briefly see SG-biased results before GPS resolved (PLATFORM REFRACTORING §3 "no fallback leaks to Singapore"). The Menu TMA had two `|| 'SG'` fallbacks in the anchor setters and no equivalent loading-gate — so a Malaysia user briefly saw the train panel active and all SG-only tiles enabled before the cached-anchor fetch resolved. v0.61.279:
>    - Drops both `b.region || 'SG'` fallbacks (now `b.region || null`).
>    - Adds `anchorLoading` state (default `true`, flipped `false` in both the success and error branches of the cached-anchor fetch).
>    - New `regionUnresolved = anchorLoading || (anchor && !anchor.region)` flag gates the train panel and SG-only tiles, so they render in the same disabled state used for MY anchors until the region resolves.
>
> 3. **O-28 — `/api/cuisine/place-search-by-country` deletion.** v0.61.191's country-constrained Places search route was deprecated in v0.61.269 when both Cuisine and Menu TMAs migrated to `/api/cuisine/place-autocomplete`. The DEPRECATED log warning has been live in prod for 11 patches (Cuisine v0.61.269 → v0.61.278). 24h log-clear window passed with zero non-deprecated callers. v0.61.279 deletes the route (183 lines, including the `OTHER_COUNTRY_CODES` Set that wasn't used elsewhere). Three remaining `place-search-by-country` references in `index.js` are all comments documenting the deletion or historical context.
>
> Commit 493 / journal 480 / build 282, on branch
> `claude/v0.61.279-o26-o27-o28-backlog-batch` off `main` at v0.61.278.

## New

### [HDR] #480 | 15:00 SGT 30-05-30 | v0.61.279 — O-26 + O-27 + O-28 backlog batch | 6 files | 1 PR | 1 build |

- **[INTENT]** Close out three Register Open items in one patch. Two are refactors (O-26 + O-28 — shared-helper extraction and dead-route deletion), one is the Menu TMA equivalent of the Cuisine TMA's v0.61.273 first-paint `__NONE__` sentinel (O-27).

- **[DELTA]**

  **1. `location-mode.js`** — new shared helpers:
  ```js
  const JB_CBD = Object.freeze({ lat: 1.4927, lng: 103.7414 });
  const JB_FALLBACK_THRESHOLD_M = 150000;
  function isFarFromJB(lat, lng) {
    if (!Number.isFinite(lat) || !Number.isFinite(lng)) return false;
    const distM = haversineMeters(JB_CBD, { lat, lng });
    return Number.isFinite(distM) && distM > JB_FALLBACK_THRESHOLD_M;
  }
  module.exports = { ..., JB_CBD, JB_FALLBACK_THRESHOLD_M, isFarFromJB, ... };
  ```

  **2. `index.js:/api/cuisine/set-location`** — replace inlined haversine math with `isFarFromJB(lat, lng)` + `haversineMeters(JB_CBD, { lat, lng })` for the log line. ~12 lines → 3 lines.

  **3. `index.js:/api/cuisine/search` (graceful exit guard)** — same swap: inlined haversine → `isFarFromJB` + `haversineMeters` for the log distance.

  **4. `web/menu/src/App.jsx`** — O-27 first-paint parity:
  - New `const [anchorLoading, setAnchorLoading] = useState(true)`.
  - User-location fetch flips it to `false` in both success and error branches.
  - `region: b.region || 'SG'` → `region: b.region || null` (in both anchor setters: cached-fetch on line 139 + auto-detect on line 338).
  - New `regionUnresolved = anchorLoading || (anchor && !anchor.region)`.
  - TrainPanel wrapper: `style={(isMy || regionUnresolved) ? { opacity: 0.4, pointerEvents: 'none' } : {}}`.
  - Tile disable: `const disabled = (isMy || regionUnresolved) && SG_ONLY_TILES.has(tile.id)`.

  **5. `index.js`** — O-28: deleted the `/api/cuisine/place-search-by-country` route body (183 lines, including the `OTHER_COUNTRY_CODES` Set that was only used inside this route). Replaced with a v0.61.279 comment block documenting the deletion + pointing to v0.61.191 / v0.61.207 / v0.61.269 journals for historical detail.

  **6. `package.json`** — `0.61.278 → 0.61.279`.

  **7. `doc/.serial-state.yml`** — commit 492→493, build 281→282, journal 479→480; re-anchored 30-05 '26 15:00 SGT.

- **[VERIFICATION (sandbox)]**

  - `node --check index.js` — clean.
  - `node --check location-mode.js` — clean.
  - `esbuild.transform` (loader: jsx) of `web/menu/src/App.jsx` — clean.
  - Vitest still BLOCKED locally by the recurring iCloud `ERR_INVALID_PACKAGE_CONFIG` throttle (Register O-22). CI will run authoritatively. Expected: no test changes; jb-focus-points and smart-place-label both green (restored in v0.61.278).

- **[STATUS]** PR-OPEN on push. Railway redeploys server + Menu TMA on merge.

- **[TEST]** Operator (post-merge):
  1. **O-26 — JB→OTHER fallback still fires.** Reproduce the v0.61.276 scenario: open Cuisine TMA from KL with JB pill sticky from a prior session, run a durian search. Railway logs should still show `[Cuisine-Search] D703b JB-fallback: filter wiped N→0 at coords Xm from JB CBD. Treating request as OTHER.` and the v0.61.278 amber banner should still render in the TMA. Behaviour identical; only the underlying code path is shared now.
  2. **O-26 — set-location sanity guard.** `curl /api/cuisine/set-location` with `{ region: 'JB', lat: 3.14, lng: 101.69, initData: '…' }` (Putrajaya). Server logs `[set-location] sanity guard: region=JB requested at coords 3.14,101.69 (Nkm from JB CBD) — downgrading to OTHER`. Same log line as v0.61.276–278; only the haversine is shared now.
  3. **O-27 — Menu TMA first-paint.** Open Menu TMA from outside SG (KL / Putrajaya, fresh launch with no cached anchor). Before the cached-anchor fetch resolves (~150 ms), the train panel and SG-only tiles should render in their disabled state instead of briefly flashing as active. Once the fetch resolves to a Malaysia anchor, they stay disabled (no change). Once it resolves to an SG anchor (or the user is in SG), they activate.
  4. **O-28 — deleted route returns 404.** `curl /api/cuisine/place-search-by-country -X POST -d '{"input":"test","countryCode":"MY"}' -H 'Content-Type: application/json'`. Server returns 404 (Express default). No frontend caller exists (verified via grep across `web/cuisine/src` + `web/menu/src`).

- **[KNOWN GAPS]**
  - **O-25 toast** — done in v0.61.278. Doesn't apply here.
  - **Menu TMA SG-mode autocomplete migration (DEF-2)** — still pending; folds with O-27 but operator deferred. SG mode in `LocationFieldMenu.jsx:574-601` still uses v0.61.124 inline-fetch autocomplete instead of the v0.61.269 `/place-autocomplete` round-trip. Not a regression; the v0.61.279 Menu changes don't touch this code path.
  - **Removed Features table update** — `doc/Feature/cuisines_js.MD` should gain an entry for the v0.61.279 deletion of `/api/cuisine/place-search-by-country` per AU-2. Logged in [KNOWN GAPS]; not done in this PR to avoid scope creep.

- **Confirmation Gates**

  | Gate | Resolution | Date |
  |---|---|---|
  | G1 — version bump | PATCH `0.61.278 → 0.61.279`. O-26 + O-28 are pure refactor/dead-code-delete (no behaviour change). O-27 is a UX correction (no SG bias before location resolves) — could justify MINOR, but the change is invisible to SG users and only suppresses a brief flash for MY users. Keeping PATCH per operator's prior precedent (v0.61.273 was also PATCH). | 30-05 '26 |
  | G2 — destructive action | YES (mild) — O-28 deletes 183 lines of route code. Mitigation: route has carried the DEPRECATED warning since v0.61.269 (11 patches); zero non-deprecated callers in 24h logs; frontend grep across all TMAs returns no hits. Recoverable from git history if needed. | 30-05 '26 |
  | G3 — new decision rule | _n/a_. Codifies existing v0.61.276 (O-26) + v0.61.273 (O-27) decisions. | 30-05 '26 |
  | G4 — paid external API call | _n/a_. | 30-05 '26 |

## Amendments

- **`location-mode.js`** — three new exports: `JB_CBD`, `JB_FALLBACK_THRESHOLD_M`, `isFarFromJB`.
- **`index.js:/api/cuisine/set-location`** — JB-distance check now uses `isFarFromJB`; ~12 lines of inline haversine math removed.
- **`index.js:/api/cuisine/search`** — JB graceful-exit guard now uses `isFarFromJB`; same simplification.
- **`index.js`** — deleted `/api/cuisine/place-search-by-country` route (183 lines + the `OTHER_COUNTRY_CODES` Set); replaced with a deletion-record comment block.
- **`web/menu/src/App.jsx`** — new `anchorLoading` state + `regionUnresolved` flag; `|| 'SG'` fallbacks dropped; train panel + SG-only tiles gated on `(isMy || regionUnresolved)`.

## Deleted

### `/api/cuisine/place-search-by-country` (POST)

Was v0.61.191's country-constrained Places search route. v0.61.201 added parallel Geocoding fallback. v0.61.207 plugged in `smartPlaceLabel` for the "1, Jln Imbi …" displayName problem. v0.61.222 added the `primaryText: null` fallback. v0.61.269 marked the route DEPRECATED when both TMAs migrated to `/api/cuisine/place-autocomplete`. Deleted in v0.61.279 after 11 patches of zero non-deprecated callers in prod logs.

Full route body (183 lines) preserved in git history at commit `814da317e79317c2087d0f802ff9a5c56192a8fb` (v0.61.277).
