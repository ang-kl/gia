# Journal — v0.62.603 (Hawker + Train headers standardised on the Cuisine TMA)

_(№ 947 — 19-07 '26 08:45 SGT)_

## New

### [HDR] v0.62.603 — Hawker + Train TMA: header standardised on Cuisine (language · temp · refresh flush right) + Train date/time-then-status row

- **[INTENT]** Operator (IMG_3582 Train, IMG_3583 Cuisine, IMG_3584 Hawker): *"hawker TMA and train TMA
  standardised the header using the Cuisine TMA as the standard, for flush right for: language selector
  / temperature / refresh."* Plus a **Train-only** addition: *"below the header … place the `<date &
  time>` then `<status>`."*

- **[DELTA — Train]** `web/transport/src/App.jsx` — `headerEl` restructured into **three rows**:
  - **Row 1**: the title (left) + a `justify-between` **flush-right cluster** `[LocaleToggle ·
    WeatherBadge · ↻ refresh]` (the Cuisine order).
  - **Row 2**: the live SGT clock (**date & time**) `·` the tappable **status** (opens the service /
    engineering popup) — was previously crammed onto row 1's right side.
  - **Row 3**: the line pills (unchanged).

- **[DELTA — Hawker]** `web/hawker/src/App.jsx` — **both** headers reworked so the right cluster is
  `[LocaleToggle · WeatherBadge · ↻ refresh · NEA]`:
  - **Portrait two-panel header**: the weather + refresh moved **off the left** (they were under the
    title) into the flush-right trio; the title now takes the flexible left column.
  - **Landscape / carousel header**: reordered to the Cuisine trio and **gained a ↻ refresh** button it
    previously lacked. NEA trails the trio in both.

- **[VERIFICATION (sandbox)]** **Playwright** (stubbed) confirmed:
  - Train header — title left; 🇬🇧 / weather / ↻ flush right; **row 2 "19 Jul 08 42 · ⚠️ 1 line
    affected"**; row 3 pills.
  - Both Hawker headers — title left; 🇬🇧 / weather / ↻ / NEA flush right.
  - No page errors (the weather emoji is blank only because the harness has no weather feed; it renders
    "☁️ 28.9°C" live). Transport + Hawker `vite build` green (`transport index-CwI0JudL.js 268.27 kB │
    gzip 87.45`; `hawker index-DZK2mp5N.js 256.69 kB │ gzip 83.99`); full `npm test` **3738/3738 across
    147 files**; `package.json` + `package-lock.json` 0.62.602 → 0.62.603; `.serial-state.yml` commit
    1082→1083, build 704→705, journal 946→947, re-anchor **19-07 '26 08:45 SGT**.

- **[TIME ANCHOR]** data.gov.sg blocked by the environment network policy (`403` `CONNECT`); anchored
  to the agent-proxy status timestamp `2026-07-19T00:45Z` = **19-07 '26 08:45 SGT** (TF-9 fallback;
  TF-10 lower bound).

- **[STATUS]** DRAFT → squash-merge to `main` on green CI (auto-merge SQUASH). New PR.

- **[TEST]** No new unit test (a header-layout change; verified visually via Playwright). Existing suite
  unchanged and green.

- **[KNOWN GAPS]** Hawker keeps **NEA** trailing the standardised trio (Cuisine has no NEA). The
  Cuisine TMA itself is the reference and was **not** modified. Carried: the Transport full-map toggle
  (carousel in portrait) + the unified card↔pin flash; the card's `mrt.*` + closure strings are
  en/fr-lean; Cuisine `tg.js` safe-area gap.

## Confirmation Gates
| Gate | Status | Note |
|------|--------|------|
| G1 | ✅ | PATCH bump 0.62.602 → 0.62.603; both TMA builds + 3738 tests green; headers verified via Playwright. |
| G2 | ➖ | no destructive removal — header elements were relocated/reordered, none removed (Train's status moved to row 2, not deleted). |
| G3 | ➖ | no new decision rule (executes the operator's standardisation instruction). |
| G4 | ➖ | no external paid API call. |
