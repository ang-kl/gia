(№ 260 - 20-05 '26 00:30 SGT) v0.61.49

# Journal — soleat v0.61.48 → v0.61.49 (attraction pin · exit-door removal · Michelin ✴️)

> Operator: *"Attractions pin inside google map be 📌 with off-white background"*;
> *"Remove the door that represent Exit"*; *"The Michelin pin is currently white,
> change to this emoji ✴️"*.
>
> Sequenced after PR #536 — this entry is commit 260 / journal 253 / build 60.

## New

### [HDR] #253 | 00:30:00 SGT 20-05-26 | v0.61.49 — attraction 📌 pin · exit-door removal · Michelin ✴️ | ~1 kb | 7 files | 1 PR | 1 build |

- **[INTENT]** Three operator-requested map-pin tweaks across the TMAs.

- **[DELTA]**
  - **`mapOverlays.js`** (byte-identical ×3) —
    - the attractions overlay pin is now `📌` on an **off-white** dot (`#f4f3ef`),
      was `🎡` on orange `#FF8F00`;
    - the nearest-station **exits** line in the popup drops its `🚪` door glyph
      (operator: remove the door).
  - **`MrtMapPanel.jsx` / `HawkerMapPanel.jsx`** — the station-popup exits line drops
    the `🚪` door glyph too (Transport keeps the translated `Exits:` text label).
  - **`MapPanel.jsx`** (Cuisine) — the Michelin pin glyph is `✴️` (was `✳️`) and now
    renders in its **native colour** — a gold star — instead of the white silhouette;
    the pet `🐾` / dessert `🍮` glyphs keep their white silhouette.
  - **`package.json`** — `0.61.48` → `0.61.49` (PATCH).

- **[VERIFICATION (sandbox)]**

  ```bash
  $ npm run build:cuisine / build:hawker / build:transport  → all ✓ built
  $ npm test  → 65 files / 1928 tests green
  ```
  `mapOverlays.js` confirmed byte-identical ×3.

- **[STATUS]** DRAFT → PR (base `main`, branch `claude/review-gia4lunch-readme-3s798`).
  Deploys via the Railway auto-deploy on `main` once merged.

- **[TEST]** Attraction overlay pins show 📌 on an off-white dot. Station-context
  popups list exit codes with no door glyph. Cuisine Michelin venue pins show a gold
  ✴️ star.

- **[KNOWN GAPS]** The attraction-popup readability the operator also raised is
  already addressed by v0.61.47 (the fixed white popup palette, PR #536). Drawing a
  real attraction *boundary polygon* is **not possible from the Google Places API** —
  Places returns a point + a rectangular `viewport`, not an outline; a true boundary
  would need a polygon dataset (as the parks layer has). The 24 hours layer remains
  disabled; the `doc/Legal/` revision (journal #239) remains.

- **Confirmation Gates**

  | Gate | Resolution | Date |
  |------|-----------|------|
  | G1 — version bump | PATCH `0.61.48` → `0.61.49`, established 0.61.x cadence | 20-05 '26 |
  | G2 — destructive action | _n/a_ | — |
  | G3 — new decision rule | _n/a_ | — |
  | G4 — paid external API call | _n/a_ | — |

## Amendments

_None._

## Deleted

_None._
