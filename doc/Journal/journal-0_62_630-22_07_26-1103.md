# Journal — v0.62.630 (Train: cards survive the map's ⇲ Expand — carousel lifted above the fullscreen overlay)

_(№ 1,113 — 22-07 '26 11:03 SGT)_

## New

### [HDR] v0.62.630 — keep the station carousel visible when the map is expanded

- **[INTENT]** Operator (Train TMA, desktop; v0.62.629 confirmed the map's toggle row + nav cluster are
  now visible — good): *"i expand the screen, the cards are gone (in train TMA)."*

- **[ROOT CAUSE]** The station carousel lived **inside** the map's `flex-1` container at `z-30`. The map's
  **⇲ Expand** button (v0.62.626) promotes the panel to a `fixed inset-0 z-[35]` overlay, which painted
  **over** the `z-30` carousel — so the cards vanished the moment the map was expanded.

- **[DELTA]**
  - `web/transport/src/App.jsx` (carousel return) — moved the carousel **out** of the map container to be a
    **fixed sibling**: `fixed inset-x-0 bottom-14 z-[38] pointer-events-none`. `z-[38]` sits **above** the
    expand overlay (`z-[35]`) and **below** the footer (`z-40`), so the cards float over the map's bottom
    edge in **both** the normal and expanded states and stay visible. Header stays in flow on top
    (v0.62.629 — toggle row + nav cluster visible); `items-end` natural card heights kept.
  - `web/transport/src/components/MrtMapPanel.jsx` — the **Overview / Back** control `z-[36]` → **`z-[39]`**
    so it stays above the now-`z-[38]` carousel and clickable in the normal (unexpanded) state (Codex P2
    fix preserved).

- **[VERIFICATION (sandbox)]** `transport` `vite build` green (`z-index:38` + `z-index:39` in the built
  CSS); full `npm test` **3756/3756 across 148 files**. **Playwright** (desktop 1280×800) confirmed the
  carousel is now `fixed bottom-14 z-[38]`, `itemsEnd = true` / `itemsStretch = false`, cards render, zero
  page errors. **CANNOT** render the Google Map tiles / the ⇲ expand overlay over live tiles in-sandbox (no
  Maps key) — operator device-check that the cards persist when the map is expanded. `package.json` +
  lockfile 0.62.629 → 0.62.630; `.serial-state.yml` commit 1112→1113, build 731→732, journal 973→974,
  re-anchor **22-07 '26 11:03 SGT**.

- **[TIME ANCHOR]** data.gov.sg blocked by the environment network policy (`403` `CONNECT`); anchored to
  the container system clock `2026-07-22T03:03Z` = **22-07 '26 11:03 SGT** (TF-9 terminal-runtime source).

- **[STATUS]** DRAFT → squash-merge to `main` on green CI (auto-merge SQUASH). New PR (branch restarted
  from `origin/main` `137c2b5` = `#1638` merged).

- **[TEST]** No new unit test — a z-index / DOM-placement change; verified via build + the Playwright
  computed-layout probe.

- **[KNOWN GAPS / caveats]** When the map is **expanded** (fullscreen overlay), the Overview button (inside
  the `z-[35]` panel's stacking context) can sit under a card — a minor edge; the priority was keeping the
  **cards** visible. Needs an operator device-check over live tiles. **Serial protocol:** operator flagged
  that chat **replies** must also carry the `(№ …)` serial line (`doc/CLAUDE.md` §5) — now prefixing every
  reply, continuing the running thousands-counter.

## Confirmation Gates
| Gate | Status | Note |
|------|--------|------|
| G1 | ✅ | PATCH bump 0.62.629 → 0.62.630; transport build + 3756 tests green; Playwright confirmed the fixed z-[38] carousel. |
| G2 | ➖ | additive/relocation — carousel moved to a fixed sibling + two z-index bumps; nothing removed. |
| G3 | ➖ | no new decision rule. |
| G4 | ➖ | no external paid API call. |
