(№ 811 - 01-07 '26 13:15 SGT) v0.62.457

# Journal — soleat v0.62.457 (Cuisine — localise the "Dishes" label)

> Operator: "Translated?"

## New

### [HDR] #811 | 13:15 SGT 01-07-26 | v0.62.457 — cat.dishes 8-locale | 3 files | PR #(pending) |
- **`web/cuisine/src/v2/lib/i18n.js`** — the `cat.dishes` label shipped in v0.62.453 with only
  **en/fr** (others fell back to English). Now localised across **all 8 supported locales**:
  base **en** "Dishes" / **fr** "Plats"; per-map **id** "Hidangan", **ru** "Блюда", **de**
  "Gerichte", **zh** "菜品", **ja** "料理", **es** "Platos" (`cat.*` labels live in the per-locale
  `ID/RU/DE/ZH/JA/ES_STRINGS` maps, not inline in the base entry).
- **Scope note:** the dish **names** in the pop-up stay **English + native `local` script** — the
  same way the Cuisine TMA already renders dishes. Device-language translation of dish names is the
  separate long-tail corpus program, not this PR.
- **[VERIFICATION (sandbox)]** `node --check` i18n.js (7 `cat.dishes` entries = base + 6 maps);
  cuisine build + vitest via CI.
- **[STATUS]** PROD — cuisine TMA rebuild on merge.

## Confirmation Gates
| Gate | Status | Note |
|------|--------|------|
| G1 | ✅ | node --check; CI builds cuisine |
| G2 | ⏳ | full vitest via CI |
| G3 | ✅ | serial 810→811 |
| G4 | ⏳ | redeploy + operator eyeball |
