(№ 768 - 28-06 '26 15:25 SGT) v0.62.414

# Journal — soleat v0.62.414 (dish-card action labels: 🧆 picture / close)

> Operator: "the [ picture ] should be 🧆 picture; [ close ] should be just: close. did you wire
> the picture?" — yes, wired since v0.62.407/411 (openDishPicture ×4 → Commons File page).

## New

### [HDR] #768 | 15:25 SGT 28-06-26 | v0.62.414 — dish-card action labels | 1 file | PR #(pending) |
- **[INTENT]** Relabel the dish-card actions: bracketed `[ picture ]`/`[ close ]` → `🧆 picture`
  (fr `🧆 photo`) and plain `close` (fr `fermer`), brackets removed.
- **[DELTA]** `web/cuisine/src/v2/components/ArrivalPlate.jsx` — replaced both label strings across
  all four dish-card render sites. `[ picture ]` was already wired to `openDishPicture(d.dish)`
  (opens the dish's Wikimedia Commons File: page via `tg.openLink`); this PR is label-only.
  `package.json` 0.62.413 → 0.62.414.
- **[VERIFICATION (sandbox)]** `esbuild` parse OK; 4× `🧆 picture`/`🧆 photo`, 4× `close`/`fermer`.
- **[STATUS]** PROD — cuisine TMA rebuild; redeploys per PR.
- **[TEST]** None (label-only).
- **[KNOWN GAPS]** Labels en/fr only (other locales fall back to these strings — picture/close are
  not yet in the i18n overlays). Community sub-grouping still SG-only (MY/TH/PH/ID follow-up).

## Confirmation Gates

| Gate | Status | Note |
|------|--------|------|
| G1 build/parse | ✅ | esbuild OK |
| G2 tests | ⏳ | CI |
| G3 journal/serial | ✅ | serial 767→768 |
| G4 live deploy | ⏳ | redeploy |
