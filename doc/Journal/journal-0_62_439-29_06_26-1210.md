(№ 793 - 29-06 '26 12:10 SGT) v0.62.439

# Journal — soleat v0.62.439 (fix: /clipboard showed nothing; button even when empty)

> Operator: "/clipboard — nothing appears"; and "even if there aren't clips, show the Open
> Sketchbook button".

## New

### [HDR] #793 | 12:10 SGT 29-06-26 | v0.62.439 — /clipboard send fix | 1 file | PR #(pending) |
- **[ROOT CAUSE]** v0.62.438 left `keyboardRows` EMPTY when `webhookDomain` was falsy, so the
  message went out with `reply_markup: { inline_keyboard: [] }` — Telegram rejects an empty
  inline keyboard and the whole `/clipboard` message failed to send (silent).
- **[DELTA]** `index.js` `runClipCommand` — build the **Open Sketchbook** button once
  (`sketchbookRows`); attach `reply_markup` **only when non-empty** (`replyMarkupOpt`). The
  empty-clips path now also sends via `bot.sendMessage` with that button (operator: show it even
  with no clips) instead of plain `safeSend`.
- **[VERIFICATION (sandbox)]** `node --check` index.js OK.
- **[STATUS]** PROD — bot redeploy per PR.

## Confirmation Gates
| Gate | Status | Note |
|------|--------|------|
| G1 | ✅ | node --check |
| G2 | ⏳ | CI |
| G3 | ✅ | serial 792→793 |
| G4 | ⏳ | redeploy; test /clipboard |
