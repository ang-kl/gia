# Journal — v0.62.633 (Train: keep the per-line "Station info" link visible without timings — Codex P2 on #1642)

_(№ 1,146 — 23-07 '26 06:41 SGT)_

## New

### [HDR] v0.62.633 — un-gate the per-line more_info_url link from the timings fold

- **[INTENT]** Codex P2 review on #1642 (`web/transport/src/components/StationCard.jsx:152`): *"Keep station
  info links reachable without timings"* — a valid regression I introduced in v0.62.632.

- **[ROOT CAUSE]** The v0.62.632 `LineSubCard` rewrite made each line's first/last-train detail fold behind
  its own ▾, and moved the `more_info_url` "Station info" link to render only when `open`. But a line with a
  `more_info_url` and **no** `first_last_train` rows (the CGL / SLRT / PLRT entries in `data/stations.json`)
  has `hasDetail === false`, so the header toggle never flips `open` true → the Station info link (which used
  to render unconditionally) became **permanently hidden** on those lines.

- **[DELTA]** (1 file) `web/transport/src/components/StationCard.jsx` — render
  `{line.more_info_url && (<a …>)}` **independent** of `open` (dropped the `&& open`). The link is always
  visible when present; the first/last-train **detail** still folds behind the line's own ▾. Detail-less lines
  (no timings) simply show the header + the info link.

- **[VERIFICATION (sandbox)]** `transport` `vite build` green; FULL `npm test` **3756/3756 across 148 files**.
  `package.json` + lockfile 0.62.632 → 0.62.633; `.serial-state.yml` commit 1115→1116, build 734→735, journal
  976→977, re-anchor **23-07 '26 06:41 SGT**.

- **[TIME ANCHOR]** data.gov.sg blocked by the environment network policy (`403` `CONNECT`); anchored to the
  container system clock `2026-07-22T22:41Z` = **23-07 '26 06:41 SGT** (TF-9).

- **[STATUS]** DRAFT → squash-merge to `main` on green CI (auto-merge SQUASH). New PR — branch restarted from
  `origin/main` `b18c040` (#1642 merged).

- **[TEST]** No new unit test — a one-line render-condition fix; verified via build + the full suite. (The
  live-map render can't be exercised in-sandbox.)

- **[KNOWN GAPS / caveats]** The rest of the v0.62.632 iPad-mini redesign (4/6-per-row density, uniform tile
  height, per-line + Around triangles, select pop/centre/zoom) still awaits the operator's live iPad-mini
  device-check. Pre-existing Hume (DT4) `mrt-coords.json` gap carried forward.

## Confirmation Gates
| Gate | Status | Note |
|------|--------|------|
| G1 | ✅ | PATCH bump 0.62.632 → 0.62.633; transport build + 3756 tests green. |
| G2 | ➖ | one render-condition relaxed (link no longer gated on `open`); nothing removed. |
| G3 | ➖ | no new decision rule. |
| G4 | ➖ | no external paid API call. |
