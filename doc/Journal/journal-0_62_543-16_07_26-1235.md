(№ 899 - 16-07 '26 12:35 SGT) v0.62.543

# Journal — soleat v0.62.543 (Big ring 3: label on 4 sides + km/miles unit-awareness)

> Operator, from IMG_3465 (Brisbane, the 18.6 km ring 3 auto-framed at z10): **(1)** *"if 3rd ring
> outside SG sits at farthest result (uncapped) if above 8km has to indicate at four sides rather than
> just the north the #.#km"*; **(2)** *"Mindful that some countries use miles or different measurement
> units than km."*

## New

### [HDR] #899 | 12:35 SGT 16-07-26 | v0.62.543 — 4-side big-ring labels + miles unit | 4 files | PR #1551 (stacked) |

- **[DELTA].**
  - **`web/_shared/lib/distance-rings.js`**
    - `drawRing` places the distance pill on **all four sides (N/E/S/W)** when `radiusM > LARGE_RING_M`
      (8000 m), else just north. So only the big non-SG ring 3 gets four labels; the 750 m / 2 km /
      SG-capped rings keep the single north pill.
    - **`formatDist(m, unit)`** is now unit-aware: `unit:'mi'` → `###ft` below 0.1 mi (rounded 10 ft),
      else `#.#mi`; `'km'` (default) unchanged (existing km tests intact).
    - `draw(centre, results, isSG, unit)` threads the unit into every label; the non-SG ring-2 label is
      `2km` (km) or `1.2mi` (mi); the SG reach label is rebuilt as `formatDist(radiusM, unit) + " (~N stops)"`.
  - **`web/cuisine/src/v2/components/MapPanel.jsx`** — NEW `distanceUnit(region, countryPref)`: SG → km;
    else **miles** iff `countryPref ∈ {US, GB, MM, LR}` (road-distance imperial countries), else km
    (AU → km, matching the screenshot). Added the `countryPref` prop + pass `distanceUnit(...)` as the
    4th arg to `draw()`. **Unit follows the searched country (`state.countryPref`), not the device locale.**
  - **`web/cuisine/src/v2/App.jsx`** — pass `countryPref={state.countryPref}` to `<MapPanel>`.
  - **`__tests__/distance-rings.test.js`** — +1 test (`formatDist` miles).
  - `package.json` 0.62.542 → **0.62.543**; Journal #899 + serial-state (commit 1016→**1017**, build
    642→**643**, journal 898→**899**; re-anchor 16-07 '26 12:35 SGT, prior 898 demoted verbatim AU-7).

- **[VERIFICATION (sandbox)].**
  - `node --check web/_shared/lib/distance-rings.js` OK.
  - Ring tests **20/20** (was 19 → +1); **FULL `npm test` → 3719/3719 across 145 files** (was 3718).
  - `web/` changed (shared helper + cuisine App/MapPanel) → **cuisine + hawker vite builds BOTH green**.

- **[STATUS]** STAGED → PROD on merge. **Stacked** on branch `claude/handover-july-11-49uzvf` @ 4936df6
  → 2nd commit on **PR #1551** (still open). Merging #1551 lands v0.62.542 + v0.62.543.

- **[KNOWN GAPS].**
  1. 🌍 **Imperial-country set** `{US, GB, MM, LR}` — may need tuning (confirm).
  2. 🎨 **Pin-colour precedence** grey > pink > red > green — confirm (Michelin red may want to win).
  3. 🔭 Non-SG ring-3 auto-frame can zoom **out** far for a distant outlier.
  4. 🃏 **ResultDrawer left-peek/overlap** — separate layout follow-up.
  5. 🧩 **Consolidation PR-5 / PR-6**, PR-3 (gemini), country-pref redis-kv — deferred.
  6. 🚪 **CCL6 + Punggol Coast exit pins** — need the LTA exit file.

## Confirmation Gates
| Gate | Status | Note |
|------|--------|------|
| G1 | ✅ | PATCH bump 0.62.542→0.62.543; additive front-end change, full suite + both TMA builds green |
| G2 | ➖ | no destructive action |
| G3 | ➖ | no new decision rule (feature choices captured from the operator's message) |
| G4 | ➖ | no external paid API call |
