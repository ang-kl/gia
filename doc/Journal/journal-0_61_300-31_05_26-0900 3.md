(№ 501 - 31-05 '26 09:00 SGT) v0.61.300

# Journal — soleat v0.61.300 (Slavic/EE seed-tuning — native-language generic phrases)

> Operator (31-05 '26 08:55 SGT, post v0.61.299 + variance analysis): *"ship v0.61.300 seed-tuning"*.
>
> Key finding from the v0.61.297 variance JSON analysis: native-language generic phrases like Hungarian's `magyar étterem` (80 hits aggregated across 8 cities) and Bulgarian's `българска кухня` (80 hits) **dominated** their respective per-seed productivity tables — **2–5× any specific dish name in the same cuisine**. Hypothesis: restaurants brand as "<cuisine> restaurant" more than they list specific dishes in their names. v0.61.300 applies the same form to Russian, Polish, Ukrainian, and Czech.
>
> Also drops `holubtsi` from Ukrainian (only 4 hits across 8 cities — dead weight).
>
> Romanian was already tuned in v0.61.298; Hungarian + Bulgarian already had the generic phrases. Net: 4 cuisines updated, 5 new seeds added, 1 weak seed dropped.
>
> Commit 514 / journal 501 / build 303, on branch
> `claude/v0.61.300-seed-tuning` off `main` at v0.61.299.

## New

### [HDR] #501 | 09:00 SGT 31-05-31 | v0.61.300 — Slavic/EE seed-tuning | 3 files | 1 PR | 1 build |

- **[INTENT]** Improve Slavic/EE variance coverage based on the v0.61.297 data. The native-language generic phrase pattern (`magyar étterem`, `българска кухня`) outperformed dish-name seeds by 2-5×; adding it to the 4 cuisines that lacked it should lift their totals significantly.

- **[DELTA]**

  **1. `scripts/slavic-ee-variance.js` SEEDS block** — 4 cuisine changes:

  | Cuisine | Before | After | Change |
  |---|---|---|---|
  | russian | 5 seeds | 6 seeds | + `русский ресторан` |
  | polish | 4 seeds | 5 seeds | + `restauracja polska` |
  | ukrainian | 4 seeds | 4 seeds | − `holubtsi` (4 hits), + `український ресторан` |
  | czech | 4 seeds | 5 seeds | + `restaurace česká` |
  | hungarian | (unchanged — already has `magyar étterem`) | | |
  | bulgarian | (unchanged — already has `българска кухня`) | | |
  | romanian | (unchanged — tuned in v0.61.298) | | |

  Plus an inline comment block (~10 lines) explaining the v0.61.297 productivity analysis + the v0.61.300 rationale.

  **2. `package.json`** — `0.61.299 → 0.61.300`.

  **3. `doc/.serial-state.yml`** — commit 513→514, build 302→303, journal 500→501; re-anchored 31-05 '26 09:00 SGT.

- **[VERIFICATION (sandbox)]**

  - `node --check scripts/slavic-ee-variance.js` — clean.
  - No re-run in this PR; estimated cost of a full re-run: ~$1.40 for 7 cuisines × 8 cities × ~5 seeds × $0.005.
  - Vitest still BLOCKED locally by recurring iCloud `ERR_INVALID_PACKAGE_CONFIG` (Register O-22 + O-37). CI authoritative.

- **[STATUS]** PR-OPEN on push. Operator-only script; no prod runtime change. The v0.61.299 Gemini cache mode list is unchanged — these 4 cuisines are already in `GEMINI_CACHE_KEYS`, so any false positives picked up by these new seeds get dropped at search time.

- **[TEST]** Operator (post-merge, optional re-run):
  1. **Re-run the variance.** `GOOGLE_MAPS_API_KEY=<key> node scripts/slavic-ee-variance.js`. Expected uplift on cuisine totals:
     - **russian**: 346 → ~420+ (adding the generic phrase)
     - **polish**: 215 → ~290+
     - **ukrainian**: 133 → ~210+ (the generic should be huge; offset by losing `holubtsi`'s 4)
     - **czech**: 90 → ~170+ (biggest uplift — Czech generic phrase should perform like Hungarian's did)
  2. **Stronger seeds dominate.** Look at the seed-productivity table per cuisine. The new generic phrases should be #1 or #2 by hit count.
  3. **No regression in other cuisines.** Hungarian / Bulgarian / Romanian sections unchanged.

- **[KNOWN GAPS]**
  - **No re-run cost estimate is built into the PR.** Operator triggers + pays.
  - **Holubtsi removal is a judgement call.** It's a dish that exists in Ukrainian cuisine — but the variance shows it doesn't match venue names. A future seed pass could try the Cyrillic spelling `голубці` if the operator wants to recover it.
  - **The variance script remains operator-only.** No CI / Railway integration.

- **Confirmation Gates**

  | Gate | Resolution | Date |
  |---|---|---|
  | G1 — version bump | PATCH `0.61.299 → 0.61.300`. Seed-config tuning in an operator-only script. Milestone version number coincidence. | 31-05 '26 |
  | G2 — destructive action | One seed removed (`holubtsi`), 4 added — minor net change. The removed seed is preserved in git history. | 31-05 '26 |
  | G3 — new decision rule | YES (refinement) — *"for Slavic/EE seed sets in variance scripts, include a native-language generic phrase ('<adjective> restaurant' / '<cuisine> kitchen') as the primary seed. Dish names anchor the long tail; the generic phrase carries the head."* | 31-05 '26 |
  | G4 — paid external API call | The next operator-triggered run costs ~$1.40 for the full sweep. | 31-05 '26 |

## Amendments

- **`scripts/slavic-ee-variance.js`** — 4 cuisine SEED blocks updated (russian / polish / ukrainian / czech). 5 seeds added across the 4. 1 seed removed (`holubtsi` from Ukrainian). Inline comment block added explaining the v0.61.300 tuning rationale.

## Deleted

- **`holubtsi`** as a Ukrainian seed in `scripts/slavic-ee-variance.js`. Produced only 4 hits aggregated across 8 cities. The Cyrillic spelling `голубці` was not added; if Ukrainian coverage stays thin after the v0.61.300 re-run, that's the next candidate.
