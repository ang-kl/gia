(№ 448 - 29-05 '26 12:25 SGT) v0.61.247

# Journal — soleat v0.61.247 (Revert v0.61.241 location-suffix bubble + mt-7 gap)

> Operator (29-05 '26 morning, mid-batch with the open-hours
> investigation): *"The Cusine TMA bubble about the Location field
> isn't permanent there, disappear after 5 second, don't have
> large UI spacing gaps between SG/JB/Others button inside the
> Cuisine TMA. revert to earlier without the bubble about the
> location."*
>
> Commit 460 / journal 448 / build 250, on branch
> `claude/v0.61.247-revert-loc-bubble` off `main` at v0.61.246.

## New

### [HDR] #448 | 12:25 SGT 29-05-29 | v0.61.247 — Revert v0.61.241 bubble + mt-7 | 4 files | 1 PR | 1 build |

- **[INTENT]** Partial revert of v0.61.241. The location-suffix
  speech bubble (the small accent-coloured pill floating ABOVE the
  LocationField saying *"5 places nearby · tap to change"*) is gone;
  the suffix + "tap to change" hint go back INLINE inside the
  pill button, where they lived pre-v0.61.241. The 28-px `mt-7`
  wrapper that v0.61.241 added to make room for the bubble's
  overhang is also removed — region pill row (SG / JB / 🌏
  Others) now sits tight against the LocationField again.

  Operator's "disappear after 5 second" observation likely refers
  to the v0.61.174 🔍 FAB hint bubble that pulses for 5 s on every
  load — easy to confuse with the v0.61.241 location bubble that
  shares the bg-tg-accent rounded-2xl + rotated-square-tail
  silhouette. Either way, removing the location bubble closes the
  ambiguity.

  v0.61.244 *"Tap 🔍 to search"* 6-second idle reminder bubble is
  **kept** — it's a different bubble (only appears after 6 s of
  typing pause, dismisses on next keystroke / 🔍 tap) and the
  operator hasn't asked to revert it. Confirmed via AskUserQuestion
  before committing this revert.

- **[DELTA]**

  **1. `web/cuisine/src/v2/components/LocationField.jsx`** —
     SG/JB branch (`if (region !== 'OTHER')` return):
     - Removed the `{!open && suffix && <div … bg-tg-accent
       rounded-2xl …>}` bubble block (was lines 229-245 in v0.61.246).
     - Restored the inline button-content layout from pre-v0.61.241:
       `flex items-baseline gap-1.5` on the resting button, with the
       location label, the `· {suffix}` span, and the italic
       *"tap to change"* hint span all sitting next to each other.
     - Inline comment notes the revert + the v0.61.244 idle bubble
       remains.

  **2. `web/cuisine/src/v2/components/LocationField.jsx`** —
     OtherLocationPicker compact pill (`showCompact` branch):
     - Same revert. Outer `<div className="relative">` wrapper
       removed; absolute bubble block removed. Compact pill is
       back to a single-row layout `flex items-center gap-2` with
       inline suffix + "tap to change" + ✏️ pencil + pin.

  **3. `web/cuisine/src/v2/App.jsx`** — LocationField mount:
     - Removed the `<div className="mt-7">…</div>` wrapper that
       v0.61.241 added (and the trailing `</div>` after the
       LocationField close tag). The location field sits directly
       inside the outer column flex parent again — gap-2 (8 px)
       between region pills and LocationField, matching the
       pre-v0.61.241 spacing the operator wants back.

  **4. `package.json`** — `0.61.246 → 0.61.247`.

  **5. `doc/.serial-state.yml`** — commit 459→460, build 249→250,
     journal 447→448; re-anchored 29-05 '26 12:25 SGT (PR #749
     v0.61.246 merge timestamp `2026-05-29T04:19:59Z` = 12:19 SGT,
     this PR built minutes later).

- **[VERIFICATION (sandbox)]**

  - JSX `node --check` doesn't validate JSX (Vite/Babel does at
    build time); structural sanity: the SG/JB branch has zero
    bubble blocks, the OtherLocationPicker compact pill has zero
    bubble blocks, App.jsx mount has zero `mt-7` wrappers around
    LocationField.
  - `npm test -- --run` pending — JS-side tests untouched, no
    LocationField unit tests exist.

- **[STATUS]** PR-OPEN on push.

- **[TEST]** Operator (post-merge + Railway redeploy):
  1. Open Cuisine TMA in SG. The region pill row (🇸🇬 SG · MY JB ·
     🌏 Others) now sits **right above** the LocationField with
     just an 8-px gap (the parent flex column's `gap-2`). No
     floating bubble above the pill.
  2. The LocationField shows the inline format you wanted:
     `📍 <neighbourhood> · 5 places nearby · tap to change · ✏️`.
  3. Switch to 🌏 Others compact pill — same inline layout:
     `📍 🇲🇾 Kuala Lumpur · 5 places nearby · tap to change · ✏️`.
  4. Type into the LocationField input — after 6 s of pause, the
     v0.61.244 `Tap 🔍 to search` bubble still appears below the
     pill (this one's NOT reverted; the operator didn't object to
     it).
  5. Open hours card (v0.61.246) still shows `Open · Closes 3:00
     PM · Reopens 6:00 PM` for lunch+dinner restaurants.

- **[KNOWN GAPS]**
  - SG/JB-mode truncation of the resting label is back to the
    pre-v0.61.241 behaviour: long labels (`SPH Media Centre…`)
    can be visually squeezed by the trailing `· suffix · tap to
    change` text on narrow viewports. This was the pain that
    motivated v0.61.241; operator has explicitly chosen the older
    layout over the bubble. If it bites later, alternatives are
    (a) drop the `tap to change` hint, (b) move only the
    `tap to change` to a sub-line, (c) reintroduce the bubble
    with a different trigger.
  - Menu TMA's location field (`LocationFieldMenu.jsx`) was never
    touched in v0.61.241, so it had no bubble to revert. It's
    being addressed in the next PR per the operator's earlier
    plan (Menu TMA Big Bug fix).

- **Confirmation Gates**

  | Gate | Resolution | Date |
  |---|---|---|
  | G1 — version bump | PATCH `0.61.246 → 0.61.247`. UI revert + small layout undo. | 29-05 '26 |
  | G2 — destructive action | _n/a_. The v0.61.241 bubble code is preserved in `git log -p` / vault snapshots; the visible UI element is removed. | 29-05 '26 |
  | G3 — new decision rule | YES — for location-field ancillary metadata, inline text wins over a floating speech bubble in this codebase. Future "what about a bubble?" proposals should weigh against this precedent. | 29-05 '26 |
  | G4 — paid external API call | _n/a_. | 29-05 '26 |

## Amendments

- **`LocationField.jsx` (SG/JB)** — bubble div removed; button
  className back to `flex items-baseline gap-1.5`; inline suffix
  + "tap to change" restored.
- **`LocationField.jsx` (OtherLocationPicker compact)** — bubble
  + outer `relative` removed; single-row compact pill restored.
- **`App.jsx`** — `<div className="mt-7">` wrapper removed.

## Deleted

The v0.61.241 location-suffix speech bubble (visible UI element).
Code path no longer exists in `LocationField.jsx`; if the operator
wants it back, restore from `git log -p v0.61.241`.
