# Journal — v0.62.616 (Hawker: left-align the zone pills + show the selected-zone centre count above the version)

_(№ 960 — 20-07 '26 14:20 SGT)_

## New

### [HDR] v0.62.616 — Hawker footer/zone tweaks (2 of 4 operator asks); the other 2 diagnosed & deferred

- **[INTENT]** Operator, four Hawker requests (on **tablet** — the footer now reads `(tablet)`, confirming
  the wide layout is finally rendering on their **Telegram Desktop for Mac** once the window is wide
  enough):
  1. Move the **NEA ↗** hyperlink to the **left, beside the map**.
  2. *"Where are the quick buttons inside the map for hawker TMA and train TMA?"*
  3. When a zone is selected, show the **number of hawker centres in that zone** at the footer, **above
     the version**.
  4. **Don't centre the zone pills — keep them left.**

- **[DELTA — done: #4 + #3]** `web/hawker/src/App.jsx`:
  - **#4** — the `portraitTabletPanel` and `drawerLayout` zone-pill rows `justify-center` → **`justify-start`**
    (the landscape row was already left).
  - **#3** — `FooterDock` gains a **`zoneInfo`** prop rendered on its own line **above the version**
    (`text-[10px] text-tg-text`, centred); a new App-body `zoneInfo` const =
    `${REGION_EMOJI} ${regionLabel} · ${active.centres.length} centres` ("centres" is the same token in
    en/fr), passed to all three `FooterDock` calls.

- **[DEFERRED — #1 + #2, deliberately not rushed]**
  - **#1 (NEA → left beside map)** — spatially ambiguous: the footer's left slot already holds the
    map/list toggle, and "beside the map" could mean the footer-left or an on-map control. **Asked the
    operator to pin the spot** before moving it.
  - **#2 (map quick buttons)** — **diagnosed as a real bug.** A Playwright bounding-box probe shows the
    `MapControls` quick-toggle row (⋮ / Colour / 🚉 Train / 🅿 Carpark / 🚌 Bus Stop) renders at **y=648 —
    the *bottom* of the framed tablet map** — even though its class is `top-2`. It's visible but shoved to
    the map's bottom edge (hence "where are they"). The mechanism (why `top-2` resolves to the map bottom
    in the tablet framed layout) is not yet understood, so it was **not blind-fixed** — flagged for a
    focused follow-up on **both Hawker and Train**.

- **[VERIFICATION (sandbox)]** **Hawker** `vite build` green; full `npm test` **3738/3738 across 147
  files**; **Playwright** (tablet 744) confirmed the zone pills are now **left-aligned** and the footer
  shows **"🏙 Central · 6 centres"** above `… v0.62.616 · (tablet)`. `package.json` + `package-lock.json`
  0.62.615 → 0.62.616; `.serial-state.yml` commit 1097→1098, build 717→718, journal 959→960, re-anchor
  **20-07 '26 14:20 SGT**.

- **[TIME ANCHOR]** data.gov.sg blocked by the environment network policy (`403` `CONNECT`); anchored to
  the container system clock `2026-07-20T06:20Z` = **20-07 '26 14:20 SGT** (TF-9 terminal-runtime source).

- **[STATUS]** DRAFT → squash-merge to `main` on green CI (auto-merge SQUASH). New PR (branch restarted
  from `origin/main` `3c1e2a3` = `#1623` merged).

- **[TEST]** No new unit test — CSS-alignment + a footer line; verified visually via Playwright.

- **[KNOWN GAPS]** **STILL DUE:** #1 (NEA placement — awaiting operator's spot) and #2 (the
  map-quick-buttons-at-the-bottom bug, on Hawker **and** Train — needs the `top-2`-resolves-to-bottom
  mechanism understood before a safe fix). Carried: v0.62.610 `disableVerticalSwipes` + v0.62.613
  iPad-mini layout still await a real-device eyeball (the tablet layout is now confirmed working via the
  `(tablet)` footer tag).

## Confirmation Gates
| Gate | Status | Note |
|------|--------|------|
| G1 | ✅ | PATCH bump 0.62.615 → 0.62.616; Hawker build + 3738 tests green; both changes verified via Playwright. |
| G2 | ➖ | additive — alignment class + a footer line + a prop; nothing removed. |
| G3 | ➖ | no new decision rule (two direct operator tweaks; two deferred pending clarification/diagnosis). |
| G4 | ➖ | no external paid API call. |
