# Journal — v0.62.614 (recode slice A — dedup `device-id` + `iata-cities` into `_shared/`; the rest are intentional divergences)

_(№ 958 — 20-07 '26 11:34 SGT)_

## New

### [HDR] v0.62.614 — recode slice A: consolidate the truly-identical data/util duplicates; discover the rest diverge on purpose

- **[INTENT]** Operator: **"go"** — start recode **slice A** (Register O-55): consolidate the pure
  data/util duplicates into `web/_shared/`, branched fresh from the checkpoint `vault-v0.62.613`
  (`6c7dce9`).

- **[FINDING — important, reshapes the plan]** The raw filename-count "duplication" (`device-id`,
  `coords-to-country`, `countries`, `cities`, `iata-cities`, `fun-facts` — each ×2) is **mostly
  INTENTIONAL divergence, not accidental copies.** Only **2 of 6** are byte-identical code:
  - `device-id.js` (cuisine + menu) — diff is **comment-only**.
  - `iata-cities.js` (cuisine + menu) — diff is **comment-only** (its own header even documents the
    "parallel copy, keep in sync" convention this slice removes).

  The other four **diverge deliberately** and were **NOT merged** (merging would change behaviour /
  introduce bugs):
  - `countries.js` — the **Menu** TMA appends `🇸🇬 SG` to its flag dropdown (operator, 28-05 '26); the
    **Cuisine** TMA omits SG (uses a separate SG pill).
  - `coords-to-country.js` — **Cuisine** carries the v0.61.281 / v0.62.100 **west-of-strait SG/MY border
    carve-out** (Tuas / Legoland / Iskandar Puteri fix) that **Menu lacks** — different geo-classification.
  - `cities.js` — Johor-state-row wording + curated-radius data drift.
  - `fun-facts.js` — `cuisine/…/data/` (658 lines) vs `cuisine/…/lib/` (228 lines) — **different files**,
    not a pair.

- **[DELTA]** `git mv` the two identical files → `web/_shared/lib/device-id.js` + `iata-cities.js` (kept
  Cuisine's copy as canonical), `git rm` the Menu dupes, and repointed all **6 import sites** — Cuisine
  `v2/lib/api.js` + `v2/App.jsx`; Menu `App.jsx` (×2) + `components/LocationFieldMenu.jsx` (×2) — plus
  `__tests__/iata-cities.test.js` — to `_shared`. Menu already resolves `../../_shared/` (it imports
  `FooterNav` + `location-sync` from there), so **no Vite config change** was needed. Headers of the moved
  files updated to note they're the shared canonical.

- **[VERIFICATION (sandbox)]** **cuisine + menu** `vite build` green; full `npm test` **3738/3738 across
  147 files** (the transient `iata-cities.test.js` "Failed to load url …/cuisine/…/iata-cities.js" was the
  test's own stale import — repointed to `_shared`). `package.json` + `package-lock.json` 0.62.613 →
  0.62.614; `.serial-state.yml` commit 1095→1096, build 715→716, journal 957→958, re-anchor
  **20-07 '26 11:34 SGT**.

- **[TIME ANCHOR]** data.gov.sg blocked by the environment network policy (`403` `CONNECT`); anchored to
  the container system clock `2026-07-20T03:34Z` = **20-07 '26 11:34 SGT** (TF-9 terminal-runtime source).

- **[STATUS]** DRAFT → squash-merge to `main` on green CI (auto-merge SQUASH). New PR (branch restarted
  from `origin/main` `6c7dce9` = the `vault-v0.62.613` checkpoint).

- **[TEST]** No new test — a file move + import repoint; the existing `iata-cities.test.js` (repointed)
  still exercises the shared table. Suite unchanged and green.

- **[KNOWN GAPS]** The four divergent files are **not dedup candidates** — a genuine consolidation would
  need parameterising the intentional per-TMA differences (SG-in-dropdown, the SG/MY carve-out), which is
  a behaviour-touching change for a later, deliberately-verified slice, not a "safe dedup." Next slices
  (Register O-55): **B** small shared UI (`LocaleToggle`/`BackFab`/`MapControls`/`tg.js` core); **C**
  `mapOverlays.js` (×3, diverged); **D** split cuisine `App.jsx`; **E** comment hygiene (operator policy
  call). Carried: v0.62.610 `disableVerticalSwipes` + v0.62.613 iPad-mini layout still await a real-device
  eyeball.

## Confirmation Gates
| Gate | Status | Note |
|------|--------|------|
| G1 | ✅ | PATCH bump 0.62.613 → 0.62.614; cuisine + menu builds + 3738 tests green; behaviour-neutral file move. |
| G2 | ✅ | Deletion of the two Menu duplicate files — **allowed**: identical code, content preserved in `_shared/` (git-tracked); no information lost. The four divergent files were **kept** precisely to avoid destructive merges. |
| G3 | ➖ | no new decision rule (executing the agreed O-55 slice A). |
| G4 | ➖ | no external paid API call. |
