# Journal — v0.62.604 (Hawker: tap-zoom 16 + bus stops two-per-row in the card)

_(№ 948 — 19-07 '26 09:03 SGT)_

## New

### [HDR] v0.62.604 — Hawker: tap a centre card/pin → zoom 16 (bus stops visible), and pack card bus stops two-per-row

- **[INTENT]** Operator (IMG_3588), two Hawker tweaks:
  1. *"each time I select hawker centre card or pin, zoom to 16 so that the bus stops can be seen.
     right now is 15."*
  2. *"the bus stops can be two in a row in the card (not google pin card) if size permit."*

- **[DELTA — map]** `web/hawker/src/components/HawkerMapPanel.jsx`: `activateCentre` (the unified
  card↔pin tap flow) now zooms **`Math.max(TAP_ZOOM_PHONE, 16)`** on a phone (was the shared
  `TAP_ZOOM_PHONE = 15`); tablet stays `TAP_ZOOM_WIDE = 17`. The **shared** `map-interaction.js`
  constant is left at 15 so the **Cuisine TMA is unaffected** — only Hawker leans one step closer, which
  is the zoom at which its nearby bus-stop pins become visible.

- **[DELTA — card]** `web/hawker/src/App.jsx`: in `renderCentreCard`, the bus-stop container goes
  `flex flex-col` → **`flex flex-wrap`**, and each bus button gains `truncate
  max-w-[calc(50%-0.125rem)]` — so the stops pack **two to a row** when size permits (capped at half
  the card width, long labels ellipsize), instead of one per row. This is the in-card list only; the
  Google-pin InfoWindow is untouched.

- **[VERIFICATION (sandbox)]** **Playwright** (stubbed four bus stops) confirmed the card renders the
  bus stops **two-per-row** — `🚌 03011 · Maxwell Rd | 🚌 03019 · Opp URA Centre` /
  `🚌 03111 · Blk 1 | 🚌 03219 · S Bridge Rd` — with no page errors. Hawker `vite build` green
  (`index-A1eMWGqg.js 256.74 kB │ gzip 84.03 kB`); full `npm test` **3738/3738 across 147 files**;
  `package.json` + `package-lock.json` 0.62.603 → 0.62.604; `.serial-state.yml` commit 1083→1084, build
  705→706, journal 947→948, re-anchor **19-07 '26 09:03 SGT**. (The zoom is a numeric `setZoom` change,
  not visually asserted here — it needs the live Google map.)

- **[TIME ANCHOR]** data.gov.sg blocked by the environment network policy (`403` `CONNECT`); anchored
  to the agent-proxy status timestamp `2026-07-19T01:03Z` = **19-07 '26 09:03 SGT** (TF-9 fallback;
  TF-10 lower bound).

- **[STATUS]** DRAFT → squash-merge to `main` on green CI (auto-merge SQUASH). Rides on **PR #1612**
  (see the note below) alongside v0.62.603.

- **[GIT — squash-divergence fix]** #1612's v0.62.603 commit had been built on the **local** v0.62.602
  (94fda1c), not the squash-merged main (`a7c7a3d`, #1611) — so GitHub reported the PR
  `mergeable_state: dirty` (a phantom conflict). Fixed by **restarting the branch from `origin/main`
  `a7c7a3d`**, **cherry-picking** v0.62.603 (now `1ec3d53`), and adding this v0.62.604 on top, then
  force-pushing — so #1612 now carries **both** cleanly with no conflict. (Codifies the standing
  restart-after-squash rule: build each new version on the merged main SHA, never the local one.)

- **[TEST]** No new unit test (a zoom constant + a Tailwind wrap class; verified visually via
  Playwright). Existing suite unchanged and green.

- **[KNOWN GAPS]** A very long single bus-stop label still truncates rather than wrapping (by design —
  keeps two per row). Carried: the Transport full-map toggle + unified flash; the card's `mrt.*` +
  closure strings are en/fr-lean; Cuisine `tg.js` safe-area gap.

## Confirmation Gates
| Gate | Status | Note |
|------|--------|------|
| G1 | ✅ | PATCH bump 0.62.603 → 0.62.604; Hawker build + 3738 tests green; two-per-row verified via Playwright. |
| G2 | ➖ | no destructive removal — a zoom value + a layout class changed; the shared constant untouched. |
| G3 | ➖ | no new decision rule (executes two direct operator tweaks). |
| G4 | ➖ | no external paid API call. |
