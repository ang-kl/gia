(№ 523 - 05-06 '26 18:13 SGT) v0.61.322

# Journal — soleat v0.61.322 (Cuisine TMA: FULL splash gate + "Keep" full-commit)

> Operator (05-06 '26, follow-up to v0.61.321): confirmed the FULL SPLASH GATE shape — *"check before load"*. The TMA must confirm location BEFORE the body renders: the user should see ONLY a "Confirming your location…" splash + (if needed) the mismatch modal — never a half-loaded TMA sitting under the modal.
>
> Also (the v0.61.321 KNOWN GAP, now closed): *"if i keep to JP, the location should change, but still show Downtown Cove. it is confusing."* — choosing **Keep** on the anchor-mismatch modal previously only moved `searchCenter` and left region/flag = SG, so a JP place rendered under a 🇸🇬 flag (name says Japan, flag says SG → incoherent).
>
> Commit 536 / journal 523 / build 323, on branch `claude/v0.61.322-splash-gate` off `main` at v0.61.321 (sha 9e29be5).

## New

### [HDR] #523 | 18:13 SGT 05-06-26 | v0.61.322 — splash gate before TMA body + "Keep" full-commit | 2 files | 1 PR | 1 build |

- **[INTENT]** (A) Gate the whole TMA body behind a location-confirmation splash so no half-loaded UI ever shows under a coherence modal. (B) Make BOTH anchor-mismatch choices fully coherent: after "Use current" OR "Keep", the name + flag + map + search all agree.

- **[DELTA]**

  **1. `web/cuisine/src/v2/App.jsx`**

  **Part A — FULL SPLASH GATE (early return):**
  - New state `locationGateOpen` (`useState(false)`) + `modalPendingRef` (`useRef(false)`), declared with the v0.61.274 coherence block.
  - All three coherence checks (v0.61.274 country-coords, v0.61.276 region-coords, v0.61.321 anchor-device) now set `modalPendingRef.current = true` at the point they set their `*Mismatch` state.
  - All three `apply*Choice` fns now set `modalPendingRef.current = false` before clearing the mismatch.
  - New **gate-opener effect**, declared AFTER the v0.61.321 anchor check (so the one-shot `*CheckedRef`s are already set synchronously in the same commit). Opens the gate once: device coords are known, all applicable checks have evaluated (`coherenceCheckedRef && anchorCoherenceCheckedRef && (region==='JB' ? regionCoherenceCheckedRef : true)`), and `modalPendingRef` is false. Deps include the three mismatch states so dismissing a modal re-runs it → gate opens.
  - The three coherence-modal JSX blocks are extracted into `const locationModals = (<>…</>)` declared just before `return (`.
  - **Early return** before the main return: when `!locationGateOpen`, render the SAME outer `<div>` (identical className + style) containing `{locationModals}` + a centred spinner ("Confirming your location…" / "Confirmation de votre position…"). Spinner is a border-accent ring (no red/green).
  - Main return now renders `{locationModals}` in place of the three inline blocks.

  **Part B — "Keep" / "Use current" full-commit in `applyAnchorCoherenceChoice`:**
  - **Use current** — now builds an explicit `stateDelta` (region SG / JB / OTHER+countryPref MY) and fires a fresh `runSearch({...state, ...stateDelta}, {device coords})` so the 5 venues match the new region (previously only `searchCenter` moved).
  - **Keep** — keeps `locationAnchor` (its name is the kept spot's, correct to display) but now flips region/flag to the kept spot's actual country: `coordsToCountry(anchor)` → SG / (JB|OTHER+MY); else (overseas, null) → `nearestIataCity(anchor)` → if `near.city.countryCode` ∈ `OTHER_COUNTRIES` → region OTHER + that countryPref (e.g. JP); unresolvable → region OTHER (safest non-SG fallback). Then fires a fresh `runSearch` at the kept coords. Net: JP place now shows JP flag, not 🇸🇬.

  **2. `package.json`** — `0.61.321 → 0.61.322`.

- **[VERIFICATION (sandbox)]**
  - `web/cuisine/src/v2/App.jsx` passes **esbuild** JSX parse (`node_modules/.bin/esbuild --loader:.jsx=jsx --bundle=false --format=esm src/v2/App.jsx` → no error).
  - `node --check index.js` clean. `npm test -- --run` → **104 files / 2892 tests passed**.
  - Local `npm run build` still hits the iCloud `vite/dist` corruption (`does not provide an export named 'B'`) — an env artifact, NOT this code; **CI TMA-build is the authoritative gate** (v0.61.319/320/321 precedent).
  - Splash + modal are colour-safe (📍 + text + labelled buttons; spinner ring is border-accent) — fine for red-green deficiency.

- **[STATUS]** PR-OPEN on push; auto-merge squash armed. Railway redeploys on merge.

- **[TEST]** Operator (post-merge, live):
  1. Cold launch with a clean SG location → splash spins briefly → TMA renders SG (no modal).
  2. Stale overseas anchor cached ("Downtown Cove"/"Naka Ward") in SG → splash shows, modal appears ON the splash (no TMA underneath) → **Use my current location** → TMA renders with real SG place + 🇸🇬 + SG results.
  3. Same, but **Keep "Downtown Cove" (JP)** → TMA renders with the JP name AND the JP flag + JP-centred map + JP results (no more JP-name-under-SG-flag).
  4. JB pill sticky at SG coords → region modal on splash → choose → gate opens.

- **[KNOWN GAPS]**
  - The auto-detect / warm-start searches may fire underneath during the splash — harmless, the body is hidden, and the apply* path fires a fresh correct search. If NO mismatch occurs the gate opens as soon as the checks evaluate (typically same frame as device resolve), so the splash is a brief flash, not a stall.
  - "Keep" overseas-country resolution depends on `nearestIataCity` having the country in `OTHER_COUNTRIES`; an unsupported country falls back to region OTHER (no specific flag) but never leaves SG — so the 🇸🇬-over-foreign-name incoherence is eliminated even in the fallback.

- **Confirmation Gates**

  | Gate | Resolution | Date |
  |---|---|---|
  | G1 — version bump | PATCH `0.61.321 → 0.61.322`. UX gate + choice-handler change; no API contract change. | 05-06 '26 |
  | G2 — destructive action | _n/a_. Adds a render gate + extends a handler; no data deletion. | 05-06 '26 |
  | G3 — new decision rule | TMA body is gated behind location confirmation; "Keep" now commits region/flag/countryPref to the kept spot's true country. | 05-06 '26 |
  | G4 — paid external API call | _n/a_ at gate time; apply* fires the same reverse-geocode / search calls already made on a normal location pick. | 05-06 '26 |

## Amendments

- **`web/cuisine/src/v2/App.jsx`** — added the v0.61.322 splash gate (state `locationGateOpen` + `modalPendingRef`, gate-opener effect, extracted `locationModals`, early-return splash) and the "Keep"/"Use current" full-commit in `applyAnchorCoherenceChoice` (closes the v0.61.321 "Keep + overseas flag" KNOWN GAP).
- **`package.json`** — version `0.61.321 → 0.61.322`.
- **`doc/.serial-state.yml`** — counters + last_anchor_time re-anchored 05-06 '26 18:13 SGT.

## Deleted

_None._
