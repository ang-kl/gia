(№ 257 - 19-05 '26 23:45 SGT) v0.61.46

# Journal — soleat v0.61.45 → v0.61.46 (🎨 Colour-button icon contrast)

> Operator: *"The colour of this icon 'palette' for 'colour' when selected washout the
> 'palette' as it is also green."* — the multi-colour 🎨 emoji loses contrast against
> the Colour toggle button's selected-state background.
>
> Rides the same branch / PR as v0.61.45 (#534) — this entry is commit 257 /
> journal 250 / build 58.

## New

### [HDR] #250 | 23:45:00 SGT 19-05-26 | v0.61.46 — 🎨 Colour-button icon contrast | ~0.5 kb | 4 files | (PR #534) | 1 build |

- **[INTENT]** Stop the 🎨 emoji on the Colour toggle button from washing out against
  the button background.

- **[DELTA]**
  - **`MrtMapPanel.jsx` / `HawkerMapPanel.jsx` / `MapPanel.jsx`** — the 🎨 `<span>` on
    the Colour nav button is now rendered as a **monochrome silhouette** via a CSS
    `filter`: `brightness(0)` (solid dark) when the button is ON / selected (amber
    `#F59E0B` background), `brightness(0) invert(1)` (solid white) when OFF (dark
    `#1F2937` background). The multi-colour emoji no longer clashes with the button
    fill — same technique already used for the Cuisine venue-pin emojis.
  - **`package.json`** — `0.61.45` → `0.61.46` (PATCH).

- **[VERIFICATION (sandbox)]**

  ```bash
  $ npm run build:cuisine / build:hawker / build:transport  → all ✓ built
  $ npm test  → 65 files / 1928 tests green
  ```

- **[STATUS]** DRAFT → PR #534 (base `main`, branch `claude/review-gia4lunch-readme-3s798`),
  bundled with the v0.61.45 Cuisine greyscale fix. Deploys via the Railway auto-deploy
  on `main` once merged.

- **[TEST]** On any TMA map the 🎨 Colour button shows a crisp single-colour palette
  icon — dark on the amber selected state, white on the dark unselected state — with
  no wash-out.

- **[KNOWN GAPS]** Only the 🎨 Colour button's icon is monochromed; the MapControls
  row-pill emojis (✨ / 🅿️ / 🚌) keep their native rendering (not reported as a
  problem). Sandbox-unverified visual change (no browser in the build env). The
  24 hours layer remains disabled; the `doc/Legal/` revision (journal #239) remains.

- **Confirmation Gates**

  | Gate | Resolution | Date |
  |------|-----------|------|
  | G1 — version bump | PATCH `0.61.45` → `0.61.46`, established 0.61.x cadence | 19-05 '26 |
  | G2 — destructive action | _n/a_ | — |
  | G3 — new decision rule | _n/a_ | — |
  | G4 — paid external API call | _n/a_ | — |

## Amendments

_None._

## Deleted

_None._
