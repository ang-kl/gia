(№ 888 - 14-07 '26 20:53 SGT) v0.62.532

# Journal — soleat v0.62.532 (Parity B: CCL6 first/last-train timings from SMRT screenshots)

> Operator unblocked Parity B: *"Information about 3 stations … https://github.com/ang-kl/gia/tree/main/data/trains"* —
> after I confirmed every external source (OneMap, OpenStreetMap/Overpass, SMRT, LTA) is denied by the
> sandbox network policy (403 CONNECT), the operator uploaded **SMRT `journey.smrt.com.sg` screenshots**
> to `data/trains/` (via #1542). This wires in the **timings** (fully authoritative). **Exits remain
> pending coordinates** — the photos give exit numbers + landmarks but no lat/lng, and I do not fabricate.

## New

### [HDR] #888 | 20:53 SGT 14-07-26 | v0.62.532 — CCL6 first/last-train timings | 3 files | PR #(pending) |

- **[INTENT].** Fill the CCL6 first/last-train gap flagged since #876/#884 — the one part of Parity B
  that authoritative operator-supplied data now covers.

- **[SOURCE].** `data/trains/IMG_0654…0662.jpeg` (operator screenshots of the SMRT station pages) —
  3 stations × {First & Last Train, Exits, Map}. Read all 9; transcribed the timings **verbatim**.

- **[DELTA].**
  - `data/sg_train_timings.json` — filled the existing `clockwise` null rows for CC30/31/32 and **added
    an `anticlockwise` direction** for the 3. Refreshed 3 now-stale metadata notes ("CCL6 not yet open"
    → open + both-direction SMRT source).
  - `data/stations.json` — **surgical** patch of the 3 stations' `first_last_train[]` + `data_quality_notes`
    + `_meta.stationsWithTrainTimings` 152→**155**. (Deliberately **not** a full `build-station-info.js`
    regen — a fresh regen bumps every station's `last_updated_at` for ~500 lines of noise; the source is
    updated so a future regen stays correct. Matches #876's surgical precedent on this generated-but-
    hand-patched file.)
  - `package.json` — 0.62.531 → **0.62.532**.
  - `doc/Journal/journal-0_62_532-14_07_26-2053.md` — this entry.
  - `doc/.serial-state.yml` — commit 1005→**1006**, build 631→**632**, journal 887→**888**; re-anchor
    14-07 '26 20:53 SGT (prior 887 anchor demoted verbatim, AU-7).

- **[⚠️ DIRECTION MAPPING — verified, easy to flip].** The file's `clockwise` station array runs
  CC1…CC29 **…CC30…CC31…CC32**, so at the CCL6 stations **clockwise continues *past* HarbourFront
  towards Marina Bay** (closing the now-complete loop). Therefore:
  - `clockwise` = SMRT **"towards NS27/TE20/CE2 Marina Bay"**
  - `anticlockwise` = SMRT **"towards NE1/CC29 HarbourFront"**
  I checked the array order specifically because the intuitive reading (clockwise = towards HarbourFront)
  is **backwards** for these post-HarbourFront stations.

- **[VALUES]** (clockwise / Marina Bay · anticlockwise / HarbourFront; `first_weekday`=Mon-Fri=Sat,
  `first_weekend`=Sun/PH — day-folding is lossless: SMRT Saturday first-train == Mon-Fri in all 6
  combos, last-train uniform across day types):
  | Station | clockwise (→Marina Bay) | anticlockwise (→HarbourFront) |
  |---|---|---|
  | Keppel CC30 | 5:49am / 6:14am · last 11:53pm | 6:05am / 6:30am · last 11:59pm |
  | Cantonment CC31 | 5:59am / 6:23am · last 12:37am | 5:30am / 5:51am · last 12:40am |
  | Prince Edward Rd CC32 | 5:54am / 6:19am · last 11:58pm | 6:01am / 6:25am · last 11:55pm |

- **[EXITS — STILL NOT DONE, honest].** The SMRT pages list exit **numbers + landmark lists + rough map
  position + bus-stop codes** (Keppel 14081/14089, Cantonment 14069/14061), but **no exit lat/lng**. The
  card's `exits[]` draws pins from coordinates, so `exits[]` stays `[]` and a `data_quality_note` records
  that exit coords are still needed (OneMap/Overpass blocked; operator can provide) and that they are
  **not fabricated**. Bus stops / taxis / carparks were already at full parity (computed live). The
  landmark data is preserved in the `data/trains/` photos for when coordinates arrive.

- **[VERIFICATION (sandbox)].**
  - Both JSON valid; each of the 3 stations now has **2** `first_last_train` entries (clockwise +
    anticlockwise) with the render shape `{direction, timings:{first_weekday,first_weekend,last_weekday,
    last_weekend}}` that `firstLastTrainHtml`/`fltTimes` (`web/{transport,hawker}/…/mapOverlays.js`) expect.
  - `stations.json` diff **confined to the 3 stations + `_meta`**; no other station churned.
  - **Full `npm test` → 3699/3699 across 144 files.**
  - No `.js` changed (2 JSON data files) → **no web build** (`stations.json` is server-read by
    `index.js` `/api/transport/station-context`, not bundled — #876/#1518 precedent).

- **[STATUS]** PROD — the version bump redeploys the bot (Railway); the 3 CCL6 station cards on **both**
  the Transport and Hawker TMAs now show the "🚆 First / Last Train" block (both directions) via the
  shared `/api/transport/station-context`.

- **[TEST]** TMA data change (server-read); covered by full `vitest`. No new test (verbatim data).

- **[KNOWN GAPS].**
  1. 🚪 **CCL6 exit PINS** — need exit lat/lng (operator to provide, or allowlist OneMap/Overpass). Exit
     numbers + landmarks captured in `data/trains/` photos.
  2. 🧱 **Consolidation queue** — PR-2…PR-6 remain (paused on the merge-model choice).
  3. 🗒️ **#1525** now **closed** (superseded by #1535).

## Confirmation Gates
| Gate | Status | Note |
|------|--------|------|
| G1 | ✅ | PATCH bump 0.62.531→0.62.532; authoritative data addition, standard per-PR rule |
| G2 | ➖ | no destructive action; filled nulls + surgical add |
| G3 | ➖ | no new decision rule |
| G4 | ➖ | no external paid API call (data was operator-supplied screenshots; all live sources are policy-blocked) |
