(№ 386 - 28-05 '26 01:00 SGT) v0.61.185

# Journal — soleat v0.61.185 (region rename MY-PUT → OTHER + Putrajaya 20 km + region-aware search)

> Operator: *"Want me to file v0.61.185 for the Putrajaya/JB
> region-mismatch fix. Test it. I stated that Putrajaya range is
> 20 km and is part of 'others'. Remember I have three settings -
> Singapore, Johor Bahru and Others. what is my criteria for
> 'others'"* — answered (location-mode.js rules §2.2 + §2.3) and
> the PR aligns the TMA pill semantics to the classifier.
>
> Commit 398 / journal 386 / build 188, on branch
> `claude/v0.61.185-other-rename` off `main` at v0.61.184.

## New

### [HDR] #386 | 01:00 SGT 28-05-28 | v0.61.185 — Region rename + 20 km cap + region-aware search | 12 files | 1 PR | 1 build |

- **[INTENT]** Three coupled changes:
  1. **Rename** the TMA's third region pill from `MY-PUT`
     (Putrajaya-specific) to `OTHER` (generic — matches
     location-mode.js classifier). Surfaces a `🌏 Others` pill in
     the UI.
  2. **Bump** the IOI Resort City Putrajaya radius cap from
     **15 km → 20 km** per operator's spec.
  3. **Fix the Putrajaya 0-result bug** — cuisine-search
     hardcoded `regionCode: 'SG'` for everything that wasn't JB,
     so Putrajaya anchors returned SG venues that the distance
     filter dropped. New behaviour: `OTHER` drops the default
     `regionCode` and relies on `locationBias.circle` around the
     anchor (operator's confirmed choice — "drop the suffix
     entirely; rely on locationBias.circle").

- **[DELTA]**
  - **`precincts.js`**:
    - `ioi-resort-putrajaya.region`: `'MY-PUT' → 'OTHER'`
    - `ioi-resort-putrajaya.radiusCapM`: `15000 → 20000`
    - Description updated to mention 20 km coverage (Putrajaya,
      Cyberjaya, Kajang, Puchong, Subang).
  - **`web/cuisine/src/v2/lib/state.js`**:
    - `REGIONS = ['SG', 'JB', 'MY-PUT'] → ['SG', 'JB', 'OTHER']`
  - **`web/cuisine/src/v2/lib/i18n.js`**:
    - `'region.putrajaya'` retired; new `'region.others'`:
      EN `Others` / FR `Autres`.
  - **`web/cuisine/src/v2/App.jsx`** (2 edits):
    1. Region pill array now has 3 entries:
       ```jsx
       { id: 'SG',    flag: '🇸🇬',           label: t('region.singapore', lang) },
       { id: 'JB',    flag: 'johor-flag.png', label: t('region.johor', lang) },
       { id: 'OTHER', flag: '🌏',           label: t('region.others', lang) }
       ```
    2. `tryServerCache` auto-flip — anchor `region === 'MY-PUT'`
       OR `region === 'OTHER'` now sets the TMA pill to `'OTHER'`
       (was setting to `'JB'`, which masked the operator's
       Putrajaya 0-result mismatch).
  - **`index.js`** (4 edits):
    1. **Location quick-pick keyboard** (line ~1725): flag
       mapping for precinct rows extended — `'OTHER'` rows get
       🇲🇾 when `country === 'Malaysia'`, else 🌏.
    2. **`ftRegionCode`** (free-text search, line ~8953):
       was `(JB || MY-PUT) ? 'MY' : 'SG'`. Now `JB || MY-PUT ||
       (OTHER+Malaysia) → 'MY'`, `OTHER+other-country →
       undefined`, default `SG`. Lets Places' own locality
       handle Indonesia / Thailand / etc.
    3. **`preserveRegion`** check (set-location resolve, line
       ~11281): now accepts `'OTHER'` alongside the legacy
       `'MY-PUT'` / `'JB'`.
    4. **`searchRegionCode`** (cuisine-search, line ~11789):
       new `isOther` flag; `isOther ? undefined : 'SG'` for the
       non-JB branch. **This is the actual 0-result fix.**
  - **`location-cache.js`** — comment updated to document the
    'MY-PUT'-accepted-on-read, 'OTHER'-written-going-forward
    contract. The schema is permissive — no migration required.
  - **`vibe-suggest.js`** — region branch accepts both
    `'MY-PUT'` and `'OTHER'`; `biasRadiusM` for the Putrajaya
    anchor bumped 15 km → 20 km in lockstep with precincts.js.
  - **`web/menu/src/App.jsx`** + **`LocationFieldMenu.jsx`** —
    `isMy` / `(JB || MY-PUT)` checks extended to also accept
    `'OTHER'`. Menu TMA's Malaysia-disable-some-features gate
    keeps working for OTHER-anchored users.
  - **`__tests__/precincts.test.js`** (3 assertion updates):
    - `ioi.region` `'MY-PUT' → 'OTHER'`
    - `ioi.radiusCapM` `15000 → 20000`
    - `all[11].region` `'MY-PUT' → 'OTHER'`
  - **`__tests__/scope-guard.test.js`** (comment-only changes
    documenting the v0.61.185 rename; classifier expectations
    unchanged — the classifier always returned 'OTHER' for
    Putrajaya).
  - **`package.json`** — `0.61.184 → 0.61.185`.
  - **`doc/.serial-state.yml`** — commit 397 → 398, build 187 →
    188, journal 385 → 386; re-anchored 28-05 '26 01:00 SGT.
  - **`doc/Journal/journal-0_61_185-28_05_26-0100.md`** — this entry.

- **[VERIFICATION (sandbox)]**

  ```
  $ node --check index.js precincts.js vibe-suggest.js location-cache.js   (all OK)
  $ npx vitest run                                                          (2460 / 2460)
  $ cd web/cuisine && npm run build                                         (✓ built in 8.27s)
  ```

- **[STATUS]** PR-OPEN on push. Operator merges manually.

- **[TEST]** Operator (post-merge + Railway redeploy):
  - Open Cuisine TMA — see a **third pill** `🌏 Others`
    alongside `🇸🇬 Singapore` and `🇲🇾 Johor Bahru`.
  - Chat: `/location` → tap `🇲🇾 IOI Resort City, Putrajaya`.
    Anchor sets to Putrajaya with the 20 km cap (was 15 km).
    Open Cuisine TMA — region pill auto-flips to **🌏 Others**
    (was flipping to 🇲🇾 JB by mistake).
  - Pick European cuisine (or any cuisine) → tap 🔍 — search now
    uses `locationBias.circle` around Putrajaya at 20 km radius,
    no SG `regionCode` constraint. Places returns Putrajaya-area
    venues. Expect 5+ results (the prior 0-result complaint).
  - Toggle to 🇸🇬 — search reverts to SG with the 50 km cap.

- **[KNOWN GAPS]**
  - **Other-country OTHER anchors not yet wired**. precincts.js
    only has the Putrajaya OTHER precinct. Adding a Batam /
    Bintan / KL precinct is a 5-line follow-up — the search
    logic now handles any OTHER country correctly.
  - **vibe-suggest.js still uses `' Putrajaya, Malaysia'` as
    the suffix** for both 'MY-PUT' and 'OTHER' branches. If a
    non-Putrajaya OTHER precinct lands later, this branch needs
    precinct-specific suffixes (or to drop the suffix entirely
    in line with the cuisine-search change).
  - **No migration of cached anchors**. Users with
    `cached.region === 'MY-PUT'` in Redis keep working via the
    backward-compat shims; the value will be written as
    `'OTHER'` next time they re-anchor. Eventually we could
    sweep Redis to rewrite legacy keys, but not necessary —
    behaviour is identical between the two values everywhere
    they're read.

- **Confirmation Gates**

  | Gate | Resolution | Date |
  |------|-----------|------|
  | G1 — version bump | PATCH `0.61.184 → 0.61.185`. UI rename + cap bump + search-path fix. No schema migration. | 28-05 '26 |
  | G2 — destructive action | _n/a_ — backward-compat shims accept legacy `'MY-PUT'` reads alongside `'OTHER'` writes. No data loss. | 28-05 '26 |
  | G3 — new decision rule | YES — TMA pill semantics formally aligned to the location-mode.js SG/JB/OTHER classifier (operator's "three settings" mental model). OTHER's Putrajaya range = 20 km. | 28-05 '26 |
  | G4 — paid external API call | _n/a_ — this PR REDUCES the cost of mis-targeted SG queries from OTHER anchors (they used to return 0 after distance-filtering; now they return real venues from the anchor area). | 28-05 '26 |

## Amendments

- **`REGIONS` array** — was `['SG', 'JB', 'MY-PUT']` (v0.61.159).
  Now `['SG', 'JB', 'OTHER']`.
- **`i18n.region.putrajaya`** — retired. New `i18n.region.others`
  with `Others` / `Autres`.
- **IOI Resort Putrajaya `radiusCapM`** — was `15000`
  (v0.61.129). Now `20000` per operator's stated spec.
- **`searchRegionCode`** branch — was `isJB ? 'MY' : 'SG'`. Now
  `isJB ? 'MY' : (isOther ? undefined : 'SG')`.

## Deleted

_None._
