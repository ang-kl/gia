(№ 586 - 08-06 '26 09:35 SGT) v0.61.386

# Journal — soleat v0.61.386 (Apex root route — stop soleat.net/ 404-ing)

> Operator pasted an edge log: `GET /` on `soleat.net` → **404** (clientUa `axios/0.19.2`), and asked whether reducing Google APIs caused it.
>
> **Left OPEN for review — auto-merge to be armed on the operator's word.** Commit 599 / journal 586 / build 378 (held). Server-only.

## New

### [HDR] #586 | 09:35 SGT 08-06-26 | v0.61.386 — apex `/` handler (browser→app, monitor→200) | index.js | 1 PR | 0 build |

- **[INTENT]** The bare apex `soleat.net/` returned **404** — a monitor/script (axios UA, Thai mobile IP) was hitting it. The app had no `/` handler.

- **[ROOT (traced, read-only)]** `index.js` registers `/health`, `/healthz`, `/privacy`, `/about`, `/doc/*`, `/app`, `/app/menu`, `/app/map`, `/app/cuisine`, the hawker/transport/oversight TMA roots + API routes — but **no `app.get('/')`**, so the apex 404s. **Unrelated to Google:** a root 404 is the Express app, not a Maps API. Disabling the unused Google APIs has zero effect (gia never called them); no Google plan reduces *results*; the only Google risk is an over-restrictive `GOOGLE_MAPS_API_KEY` (server keys need None/IP, not HTTP-referrer), which would show as **failed search**, not a `/` 404.

- **[DECISION]** Operator chose **"2, 3"** = redirect real browsers to the app **and** return 200 for monitors.

- **[DELTA]** (`index.js`, right after the `/health` route)
  - NEW `app.get('/')`: if the `Accept` header includes `text/html` (a browser) → `res.redirect(302, '/app/cuisine')` (the canonical Cuisine TMA path); else (script/monitor/health probe) → `res.status(200).type('text/plain').send('ok')`.
  - **`package.json`** `0.61.385 → 0.61.386`; **`doc/.serial-state.yml`** commit 598 → 599, journal 585 → 586 (**build held 378**); re-anchored 09:35 SGT.

- **[VERIFICATION (sandbox)]** `node --check index.js` clean. Full suite **113 files / 3092 tests**. No dedicated route unit test (an Express route inside the 16k-line `index.js`, analogous to the untested `/health`); `node --check` + the suite cover regression.

- **[STATUS]** PR-**OPEN**, **auto-merge to arm on the operator's word**. Railway redeploys on merge.

- **[TEST]** Operator: `soleat.net/` in a browser now bounces to the Cuisine app; the axios monitor gets a plain `200 ok` instead of 404.

- **[KNOWN GAPS]**
  - The browser branch sends visitors to `/app/cuisine`, a Telegram Mini App — it loads in a plain browser but is best inside Telegram. A dedicated public landing page is a possible later refinement (the operator picked the app over `/about`).
  - Content-negotiation by `Accept` is a heuristic; an odd client sending `text/html` would be redirected, an odd browser without it would get `ok`. Acceptable for an apex handler.
  - Not live-E2E tested (no live edge).

- **Confirmation Gates**

  | Gate | Resolution | Date |
  |---|---|---|
  | G1 — version bump | PATCH `0.61.385 → 0.61.386`. | 08-06 '26 |
  | G2 — destructive action | None — a new route added; no existing route touched. | 08-06 '26 |
  | G3 — new decision rule | The apex `/` serves the app to browsers and `200 ok` to scripts/monitors (no more 404). | 08-06 '26 |
  | G4 — paid external API call | None. | 08-06 '26 |

## Amendments

- **`index.js`** — NEW `app.get('/')` (browser → `/app/cuisine`, else `200 ok`).
- **`package.json`** — `0.61.385 → 0.61.386`.
- **`doc/.serial-state.yml`** — counters (commit 599 / journal 586 / build held 378) + anchor 09:35 SGT 08-06-26.

## Deleted

- _None_.
