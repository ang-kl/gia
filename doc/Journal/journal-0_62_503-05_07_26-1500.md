(№ 857 - 05-07 '26 15:00 SGT) v0.62.503

# Journal — soleat v0.62.503 (Register catch-up rollup: v0.62.76 → v0.62.502) — doc housekeeping

> Operator: "continue outstanding" (the `uv: command not found` investigation parked pending operator input on where the message surfaces).
> Read as: pick up the outstanding work — the concrete in-window gap is the stale Register (last snapshot v0.62.76 / 14-06; current v0.62.502). Bring it current.

## New

### [HDR] #857 | 15:00 SGT 05-07-26 | v0.62.503 — Register catch-up rollup (№ 130) | 4 doc/meta files | PR #(pending) |

- **[INTENT].** `doc/Register/` last updated at `register-0_62_76-14_06_26-1005.md` (№ 129). Current is
  **v0.62.502** — a **113-journal / ~470-PR** arc (v0.62.77 → v0.62.502, 14-06 → 05-07 '26) with no Register
  snapshot. The Journal `[KNOWN GAPS]` had repeatedly flagged "Register still due".

- **[METHOD] (state rollup, not per-PR).** The Register is a **rolling state ledger** (open / deferred /
  decisions / completed / removed), distinct from the per-PR Journal + VibeCodingRecord. So the catch-up is
  **one snapshot** that carries the ledger forward and records the **major decision arcs** — it does *not*
  restate ~470 per-PR decisions (those live in the arc journals). Verified against `origin/main` (gh-API;
  iCloud tree stale).

- **[DELTA] (4 files).**
  - `doc/Register/register-0_62_502-05_07_26-1500.md` — **NEW (№ 130)**. Open items: **O-47** carried
    forward *still-open* `[UNVERIFIED whether incidentally resolved — no arc journal records a knownTotal
    new-search reset]`; **O-48 CLOSED** (durian curated-pick dropped → D-62.79 root fix); **+O-49** (`uv:
    command not found` is absent from all 8 workflow YAMLs / CI logs / annotations / hooks / package.json,
    no Python/Railway markers → external producer, **blocked on operator**), **+O-50** (`index.js:5739`
    hard-coded `Asia/Singapore` bot forecast), **+O-51** (FR Michelin BBOX geo-guard gap). Deferred **DF-1**
    (MRT CCL6 flip, 12-Jul), **DF-2** (dish city-expansion Phase 4, dropped), **DF-3** (FR BBOX). Thematic
    Decisions **D-A1…D-A8** (Cuisine MVP folio+line-icons; card-tab polish; locale 5→8 langs; device-locale
    precedence; dish backfill 100% + 7 pruned; France scaffold; boot/FAB/strip polish; doc-rhythm).
  - `doc/.serial-state.yml` — commit 974→**975**, build 613→**614**, journal 856→**857**, register
    129→**130**; anchor re-set `05-07 '26 15:00 SGT`; prior anchor demoted to `…_superseded_1415`.
  - `doc/Journal/journal-0_62_503-05_07_26-1500.md` — this entry.
  - `package.json` — 0.62.502 → **0.62.503** (even docs-only PRs get a PATCH bump).

- **[VERIFICATION (sandbox)]** Docs-only — no `.js` touched (`node --check` N/A; no web build). Register
  entry follows the `Register.md` skeleton + the AU-1 append-only Recipe (prior ledgers unchanged; O-47
  carried, not deleted).

- **[STATUS]** PROD — merges to `main` (docs-only; no deploy surface).

- **[TEST]** N/A (docs-only).

- **[KNOWN GAPS]** Companion `doc/Technical/` arc entry + `vault/v0.62.502/` snapshot **still due** (logged
  in the Register's Reference section). O-47 resolution `[UNVERIFIED]` — re-confirm on a live 1-result
  search. O-49 (`uv`) blocked on operator: *where* the message surfaces. Standing: MRT CCL6 flip (DF-1,
  12 Jul); `index.js:5739` SGT bot line (O-50); FR Michelin BBOX (O-51).

## Confirmation Gates
| Gate | Status | Note |
|------|--------|------|
| G1 | ➖ | no code — docs-only; version bumped 0.62.502→0.62.503 |
| G2 | ✅ | append-only Register (AU-1): O-47 carried forward, O-48 moved to CLOSED, prior ledgers untouched |
| G3 | ✅ | serial commit 974→975, build 613→614, journal 856→857, register 129→130; anchor re-set 05-07 '26 15:00 SGT |
| G4 | ➖ | no user-facing surface / no paid API |

### [HDR] #858 | 18:49 SGT 05-07-26 | v0.62.503 — merge PR #1506 (Soleat manual localisation workflow prep) | 10 files | PR #1506 |

> Operator: "merge 1506"

- **PR #1506** (`docs/soleat-manual-l10n-prep` → `main`) was opened as **draft**; marked
  ready-for-review and **squash-merged** to `main` (merge commit `fd621ca`).
- **`docs/UserManual/translation-status.json`** — adds global translation gates, updates English
  source status, records recommended language order (zh + ja first for native-script food-term
  pressure testing).
- **`docs/UserManual/l10n/README.md`** (NEW) + placeholders for **zh / ja / id / fr / de / es / ru**
  — no translated manual content yet; explicitly gated on English master, glossary, command
  aliases, help IDs, image registry, and food-term review being complete.
- **`docs/UserManual/FOOD-TERM-REVIEW-QUEUE.md`** (NEW) — lists ambiguous food terms + examples
  requiring meaning-aware review before translation.
- Docs-only; no application code touched. No `package.json` version bump in the PR itself (stays
  at 0.62.503 landed by the Register-catchup rollup above).
- **[VERIFICATION (sandbox)]** N/A — no code changed; PR's review notes confirm translation
  remains blocked until the review gates listed above are complete, and that search examples
  should be translated by intent (not literally).
- **[STATUS]** PROD — merged to `main`.
- **[TEST]** N/A (docs-only).
- **[KNOWN GAPS / NEXT]** Translation itself still gated on the six review items above; no
  translated body copy exists yet in any of the 7 placeholder folders. Standing gaps from the
  rollup entry above (companion `doc/Technical/` arc entry + `vault/v0.62.502/` snapshot; O-47
  live re-confirm; O-49 `uv` blocked on operator; O-50 SGT bot line; O-51 FR Michelin BBOX) all
  carried forward unchanged.

## Confirmation Gates (PR #1506 merge)
| Gate | Status | Note |
|------|--------|------|
| G1 | ➖ | no version bump — docs-only, no code changed (stays at 0.62.503) |
| G2 | ➖ | no destructive action |
| G3 | ✅ | serial 857→858; commit 975→976 |
| G4 | ➖ | no paid external API call |

### [HDR-amend] serial re-anchor | 13:34 SGT 06-07-26 | corrects a numbering collision in the #858 record above

The `#858` header and `serial 857→858; commit 975→976` note on the PR #1506 record above were
derived from this branch's own snapshot (baseline 857/975), taken before this branch had visibility
into concurrent work. **PR #1508 (v0.62.504, quiet the Vault-Index + Pulse heartbeat logs)** merged
to `main` from the same 857/975 baseline and *also* claimed journal 858 / commit 976 for itself
(see `journal-0_62_504-06_07_26-0726.md`) — a genuine collision between two independently-prepared
branches, not simple staleness. Per AU-1 (add, never compress) the original `#858` text above is
left verbatim; the PR #1506 record's **actual** serial number is re-anchored here to the first free
slot after PR #1508 landed: **journal 858→859, commit 976→977**. `doc/.serial-state.yml`'s
counters/anchor block reflects this final **859/977** state directly (build stays at 615 — PR #1506
is docs-only, no code build).

## Confirmation Gates (PR #1506 merge, corrected)
| Gate | Status | Note |
|------|--------|------|
| G1 | ➖ | no version bump — docs-only, no code changed (stays at 0.62.503) |
| G2 | ➖ | no destructive action |
| G3 | ✅ | serial 858→859; commit 976→977 (corrects the collision above) |
| G4 | ➖ | no paid external API call |
