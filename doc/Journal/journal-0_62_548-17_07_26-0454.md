(№ 905 - 17-07 '26 04:54 SGT) v0.62.548

# Journal — soleat v0.62.548 (Hawker landscape = Cuisine carousel · bus-stop descriptions · toggle to bottom-right)

> Operator (IMG_0674 portrait + IMG_0675 landscape + IMG_0677, iPad Pro, v0.62.547), three points:
> 1. In **landscape** it should be like the **Cuisine TMA carousel** result cards.
> 2. In the **same Hawker card, both orientations**, give the bus stops **more explanation** —
>    "Bus Stop 41129 · Opposite S'pore Bible College" — and the MRT station "NE9 Boon Keng";
>    the bus-stop + MRT pills should follow the Cuisine/Transport standard (all three consistent).
> 3. The **map toggle** should sit **bottom-right** (beside end / down) — its top-right spot
>    (IMG_0677) collided with Telegram's fullscreen ⌄ ··· controls.
>
> Answered clarifier (landscape structure): **Full carousel** — drop the left glass panel; map
> fills the whole screen; cards become a bottom-docked horizontal swipe carousel exactly like
> Cuisine; region chips + a slim header ride a compact translucent top bar over the map.

## New

### [HDR] #905 | 04:54 SGT 17-07-26 | v0.62.548 — landscape carousel + bus descriptions + toggle move | 2 files | PR #(pending) |

- **[DELTA 1 — landscape = Cuisine carousel].** `web/hawker/src/App.jsx`: `landscapeTablet` now
  returns a dedicated layout — full-bleed `HawkerMapPanel fill` + a **slim translucent top bar**
  (header title + weather + LocaleToggle + NEA pill, then the region chips), cleared below
  Telegram's fullscreen controls via `--tg-content-safe-area-inset-top` — and a **bottom-docked
  horizontal snap carousel** of the centre cards (`flex overflow-x-auto snap-x snap-mandatory`,
  cards `snap-center shrink-0 basis-[68%] sm:basis-[44%] lg:basis-[30%]`), mirroring
  `web/cuisine/.../ResultCarousel.jsx`. The v0.62.546 **left glass panel is removed**.

- **[DELTA 2 — one shared card renderer + bus-stop descriptions].** Extracted `renderCentreCard(c, i)`
  used by BOTH the landscape carousel and the portrait/mobile grid (both orientations, single
  source). The bus-stop pill now renders **`🚌 {code} · {description}`** (falls back to `roadName`)
  — the data already carried `description`/`roadName` (only the Maps deep-link used it); this
  matches the map InfoWindow's `🚌 code desc` standard (`HawkerMapPanel.jsx:311`). Bus stops now
  stack one-per-line (`flex flex-col`) so the description fits. MRT stays `codeHex` line-code chips
  + station name (`NE9 Boon Keng`), already consistent with the InfoWindow + Cuisine standard.

- **[DELTA 3 — toggle to bottom-right].** New module-level `MapToggle` docked
  `fixed right-3 bottom:calc(3.5rem + safe-area)` — just above the FooterNav (down / end),
  per operator. Replaces the v0.62.547 top-right pill that overlapped Telegram's ⌄ ··· cluster.
  `🗺 Map` hides the list/carousel to reveal the full map; `📋 List` brings it back.

- **[DELTA — housekeeping].** Portrait/mobile stacked return cleaned of the now-dead
  `landscapeTablet` branches (landscape returns early). `package.json` 0.62.547 → **0.62.548**;
  Journal #905 + serial-state (commit 1022→**1023**, build 647→**648**, journal 904→**905**;
  re-anchor 17-07 '26 04:54 SGT, prior 904 demoted verbatim AU-7).

- **[VERIFICATION (sandbox)].** **Hawker vite build green** (42 modules); **full `npm test`
  3719/3719 across 145 files**. ⚠️ The carousel feel, bus-description fit, and toggle position can
  only be confirmed on the real iPad — shipping live.

- **[STATUS]** STAGED → PROD on merge (operator "make it live").

- **[TEST]** Suite excludes `web/`; the JSX is validated by the vite build (tag balance + imports).
  No new unit test (DOM-layout change).

- **[KNOWN GAPS].**
  1. 👀 **iPad recheck** — landscape reads like the Cuisine carousel (swipe, peek)? Bus-stop
     descriptions fit in the card without crowding? Toggle sits cleanly by down / end? Card `basis`
     width per breakpoint still tunable.
  2. 🧭 Portrait bus-stop pills also now carry descriptions (both orientations, per operator) —
     confirm they don't over-wrap the 2-col grid on portrait.
  3. 🧭 Port the pattern (carousel + bus descriptions) to **Cuisine + Transport** if the operator
     wants the browse surfaces aligned (Cuisine already uses this carousel for results).

## Confirmation Gates
| Gate | Status | Note |
|------|--------|------|
| G1 | ✅ | PATCH bump 0.62.547→0.62.548; additive/restyle, gated to tablet landscape; build + 3719 tests green |
| G2 | ➖ | no destructive action (dead-branch cleanup only) |
| G3 | ➖ | no new decision rule |
| G4 | ➖ | no external paid API call |
