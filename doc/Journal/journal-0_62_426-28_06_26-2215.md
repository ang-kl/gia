(№ 780 - 28-06 '26 22:15 SGT) v0.62.426

# Journal — soleat v0.62.426 (Sketchbook — follow the SAMPLE palette, cobalt + ice)

> Operator: "you didn't follow the colour codes and the whole look and feel of the sample" (added
> 6 sample PNGs to UI/sketchbook).

## New

### [HDR] #780 | 22:15 SGT 28-06-26 | v0.62.426 — sample palette reskin | 3 files | PR #(pending) |
- **[ROOT CAUSE]** The clipboard TMA used `tg-*` tokens bound to the runtime **Telegram theme**
  CSS vars (dark / Telegram-blue defaults). So it rendered in the user's Telegram theme, NOT the
  sample's fixed **cobalt + ice-blue** palette.
- **[DELTA]**
  - `tailwind.config.js` — `tg-*` remapped to the FIXED sample hex (HANDOFF §2): bg `#eef2fb`,
    card/header `#ffffff`, text `#141a36`, hint `#7e88a8`, accent cobalt `#2b59c9`, border
    `#e4e8f2`; added `sk-soft #eaf0fd`, `sk-head #f5f8fd`, `sk-pin #d1495b`, `sk-star #e0a500`,
    `sk-open #1f8a5b`. The whole app reskins with no per-class churn.
  - `styles.css` — bottom-sheet + drag-ghost forced to the light sample palette (were Telegram
    vars → could be dark).
  - `DrawerRow.jsx` — count pill → cobalt `sk-soft` (was `bg-white/5`, invisible on white).
- **[VERIFICATION (sandbox)]** Real `vite build` OK (50 modules; css 14.9 kB).
- **[STATUS]** PROD — clipboard TMA rebuild; redeploys per PR.
- **[OPEN — operator decision]** Logo conflict: the sample shows a **teal-blue gradient ☼ tile**,
  but `soleat-icon.png` (the other TMAs) is a **red food circle**. Asked which to use.
- **[STILL PENDING]** card → Cuisine `ResultCard` (store-venue-on-Copy) [B]; DrawerRow Edit +
  area/loc subline [D]; Cabinets ★DEFAULT/OPEN badges + counts + touched [E].

## Confirmation Gates

| Gate | Status | Note |
|------|--------|------|
| G1 build/parse | ✅ | real vite build (50 modules) |
| G2 tests | ⏳ | CI |
| G3 journal/serial | ✅ | serial 779→780 |
| G4 live deploy | ⏳ | redeploy + eyeball |
