(№ 794 - 29-06 '26 12:35 SGT) v0.62.440

# Journal — soleat v0.62.440 (Sketchbook item 4 — filter chips = folio dropdown)

> Operator: the "Cuisine & Filter" and "Pick Local Dish" chips should drop DOWN like a folio tab
> (not a bottom-up sheet); Cuisine & Filter should read like the Cuisine tab.

## New

### [HDR] #794 | 12:35 SGT 29-06-26 | v0.62.440 — folio dropdown filter | 2 files | PR #(pending) |
- **`Shell.jsx`** — the two chips are now **folio tabs**: tapping a chip squares its bottom corners
  and drops a bordered `rounded-b` panel directly beneath it (no bottom sheet). **Cuisine & Filter**
  → "All" + the cuisine **pick-list** (with counts); **Pick Local Dish** → keyword input + a small
  Save pill. ▾/▴ indicator; ✕ on an active chip clears.
- **`App.jsx`** — passes `cuisineOptions` + `onSetCuisine`/`onSetDish`; removed the bottom
  `FilterSheet` usage + `filterSheet` state + import (FilterSheet.jsx now unused).
- **[VERIFICATION (sandbox)]** real `vite build` OK (50 modules).
- **[STATUS]** PROD — clipboard TMA rebuild.
- **[NOTE]** The deeper "exact Cuisine criteria-search facets" (rating / price / open toggles) are
  not ported — the chip filters saved cards by cuisine/dish. Can extend to the full facet set if
  wanted. This completes the visible folio-dropdown behaviour the operator asked for.

## Confirmation Gates
| Gate | Status | Note |
|------|--------|------|
| G1 | ✅ | vite build |
| G2 | ⏳ | CI |
| G3 | ✅ | serial 793→794 |
| G4 | ⏳ | redeploy |
