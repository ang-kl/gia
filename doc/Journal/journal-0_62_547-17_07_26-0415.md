(№ 904 - 17-07 '26 04:15 SGT) v0.62.547

# Journal — soleat v0.62.547 (Hawker: Cuisine-style centre cards + tablet "list" toggle)

> Operator (on the responsive-tablet arc): "1. approach — **A** [restyle the Hawker
> cards in place]. 2. Apply with **both orientation** with an option **like Cuisine TMA
> to list in the footer**. Use the coding to list." So: give the Hawker centre list the
> same card look as the Cuisine result cards (name header, stall/status chip, coloured
> MRT-line pills, pill actions) in every layout, and add a Cuisine-style toggle to
> collapse the list and reveal the full map.

## New

### [HDR] #904 | 04:15 SGT 17-07-26 | v0.62.547 — Cuisine-style Hawker cards + list toggle | 3 files | PR #(pending) |

- **[DELTA — approach A, restyle in place].** `web/hawker/src/App.jsx`:
  - **Centre cards restyled to the Cuisine result-card look** (both orientations, phones
    included — approach A restyles the one list, it is not layout-gated): a
    `rounded-lg border bg-tg-card/95` card with a **`text-tg-text` name header**
    (`{i+1} · {name}` + 🆕), a `📇 address` line, a **stall/status chip row**
    (`🍳 {n} stalls` + a green/amber status pill via the new `statusIsOpen` helper),
    the nearest-MRT row as **`codeHex`-coloured line-code chips** + station name +
    `🚌 {code}` bus pills (each a Google-Maps deep-link), and **rounded-full pill
    actions** (`📍 Maps`, Save-to-chat). Mirrors `web/cuisine/.../ResultCard.jsx`.
  - New helpers `statusLabel(centre, lang)` (the localised NEA status on its own —
    `formatStalls` joins it with the count; here it is wanted separately) and
    `statusIsOpen(centre)` (`/^(oper|exist)/i` → green, else amber).
  - `import { codeHex } from './lib/mapOverlays.js'` for the official MRT-line colours.

- **[DELTA — tablet "list" toggle, "like Cuisine TMA to list"].**
  - New `const [listHidden, setListHidden] = useState(false)`.
  - A **fixed pill** (top-right, cleared below Telegram's fullscreen top controls via
    `--tg-content-safe-area-inset-top`), shown only when **`isWide`** (tablet/desktop),
    that flips `listHidden`: `🗺 Map` (list shown → tap to reveal the map) / `📋 List`
    (map full → tap to bring the list back). Fixed (`z-30`) so it stays reachable even
    when the landscape glass panel is hidden. Mirrors the Cuisine TMA's hide-results.
  - The **landscape glass panel** gets `hidden` when `landscapeTablet && listHidden`
    (full-bleed map); the card list is wrapped `{!(isWide && listHidden) && …}` so in
    **portrait** the toggle hides just the cards, leaving the inline map on top.
  - `web/hawker/src/i18n.js` — new `btn.showMap` / `btn.showList` (en/fr; other locales
    degrade to English via `t()`'s `entry[l] ?? entry.en`).

- **[VERIFICATION (sandbox)].** `node --check` on `i18n.js` + `tg.js` OK; **Hawker vite
  build green** (42 modules); **full `npm test` 3719/3719 across 145 files**. ⚠️ The card
  look + toggle FEEL can only be confirmed on the real iPad — shipping live.

- **[STATUS]** STAGED → PROD on merge (operator "make it live").

- **[TEST]** Suite excludes `web/`; the JSX card markup is validated by the vite build
  (tag balance + import resolution). No new unit test (DOM-layout change).

- **[KNOWN GAPS].**
  1. 👀 **iPad recheck** — cards read like the Cuisine cards in both orientations? The
     `🗺/📋` toggle collapses the panel to full map and back? Card density / chip sizes
     still tunable.
  2. 🧭 **Toggle-label interpretation** — "option like Cuisine TMA to list in the footer"
     was read as a hide/show-list toggle (mirroring Cuisine's hide-results); flag to the
     operator if a literal footer *link* was meant instead.
  3. 🧭 Port the confirmed pattern (fullscreen re-read + `ipados` gate + cards) to
     **Cuisine + Transport**.

## Confirmation Gates
| Gate | Status | Note |
|------|--------|------|
| G1 | ✅ | PATCH bump 0.62.546→0.62.547; additive (restyle + gated toggle); build + 3719 tests green |
| G2 | ➖ | no destructive action |
| G3 | ➖ | no new decision rule |
| G4 | ➖ | no external paid API call |
