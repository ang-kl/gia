# Journal — v0.62.593 (Hawker: a hair more headroom on the landscape title row)

_(№ 937 — 18-07 '26 17:58 SGT)_

## New

### [HDR] v0.62.593 — Hawker: nudge the landscape carousel title down so it isn't clipped

- **[INTENT]** Operator: *"give the landscape title row a hair more headroom."* On the short landscape
  carousel top bar, the "🍚 Hawker Centre (2025)" title read slightly clipped by the bar's top edge.

- **[DELTA]** `web/hawker/src/App.jsx` — the carousel (landscape) top-bar `paddingTop` calc bumped from
  `… + 0.375rem` to `… + 0.625rem` (still `var(--tg-content-safe-area-inset-top, env(…))` + a hair), so
  the title/chips sit a touch lower and clear the top edge.

- **[VERIFICATION (sandbox)]** Hawker `vite build` green; full `npm test` **3719/3719 across 145 files**;
  `package.json` + `package-lock.json` 0.62.592 → 0.62.593; `.serial-state.yml` commit 1071→1072, build
  694→695, journal 936→937, re-anchor 18-07 '26 17:58 SGT. ⚠️ Live-only. Branch restarted from
  `origin/main` after #1600 merged.

- **[STATUS]** DRAFT → squash-merge to `main` on green CI (auto-merge SQUASH).

- **[TEST]** Existing suite 3719/3719. Pure CSS spacing (verified by build).

- **[KNOWN GAPS]** Cuisine portrait-tablet listing: freeze header + independently-scrollable columns
  (awaiting the operator's map-placement call). Cuisine `tg.js` safe-area gap; folio-tab folder
  connection; dark-mode carousel glass; Transport port.

## Confirmation Gates
| Gate | Status | Note |
|------|--------|------|
| G1 | ✅ | PATCH bump 0.62.592 → 0.62.593; Hawker build + 3719 tests green. |
| G2 | ➖ | no destructive action. |
| G3 | ➖ | no new decision rule. |
| G4 | ➖ | no external paid API call. |
