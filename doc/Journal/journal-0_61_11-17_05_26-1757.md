(№ 222 - 17-05 '26 17:57 SGT) v0.61.11

# Journal — soleat v0.61.9 → v0.61.11 (map-refinement arc, 3-PR stack)

> Operator reviewed the v0.61.x maps and gave ~12 refinements + new features across the three
> TMAs. Delivered as a 3-PR stack, "fewer, larger" per the operator's branch-strategy choice,
> continuing the 0.61.x PATCH line. Each PR stacks on the previous; PR 1 stacks on #494.

## New

### [HDR] #213 | 17:43:00 SGT 17-05-26 | v0.61.9 — map polish: zoom row, square pins, per-layer overlay radius | ~4 kb | 9 files | 1 PR | 1 build |

- **[INTENT]** First of the 3-PR map-refinement arc. Operator asks: zoom controls as a small
  horizontal row flush at the map's top-right; square station pins on the transport map; drop
  the "Showing EWL · N stations" banner and fold the per-line station list into the focused-line
  panel; split the flat overlay radius into per-layer reaches.

- **[DELTA]**
  - **`web/transport/src/components/MrtMapPanel.jsx`** — zoom/expand controls changed from a
    vertical stack to a horizontal row (`w-7` buttons); `stationDotNode` drops `border-radius`
    so station markers render square; the focused-line banner block removed.
  - **`web/hawker/src/components/HawkerMapPanel.jsx`**, **`web/cuisine/src/v2/components/MapPanel.jsx`**
    — same horizontal zoom-row treatment.
  - **`web/transport/src/components/LineStatusPanel.jsx`** — gains a collapsible `<details>`
    dropdown listing the focused line's stations as `[code · name]` in running order.
  - **`web/transport/src/data/line-paths.js`** — exports `parseCode`, `PREFIX_TO_LINE`, and a
    new `lineStations(stations, lineCode)` helper feeding the dropdown.
  - **`web/{transport,hawker,cuisine}/src/.../mapOverlays.js`** (byte-identical ×3) — flat
    `RADIUS_M` 1000 m / `TRAIN_RADIUS_M` 1500 m replaced by `RADIUS_ATTRACTIONS_M` 800 m,
    `RADIUS_NEAR_M` 400 m (carpark / taxis / exits), `TRAIN_RADIUS_M` 800 m.

- **[VERIFICATION (sandbox)]**

  ```bash
  $ node --check web/transport/src/lib/mapOverlays.js web/transport/src/data/line-paths.js  → OK
  $ npm run build:transport / build:hawker / build:cuisine  → all green
  $ npm test -- --run                                       → 1924 passed (1924)
  ```

- **[STATUS]** DRAFT → PR #495 (base `claude/journal-version-fix`, stacks on #494).

- **[TEST]** Open any TMA map: zoom row sits top-right as a horizontal strip; transport station
  pins are square; tapping a line shows its station dropdown in the focused-line card; toggle an
  overlay chip and confirm attractions/train reach ~800 m, carpark/taxi/exits ~400 m.

- **[KNOWN GAPS]** Exits-on-station-select deferred to PR 2 (needs the `station-exits.json`
  data). "Station codes everywhere" is a pervasive minor — the transport InfoWindow + hawker
  card already carried codes.

### [HDR] #214 | 17:52:00 SGT 17-05-26 | v0.61.10 — rich map pins + realtime crowd & accidents | ~12 kb | 12 files | 1 PR | 1 build |

- **[INTENT]** Second PR: rich InfoWindow templates for attractions and hawker map-pins, plus
  three realtime layers — platform crowd, traffic accidents, and per-station exit/bus/taxi
  context. Operator decisions folded in: drop the "exit with a lift" line (no lift data exists);
  omit operating hours from the station popup (no per-station data).

- **[DELTA]**
  - **`scripts/build-geo-overlays.js`** — emits `data/station-exits.json` (station → exits,
    `EXIT_CODE` verbatim — letter codes on older stations, number codes on TEL/newer); enriches
    each attraction in `geo-attractions.json` with its nearest station's exit list.
  - **`data/station-exits.json`** (new) — 183-station exit index.
  - **`index.js`** — new `loadStationExits()` / `exitsForStation()`; `/api/hawker/centre-transit`
    extended to return `station.exits`; new endpoints `/api/transport/crowd`,
    `/api/geo/incidents` (250 m), `/api/transport/station-context`.
  - **`web/{transport,hawker,cuisine}/src/.../mapOverlays.js`** — attraction InfoWindow gains a
    station-exits line.
  - **`web/hawker/src/components/HawkerMapPanel.jsx`** — map-pin InfoWindow shows the full
    transit template (operating status, address, 2 bus stops, station code/name/line, exits),
    transit half lazy-fetched and cached per centre.
  - **`web/transport/src/components/MrtMapPanel.jsx`** — fetches `/api/transport/crowd`;
    crowded stations (`h`) blink; station InfoWindow shows crowd level + exits/bus/taxi from
    `/api/transport/station-context`.
  - **`web/transport/src/i18n.js`** — crowd / exits / bus / taxi labels (en + fr).

- **[VERIFICATION (sandbox)]**

  ```bash
  $ node --check index.js transport.js scripts/build-geo-overlays.js   → OK
  $ node scripts/build-geo-overlays.js   → station-exits: 183 stations
  $ npm run build:transport / build:hawker / build:cuisine  → all green
  $ npm test -- --run                                       → 1924 passed (1924)
  ```

- **[STATUS]** DRAFT → PR #496 (base `claude/map-polish-radius`, stacks on #495).

- **[TEST]** Tap an attraction / hawker pin → full template renders; with `LTA_ACCOUNT_KEY` set,
  crowded station pins blink and the station popup lists exits + bus + taxi; run a cuisine search
  near a road incident → ⚠️ marker appears within 250 m.

- **[KNOWN GAPS]** Realtime layers need `LTA_ACCOUNT_KEY` (Railway) — degrade to empty without
  it. Station-context bus/taxi are station-level (≤400 m), not strictly grouped per exit.

### [HDR] #215 | 17:56:00 SGT 17-05-26 | v0.61.11 — cuisine result emphasis on the train overlay | ~6 kb | 5 files | 1 PR | 1 build |

- **[INTENT]** Final PR: when cuisine search results are on the map, the train overlay
  highlights the line segments near the 3 stations closest to the search anchor and dims the
  rest, drawing square half-transparent station markers along the visible lines.

- **[DELTA]**
  - **`web/{transport,hawker,cuisine}/src/.../mapOverlays.js`** — the `train` layer becomes a
    composite of polylines + square station markers; new `setTrainEmphasis(lat,lng)` controller
    method; station markers show only while emphasis is active (so the hawker train overlay,
    which never sets emphasis, is unchanged).
  - **`web/cuisine/src/v2/components/MapPanel.jsx`** — toggles `setTrainEmphasis` on the search
    anchor whenever `venues` are present.

- **[VERIFICATION (sandbox)]**

  ```bash
  $ node --check web/transport/src/lib/mapOverlays.js   → OK
  $ npm run build:cuisine / build:hawker / build:transport  → all green
  $ npm test -- --run                                       → 1924 passed (1924)
  ```

- **[STATUS]** DRAFT → PR #497 (base `claude/rich-pins-realtime`, stacks on #496).

- **[TEST]** Run a cuisine search with the train overlay on → the nearby line stretch is bold,
  the rest of each line faint, station pins square + translucent; confirm the hawker train
  overlay looks the same as before.

- **[KNOWN GAPS]** Emphasis "hot" segment threshold is a fixed 500 m around each of the 3
  nearest stations; not operator-tuned.

## Confirmation Gates Satisfied

| Gate | Authority | Stamp |
| :--- | :--- | :--- |
| G1 — version bump (PATCH ×3: 0.61.9 / 0.61.10 / 0.61.11) | _operator: 0.61.x PATCH sequence_ | 17-05 '26 |
| G2 — destructive action | _n/a — no deletions_ | — |
| G3 — new decision rule | _n/a_ | — |
| G4 — paid external API call | _n/a — LTA DataMall within free quota_ | 17-05 '26 |

## Amendments

(none)

## Deleted

(none — the focused-line banner block in `MrtMapPanel.jsx` was replaced by the
`LineStatusPanel` station dropdown; no document content was deleted.)
