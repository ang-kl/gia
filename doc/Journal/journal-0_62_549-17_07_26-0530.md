(№ 906 - 17-07 '26 05:30 SGT) v0.62.549

# Journal — soleat v0.62.549 (Hawker: pill-tap map highlight + toggle · 3-card opaque carousel · landscape header below TG buttons)

> Operator (IMG_0683 portrait + IMG_0684 landscape + IMG_0685 close-up, iPad Pro, v0.62.548), three points:
> 1. Tapping a **Station / Bus-stop pill inside a card** should **highlight the point on the embedded
>    map for 3 s** (like the Cuisine TMA map effect), and toggle the tapped pill to a **pale toggled
>    colour** (light/dark aware). Tapping the toggled pill again **opens external Google Maps** and
>    returns the pill to un-toggled. All orientations, tablet/desktop/phone.
> 2. **Landscape carousel** should show **three centre cards in focus with an opaque background**
>    (= the card background colour, not translucent); **one** card in focus for a phone aspect ratio.
> 3. **Landscape**: move the **NEA link + temperature down below** Telegram's top-right system buttons.

## New

### [HDR] #906 | 05:30 SGT 17-07-26 | v0.62.549 — pill-tap highlight/toggle + 3-card opaque carousel + header re-flow | 3 files | PR #(pending) |

- **[DELTA 1 — two-tap station/bus pill → map highlight + toggle].**
  - `web/hawker/src/lib/mapOverlays.js` — `flashPin(lat, lng, ms=2000)` now takes a duration (pulse
    iterations follow it); new controller method **`highlightLoc(lat, lng, ms=3000)`** pans + zooms
    (≥16) + pulses a 3 s halo.
  - `web/hawker/src/components/HawkerMapPanel.jsx` — new global **`window.__giaHawkerHighlight(lat, lng)`**
    → `highlightLoc(…, 3000)`, torn down on unmount.
  - `web/hawker/src/App.jsx` — the station + bus-stop pills are now **two-tap `<button>`s**. New
    `activePill` state (one toggled at a time) + `handlePillTap(id, lat, lng, url)`: 1st tap →
    `__giaHawkerHighlight` + toggle this pill to **`bg-tg-accent/20 border-tg-accent`** (the pale,
    theme-aware toggled colour); 2nd tap on the same pill → `openLink(url)` (external Google Maps) +
    clear the toggle. Station highlights on `tr.station.lat/lng`, bus on `b.lat/b.lng` (both already
    in the `/api/hawker/centre-transit` payload).

- **[DELTA 2 — 3-card opaque carousel].** `App.jsx` — carousel card wrapper `basis-[82%] md:basis-[30%]`
  (≈ 3 cards in focus on tablet/desktop, 1 on a phone aspect); the shared card surface is now
  **opaque `bg-tg-card`** (was `bg-tg-card/95`), per operator "opaque background = the card colour."

- **[DELTA 3 — landscape header re-flow].** `App.jsx` — the landscape top bar's **temperature +
  LocaleToggle + NEA** move OFF row 1 (where they sat level with Telegram's ⌄ ··· buttons) onto the
  **region-chips row (row 2), right-aligned**; row 1 is the title alone, `pr-24` to clear the buttons.

- **[DELTA — housekeeping].** `package.json` 0.62.548 → **0.62.549**; Journal #906 + serial-state
  (commit 1023→**1024**, build 648→**649**, journal 905→**906**; re-anchor 17-07 '26 05:30 SGT,
  prior 905 demoted verbatim AU-7).

- **[VERIFICATION (sandbox)].** `node --check mapOverlays.js` OK; **Hawker vite build green** (42
  modules); **full `npm test` 3719/3719 across 145 files**. ⚠️ The map highlight, pill-toggle two-tap,
  3-card fit, and header position are live-only (iPad) — shipping live.

- **[STATUS]** STAGED → PROD on merge (operator "make it live").

- **[TEST]** Suite excludes `web/`; JSX validated by the vite build. No new unit test (DOM/interaction).

- **[KNOWN GAPS].**
  1. 👀 **iPad recheck** — pill tap highlights the right pin for 3 s + toggles pale, 2nd tap opens
     Maps + un-toggles? 3 cards in focus, opaque? NEA + temp clearly below the TG buttons?
  2. 🧭 In the **portrait/mobile stacked** layout the inline map sits ABOVE the cards, so a pill-tap
     highlight may be off-screen (no auto-scroll) — confirm that's acceptable or add a scroll-to-map.
  3. 🧭 Port the pill-highlight + carousel to **Cuisine + Transport** if the browse surfaces align.

## Confirmation Gates
| Gate | Status | Note |
|------|--------|------|
| G1 | ✅ | PATCH bump 0.62.548→0.62.549; additive/interaction; build + 3719 tests green |
| G2 | ➖ | no destructive action |
| G3 | ➖ | no new decision rule |
| G4 | ➖ | no external paid API call |
