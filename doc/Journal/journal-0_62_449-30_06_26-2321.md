(№ 811 - 01-07 '26 10:50 SGT, renumbered №807→№810 at 13:50 SGT, then №810→№811 at 14:11 SGT — see Amendments below) v0.62.449 (stocktake, no code bump)

# Journal — soleat / Gia4lunch — Full-project feature stocktake as of 30 June 2026

> Operator (session task, verbatim): "stocktake all the features in this project as of 30 June 2026." Delivered
> via session branch `claude/feature-stocktake-june-2026-nw6ooz`, as a **docs-only** PR — no application code
> touched, no version bump (follows the `doc(Feature #57): …` precedent for a Feature-folder entry made outside
> the normal per-PR version cadence).

### [HDR] #810 | 10:50 SGT 01-07-26 | Full-project feature stocktake as of 30 June 2026 | doc/Feature · doc/.serial-state.yml | 0 PRs (new doc-only PR) | 0 explicit build

- **[INTENT]** The repo's `doc/Feature/` canonical spec had not been updated since `feature-0_62_153-17_06_26-1730.md`
  (PR #1105) other than a scoped `feature-0_62_332-clipboard.md` (PR #1391) covering only the 4-PR Clipboard
  storage/API/TMA/polish cycle. Between PR #1106 and the 30-June-2026 cutoff (PR #1429, commit `9fa372e0`,
  v0.62.449), **316 more commits** landed with no Feature-folder synthesis — a stocktake was overdue per
  `doc/CLAUDE-FULL.md` §17.3's "per arc milestone (every ~10-15 versions)" cadence, which this arc (≈296
  versions) badly exceeded. The operator's ask was to produce that catch-up as a full, dated stocktake.

- **[DELTA]**
  - **`doc/Feature/feature-0_62_449-30_06_26-2321.md` (new, №58).** Reproduce-by-reference (CLAUDE-FULL §17.1)
    from the two prior Feature files (unchanged, not re-typed); §1 restates every active surface in one table,
    grounded directly in `index.js`'s `registerCommandsMenu()` command list and the `web/*` directory listing
    at the cutoff commit; §2 appends 13 new arc sections (G-A…G-M) synthesising the 316-commit gap — the
    Cuisine TMA floating-dock consolidation, the 1,505-entry curated-dish-note pipeline, the explain-first
    local-classic UX, the 8-language i18n expansion (id/ru/de then zh/ja/es + 5 native-script batches), the
    1,643-explanation dish-drafter + Gemini prose-translation pipeline, the Search-Insights demand strip,
    the location/nearby-places overhaul, price-tier/forex/Set-Meal work, the cross-TMA hawker-overlay port,
    result-card evolution, the **Sketchbook TMA v2 rebuild** (the largest single item — chrome, cabinets list,
    filters, archive, extending past the Clipboard doc's #1308 cutoff), a bug/crash/infra-fix roundup, and an
    icon/branding housekeeping note. Adds RF-30…RF-33 to the Removed table.
  - **`doc/Journal/journal-0_62_449-30_06_26-2321.md` (this file, №811 — see Amendments below for the
    №807→№810→№811 renumbering history).** Records the stocktake session itself per the repo-root `CLAUDE.md`
    standing rule ("After opening any PR… record it in the Journal").
  - **`doc/.serial-state.yml`.** `documents.feature` 56 → 58 (accounts for the un-bumped №57 clipboard doc plus
    this session's №58); `documents.journal` 806 → 807 at draft time, reconciled to 810 at 13:50, reconciled
    again to 811 at 14:11 (see Amendments); `commit` 924 → 925 at draft time, reconciled to 928, then 929;
    `last_anchor_time` re-anchored three times across the session. `build` left unchanged at each of this
    session's own bumps (docs-only, no code build ran) but tracks main's real value (567) after each merge.
  - **git history:** repo was a 50-commit shallow clone at session start; ran `git fetch --unshallow` (read-only,
    non-destructive) to recover full history back to the v0.62.153 boundary needed for accurate PR-range
    citations.

- **[VERIFICATION (sandbox)]** Docs-only change — no `node --check`, `npm test`, or `npm run build:*` applicable
  (gia-preflight's triggers are index.js / bot handlers / `/s` search / venue rendering / i18n.js / fuzzy
  matchers, none of which this PR touches). Verified instead by: (a) cross-checking every arc's PR-number
  citations against the extracted `git log --reverse 997d3209..9fa372e0` commit list (316 lines, read in full);
  (b) confirming the 30-June cutoff commit (`9fa372e0`, 2026-06-30 23:21 SGT) is the last commit dated on or
  before 30-06-2026 by inspecting the surrounding commit timestamps; (c) confirming both reproduce-by-reference
  targets (`feature-0_62_153-…`, `feature-0_62_332-clipboard.md`) remain in `doc/Feature/`, not `doc/Archive/`,
  satisfying CLAUDE-FULL §17.1 Rule R-1; (d) grounding §1's Active Surfaces table in the live `index.js`
  `enCommands`/`frCommands` arrays and the `web/` directory listing rather than from memory.

- **[STATUS]** DRAFT PR opened against `main` from `claude/feature-stocktake-june-2026-nw6ooz`. No deploy
  implication (docs only; Railway build/deploy path untouched).

- **[TEST]** N/A — no test suite covers `doc/`. The standing `vitest` suite was not run since no source file
  changed.

- **[KNOWN GAPS]**
  - `doc/Technical/` and `doc/Register/` remain stale (last touched at v0.62.153 and v0.62.76 respectively) —
    logged inside the new Feature doc's own Known Gaps section; a Technical + Register catch-up is the natural
    follow-up session.
  - v0.62.450 → v0.62.453 (1 July 2026, PRs #1433–#1437) landed after this stocktake's cutoff and are explicitly
    out of scope per the operator's "as of 30 June 2026" framing.
  - §2's PR citations are representative (not every one of the 316 commits is individually cited), consistent
    with the citation density already established in the prior Feature files.
  - `doc/VibeCodingRecord/` regen and a `vault/v0.62.449/` snapshot were **not** performed this session (both
    are "if convenient" per the repo-root `CLAUDE.md` §4 item 4/§16.1, not mandatory) — still due.

| Gate | Question | Verdict |
|------|----------|---------|
| **G1** | Operator request captured verbatim where it matters? | ✅ — quoted in this file's preamble. |
| **G2** | Source-of-truth = current `main` code / actual merged history (not invented)? | ✅ — every §2 arc and §1 surface is grounded in `git log` output or a live grep of `index.js` / `web/`, read in full before writing. |
| **G3** | Journal recorded per the standing per-PR rule? | ✅ — this HDR, for the stocktake PR itself. |
| **G4** | Reversals disclosed (AU-7), not silently rewritten? | ✅ — RF-30…RF-33 in the new Feature doc; no prior content amended or compressed, only reproduced by reference or appended. |

---

### Amendment (13:50 SGT 01-07-26) — PR #1438 conflict resolution: journal serial №807 → №810

**AU-7: quoting prior content verbatim before the change.** This entry originally self-stamped as **№807** (see the
unmodified DELTA/status text above, still readable). While PR #1438 sat open as a draft, `main` advanced six more
commits — v0.62.450 → v0.62.455 (PRs #1433–#1441: the "Pick Local Dish mirrors Cuisine TMA" arc, a
`curate-dish-sources` workflow fix, a Cuisine "Dishes" reveal pop-up, a dish-popup title tweak, and a Sketchbook
tile-icon fix) — three of which added their own Journal entries, independently claiming **journal serials
№807, №808, №809** (`journal-0_62_453-01_07_26-1140.md`, `-454-…`, `-455-…`) off the same `documents.journal: 806`
base this session read at draft time. Merging `origin/main` into this branch surfaced the resulting
`doc/.serial-state.yml` conflict (both sides had incremented `journal`/`commit` from the same starting counters).

**Resolution:** this file is renumbered **№807 → №810** (main's true 809 + this session's own +1), and
`doc/.serial-state.yml` is reconciled to `documents.journal: 810`, `commit: 928` (main's 927 + this session's +1),
`build: 566` (unchanged — main's bump, not this docs-only session's), `documents.feature: 58` (untouched by main's
commits, so this session's value stands unmodified). `last_anchor_time`/`last_anchor_source` are re-anchored a
second time to the merge-resolution moment (13:50 SGT), with `last_anchor_source_prior` now pointing at main's
`menu_sketchbook_icon_cabinet_2026-07-01_1235` anchor as the true chronological predecessor. No content in this
session's own DELTA, VERIFICATION, or the companion Feature doc's §1/§2/RF-30…33 was rewritten — only the serial
numbers and the counter file, per AU-1.

**Known Gaps update:** the companion Feature doc's "v0.62.450 → v0.62.453" out-of-scope note (written at PR-draft
time) is now understated — `main` has moved to **v0.62.455 / PR #1441** as of this resolution. The stocktake's
30-June-2026 cutoff (commit `9fa372e0`, v0.62.449) is unaffected and remains correct; the "still out of scope,
needs a follow-up session" list simply grows to include PRs #1439–#1441 alongside the previously-noted #1433–#1437.

| Gate | Question | Verdict |
|------|----------|---------|
| **G1** | Operator request captured verbatim where it matters? | ✅ — "resolve PR conflict", this session's task. |
| **G2** | Source-of-truth = current `main` / actual merged history? | ✅ — reconciled against `origin/main`'s real `doc/.serial-state.yml` values, read directly via `git show`. |
| **G3** | Journal recorded per the standing per-PR rule? | ✅ — this Amendment, appended (not overwritten) to the existing №807/№810 entry. |
| **G4** | Reversals disclosed (AU-7), not silently rewritten? | ✅ — prior №807 text quoted/preserved above; only the serial number and counter values changed, with rationale. |

---

### Amendment 2 (14:11 SGT 01-07-26) — PR #1438 conflict resolution #2: journal serial №810 → №811

**AU-7: quoting prior content verbatim before the change.** Amendment 1 above renumbered this entry to **№810**
and reconciled `doc/.serial-state.yml` to `journal: 810`. Within roughly 20 minutes — between this PR being
merge-conflict-resolved and then marked ready for review — `main` advanced **again**: v0.62.456 (PR #1445,
"Cuisine — tether 'Dishes' as a sub-tab on the cuisine pill") landed its own `+ journal #810`, independently
claiming the exact same serial off the same base counter Amendment 1 had just reconciled to. GitHub's webhook
notified that marking the PR ready for review flipped `mergeable_state` to `dirty`; re-fetching `origin/main`
confirmed the new commit and the renewed `doc/.serial-state.yml` conflict (this time on `build` explicitly, and
on `journal`/`commit` implicitly — git auto-merged those lines because both branches coincidentally computed the
same literal values from the same prior state, which silently produces the *wrong* answer rather than a visible
conflict marker; caught by manually diffing against `origin/main`'s real values rather than trusting git's
auto-merge).

**Resolution:** this file is renumbered a second time, **№810 → №811** (main's true 810, from PR #1445, + this
session's own +1). `doc/.serial-state.yml` reconciled to `documents.journal: 811`, `commit: 929` (main's 928 + 1),
`build: 567` (main's real bump from PR #1445, not this session's stale 566). `last_anchor_source_prior` now
points at main's `cuisine_dishes_tethered_tab_2026-07-01_1255` anchor.

**Pattern noted for future sessions:** this repo is merging a new PR roughly every 10–20 minutes at this cadence
(see the G-A…G-M arc density in the companion Feature doc — the same rapid-iteration pattern is still live).
A long-lived draft PR that touches `doc/.serial-state.yml` should re-check against `origin/main` **immediately
before every push**, not just once at open time — a single reconciliation is not sufficient insurance against
this repo's actual commit velocity. `git diff <local-base> origin/main -- doc/.serial-state.yml` is the fast
check; if non-empty, re-merge and re-verify the counter block by hand rather than trusting an auto-merged result.

| Gate | Question | Verdict |
|------|----------|---------|
| **G1** | Operator request captured verbatim where it matters? | ✅ — "proceed" (mark ready for review), read together with the standing PR-subscription instruction to keep the PR mergeable. |
| **G2** | Source-of-truth = current `main` / actual merged history? | ✅ — reconciled against `origin/main`'s real post-#1445 `doc/.serial-state.yml`, read directly via `git show`, not inferred from git's auto-merge output. |
| **G3** | Journal recorded per the standing per-PR rule? | ✅ — this second Amendment, appended (not overwritten) to the existing entry chain (№807 → №810 → №811). |
| **G4** | Reversals disclosed (AU-7), not silently rewritten? | ✅ — Amendment 1's №810 text quoted/preserved above unchanged; only the serial number and counter values changed again, with rationale. |
