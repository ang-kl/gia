(№ 807 - 01-07 '26 10:50 SGT) v0.62.449 (stocktake, no code bump)

# Journal — soleat / Gia4lunch — Full-project feature stocktake as of 30 June 2026

> Operator (session task, verbatim): "stocktake all the features in this project as of 30 June 2026." Delivered
> via session branch `claude/feature-stocktake-june-2026-nw6ooz`, as a **docs-only** PR — no application code
> touched, no version bump (follows the `doc(Feature #57): …` precedent for a Feature-folder entry made outside
> the normal per-PR version cadence).

### [HDR] #807 | 10:50 SGT 01-07-26 | Full-project feature stocktake as of 30 June 2026 | doc/Feature · doc/.serial-state.yml | 0 PRs (new doc-only PR) | 0 explicit build

- **[INTENT]** The repo's `doc/Feature/` canonical spec had not been updated since `feature-0_62_153-17_06_26-1730.md`
  (PR #1105) other than a scoped `feature-0_62_332-clipboard.md` (PR #1391) covering only the 4-PR Clipboard
  storage/API/TMA/polish cycle. Between PR #1106 and the 30-June-2026 cutoff (PR #1429, commit `9fa372e0`,
  v0.62.449), **316 more commits** landed with no Feature-folder synthesis — a stocktake was overdue per
  `doc/CLAUDE-FULL.md` §17.3's "per arc milestone (every ~10-15 versions)" cadence, which this arc (≈296
  versions) badly exceeded. The operator's ask was to produce that catch-up as a full, dated stocktake.

- **[DELTA]**
  - **`doc/Feature/feature-0_62_449-30_06_26-2321.md` (new, №58).** Reproduce-by-reference (CLAUDE-FULL §17.1)
    from the two prior Feature files (unchanged, not re-typed); §1 restates every active surface in one table,
    grounded directly in `index.js`'s `registerCommandsMenu()` command list and the `web/*` directory listing
    at the cutoff commit; §2 appends 13 new arc sections (G-A…G-M) synthesising the 316-commit gap — the
    Cuisine TMA floating-dock consolidation, the 1,505-entry curated-dish-note pipeline, the explain-first
    local-classic UX, the 8-language i18n expansion (id/ru/de then zh/ja/es + 5 native-script batches), the
    1,643-explanation dish-drafter + Gemini prose-translation pipeline, the Search-Insights demand strip,
    the location/nearby-places overhaul, price-tier/forex/Set-Meal work, the cross-TMA hawker-overlay port,
    result-card evolution, the **Sketchbook TMA v2 rebuild** (the largest single item — chrome, cabinets list,
    filters, archive, extending past the Clipboard doc's #1308 cutoff), a bug/crash/infra-fix roundup, and an
    icon/branding housekeeping note. Adds RF-30…RF-33 to the Removed table.
  - **`doc/Journal/journal-0_62_449-30_06_26-2321.md` (this file, №807).** Records the stocktake session
    itself per the repo-root `CLAUDE.md` standing rule ("After opening any PR… record it in the Journal").
  - **`doc/.serial-state.yml`.** `documents.feature` 56 → 58 (accounts for the un-bumped №57 clipboard doc plus
    this session's №58); `documents.journal` 806 → 807; `commit` 924 → 925; `last_anchor_time` re-anchored.
    `build` left unchanged (no code build ran).
  - **git history:** repo was a 50-commit shallow clone at session start; ran `git fetch --unshallow` (read-only,
    non-destructive) to recover full history back to the v0.62.153 boundary needed for accurate PR-range
    citations.

- **[VERIFICATION (sandbox)]** Docs-only change — no `node --check`, `npm test`, or `npm run build:*` applicable
  (gia-preflight's triggers are index.js / bot handlers / `/s` search / venue rendering / i18n.js / fuzzy
  matchers, none of which this PR touches). Verified instead by: (a) cross-checking every arc's PR-number
  citations against the extracted `git log --reverse 997d3209..9fa372e0` commit list (316 lines, read in full);
  (b) confirming the 30-June cutoff commit (`9fa372e0`, 2026-06-30 23:21 SGT) is the last commit dated on or
  before 30-06-2026 by inspecting the surrounding commit timestamps; (c) confirming both reproduce-by-reference
  targets (`feature-0_62_153-…`, `feature-0_62_332-clipboard.md`) remain in `doc/Feature/`, not `doc/Archive/`,
  satisfying CLAUDE-FULL §17.1 Rule R-1; (d) grounding §1's Active Surfaces table in the live `index.js`
  `enCommands`/`frCommands` arrays and the `web/` directory listing rather than from memory.

- **[STATUS]** DRAFT PR opened against `main` from `claude/feature-stocktake-june-2026-nw6ooz`. No deploy
  implication (docs only; Railway build/deploy path untouched).

- **[TEST]** N/A — no test suite covers `doc/`. The standing `vitest` suite was not run since no source file
  changed.

- **[KNOWN GAPS]**
  - `doc/Technical/` and `doc/Register/` remain stale (last touched at v0.62.153 and v0.62.76 respectively) —
    logged inside the new Feature doc's own Known Gaps section; a Technical + Register catch-up is the natural
    follow-up session.
  - v0.62.450 → v0.62.453 (1 July 2026, PRs #1433–#1437) landed after this stocktake's cutoff and are explicitly
    out of scope per the operator's "as of 30 June 2026" framing.
  - §2's PR citations are representative (not every one of the 316 commits is individually cited), consistent
    with the citation density already established in the prior Feature files.
  - `doc/VibeCodingRecord/` regen and a `vault/v0.62.449/` snapshot were **not** performed this session (both
    are "if convenient" per the repo-root `CLAUDE.md` §4 item 4/§16.1, not mandatory) — still due.

| Gate | Question | Verdict |
|------|----------|---------|
| **G1** | Operator request captured verbatim where it matters? | ✅ — quoted in this file's preamble. |
| **G2** | Source-of-truth = current `main` code / actual merged history (not invented)? | ✅ — every §2 arc and §1 surface is grounded in `git log` output or a live grep of `index.js` / `web/`, read in full before writing. |
| **G3** | Journal recorded per the standing per-PR rule? | ✅ — this HDR, for the stocktake PR itself. |
| **G4** | Reversals disclosed (AU-7), not silently rewritten? | ✅ — RF-30…RF-33 in the new Feature doc; no prior content amended or compressed, only reproduced by reference or appended. |
