(№ 339 - 25-05 '26 08:45 SGT) v0.61.138

# Journal — soleat v0.61.138 (branding fix — user-facing "Gia" → "Soleat")

> Operator (screenshot of the wake message): *"why still show 'Gia'
> should be 'Soleat'"*. The Telegram bot's display name is **Soleat**
> (per `CLAUDE.md` root: *"This is the Soleat (gia / Gia4lunch)
> Telegram bot"*); the user-facing copy was still showing the
> retired *"Gia"* persona name in 22 places (13 i18n strings × 2
> languages + 9 inline strings in index.js + 1 in the cuisine TMA's
> Header). This PR replaces them all. Pure copy change.
>
> Commit 351 / journal 339 / build 141, on branch
> `claude/v0.61.138-branding-soleat` off `main` at v0.61.137
> (commit `4a6a708`, PR #638 — Register O-24 retraction).

## New

### [HDR] #339 | 08:45 SGT 25-05-26 | v0.61.138 — Branding fix: user-facing "Gia" → "Soleat" | 4 files | 1 PR | 1 build |

- **[INTENT]** Stop showing the retired *"Gia"* persona name in
  user-facing bot copy. CLAUDE.md root names both forms — the
  repo is `gia` (legacy), the deployed bot + brand is **Soleat**
  — so internal code documentation referencing *"Gia"* / *"Tell
  Gia"* / *"I'm Gia"* / etc. stays unchanged; only **string
  literals that ship to the chat / TMA UI** are touched.

- **[DELTA]**
  - **`i18n.js`** — 8 unique keys updated (EN + FR each, 16
    string-pair replacements):
      - `bot.busy` — *"⏳ Gia is still working…"* → *"⏳ Soleat is still working…"*
      - `bot.location.locale` — *"Share your location once so Gia uses your locale"* → *"…Soleat…"*
      - `transport.train.noLocation` — *"Gia will list the nearest MRT stations too"* → *"Soleat will…"*
      - `transport.bus.noLocation` — *"share it once via the menu (📍) and Gia will remember"* → *"…and Soleat will remember"*
      - `transport.drive.noLocation` — *"Gia will offer a one-tap driving directions link"* → *"Soleat will…"*
      - `language.cleared` — *"Gia will follow your Telegram language"* → *"Soleat will follow…"*
      - `wake.locationCheck` — *"Welcome back! Gia is still using…"* → *"Welcome back! Soleat is still using…"*. **(Note: this string will be fully rewritten by PR C — next-up — to the operator's richer 2-step welcome flow with current-vs-anchor compare + 3 buttons + `/l` helper text. The branding swap here is the minimum to stop the operator-screenshot regression.)**
      - `hidden.busy` — *"⏳ Gia is still working on your last request"* → *"⏳ Soleat is still working…"*
  - **`index.js`** — 5 unique string sites updated (one of them
    is the `bot.busy`-pattern repeated 6× via `safeSend`):
      - 6× `await safeSend(chatId, '⏳ Gia is still working…');` — replaced via `Edit replace_all=true`
      - 1× share-pick blurb: *"When they tap it, Gia will send them this exact pick"* → *"…Soleat will send them…"*
      - 1× sanctuary-share inbound notice: *"A friend shared a sanctuary pick with you via Gia"* → *"…via Soleat"*
      - 1× rate-limit detail: *"Too many Tell Gia calls this hour"* → *"Too many Tell Soleat calls…"*
      - 1× share-location prompt template: *"…type a place name and Gia will search within 200 m"* → *"…and Soleat will search…"*
  - **`web/cuisine/src/components/Header.jsx`** — 1 string:
      - *"📍 Location required — Gia needs your spot…"* →
        *"📍 Location required — Soleat needs your spot…"*
  - **`package.json`** — `0.61.137 → 0.61.138`.
  - **`doc/.serial-state.yml`** — commit `350 → 351`, build
    `140 → 141`, journal `338 → 339`; re-anchored 25-05 '26 08:45
    SGT (SG air-temperature sovereign source); new comment-history
    block prepended.
  - **`doc/Journal/journal-0_61_138-25_05_26-0845.md`** — this entry.

- **[VERIFICATION (sandbox)]**

  ```
  $ node --check index.js && echo OK
       index.js OK
  $ node --check i18n.js && echo OK
       i18n.js OK
  $ ./web/cuisine/node_modules/.bin/esbuild --loader:.jsx=jsx \
       --log-level=error web/cuisine/src/components/Header.jsx > /dev/null
       (no errors → parses OK)
  $ grep -nE "['\"\`][^'\"\`]*\\bGia\\b" /tmp/work-A/index.js | head -5
       (4 matches remaining — all in code comments, not user-facing.)
  $ grep -nE "['\"\`][^'\"\`]*\\bGia\\b" /tmp/work-A/i18n.js | head -3
       (1 match remaining — code comment at line 319.)
  ```

  **`vite build` (full bundle verify) skipped** — fresh O-22 data
  point: `web/cuisine/node_modules/rollup/package.json` was
  iCloud-corrupted during this session (`Invalid package config
  while importing rollup/parseAst`). The change is a single
  string-literal swap with no logic change; Railway's
  `postinstall` rebuilds the bundle fine.

- **[STATUS]** PROD-bound on squash-merge to `main`. Auto-deploys
  via Railway; `postinstall` runs `npm run build:cuisine` so the
  new Header copy lands in the served `/app/cuisine/` bundle.

- **[TEST]** Operator (post-deploy):
  - Trigger the `bot.busy` path (e.g. tap `/cuisine` twice
    rapidly) — should now read *"⏳ Soleat is still working on
    your last request…"*.
  - Long-silence wake — should NO LONGER say *"Gia is still
    using the location you shared earlier"*; instead reads
    *"Soleat is still using…"*. (PR C will replace the full
    message; this PR just kills the branding.)
  - Open Cuisine TMA without sharing location — Header should
    read *"📍 Location required — Soleat needs your spot…"*.
  - `/transport` train / bus / drive responses, `/language`
    clear, `/hidden` busy — all should now reference *Soleat*.

- **[KNOWN GAPS]**
  - **Comments referencing `Gia` / `Tell Gia` / `I'm Gia`** are
    deliberately retained as internal code documentation. The
    operator's `CLAUDE.md` names both forms (the repo is `gia`
    legacy, the deployed brand is `Soleat`), and these refs
    appear inside JS comments only — never shipped to users.
  - **LLM system prompt** in `web/cuisine/src/components/
    PromptPreview.jsx:21` (*"You are Gia, a Singapore food
    concierge…"*) — that's the LLM persona name fed into Gemini
    via the system prompt. Not user-facing. Operator can switch
    the LLM persona separately if desired.
  - **PR C will rewrite `wake.locationCheck` entirely** — this
    PR only swaps the brand name. The full 2-step rewrite
    (current-vs-anchor compare, 3 buttons, `/l` helper text)
    lands in PR C.
  - **PR B (Menu TMA anchored-at format)** is the second of
    three PRs from this operator request — pending.
  - **No inline test** added — string-literal changes only;
    every existing test that asserts on the prior copy would
    break, so this PR's diff is itself the test surface.

- **Confirmation Gates**

  | Gate | Resolution | Date |
  |------|-----------|------|
  | G1 — version bump | PATCH `0.61.137 → 0.61.138`. Copy change only, no behaviour change. | 25-05 '26 |
  | G2 — destructive action | _n/a_ — pure string-content replacements; no structural changes. The retired *"Gia"* persona name still survives in code comments per the operator's `CLAUDE.md` name-pair convention. | 25-05 '26 |
  | G3 — new decision rule | _n/a_ — closing a fresh operator-flagged regression, not proposing a new rule. | 25-05 '26 |
  | G4 — paid external API call | _n/a_ — copy. | 25-05 '26 |

## Amendments

_None — pure copy fix._

## Deleted

_None._
