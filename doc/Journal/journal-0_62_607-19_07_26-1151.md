# Journal — v0.62.607 (Hawker: frost the footer + one-row zone pills without counts)

_(№ 951 — 19-07 '26 11:51 SGT)_

## New

### [HDR] v0.62.607 — Hawker: give the footer a real frost, and squeeze the 5 zones onto one row (drop the "(##)")

- **[INTENT]** Operator (IMG_3593), two Hawker fixes:
  1. *"should you frost the footer."*
  2. *"remove the '(##)' in the five zone and squeeze all to one row."*

- **[DELTA — footer frost]** `web/hawker/src/App.jsx` `FooterDock`: the v0.62.605 dock used
  `liquid-glass-dock` — a **Cuisine-only** CSS class that is **undefined in the Hawker build**, so it
  never blurred (it was just `bg-tg-bg/80`). Replaced with a real **`bg-tg-bg/80 backdrop-blur-md
  border-t border-tg-border`** so the footer is actually frosted.

- **[DELTA — zone pills]** both region-pill rows (the portrait header row — was
  `${isWide ? 'flex flex-wrap' : 'grid grid-cols-3'}`, a 2-row grid on phones — and the landscape
  header row — was `flex flex-wrap`) now render as **one row**: `flex gap-1 … overflow-x-auto`,
  content-sized pills (`px-2`, `text-[11px]` / `text-xs`, centre-justified in portrait). The **`({r.count})`
  count is removed** (`{regionLabel} ({r.count})` → `{regionLabel}`). The row scrolls horizontally only
  if a genuinely narrow phone can't fit all five.

- **[VERIFICATION (sandbox)]** **Playwright** (stubbed 5 regions, phone 390) confirmed the five zones on
  **one row** — `🏙 Central · 🚢 South · 🍙 East · 🌳 North · 🎆 West`, **no counts**, "Central" not
  truncated — and the **frosted footer**, with no page errors. Hawker `vite build` green
  (`index-DEjJbqgL.js 256.49 kB │ gzip 83.92 kB`); full `npm test` **3738/3738 across 147 files**;
  `package.json` + `package-lock.json` 0.62.606 → 0.62.607; `.serial-state.yml` commit 1086→1087, build
  708→709, journal 950→951, re-anchor **19-07 '26 11:51 SGT**.

- **[TIME ANCHOR]** data.gov.sg blocked by the environment network policy (`403` `CONNECT`); anchored
  to the agent-proxy status timestamp `2026-07-19T03:51Z` = **19-07 '26 11:51 SGT** (TF-9 fallback;
  TF-10 lower bound).

- **[STATUS]** DRAFT → squash-merge to `main` on green CI (auto-merge SQUASH). New PR (branch restarted
  from `origin/main` `3a9f80c`, #1614 merged).

- **[TEST]** No new unit test (a CSS-class + markup change; verified visually via Playwright). Existing
  suite unchanged and green.

- **[KNOWN GAPS]** The zone row `overflow-x-auto` will scroll on a very narrow phone (< ~360 px) rather
  than shrink further; the five SG regions are short enough that phones ≥ 390 px show all five without
  scroll. The stray `liquid-glass-dock` reference is now gone; if the Cuisine glass surface is ever
  wanted here, the class would need porting into the Hawker CSS. Carried gaps unchanged.

## Confirmation Gates
| Gate | Status | Note |
|------|--------|------|
| G1 | ✅ | PATCH bump 0.62.606 → 0.62.607; Hawker build + 3738 tests green; both fixes verified via Playwright. |
| G2 | ➖ | no destructive removal — the `(##)` count is dropped from the label and the footer class swapped; nothing structural removed. |
| G3 | ➖ | no new decision rule (two direct operator tweaks). |
| G4 | ➖ | no external paid API call. |
