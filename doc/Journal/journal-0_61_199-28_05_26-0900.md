(№ 400 - 28-05 '26 09:00 SGT) v0.61.199

# Journal — soleat v0.61.199 (place-search 400 diagnostic + friendly picker errors)

> Debug follow-up to v0.61.198. Operator's same log had two
> secondary issues: a `/api/cuisine/place-search-by-country`
> Google 400 that logged only `err.message` (useless), and the
> picker UIs surfacing "HTTP 502" verbatim (worse, the Menu TMA
> swallowed errors entirely and showed "No match in Malaysia").
>
> Commit 412 / journal 400 / build 202, on branch
> `claude/v0.61.199-debug-and-clear-on-error` off `main` at
> v0.61.198.

## New

### [HDR] #400 | 09:00 SGT 28-05-28 | v0.61.199 — place-search 400 diagnostic + friendly picker errors | 5 files | 1 PR | 1 build |

- **[INTENT]**
  1. Capture enough about each Places 400 to actually debug it
     (status, response body, request payload).
  2. Stop showing "HTTP 502" / "No match in Malaysia" to a user
     who hit a server error.

- **[DELTA]**

  - **`index.js`** — `/api/cuisine/place-search-by-country` catch
    block (~20 lines):
    ```js
    } catch (err) {
      const status = err.response?.status ?? '?';
      const bodyTxt = (() => {
        try {
          if (!err.response?.data) return '';
          const s = typeof err.response.data === 'string'
            ? err.response.data
            : JSON.stringify(err.response.data);
          return s.slice(0, 600);
        } catch { return ''; }
      })();
      const reqTxt = (() => {
        try { return JSON.stringify(body).slice(0, 400); } catch { return ''; }
      })();
      console.warn(`[place-search-by-country] Places searchText failed: status=${status} msg=${err.message} body=${bodyTxt} req=${reqTxt}`);
      return res.status(502).json({ error: 'Places search failed' });
    }
    ```
    The 502 response shape to the client is unchanged. The log
    line now carries enough context for the next failure to be
    diagnosable (regionCode? textQuery? quota? auth?).

  - **`web/cuisine/src/v2/components/LocationField.jsx`** — the
    OTHER picker's `doSearch` catch was:
    ```js
    setErrorMsg(err?.message || String(err));
    ```
    which surfaced "HTTP 502" verbatim. Now:
    ```js
    const raw = err?.message || String(err);
    const friendly = lang === 'fr'
      ? `Recherche dans ${country.name} impossible. Réessayez ou changez de pays. (${raw})`
      : `Couldn't search ${country.name}. Try again or pick a different country. (${raw})`;
    setErrorMsg(friendly);
    ```
    User sees actionable context; the raw cause is preserved in
    parens for the operator to relay.

  - **`web/menu/src/components/LocationFieldMenu.jsx`** —
    `onOtherSearch` was worse than the cuisine TMA equivalent:
    ```js
    const r = await fetch(...).then((res) => res.ok ? res.json() : null);
    const arr = Array.isArray(r?.results) ? r.results : [];
    if (arr.length === 0) setOtherNoMatch(true);
    ```
    On HTTP 502 the fetch resolved to `null` → empty array →
    "No match in Malaysia" toast — actively misleading. Rewrote:
    ```js
    const res = await fetch(...);
    if (!res.ok) throw new Error(`HTTP ${res.status}`);
    const r = await res.json();
    …
    } catch (err) {
      const raw = err?.message || String(err);
      const countryName = findCountry(countryPref)?.name || countryPref;
      const friendly = lang === 'fr'
        ? `Recherche dans ${countryName} impossible. …`
        : `Couldn't search ${countryName}. Try again …`;
      setErrorMsg(friendly);
    }
    ```
    `findCountry` was already imported from `../countries.js`
    (line 24). Friendly message matches the cuisine TMA's copy.

  - **`package.json`** — `0.61.198 → 0.61.199`.
  - **`doc/.serial-state.yml`** — commit 411 → 412, build 201 →
    202, journal 399 → 400; re-anchored 28-05 '26 09:00 SGT.

- **[VERIFICATION (sandbox)]**

  ```
  $ node --check index.js          (OK)
  $ npx vitest run                 ✓ 92 files / 2490 tests pass
  $ cd web/cuisine && npm run build  (✓ built in 3.23s)
  $ cd web/menu && npm run build     (✓ built in 2.88s)
  ```

- **[STATUS]** PR-OPEN on push. Operator merges manually.

- **[TEST]** Operator (post-merge + Railway redeploy):
  1. Reproduce the OTHER picker failure (e.g. type "Times Square"
     in MY with current pref) and capture the new log line. It
     should now include `status=<n> body=<truncated JSON>` —
     copy this into the next debug pass.
  2. The picker UI should now read "Couldn't search Malaysia.
     Try again or pick a different country. (HTTP 502)" instead
     of the bare HTTP code.
  3. Menu TMA: same picker behaviour — no more spurious "No
     match in Malaysia" on actual server errors.
  4. Clean-path regression: pick a valid OTHER place (e.g.
     "Pavilion KL") → confirmation list renders normally; no
     error toast.

- **[KNOWN GAPS]**
  - We still don't know the root cause of the Google 400 — this
    PR is *diagnostic infrastructure* for the next operator run.
    Likely candidates: textQuery containing problematic chars,
    `includedRegionCodes` not accepted for some `regionCode`
    pairs, or quota fan-out under bursty traffic.
  - The cuisine TMA result panel still shows stale results
    (e.g. "Vivocity 4 places") when the OTHER picker errors —
    operator's complaint #3. The result panel is *correct* in
    that no new search has run; clearing it on picker error
    would be misleading too. We could surface a small "anchor
    unchanged" pill but that's UX polish, not a bug. Leaving
    as-is for now.
  - The `(${raw})` suffix exposes "HTTP 502" to the end user.
    Operator can relay this when reporting. Once the underlying
    400 is rooted out we can strip the parenthetical.

- **Confirmation Gates**

  | Gate | Resolution | Date |
  |------|-----------|------|
  | G1 — version bump | PATCH `0.61.198 → 0.61.199`. Diagnostic logging + error-text rewrite. No behaviour change on the success path. | 28-05 '26 |
  | G2 — destructive action | _n/a_ — additive logging + UI-text edit. Menu TMA error-path is a fix (previously silently lied). | 28-05 '26 |
  | G3 — new decision rule | YES — `axios` catch sites for external API calls should log `err.response?.status` + body when available, not just `err.message`. Future external-API endpoints should mirror this pattern. | 28-05 '26 |
  | G4 — paid external API call | _n/a_. | 28-05 '26 |

## Amendments

- **`/api/cuisine/place-search-by-country` log line** — was a
  single `err.message`. Now includes status + truncated body +
  truncated request payload.
- **Cuisine TMA OTHER picker error display** — was raw
  `err.message`. Now wrapped in actionable copy with raw cause
  in parens.
- **Menu TMA OTHER picker** — was conflating HTTP errors with
  zero-result responses. Now distinguishes via thrown error.

## Deleted

_None._
