(№ 772 - 28-06 '26 17:40 SGT) v0.62.418

# Journal — soleat v0.62.418 (Sketchbook header chips = filter saved cards, not search)

> Operator: "The 'Cuisine & filter' and 'Local food classic' are to search for the eatery cards
> with the cuisine or food dish — it is not to search for new eatery." Reverses the P2 chip
> behaviour (and the earlier 'deep-link to Cuisine' decision).

## New

### [HDR] #772 | 17:40 SGT 28-06-26 | v0.62.418 — header chips filter saved cards | 4 files | PR #(pending) |
- **[INTENT]** Make the two header chips FILTER the user's own saved eatery cards, not navigate to
  a new search.
- **[DATA REALITY]** A saved card carries a `cuisines[]` tag + a copied text blob (`name`,
  `preview`, `body`, `note`) — there is NO structured dish list. So: cuisine = pick from the
  cuisines on the cards; dish = keyword match over the card text (operator-confirmed).
- **[DELTA]**
  - **NEW `components/FilterSheet.jsx`** — bottom-sheet with `list` mode (cuisine pick-list with
    counts) and `text` mode (dish keyword input). "All" / empty clears.
  - **`Shell.jsx`** — the chips are now prop-driven (open the relevant filter; active chip is
    accented and shows a ✕ to clear); they no longer deep-link to the Cuisine TMA.
  - **`App.jsx`** — `cuisineFilter` / `dishFilter` state; `cuisineOptions` derived from the visible
    base set; `cardMatches()` (cuisine tag + text keyword); applies to the Clipboard list
    (`filteredCatchAll`) and the open cabinet's drawers (`filteredPayload`); renders the two
    FilterSheets.
  - **`lib/i18n.js`** — `filter.*` keys (en/fr/id/ru/de).
- **[VERIFICATION (sandbox)]** Real `vite build` OK (50 modules → public/clipboard).
- **[STATUS]** PROD — clipboard TMA rebuild; redeploys per PR.
- **[TEST]** No unit test (UI). Live eyeball after the UI phases.
- **[KNOWN GAPS]** Dish filter is a text-keyword match (no structured dishes in card data yet).
  P3 VenueCard (classic), P4 File sheet + edit pills + 📍/🧭 Recents, P5 Settings still to come.

## Confirmation Gates

| Gate | Status | Note |
|------|--------|------|
| G1 build/parse | ✅ | real vite build (50 modules) |
| G2 tests | ⏳ | CI |
| G3 journal/serial | ✅ | serial 771→772 |
| G4 live deploy | ⏳ | redeploy |
