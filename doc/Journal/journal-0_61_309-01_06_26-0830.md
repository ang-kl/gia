(№ 510 - 01-06 '26 08:30 SGT) v0.61.309

# Journal — soleat v0.61.309 (chat `/cuisine` → Places-first; LLM-invents-names path retired)

> Operator (01-06 '26): *"it has to be 100% non-inventive AND non-inventive. it has to be true source from Google."* (parsed: variety / shuffle is fine; venue identity must come from Google Places, never from an LLM). The session's prior investigation surfaced two distinct cuisine-search code paths:
>
> - **Path A — TMA 🔍 button** (`POST /api/cuisine/search` → `pipeline.discover()`) — already Places-first.
> - **Path B — chat `/cuisine` + sendData web_app_data fallback + NL free-text** (`searchCuisine()` in `cuisine-search.js`) — LLM-invents-candidate-names then `validateWithPlaces` drops hallucinations.
>
> v0.61.309 retires Path B's LLM-name-invention loop and re-routes through `pipeline.discover()`, matching Path A. The Refine pass (LLM rewrites travel-advice / queue-minutes / cost from weather/traffic) is also dropped — same operator constraint applies to LLM-fabricated venue-attached content.
>
> Commit 523 / journal 510 / build 312 / feature 51, on branch `claude/v0.61.309-cuisine-places-first` off `main` at v0.61.308.

## New

### [HDR] #510 | 08:30 SGT 01-06-26 | v0.61.309 — chat /cuisine Places-first | 4 files | 1 PR | 1 build |

- **[INTENT]** Make every cuisine-search surface 100% Google-Places-sourced. Eliminate the LLM-invents-candidate-names loop (Path B) and align with the TMA 🔍 button's existing Places-first path (Path A).

- **[DELTA]**

  **1. `cuisine-search.js`** — surgical replacement of `searchCuisine()` body:
  - **Removed**: `runPipeline()` (which called `pipeline.reason()` → Claude Sonnet to invent names with vault grounding); legacy `geminiCandidates15()` fallback (Gemini Flash inventing names); `validateWithPlaces` per-candidate Places lookup; `pipeline.refine()` LLM rewrite of travel-advice / queue-minutes / cost.
  - **Added**: single `pipeline.discover({ lat, lng, cuisines, radius, mealPeriod: when, maxResults: 12, regionCode: 'SG' })` call — same Places `searchText` path the TMA Search button uses. Returns real venue objects from Places.
  - **Field defaults**: LLM-only fields (`signatureDish`, `queueMinEstimate`, `bookingRequired`, `dishes`, `costEstimateSgd`, `verifiedOpeningDate`, `verifiedGoogleMapsUrl`) are set to neutral defaults (`''`, `null`, `false`, `[]`) so downstream formatters render no fabricated values. Places venues carry `placeId`, `name`, `area`, `lat`, `lng`, `rating`, `userRatingCount`, `priceLevel`, `openNow`, `regularPeriods`, `weekdayDescriptions`, `websiteUri`, `phone`, `primaryType`, `primaryTypeDisplayName`, `url`, `directionsUri`, `reviewsUri`, `photosUri`, `googleSummary`, `reviews`, `allowsDogs`, `wheelchairAccessible`, `priceRange`, `country`.
  - **Pipeline preserved**: `rankByWalkingTime` (deterministic, Google Routes-based), `applyPostFilters` (preset rating/openNow gates), the `queueMinEstimate <= queueMaxMin` filter (no-op now since Places doesn't expose a queue estimate; kept wired in case a future Places signal populates it), `excludeChains` (fast-food chain blocker), `sortVenues`.
  - **Dead-code removal**: `buildPrompt`, `geminiCandidates15` deleted (no other callers in the file). Imports of `llm`, `withRetry`, `MODEL_NAME`, `validateWithPlaces` removed.
  - **Inputs retained for back-compat but no longer steer venue selection**: `specialRequest`, `recencyDays`, `mode` (LLM-only constraints). `preset` still drives `applyPostFilters` (rating/openNow gates).

  **2. `doc/Feature/feature-0_61_83-21_05_26-1645.md`** — appends **RF-17** to the Removed Features table. Covers the LLM-invents-names path + the refine LLM-rewrite path. Replacement column points to `pipeline.discover()`.

  **3. `package.json`** — `0.61.308 → 0.61.309`.

  **4. `doc/.serial-state.yml`** — commit 522→523, build 311→312, journal 509→510, feature 50→51; re-anchored 01-06 '26 08:30 SGT.

- **[VERIFICATION (sandbox)]**

  - `node --check cuisine-search.js` — clean.
  - `npm test -- --run` — expect green; cuisine-search has no direct unit tests but downstream callers (`pipeline-task-gates.test.js`, etc.) cover the surrounding code paths.
  - No build needed (no `web/` change).

- **[STATUS]** PR-OPEN on push. Railway redeploys on merge. The first chat `/cuisine` or `runFreeTextSearch` call after redeploy will hit `pipeline.discover()` directly; no warmup needed.

- **[TEST]** Operator (post-merge):
  1. `/cuisine` (chat) → expect a results list whose venue names ALL resolve to real Google places. No `signatureDish` / `verifiedOpeningDate` annotations on the cards.
  2. NL free-text "find me italian near orchard" → same Places-sourced behaviour.
  3. TMA 🔍 button unchanged (already on Path A; this PR doesn't touch it).
  4. `/cost` (owner) after one chat `/cuisine` → Maps counters bump (`searchText` if instrumented; currently only `placeAutocomplete` + `placeResolve` are instrumented per v0.61.307); Gemini counters do NOT bump on the Path B surface anymore (LLM not in the loop).

- **[KNOWN GAPS]**
  - **`specialRequest` / NL-typed free text is now silently ignored** for venue selection. The TMA Search button doesn't expose this field; chat `/cuisine <free text>` (rare path) will get Places-sourced results irrespective of the qualifier. Operator-acceptable trade-off given the "100% non-inventive" constraint. Follow-up if needed: surface the qualifier as a Places `textQuery` modifier (not LLM).
  - **No "Refine" weather/traffic narration**. Previously the LLM rewrote travel advice + queue minutes + cost. Now the venue cards show whatever `deliverPicks` injects via `weather.attachRainAlerts` — best-effort rain caveats only.
  - **Other LLM call sites unchanged** in this PR:
    - `surprise.js` (hidden-gems path — separate spec)
    - `gemini-client.js` Hidden Gems
    - `durian-gemini-verifier.js` (D703f cache verifier — *classifies* Places venues; does not invent names)
    - `social-profiles.js` (Gemini-grounded social-handle lookup; doesn't change venue identity)
    - `translate-review.js` (review translation)
    - `pipeline.discover()` retains its own LLM rank pass for the (separately-spec'd) /cuisine ranking. Verify it doesn't reorder by LLM verdict in the next audit pass — if it does, that's the next thing to gate.
  - **`pipeline.refine` + `pipeline.reason` + `pipeline.fetchContext` are still exported** — they're used by `pipeline-task.js` and `surprise.js`. Not touched by this PR.
  - **Anthropic temperature still defaults to 1.0** in `llm-client.js` for any remaining LLM-touching surfaces. Pin to 0 in a follow-up if you want the cache-verifier (durian/D703f) + social-profiles to be even more reproducible.
  - **Discover's empty-cuisine seed picker (`pipeline.js:985`) is still random** — `NO_CUISINE_SEEDS[Math.floor(Math.random() * 7)]`. That's variety-by-design ("tap again gives a different mix"), aligned with operator's "non-deterministic AND non-inventive" framing. Non-inventive: real Places venues only.

- **Confirmation Gates**

  | Gate | Resolution | Date |
  |---|---|---|
  | G1 — version bump | PATCH `0.61.308 → 0.61.309`. Behaviour change for chat `/cuisine` + free-text + sendData fallback — same surface area, now sourced from Places only. | 01-06 '26 |
  | G2 — destructive action | Code deletion: `buildPrompt`, `geminiCandidates15` removed from `cuisine-search.js`; LLM-related imports dropped. Authenticity trace preserved via RF-17 in the Feature ledger (AU-2). | 01-06 '26 |
  | G3 — new decision rule | Operator-confirmed: chat-side cuisine search must be 100% Google-Places-sourced. LLM-invents-names path is **forbidden**; LLM may still *classify* / *label* existing Places venues (D703e/D703f cache pattern), but never select or fabricate venue identity. | 01-06 '26 |
  | G4 — paid external API call | _n/a_. The replacement (`pipeline.discover()`) makes a Places `searchText` request — already accounted for in the user's Maps budget; the LLM calls being retired (Claude Sonnet / Gemini Flash) were the more expensive surface and are now zero on this path. Net cost: ↓. | 01-06 '26 |

## Amendments

- **`cuisine-search.js`** — `searchCuisine()` body rewired to `pipeline.discover()`; `buildPrompt` + `geminiCandidates15` + four LLM-related imports removed.
- **`doc/Feature/feature-0_61_83-21_05_26-1645.md`** — Removed Features table gains RF-17.
- **`package.json`** — version `0.61.308 → 0.61.309`.
- **`doc/.serial-state.yml`** — counters + last_anchor_time re-anchored 01-06 '26 08:30 SGT.

## Deleted

- **`cuisine-search.js: buildPrompt()`** (lines 67-115 in the pre-v0.61.309 file) — the Claude / Gemini prompt that asked the LLM to *return 15 candidate venues by name*. Authenticity trace preserved verbatim in RF-17's "Item" column reference.
- **`cuisine-search.js: geminiCandidates15()`** (lines 117-143 in the pre-v0.61.309 file) — the legacy Gemini fallback that parsed the LLM JSON response and surfaced `name`/`area`/`vibe`/`signatureDish`/`queueMinEstimate`/`bookingRequired` per candidate. Authenticity trace preserved verbatim in RF-17.
