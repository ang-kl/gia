(№ 815 - 01-07 '26 15:00 SGT) v0.62.461

# Journal — soleat v0.62.461 (cross-TMA — subtle close ✕ on remaining dropdowns)

> Operator: "proceed with 3" — the parked pass adding a subtle close ✕ to the two remaining
> self-dismissing dropdowns (language toggle, map-layers menu) across all 3 TMAs.

## New

### [HDR] #815 | 15:00 SGT 01-07-26 | v0.62.461 — LocaleToggle + MapControls close ✕ | 4 files | PR #(pending) |
- **`LocaleToggle.jsx`** (cuisine v2, transport, hawker) — the language dropdown listbox gains a
  subtle **✕** header row (first child, `flex justify-end border-b border-tg-border`,
  `text-tg-hint text-sm leading-none`) that calls `setOpen(false)`. Previously it only closed on
  outside-tap or picking a locale.
- **`MapControls.jsx`** (shared byte-identical file across all 3 TMAs) — the map-layers `⋮` menu
  gains the same ✕ header row, calling `setMenuOpen(false)`. The identical edited file was copied
  to all three paths to preserve the byte-identical invariant documented in the file's own header
  comment.
- **[VERIFICATION (sandbox)]** `esbuild` parse OK on all 4 edited files; full builds + vitest via CI.
- **[STATUS]** PROD — cuisine/transport/hawker TMA rebuild on merge.

## Confirmation Gates
| Gate | Status | Note |
|------|--------|------|
| G1 | ✅ | esbuild parse; CI builds all 3 TMAs |
| G2 | ⏳ | full vitest via CI |
| G3 | ✅ | serial 814→815 |
| G4 | ⏳ | redeploy + operator eyeball |
