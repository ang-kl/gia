(№ 481 - 30-05 '26 15:30 SGT) v0.61.280

# Journal — soleat v0.61.280 (Michelin in OTHER: strip + sparse notice — Register O-31)

> Operator (30-05 '26 15:25 SGT, post-v0.61.279 PR #785 merge): *"mnext"* — pick the next item. Closing Register O-31.
>
> The ✳️ Michelin chip is greyed in the CuisineDrawer when `region !== 'SG'` (v0.60.199 `regionScope:'SG'` gate on the catalogue category). But:
>
> 1. The v0.60.199 App.jsx strip only fired when toggling to **JB**. Toggling to **OTHER** left a previously-selected `'michelin'` chip in `state.cuisines`, so the next search request still carried the unsupported cuisine.
> 2. The greyed-out chip had no in-drawer caption explaining why. Users could read "greyed" as "broken" rather than "Singapore-only".
>
> v0.61.280:
>
> - **Strip extension** (`App.jsx:1853`): change `r.id === 'JB'` to `r.id !== 'SG'` so the strip fires on JB **and** OTHER (and any future non-SG region).
> - **Sparse notice** (`CuisineDrawer.jsx:~129`): a small italic caption rendered below the category grid when `region !== 'SG'` AND the catalogue contains at least one `regionScope:'SG'` category. Today that's just the Michelin chip; future SG-only categories surface automatically.
> - New i18n key `drawer.michelinSgOnly` (EN + FR).
>
> Commit 494 / journal 481 / build 283, on branch
> `claude/v0.61.280-michelin-other-strip-and-notice` off `main` at v0.61.279.

## New

### [HDR] #481 | 15:30 SGT 30-05-30 | v0.61.280 — Michelin OTHER strip + sparse notice | 5 files | 1 PR | 1 build |

- **[INTENT]** Close Register O-31. The Michelin chip is SG-curated; outside SG it produces 0–2 results. The chip is already greyed (v0.60.199 `regionScope` gate), but a sticky selection from a prior SG session would still ride along on the next search, and there's no caption explaining why the chip is greyed. v0.61.280 fixes both.

- **[DELTA]**

  **1. `web/cuisine/src/v2/App.jsx` (~line 1853, region-pill onClick).**
  Change the Michelin-strip condition from `r.id === 'JB'` to `r.id !== 'SG'`. Comment block extended to note the v0.61.280 generalisation.

  **2. `web/cuisine/src/v2/components/CuisineDrawer.jsx` (~line 129).**
  New caption rendered below the category grid:
  ```jsx
  {region && String(region).toUpperCase() !== 'SG'
    && catalogue.some((c) => c.regionScope && String(c.regionScope).toUpperCase() === 'SG') && (
    <div className="text-[11px] text-tg-hint px-1 pt-0.5">
      {tr('drawer.michelinSgOnly', lang)}
    </div>
  )}
  ```
  The catalogue-introspection (`some((c) => c.regionScope === 'SG')`) keeps the caption defensively gated on the actual presence of an SG-only category — if the catalogue ever ships without a Michelin card, the caption disappears automatically.

  **3. `web/cuisine/src/v2/lib/i18n.js`.** New key:
  ```js
  'drawer.michelinSgOnly': { en: '✳️ Michelin is Singapore-only (greyed outside SG).',
                             fr: '✳️ Michelin uniquement à Singapour (grisé hors SG).' }
  ```

  **4. `package.json`** — `0.61.279 → 0.61.280`.

  **5. `doc/.serial-state.yml`** — commit 493→494, build 282→283, journal 480→481; re-anchored 30-05 '26 15:30 SGT.

- **[VERIFICATION (sandbox)]**

  - `node --check web/cuisine/src/v2/lib/i18n.js` — clean.
  - `esbuild.transform` (loader: jsx) of `App.jsx` + `CuisineDrawer.jsx` — clean.
  - Vitest still BLOCKED locally by the recurring iCloud `ERR_INVALID_PACKAGE_CONFIG` throttle (Register O-22). CI will run authoritatively. Expected: no test changes; both prior CI fixes (jb-focus-points, smart-place-label) still green.

- **[STATUS]** PR-OPEN on push. Railway redeploys the Cuisine TMA on merge (no server change).

- **[TEST]** Operator (post-merge):
  1. **Sticky Michelin strip on JB.** From SG, tap Michelin chip. Verify it's selected. Tap JB region pill. Open the drawer; the Michelin chip should no longer be selected (selection cleared by the strip). Behaviour unchanged from v0.60.199.
  2. **Sticky Michelin strip on OTHER (NEW).** From SG, tap Michelin chip. Verify it's selected. Tap OTHER region pill (or the world-flag picker). Open the drawer; the Michelin chip should no longer be selected. Pre-v0.61.280 it stayed selected and silently rode along on the next search.
  3. **Sparse notice on JB.** Tap JB pill. Open the drawer. A small italic line "✳️ Michelin is Singapore-only (greyed outside SG)." appears below the category grid.
  4. **Sparse notice on OTHER.** Same as #3 for the OTHER pill. Notice appears.
  5. **No notice on SG.** Tap SG pill. Open the drawer. No caption — the regular drawer view.
  6. **FR locale.** Switch locale to FR. The caption reads "✳️ Michelin uniquement à Singapour (grisé hors SG)." in JB / OTHER.

- **[KNOWN GAPS]**
  - **The caption doesn't appear if the user never opens the drawer.** Acceptable — the only place a user would notice the missing Michelin chip is inside the drawer, and that's exactly where the caption sits.
  - **No telemetry for "Michelin tap attempt on JB / OTHER".** The chip is greyed and ignores taps; no signal flows back. Could add a one-liner if operator wants to track curiosity rate.
  - **Removed Features table update** — `doc/Feature/cuisines_js.MD` should gain a row for the v0.61.280 strip generalisation per AU-2. Not in this PR; flagged in known gaps.

- **Confirmation Gates**

  | Gate | Resolution | Date |
  |---|---|---|
  | G1 — version bump | PATCH `0.61.279 → 0.61.280`. Closes a sticky-state bug (Michelin chip riding along into OTHER searches) + adds a clarifying caption. No API contract change. | 30-05 '26 |
  | G2 — destructive action | _n/a_. Two additive changes (strip extension + new caption) + one new i18n key. | 30-05 '26 |
  | G3 — new decision rule | _n/a_. Codifies the existing v0.60.199 region-scoped chip rule for OTHER too. | 30-05 '26 |
  | G4 — paid external API call | _n/a_. Pure client-side change. | 30-05 '26 |

## Amendments

- **`web/cuisine/src/v2/App.jsx`** — region-pill onClick Michelin-strip generalised from `r.id === 'JB'` to `r.id !== 'SG'`.
- **`web/cuisine/src/v2/components/CuisineDrawer.jsx`** — new sparse-notice caption below the category grid, gated on `region !== 'SG'` AND catalogue contains at least one `regionScope:'SG'` category.
- **`web/cuisine/src/v2/lib/i18n.js`** — new EN+FR string `drawer.michelinSgOnly`.

## Deleted

_None._
