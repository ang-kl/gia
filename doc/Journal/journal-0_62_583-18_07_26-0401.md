# Journal — v0.62.583 (Cuisine: picker panel drops its top border — no header line)

_(№ 927 — 18-07 '26 04:01 SGT)_

## New

### [HDR] v0.62.583 — Cuisine: the folio picker panel no longer draws a line under the header

- **[INTENT]** Operator (IMG_3511–3515, **phone**, deployed v0.62.582 — the label fix works: "Set
  location is: 16 Thompson Street"). Four reports:
  1. *"the two tabs; the vertical line didn't align with the headers 'Cuisine & filters' / 'Pick local classic'."*
  2. *"the horizontal bottom header line that appears when I tap should disappear, as it did before this overlay changed."*
  3. *"The detail rows … review, try, social media in the carousel cards are gone except the emoji."*
  4. *"Where are the 'glassneoism' effects on the carousel cards except those in focus (opaque 95%), in both light and dark modes."*

- **[DELTA — report 2 (fixed)]** `web/cuisine/src/v2/App.jsx`. The floating folio picker panels
  (L4245/L4331) carried a full `border`, so their **top** border drew a horizontal line right under
  the header (introduced by the overlay change — before, the in-flow panel connected seamlessly to
  the active tab, whose bottom border is removed). Changed `border` → **`border-x border-b`** (no
  `border-t`) on both panels, so the solid panel meets the header with no line.

- **[NOT fixed — need an operator decision / clearer view]**
  - **Report 4** is largely the **v0.62.209 design**: the `.liquid-glass` **dark-mode** override forces
    `background-color: color-mix(in srgb, var(--tg-bg) 90%, #000 10%) !important` (near-opaque) *because
    glass was "WASHED-OUT + unreadable in dark mode"*. Making the non-focused cards glassy in dark mode
    **reverses** that readability fix — flagged the tradeoff to the operator rather than silently
    regress it. (In light mode the peek cards already carry `bg-tg-card/70 .liquid-glass` = frosted.)
  - **Report 1** (tab vertical line) and **Report 3** (detail rows "gone except emoji") aren't
    isolable from the phone shots: the collapsed carousel cards hide review/try/social behind the
    "details, review & links" toggle **by design**, and the peek cards are cut at the screen edge
    (so only fragments/emoji show). Asked the operator to confirm what they expect.

- **[VERIFICATION (sandbox)]** Cuisine `vite build` green; full `npm test` **3719/3719 across 145
  files**; `package.json` 0.62.582 → 0.62.583; `.serial-state.yml` commit 1059→1060, build 682→683,
  journal 926→927, re-anchor 18-07 04:01 SGT. ⚠️ Live-only.

- **[STATUS]** DRAFT → squash-merge to `main` on green CI (auto-merge SQUASH).

- **[TEST]** Existing suite 3719/3719 (a border-side change).

- **[KNOWN GAPS]** Reports 1/3/4 pending operator input; card "right and bottom margin" (still awaiting
  a single-card close-up); Transport TMA port.

## Confirmation Gates
| Gate | Status | Note |
|------|--------|------|
| G1 | ✅ | PATCH bump 0.62.582 → 0.62.583; Cuisine build + 3719 tests green. |
| G2 | ➖ | no destructive action. |
| G3 | ⚠️ | report 4 (dark-mode glass) would reverse the v0.62.209 readability decision — surfaced to the operator, not changed. |
| G4 | ➖ | no external paid API call. |
