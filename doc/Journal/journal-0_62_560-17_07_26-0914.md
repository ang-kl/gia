(№ 917 - 17-07 '26 09:14 SGT) v0.62.560

# Journal — soleat v0.62.560 (Bib links un-underlined · closing a map card no longer adjusts the zoom, across all embedded-map TMAs)

> Operator (IMG_0700): (1) don't underline the Bib Gourmand hyperlinks in the carousel cards — and why
> do some cards show "# stalls · <status>" and some don't; (2) when I close the information card for a
> station in the embedded Google map, don't adjust the zoom — bake this into ALL TMAs that use the
> embedded Google-map layout.

## New

### [HDR] #917 | 09:14 SGT 17-07-26 | v0.62.560 — Bib no-underline + no zoom-on-close (all TMAs) | 3 files | PR #(pending) |

- **[FIX 1 — Bib links, no underline].** `web/hawker/src/App.jsx` — the carousel/list Bib stall links
  are now `text-tg-accent` (accent colour marks them as links) **without `underline`** (operator).

- **[ANSWER — "# stalls" on some, not others].** No code change. The `🍳 N stalls · <status>` row comes
  from the data.gov.sg "Hawker Centres (GEOJSON)" join (`data/hawker-stalls.json`), which currently
  covers **37 / 122** centres — e.g. Market Street (53) is in it, but Chinatown Complex + Hong Lim are
  not, so those cards omit the row. It's dataset coverage, not a bug. (Deliberately NOT force-matching
  more via number-stripping like the Bib join — a wrong stall COUNT is worse than a missing one.)

- **[FIX 2 — closing a card no longer changes the zoom, all embedded-map TMAs].** The v0.62.115
  "return-to-prior-zoom on close" is removed so the map stays where the user left it:
  - `web/hawker/src/components/HawkerMapPanel.jsx` — dropped `prevFocusZoomRef` (the capture in
    `activateCentre` + the restore in `closeInfo`).
  - `web/cuisine/src/v2/components/MapPanel.jsx` — dropped `prevFocusZoomRef` (both capture sites —
    horizontal blink-only + vertical — and the restore in `closeInfo`).
  - Transport (`MrtMapPanel.jsx`) already had no zoom-restore on close — nothing to change.
  The tap-to-zoom-IN (17) on a pin/card tap is unchanged; only the zoom-OUT on close is gone.

- **[DELTA — housekeeping].** `package.json` 0.62.559 → **0.62.560**; Journal #917 + serial-state
  (commit 1034→**1035**, build 659→**660**, journal 916→**917**; re-anchor 17-07 '26 09:14 SGT,
  prior 916 demoted verbatim AU-7).

- **[VERIFICATION (sandbox)].** **Hawker + Cuisine vite builds green**; **full `npm test` 3719/3719
  across 145 files**. ⚠️ Live-only (iPad) — shipping live.

- **[STATUS]** STAGED → PROD on merge (operator "make it live").

- **[KNOWN GAPS].**
  1. 👀 **Device recheck** — Bib links no longer underlined; closing a station/centre card in Hawker
     AND Cuisine leaves the zoom untouched?
  2. 🧭 Stall-data coverage (37/122) — could be widened if the operator wants (with the wrong-count risk
     flagged).
  3. 🅺🅸🆅 Hawker CLEANING-closure still shelved; Q4 carpark repro still awaited.

## Confirmation Gates
| Gate | Status | Note |
|------|--------|------|
| G1 | ✅ | PATCH bump 0.62.559→0.62.560; UI + behaviour; Hawker+Cuisine builds + 3719 tests green |
| G2 | ➖ | no destructive action (removed a superseded behaviour per operator) |
| G3 | ⚠️ | reverses the v0.62.115 "return to prior zoom on close" rule, per explicit operator request, across all embedded-map TMAs |
| G4 | ➖ | no external paid API call |
