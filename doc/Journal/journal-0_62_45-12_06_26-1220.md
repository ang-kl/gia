(№ 699 - 12-06 '26 12:20 SGT) v0.62.45

# Journal — soleat v0.62.45 (cuisine "What to order" plate — Phase 2: per-dish native names + 📜 history cards)

> Operator: *"please continue to build"* — picking up the Phase 1 follow-up logged as the open
> Phase 2 in `journal-0_62_43-12_06_26-0805.md`'s `[KNOWN GAPS]`: *"curate per-dish history +
> native names into the overlay (the city-plates depth), unlocking per-dish fact cards."* Phase 1
> shipped the cuisine plate names-only; Phase 2 adds the curated depth so a tapped cuisine reads
> like the city plate — native script on each dish + a dismissible 📜 history card on the headliners.

> Commit 716 / journal 699 / build 439 (web/ changed). PATCH 0.62.44 → 0.62.45. PR #1007 (draft).

## New

### [HDR] #699 | 12:20 SGT 12-06-26 | v0.62.45 — cuisine plate Phase 2 (native names + 📜 cards) | nation-overlay.js + dish-food-group.js + web/cuisine (ArrivalPlate.jsx) + tests | 1 PR | 1 build |

- **[INTENT]** Give the cuisine "What to order" plate the city-plate depth: every dish shows its
  native-script name when curated, and the headliners carry a one-line curated history (📜 card),
  starting with the operator's exemplar cuisine (Georgian).

- **[DELTA]**
  - **`nation-overlay.js`** — `F()`/`D()` helpers take an optional 3rd `extra`
    (`{ local, note:{en,fr} }`); back-compatible (the ~65 other cuisines pass only
    `(name[, sharedWith])` and are byte-for-byte unchanged). **Georgian fully curated** as the
    proof set: Mkhedruli `local` on all 22 dishes + bilingual one-line `note` on the headliners
    and best-known dishes (khachapuri adjaruli ხაჭაპური აჭარული, khinkali ხინკალი, satsivi საცივი,
    churchkhela ჩურჩხელა, qvevri ქვევრი, lobio ლობიო, mtsvadi მწვადი, …).
  - **`dish-food-group.js`** — new `slimDish()` carries `local`/`note` onto headliners AND group
    chips (was `{ dish: d.name }` only, dropping the depth). Names-only dishes still get just
    `{ dish }` (no empty keys).
  - **`web/cuisine/src/v2/components/ArrivalPlate.jsx`** — cuisine branch shows the native name
    inline on every dish (rows + chips) and a dismissible 📜 fact card on each headliner row
    (curated `note`, EN/FR; never LLM at runtime). Reuses the existing `factIdx` machinery, keyed
    `'h'+i` to avoid collision with the city-plate's integer indices (only one branch renders).
  - **`index.js`** — no change: it passes `grouped.headliners`/`groups` straight through, which
    now carry the extra fields.
  - **`__tests__/dish-food-group.test.js`** (+1 case) — native `local` + headliner `note`
    carry-through; `lobio → ლობიო` survives onto a chip; a names-only dish gains no empty
    `local`/`note` keys.

- **[VERIFICATION (sandbox)]** `node --check` clean (`nation-overlay.js`, `dish-food-group.js`);
  full suite **3405/3405** green (+1); `web/cuisine` build green. Georgian headliners render the
  native script + tappable history; group chips carry native names (lobio/mtsvadi/…).

- **[STATUS]** **DRAFT** — PR #1007 (rides the session branch `claude/refine-local-plan-5vh1jm`).
  Railway on merge to main. CI running at journal time.

- **[TEST]** Select Georgian (alone) in SG → the plate header "What to order in 🇬🇪 Georgian";
  expand → 3 headliner rows each with native name + 📜 → tap 📜 → one-line history bubble; group
  sections (ascending) show native names on the chips. Tap a dish → fires the dish search.

- **[KNOWN GAPS / Phase 2b]**
  - **Fact cards on headliners only.** Native names render on every dish; the 📜 card renders on
    the 3 headliner rows. Group-chip dishes that carry a `note` ship the data but don't yet expose
    a card from the compact chip — Phase 2b.
  - **Georgian is the only fully-curated cuisine.** The mechanism is generic; the remaining ~65
    cuisines stay names-only (additive, no regression) until curated.
  - Places per-SKU telemetry remains the other open follow-up (unrelated to this arc).

- **Confirmation Gates**

  | Gate | Resolution | Date |
  |---|---|---|
  | G1 — version bump | PATCH `0.62.44 → 0.62.45`. | 12-06 '26 |
  | G2 — destructive action | None — additive; helper signature extended back-compatibly. | 12-06 '26 |
  | G3 — new decision rule | Per-dish `local` (native) + `note{en,fr}` curated in the overlay; headliners render a 📜 card (curated, never LLM). | 12-06 '26 |
  | G4 — paid external API call | None — pure in-memory from curated overlays. | 12-06 '26 |

## Amendments

- **`nation-overlay.js`** (F/D `extra` arg + Georgian curation) · **`dish-food-group.js`**
  (`slimDish` carry-through) · **`web/cuisine/src/v2/components/ArrivalPlate.jsx`** (native names +
  headliner 📜 card) · **`__tests__/dish-food-group.test.js`** (+1) · **`package.json`**
  `0.62.44 → 0.62.45` · **`doc/.serial-state.yml`** counters + anchor.

## Deleted

- (none) — additive; names-only cuisines and the city-plate path are untouched.
