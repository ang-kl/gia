# Journal — v0.62.624 (restore desktop auto-fullscreen — "why I cannot expand in telegram desktop")

_(№ 968 — 21-07 '26 15:52 SGT)_

## New

### [HDR] v0.62.624 — re-add the desktop requestFullscreen branch (reverses v0.62.623)

- **[INTENT]** Operator (after v0.62.623 removed the desktop branch): *"why I cannot expand in telegram
  desktop, did you get the code correctly."*

- **[RESEARCH / FACTS]** (WebSearch; `core.telegram.org` 403'd through the proxy fetcher):
  `requestFullscreen()` (Bot API 8.0) is the **only** programmatic way to enlarge a Mini App — `expand()`
  controls **height** only, and there is **no API to widen a desktop window**. On clients that don't
  support fullscreen, `requestFullscreen()` fires `fullscreenFailed` with **UNSUPPORTED** and no-ops.

- **[RE-DIAGNOSIS]** The operator's earlier *"stuck at expand mode"* (v0.62.617) proves their Telegram
  Desktop **does** support fullscreen — it went fullscreen; the **weird cards were the v0.62.622 classifier
  bug** (already fixed). So v0.62.623's removal of the desktop branch — plus my incorrect *"use the native
  corner button"* guidance — left **no code path to expand** on desktop = *"cannot expand."* My mistake.

- **[DELTA]** Reverses v0.62.623 (restores the v0.62.617 intent, now that the classifier is fixed):
  re-added the `desktopClient` (`tdesktop || macos`) branch to the fullscreen gate in
  **`web/cuisine/src/api/tg.js`**, **`web/hawker/src/tg.js`**, **`web/transport/src/tg.js`** — the
  condition is back to `desktopClient || (touchClient && coarse && minScreen >= 700)`. Added a
  `fullscreenFailed` `onEvent` listener in each so an **UNSUPPORTED** client fails silently (`expand()`
  height still applies). `web/menu` (iPad-only) + `web/oversight` (no fullscreen) unchanged.

- **[VERIFICATION (sandbox)]** `node --check` clean on the three files; `desktopClient` present in all
  three; `cuisine + hawker + transport` `vite build` green; full `npm test` **3756/3756 across 148 files**.
  `package.json` + lockfile 0.62.623 → 0.62.624; `.serial-state.yml` commit 1105→1106, build 725→726,
  journal 967→968, re-anchor **21-07 '26 15:52 SGT**.

- **[TIME ANCHOR]** data.gov.sg blocked by the environment network policy (`403` `CONNECT`); anchored to
  the container system clock `2026-07-21T07:52Z` = **21-07 '26 15:52 SGT** (TF-9 terminal-runtime source).

- **[STATUS]** DRAFT → squash-merge to `main` on green CI (auto-merge SQUASH). New PR (branch restarted
  from `origin/main` `9562f6f` = `#1631` merged).

- **[TEST]** No new unit test — a platform-gate condition change verifiable only on a real Telegram Desktop
  client; verified via build + `node --check` + the full suite staying green.

- **[KNOWN GAPS / caveats]** **CANNOT** verify on real Telegram Desktop in-sandbox (platform-gated). On the
  operator's client (shown to support fullscreen) the app now expands wide again **and** renders correctly
  (v0.62.622). **Flip-flop trail** for honesty: v0.62.617 enabled → v0.62.623 removed → v0.62.624 restored;
  the churn came from mis-reading *"wrong system command"* as *"don't fullscreen"* when the real defect was
  the classifier bug. **Follow-up OFFERED:** a **manual in-app fullscreen TOGGLE button** (user-gesture
  `requestFullscreen`/`exitFullscreen`, respecting `fullscreenChanged`) instead of auto-on-load, if the
  operator prefers control over an automatic expand.

## Confirmation Gates
| Gate | Status | Note |
|------|--------|------|
| G1 | ✅ | PATCH bump 0.62.623 → 0.62.624; three builds + 3756 tests green; `node --check` clean; `desktopClient` restored in all three. |
| G2 | ➖ | re-adds a previously-present code path + a failure listener; nothing removed. |
| G3 | ➖ | no new decision rule (restores the v0.62.617 policy per operator). |
| G4 | ➖ | no external paid API call. |
