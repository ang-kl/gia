(№ 807 - 01-07 '26 11:40 SGT) v0.62.453

# Journal — soleat v0.62.453 (Cuisine — "Dishes" reveal per cuisine, pop-up)

> Operator: "we curated a lot of dishes/cooking methods by cuisine; include it in Cuisine & Filters
> › by Cuisine, one tappable word 'Dishes' … clicking opens a pop-up like Pick local classic …
> font size (light/dark) same as the footer hide/list. Dishes only. Cuisine TMA only."

## New

### [HDR] #807 | 11:40 SGT 01-07-26 | v0.62.453 — by-cuisine "Dishes" pop-up | 6 files | PR #(pending) |
- **`index.js`** — new **`GET /api/cuisine/dishes?slug=`** → `nation-overlay.getNationOverlay(slug).iconicDishes`
  → `[{ name, local, kind }]`. Read-only, no auth (public curated data).
- **`web/cuisine/src/v2/components/CuisineCategoryDrawer.jsx`** — each cuisine in the drill-in now
  carries a tappable **"Dishes"** styled **`text-[11px] font-semibold text-tg-link`** — identical to
  the footer hide/list (theme-aware in light + dark). Tapping opens a **pop-up modal** (z-40, same
  look as "Pick local classic": 2-col grid of dish name + native `local` script, subtle ✕). Tapping
  a dish calls `onPickDish` → runs a dish search.
- **`CuisineDrawer.jsx` + `App.jsx`** — thread `onPickDish` down; the App handler mirrors the
  classic-picker `onTryDish` (`freeTextOverride` + `hawkerFirst` in SG) but closes the cuisine
  picker instead of the classic one.
- **`web/cuisine/src/v2/lib/i18n.js`** — `cat.dishes` (en "Dishes" / fr "Plats"; other locales
  EN-fallback, per the tiered control-label strategy).
- **[VERIFICATION (sandbox)]** `esbuild` parse (App, CuisineDrawer, CuisineCategoryDrawer);
  `node --check` index.js + i18n. Full cuisine build + vitest gated by CI.
- **[STATUS]** PROD — server + cuisine TMA rebuild on merge.
- **[KNOWN GAPS]** Cooking-methods/"Techniques" deferred (operator: dishes only this round).
  Sketchbook untouched.

## Confirmation Gates
| Gate | Status | Note |
|------|--------|------|
| G1 | ✅ | esbuild parse; CI builds cuisine |
| G2 | ⏳ | full vitest via CI |
| G3 | ✅ | serial 806→807 |
| G4 | ⏳ | redeploy + operator eyeball |
