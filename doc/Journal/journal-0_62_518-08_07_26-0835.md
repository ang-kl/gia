(№ 874 - 08-07 '26 08:35 SGT) v0.62.518

# Journal — soleat v0.62.518 (Transport: CCL6 station coordinate accuracy — Keppel / Cantonment / Prince Edward Road)

> Operator task **#17**: *"Curate 3 new MRT stations (Keppel, Cantonment, Prince Edward Road) for
> 12 Jul 2026 launch."* Reply **1** — *"prep the launch-flip PR now + source accurate coords."*
> The three Circle Line Stage 6 stations were **already** curated in `data/mrt-coords.json` (codes
> CC30–32, CCL line, `opensDate "12 July 2026"` en+fr, `status "future"`). An audit found their
> stored `lat`/`lng` sit notably **off** the real positions. This PR corrects the coordinates only;
> the `future` → `operational` **launch flip** is a separate, held PR for 12 Jul (merging it early
> would show the stations open before service starts).

## New

### [HDR] #874 | 08:35 SGT 08-07-26 | v0.62.518 — CCL6 station coordinate accuracy | 4 files | PR #(pending) |

- **[INTENT].** Operator #17 / reply **1**. Curate the 3 CCL6 stations for the 12 Jul 2026 launch.
  On inspection the base curation (codes / line / opening date / status) was already present and
  shipped; the actionable gap was **pin accuracy** — the stored coordinates were carried at low
  precision and drift ~150–500 m from the true station positions. Source of truth for the
  correction: the Wikipedia station infoboxes (LTA-derived).

- **[DELTA].**
  - `data/mrt-coords.json` — corrected `lat`/`lng` for the 3 CCL6 stations. This is the **single**
    source of truth for station coordinates (`web/transport/src/data/line-paths.js` uses station
    **names** only; `web/cuisine/src/v2/lib/mrt-stations.generated.js` does not carry these three):
    - **Keppel** (CC30): `1.2705, 103.8266` → `1.27, 103.83111` (lng ~500 m E).
    - **Prince Edward Road** (CC32): `1.2767, 103.845` → `1.27333, 103.84722` (~450 m S).
    - **Cantonment** (CC31): `1.2741, 103.8359` → `1.27278, 103.83667` (~165 m).
  - `__tests__/transport-line-paths.test.js` — the ring-closure test keeps its **own inline**
    `STATIONS` fixture (it does not read the JSON), with the three CCL6 rows labelled as the real
    stations. Updated the fixture rows (lines 90–92) and the matching `ring).toContainEqual(...)`
    assertions (166–168) to the corrected coordinates so the fixture stays honest to the data.
    Green by construction (assertion literals equal fixture literals; `line-paths.js` passes
    coords through unchanged).
  - `package.json` — 0.62.517 → **0.62.518** (a version bump also triggers the Railway redeploy so
    the corrected pins reach the live Transport TMA).
  - `doc/Journal/journal-0_62_518-08_07_26-0835.md` — this entry.
  - `doc/.serial-state.yml` — commit 991→**992**, journal 873→**874**, build 617→**618**;
    re-anchor 08-07 '26 08:35 SGT (prior 873 anchor demoted to `..._superseded_873`).

- **[VERIFICATION (sandbox)]** `JSON.parse(data/mrt-coords.json)` → OK. `npx vitest run
  __tests__/transport-line-paths.test.js` → **24/24 green**. Full `npx vitest run` → **3699
  passed / 144 files** (exit 0). No TMA source changed → no web build required (the JSON is
  read server-side by `index.js` and served via `/api/transport/stations`).

- **[STATUS]** PROD — merges to main; the per-PR bot redeploys. The pin correction is live as soon
  as the Transport TMA refetches `/api/transport/stations`. The stations remain **greyed / future**
  on the map — no visible "open" change until the launch-flip PR lands.

- **[TEST]** `__tests__/transport-line-paths.test.js` (ring closure through the CCL6 arc) updated
  and green. Full suite green. Outside the `gia-preflight` trigger set (no `index.js` / bot-handler
  / search / matcher / i18n surface — a data value + test-fixture change).

- **[KNOWN GAPS].**
  1. 🗓️ **Launch flip still due** — `status "future"` → `"operational"` + drop `opensDate` /
     `opensDateFr` / `opensYear` for the 3 stations, to land **on 12 Jul 2026** (held PR; must not
     merge early).
  2. 📍 **Exit-precise centroids** — `data/stations.json` (accurate `exit_centroid`, the LTA
     GeoJSON exit mean) has **no** entries for the 3 CCL6 stations, and the in-repo
     `geoloc/LTAMRTStationExitGEOJSON.geojson` predates CCL6. The corrected mrt-coords values are
     station-centre accurate; exit-mean precision would need fresh LTA/OneMap exit data.
  3. Coordinates sourced from Wikipedia infoboxes (LTA-derived); [UNVERIFIED] against a primary
     LTA DataMall / OneMap pull.

## Confirmation Gates
| Gate | Status | Note |
|------|--------|------|
| G1 | ✅ | JSON.parse OK; vitest transport-line-paths 24/24; full suite 3699/144 green; version 0.62.517→0.62.518 |
| G2 | ✅ | task #17 = curate CCL6 stations; base curation already shipped, so this PR closes the pin-accuracy gap (3 coords corrected vs Wikipedia/LTA); launch flip deliberately deferred to a held 12-Jul PR |
| G3 | ✅ | serial commit 991→992, journal 873→874, build 617→618; anchor 08-07 '26 08:35 SGT (prior 873 demoted) |
| G4 | ➖ | operator eyeball on the live Transport TMA map (the 3 pins now sit on the real streets) once redeployed |
