(№ 767 - 28-06 '26 15:10 SGT) v0.62.413

# Journal — soleat v0.62.413 (community sub-grouping in "Pick local classic" — SG)

> Operator: "further segment for SG/MY/TH/PH/ID … which major community would you find this
> dish/drink/dessert (e.g. Chendol by Straits Chinese more than Malay …, Indonesian sell Chendol)."
> Chose: sub-group by community within each meal section; ambiguous → a 'Shared' bucket.

## New

### [HDR] #767 | 15:10 SGT 28-06-26 | v0.62.413 — community sub-grouping (SG) | 3 files | PR #(pending) |
- **[INTENT]** Inside each meal section (Breakfast / Lunch & Dinner / Snacks / Desserts / Drinks),
  nest the classics under community sub-headers (Chinese / Straits Chinese / Malay / Indian /
  Indonesian / Eurasian / Shared).
- **[DELTA]**
  - **NEW `dish-community.js`** — `communityFor(countryCode, name, sharedWith)` → community key.
    DATA-GROUNDED: a curated SG map built from `nation-overlay.js`'s OWN section structure
    (// Indian-Singaporean, // Hokkien-SG, // Peranakan-SG …) + a `sharedWith` rollup
    (Hokkien/Teochew/Cantonese/Hainanese/Hakka → Chinese; Peranakan → Straits Chinese; Malaysian
    → Malay; Indonesian; Indian). Genuinely pan-ethnic dishes → `shared` (operator-approved, not
    force-assigned). Non-SG countries → `null` (flat list, back-compat) until their maps land.
  - **`city-plates.js`** — `_overlayDishMeta` now attaches `meta.community` to every classic
    (merge-preserve so the curated `classics-notes.js` batch doesn't strip it). Flows to the
    client via `classicGroups[].dishes[].community`.
  - **`web/cuisine/src/v2/components/ArrivalPlate.jsx`** — `groupByCommunity()` + localized
    `COMMUNITY_LABEL`/`COMMUNITY_ORDER`; each meal section renders community sub-headers
    (`— Straits Chinese —`) when dishes are tagged, else the flat middle-dot list. Extracted
    `dishLink` so flat + sub-grouped paths share one renderer.
- **[VERIFICATION (sandbox)]** `esbuild` parse OK (ArrivalPlate); `node --check` OK
  (dish-community, city-plates); `communityFor` spot-tests pass (thosai→indian,
  kueh salat→straits-chinese, chilli crab→chinese, coconut shake→shared, nasi padang→indonesian,
  unknown-SG→shared, TH→null). Additive `community` field doesn't break city-plates.test.js
  (field-specific assertions).
- **[STATUS]** PROD — server data + cuisine TMA rebuild; redeploys per PR.
- **[TEST]** Existing city-plates.test.js + TMA build in CI. No new unit test this pass.
- **[KNOWN GAPS]** SG only — MY/TH/PH/ID community maps are the follow-up (sharedWith rollup
  already covers their tagged dishes; untagged → 'shared' fallback only fires for SG, so non-SG
  stays flat until curated). Community labels are en/fr only (other locales fall back to en).

## Confirmation Gates

| Gate | Status | Note |
|------|--------|------|
| G1 build/parse | ✅ | esbuild + node --check |
| G2 tests | ⏳ | CI Vitest + TMA build |
| G3 journal/serial | ✅ | this entry; serial 766→767 |
| G4 live deploy | ⏳ | redeploy; eyeball pending |
