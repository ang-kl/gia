(№ 435 - 28-05 '26 17:30 SGT) v0.61.234

# Journal — soleat v0.61.234 (Rare-cuisine fixes: Australasia demote + Georgian keywords + sparse-coverage hint)

> Operator (28-05 '26) selected 3 of 4 fixes after the rare-cuisine
> investigation: Australasia catch-all out, Georgian keywords +
> override in, sparse-coverage hint on. Eastern European OR-fanout
> chip deferred.
>
> Commit 447 / journal 435 / build 237, on branch
> `claude/v0.61.234-rare-cuisine-fixes` off `main` at v0.61.233.

## New

### [HDR] #435 | 17:30 SGT 28-05-28 | v0.61.234 — rare-cuisine fixes | 5 files | 1 PR | 1 build |

- **[INTENT]** Reduce false-bug perception on rare cuisines (operator's earlier "Australasia returned only 3" report) by (a) removing the catch-all that drove that, (b) broadening Georgian's signal so the few real venues actually surface, (c) telling the user when low-count results are the real ceiling, not a search bug.

- **[DELTA]**

  **1. `doc/Feature/cuisines_js.MD` — remove `'Australasia'` catch-all.**
  ```diff
  -      'Australian', 'New Zealand', 'Australasia', 'Burmese'
  +      'Australian', 'New Zealand', 'Burmese'
  ```
  Australian + New Zealand stay; only the umbrella catch-all is gone (Places couldn't match "Australasia" as a strong term — most venues self-identify as "Modern Australian" / "Contemporary Australian" or by their own name).

  **2. `cuisine-dish-keywords.js` — add Georgian section.**
  ```js
  georgian: ['khachapuri', 'khachapuri adjarian', 'imeretian', 'megrelian',
             'khinkali', 'lobio', 'pkhali', 'shoti', 'tonis puri', 'shotis puri',
             'chacha', 'badrijani', 'badrijani nigvzit',
             'mtsvadi', 'mtsvadi shashlik', 'churchkhela', 'satsivi', 'tkemali',
             'kharcho', 'chakhokhbili', 'ostri', 'kupati', 'tarragon',
             'georgian', 'caucasus', 'caucasian',
             'ქართული', 'ხაჭაპური', 'ხინკალი'],
  ```
  Previously the post-filter dropped legitimate Caucasus venues because Georgian was outside the file's "African / European / Americas" scope.

  Bonus: added `'bobotei'` as an alias for `'bobotie'` in the South African list (operator-spotted historical typo in some Places venue names).

  **3. `index.js` — `SEARCH_QUERY_OVERRIDE` populated.**
  ```js
  // v0.61.234 — was {} (no overrides). Georgian now broadens.
  const SEARCH_QUERY_OVERRIDE = {
    'Georgian': 'Georgian Caucasus cuisine restaurant Singapore'
  };
  ```
  Places gets a richer signal vs the default `"Georgian restaurant Singapore"` which couldn't fan out the few SG matches.

  **4. `web/cuisine/src/v2/App.jsx` — sparse-coverage hint.**
  - New `sparseNotice` state.
  - Response handler: when the user's cuisines include any of `{african, south-african, georgian}` AND the result count is 1–5, set `sparseNotice` to the matching slug.
  - Render an amber-bordered card under the special-mode notice with text "Limited coverage in Singapore — Google Maps has few {cuisine} restaurants listed. Showing all matches." (FR equivalent included).

  **5. `package.json`** — `0.61.233 → 0.61.234`.

  **6. `doc/.serial-state.yml`** — commit 446→447, build 236→237, journal 434→435; re-anchored 28-05 '26 17:30 SGT.

- **[VERIFICATION (sandbox)]**

  ```
  $ node --check index.js                           (OK)
  $ node --check cuisine-dish-keywords.js           (OK)
  ```

  TMA build deferred to CI (the recurring local iCloud / Node-24 PostCSS quirk).

- **[STATUS]** PR-OPEN on push.

- **[TEST]** Operator (post-merge + Railway redeploy):
  1. Cuisine TMA accordion (Common Here group) — "Australasia" chip is gone; "Australian" and "New Zealand" remain.
  2. Pick "Georgian" → fewer false drops; Suprë by Sandro and any Caucasus-adjacent venue surfaces (post-filter no longer drops Georgian names).
  3. Pick "African" alone → if result count ≤ 5, amber "Limited coverage" hint appears below the search-criteria card.
  4. Pick "Georgian" alone → same hint, with "Georgian" inserted.
  5. Pick a high-coverage cuisine (Japanese, Korean) → hint stays hidden.

- **[KNOWN GAPS]**
  - The Eastern European OR-fanout chip operator-deferred. Catalogue still has no selectable Eastern Europe umbrella.
  - Sparse-slug set is fixed at three. Could expand to JP-rare / KR-rare cuisines if Places coverage there is also thin (Mongolian, Tibetan, etc.).
  - Hint i18n is hardcoded en/fr inline. A future PR could move to `t('cuisine.sparseLimited', lang)` proper i18n key.

- **Confirmation Gates**

  | Gate | Resolution | Date |
  |---|---|---|
  | G1 — version bump | PATCH `0.61.233 → 0.61.234`. | 28-05 '26 |
  | G2 — destructive action | YES — `'Australasia'` catalogue entry removed (operator-confirmed). Australian + New Zealand chips preserved. | 28-05 '26 |
  | G3 — new decision rule | When Places coverage is the real ceiling for a cuisine, surface a "limited coverage" hint at ≤ 5 results so the user doesn't perceive the count as a search bug. | 28-05 '26 |
  | G4 — paid external API call | _n/a in this PR_. | 28-05 '26 |

## Amendments

- **`doc/Feature/cuisines_js.MD`** — Common Here items list loses `'Australasia'`.
- **`cuisine-dish-keywords.js`** — adds `georgian:` section (16 dish names + 3 demonyms + 3 Mkhedruli aliases); South African gains `'bobotei'` alias.
- **`index.js`** — `SEARCH_QUERY_OVERRIDE` populated with the Georgian entry.
- **`web/cuisine/src/v2/App.jsx`** — `sparseNotice` state + render.

## Deleted

- `'Australasia'` cuisine catch-all (the slug remains as a *category* bucket, holding Australian + New Zealand entries).
