(№ 493 - 30-05 '26 22:35 SGT) v0.61.292

# Journal — soleat v0.61.292 (Removed Features RF-12 → RF-15 in feature-0_61_83 master)

> Operator (30-05 '26 22:25 SGT, post-PR-#797 merge): *"6"* — clear the accumulated *"Removed Features / amendments table"* known-gaps that the v0.61.279 / .280 / .284 / .285 journals had been flagging.
>
> **Course correction**: the prior journals referenced `doc/Feature/cuisines_js.MD` as the target, but that file is a JS-source snapshot of the cuisine catalogue (legacy `// v0.23.0 cuisine catalogue` doc) without any Removed table. The actual Removed Features table sits at the EOF of `doc/Feature/feature-0_61_83-21_05_26-1645.md` (the v0.61.83 fresh-master document), where rows RF-1 through RF-11 already track the v0.60.193 → v0.61.79 arc.
>
> v0.61.292 appends rows RF-12 → RF-15 covering 4 removals across the v0.61.84 → v0.61.291 arc (208 patches, 10 days):
>
> - **RF-12**: `POST /api/cuisine/place-search-by-country` route (v0.61.191 → v0.61.279)
> - **RF-13**: 6-second idle timer on the "Tap 🔍 to search" hint (v0.61.244 → v0.61.284)
> - **RF-14**: `fun-facts.json` data file (v0.61.285 → v0.61.290)
> - **RF-15**: `region: b.region || 'SG'` first-paint anchor fallback in Menu TMA (v0.61.123 → v0.61.279)
>
> Frontmatter of the v0.61.83 file gains a note acknowledging the in-place extension under AU-1 / AU-EOF (append-only). A future master refresh at v0.62.x+ will fold the new active state into §1–§3; the Removed table stays authoritative in the meantime.
>
> Commit 506 / journal 493 / build 295 / feature 49, on branch
> `claude/v0.61.292-removed-features-rf12-rf15` off `main` at v0.61.291.

## New

### [HDR] #493 | 22:35 SGT 30-05-30 | v0.61.292 — Removed Features RF-12 → RF-15 | 3 files | 1 PR | 1 build |

- **[INTENT]** Close the four [KNOWN GAPS] entries from the v0.61.279 / .280 / .284 / .285 journals that said *"the Removed-Features table in `doc/Feature/cuisines_js.MD` should gain a row for this PR per AU-2"*. Both the target file (was the wrong one) and the rows themselves are addressed in this single patch.

- **[DELTA]**

  **1. `doc/Feature/feature-0_61_83-21_05_26-1645.md`** — append-only extension under AU-1 / AU-EOF:

  - **Frontmatter line added**: `> **Removed Features table — incremental extension (30-05 '26):** rows RF-12 → RF-15 below were appended in v0.61.292 …`. Sits below the existing "Reflects: project state at v0.61.83" line so a reader sees up-front that the table runs past the master's nominal date.

  - **Four new rows in the Removed Features table**:

    | # | Item summary | Removed in | Replacement |
    |---|---|---|---|
    | RF-12 | `/api/cuisine/place-search-by-country` route + `OTHER_COUNTRY_CODES` set (~183 lines) | v0.61.279 | `/api/cuisine/place-autocomplete` |
    | RF-13 | 6-second `setTimeout` before showing the "Tap 🔍 to search" speech bubble + clear-on-keystroke | v0.61.284 | Immediate-show on `query.trim().length >= 1 && open`; persists while typing |
    | RF-14 | `web/cuisine/src/v2/data/fun-facts.json` (40-entry JSON array) | v0.61.290 | `web/cuisine/src/v2/data/fun-facts.js` with `export default [...]` |
    | RF-15 | `region: b.region \|\| 'SG'` first-paint anchor-setter fallback in Menu TMA App.jsx (two callsites) | v0.61.279 | `region: b.region \|\| null` + `anchorLoading` gate + `regionUnresolved` flag |

    Each row carries full reason narrative, replacement detail, AU-2 rule reference, and the serial number from its originating journal (№ 480 / 485 / 491 / 480 respectively).

  **2. `package.json`** — `0.61.291 → 0.61.292`.

  **3. `doc/.serial-state.yml`** — commit 505→506, build 294→295, journal 492→493, **feature 48→49** (this is the first Feature-folder edit since v0.61.83 — the counter had been static); re-anchored 30-05 '26 22:35 SGT.

- **[VERIFICATION (sandbox)]**

  - Markdown lint on the four new rows: 9-column alignment matches the existing 11 rows (id / item / orig / removed / date / reason / replacement / rule / serial).
  - No code change.
  - Vitest still BLOCKED locally by recurring iCloud `ERR_INVALID_PACKAGE_CONFIG` (Register O-22 + O-37). CI authoritative — expected to stay green, no test contract changed.

- **[STATUS]** PR-OPEN on push. Railway no-op deploy (docs-only); no TMA bundle affected.

- **[TEST]** Operator (post-merge):
  1. **Table extension visible.** Open `doc/Feature/feature-0_61_83-21_05_26-1645.md`. The Removed Features table now ends at row RF-15; the frontmatter has the new "incremental extension (30-05 '26)" line right before §0.
  2. **No regression in §1–§3.** The active-feature summary, per-feature detail, and evolution highlights sections are unchanged — only the EOF table grew.

- **[KNOWN GAPS]**
  - **A full v0.61.292 feature master refresh** would update §1–§3 to reflect the v0.61.84 → v0.61.291 arc (208 patches). Deferred — no operator request, and the per-PR journals cover the active-feature changes individually.
  - **`doc/Feature/cuisines_js.MD`** stays as-is (legacy JS-source snapshot). If the operator wants it folded into the next master refresh, that's a separate decision.
  - **A few v0.61.286 / .287 / .288 / .290 / .291 changes aren't in the Removed table** because they're amendments (logic refinements, scriptVersion bumps, selector tiering, blob restoration) rather than feature removals. The journal trace covers them.

- **Confirmation Gates**

  | Gate | Resolution | Date |
  |---|---|---|
  | G1 — version bump | PATCH `0.61.291 → 0.61.292`. Pure docs change. | 30-05 '26 |
  | G2 — destructive action | _n/a_. Append-only edit; rows RF-1 → RF-11 unchanged. AU-1 / AU-EOF preserved. | 30-05 '26 |
  | G3 — new decision rule | _n/a_. Codifies the existing AU-2 contract. | 30-05 '26 |
  | G4 — paid external API call | _n/a_. | 30-05 '26 |

## Amendments

- **`doc/Feature/feature-0_61_83-21_05_26-1645.md`** — appended RF-12 / RF-13 / RF-14 / RF-15 to the Removed Features table; added one-line frontmatter note acknowledging the v0.61.292 extension. Rows RF-1 → RF-11 are unchanged verbatim.

## Deleted

_None._
