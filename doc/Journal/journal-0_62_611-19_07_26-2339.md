# Journal — v0.62.611 (Train 2K carousel: uniform + centered · Hawker: NEA ↗ moves to the footer under ↩ back)

_(№ 955 — 19-07 '26 23:39 SGT)_

## New

### [HDR] v0.62.611 — the Train carousel stops sprawling on 2K, and Hawker's NEA link becomes a plain footer entry under ↩ back

- **[INTENT]** Operator, two asks:
  1. *"Train TMA if it isn't a list, then should show the carousel cards. why you didn't set for 2K
     aspect ratio"* — on a large **2K** screen (a 2560-wide desktop / a 1440×2560 big tablet) the
     non-list carousel cards **rendered but sprawled edge-to-edge** as a **ragged-height** strip (the
     v0.62.609 `items-start` sized each card to its own content — HarbourFront tall, the rest short),
     with the map filling the huge empty area above.
  2. *"In hawker TMA, place the NEA ↗ below the ↩ back without the pill style, same font, font size &
     font style as ↩ back."*

- **[DECISION — G-ask]** For (1) I asked the operator (AskUserQuestion) how the 2K carousel should
  behave; they chose **"Uniform, centered row"** (keep the bottom carousel, make cards the same height,
  constrain/center so it doesn't sprawl) over a bigger two-panel redesign.

- **[DELTA — Train 2K carousel]** `web/transport/src/App.jsx` `StationCarousel` track:
  `items-start` → **`items-stretch`** (cards share one uniform height — no ragged strip) **+
  `max-w-6xl mx-auto`** (~1152 px) so on a 2K screen the row is **centered** and doesn't run
  edge-to-edge. Note the phone no longer uses this carousel (phone-portrait is the v0.62.609 **drawer**),
  so `items-stretch` does **not** bring back the phone "takes too much space" tall-empty-card problem —
  that was a phone-carousel issue; the carousel now serves only phone-landscape / tablet / desktop,
  where `max-h-[44vh]` still caps height.

- **[DELTA — Hawker NEA link]** `web/hawker/src/App.jsx`: the **NEA ↗** link **moves off the header**
  (it was a `skeuo-pill` in all three Hawker header layouts — landscape-carousel, portrait two-panel,
  and the drawer) **into the `FooterDock`**. It now sits in a `flex-col` stack **directly under the
  ↩ back / 🔚 end control**, as a plain `<button>` (`px-2 py-1.5 rounded-lg`, **no pill background**)
  that inherits the footer row's `text-[11px] font-semibold text-tg-link` — i.e. the **same font / size /
  style as "↩ back"**. All three header pill instances removed (`openLink` + `NEA_HOME` are module-scope,
  so `FooterDock` uses them directly).

- **[VERIFICATION (sandbox)]** **Playwright** confirmed: (a) Train **2K landscape (2560)** + **2K
  portrait-touch (1440×2560)** — the carousel cards now sit **uniform-height + centered** (`max-w-6xl`),
  not sprawling; (b) Hawker phone drawer footer — **"NEA ↗" now sits below "↩ back"** as plain text (no
  pill), with the header pill gone — no page errors. **Transport** `vite build` green
  (`index-5urxVOZn.js 270.99 kB │ gzip 88.27 kB`); **Hawker** `vite build` green (`index-B-r2SKdD.js
  260.29 kB │ gzip 84.60 kB`); full `npm test` **3738/3738 across 147 files**; `package.json` +
  `package-lock.json` 0.62.610 → 0.62.611; `.serial-state.yml` commit 1090→1091, build 712→713, journal
  954→955, re-anchor **19-07 '26 23:39 SGT**.

- **[TIME ANCHOR]** data.gov.sg blocked by the environment network policy (`403` `CONNECT`); anchored to
  the container system clock `2026-07-19T15:39Z` = **19-07 '26 23:39 SGT** (TF-9 terminal-runtime source).

- **[STATUS]** DRAFT → squash-merge to `main` on green CI (auto-merge SQUASH). New PR (branch restarted
  from `origin/main` `f50311b`, #1618 merged — to avoid the squash-divergence conflict).

- **[TEST]** No new unit test (CSS-class + markup relocation; verified visually via Playwright). Existing
  suite unchanged and green.

- **[KNOWN GAPS]** The 2K carousel is centered at `max-w-6xl` (~1152 px) — a deliberately readable width
  rather than filling all 2560 px; widen if the operator wants more of the screen used. The map in the
  screenshots shows "Map failed to load" only because the sandbox stub has no Google key (fills in prod).
  Carried: the drawer doesn't auto-expand on a collapsed-sheet pin tap; the v0.62.610 `disableVerticalSwipes`
  fix is still UNVERIFIED on a real phone; `mrt.*` strings en/fr-lean; Cuisine `tg.js` safe-area gap.

## Confirmation Gates
| Gate | Status | Note |
|------|--------|------|
| G1 | ✅ | PATCH bump 0.62.610 → 0.62.611; both builds + 3738 tests green; both changes verified via Playwright. |
| G2 | ➖ | additive — carousel classes changed; the NEA pill is relocated (header → footer), not deleted; nothing lost. |
| G3 | ➖ | no new decision rule (a requested layout choice, confirmed via AskUserQuestion). |
| G4 | ➖ | no external paid API call. |
