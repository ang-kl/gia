(№ 317 - 23-05 '26 13:52 SGT) v0.61.116 — vault pass

# Journal — soleat v0.61.116 (vault snapshot `vault/v0.61.116/`)

> Operator instruction (23-05 '26): *"vault first, then do a code audit"* —
> take a fresh `vault/<version>/` reproducible snapshot at the current head,
> then proceed to a full v0.61.116 code audit (separate workstream).
>
> This entry is commit 324 / journal 317 (no build, no version bump —
> a vault pass copies the existing tree, it does not change code).
> Journal #317 follows #316 (the v0.61.116 feature entry).

## New

### [HDR] #317 | 13:52 SGT 23-05-26 | v0.61.116 — vault snapshot `vault/v0.61.116/` | ~154 MB | 932 files | 1 PR | 0 build |

- **[INTENT]** Capture a frozen, bootable, append-only snapshot of the
  repository at v0.61.116 — rollback safety + reproducibility + audit
  trail (`doc/CLAUDE-FULL.md` §16.1). The operator paired this with a
  follow-on code-audit pass; the audit is a separate workstream and is
  not part of this journal entry.

- **[DELTA]**
  - **`vault/v0.61.116/`** (new, ~154 MB, 932 files) — a faithful mirror
    of the repo at v0.61.116: `index.js` + ~80 root modules, `web/<app>/
    src`, `scripts/`, `public/app.js`, `data/`, `data_realtime/`,
    `geoloc/`, the `__tests__/` suite, and the full `doc/` system. Built
    in a fresh `/tmp` clone with `git archive HEAD | tar -x` (working
    entirely off iCloud Drive to avoid the iCloud-bound `lstat` storms
    that wedged the iCloud working-tree checkout earlier in the session
    — recorded in `[KNOWN GAPS]` below). The §16.1 strict exclusions
    apply: `node_modules/` (root + nested), `.git/`, `vault/`, `dist/` +
    `web/*/dist/`, `public/{cuisine,menu,hawker,transport,oversight}/`
    (Vite build output — gitignored, so absent from `git archive`),
    `*.log`, `.env`, `.claude/settings.local.json`, `tmp/`.
    `.env.example`, `public/app.js`, `public/index.html`, `public/doc/`
    and `geoloc/*.geojson` are kept.
  - **`vault/v0.61.116/VAULT_README.md`** (new) — the required §16.1
    sections: headline blurb, captured timestamp, exclusion table,
    file count + size, boot instructions, and the condensed
    v0.61.91 → v0.61.116 arc table (8 thematic rows covering 25 PRs).
  - **`doc/.serial-state.yml`** — `commit` 323 → 324, `journal` 316 →
    317 (`build` unchanged — no build); `last_anchor_*` re-anchored to
    23-05 '26 13:52 SGT (system clock UTC `2026-05-23T05:52:58Z`,
    Rule TF-9 NTP-synced); a v0.61.116-vault comment added.
  - No code file changed; `package.json` is NOT bumped — a vault pass
    snapshots the existing v0.61.116 tree.

- **[VERIFICATION (sandbox)]**

  ```
  find vault/v0.61.116 -type f | wc -l              932 files
  du -sh vault/v0.61.116                            154 MB
  exclusion guard (vault, node_modules, .git)       none present inside
  spot check                                        index.js, package.json,
                                                    doc/, web/<5 TMAs>/src,
                                                    public/{app.js,index.html,doc}
                                                    present; public/<tma>/ absent
                                                    .env.example present; .env absent
  vault/v0.61.116/package.json version              0.61.116
  ```

- **[STATUS]** DRAFT — PR opened on `claude/v0.61.116-vault` (branched
  off `main` at v0.61.116, `4efd8e0`). The vault is committed data;
  nothing deploys.

- **[TEST]** Boot test (per `VAULT_README.md`): `cp -a vault/v0.61.116
  /tmp/soleat-v0.61.116 && cd … && npm install && npm test` reproduces
  the green suite. Not run in-sandbox (a full `npm install` in `/tmp`
  is outside this pass's scope) — the snapshot is a verbatim
  `git archive | tar -x` of the working tree, which already builds +
  tests green on the v0.61.116 main HEAD.

- **[KNOWN GAPS]**
  - **Rule V-3** — this snapshot's `vault/v0.61.116/doc/Journal/` does
    NOT contain this self-referential addendum (it documents the vault
    that contains it). Recorded here and in the creating commit message.
  - **Rule V-2** — `vault/v0.61.90/` is **kept**, not superseded: each
    vault is an append-only point-in-time snapshot (Rule V-1). This one
    captures the v0.61.91 → v0.61.116 delta (26 versions, 25 PRs); both
    coexist.
  - No fresh master document set (Builder / Persona / Feature /
    Technical / Legal / Register) was produced — the operator scoped
    this pass to the snapshot + a follow-on code audit; the master set
    is still recent (v0.61.83: Feature #48 / Technical #48 / Register
    #116; Builder / Persona / Legal at v0.61.76).
  - `doc/VibeCodingRecord/` regen still due — now ~78 PRs behind the
    last regenerated state (was ~52 at v0.61.112). Carried forward.
  - Side-effect of the same session: the closed/superseded PR #605
    ("docs: v0.61.112 journal STATUS post-merge update") — the v0.61.112
    journal entry's `[STATUS]` line still reads DRAFT on main, although
    the PR #604 squash `f50ab26` landed. Not re-applied here (operator
    chose to abandon the docs-redo branch in favour of vault + audit).
    A future docs pass should append the STATUS-flip amendment.
  - iCloud-bound git wedges from earlier in the session (stale
    `.git/index.lock` + multiple hung `git status` / `git checkout`
    processes) — the operator authorised killing them; root cause is
    iCloud Drive `lstat` latency on the 10 k-file working tree. Logged
    here as context for why this vault pass was built in a `/tmp` clone
    rather than the iCloud working tree.

- **Confirmation Gates**

  | Gate | Resolution | Date |
  |------|-----------|------|
  | G1 — version bump | _n/a_ — a vault pass; `package.json` stays 0.61.116 | 23-05 '26 |
  | G2 — destructive action | _n/a_ — purely additive (`vault/v0.61.116/` new); Rule V-1 — no prior vault deleted or rewritten | 23-05 '26 |
  | G3 — new decision rule | _n/a_ — operator instruction, no new rule | 23-05 '26 |
  | G4 — paid external API call | _n/a_ | — |

## Amendments

_None._

## Deleted

_None._
