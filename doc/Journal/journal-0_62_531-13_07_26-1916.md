(№ 887 - 13-07 '26 19:16 SGT) v0.62.531

# Journal — soleat v0.62.531 (Parity A: cuisine TMA MRT overlay — regen picks up the 3 CCL6 stations)

> Follows the cross-TMA station-card parity investigation (operator: *"check that the station card
> in Google Map for Cuisine TMA and Train TMA and Hawker TMA and other TMA differences and
> similarities"*). That surfaced that the **cuisine** TMA's nearest-MRT overlay was missing the 3
> CCL6 stations. This closes it. (Second consolidation follow-up after PR-1 #1537.)

## New

### [HDR] #887 | 19:16 SGT 13-07-26 | v0.62.531 — cuisine MRT overlay regen (CCL6) | 3 files | PR #(pending) |

- **[INTENT].** Close the cuisine-side gap found in the parity investigation: the 3 CCL6 stations
  (Keppel CC30 / Cantonment CC31 / Prince Edward Road CC32) were absent from the cuisine TMA's
  nearest-MRT overlay.

- **[ROOT CAUSE].** `web/cuisine/src/v2/lib/mrt-stations.generated.js` — consumed by
  `web/cuisine/src/v2/lib/nearby-zones.js` `nearestStation()` to label result venues with their
  nearest-MRT "zone" — is **GENERATED** from `data/mrt-coords.json` by `scripts/gen-cuisine-stations.mjs`,
  which filters `status === 'operational'` (`:15`). The 3 CCL6 stations were `future` until #1535's
  launch flip (v0.62.528) made them `operational`, so the last regen predated their eligibility.

- **[DELTA].**
  - `web/cuisine/src/v2/lib/mrt-stations.generated.js` — **regenerated** via
    `node scripts/gen-cuisine-stations.mjs` → now lists **183** stations (was 180), including the 3
    CCL6. **No hand-edit** (the file header forbids it: *"GENERATED … do not hand-edit"*).
  - `package.json` — 0.62.530 → **0.62.531** (re-hashes the cuisine bundle → cache-bust + Railway
    redeploy).
  - `doc/Journal/journal-0_62_531-13_07_26-1916.md` — this entry.
  - `doc/.serial-state.yml` — commit 1004→**1005**, build 630→**631**, journal 886→**887**; re-anchor
    13-07 '26 19:16 SGT (prior 886 anchor demoted verbatim, AU-7).

- **[WHERE THIS SITS IN THE CROSS-TMA PICTURE].** The real interactive station **card**
  (exits / bus / taxi / carpark detail) lives only in the **Transport** and **Hawker** TMAs — both fetch
  the same `/api/transport/{stations,line-paths,station-context,bus-arrival}` endpoints via a
  near-identical `mapOverlays.js` (the audit's #1 duplication). **Cuisine has no card** — it only needs
  the station name + coords for proximity labels, which this regen now supplies. So this PR fixes the
  cuisine label gap; it does **not** touch the card (that's Parity B).

- **[VERIFICATION (sandbox)].**
  - `node --check web/cuisine/src/v2/lib/mrt-stations.generated.js` OK; the 3 CCL6 present (grep).
  - **Full cuisine TMA `vite build` green** (`App-*.js` emitted; the 500 kB chunk warning is
    pre-existing/benign). Built `public/cuisine/*` is git-ignored → the diff is the single generated
    `.js` file only.
  - **Full `npm test` → 3699/3699 across 144 files.**
  - `web/` changed → ran the cuisine build per `gia-preflight` §0.

- **[STATUS]** PROD — the version bump re-hashes the cuisine bundle (cache-bust) and redeploys
  (Railway). On next load, cuisine result venues near the 3 CCL6 stations get the correct nearest-MRT
  zone label.

- **[TEST]** TMA data-file regen; `web/` change → cuisine build + full `vitest`. No new test (the
  generated list is mechanically derived from `mrt-coords.json`).

- **[KNOWN GAPS].**
  1. 🚉 **Parity B — CCL6 `exits[]` / `first_last_train[]`** — the real cross-TMA card gap (affects
     **both** Transport + Hawker via the shared `station-context`); blocked on LTA publishing the exit
     GeoJSON + train timings. Bus stops / taxis / carparks are already at full parity (computed live by
     proximity).
  2. 🧱 **Consolidation queue** — PR-2…PR-6 + the offline track remain; §4 behaviour decisions
     (D-1…D-4) precede their extractions.
  3. 🗒️ **#1525** closeable as superseded by #1535 (operator decision).

## Confirmation Gates
| Gate | Status | Note |
|------|--------|------|
| G1 | ✅ | PATCH bump 0.62.530→0.62.531; generated-overlay regen, standard per-PR rule |
| G2 | ➖ | no destructive action; regenerated one generated file (no hand-edit) |
| G3 | ➖ | no new decision rule |
| G4 | ➖ | no external paid API call |
