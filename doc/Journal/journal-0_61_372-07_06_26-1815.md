(№ 572 - 07-06 '26 18:15 SGT) v0.61.372

# Journal — soleat v0.61.372 (Follow-sync race gate (tested) + OTHER-picker ✏️ overflow)

> Operator [Image 9 + Railway]: *"the UI (✏️) is off the screen. same issue, I am in Other mode, and still search in singapore … do you need a SG/JB/Other field … are we over-compress in codes. do a code verification, be careful not to change unless there is a test."*
>
> **Left OPEN for review — auto-merge to be armed on the operator's word.** Commit 585 / journal 572 / build 370. Cuisine TMA.

## New

### [HDR] #572 | 18:15 SGT 07-06-26 | v0.61.372 — follow-sync race gate (pure + tested) + ✏️ min-w-0 | 3 files | 1 PR | 1 build |

- **[INTENT]** (A) Make a Menu pick (e.g. Wellington / NZ) actually drive the Cuisine search centre instead of the device GPS. (B) Stop the OTHER-picker's ✏️ being clipped off-screen.

- **[VERIFIED — A: centre]** Railway: `[menu/set-location] Wellington -41.2865,174.7762` + `country-pref → NZ`, then `[set-location] <redacted>` (SG GPS) and search `region=SG center=<redacted>` (1st) then `region=OTHER center=<redacted> cc=NZ → 2` (2nd). The v0.61.371 explicit-anchor guard was **raced**: the 20 s follow-sync's first GPS tick fired **before** `tryServerCache` read the Menu pick, so it overwrote Wellington (coords-only, **label lost**) → `runInitialLoad` (prefers `locationAnchor → searchCenter → userLoc`) had no anchor → searched SG.

- **[VERIFIED — B: ✏️]** The OTHER-picker expanded input was missing **`min-w-0`** (the SG/JB sibling input at line ~464 has it). A long city placeholder ("Wellington") then forced the input's intrinsic width and pushed the trailing ✏️ off-screen.

- **[MECHANISM]**
  - **`web/cuisine/src/v2/lib/location-follow.js`** (NEW, pure) — `shouldFollowDevice({ initialResolveDone, explicitAnchorName, current, loc, thresholdM })` + `haversineMeters`. Follows only when: the mount resolution settled, there's **no** explicit named pick, and the device moved ≥ threshold.
  - **`__tests__/location-follow.test.js`** (NEW) — 10 cases (race gate, explicit-pick hold, blank-name, jitter, real move, first reading, invalid).
  - **`web/cuisine/src/v2/App.jsx`** — new `initialResolveDoneRef` set true at the end of the mount resolution IIFE; the sync `onLocation` now defers to `shouldFollowDevice`; inline `havM` removed.
  - **`web/cuisine/src/v2/components/LocationField.jsx`** — `min-w-0` on the OTHER input.

- **[DELTA]**
  - **`web/cuisine/src/v2/lib/location-follow.js`** (NEW) · **`__tests__/location-follow.test.js`** (NEW) · **`web/cuisine/src/v2/App.jsx`** · **`web/cuisine/src/v2/components/LocationField.jsx`**.
  - **`package.json`** — `0.61.371 → 0.61.372`. **`doc/.serial-state.yml`** — commit 584 → 585, journal 571 → 572, build 369 → 370; re-anchored 18:15 SGT.

- **[VERIFICATION (sandbox)]** `npm run build` (cuisine) clean; full `npx vitest run` → **106 files / 3017 tests green** (+10). Trace re-walked: with the gate, the sync can't write SG before `tryServerCache` installs Wellington → anchor=Wellington → `runInitialLoad` searches Wellington.

- **[STATUS]** PR-**OPEN**, **auto-merge to arm on the operator's word**. Cuisine TMA. Field test: Menu → pick an overseas city while in SG → Cuisine map + first 5 should be that city; the ✏️ is visible.

- **[TEST]** Operator: a Menu pick now carries the search centre (not just the country filter); the pencil no longer runs off the edge.

- **[OPERATOR Qs ANSWERED]**
  - *"do you need a SG/JB/Other field?"* — the search **already** sends a region field (SG/JB/OTHER); the bug was the **centre** not following it, not a missing field. Region + centre are now coherent.
  - *"are we over-compress in codes?"* — honestly **yes**: the location layer has many overlapping mechanisms (tryServerCache / initData / GPS / sync / 3 coherence checks / gate) that fight each other; this race was a symptom. Extracting `shouldFollowDevice` to a tested module is one decompression step; a single-source-of-truth location refactor is the real long-term fix (flagged, not done).

- **[KNOWN GAPS]**
  - **Not live-E2E tested** — no Telegram WebView here.
  - The broader location-layer simplification remains a future refactor.
  - Still parked: scrollbar (#3), station/address (#4).

- **Confirmation Gates**

  | Gate | Resolution | Date |
  |---|---|---|
  | G1 — version bump | PATCH `0.61.371 → 0.61.372`. | 07-06 '26 |
  | G2 — destructive action | New pure module + a ref gate + a CSS class; device-follow behaviour preserved when no pick; **unit-tested** (+10); full suite green. | 07-06 '26 |
  | G3 — new decision rule | The follow-device sync is gated behind the mount resolution and an explicit-pick hold (tested `shouldFollowDevice`); OTHER input gets `min-w-0`. | 07-06 '26 |
  | G4 — paid external API call | None. | 07-06 '26 |

## Amendments

- **`web/cuisine/src/v2/lib/location-follow.js`** (NEW) — tested follow decision.
- **`__tests__/location-follow.test.js`** (NEW) — 10 cases.
- **`web/cuisine/src/v2/App.jsx`** — `initialResolveDoneRef` gate + `shouldFollowDevice` wiring; removed `havM`.
- **`web/cuisine/src/v2/components/LocationField.jsx`** — `min-w-0` on the OTHER input.
- **`package.json`** — `0.61.371 → 0.61.372`.
- **`doc/.serial-state.yml`** — counters (commit 585 / journal 572 / build 370) + anchor 18:15 SGT 07-06-26.

## Deleted

- Inline `havM` in the sync effect (moved to the tested `location-follow.js`).
