(№ 885 - 13-07 '26 05:23 SGT) v0.62.529

# Journal — soleat v0.62.529 (VibeCodingRecord catch-up regen: PRs #1528, #1532–#1535)

> Operator: **"continue"** after PR #1535 (v0.62.528, the CCL6 flip + consolidation scope) merged to
> `main`; on a scope-depth question the operator chose **"Both, in sequence"**. This is the **first**
> of the two follow-ups — the VibeCodingRecord ledger catch-up. (The second, PR-1 `index.js`
> require-hoist, follows once this merges.)

## New

### [HDR] #885 | 05:23 SGT 13-07-26 | v0.62.529 — VibeCodingRecord catch-up regen | 7 files | PR #(pending) |

- **[INTENT].** Bring the generated VibeCodingRecord ledger + hosted Vibe Journal back in sync with
  `main`. The ledger was stale at **#1531 / GEN_DATE 2026-07-10**; five merged PRs had never been
  balloted into it.

- **[THE FIVE MISSING PRs].**
  - **#1528** (v0.62.521) — VCR catch-up regen (PRs #1512–#1527). *A regen PR can't include its own
    row, so it was left for the next pass — this one.*
  - **#1532** (v0.62.525) — VCR catch-up regen (PRs #1529–#1531). *Same reason.*
  - **#1533** (v0.62.526) — Register: ms/vi deferred-locale entries.
  - **#1534** (v0.62.527) — code-reuse audit report.
  - **#1535** (v0.62.528) — CCL6 launch flip + consolidation scope.
  - **#1525 deliberately still EXCLUDED** — it is the *original* held CCL6-flip draft PR, **still open**
    and now **superseded** by the merged #1535. Prior regens excluded it "HELD open by design"; that
    holds, and it is now closeable (surfaced to the operator, **not** auto-closed).

- **[DELTA].**
  - `doc/VibeCodingRecord/data/prs.ndjson` — **+5 rows** (`{n,title,state,merged,body}`). Bodies fetched
    via GitHub MCP `pull_request_read` and `html.unescape`d to match the existing decoded rows;
    **1501 → 1506** records.
  - `doc/VibeCodingRecord/data/pr-files.tsv` — **+5** squash-commit file lists (from
    `git log origin/main`).
  - `doc/VibeCodingRecord/generate.mjs` — `GEN_DATE` 2026-07-10 → **2026-07-13**.
  - `doc/VibeCodingRecord/records.tsv`, `doc/VibeCodingRecord/vibe-coding-record.md`,
    `public/doc/vibe-journal.html`, `public/doc/vibe-journal.json` — **regenerated** (1506 rows).
  - `package.json` — 0.62.528 → **0.62.529**.
  - `doc/Journal/journal-0_62_529-13_07_26-0523.md` — this entry.
  - `doc/.serial-state.yml` — commit 1002→**1003**, build 628→**629**, journal 884→**885**; re-anchor
    13-07 '26 05:23 SGT (prior 884 anchor demoted verbatim, AU-7).

- **[COUNT NOTE].** 1506 = prior 1501 + 5. The two pre-existing blank lines at `prs.ndjson` 991/1011
  (predating this session) are still tolerated/skipped by the generator, per the #1532 precedent — so
  the arithmetic is clean (no off-by-two).

- **[TIME ANCHOR].** SG gov sovereign source (`api.data.gov.sg`) again **blocked by the sandbox
  proxy**; fell back to the container NTP clock per **TF-9** → **13-07 '26 05:23 SGT**. (Consistent with
  the #1535 merge stamp 2026-07-12T18:35Z = 13-07 02:35 SGT.)

- **[VERIFICATION (sandbox)].**
  - All **1506** `prs.ndjson` records `JSON.parse` OK (+2 tolerated blank lines).
  - `records.tsv` last row **#1535 / 0.62.528 / merged 2026-07-12 18:35:02**.
  - `vibe-journal.json`: `count` **1506**, `generated` **2026-07-13**, last `n` **1535**.
  - `vibe-coding-record.md` footer: *"1506 pull requests (#1–#1506)"*.
  - **#1535** present in `public/doc/vibe-journal.html`.
  - Category tally: `feature=1015 fix=204 docs=96 copy=62 prompt-tune=45 refactor=43 test=21 infra=20`,
    rework-flagged=129 (the deterministic classifier is the source of truth — **not** hand-tuned).

- **[BUILT FROM].** Branch `claude/handover-july-11-49uzvf` **restarted from `origin/main` @ `9737b4f`**
  after #1535 merged — per the merged-branch rule, never stacking on already-merged history.

- **[STATUS]** PROD — generated-artifact change; the refreshed page is live at `/doc/vibe-journal.html`
  on the next deploy (the version bump triggers the Railway redeploy; `public/doc` is served static).

- **[TEST]** Generated-artifact change (**not** under the AU append-only Recipe per `doc/CLAUDE.md`) →
  outside the `gia-preflight` trigger set; **no web build** (`public/doc` served static). Full
  `npm test` in CI.

- **[KNOWN GAPS].**
  1. 🧱 **PR-1 `index.js` require-hoist** — the second of the "both in sequence" follow-ups; starts once
     this VCR PR merges (single designated branch → sequential).
  2. 🗺️ **#1525** — closeable as superseded by #1535 (operator decision); and its `exits[]` /
     `first_last_train[]` for the 3 CCL6 stations still await LTA publication.
  3. 🗄️ **Local iCloud git tree** — still ~70 PRs behind `origin/main`; this PR built from fresh
     `origin/main` state.

## Confirmation Gates
| Gate | Status | Note |
|------|--------|------|
| G1 | ✅ | PATCH bump 0.62.528→0.62.529; generated-artifact refresh, standard per-PR rule |
| G2 | ➖ | no destructive action; append + wholesale regen of derived artifacts only |
| G3 | ➖ | no new decision rule |
| G4 | ➖ | no external paid API call (GitHub MCP + git only; free SG gov time source blocked, fell back to TF-9) |
