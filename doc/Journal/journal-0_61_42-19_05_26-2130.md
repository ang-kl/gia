(№ 253 - 19-05 '26 21:30 SGT) v0.61.42

# Journal — soleat v0.61.41 → v0.61.42 (Bus Stop overlay layer enabled)

> Operator: *"Previous PRs have creation and changes to Bus Stop code. Check"* — and,
> on confirming the bus-stop data + popup + arrivals code already existed, *"Yes"*
> (wire up the Bus Stop overlay layer).
>
> Sequenced after PR #527 — this entry is commit 253 / journal 246 / build 55.

## New

### [HDR] #246 | 21:30:00 SGT 19-05-26 | v0.61.42 — Bus Stop overlay layer | ~3 kb | 9 files | 1 PR | 1 build |

- **[INTENT]** Enable the long-disabled `🚌 Bus Stop` map toggle by wiring the existing
  bus-stop catalogue + arrivals popup into a proper overlay layer.

- **[DELTA]**
  - **`transport.js`** — new `allStops(redis)`: one Singapore-wide `GEOSEARCH` over the
    cached LTA bus-stop geo set (~5500 stops) + a batched hash read for each stop's
    description / road name. Exported.
  - **`index.js`** — new `GET /api/geo/bus-stops` endpoint: serves the full catalogue
    from `transport.allStops`, Redis-cached 24 h (the catalogue rarely changes); empty
    list when Redis is down. Mirrors `/api/geo/carpark`.
  - **`mapOverlays.js`** (byte-identical ×3) — new `fetchBusStops()` (fetch-once) and
    `buildBusMarkers()` (the shared `🚏№<code>` amenity label; a tap opens the existing
    `openBusInfo` live-arrivals popup); `ensureLayer` gains a `busstop` branch that
    builds the markers and radius-clips them to the map anchor like the other point
    layers.
  - **`MrtMapPanel.jsx` / `HawkerMapPanel.jsx` / `MapPanel.jsx`** — the `busstop` row
    toggle loses `disabled: true`, and `applyOverlayLayers` now pushes
    `ctrl.setLayer('busstop', …)`.
  - **`package.json`** — `0.61.41` → `0.61.42` (PATCH).

- **[VERIFICATION (sandbox)]**

  ```bash
  $ node --check transport.js / index.js / mapOverlays.js  → OK
  $ npm run build:cuisine / build:hawker / build:transport  → all ✓ built
  $ npm test  → 65 files / 1928 tests green
  ```
  `mapOverlays.js` confirmed byte-identical ×3.

- **[STATUS]** DRAFT → PR (base `main`, branch `claude/review-gia4lunch-readme-3s798`).
  Deploys via the Railway auto-deploy on `main` once merged.

- **[TEST]** The `🚌 Bus Stop` quick toggle is now active (no longer greyed). Toggle it
  on — bus-stop pins appear within the map-anchor radius. Tap one: the popup shows the
  stop name / road / code / distance and live arrivals (≤5 / ≤10 / ≤20 / 20+ min).

- **[KNOWN GAPS]** All ~5500 bus-stop markers are instantiated on the first toggle-on
  (then radius-clipped) — consistent with the carpark layer's fetch-once pattern; if
  marker-creation jank shows on low-end devices a per-anchor query endpoint would be
  the next step. `/api/geo/bus-stops` needs Redis up + the boot-time bus-stop cache
  populated (needs `LTA_ACCOUNT_KEY`); it degrades to an empty layer otherwise. The
  24 hours layer remains disabled. The formal `doc/Legal/` revision (journal #239)
  remains.

- **Confirmation Gates**

  | Gate | Resolution | Date |
  |------|-----------|------|
  | G1 — version bump | PATCH `0.61.41` → `0.61.42`, established 0.61.x cadence | 19-05 '26 |
  | G2 — destructive action | _n/a_ | — |
  | G3 — new decision rule | _n/a_ | — |
  | G4 — paid external API call | _n/a_ (LTA DataMall feed already in use) | — |

## Amendments

_None._

## Deleted

_None._
