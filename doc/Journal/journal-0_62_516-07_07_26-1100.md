(№ 872 - 07-07 '26 11:00 SGT) v0.62.516

# Journal — soleat v0.62.516 (Sketchbook: QuickFilters verbatim port)

> Operator: *"you dont understand the word 'follow' ... follows meant layout exactly the same as
> Cuisine, the logic is based on the saved cards in Sketchbook."* v0.62.515 only reordered the
> hand-rolled facets; it did NOT make them *the Cuisine TMA's chips*. This PR replaces the
> Sketchbook's home-grown RATING/PRICE/CROWD facets with the **verbatim** `QuickFilters.jsx`
> component from the Cuisine TMA, wired to filter the **saved cards** client-side (Sketchbook has
> no server search). The 3 chips with no per-saved-card data source (New / Home-based / Recommend)
> are **shown but greyed** — the layout matches Cuisine exactly, without fabricating data.

## New

### [HDR] #872 | 11:00 SGT 07-07-26 | v0.62.516 — Sketchbook QuickFilters verbatim port | 6 files | PR #(pending) |

- **[INTENT].** Operator: *"follows meant layout exactly the same as Cuisine, the logic is based
  on the saved cards in Sketchbook."* Workstream **C** (reopened). The v0.62.515 reorder was a
  half-measure — it kept the *hand-rolled* facets. "Mirror TMA verbatim" means: port the exact
  Cuisine source component, don't hand-roll a substitute. Operator reply *"1"* = **grey** the 3
  dataless chips (show all 9, disable the 3 with no per-saved-card boolean), not drop them.

- **[DELTA].**
  - `web/clipboard/src/components/QuickFilters.jsx` — **NEW.** Verbatim copy of
    `web/cuisine/src/v2/components/QuickFilters.jsx` with exactly **3** adaptations:
    (1) `const lang = useLocale();` — clipboard `useLocale()` returns a **string**, not the
    cuisine `[lang, setLang]` tuple (i18n.js:288). (2) added a `disabledKeys = []` prop.
    (3) greying — `toggle()` early-returns for a disabled key; the PRIMARY halal chip and each
    OVERFLOW chip wrap in `opacity-40 pointer-events-none` when disabled. All accessibility kept
    (Arabic حلال glyph, `RadioDot` shape-not-colour, Save-button state).
  - `web/clipboard/src/components/Shell.jsx` — swapped the hand-rolled facets `<div>`
    (RATING/PRICE/CROWD/openNow+michelin + `facet.clear`) for
    `<QuickFilters … disabledKeys={['newlyOpened','recommend','homeBased']} />`; kept the
    `CuisineGroupPicker` grid **below** (Cuisine order: filters → cuisine grid); moved the
    clear-all button under the grid. Michelin stays a **cuisine-grid category** (matching the
    Cuisine TMA), not a filter chip — the old redundant michelin facet button is dropped.
  - `web/clipboard/src/App.jsx` — replaced `facets` state with the Cuisine `filters` object
    (`halal/vegetarian/petFriendly/openNow/prices[]` …) + a separate `ratingPref` string
    (default **`'any'`** — a deliberate divergence from Cuisine's `'3.7'` floor, which would hide
    deliberately-saved cards). `cardMatches` now filters saved cards on those flags:
    `v.halal`/`v.vegetarian`/`v.allowsDogs`/`v.openNow`, price number→`'$'.repeat(n)`, and the
    rating-pref (`unrated` / min-number / `any`).
  - `web/clipboard/src/lib/i18n.js` — added ~25 `filter.*` / `rating.*` keys (Cuisine values;
    en/fr/id + a few more, English fallback via `t()`).
  - `web/cuisine/src/v2/components/ResultCard.jsx` — forward `halal` + `vegetarian` in the
    `copyOneToChat` payload so **newly-saved** cards carry the flags the halal / vegetarian chips
    filter on. Forward-only: older clips lack them → those two chips simply won't match them
    (honest; no fabricated data).
  - `package.json` — 0.62.515 → **0.62.516** (re-hashes the bundle → `index-Ch9O27Uu.js`, doubles
    as a cache-bust).
  - `web/clipboard/index.html` — diagnostic-boot marker **515 → 516**.
  - `doc/Journal/journal-0_62_516-07_07_26-1100.md` — this entry.
  - `doc/.serial-state.yml` — commit 989→**990**, journal 871→**872**, build 615→**616**;
    re-anchor 11:00 SGT (prior 1022 anchor demoted).

- **[VERIFICATION (sandbox)]** `esbuild` parse-check → OK on App.jsx, Shell.jsx, QuickFilters.jsx,
  ResultCard.jsx; `node --check` OK on i18n.js. `npm run build` in `web/clipboard/` → **BUILD_OK**
  (vite, **54** modules — +1 for the new QuickFilters module). New bundle
  `public/clipboard/assets/index-Ch9O27Uu.js`. `public/` is git-ignored → no built assets in diff.

- **[STATUS]** PROD — merges to main; the per-PR bot redeploys the Sketchbook (clipboard) TMA and
  the Cuisine TMA (ResultCard change). The version bump re-hashes the bundle, so Telegram refetches.

- **[TEST]** No unit test for TMA presentation; the vite build is the parse gate. Full `npm test`
  runs in CI (TMA-only change — `web/clipboard`, one `web/cuisine` component, `package.json`, docs
  — outside the gia-preflight trigger set: no `index.js` / bot-handler / search / matcher surface).

- **[KNOWN GAPS].** (1) **3 greyed chips** — New / Home-based / Recommend have no per-saved-card
  boolean; they render disabled. If the operator wants them live, each needs a stored flag on the
  clip payload (a future enrich). (2) **halal / vegetarian** only match cards saved *after* this
  ships (forward-only enrich). (3) **drill-in modal** — the Sketchbook `CategoryModal` still lacks
  the Cuisine TMA's per-chip **Dishes ›** reveal + taxonomy badges + `imgFlag` photos
  (v0.62.463/469). **E** (free-text search FAB, port `TellMePanel`) still pending. MRT stations
  (Keppel / Cantonment / Prince Edward Road) still due for the 12 Jul 2026 launch.

## Confirmation Gates
| Gate | Status | Note |
|------|--------|------|
| G1 | ✅ | clipboard vite build green (54 modules, +1 QuickFilters); esbuild/node --check on all 5 changed files OK; version 0.62.515→0.62.516; new bundle index-Ch9O27Uu.js |
| G2 | ✅ | "follow" = port the exact Cuisine component verbatim (not hand-roll); QuickFilters.jsx copied with 3 documented adaptations; filters the saved cards client-side |
| G3 | ✅ | serial commit 989→990, journal 871→872, build 615→616; anchor 07-07 '26 11:00 SGT (prior 1022 demoted) |
| G4 | ➖ | operator reply "1" = grey (not drop) the 3 dataless chips; layout now matches Cuisine exactly — operator eyeball on the live TMA is the final gate |
