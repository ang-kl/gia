(journal № 1,062 - 22-08 '26 06:20 SGT)

# Journal — soleat / Gia4lunch v0.62.722

## New

### [HDR] v0.62.722 — every Gemini model name in the repo was retired by Google

- **[INTENT]** Not a feature request. The operator pasted Railway logs from the
  v0.62.721 deployment and asked, in effect, why nothing works. The logs answered
  it:

  ```
  [Extract-Dishes] gemini-2.5-flash-lite failed: [404 Not Found] This model
  models/gemini-2.5-flash-lite is no longer available to new users. Please
  update your code to use models/gemini-3.5-flash-lite

  [Extract-Dishes] gemini-flash-latest failed: [429 Too Many Requests] Your
  prepayment credits are depleted.

  [Extract-Dishes] gemini-2.5-flash failed: [404 Not Found] This model
  models/gemini-2.5-flash is no longer available to new users. Please update
  your code to use models/gemini-3.6-flash
  ```

  Three models, three failures, one request. Every Gemini path in the bot was
  dead: `/hidden`, search-intent classification, cooking-method descriptions,
  dish extraction, authenticity validation, review and name translation, durian
  verification, the i18n audit, and four `scripts/*.mjs` runners.

- **[THE CORRECTION THAT MATTERS MORE THAN THE CODE]** On 21-08 the operator
  wrote: *"change the model to gemini-3.5-flash-lite"*. This session overrode
  them, and wrote the reasoning into two files as if it were a principle. From
  `i18n-audit.js`, verbatim, now deleted:

  > gemini-2.5-flash-lite is the only candidate with evidence in this repo rather
  > than from memory: present in api-cost.js PRICES, present in gemini-client.js
  > FALLBACK_CHAIN, and recorded there as GA and verified at v0.58.44.

  Google's own 404 body names `gemini-3.5-flash-lite` as the replacement for the
  exact model that comment defended. The operator was right and was overruled on
  a rule that sounded rigorous.

  The rule's flaw is worth stating precisely, because the surface form of it —
  *prefer measured repo state over recalled facts* — is correct and was earned
  the hard way this session (X-3, X-4, X-5, all three of them the same mistake in
  the other direction). What it missed: **a repo records what was true when it
  was written.** `FALLBACK_CHAIN`'s "verified at v0.58.44" was a true statement
  about v0.58.44. Treating it as a claim about today is the *same* error as
  reading a Redis receipt as present-tense config — X-5, three days earlier, by
  this session. Fourth instance. Recorded as **D-199** and **X-7**.

  A numbering note, since it is the same class of problem: `D-124` would have
  been the next number in the sequence this arc has been using — and it is
  already taken, by `register-0_60_153` (14-05 '26, Michelin `criteriaHash`
  omits `radius`). So are **D-119 through D-123**, every decision number this
  session and the two before it have issued. The recent arc restarted the
  counter without reading the older registers first. New IDs here start above
  the global maximum (`D-198`) instead. The existing five collisions are logged
  as an open item rather than renumbered in place — renumbering would rewrite
  merged records, which the AU Recipe forbids.

  A second-order note: this session used that reasoning to overrule the one
  participant with live access to the deployed system. The operator was not
  quoting a fact from memory; they were reporting what their environment needed.

- **[DELTA]**
  - **`gemini-models.js` (new)** — the single place Gemini model names live.
    `FLASH = 'gemini-3.6-flash'` and `LITE = 'gemini-3.5-flash-lite'` are
    **Google's own named replacements**, quoted from the 404 bodies above, not a
    guess at what the current generation is called. `LATEST =
    'gemini-flash-latest'` stays first in `MODEL_CHAIN` because the same logs
    show it returning 429 (billing) rather than 404 — it is the only name in the
    file with live proof that it still resolves. No pro-tier name is invented:
    `gemini-2.5-pro` has no published replacement in any log this repo has seen,
    and nothing in the chain needs one.
  - **`defaultModel()`** honours `GEMINI_MODEL` even when the value is a retired
    name, and warns with the replacement. Honouring beats rewriting: the operator
    sets that var in Railway, and a module that silently disagrees with the
    environment makes the *next* outage harder to read, not easier.
  - **Eleven call sites rewired** to the shared table: `gemini-client.js`
    (`DEFAULT_MODEL`, `FALLBACK_CHAIN`, `SEARCH_INTENT_MODEL_CHAIN`), `index.js`
    (two inline literals + the `/model` help text), `translate-name.js`,
    `translate-review.js`, `name-gloss.js`, `durian-gemini-verifier.js`,
    `i18n-audit.js`, `ztest-vision.js`, and `scripts/{translate-content,
    curate-dish-sources,draft-dish-notes,draft-dish-taxonomy,
    build-i18n-audit-jobs}.mjs`.
  - **`api-cost.js`** — real 3.x rates, read off
    `https://ai.google.dev/gemini-api/docs/pricing` on 22-08 '26, replacing the
    v0.62.719 flagged estimate. The 2.5 rows are **retained deliberately**: Redis
    holds per-day receipts stamped with those names and `/cost` must price
    history correctly rather than re-label it as an estimate.
  - **`__tests__/gemini-models.test.js` (new, 7 tests)** — constants, chain
    order, chain-is-all-flash, the honour-and-warn path, and a grep of live
    source for retired names.

- **[WHAT v0.62.719 GOT WRONG ABOUT ITS OWN EVIDENCE]** That entry recorded, under
  `[NO INVENTED PRICE]`, that a `gemini-3.x` rate was *"deliberately not added"*
  because the sandbox could not verify Google's pricing. The sandbox could: the
  pricing page is public and was fetched in one call. "Cannot verify" was an
  assumption, not an attempt — and it is the same shape as the model-name error
  in the same arc. Real rates are now in `PRICES`.

  Worse, that entry also explained away the one piece of live evidence it had.
  The 2026-08-19 `/cost` receipt named `gemini-3.1-flash-lite`, and both
  v0.62.719 and this session treated it as a stale artifact to be reasoned around
  — *"a name that appears nowhere in this repo"*. It is real, current, and at
  **$0.25 in / $1.50 out per 1M** the cheapest option on Google's list — cheaper
  than the `gemini-3.5-flash-lite` now defaulted to ($0.30 / $2.50). It was never
  an anomaly; it was the only direct evidence in the system of a model that
  actually worked in this project. It is now in `PRICES` and surfaced to the
  operator as the cheaper choice rather than substituted for theirs.

- **[THE TEST EARNED ITS KEEP ON FIRST RUN]** The grep-live-source invariant
  failed immediately on an **eleventh** copy of the dead chain, in
  `name-gloss.js:19`, which three separate manual `grep -rn` sweeps had reported
  clean. Cause: the file holds raw CJK gloss data, git and grep classify it as
  binary, and both silently skip it — no error, no warning, just absence from the
  results. A unit test on the constants alone would never have caught this; the
  constants were fine in their own file, the copies elsewhere were the defect.
  The test runs `git grep -a` for that reason, and says so in a comment.

- **[VERIFICATION (sandbox)]**
  - `node --check` clean on all 16 changed `.js` / `.mjs`.
  - `npx vitest run` — **172 files / 4,199 tests, 0 failures** (was 171 / 4,192).
  - Module load smoke-tested: `gemini-client.DEFAULT_MODEL` → `gemini-3.6-flash`,
    `i18n-audit.DEFAULT_MODEL` → `gemini-3.5-flash-lite`, retired-name warning
    fires and names the replacement.
  - `package.json` 0.62.721 → 0.62.722. Server-only; no `web/` change.
  - Diff scanned for API-key prefixes before push: 0 hits.

- **[STATUS]** **DRAFT** on PR #1713, head `289bbd0`. Deploys to Railway on merge
  to `main` like every other server change.

- **[TEST]** After merge, trigger any Gemini path (`/hidden`, or a free-text
  search that needs intent classification). Two outcomes are informative:
  a 404 naming a model would mean the table is wrong again; a **429** would mean
  the names are right and billing is the only remaining blocker. Then `/cost` —
  Gemini rows should show plain figures with **no `⚠️ est.` marker**, because
  every model the code can now select is in `PRICES`.

- **[KNOWN GAPS]**
  - **Gemini billing is depleted and this change does not touch it.** `429 …
    prepayment credits are depleted` at ai.studio/projects. Correct model names
    do not make a call succeed. Every Gemini feature stays down, and the i18n
    audit stays at **0 / 1,590**, until the operator resolves billing.
  - **`GEMINI_MODEL` in Railway still reads `gemini-2.5-flash-lite`** — a retired
    name. The new boot warning will say so, but the var still needs changing to
    `gemini-3.5-flash-lite` (or `gemini-3.1-flash-lite`, which is cheaper).
  - **The 3.6/3.7-flash rates expire.** $0.75/$3.75 holds "through December 31,
    2026", then doubles. `PRICES` has no notion of a dated rate, so from 1 Jan
    2027 those rows under-count by half — the dangerous direction for a spend
    brake. Flagged in-file and in `doc/Register/`.
  - **The 120 damaged translations are still unrepaired** — they depend on the
    audit, which depends on billing.
  - **`i18n.js` is still 0 / 276 keys translated.** Nothing user-visible has
    changed for any of the six languages.
  - **Scaffolding routes `/api/i18n-translate` and `/api/i18n-audit` are still
    live** (O-197) and still deletable.

## Confirmation Gates
| Gate | Status | Note |
|------|--------|------|
| G1 | ✅ | PATCH bump 0.62.721 → 0.62.722; server-only; 172 files / 4,199 tests green. |
| G2 | ➖ | no deletion. Retired 2.5 rates were *retained* in `PRICES` precisely to avoid one — historical Redis receipts still need pricing. |
| G3 | ✅ | **D-199**: repo state is evidence about the past, not the present. A comment saying "verified at v0.58.44" is a true statement about v0.58.44 and says nothing about today. When an external API is the authority, its live response outranks any in-repo record of it — and outranks a rule that merely *sounds* like measurement. |
| G4 | ➖ | no new external paid API. One unauthenticated read of Google's public pricing page. |

### [HDR-2] The embedded map: not a code bug, and not the key being invalid

- **[INTENT]** Operator, twice: *"the embedded map cannot be loaded. why"* /
  *"what happen, the map isn't loaded"*, with a screenshot reading *"Sorry!
  Something went wrong. This page didn't load Google Maps correctly."*

- **[FALSE STARTS, RECORDED]** Two hypotheses were stated before being measured,
  and both were wrong. (1) *Maps JavaScript API not enabled* — the bootstrap
  loader returns 200 and a valid script for this key, so the API resolves. (2)
  *HTTP-referrer restriction blocking `soleat.net`* — ruled out by the opposite
  evidence: a **referrer-restricted key is rejected outright for server-side web
  service calls**, and Geocoding succeeds from this sandbox with no `Referer`
  header at all. A browser reproduction via the pre-installed Chromium was
  attempted and abandoned — it cannot reach the sandbox egress proxy
  (`ERR_CONNECTION_RESET` on every host, `curl` unaffected).

- **[THE MEASUREMENT]** Three probes against the live key from `/maps-key`:

  | API | Result |
  |---|---|
  | Geocoding | ✅ valid Singapore results |
  | Places API (New) `searchText` | ✅ returns Lau Pa Sat |
  | Maps **Static** API | ❌ **403** — *"This API key is not authorized to use this service or API. Please check the API restrictions settings of your API key in the Google Cloud Console"* |

  That 403 is the **API-restrictions** message, not the *API-not-enabled*
  message — a distinction that names the cause exactly. The rotated
  `GOOGLE_MAPS_API_KEY` carries an allow-list containing the server-side APIs the
  bot calls (Geocoding, Places, Routes — the latter corroborated by the 30 billed
  requests in the operator's `/cost` reading) and **excluding** the browser-side
  Maps JavaScript API. The overlay the operator sees is `ApiTargetBlockedMapError`.

- **[DELTA]** **None.** No code changed for this. `web/_shared/lib/gmaps-loader.js`
  and `/maps-key` are both behaving correctly; the loader fetches a key that is
  valid but not permitted for the API it is being used with. The fix is entirely
  in GCP Console → Credentials → the key → API restrictions → add **Maps
  JavaScript API**. Recording a diagnosis with an empty delta on purpose: the
  next session should not go looking for a code cause.

- **[WHY IT LOOKED LIKE A REGRESSION]** It appeared immediately after the
  operator rotated `GOOGLE_MAPS_API_KEY`, alongside a genuine v0.62.721
  deployment. Restrictions on a replacement key are re-entered by hand, from
  memory, under time pressure — omitting one entry from a list of six is the
  normal outcome, not an unusual one.

- **[STATUS]** DIAGNOSED, awaiting an operator action in GCP Console. Nothing to
  merge.

- **[KNOWN GAPS]** Which APIs are on the allow-list has not been read directly —
  it is inferred from what answers and what 403s. The Static Maps 403 is
  evidence about Static Maps; Maps JavaScript API is inferred to share its fate
  because it is the only remaining explanation consistent with a valid, billed,
  unrestricted-by-referrer key that fails only in the browser. If adding Maps
  JavaScript API does not fix it, that inference is where to look first.

## Amendments

### [AMD-1] PR #1713 merged — content verified on `main` by inspection, not by notification

- **[AMENDS]** the `[STATUS]` line of `[HDR]` above, which read verbatim:

  > - **[STATUS]** **DRAFT** on PR #1713, head `289bbd0`. Deploys to Railway on merge
  >   to `main` like every other server change.

  Now **merged**: squash-merged as `2106c07` at `2026-08-22T07:03:00+08:00`
  (22-08 '26 07:03 SGT) by the operator, seconds after marking it ready for
  review — the same merge pattern as #1706, #1708, #1709 and #1712. All four
  required checks were green on the head (`1c14ded`): Vitest, TMA build, syntax
  check, empty-blob guardrail.

- **[VERIFIED ON `main`, PER D-123]** A merge notification proves a merge
  happened, not that the work is in it — the lesson of #1711, which merged
  without the Copy-all fix it was believed to carry. So, checked against
  `origin/main` directly rather than against the event:

  | Check | Result |
  | :--- | :--- |
  | `gemini-models.js` exists | ✅ |
  | `FLASH = 'gemini-3.6-flash'` / `LITE = 'gemini-3.5-flash-lite'` | ✅ both present |
  | `gemini-client.js` `DEFAULT_MODEL` / `FALLBACK_CHAIN` resolve through the shared table | ✅ |
  | `name-gloss.js:23` — the copy three manual sweeps missed | ✅ repinned |
  | `package.json` | ✅ 0.62.722 |
  | `journal-0_62_722` + `register-0_62_722` + serial state | ✅ all on `main` |
  | `__tests__/gemini-models.test.js` re-run against the merged tree | ✅ 7/7 |

  The last row is the one that matters: the invariant test was re-run **against
  the merged tree**, not against the local branch that produced it. A test that
  only ever passes on the branch it was written on has not been shown to hold on
  `main`.

- **[STATE AT MERGE]** Deployed to Railway. Every Gemini model name in the repo
  now resolves through one file. **Nothing about that makes a Gemini call
  succeed** — `O-200` (depleted prepayment credits) is untouched by this merge
  and still returns `429` for every model. The correct expectation for the next
  Railway log is a **429, not a 404**; a 404 would mean the table is wrong again.

- **[STILL OPEN, UNCHANGED BY THIS MERGE]** O-200 (billing), O-201
  (`GEMINI_MODEL` in Railway still reads a retired name), O-202 (Maps
  JavaScript API missing from the key's allow-list — the embedded map),
  O-196 (0 / 1,590 audited), O-197 (scaffolding routes), O-198 (decision-number
  collisions), O-199 (the 3.6/3.7-flash rate expires 31-12 '26).
