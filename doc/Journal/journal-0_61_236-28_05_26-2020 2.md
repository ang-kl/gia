(№ 437 - 28-05 '26 20:20 SGT) v0.61.236

# Journal — soleat v0.61.236 (Cuisine TMA OTHER picker: compact resting pill)

> Operator (28-05 '26 20:15 SGT, two screenshots):
> *"Where is the city code, and the function/feature of Location
> (edit/display UI). The {Country code} + {city Code} is taking
> too much UI estate. it should be more the left and give more
> typing character space to the location in Cuisine TMA and Menu
> TMA."*
>
> Image 1 — SG mode at "📍 Pattaya · 24 places nearby tap to change × 🔍" (compact, lots of room).
> Image 2 — OTHER mode at "🇹🇭 TH · — — Pattaya" (flag pill + city pill + text input + 🔍, all on one row → text squeezed).
>
> Commit 449 / journal 437 / build 239, on branch
> `claude/v0.61.236-other-compact-pill` off `main` at v0.61.235.

## New

### [HDR] #437 | 20:20 SGT 28-05-28 | v0.61.236 — OTHER picker compact resting pill | 3 files | 1 PR | 1 build |

- **[INTENT]** Match SG mode's "set-and-collapse" UX in OTHER mode. Once a city is picked, hide the full form and surface a single-line pill; tap to edit.

- **[DELTA]**

  **`web/cuisine/src/v2/components/LocationField.jsx`** — `OtherLocationPicker`:
  - New `expanded` state (default false).
  - Derived `showCompact = !!(anchor && anchor.name) && !expanded && results.length === 0`.
  - When `showCompact`: render a single-row pill:
    ```
    📍 🇹🇭 Pattaya · {suffix} tap to change ✏️
    ```
    The whole pill is a button — tap to `setExpanded(true)` and reveal the full form.
  - When `!showCompact`: render the existing full form (CountryDropdown + CityDropdown + text + 🔍 + results panel below).
  - `onCityPick` and `pickResult` now both `setExpanded(false)` so a successful pick auto-collapses back to the compact pill.
  - Anchor-hint line below the form (when expanded) gains a `[collapse]` link for quick return.

  **`package.json`** — `0.61.235 → 0.61.236`.

  **`doc/.serial-state.yml`** — commit 448→449, build 238→239, journal 436→437; re-anchored 28-05 '26 20:20 SGT.

- **[VERIFICATION (sandbox)]**

  TMA build deferred to CI (recurring iCloud / Node-24 PostCSS quirk on local). Logic change is local to one component; the showCompact branch + the wrapper of the existing form match React conditional patterns elsewhere in the file.

- **[STATUS]** PR-OPEN on push.

- **[TEST]** Operator (post-merge + Railway redeploy):
  1. From any OTHER anchor (e.g. `/location Pattaya` → Cuisine TMA): the location field should be a SINGLE compact row reading `📍 🇹🇭 Pattaya · N places nearby tap to change ✏️`.
  2. Tap the pill → it expands to the full flag + city + text + 🔍 form for editing.
  3. Pick a different city from the cascade → row auto-collapses back to the compact pill at the new city.
  4. SG anchor unchanged — already had its own compact pill.

- **[KNOWN GAPS]**
  - Menu TMA's `LocationFieldMenu.jsx` NOT updated this PR. Its layout includes the always-visible precinct quick-pick (v0.61.223 operator-locked) so the same compact-pill pattern doesn't apply directly. Operator-deferred — can add a parallel "OTHER row" collapse in a follow-up if needed.
  - The compact pill shows the country flag emoji (🇹🇭) instead of the CC code (TH) — slightly more readable. Operator can request the CC form if preferred.

- **Confirmation Gates**

  | Gate | Resolution | Date |
  |---|---|---|
  | G1 — version bump | PATCH `0.61.235 → 0.61.236`. Single-component UX collapse. | 28-05 '26 |
  | G2 — destructive action | _n/a_. | 28-05 '26 |
  | G3 — new decision rule | YES — OTHER mode now mirrors SG mode's "set-and-collapse" affordance. Future location-picker variants should follow this resting-pill pattern. | 28-05 '26 |
  | G4 — paid external API call | _n/a_. | 28-05 '26 |

## Amendments

- **`OtherLocationPicker`** — new `expanded` state + `showCompact` derived flag + the resting pill branch. `onCityPick` / `pickResult` reset `expanded` to false.

## Deleted

_None._
