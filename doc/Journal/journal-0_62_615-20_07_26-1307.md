# Journal — v0.62.615 (recode slice B — share `MapControls` + `giaToggleStyle`; the rest of the UI diverges on purpose)

_(№ 959 — 20-07 '26 13:07 SGT)_

## New

### [HDR] v0.62.615 — recode slice B: consolidate the one clean shared-UI candidate (MapControls) + its `giaToggleStyle` helper

- **[INTENT]** Operator: **"Continue to slice B"** (Register O-55 — small shared UI), branched fresh from
  `#1622`'s merged main (`2c31798`).

- **[FINDING — same pattern as slice A]** Most slice-B candidates are **intentionally divergent**, not
  shareable copies:
  - `LocaleToggle.jsx` (×4) — differs because the TMAs have **different i18n APIs**
    (cuisine `useLocale() → [lang, setLang]` vs hawker `useLocale() → lang` + separate `setActiveLocale`).
    Sharing needs an i18n-API unification → **deferred** (behaviour-touching).
  - `BackFab.jsx` (×4) — **genuinely different components** (cuisine's has `inline` / `closeOnly` / `mode`
    / `onBack` + SPA page-history; hawker's is a plain FAB). Not a dupe.
  - `tg.js` (×6) — per-TMA (fullscreen gates, theme, platform logic all differ). Not a dupe.

  The **one clean win**: `MapControls.jsx` — **cuisine == transport** (byte-identical) and **hawker is a
  clean backward-compatible superset** (adds `fill=false`; with the default it renders identically).

- **[DELTA]**
  1. Extracted **`giaToggleStyle`** — byte-identical in all three `mapOverlays.js` (same hash) — to a new
     **`web/_shared/lib/gia-toggle-style.js`**. Each `mapOverlays.js` now **re-exports** it
     (`export { giaToggleStyle } from …`), so existing importers (the 3 `MapControls`, cuisine `App.jsx`)
     keep their import paths unchanged while the function body lives once.
  2. `git mv` hawker's superset **`MapControls.jsx` → `web/_shared/components/`** (its `giaToggleStyle`
     import repointed to `../lib/gia-toggle-style.js`); `git rm` the cuisine + transport copies; repointed
     the **3 map-panel imports** (cuisine `MapPanel.jsx`, hawker `HawkerMapPanel.jsx`, transport
     `MrtMapPanel.jsx`) to `_shared`.

- **[VERIFICATION (sandbox)]** **cuisine + hawker + transport** `vite build` green; full `npm test`
  **3738/3738 across 147 files**; **Playwright DOM-check** confirmed the `MapControls` toggle pills
  (Monochrome / Train / Bus Stop) still render in hawker at 744 px — **no regression** (the shared file is
  byte-identical to hawker's original apart from the header + import edits). `package.json` +
  `package-lock.json` 0.62.614 → 0.62.615; `.serial-state.yml` commit 1096→1097, build 716→717, journal
  958→959, re-anchor **20-07 '26 13:07 SGT**.

- **[TIME ANCHOR]** data.gov.sg blocked by the environment network policy (`403` `CONNECT`); anchored to
  the container system clock `2026-07-20T05:07Z` = **20-07 '26 13:07 SGT** (TF-9 terminal-runtime source).

- **[STATUS]** DRAFT → squash-merge to `main` on green CI (auto-merge SQUASH). New PR (branch restarted
  from `origin/main` `2c31798` = `#1622` merged).

- **[TEST]** No new test — a file move + import repoint + a re-export; existing suite unchanged and green;
  visual behaviour DOM-verified.

- **[KNOWN GAPS]** `LocaleToggle` is a genuine future win but needs the two i18n APIs unified first (a
  deliberate, verified change — not a safe dedup). `BackFab` + `tg.js` are per-TMA by design. The bigger
  line-count targets remain: **slice C** `mapOverlays.js` (×3, diverged — the hard one), **slice D** split
  cuisine `App.jsx` (5,456 lines), **slice E** comment hygiene (operator policy call). Carried: v0.62.610
  `disableVerticalSwipes` + v0.62.613 iPad-mini layout still await a real-device eyeball.

## Confirmation Gates
| Gate | Status | Note |
|------|--------|------|
| G1 | ✅ | PATCH bump 0.62.614 → 0.62.615; all three builds + 3738 tests green; MapControls render DOM-verified (no regression). |
| G2 | ✅ | Deletion of the cuisine + transport `MapControls` duplicates and the 3 `giaToggleStyle` bodies — **allowed**: identical/superset code, content preserved in `_shared/` (git-tracked); no information lost. LocaleToggle/BackFab/tg.js **kept** to avoid destructive merges. |
| G3 | ➖ | no new decision rule (executing the agreed O-55 slice B). |
| G4 | ➖ | no external paid API call. |
