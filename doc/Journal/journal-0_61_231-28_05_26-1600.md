(№ 432 - 28-05 '26 16:00 SGT) v0.61.231

# Journal — soleat v0.61.231 (Notebook aspect ratio: raise TMA viewport caps from 1280 → 1600 px; Transport double-cap removed)

> Operator (28-05 '26): *"If I am using Notebook or web browser in
> Notebook other than Phone or tablet, the screen size for TMA
> should be expand to fill the notebook aspect ratio, currently is
> narrow especially the Train MTA."*
>
> Diagnosis: Transport TMA had a double cap — App.jsx capped at
> 960 px on top of the 1280 px CSS root cap, so a 1440 / 1920 px
> notebook saw a 960 px column with wide black letterbox.
>
> Commit 444 / journal 432 / build 234, on branch
> `claude/v0.61.231-notebook-aspect-ratio` off `main` at v0.61.230.

## New

### [HDR] #432 | 16:00 SGT 28-05-28 | v0.61.231 — TMA viewport caps 1280 → 1600 + Transport double-cap fix | 6 files | 1 PR | 1 build |

- **[INTENT]** Notebook viewports (≥ 1366 px) should fill the TMA
  width without wide black letterbox bars. Train TMA was the
  worst offender (operator-reported).

- **[DELTA]**

  | TMA | Before | After |
  |---|---|---|
  | Cuisine | `#root` max-width 1280 px · App `max-w-[1280px]` | 1600 / 1600 |
  | Menu | `#root` max-width 1280 px | 1600 |
  | Hawker | `#root` max-width 1280 px | 1600 |
  | Transport | `#root` max-width 1280 px · App `max-w-[960px]` (worst — effective 960) | **1600 / 1600** |
  | Oversight | `max-w-md` (~448 px) | Unchanged (admin-only; operator scope didn't include it) |

  Six files edited:
  - `web/cuisine/src/styles.css` — `#root max-width: 1280 → 1600`
  - `web/cuisine/src/v2/App.jsx` — `max-w-[1280px] → max-w-[1600px]`
  - `web/menu/src/styles.css` — `#root max-width: 1280 → 1600`
  - `web/hawker/src/styles.css` — `#root max-width: 1280 → 1600`
  - `web/transport/src/styles.css` — `#root max-width: 1280 → 1600`
  - `web/transport/src/App.jsx` — `max-w-[960px] → max-w-[1600px]`

  Sizing rationale: 1600 px lets MacBook Pro 14" / 16" (1512 / 1728
  effective CSS) fill the viewport without overflow; 1366 / 1440 px
  laptops fill entirely; 1920 px desktops get a centered 1600 px
  column with ~160 px each side (within acceptable letterbox).

  **7. `package.json`** — `0.61.230 → 0.61.231`.

  **8. `doc/.serial-state.yml`** — commit 443→444, build 233→234, journal 431→432; re-anchored 28-05 '26 16:00 SGT.

- **[VERIFICATION (sandbox)]**

  CSS / max-w width-class swaps only — no logic change. CI's TMA
  build job validates the Vite build still produces.

- **[STATUS]** PR-OPEN on push.

- **[TEST]** Operator (post-merge + Railway redeploy):
  1. Open Telegram Desktop on a notebook (≥1366 px wide).
  2. Open Transport TMA — Train map should fill the viewport up to ~1600 px instead of the prior 960 px narrow column.
  3. Open Cuisine TMA — result cards + map should fill up to ~1600 px.
  4. Open Menu TMA and Hawker TMA — should fill up to ~1600 px.
  5. Phone view (< 600 px) unchanged — the `@media (min-width: 600px)` gate isn't touched.
  6. Oversight TMA still narrow (admin tool by design).

- **[KNOWN GAPS]**
  - Inner components inside Transport TMA's Train map view may have their own width assumptions (e.g. fixed-pixel SVG schematic dimensions). If the map content doesn't scale up after this PR, a follow-up could expand the SVG viewBox / Google Map iframe sizing.
  - Oversight TMA left narrow on purpose; operator can request a widen separately.
  - 1600 px is a value judgement. If 1920 px or removing the cap entirely is preferred (full-bleed on every notebook), follow-up patch.

- **Confirmation Gates**

  | Gate | Resolution | Date |
  |---|---|---|
  | G1 — version bump | PATCH `0.61.230 → 0.61.231`. CSS / className edits only. | 28-05 '26 |
  | G2 — destructive action | _n/a_. | 28-05 '26 |
  | G3 — new decision rule | YES — TMA viewport cap is now 1600 px across cuisine / menu / hawker / transport. Future TMAs should match this convention unless there's a content reason to narrow. | 28-05 '26 |
  | G4 — paid external API call | _n/a_. | 28-05 '26 |

## Amendments

- **All four primary TMA stylesheets** — `#root max-width: 1280 → 1600`.
- **Cuisine TMA `App.jsx`** — `max-w-[1280px] → max-w-[1600px]`.
- **Transport TMA `App.jsx`** — `max-w-[960px] → max-w-[1600px]` (the double-cap fix).

## Deleted

_None._
