(№ 848 - 04-07 '26 12:15 SGT) v0.62.494

# Journal — soleat v0.62.494 (Clipboard TMA: backfill ru + de on the 7 cat.* category keys) — closes journal #847 known gap

> Operator: *"7 cat.* category keys in Clipboard carry only en/fr/id in main — they lack ru/de. Fix"*
> Read as: backfill the missing **ru** + **de** on those 7 keys so the `cat.*` group is uniform 8-locale.
> This closes the KNOWN GAP recorded in journal #847 (the Clipboard 5→8 locale PR, #1496).

## New

### [HDR] #848 | 12:15 SGT 04-07-26 | v0.62.494 — Clipboard cat.* keys gain ru + de | 1 web file + 3 doc/meta | PR #(pending) |

- **[INTENT].** In `main`, 7 category keys carried only `en`/`fr`/`id` (no `ru`/`de`) — a pre-existing
  hole, not introduced by the locale-extension PRs. With ru/de missing, those two languages fell back
  to English for the cuisine-category filter labels. Fill them so all 11 `cat.*` keys are uniform.

- **[DELTA] (1 code file).**
  - `web/clipboard/src/lib/i18n.js` — added `ru` + `de` to the **7** keys that lacked them:
    `cat.commonHere`, `cat.southeastAsian`, `cat.eastAsian`, `cat.southAsian`, `cat.middleEastern`,
    `cat.european`, `cat.americas`. Inserted **after `id`**, matching the field order of the sibling
    `cat.*` keys (`sweetsFusion` / `michelinBib` / `setMeal` / `dishes`) and the file-wide
    `{en,fr,id,ru,de,zh,ja,es}` convention. These 7 already carried `zh`/`ja`/`es` (from #1496), so the
    whole `cat.*` group is now uniform **8-locale**.

- **Translation provenance.** Standard region names — ru `Юго-Восточная Азия` / de `Südostasiatisch`,
  ru `Восточная Азия` / de `Ostasiatisch`, etc.; `cat.commonHere` → ru `Популярно в Сингапуре` /
  de `In Singapur verbreitet`. de umlauts (`ü`, `ö`) intact.

- **[VERIFICATION (sandbox)]** `node --check` clean. A node eval-harness (imports/exports stripped,
  `STRINGS` reconstructed via `new Function`) confirmed **all 7 `cat.*` keys resolve 8/8 locales**
  (en/fr/id/ru/de/zh/ja/es). Programmatic Hangul scan on the new ru/de/ja fields: **0 hits**.
  Full vitest + `web/clipboard` build in CI.

- **[STATUS]** PROD — Clipboard TMA redeploys on merge (Railway). Verify on Telegram: switching the
  shared locale to ru or de now shows localised category labels in the cuisine-group picker.

- **[TEST]** Full vitest via CI.

- **[KNOWN GAPS]** None for this fix — the journal #847 gap is now closed. CJK/ru/de live-render
  eyeball (G4) across the task-2 TMAs remains the standing open item. Search auto-fire investigation (A)
  is still separate and unstarted.

## Confirmation Gates
| Gate | Status | Note |
|------|--------|------|
| G1 | ✅ | 7 cat.* keys resolve 8/8 locales (node harness); node --check clean; Hangul scan 0 hits |
| G2 | ⏳ | full vitest via CI |
| G3 | ✅ | serial 847→848, commit 965→966, build 604→605; anchor re-set 04-07 '26 12:15 SGT |
| G4 | ⏳ | redeploy + operator eyeball (ru/de category labels render in Clipboard cuisine picker) |
