(№ 593 - 08-06 '26 13:26 SGT) v0.61.393

# Journal — soleat v0.61.393 (REVERT v0.61.391 — the countryPref gate hung the first load)

> Operator (Railway logs, **CRITICAL**): *"Now loading first 5 hang again, it is critical."*
>
> **Left OPEN for review — auto-merge to be armed on the operator's word.** Commit 606 / journal 593 / build 383. web/cuisine. **Emergency revert.**

## New

### [HDR] #593 | 13:26 SGT 08-06-26 | v0.61.393 — REVERT the v0.61.391 splash gate | App.jsx | 1 PR | 1 build |

- **[INTENT]** v0.61.391's `countryPrefResolved` gate **hung the first load** in production. Railway (deploy running 0.61.391): after `[menu/set-location] D776` + `[set-location] → <redacted>`, **no `[Cuisine-Search]` ever fired** — the boot search never reached the server, the "loading first 5" splash hung. **Same failure class** as the v0.61.365/367 settle timer reverted in v0.61.368.

- **[DECISION]** Per the standing rule (and my own v0.61.391 ship-note: *"if anything hangs, revert immediately — no debate"*), **reverted v0.61.391 at once** — no fix-forward in this minefield.

- **[DELTA]** (`web/cuisine/src/v2/App.jsx`)
  - Removed the `countryPrefResolved` state + the 3 s-backstop loader → restored the simple v0.61.196 mount-time `fetchCountryPref` seed.
  - Removed `if (!countryPrefResolved) return;` from the gate-opener + dropped `countryPrefResolved` from its deps.
  - Net = back to the **v0.61.368 synchronous gate** (functional). The v0.61.392 footer change is **kept**.
  - **`package.json`** `0.61.392 → 0.61.393`; **`doc/.serial-state.yml`** commit 605 → 606, journal 592 → 593, **build 382 → 383**; re-anchored 13:26 SGT.

- **[VERIFICATION (sandbox)]** Cuisine build green. Full suite **113 files / 3088 tests**.

- **[STATUS]** PR-**OPEN**, **auto-merge to arm on the operator's word**. Railway redeploys on merge.

- **[TEST]** Operator: the first load fires the search again (no hang). The "Location mismatch over loading" cosmetic (IMG_2333) returns — that is the lesser evil vs a hang.

- **[KNOWN GAPS]**
  - **The IMG_2333 double-state is NOT fixed** — it's reverted. The "deterministic countryPref gate" keeps hanging because the gate-opener is more entangled than the resolved-flag accounts for; **root still open**, needs a careful, instrumented pass (likely a server-side or sequencing fix, not another client gate).
  - The Railway `[Weather] v2 … empty → v1 fallback` and the `401 /user-location` (transient, during the deploy window) are unrelated noise.

- **Confirmation Gates**

  | Gate | Resolution | Date |
  |---|---|---|
  | G1 — version bump | PATCH `0.61.392 → 0.61.393`. | 08-06 '26 |
  | G2 — destructive action | Revert of v0.61.391 only; v0.61.392 footer preserved; no data touched. | 08-06 '26 |
  | G3 — new decision rule | Reverted — the synchronous gate (v0.61.368) stands; the double-state fix is deferred to a safer approach. | 08-06 '26 |
  | G4 — paid external API call | None. | 08-06 '26 |

## Amendments

- **`web/cuisine/src/v2/App.jsx`** — reverted the v0.61.391 `countryPrefResolved` gate (restored the simple loader + synchronous gate).
- **`package.json`** — `0.61.392 → 0.61.393`.
- **`doc/.serial-state.yml`** — counters (commit 606 / journal 593 / build 383) + anchor 13:26 SGT 08-06-26.

## Deleted

- The v0.61.391 `countryPrefResolved` flag + 3 s backstop + gate condition.
