# Journal — v0.62.587 (Cuisine: result-card address drops the country name)

_(№ 931 — 18-07 '26 05:57 SGT)_

## New

### [HDR] v0.62.587 — Cuisine: the eatery address drops the trailing country name

- **[INTENT]** Operator (IMG_0752, Brisbane tablet): *"Where are the addresses without country name?
  Wasn't it set canonically set in previous PRs?"* Follow-up to the v0.62.586 batch (PR #1594, merged),
  answered via `AskUserQuestion` — the operator chose **"Keep in details, drop country"**.

- **[BACKGROUND]** The result-card address renders via `shortenCountry(venue.area)` **only inside the
  expanded "details, review & links" section** (placed there by **v0.62.124**, an operator row re-order)
  and it **shortened** the trailing country to a 2-letter code (**v0.62.151** — "…, Johor, Malaysia" →
  "…, Johor, MY"); it never dropped it. So the ask sat against two prior decisions — hence the confirm.

- **[DELTA]** `web/cuisine/src/v2/components/ResultCard.jsx`. Renamed `shortenCountry` → `dropCountry`:
  when the trailing comma-segment is a **recognised country** (the `COUNTRY_CODE` map, built from
  `OTHER_COUNTRIES` + Singapore) it is **popped entirely** (e.g. *"…, Maroochydore QLD, Australia"* →
  *"…, Maroochydore QLD"*), tidying any trailing comma/space. An **unrecognised** last segment passes
  through untouched, so a genuine final address line is never eaten. **Placement is unchanged** — the
  address stays in the collapsible "details" section (v0.62.124 kept, per the operator's choice).

- **[VERIFICATION (sandbox)]** Full `npm test` **3719/3719 across 145 files**; Cuisine `vite build`
  green; `package.json` + `package-lock.json` 0.62.586 → 0.62.587; `.serial-state.yml` commit
  1064→1065, build 687→688, journal 930→931, re-anchor 18-07 '26 05:57 SGT. ⚠️ Live-only (address is a
  rendered card row). Branch restarted from `origin/main` after PR #1594 merged (merged-branch rule).

- **[STATUS]** DRAFT → squash-merge to `main` on green CI (auto-merge SQUASH).

- **[TEST]** Existing suite 3719/3719. `dropCountry` is a display transform on the expanded card row
  (verified by build); the country-map recognition mirrors the tested `shortenCountry` predecessor.

- **[KNOWN GAPS]** Classics content edits (flat-white dupe, barramundi pie) pending operator G-gate;
  folio-tab folder connection (v0.62.585 items 2–5); dark-mode carousel glass (v0.62.209 tradeoff);
  Transport TMA responsive port.

## Confirmation Gates
| Gate | Status | Note |
|------|--------|------|
| G1 | ✅ | PATCH bump 0.62.586 → 0.62.587; Cuisine build + 3719 tests green. |
| G2 | ➖ | no destructive action (unrecognised address tails pass through untouched). |
| G3 | ➖ | no new decision rule (operator confirmed the display choice). |
| G4 | ➖ | no external paid API call. |
