# Journal — v0.62.627 (Hawker: NEA beside the Map toggle, footer slimmed to match Cuisine, cards no longer cut, working Expand button)

_(№ 971 — 21-07 '26 16:34 SGT)_

## New

### [HDR] v0.62.627 — Hawker footer + map-Expand fixes

- **[INTENT]** Operator (Hawker TMA, desktop): *"the Hawker TMA cards are cut at the bottom. where is the
  expand button. and the NEA i mention should be beside the 'map' and reduce the footer to the same as
  CUISINE TMA"* + *"All TMA footer height must be standard to follow CUISINE TMA footer height."*

- **[DELTA]**
  1. **Footer slimmed → NEA beside Map, cards no longer cut** (`web/hawker/src/App.jsx`, `FooterDock`).
     The footer was **four stacked rows** (control row · a dedicated **NEA ↗** line · zone-count line ·
     version line) — taller than the card carousel's `bottom-16` (4 rem) reserve, so it **overlapped the
     card bottoms** (the "cards cut"). Moved **NEA ↗ into the control row** beside the Map toggle (the
     `leading` slot) and **merged the zone-count + version onto one tiny line** → the footer collapses from
     **4 rows to 2** (~3 rem), matching the Cuisine footer height and clearing the carousel.
  2. **Working Expand button** (`web/hawker/src/components/HawkerMapPanel.jsx`). In the landscape full-bleed
     carousel (`fill` + no `onToggleExpand`) the ⇲ button was **greyed/disabled** — hence *"where is the
     expand button."* Added a separate local `overlayFull` state: in that case **Expand now promotes the
     map to a full-viewport overlay** (`fixed inset-0 z-30`) — the same fix Transport got in v0.62.626 —
     with a Google Maps `resize` + recenter. The portrait-tablet **layout-switch** (`onToggleExpand`) path
     is untouched. **Codex P2 (pre-merge review) fix:** the overlay was `z-30`, the same as the bottom card
     carousel (`fixed … z-30`) which paints later in the DOM, so the cards sat *over* the expanded map;
     raised the overlay to **`z-[35]`** (above the carousel, below the `z-40` footer) so it truly covers the
     carousel while keeping the footer usable.
  - **Transport** footer is already a single compact control row (version inline) → already matches the
    Cuisine standard; **no change**. (Transport doesn't carry an NEA link — its data source lives in the
    header popup.)

- **[VERIFICATION (sandbox)]** `hawker` `vite build` green; full `npm test` **3756/3756 across 148 files**.
  **CANNOT** render the Hawker map or the exact footer height in-sandbox (no Google Maps key → the panel
  shows the "map unavailable" state) — needs an operator eyeball. `package.json` + lockfile 0.62.626 →
  0.62.627; `.serial-state.yml` commit 1108→1109, build 728→729, journal 970→971, re-anchor **21-07 '26
  16:34 SGT**.

- **[TIME ANCHOR]** data.gov.sg blocked by the environment network policy (`403` `CONNECT`); anchored to
  the container system clock `2026-07-21T08:34Z` = **21-07 '26 16:34 SGT** (TF-9 terminal-runtime source).

- **[STATUS]** DRAFT → squash-merge to `main` on green CI (auto-merge SQUASH). New PR (branch restarted
  from `origin/main` `a09a544` = `#1634` merged).

- **[TEST]** No new unit test — footer layout + a map-overlay state; verified via build + reasoning.

- **[KNOWN GAPS / caveats]** Footer height + card clearance + the map Expand overlay all need an operator
  device-check (in-sandbox the map never initialises). The Cuisine footer is a bespoke floating-FAB
  cluster, so "match Cuisine" was implemented as *"a single compact control row + one tiny version line"*
  (the practical common denominator), not a pixel-for-pixel copy of the FAB dock. If the operator wants the
  full Cuisine FAB-dock treatment on Hawker/Transport, that's a larger follow-up.

## Confirmation Gates
| Gate | Status | Note |
|------|--------|------|
| G1 | ✅ | PATCH bump 0.62.626 → 0.62.627; hawker build + 3756 tests green. |
| G2 | ✅ | footer rows consolidated (NEA moved, two text lines merged) — content preserved, not deleted; the greyed Expand button is now functional. |
| G3 | ➖ | no new decision rule. |
| G4 | ➖ | no external paid API call. |
