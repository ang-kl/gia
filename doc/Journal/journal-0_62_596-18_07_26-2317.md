# Journal — v0.62.596 (Hawker: redevelopment closure class + pin recolour & CLOSE badge)

_(№ 940 — 18-07 '26 23:17 SGT)_

## New

### [HDR] v0.62.596 — Hawker: a third "Redevelopment" closure class + the map pin recolours to the tab

- **[INTENT]** Operator follow-up on the v0.62.595 closure feature:
  1. Add a **Redevelopment** class — tab *"Redevelopment till DDth"* / *"till DD MMMM"*, **near-black
     background + light text**.
  2. Recolour the **map pin** to its tab's background (cleaning red / renovation grey / redevelopment
     near-black) and add a **"CLOSE"** badge above the pin (like the existing "NEW" badge).

- **[DELTA — data]** `scripts/build-hawker-closures.js` now splits `other_works` into
  **redevelopment** (`/redevelop/`) vs **renovation** (repairs / redecoration / works), and carries the
  CSV `lat`/`lng`. Regenerated `data/hawker-closures.json` → **122 cleaning, 40 renovation, 1
  redevelopment** (Bukit Timah).

- **[DELTA — vault/API]** `hawker-vault.js`: **un-skipped** redevelopment centres — Bukit Timah Market is
  now **shown** (with a redevelopment tab/pin) instead of hidden (**122 → 123** centres); its coords come
  from the closure CSV (`hawker-coords.json` has none for a closed centre) via a new coord fallback. The
  closure merge attaches `redevelopment`. `index.js` includes `redevelopment` in the attach condition.

- **[DELTA — client]** New shared `web/hawker/src/closure.js` (`activeClosure` — precedence
  **redevelopment > renovation > cleaning**; `closureTill`; `CLOSURE_PIN_COLOR`; `CLOSURE_TAB_BG`),
  imported by **both** the card tab (`App.jsx`) and the pin (`HawkerMapPanel.jsx`) so they never drift.
  The card tab adds the redevelopment variant (`bg-neutral-900`, white text). `hawkerPinNode` gains a
  `closureKind` arg → the droplet recolours to `CLOSURE_PIN_COLOR` and shows a **"CLOSE"** badge (wins
  over "NEW") while a closure is active. The old inline closure helpers in `App.jsx` were removed in
  favour of the shared module.

- **[DELTA — test]** `__tests__/hawker-vault.test.js`: the "skips Bukit Timah" test → **"keeps Bukit
  Timah (shown with a redevelopment tab)"**; the count description updated to 123.

- **[VERIFICATION (sandbox)]** `node --check` on server JS OK; Hawker `vite build` green; full `npm test`
  **3719/3719 across 145 files**; `package.json` + `package-lock.json` 0.62.595 → 0.62.596;
  `.serial-state.yml` commit 1074→1075, build 697→698, journal 939→940, re-anchor 18-07 '26 23:17 SGT.
  **Active today (18 Jul):** Bukit Timah *REDEVELOPMENT till 2029-12-31* (black); Blk 409 AMK Ave 10
  *RENOVATION till 2026-08-07* (grey). ⚠️ Live-only (renders on the deployed map/cards).

- **[STATUS]** DRAFT → squash-merge to `main` on green CI (auto-merge SQUASH).

- **[TEST]** Existing suite 3719/3719 incl. the updated Bukit Timah vault test.

- **[KNOWN GAPS]** Tab/badge text EN/FR only; the 9 unmatched multi-block centres (postal hand-map)
  from v0.62.595 still open. Cuisine `tg.js` safe-area gap; folio-tab folder connection; Transport port.

## Confirmation Gates
| Gate | Status | Note |
|------|--------|------|
| G1 | ✅ | PATCH bump 0.62.595 → 0.62.596; Hawker build + 3719 tests green. |
| G2 | ➖ | no destructive removal — un-skipping Bukit Timah is additive (a hidden centre now shown). |
| G3 | ➖ | no new decision rule (extends the operator's closure spec). |
| G4 | ➖ | no external paid API call. |
