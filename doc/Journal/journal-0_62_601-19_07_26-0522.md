# Journal — v0.62.601 (Transport TMA: responsive scaffold — device detection + two-panel / carousel)

_(№ 945 — 19-07 '26 05:22 SGT)_

## New

### [HDR] v0.62.601 — Transport TMA: device detection + the two-panel / carousel layout (Slice 2 of the port)

- **[INTENT]** Operator: *"Proceed to build device detection + the two-panel/carousel layout."* (The
  order-log / veto / TP-calculation text earlier in the same message was flagged by the operator as
  *"Oops! Wrong entry"* — unrelated to gia — and ignored.) This is the responsive scaffold that hosts
  the v0.62.598 StationCard across device classes.

- **[DELTA — map]** `web/transport/src/components/MrtMapPanel.jsx`: **+`fill` prop** — when set, the
  map fills its (bounded) parent (`height: 100%`) instead of the fixed 70vh self-sized block, so the
  two-panel / carousel shells can give it a defined height. Default unchanged.

- **[DELTA — app]** `web/transport/src/App.jsx`:
  - **`useViewport()`** wired in (placed above the error/loading early returns — Rules of Hooks) for
    `{ deviceClass, orientation, isWide }`.
  - New **`StationCarousel`** — a horizontal snap-scroll carousel of station cards (IntersectionObserver
    gives the peeking cards the frosted "glass" look), mirroring the Hawker `CentreCarousel`.
  - The render was refactored into shared consts (`headerEl`, `mapBlock(fill)`, `renderStationCard`,
    `singleFocusedCard`, `secondaryPanels`, `sourceFooter`, `footerBar`) feeding **three layouts**:
    - **Mobile (phone)** — the **original stacked single column**, unchanged.
    - **Wide + portrait (tablet/desktop)** — **two-panel**: fixed header + a `h-[40vh]` fill map +
      a `flex-1` scrolling **station-card list** (`grid-cols-2` ≥ 1100 px).
    - **Wide + landscape** — full-bleed fill map + a bottom **StationCarousel** of the focused line's
      station cards.
  - The list / carousel items are the focused line's stations (`lineStationsFull(coarseStations,
    focusedCode)`); tapping a card focuses that station (accent ring + the map reframes on
    `focusedStation`). With no line focused but a station tapped, the single `StationCard` shows.

- **[VERIFICATION (sandbox)]** **Playwright screenshots** (stubbed APIs, `affectedCodes:['NEL']` →
  auto-focus) at **390 / 834 / 1194 px** confirmed all three layouts render correctly — the two-panel
  list, the landscape carousel, and the unchanged phone stack — with coarse-pointer detection
  classifying tablet vs phone and **no page errors** (the only artifact is "Map failed to load" — no
  Google Maps key in the harness; the map renders in production). Transport `vite build` green
  (`index-CtKyFm-3.js 265.92 kB │ gzip 86.93 kB`); full `npm test` **3738/3738 across 147 files**;
  `package.json` + `package-lock.json` 0.62.600 → 0.62.601; `.serial-state.yml` commit 1080→1081,
  build 702→703, journal 944→945, re-anchor **19-07 '26 05:22 SGT**.

- **[TIME ANCHOR]** data.gov.sg blocked by the environment network policy (`403` `CONNECT`); anchored
  to the agent-proxy status timestamp `2026-07-18T21:22Z` = **19-07 '26 05:22 SGT** (TF-9 fallback;
  TF-10 lower bound).

- **[STATUS]** DRAFT → squash-merge to `main` on green CI (auto-merge SQUASH). New PR.

- **[TEST]** No new unit test (a layout/viewport change; verified visually via Playwright). Existing
  suite unchanged and green.

- **[KNOWN GAPS]** The **unified pan/zoom/blink tap flow** (a `flashPin(lat,lng,ms)` raw-blink + a
  global card→pin bridge in the Transport `mapOverlays.js`) is **not** in this slice — tapping a card
  already reframes the map via the existing `fitBounds` on `focusedStation`, which is a reasonable
  card→pin behaviour; the full Hawker-style flash is a follow-up. List / carousel cards render without
  a per-station amenity fetch, so they show exits (from the rich `station.exits`) but not taxi / nearest
  hawker until a card is tapped (single fetch). Carried: the card's `mrt.*` strings + closure tab are
  en/fr-lean; Cuisine `tg.js` safe-area gap; folio-tab folder connection.

## Confirmation Gates
| Gate | Status | Note |
|------|--------|------|
| G1 | ✅ | PATCH bump 0.62.600 → 0.62.601; build + 3738 tests green; 3 layouts verified via Playwright. |
| G2 | ➖ | no destructive removal — the mobile layout is preserved verbatim; the wide layouts are additive branches. |
| G3 | ➖ | no new decision rule (executes the operator's "proceed" instruction). |
| G4 | ➖ | no external paid API call. |
