(№ 896 - 16-07 '26 10:06 SGT) v0.62.540

# Journal — soleat v0.62.540 (Distance rings: region-gated — non-SG gets plain distance rings)

> After I explained the 🚆 stops rings are suppressed outside Singapore (SG-only station list + the
> 2 km nearest-station gate) and offered to make it airtight, the operator said: *"Yes. Have a three
> rings. If location is set in Singapore, 2nd + 3rd ring as earlier prescribed. If the location isn't
> set in Singapore, 2nd ring is placed at 2 km (remove the walk-man emoji), 3rd ring appears after the
> furthest result card location #.#km (without the walk-man emoji)."*

## New

### [HDR] #896 | 10:06 SGT 16-07-26 | v0.62.540 — region-gated distance rings | 4 files | PR #1550 (stacked) |

- **[INTENT].** Always show three rings. Keep the SG train rings as-is; outside SG, replace rings 2 + 3
  with plain distance rings (2 km + farthest result, no glyph), keeping the 🚶 750 m walk ring in both.

- **[DELTA].**
  - **`web/_shared/lib/distance-rings.js`** — `draw()` gained an `isSG` arg (default `true`). Ring 1
    (🚶 750 m walk) unchanged in **both** regions.
    - `isSG=true` → ring 2 = 🚆 computed 2-MRT-stops; ring 3 = 🚆 capped "reach" (exactly as before).
    - `isSG=false` → ring 2 = a plain **2 km** ring (label `2km`, **no** glyph); ring 3 = a plain ring at
      the **farthest result** (`#.#km`, **no** glyph), drawn when `far > 750 m·1.05` and
      `|far − 2000| > 150 m` (so it never overlaps rings 1/2). Non-SG ring 3 is **uncapped** — per the
      operator's "furthest result" instruction.
    - NEW exported **`farthestResultDist(lat, lng, results)`** (max straight-line distance to any result;
      0 when none) — shared by the SG reach ring (`mrtReachRadius` refactored onto it) and non-SG ring 3.
    - `ringLabelNode` renders the distance **alone** when the icon is empty.
    - Side benefit: suppression is now **region-gated**, not just distance-gated → fixes the SG–JB-border
      edge case I flagged (a JB anchor within 2 km of an SG station no longer borrows a train ring).
  - **`web/cuisine/src/v2/components/MapPanel.jsx`** — `syncMarkers` passes `(region||'SG')==='SG'` as
    `isSG`.
  - **`web/hawker/src/components/HawkerMapPanel.jsx`** — unchanged (SG-only, no results → `isSG` defaults
    true → two rings).
  - **`__tests__/distance-rings.test.js`** — +2 tests (`farthestResultDist`).
  - `package.json` — 0.62.539 → **0.62.540**.
  - `doc/Journal/journal-0_62_540-16_07_26-1006.md` — this entry.
  - `doc/.serial-state.yml` — commit 1013→**1014**, build 639→**640**, journal 895→**896**; re-anchor
    16-07 '26 10:06 SGT (prior 895 anchor demoted verbatim, AU-7).

- **[VERIFICATION (sandbox)].**
  - `node --check web/_shared/lib/distance-rings.js` OK.
  - Ring tests **19/19** (was 17 → +2); `mrtReachRadius` tests still green after the refactor.
  - **FULL `npm test` → 3718/3718 across 145 files** (was 3716).
  - `web/` changed (shared helper feeds both bundles) → **cuisine + hawker vite builds BOTH green**.

- **[STATUS]** STAGED → PROD on merge. **Stacked** on branch `claude/handover-july-11-49uzvf` @ 84ff3a8
  (PR #1550's tip, still open/unmerged) → this commit **joins PR #1550** (the single working branch
  already has an open PR; per the git rule I push to it rather than open a second). Merging #1550 lands
  both the card fix (v0.62.539) and this (v0.62.540).

- **[TEST]** Pure `farthestResultDist` unit-tested; the DOM draw path is exercised at runtime in the TMAs
  (no JSDOM in the node suite), same policy as #1548/#1549.

- **[KNOWN GAPS].**
  1. 🎯 **Non-SG ring 3 is uncapped** — a far-flung outlier makes a large ring (by the operator's
     "furthest result" instruction). Add a cap if it reads too big in practice.
  2. 🃏 **ResultDrawer left-peek/overlap** — separate layout follow-up.
  3. 🧩 **Consolidation PR-5 / PR-6**, PR-3 (gemini), country-pref redis-kv — deferred.
  4. 🚪 **CCL6 + Punggol Coast exit pins** — need the LTA exit file.

## Confirmation Gates
| Gate | Status | Note |
|------|--------|------|
| G1 | ✅ | PATCH bump 0.62.539→0.62.540; additive front-end change, full suite + both TMA builds green |
| G2 | ➖ | no destructive action |
| G3 | ➖ | no new decision rule (feature choices captured from the operator's message) |
| G4 | ➖ | no external paid API call |
