(№ 315 - 22-05 '26 20:16 SGT) v0.61.108

# Journal — soleat v0.61.108 (Cuisine free-text — keep the typed dish)

> Operator: *"i entered 'fishball noodle' as free text in Cuisine TMA, it
> detected as Singaporean dish and toggled 'Singaporean' cuisine. it didn't
> search explicitly first 'Fishball Noodle' instead drop the fishball and
> focus on cuisine"*. Via AskUserQuestion the operator chose **"Keep both —
> dish text + cuisine chip"**: keep auto-toggling the inferred cuisine chip,
> but also keep the typed dish text visible and part of the search.
>
> This entry is commit 315 / journal 308 / build 111, off `main` at
> v0.61.106 (#599), concurrent with PR #600 (v0.61.107).

## New

### [HDR] #308 | 20:16 SGT 22-05-26 | v0.61.108 — Cuisine free-text keep | ~4 kb | 2 files | 1 PR | 1 build |

- **[INTENT]** In the Cuisine TMA, typing a specific dish as free text
  (e.g. `fishball noodle`) inferred a cuisine (Singaporean) and toggled
  that chip — but the typed phrase itself vanished from the search
  criteria, so the search no longer scoped to the dish. The operator
  wants both kept: the inferred cuisine chip **and** the typed dish text.

- **[DELTA]**
  - **`web/cuisine/src/v2/App.jsx`** — `handleNLSubmit(text, opts)`
    inferred cuisines/filters and applied them with `setState(nextState)`,
    but never preserved the typed `text`. Added `setNlText(text)` right
    after `setState(nextState)`. `nlText` is App state already bound to
    the Tell-me input (value) and already fed to `runSearch` as the
    `freeText` qualifier — so the typed dish now stays visible in the
    Search-criteria builder and scopes the next search, alongside the
    inferred cuisine chip. The server side already keeps the literal
    phrase in the venue query (the `/api/cuisine/nl-query` brand-name
    prepend), so no server change was needed.
  - **`package.json`** — `0.61.106` → `0.61.108` (PATCH). `0.61.107`
    is PR #600 (concurrent); this branch is off `main` at v0.61.106.

- **[VERIFICATION (sandbox)]**

  ```
  npm run build:cuisine   clean (vite build, 69 modules)
  npm test                1944/1944 passing, 67 files
  ```

- **[STATUS]** DRAFT — PR #601 on
  `claude/v0.61.108-cuisine-freetext-keep` (off `main` at v0.61.106,
  `d162e82`). On merge deploys via the Railway auto-deploy on `main`.

- **[TEST]** Cuisine TMA: type a specific dish (e.g. `fishball noodle`)
  in the Tell-me box and submit — the inferred cuisine chip toggles on,
  **and** the typed phrase stays in the Search-criteria builder and is
  sent as the `freeText` qualifier, so the result is scoped to the dish.

- **[KNOWN GAPS]**
  - No in-sandbox UI verification (no Telegram webview).
  - Concurrent with PR #600 (v0.61.107) — both off `main` at v0.61.106.
    If #600 merges second the doc counters renumber — deferred to the
    operator per the concurrent-PR precedent.
  - The CI runner-startup failures are unaddressed by operator choice
    (a CI-environment issue, not the code).
  - `doc/VibeCodingRecord/` regen still due — last generated through
    PR #552; ~48 PRs now behind.

- **Confirmation Gates**

  | Gate | Resolution | Date |
  |------|-----------|------|
  | G1 — version bump | PATCH `0.61.106` → `0.61.108` | 22-05 '26 |
  | G2 — destructive action | _n/a_ — one-line additive change in `handleNLSubmit` | 22-05 '26 |
  | G3 — new decision rule | _n/a_ — operator chose "keep both" via AskUserQuestion | 22-05 '26 |
  | G4 — paid external API call | _n/a_ — no new API call | 22-05 '26 |

## Amendments

_None._

## Deleted

_None._
