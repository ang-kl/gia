(№ 604 - 08-06 '26 22:43 SGT) v0.61.404

# Journal — soleat v0.61.404 (Location-mismatch resolution: sequence the move, and DON'T auto-load when a far spot is kept)

> Operator: **"go"** to the proposed sequenced flow — *save the old spot in the drawer → (sequential) centre the map on the chosen spot first → save as set-location → message "moved Singapore → Presint 2" → show 📍 return-to-Singapore → pause 1 s → load the first 5 only if device == set-location, else stop.*
>
> Commit 617 / journal 604 / **build 388**. web/cuisine. **Revert-ready** (contained to `applyAnchorCoherenceChoice` + a toast; the hang-prone gate-opener is untouched).

## New

### [HDR] #604 | 22:43 SGT 08-06-26 | v0.61.404 — sequenced mismatch resolve + stop-if-kept-far | App.jsx | 1 PR | 1 build |

- **[INTENT]** The quoted modal — *"Your saved spot is {saved}, but you appear to be at {device}"* — is the **anchorMismatch** modal (`applyAnchorCoherenceChoice`). Today **both** choices immediately fire a search. The operator wants a **strict sequential** resolve and, crucially, **no auto-load when a far spot is kept**.

- **[DELTA]** (`web/cuisine/src/v2/App.jsx` — `applyAnchorCoherenceChoice` only)
  - **Both branches centre the map FIRST** — `setFlyTarget(chosen)` before committing (sequential, not parallel).
  - **Use-device** (set == device): centre → commit (anchor/region/persist) → **move toast** *"Location moved to {device} · 📍 to return to {saved}"* → **pause ~1 s** (a **single explicit-snapshot `setTimeout`** — no state-settle race) → `runSearch`. The old saved spot stays in the **📍 recents drawer** to return to.
  - **Keep saved** (set = saved, which by definition ≠ device — the modal only fires >150 km apart): centre on the kept spot → commit region/flag → **STOP** (no `runSearch`). **Critically** `setFirstLoadPending(false)` + `setLoading(false)` — `runSearch` normally clears the splash gate (`App.jsx:1697/1773`); with no search the stop-branch must clear it itself or the TMA **hangs on the boot splash**. Toast: *"Kept {saved}. You're at {device} — tap 🔍 to search here."*
  - New `locMoveNote` state + `locMoveTimerRef` + 7 s auto-dismiss + a **narrow, bottom-anchored, non-blocking** toast (`pointer-events-none` wrapper; tap to dismiss).
  - The country-level `coherenceMismatch` modal (`applyCoherenceChoice`) is **untouched** (the operator's quote was the anchor modal).
  - **`package.json`** `0.61.403 → 0.61.404`; **`doc/.serial-state.yml`** commit 616 → 617, journal 603 → 604, **build 387 → 388**; re-anchored 22:43 SGT.

- **[VERIFICATION (sandbox)]** Cuisine build green. Full suite **113 files / 3107 tests**.

- **[STATUS]** PR-**OPEN**, **auto-merge armed (squash)**. Railway redeploys on merge.

- **[TEST]** Operator: with device ≠ saved, open the TMA → the modal shows.
  - **Use {device}** → map flies to the device spot, "moved" toast, ~1 s later the 5 load.
  - **Keep {saved}** → map flies to the kept spot, "tap 🔍" toast, **no auto-load, no spinner hang**; tapping 🔍 searches the kept spot; 📍 recents returns to the other spot.

- **[KNOWN GAPS / SAFETY]**
  - **Contained to `applyAnchorCoherenceChoice` + the toast** — the auto gate-opener timing (the v0.61.391 hang area) is **not** touched. The stop branch explicitly clears the splash gate + loading (no hang). The 1 s pause is a single explicit-snapshot timer (no settle race; the 20 s follow-sync can't interfere). **Revert-ready.**
  - Keep-far leaves the result list empty until the user taps 🔍 — intentional ("stop here"); the toast explains.
  - Not unit-tested (React modal/gate flow). **Operator must verify the route + NO hang on the live TMA.**

- **Confirmation Gates**

  | Gate | Resolution | Date |
  |---|---|---|
  | G1 — version bump | PATCH `0.61.403 → 0.61.404`. | 08-06 '26 |
  | G2 — destructive action | Restructures the apply-fn; the SG path + the gate-opener are untouched; the kept branch trades a search for a stop + clear. | 08-06 '26 |
  | G3 — new decision rule | After the anchor-mismatch modal, auto-load the first 5 **only when set-location == device**; keeping a far spot **stops** (user taps 🔍). | 08-06 '26 |
  | G4 — paid external API call | **Fewer** — keeping a far spot no longer fires a Places search. | 08-06 '26 |

## Amendments

- **`web/cuisine/src/v2/App.jsx`** — `applyAnchorCoherenceChoice` sequenced (fly→commit→toast→pause/stop); `locMoveNote` state + ref + auto-dismiss + bottom toast.
- **`package.json`** — `0.61.403 → 0.61.404`.
- **`doc/.serial-state.yml`** — counters (commit 617 / journal 604 / build 388) + anchor 22:43 SGT 08-06-26.

## Deleted

- _None_ (the kept-branch search became a stop + gate-clear).
