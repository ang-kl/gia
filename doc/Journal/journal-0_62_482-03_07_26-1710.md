(№ 836 - 03-07 '26 17:10 SGT) v0.62.482

# Journal — soleat v0.62.482 (Michelin walk slice 12→10 for 20s-deadline headroom)

> Operator (Railway log paste): *"why the Michelin list has problem to scroll past 24 eateries in singapore there are more than 130"*
> Then: *"if enrichment is too slow per page, reduce to 10 or 9."*
> Read as: cut the per-tap slice so page-3 enrichment finishes inside the 20s deadline.

## New

### [HDR] #836 | 17:10 SGT 03-07-26 | v0.62.482 — Michelin per-tap slice 12→10 | 1 code file + 3 doc/meta | PR #(pending) |
- **[INTENT].** Michelin SG (130 curated entries) froze at **24** on the 3rd tap. Keep the
  walk paging through all 130 by trimming per-page enrichment cost under the route deadline.
- **Root cause (traced from the log paste).** Data is fine — logs show `pool=130`. The walk
  serves the slice 12 at a time: tap 1 (seen 0→12) in **1.6s**, tap 2 (12→24) in **11s**, tap 3
  hit the route's **20s deadline** → `[Cuisine-Search] D706 deadline-degraded … returning early
  with 0 venues`. The Michelin handler's `res.headersSent` guard (index.js ~10622) then
  **correctly declines to advance** the seen-set (so the batch isn't burned), but the client got
  **0** — the list read as capped at 24. Per-page cost grows because each Michelin page
  re-enriches every venue server-side (`crowd 2.4s + sanctuary 2.3s + footfall 1.1s + dishes
  0.6s ≈ 7s`) and **Gemini Extract-Dishes 429s** (`prepayment credits depleted`) burn ~6s across
  3 model-fallback retries per page — tipping tap 3 past 20s.
- **[DELTA] (1 code file).**
  - `index.js` (~9911) — `const sliceCap = Math.min(unseen.length, 12)` → **`10`**, with a
    comment recording the root cause + the deeper follow-up. 10 per tap = 13 taps to cover 130
    (was 11); **no venue dropped**, just paged in smaller, faster batches.
- **[VERIFICATION (sandbox)]** `node --check v482_index.js` clean. No unit covers the walk slice
  size; behaviour is unchanged except the batch count.
- **[STATUS]** PROD — bot redeploys on merge (Railway). Verify on Telegram: tap Michelin SG
  repeatedly → the list keeps advancing past 24 toward ~130 without a zero-result stall.
- **[TEST]** Full vitest via CI.
- **[KNOWN GAPS]** This is a **latency-headroom** trim, not a structural fix. If page-N
  enrichment still exceeds 20s (Gemini 429s persist, or a slow tap), the list can still stall.
  **Deeper fix (option 1, offered):** skip enrichment entirely for the pure-Michelin curated
  walk — the cards don't need live crowd/dishes/footfall to page through a curated list.
  **Also aggravating:** Gemini prepayment credits are depleted (top-up removes the ~6s/page
  retry cost). Secondary: a swarm of `/api/cuisine/social-profiles` calls, some with stuck
  `ms:2474130` sockets.

## Confirmation Gates
| Gate | Status | Note |
|------|--------|------|
| G1 | ✅ | node --check clean (index.js) |
| G2 | ⏳ | full vitest via CI |
| G3 | ✅ | serial 835→836, commit 953→954, build 592→593; anchor re-set 17:10 SGT |
| G4 | ⏳ | redeploy + operator eyeball (Michelin SG pages past 24 toward 130, no zero stall) |
