(№ 571 - 07-06 '26 17:45 SGT) v0.61.371

# Journal — soleat v0.61.371 (Cuisine holds an explicit pick — stop the device-sync overriding a Menu location)

> Operator: *"select a different loc in menu tma, launch cuisine tma which didn't conflict with loc, but the map is still singapore, the first 5 becomes singapore, it should first move to the centroid of the location, double-check and verify … then fire first load."*
>
> **Left OPEN for review — auto-merge to be armed on the operator's word.** Commit 584 / journal 571 / build 369. Cuisine TMA.

## New

### [HDR] #571 | 17:45 SGT 07-06-26 | v0.61.371 — sync holds an explicit pick | 1 code file | 1 PR | 1 build |

- **[INTENT]** When the user picks a location in Menu (e.g. Tokyo) and launches Cuisine, anchor + load at **that** place — not the device's physical location.

- **[EVIDENCE — Railway]** `[menu/set-location] … Tokyo 35.6762,139.6503 country=JP` + `country-pref → JP`, then `[set-location] … <redacted>` (SG device GPS) and `[Cuisine-Search] region=SG center=<redacted>` → 5 **SG** venues. The Menu pick was overwritten.

- **[ROOT CAUSE]** The v0.61.355 20 s "follow device" location-sync **overrode** the Menu-picked Tokyo anchor (correctly set by `tryServerCache` from the per-device loc table, with its label) with the SG device GPS — the operator is physically in SG. The sync's `onLocation` fired (SG vs Tokyo > 1.5 km) → `setUserLoc(SG)` + `setLocationAnchor(SG, name:'')` + `saveUserLocation(SG)` → `runInitialLoad` searched SG.

- **[MECHANISM]** (`web/cuisine/src/v2/App.jsx`) — new `locationAnchorRef` (tracks `locationAnchor`) + a guard at the **top** of the sync's `onLocation`: if the current anchor is an **explicit pick** (non-empty `name` — Menu / deep-link / LocationField picks carry a name; device-followed anchors have `name:''`), `return` without following. The explicit pick wins until the user clears or changes it. Race-safe in practice: `tryServerCache` (fast loc-table fetch) resolves before the sync's fresh-GPS tick (`maximumAge:0`, 3.5 s timeout).

- **[DELTA]**
  - **`web/cuisine/src/v2/App.jsx`** — `locationAnchorRef` + tracking effect; explicit-pick guard in the sync `onLocation`.
  - **`package.json`** — `0.61.370 → 0.61.371`. **`doc/.serial-state.yml`** — commit 583 → 584, journal 570 → 571, build 368 → 369; re-anchored 17:45 SGT.

- **[VERIFICATION (sandbox)]** `npm run build` (cuisine) clean; full `npx vitest run` → **106 files / 3007 tests green**. Trace re-walked against the Railway log: Tokyo anchor (named) → guard holds the SG GPS tick → gate opens at Tokyo → `runInitialLoad` runs `runSearch` at the Tokyo anchor.

- **[STATUS]** PR-**OPEN**, **auto-merge to arm on the operator's word**. Cuisine TMA. Field test: in Menu pick a city (e.g. Tokyo) while physically elsewhere → open Cuisine → the map + first 5 should be **that** city, not your device location.

- **[TEST]** Operator: a Menu location pick now carries into Cuisine — it moves to that place and loads there, instead of snapping back to your device's city.

- **[KNOWN GAPS]**
  - **Explicit pick stays until changed** — while a named anchor is held, walking around physically won't move it (by design; the user chose that spot). Clearing the anchor resumes device-follow.
  - **Rare GPS race** — if a fresh device fix somehow beats the loc-table fetch, the first tick could slip through before the anchor is set; not observed (GPS `maximumAge:0` is slower than the fetch).
  - **Not live-E2E tested** — no Telegram WebView here.

- **Confirmation Gates**

  | Gate | Resolution | Date |
  |---|---|---|
  | G1 — version bump | PATCH `0.61.370 → 0.61.371`. | 07-06 '26 |
  | G2 — destructive action | One guard + a ref; device-follow unchanged when there's no explicit pick; full suite green. | 07-06 '26 |
  | G3 — new decision rule | An explicit location pick (named anchor) is held against the device-follow sync until the user changes it. | 07-06 '26 |
  | G4 — paid external API call | None. | 07-06 '26 |

## Amendments

- **`web/cuisine/src/v2/App.jsx`** — `locationAnchorRef` + explicit-pick guard in the location sync.
- **`package.json`** — `0.61.370 → 0.61.371`.
- **`doc/.serial-state.yml`** — counters (commit 584 / journal 571 / build 369) + anchor 17:45 SGT 07-06-26.

## Deleted

- _None._
