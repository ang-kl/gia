(№ 582 - 08-06 '26 08:17 SGT) v0.61.382

# Journal — soleat v0.61.382 (Readable foreign-name line — Gemini romanisation)

> Operator 2-task spec (reacting to the Seoul card **종로은행나무집 시청점**): Task 2 — translate a foreign-script name into the device language so it's readable.
>
> **Left OPEN for review — auto-merge to be armed on the operator's word.** Commit 595 / journal 582 / build 375. Server + web/cuisine. (Part **B** of 3.)

## New

### [HDR] #582 | 08:17 SGT 08-06-26 | v0.61.382 — translate-name.js (Gemini) + 🔤 reading line | translate-name · index · venue-templates · ResultCard | 1 PR | 1 build |

- **[INTENT]** A foreign-script venue name (Seoul: 종로은행나무집 시청점) is unreadable to a non-Korean user. Add a short **device-language reading** under the name so it can be read — without losing the native name (Maps search + identity).

- **[DECISIONS]** (operator AskUserQuestion — all three *recommended*)
  - **Engine:** reuse **Gemini** — *not* Google Cloud Translate REST API. That API is used **nowhere** today, and this same session was *disabling* paid Google APIs; we already have a working Gemini translator (`translate-review.js`). No new paid API, no key-proxy.
  - **Display:** **keep native + add a readable line** (not replace) — machine-translating a brand name breaks Maps search + identity.
  - **Facts (Task 1):** curated localized list → that's the next PR (**C**).

- **[PRIOR ART FOUND]** Both tasks half-existed: `translate-review.js` already translates **reviews** → device language via Gemini; `local-name.js` already attaches a **native-script subtitle** (`nameLocal`) with RULE A/B country gating. So this PR **mirrors** those rather than inventing.

- **[DELTA]**
  - **`translate-name.js`** (NEW) — `translateName({name,targetLang,placeId,redis,_genAIFactory})` → a short romanisation + brief gloss (e.g. `Jongno Eunhaengnamu-jip (City Hall branch)`). Gemini model chain, 30-day Redis cache `name-reading:v1:<placeId>:<tgt>`, graceful (null on failure). `isValidReading()` drops output still in the foreign script / an unchanged echo / model meta-talk. `attachNameReadings(venues,cc,displayLang,redis,…)` reuses `localLangForCountry` (null → skip with **no Gemini calls**) so a Korean user viewing a Korean name gets no redundant reading.
  - **`index.js`** — wired beside `attachLocalNames` at both enrich sites: main cuisine `/search` (15643) + `handleMichelinSearch` (9770).
  - **`venue-templates.js`** — after the `nameLocal` line, render `🔤 <reading>` (bot + 📋 copy card).
  - **`web/cuisine/src/v2/components/ResultCard.jsx`** — a `text-[11px]` hint line `🔤 <reading>` under `nameLocal`, plus `nameReading` forwarded in the copy-one payload.
  - **`__tests__/translate-name.test.js`** (NEW) — 12 tests via mocked `_genAIFactory` (no network).
  - **`package.json`** `0.61.381 → 0.61.382`; **`doc/.serial-state.yml`** commit 594 → 595, journal 581 → 582, **build 374 → 375** (web rebuilt); re-anchored 08:17 SGT.

- **[SECURE PROXY]** Translation runs **server-side** — `GEMINI_API_KEY` never reaches the client. The client only renders `nameReading`; it calls no API. That is the "secure proxy" requirement, answered by architecture (no client key at all).

- **[VERIFICATION (sandbox)]** `node --check` clean (translate-name.js, index.js, venue-templates.js). Cuisine build green. Full suite **113 files / 3081 tests** (+12). The new tests assert: romanise KR, skip a Latin name, drop a foreign-script echo, first-line-only, and the RULE A/B gating (KR+en attaches, KR+ko skips, SG skips).

- **[STATUS]** PR-**OPEN**, **auto-merge to arm on the operator's word**. Railway redeploys on merge.

- **[TEST]** Operator: the Seoul card now shows `종로은행나무집 시청점` with a `🔤 Jongno Eunhaengnamu-jip (…)` line under it — native name kept, reading added.

- **[KNOWN GAPS]**
  - **Vercel webapp (`gia-web`) is a separate repo** — it mirrors this in its own repo as a follow-up (never touched from here).
  - Not live-E2E tested (no live Gemini call in sandbox); the romanisation quality depends on the model.
  - `🔤` glyph chosen as a colour-free "reading" marker; swap easily if the operator prefers another icon.

- **Confirmation Gates**

  | Gate | Resolution | Date |
  |---|---|---|
  | G1 — version bump | PATCH `0.61.381 → 0.61.382`. | 08-06 '26 |
  | G2 — destructive action | None — additive; native name preserved, reading is a new optional line. | 08-06 '26 |
  | G3 — new decision rule | Foreign-script names get a device-language reading via **Gemini** (not Google Translate); kept alongside the native name; RULE A/B gating reused. | 08-06 '26 |
  | G4 — paid external API call | None new — reuses the existing `GEMINI_API_KEY` path (no Google Cloud Translate added). | 08-06 '26 |

## Amendments

- **`translate-name.js`** (NEW) — Gemini name romanisation + `attachNameReadings`.
- **`index.js`** — wired at the two enrich sites (cuisine search + Michelin).
- **`venue-templates.js`** — `🔤` reading line (bot/copy).
- **`web/cuisine/src/v2/components/ResultCard.jsx`** — `🔤` reading line (TMA) + copy-payload field.
- **`__tests__/translate-name.test.js`** (NEW) — 12 mocked tests.
- **`package.json`** — `0.61.381 → 0.61.382`.
- **`doc/.serial-state.yml`** — counters (commit 595 / journal 582 / build 375) + anchor 08:17 SGT 08-06-26.

## Deleted

- _None_ (additive).
