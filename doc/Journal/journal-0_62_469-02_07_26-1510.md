(№ 823 - 02-07 '26 15:10 SGT) v0.62.469

# Journal — soleat v0.62.469 (Cuisine — wire grounded taxonomy badges for classified dishes)

> Operator: wire the client badges now (for batch 1's 99 classified dishes) while the rest of the
> taxonomy stays quota-blocked.

## New

### [HDR] #824 | 15:10 SGT 02-07-26 | v0.62.469 — dish taxonomy badges | 3 files | PR #(pending) |
- **`index.js`** — `/api/cuisine/dishes`: the dish-type `group` now **prefers the grounded Gemini
  `d.type`** (covers every cuisine) over the SG/Malaysian-only `foodGroupFor` regex fallback; also
  passes through **`mealTime[]` / `dietary` / `course`**.
- **`CuisineCategoryDrawer.jsx`** — the dish detail pop-up gains a badge row: **meal-time**
  (icon + label — 🌅 breakfast / ☀️ lunch / 🌙 dinner / 🍡 snack / 🕒 anytime), **dietary**
  (🥬 vegetarian / 🍖 meat / 🦐 seafood / 🍽️ mixed), and **course** (🍽 {course}). All hidden when a
  dish isn't classified yet (only ~99 dishes carry taxonomy data so far).
- **Enum labels stay English for now** — localising the ~15 controlled-vocab values is the planned
  enum-label translation step, deferred until the taxonomy corpus is fuller.
- **[VERIFICATION (sandbox)]** `node --check` index; `esbuild` CuisineCategoryDrawer; CI cuisine
  build + vitest.
- **[STATUS]** PROD — server + cuisine TMA rebuild on merge.
- **[KNOWN GAPS]** Taxonomy still quota-blocked (Gemini 429 persists even on a new key — free-tier
  quota is per Google Cloud **project**, not per key; needs a different-project key or paid billing).
  Only ~99/1,689 dishes classified.

## Confirmation Gates
| Gate | Status | Note |
|------|--------|------|
| G1 | ✅ | node --check + esbuild; CI builds cuisine |
| G2 | ⏳ | full vitest via CI |
| G3 | ✅ | serial 822→823 |
| G4 | ⏳ | redeploy + operator eyeball (Singaporean dishes carry data) |
