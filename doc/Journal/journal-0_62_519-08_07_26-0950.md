(№ 875 - 08-07 '26 09:50 SGT) v0.62.519

# Journal — soleat v0.62.519 (Transport: CCL6 pins re-sourced to OneMap government geocoder)

> Operator: *"check the LTA data … operator website … check the openmap … compare with google
> map … get ready to flip on 12 JUL."* Verified the 3 CCL6 stations against authoritative sources
> and re-curated the pins. **LTA** confirms passenger service opens **12 Jul 2026** (public preview
> 4 Jul) — flip date unchanged. **OneMap** (the SLA/LTA government geocoder) returned building-
> accurate coordinates that differ from the Wikipedia values shipped in v0.62.518 — notably
> **Keppel was ~123 m off**. Curated all 3 to OneMap.

## New

### [HDR] #875 | 09:50 SGT 08-07-26 | v0.62.519 — CCL6 pins re-sourced to OneMap | 4 files | PR #(pending) |

- **[INTENT].** After v0.62.518 corrected the CCL6 pins from very-stale values to the Wikipedia
  infobox values, the operator asked to verify against the **LTA**, the **operator (SMRT)**, and
  **OneMap**, and to compare **Google Maps** — then be ready for the 12 Jul flip. The goal: the 3
  pins must sit on the **real station buildings**.

- **[DELTA].**
  - `data/mrt-coords.json` — re-curated `lat`/`lng` for the 3 CCL6 stations to the **OneMap**
    (`onemap.gov.sg` elastic-search) building geocodes, which resolve each station by its
    registered postal address:
    - **Keppel** (CC30) — `1.27, 103.83111` → `1.26977, 103.83003` (**~123 m**; bldg "51 Keppel Road S099423").
    - **Cantonment** (CC31) — `1.27278, 103.83667` → `1.27287, 103.83706` (~45 m; "100 Everton Road S089395").
    - **Prince Edward Road** (CC32) — `1.27333, 103.84722` → `1.27316, 103.8471` (~23 m; "80 Shenton Way S079122").
  - `__tests__/transport-line-paths.test.js` — updated the 3 CCL6 fixture rows (90–92) and the
    matching ring assertions (166–168) to the OneMap values (self-contained fixture; green by
    construction).
  - `package.json` — 0.62.518 → **0.62.519** (triggers the Railway redeploy → corrected pins reach
    the live Transport TMA).
  - `doc/Journal/journal-0_62_519-08_07_26-0950.md` — this entry.
  - `doc/.serial-state.yml` — commit 992→**993**, journal 874→**875**, build 618→**619**;
    re-anchor 08-07 '26 09:50 SGT (prior 874 anchor demoted).

- **[SOURCES / VERIFICATION].**
  - **OneMap** (SLA/LTA government geocoder — the SG authority): `KEPPEL MRT STATION (CC30)`,
    `CANTONMENT MRT STATION (CC31)`, `PRINCE EDWARD ROAD MRT STATION (CC32)` each returned a single
    building match with postal code. Used as the source of truth.
  - **LTA** news release: *"Following the public preview, Circle Line Stage 6 (CCL6) will open for
    passenger service on 12 July 2026."* Preview 4 Jul 2026, 09:30–21:00. **Flip date confirmed.**
  - **Wikipedia** infoboxes: matched the v0.62.518 (previous) values, confirming those were the
    prior source; OneMap supersedes them.
  - **OpenStreetMap** (Nominatim): no results — the stations are too new to be geocoded there yet.
  - **Google Maps**: **not** cross-checked live — the Chrome browser extension is disconnected.
    OneMap is the SG-authoritative source (Google's SG basemap also derives from SLA), so the pins
    were curated to OneMap; a Google eyeball can reconcile later if the operator reconnects Chrome.
  - `JSON.parse(data/mrt-coords.json)` → OK. `npx vitest run __tests__/transport-line-paths.test.js`
    → **24/24 green**. Full `npx vitest run` in CI.

- **[STATUS]** PROD — merges to main; the per-PR bot redeploys. Pins are live once the Transport
  TMA refetches `/api/transport/stations`. Stations remain **greyed / future** — no "open" change
  until the launch-flip PR.

- **[TEST]** `__tests__/transport-line-paths.test.js` updated + green. Data value + test-fixture
  change (no `index.js` / handler / search / matcher / `i18n`) → outside the `gia-preflight`
  trigger set; no web build.

- **[KNOWN GAPS].**
  1. 🗓️ **Launch flip** — held PR **#1525** (`future` → `operational`, drop `opens*`), to be
     **rebased on this correction** and merged **on 12 Jul 2026**.
  2. 🗺️ **Google Maps cross-check** deferred (Chrome extension offline). OneMap is authoritative;
     reconcile only if a later Google check materially disagrees.
  3. 🚪 **Station-exit + nearby-bus-stop card details** — operator asked to ensure the station
     **card** carries exits and nearby bus stops. Separate work item (needs LTA exit/bus-stop data
     + Transport-TMA card rendering); tracked as still-due, not in this PR.
  4. 📍 **Exit-precise `exit_centroid`** in `data/stations.json` still absent for the 3 (LTA exit
     GeoJSON predates CCL6). The OneMap values are building-accurate.

## Confirmation Gates
| Gate | Status | Note |
|------|--------|------|
| G1 | ✅ | JSON.parse OK; vitest transport-line-paths 24/24; version 0.62.518→0.62.519 |
| G2 | ✅ | operator asked to verify vs LTA/operator/OneMap/Google + ready for 12 Jul; re-sourced to OneMap (govt geocoder), LTA-confirmed the 12 Jul date; Google deferred (browser offline, OneMap authoritative) |
| G3 | ✅ | serial commit 992→993, journal 874→875, build 618→619; anchor 08-07 '26 09:50 SGT (prior 874 demoted) |
| G4 | ➖ | operator eyeball on the live Transport TMA map (3 pins on the real station buildings) once redeployed; optional Google reconcile |
