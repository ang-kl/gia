(№ 884 - 12-07 '26 10:11 SGT) v0.62.528

# Journal — soleat v0.62.528 (CCL6 launch flip #1525 + consolidation scope from the 11-Jul audit §5)

> Operator, resuming from the 11-Jul 05:52 handover (journal #883 / v0.62.527, the code-reuse audit):
> *"pick up the CCL6 launch flip (#1525), start scoping an actual consolidation pass from the audit's
> §5 priority order."* Then, on a scope-depth question: **"Scoping doc only"** — no source consolidated
> this session. Two deliverables land together on branch `claude/handover-july-11-49uzvf`.

## New

### [HDR] #884 | 10:11 SGT 12-07-26 | v0.62.528 — CCL6 launch flip + consolidation scope | 5 files | PR #(pending) |

- **[INTENT].** (1) Execute the held CCL6 launch flip (#1525) — flip the 3 Circle Line Stage 6
  stations from `future` to `operational` on their LTA passenger-service open date, which **is today**
  (12 Jul 2026). (2) Turn the 11-Jul audit's §5 priority list into a decision-ready, sequenced
  consolidation PR queue — **plan only**, per the operator's explicit choice; no consolidation code
  written.

- **[TIME ANCHOR].** SG gov sovereign source (`api.data.gov.sg/v1/environment/air-temperature`) was
  **blocked by the sandbox proxy** (no response). Fell back to the container NTP clock per **TF-9**
  (Claude Code terminal runtime): **12-07 '26 10:11 SGT**, which agrees with the harness-supplied date
  (2026-07-12). The #1525 hold condition ("flip on 12 Jul") is therefore met. Gap vs the prior anchor
  (11-07 05:52) is a new session, not clock drift.

- **[DELTA].**
  - `data/mrt-coords.json` — **the flip.** Keppel (CC30) / Cantonment (CC31) / Prince Edward Road
    (CC32): `status "future"` → `"operational"`; dropped their future-only `opensYear` / `opensDate` /
    `opensDateFr` fields (schema: `opensYear` is "future only"; operational stations carry none).
    Header `_meta`: `operationalCount` 180→**183**, `futureCount` 29→**26**, `lastUpdated`
    2026-05-27→**2026-07-12**, CCL6 dropped from the "announced future stations" list, + a v0.62.528
    flip note appended to the comment.
  - `__tests__/transport-line-paths.test.js` — the 3 inline-fixture CCL6 rows (90–92) flipped
    `future`→`operational` for honesty-to-data. Ring assertions (166–168) are coordinate-based and
    status-independent; the future-line test (174–175) uses its own JS1/JS2 fixtures — both stay green.
  - `audit/consolidation-scope-2026-07-12.md` — **NEW**, companion to
    `audit/tma-code-reuse-audit-2026-07-11.md`. The consolidation scope (below). No source touched.
  - `package.json` — 0.62.527 → **0.62.528** (also triggers the Railway redeploy → flipped data reaches
    the live Transport TMA).
  - `doc/Journal/journal-0_62_528-12_07_26-1011.md` — this entry.
  - `doc/.serial-state.yml` — commit 1001→**1002**, build 627→**628**, journal 883→**884**; re-anchor
    12-07 '26 10:11 SGT (prior 883 anchor demoted verbatim to `..._superseded_883`, AU-7).

- **[HOW THE FLIP WORKS — traced, not assumed].** Gating is **purely `status`-driven**, so the single
  JSON flip is sufficient and complete:
  - `index.js:18203` maps `v.status === 'future' ? 'future' : 'operational'` for `/api/transport/stations`.
  - `MrtMapPanel.jsx` keys every future affordance off `s.status === 'future'`: the small dim dot
    (`:678,680,681`), the skipped station-context (`:561` `if (!ctx || isFuture) return ''`), the
    opens-date popup line (`:540-543`), the suppressed live status/crowd (`:547,557`), the ops/future
    counts (`:783-784`).
  - `line-paths.js:123` derives the CCL ring from `codes` + `status` on the **passed-in** station list
    (not hardcoded), so the ring redraws the 3 as operational automatically.
  - **Result on launch:** full-size coloured CCL dots; live station-context (bus stops compute live via
    `transport.nearestStops` within 600 m; **exits stay blank** — `data/station-exits.json` is still
    empty, no authoritative source, **not fabricated**, per #876); line-status + more-info link + CCL
    badge (all readied in #876/v0.62.520). Only exits + first/last-train remain to fill once LTA
    publishes them.

- **[THE CONSOLIDATION SCOPE].** `audit/consolidation-scope-2026-07-12.md` converts audit §5 into an
  ordered, **behaviour-preserving** PR queue (lowest-risk first): **PR-1** `index.js` require-hoist
  (~60 LOC, zero-risk warm-up) → **PR-2** `web/_shared/` byte-identical small components (~700) →
  **PR-3** `lib/gemini/` unified client migrating **only** the 3 outside call sites
  (`translate-name`/`translate-review`/`durian-gemini-verifier`), `gemini-client.js` internals left
  untouched (~380–450) → **PR-4** `lib/redis-kv.js` migrating only off-hot-path
  `recent-locations`/`country-pref` (~100–150) → **PR-5** `web/_shared` build-config + `api.js` core +
  i18n infra (~360) → **PR-6** the big `mapOverlays.js` extraction **last** (~4,800); plus an offline
  CI-only track (`pipeline-scaffold.mjs` / composite GH Action / `nation-overlay` merge helper). §4
  quarantines the audit's **DO-NOT-BLIND-MERGE** items as behaviour decisions that must land *first*
  (D-1 hawker `tg.js` `history.length` heuristic, D-2 hawker `BackFab.jsx`, D-3 CJK U+3000-vs-U+3040
  canonical-range pick, D-4 `LocationField` as a shared *hook* not component). §5 keeps
  `gemini-client.js` internals, hot-path Redis modules, `about`/`oversight`, the Places normalizer and
  the `registerCommand` wrapper explicitly out of scope.

- **[NOT DONE — BY INSTRUCTION].** No consolidation code. No `web/_shared/`, `lib/gemini/`,
  `lib/redis-kv.js`, pipeline scaffold, or composite Action created. The scope names the first PR
  targets and their guards; each still needs its own G1 (version) / G3 (structural sign-off) gate
  before it lands.

- **[VERIFICATION (sandbox)].**
  - `JSON.parse` OK on `data/mrt-coords.json`; a status walk reconciles **183 operational + 26 future**
    station bodies against the header counts exactly, and confirms no CC30/31/32 remains `future`.
  - `node --check __tests__/transport-line-paths.test.js` OK.
  - `vitest run __tests__/transport-line-paths.test.js` → **24/24**.
  - **Full `npm test` → 3699/3699 across 144 files.**
  - `/gia-preflight` run (transport render-data path).
  - No `web/*/src` change → **no web build** (`line-paths.js`/`mapOverlays.js` read the served
    `/api/transport/stations` data at runtime; matches the #1518/#1519 CCL6 coord-PR precedent).

- **[STATUS]** PROD — the version bump redeploys the bot (Railway) so the flipped `mrt-coords.json`
  serves via `/api/transport/stations`; the Transport TMA lights the 3 stations up as operational on
  next load. `audit/` is not built or served.

- **[TEST]** `data/mrt-coords.json` is server-read (not bundled); the only changed `.js` is the
  self-contained test fixture. Inside the `gia-preflight` awareness set (transport render data) →
  ran node --check + full vitest. `audit/` + `doc/` are doc-only.

- **[KNOWN GAPS].**
  1. 🚉 **#1525 residual** — `exits[]` + `first_last_train[]` for the 3 CCL6 stations remain empty;
     fill on/after open once LTA publishes the exit GeoJSON + timings (carried from #876).
  2. 🧱 **Consolidation queue is scoped but unexecuted** — PR-1…PR-6 + the offline track are planned,
     not built; each needs its own gate. §4 behaviour decisions (D-1…D-4) must precede their extractions.
  3. 🗄️ **Local iCloud git tree** — still ~70 PRs behind `origin/main`; this PR built from fresh
     `origin/main` state.
  4. 🧾 **VibeCodingRecord regen** for this PR still due (append #(this) to `data/prs.ndjson` +
     `data/pr-files.tsv`, bump `GEN_DATE`, run the generator) — do on next catch-up.

## Confirmation Gates
| Gate | Status | Note |
|------|--------|------|
| G1 | ✅ | PATCH bump 0.62.527→0.62.528; scheduled launch-day data flip, standard per-PR rule |
| G2 | ➖ | dropped `opensYear`/`opensDate`/`opensDateFr` on the 3 flipped stations — a schema-mandated field removal on a status transition (future-only fields), not a destructive doc/file action; no Removed table applies |
| G3 | ➖ | no new decision rule — the consolidation doc is a scope/plan, not a policy |
| G4 | ➖ | no external paid API call (SG gov time source is free; it was blocked anyway, fell back to TF-9) |
