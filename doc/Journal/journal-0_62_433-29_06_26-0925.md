(№ 787 - 29-06 '26 09:25 SGT) v0.62.433

# Journal — soleat v0.62.433 (Sketchbook — catch-all header row, sort, archive/restore)

> Operator: proceed 7+11, and add a "Clear all" in the Catch-all (via ✍️ Edit) that ARCHIVES for
> 30 days (archive all / restore).

## New

### [HDR] #787 | 09:25 SGT 29-06-26 | v0.62.433 — items 7+11 + archive | 7 files | PR #(pending) |
- **7+11** `CatchAllStrip.jsx` — header row (under the chips): title · count pill · **sort**
  (Title / Country / City / Cuisine / Date + ⇧⇩ direction) · **✍️ Edit**. Cards sorted client-side
  (city is best-effort from the address; country from region).
- **Archive (operator add)** — ✍️ Edit reveals **🗑 Clear all** (confirm → archive all catch-all
  cards for 30 days) + **↩ Restore (N)**.
  - `clip-store.js` — `archiveAllClips` / `restoreArchive` / `archivedCount` (a separate
    `clip_archive:` list, 30-day TTL; card hashes kept at 30 days so Restore is intact).
  - `clipboard-routes.js` — POST `/archive-all`, `/restore-archive`; `/state` returns
    `archivedCount`.
  - `api.js` `archiveAll`/`restoreArchive`; `state.js` tracks `archivedCount`; `App.jsx` wires both.
- `lib/i18n.js` — `sort.*`, `catchAll.clearAll/restore/archiveConfirm`.
- **[VERIFICATION (sandbox)]** clip-store 42 + clipboard-routes 69 = 111 green; real `vite build`
  OK (50 modules); `node --check` clipst.js + cr.js.
- **[STATUS]** PROD — server + clipboard TMA rebuild.
- **[REMAINING]** 4 (cuisine filter layout), 8 (location list), 9 (drawer 3-buttons + wiring),
  12b (drawer description), 13 (note), 14 (bot /clipboard).

## Confirmation Gates
| Gate | Status | Note |
|------|--------|------|
| G1 | ✅ | vite + node --check |
| G2 | ✅ | 111 tests |
| G3 | ✅ | serial 786→787 |
| G4 | ⏳ | redeploy |
