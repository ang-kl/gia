(№ 877 - 08-07 '26 10:40 SGT) v0.62.521

# Journal — soleat v0.62.521 (VibeCodingRecord catch-up regen: PRs #1512–#1527)

> Operator: *"Update Vibe code, vault"*. This PR is the **Vibe-code half** — regenerate the
> VibeCodingRecord ledger + hosted Vibe Journal so they track main (#1527/v0.62.520). The
> **vault half is raised for a decision, not assumed**: `vault/` was deleted + `.gitignore`'d in
> the operator-approved v0.62.508 audit, so recommitting a snapshot would reverse that decision.
> The git-authoritative backup is `main @ #1527` already on GitHub.

## New

### [HDR] #877 | 10:40 SGT 08-07-26 | v0.62.521 — VCR catch-up regen (#1512–#1527) | 10 files | PR #(pending) |

- **[INTENT].** The VibeCodingRecord ledger was stale at **#1511 / GEN_DATE 2026-07-06**; main has
  since merged through **#1527 / v0.62.520**. Bring the generated ledger + hosted page current.

- **[DELTA].**
  - `doc/VibeCodingRecord/data/prs.ndjson` — appended **15 merged PRs** (#1512–#1524, #1526, #1527);
    schema `{n, title, state, merged, body}`. 1483 → **1498** records.
  - `doc/VibeCodingRecord/data/pr-files.tsv` — appended the 15 squash-commit file lists.
    - **#1514** (vault removal) recorded with only its **4 non-vault files** (`.gitignore`,
      `doc/.serial-state.yml`, its journal, `package.json`) — the 296 `vault/` deletions the
      compare API returns are the exact noise the audit removed, so they are **not** re-listed.
  - `doc/VibeCodingRecord/generate.mjs` — `GEN_DATE` 2026-07-06 → **2026-07-08**.
  - Ran `node doc/VibeCodingRecord/generate.mjs` → regenerated `records.tsv` (**1498 rows**),
    `vibe-coding-record.md`, `public/doc/vibe-journal.html`, `public/doc/vibe-journal.json`.
  - `package.json` — 0.62.520 → **0.62.521** (per the standing per-PR bump rule; also cache-busts
    the served static page).
  - `doc/Journal/journal-0_62_521-08_07_26-1040.md` — this entry.
  - `doc/.serial-state.yml` — commit 994→**995**, build 620→**621**, journal 876→**877**;
    re-anchor 08-07 '26 10:40 SGT (prior 876 anchor demoted verbatim).

- **[EXCLUSIONS].** **#1510** (docs: CN/JP food-term notes — still **OPEN**) and **#1525** (CCL6
  launch flip — **HELD open** by design until 12 Jul) are correctly **not** in the ledger; #1510
  folds into the next regen once merged.

- **[CLASSIFICATION NOTE].** The deterministic title/body classifier bucketed all 15 new PRs as
  `feature` (tally feature 994→**1009**; fix/docs/copy/prompt-tune/refactor/test/infra unchanged;
  rework-flagged 128). **Not hand-tuned** — the VCR is an auto-generated artifact and the
  classifier is its source of truth; per-PR overrides would break reproducibility. Classifier
  tuning, if wanted, is a separate task.

- **[VERIFICATION (sandbox)].** Generator ran clean. `records.tsv` last row **#1527**
  (0.62.520, dated 2026-07-08); `vibe-journal.json` `count` **1498** + last `n` **1527**,
  `generated` 2026-07-08; **#1527** present in the hosted HTML; md footer reads
  "1498 pull requests" @ 2026-07-08.

- **[STATUS]** PROD — merges to main; the per-PR bot redeploys and serves the refreshed
  `/doc/vibe-journal.html`. Generated-artifact change, **not** under the AU append-only Recipe
  (per `CLAUDE.md`: `doc/VibeCodingRecord/` is a generated artifact).

- **[TEST]** Generated-doc + data change (no `index.js` / handler / search / matcher / `i18n`
  surface) → outside the `gia-preflight` trigger set; no web build (`public/doc` served static).
  Full `npx vitest run` in CI.

- **[KNOWN GAPS].**
  1. 🗄️ **Vault** — the second half of the request. `vault/` was removed from git in v0.62.508;
     the decision (bump the `~/.claude/.last_vault` marker to today vs. recommit a git `vault/`
     snapshot, reversing the audit) is **raised for the operator**, not assumed.
  2. 📋 **#1510** — folds into the next VCR regen once it merges.

## Confirmation Gates
| Gate | Status | Note |
|------|--------|------|
| G1 | ✅ | generator ran clean; records.tsv/json/html/md all show 1498 + last #1527; version 0.62.520→0.62.521 |
| G2 | ✅ | "Update Vibe code" done (ledger + hosted page current to #1527); "vault" surfaced as a decision (audit-reversal risk), not silently assumed |
| G3 | ✅ | serial commit 994→995, build 620→621, journal 876→877; anchor 08-07 '26 10:40 SGT (prior 876 demoted verbatim) |
| G4 | ➖ | operator eyeball on the live /doc/vibe-journal.html once redeployed; decide the vault half |
