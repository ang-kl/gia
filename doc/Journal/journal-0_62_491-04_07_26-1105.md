(№ 845 - 04-07 '26 11:05 SGT) v0.62.491

# Journal — soleat v0.62.491 (Hawker TMA: extend UI locales from 5 → 8, adding zh · ja · es)

> Operator (task 2 — TMA language-set audit):
> *"audit the language set across all TMA, Menu TMA doesn't have the full language listing like Cuisine."*
> Read as: bring every TMA up to Cuisine's 8-language UI set (en/fr/id/ru/de/**zh/ja/es**), one PR per TMA.
> **This is PR 2/4 — Hawker.** (PR 1/4 Menu = v0.62.490 #1493, merged.)

## New

### [HDR] #845 | 11:05 SGT 04-07-26 | v0.62.491 — Hawker TMA gains zh · ja · es | 2 web files + 3 doc/meta | PR #(pending) |

- **[INTENT].** Close the Hawker-vs-Cuisine locale gap. Add **Chinese (zh)**, **Japanese (ja)**,
  **Spanish (es)** so the Hawker Centre hub — header, map layers, region chips, per-centre buttons,
  stall-status labels, FABs, save-to-chat messages — renders in all 8 languages.

- **[DELTA] (2 code files).**
  - `web/hawker/src/i18n.js` — `SUPPORTED` `['en','fr','id','ru','de']` → **`+['zh','ja','es']`**.
    Appended three flat `ZH_STRINGS` / `JA_STRINGS` / `ES_STRINGS` overlays (**64 keys each** — the exact
    `ID_STRINGS` key set, **parity-validated** by script before assembly) + **3 merge loops** after the
    DE loop, same overlay pattern as id/ru/de (base `en`/`fr` win, overlay fills the gap).
  - `web/hawker/src/components/LocaleToggle.jsx` — **3 dropdown rows**: `🇨🇳 中文` / `🇯🇵 日本語` / `🇪🇸 Español`.

- **Translation provenance.** Agent-drafted per language from the EN base. Singapore-standard terms used
  (`小贩中心`, `德士站` for taxi stand). All emoji, `{v}`/`{n}`/`{from}`/`{to}`/`{mapped}`/`{total}` tokens
  preserved in place. Brand/proper nouns (`Google Maps`, `MRT`, `LRT`, `LTA`, `NEA`, `UNESCO`) verbatim.
  ja overlay verified all-Japanese (no stray Korean); es overlay written apostrophe-free (single-quoted JS).

- **[VERIFICATION (sandbox)]** Key-parity assertion: each overlay covers exactly the 64 `ID_STRINGS` keys
  (no missing, no extra). `esbuild` bundle-parse **clean** on both files (only `./tg.js` + `react`
  module-resolve warnings — not syntax). A node merge-harness confirmed **all 8 locales resolve** for
  `header.title`, `footer.tag`, `stalls.status.existing`. Full vitest + `web/hawker` build in CI.

- **[STATUS]** PROD — Hawker TMA redeploys on merge (Railway). Verify on Telegram: the language dropdown
  lists 8; picking 中文 / 日本語 / Español re-renders the hub. **CJK render eyeball** (font + pill fit for
  zh/ja) is the G4 gate.

- **[TEST]** Full vitest via CI.

- **[KNOWN GAPS]** (1) **Transport** and **Clipboard** TMAs still at 5 locales (task-2 PRs 3/4, 4/4,
  still due). (2) Hawker **data-driven** strings from data.gov.sg (centre names, unknown stall-status
  values) pass through untranslated by design (the `formatStalls` lookup-or-passthrough). (3) CJK
  live-render eyeball outstanding. (4) Search auto-fire investigation (A) remains separate. G4 gates for
  v0.62.483–491 remain open.

## Confirmation Gates
| Gate | Status | Note |
|------|--------|------|
| G1 | ✅ | key-parity 64/64 per lang; esbuild parse clean (i18n + LocaleToggle); node merge harness — 8/8 locales resolve |
| G2 | ⏳ | full vitest via CI |
| G3 | ✅ | serial 844→845, commit 962→963, build 601→602; anchor re-set 04-07 '26 11:05 SGT |
| G4 | ⏳ | redeploy + operator eyeball (8-language dropdown; zh/ja/es render + CJK pill fit) |
