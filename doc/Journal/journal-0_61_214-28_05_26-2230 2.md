(№ 415 - 28-05 '26 22:30 SGT) v0.61.214

# Journal — soleat v0.61.214 (psv: + cv: callback `messageId` ReferenceError fix)

> Operator: tapped `/ver` → 🧪 Place-search variance → *"Sorry, that
> variance-test action hit an error."* Server log:
> `[Error] /psv callback failed: messageId is not defined`. Hotfix.
>
> Commit 427 / journal 415 / build 217, on branch
> `claude/v0.61.214-messageid-fix` off `main` at v0.61.213.

## New

### [HDR] #415 | 22:30 SGT 28-05-28 | v0.61.214 — psv:/cv: messageId hotfix | 3 files | 1 PR | 1 build |

- **[INTENT]** Make the v0.61.213 `/ver` variance menu actually
  function. Also patch the same latent bug in the v0.61.177 cv:
  branch.

- **[DELTA]** The `bot.on('callback_query')` handler does NOT
  declare `messageId` at the function scope. Each callback
  branch that needs it must declare locally (the per: branch at
  line 2731 set this pattern with `const messageId =
  q.message?.message_id || null;`). v0.61.213's psv: branch
  referenced `messageId` without declaring → ReferenceError
  every tap. The v0.61.177 cv: branch had the same bug latently
  (operator hadn't noticed, possibly because `bot.editMessageText`
  with `messageId: undefined` was being caught silently).

  **`index.js`** (2 edits):
  1. **psv: branch** — added `const psvMessageId = q.message?.message_id || null;` at the top, before the `try { … }`. Updated `psv:menu`, `psv:close`, `psv:run-<mode>` paths to pass `psvMessageId`. (Names are namespaced so future branches can't collide.)
  2. **cv: branch** — same fix with `cvMessageId`. Belt-and-braces.

  - **`package.json`** — `0.61.213 → 0.61.214`.
  - **`doc/.serial-state.yml`** — commit 426 → 427, build 216 → 217, journal 414 → 415; re-anchored 28-05 '26 22:30 SGT.

- **[VERIFICATION (sandbox)]**

  ```
  $ node --check index.js   (OK)
  $ npx vitest run          ✓ 2538/2538 pass
  ```

- **[STATUS]** PR-OPEN on push.

- **[TEST]** Operator (post-merge + Railway redeploy): `/ver` → 🧪 Place-search variance → tap any Run mode → confirm screen renders correctly (was: "Sorry, that variance-test action hit an error.").

- **[KNOWN GAPS]** None — pure hotfix.

- **Confirmation Gates**

  | Gate | Resolution | Date |
  |---|---|---|
  | G1 — version bump | PATCH `0.61.213 → 0.61.214`. Hotfix. | 28-05 '26 |
  | G2 — destructive action | _n/a_. | 28-05 '26 |
  | G3 — new decision rule | YES — callback branches that need `messageId` MUST declare it locally (`const xMessageId = q.message?.message_id || null;`) since the dispatcher doesn't expose it at function scope. Future branches should follow the per: pattern. | 28-05 '26 |
  | G4 — paid external API call | _n/a_. | 28-05 '26 |

## Amendments

- **`psv:` callback branch** (v0.61.213) — was: referenced undeclared `messageId`. Now: local `psvMessageId`.
- **`cv:` callback branch** (v0.61.177) — same fix as preventive: local `cvMessageId`.

## Deleted

_None._
