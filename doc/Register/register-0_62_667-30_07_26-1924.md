(№ 145 - 30-07 '26 19:24 SGT)

# Register — soleat / Gia4lunch v0.62.667 (correction: restore SG's 2 "dropped" Bib Gourmand rows)

> **Document type:** Register (per `doc/CLAUDE.md` §3; §4.1 — items move between sections, never deleted).
> **Continues** `register-0_62_666-30_07_26-1843.md` (№ 144). **Scope:** correcting the v0.62.665 deletion of 2
> lapsed Singapore Bib Gourmand entries. **Reflects:** project state at **v0.62.667**.
> **Targeted addition** — restates only the items this entry touches.

---

## Completed (new this entry)

| # | Item | Shipped | PR(s) |
| :--- | :--- | :--- | :--- |
| C-69 | **Restored "Eminent Frog Porridge & Seafood" and "Soon Huat" to `SG-michelin.js`** as `awardYears: ["'25"]` historical rows (2025-held, not retained 2026) — corrects the v0.62.665 decision to delete them outright. Original address text recovered from git history, not reconstructed from memory. | v0.62.667 | #1670 |
| C-70 | **Fixed the one real correctness consequence of the restoration**: `index.js`'s Michelin criteria tile description string read `BIB_GOURMAND.length` directly (would have silently reported 99 instead of the true current 97); now filters to entries holding `"'26"`. | v0.62.667 | #1670 |

## Decisions (new this entry)

| # | Decision | Rationale | Where recorded |
| :--- | :--- | :--- | :--- |
| D-36 | **CORRECTS D-32/the v0.62.665 Journal's G2 note**: `SG-michelin.js`'s flat schema DOES have a historical-listing mode — a category entry whose `awardYears` holds only past years and no current one. The v0.62.665 premise ("this file has no historical/dropped-listing mode") was wrong; deleting a lapsed award's row was never requested and is reversed here. D-32 (the `awardYears` field addition) and D-33 (the `michelinYear`→`michelinAwardYears` rename) are UNCHANGED by this correction — only the "how do we represent a drop" question is corrected, not the field-shape decisions. | Operator, directly: *"Actually don't drop 2025. I didn't tell you to keep 2025 even though is dropped"* — confirmed via clarifying question as "restore the 2 deleted rows." Matches the pattern already used for Taiwan (D-34, v0.62.666): a schema's own drop-handling convention should be "no current award," not "delete the row," regardless of whether the schema is flat or venue-centric. | Journal `journal-0_62_667-30_07_26-1924.md` |

## Note

Rides on the still-open PR #1670 (v0.62.665 Singapore + v0.62.666 Taiwan) — a new commit was added rather than
amending the prior ones, per standing instruction to never amend without explicit request.
