(№ 142 - 30-07 '26 14:28 SGT)

# Register — soleat / Gia4lunch v0.62.664 (URGENT fix: Telegram Desktop couldn't close, a same-session v0.62.663 regression)

> **Document type:** Register (per `doc/CLAUDE.md` §3; §4.1 — items move between sections, never deleted).
> **Continues** `register-0_62_663-30_07_26-1342.md` (№ 141). **Scope:** the urgent close-button regression.
> **Reflects:** project state at **v0.62.664**.
> **Targeted addition** — restates only the items this entry touches.

---

## Completed (new this entry)

| # | Item | Shipped | PR(s) |
| :--- | :--- | :--- | :--- |
| C-64 | **URGENT FIX: Menu, Clipboard, and Oversight can be closed again on Telegram Desktop.** All three gained a Telegram `BackButton` wiring (Clipboard's first close affordance of any kind), matching the pairing Cuisine/Hawker/Transport have always shipped alongside desktop auto-fullscreen. | v0.62.664 | pending |

## Decisions (new this entry)

| # | Decision | Rationale | Where recorded |
| :--- | :--- | :--- | :--- |
| D-31 | **Any Mini App gaining `requestFullscreen()` on Telegram Desktop MUST be paired, in the same change, with a working `BackButton` (or equivalent verified close affordance).** | v0.62.663 added desktop fullscreen to Menu/Clipboard/Oversight without checking that each already had a way to close once Telegram's native window chrome disappears — Clipboard had NONE at all. Menu's own v0.60.52 comment had already named this exact failure mode ("fullscreen takeover with no chrome and no easy exit") before this session re-introduced it. Recorded as a named rule so the next app to gain desktop fullscreen can be checked against it directly, rather than relying on the same review gap not repeating by chance. | Journal `journal-0_62_664-30_07_26-1428.md` |

## Note on how fast this was caught

The regression this entry fixes shipped in v0.62.663 and was reported by the operator roughly 45 minutes
later — the fastest feedback loop this arc has seen. It is also the clearest evidence yet for the
device-verification caveat this Register has repeated on nearly every entry this arc (O-63, O-67, O-71,
etc.): the original break passed every build and the full test suite cleanly, exactly like the Tailwind
silent-drops and dead-selector bugs before it. Only real use caught it.
