(№ 127 - 13-06 '26 15:45 SGT)

# Register — soleat / Gia4lunch v0.62.69 (outstanding-snapshot)

> **Document type:** Register (per `doc/CLAUDE.md` §3 — *open items, known issues, deferred, decisions, completed*; §4.1 — items move between sections, never deleted).
> **Status:** **Incremental outstanding snapshot** (not a master refresh). Continues the v0.61.300 incremental snapshot (`register-0_61_300-31_05_31-0915.md`). **Scope of this file:** the **v0.62.53 → v0.62.69** arc (PRs #1012 → #1022, 13-06 '26) — the `[LocDrift]` telemetry + Railway log-collector pipeline, the rating-filter rework, the loading-overlay arc, and the Putrajaya/Cyberjaya geo-cap drift fix. The v0.61.147 + v0.61.276 + v0.61.300 ledgers remain the canonical references for items O-1 → O-41 + D-61.120 → D-61.300.
> **Known coverage gap (logged as O-44):** no Register / Technical / Feature snapshot was cut across the **v0.61.301 → v0.62.52** arc; per-PR detail for that span lives only in `doc/Journal/journal-0_61_3*.md` + `doc/Journal/journal-0_62_*.md` and in `doc/VibeCodingRecord/` (PRs #807 → #1011). This file does not backfill it.
> **Reflects:** project state at **v0.62.69, 13-06 '26 15:45 SGT**, immediately after PR #1022 merged to `main` at `a3429c0`. Live deployment on Railway converging on the v0.62.69 bundle. Operator-facing TMAs: Cuisine + Menu + Hawker + Transport + Oversight. Branch protection on `main` remains enabled (4 required checks; enforce_admins=true).

---

## Open Items (live as of v0.62.69)

| # | Item | Owner | Priority | Notes |
| :--- | :--- | :--- | :--- | :--- |
| O-42 | **Durian re-tap returns 0 — `ZERO reason=all-seen-criteria`** | agent | medium | Logged in `log/logs.1781315803421.json` (chat 313940231, Putrajaya): a second `durian` tap returned `D704 returning 0 venues` because the per-session seen-set had consumed the (small) durian pool. The pool was 15–17 candidates, rating-floored to 14–15, all already seen. A `resetSeen` fired once but the immediately-following tap still zeroed. Fix candidates: (a) auto-`resetSeen` when `trulyUnseen===0` AND `hasCriteria`, before returning zero; (b) surface the existing `zeroReasonKey` "tap 🔍 again" copy on this path (it may already exist server-side but the second tap raced it). Separate from the geo-cap fix shipped in #1021. |
| O-43 | **AI-credit depletion has no alerting** | operator + agent | medium | Same log showed **both** providers hard-failing: Gemini `[429] prepayment credits are depleted` (gemini-flash-latest / 2.5-flash / 2.5-flash-lite — `[Extract-Dishes]`) and Anthropic `[400] credit balance too low` (claude-haiku-4-5 — `vibe-summary`). Every AI-enrichment path (dish extraction, vibe summaries, name-gloss) silently degraded to empty for all users. Operator topped up both (13-06 '26). **Gap:** no log signal / metric / threshold alert distinguishes "no AI result" from "billing dead". Candidate: a `[credits]` warn-once-per-window log keyed on the 429/400 billing messages, optionally a `/ver`-surfaced health line. |
| O-44 | **Register / Technical / Feature backfill for v0.61.301 → v0.62.52** | agent | low | No snapshot of the eight-template docs was cut across ~5 weeks / ~200 PRs (#807 → #1011): the "What to order" Arrival-Plate arc, the 126-city / 279-dish curation pass, the French audit, the TMA auth guard, the rating-panel build, etc. The Journal + VibeCodingRecord are current and authoritative for that span; the Register/Technical/Feature rollups are not. Sized as a dedicated catch-up pass (mirrors the v0.62.41 VibeCodingRecord catch-up regen, PR #1004). |

> Items O-33 → O-41 from `register-0_61_300-*.md` are **not** re-listed here; their status is unchanged unless noted in Completed below. O-37 (iCloud-stale-checkout empty blobs) remains guarded by the CI Empty-blob job — that job passed on every PR in this arc (#1012 → #1022).

---

## Decisions (append-only, v0.62.53 → v0.62.69)

> Continues from D-61.300 in `register-0_61_300-30_05_31-0915.md`. Keyed `D-<minor>.<patch>` per the established convention. Per-PR detail in `doc/Journal/journal-0_62_53-13_06_26-0610.md` (the live arc journal, amendments № 7xx).

| # | Decision | Rationale | PR | Date |
|---|---|---|---|---|
| D-62.53 | **`[LocDrift]` structured telemetry.** Two JSON log lines via `loc-drift-log.js`: `set-location {chat,from,to,moved_m,label,region,country,cap_m,ambient,kept}` and `search-anchor {chat,center,region,radius_m,cap_m,cap_src}`. New `capSource` classifies the cap origin: `explicit` (persisted `radiusCapM`) / `city-default` (nearest-city `radiusM`) / `other-default` (40 km OTHER ceiling) / `none`. | Operator-reported "location drifts toward KL"; needed a fingerprint to tell anchor-drift from radius-cap drift. `cap_src='city-default'` + large `radius_m` is the drift signature. | #1012 | 13-06 '26 |
| D-62.54 | **15-minute Railway log workflow + rating-filter rework.** A scheduled GitHub Action fetches Railway logs; rating-floor handling reworked (see D-62.58/.59–61). | Make the `[LocDrift]` lines retrievable off-device for analysis. | #1012 | 13-06 '26 |
| D-62.55–57 | **Log collector hourly → `log/`; 🛑 Stop loading; 10 s rotation; 📜 dish line.** | Collect logs into the repo on a cadence; let users abort a slow curate; slow the rotating-title churn. | #1013 | 13-06 '26 |
| D-62.58 | **Rating panel → "Refine Google Rating" header + 2×2 layout.** | Operator UX pass on the rating control. | #1014 | 13-06 '26 |
| D-62.59–61 | **`actions/*`→v5; rating-panel polish; session-scoped rating + amended reset.** Rating preference scoped to the session and reset copy amended. | CI action-version upkeep + rating-control refinements. | #1015 | 13-06 '26 |
| D-62.62 | **Log fetch writes to a dedicated `railway-logs` branch, not `main`.** `main` is protected (4 required checks, enforce_admins) so a bot push to `main` is rejected; an unprotected `railway-logs` branch takes the hourly log commits with no settings change. | Branch protection (D-61.296) blocks the log-collector's push to `main`; the operator chose the dedicated-branch path over a protection bypass. | #1016 | 13-06 '26 |
| D-62.63 | **Rating-reset copy in the boot overlay + 🛑 Stop pill on every loading pop-up.** The "Rating reset: Good+ ≥ 3.7⭐ — Showing eateries with generally good Google ratings" line shows under the boot "loading random eateries…" message. | Operator: surface the rating state on relaunch; make Stop reachable from any wait. | #1017 | 13-06 '26 |
| D-62.64 | **Log fetch merges logs across the latest deployments.** Fixes an empty-window after a redeploy (a single-deployment fetch missed lines split across the rollover). | Empty log files after deploys. | #1018 | 13-06 '26 |
| D-62.66–67 | **"🛑 Stop curating" pill on its own LAST line, flush RIGHT (0.5 px amber border); modernised loading pop-up; log fetch → `railway-logs`.** | Operator: the pop-up "looks 90s"; standardise the Stop affordance placement. | #1019 | 13-06 '26 |
| D-62.68 | **`TIGHT_15KM` geo-cap tier for Putrajaya + Cyberjaya** (`city-centroids.js`). A new radius tier below DENSE-30: these two compact Klang-Valley-south cities fell into the default MAJOR-45 km tier, so a Putrajaya pin's `city-default` cap reached central KL (22.6 km), PJ (20.9 km), Shah Alam (22.8 km). 15 km keeps the Putrajaya–Cyberjaya cluster (Cyberjaya 2.5 km) and excludes the KL core. City-default fill only — an explicit `radiusCapM` still wins; `searchRadius` not mutated. | The `[LocDrift]` log (`log/logs.1781315803421.json`) confirmed `cap_src:"city-default" cap_m:45000` on a Putrajaya pin scoping results "≤45000m" → the drift. | #1021 | 13-06 '26 |
| D-62.69 | **First-load loading overlay made opaque to match `FunFactModal`.** Loading card `bg-tg-card/95` + card-level `backdrop-blur` + faint 60 % border → solid `bg-tg-card`, `border-2 border-tg-accent`, `ring-tg-accent/30`, no card blur (wrapper scrim still blurs the page). Applies to all loading reasons (initial / rotating-early / refresh). | Operator screenshots: the first load looked translucent (frosted) while the later "Did you know?" fact card was solid — inconsistent. The v0.62.66 "modernise" pass had frosted the overlay but not the fact card. | #1022 | 13-06 '26 |

---

## Completed in this arc (v0.62.53 → v0.62.69, PRs #1012 → #1022)

> Headline shifts only; per-PR detail lives in `doc/Journal/journal-0_62_53-13_06_26-0610.md` (amendments № 7xx) + the VibeCodingRecord rows #1012 → #1022.

- **Location-drift investigation closed** — instrument (`[LocDrift]` telemetry, D-62.53) → collect (Railway log pipeline, D-62.54/.55–57/.62/.64) → diagnose (operator-uploaded `log/logs.1781315803421.json` showed `cap_src:"city-default" cap_m:45000` on a Putrajaya pin) → fix (`TIGHT_15KM` tier, D-62.68). End state: Putrajaya/Cyberjaya cap at 15 km; the anchor itself never drifted (set-location moved 3 m, `kept:true`).
- **Railway log-collection pipeline** — hourly/15-min Action fetching logs into `log/`, merging across the latest deployments, committing to the unprotected `railway-logs` branch (D-62.54/.55–57/.62/.64). End state: logs retrievable off-device without touching protected `main`.
- **Loading-overlay arc** — 🛑 Stop pill (D-62.55–57) → standardised pill placement + modernised pop-up (D-62.66–67) → opaque first-load card matching the fun-fact modal (D-62.69). End state: consistent, solid busy indicator with a reachable Stop affordance on every wait.
- **Rating-filter rework** — "Refine Google Rating" 2×2 panel (D-62.58) → polish + session-scoped preference + amended reset copy (D-62.59–61) → boot-overlay rating-reset line (D-62.63). End state: rating control refined, session-scoped, surfaced on relaunch.

---

## Amendments

- This file is the first Register snapshot since `register-0_61_300-31_05_31-0915.md`. It **appends** D-62.53 → D-62.69 and Open items O-42 → O-44; it does **not** modify the prior ledgers. The v0.61.301 → v0.62.52 gap is logged as O-44, not silently backfilled (AU-1: add, never compress; the absence is recorded rather than papered over).

---

## Reference

- v0.61.147 full master: `doc/Register/register-0_61_147-25_05_26-1225.md`
- v0.61.300 incremental: `doc/Register/register-0_61_300-31_05_31-0915.md`
- Arc journal: `doc/Journal/journal-0_62_53-13_06_26-0610.md`
- Drift evidence log: `log/logs.1781315803421.json` (operator-uploaded via PR #1020; the `[LocDrift]` set-location / search-anchor lines for chat 313940231 / Putrajaya)
- Companion Technical snapshot: `doc/Technical/technical-0_62_69-13_06_26-1545.md`
- VibeCodingRecord (regenerated to #1022): `doc/VibeCodingRecord/vibe-coding-record.md` + hosted `public/doc/vibe-journal.html`
- Branch protection state: 4 required checks (Vitest, Syntax, TMA build, Empty-blob); `enforce_admins=true`; log collector uses the unprotected `railway-logs` branch (D-62.62)
