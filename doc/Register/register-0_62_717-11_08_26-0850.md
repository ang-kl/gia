(№ 193 - 11-08 '26 08:50 SGT)

# Register — soleat / Gia4lunch v0.62.717 (`/log` owner-gated)

> **Document type:** Register (per `doc/CLAUDE.md` §3; §4.1 — items move between sections, never deleted).
> **Continues** `register-0_62_716-10_08_26-1930.md` (№ 192). **Scope:** close the one un-gated route
> into verbose mode — the typed `/log` command — bringing enforcement in line with an intent the
> codebase already recorded in three places.
> **Reflects:** project state at **v0.62.717**. **Targeted addition.**

---

## Completed (new this entry)

| # | Item | Shipped | PR(s) |
| :--- | :--- | :--- | :--- |
| C-218 | **`/log` owner-gated.** Added the standard `isOwnerChat` guard (silent no-op + `[/log] denied` console line) to the typed `/log` handler. Its two sibling routes were already covered: `/log` is absent from `setMyCommands`, and the `v:logon` / `v:logoff` menu buttons sit behind the `v:` callback family's own gate. Only the typed command was open. | v0.62.717 | (this PR) |
| C-219 | **Verbose-mode entry points enumerated, not assumed.** `setVerboseMode(` has exactly two call sites (typed handler — now gated; `v:` callbacks — already gated) and `verbose.enable(` exactly one caller (`setVerboseMode` itself). No HTTP route can switch it on: `/api/vlog` only *receives* client telemetry. Coverage is complete. | v0.62.717 | (this PR) |

## Decisions (new this entry)

| # | Decision | Rationale |
| :--- | :--- | :--- |
| D-111 | **Read an instruction for the actionable change, not the literal one, when the literal one is a no-op — and say which you did.** The operator said *"Move `/log` to admin menu"*, but `/log` was **already on** the `/v` admin menu; the literal move was nothing. The actionable reading was *make it an admin command* — i.e. gate it like its menu neighbours. Three pre-existing signals confirmed owner-only was always the intent and only enforcement was missing: absent from `setMyCommands`, described as one of "the hidden owner commands" in the `/v` hub's own comment, and its menu buttons already gated. | v0.62.717 |
| D-112 | **Don't claim a mutation check where there is no logic to mutate.** This change is a guard clause whose behaviour is entirely determined by `isOwnerChat` — unchanged, and shared with eight other commands. The verification that actually mattered was *enumerating every other route into verbose mode* (C-219), not a red-green cycle. Writing "mutation-checked" in the Journal would have been theatre; the entry says so explicitly instead. | v0.62.717 |

## Open (carried forward, unchanged this entry)

| # | Item | Note |
| :--- | :--- | :--- |
| O-172 | Routes API's exact billing tier is a $0.005/element point estimate. Unchanged. |
| O-173 | `vibe.js`, `ver.js`, `vault.js` still have uninstrumented Places call sites. Unchanged. |
| O-175 | No live production `/cost` numbers confirmed. Unchanged — still the highest-value unblocked next action. |
| O-176 | No live confirmation the Places Details call count drops for cold-sanctuary venues. Unchanged. |
| O-177 | The Phase C cap is a brake, not a financial guarantee. Unchanged. |
| O-178 | The guard's accuracy is bounded by Phase A's coverage. Unchanged. |
| O-179 | No Phase C level transition exercised against real spend data. Unchanged. |
| O-180 | The 40/40/8 caps are reasoned, not measured. Unchanged. |
| O-181 | Caps are per-chatId; window is fixed rather than sliding. Unchanged. |
| O-182 | No Phase D path exercised against a live bot. Unchanged. |
| O-183 | Other bot commands remain uncapped (free government APIs — deliberate). Unchanged. |
| O-163 – O-167 | Michelin + Extract-Dishes + Gemini-model items. Unchanged. |
| O-145 – O-162, O-169 – O-171, O-88, O-99 – O-143 | All prior open items. Unchanged — this entry touches one guard clause. |

## New Open (this entry)

| # | Item | Note |
| :--- | :--- | :--- |
| O-184 | **Verbose sessions started by a non-owner before this deploy are not revoked.** Their `verbose:<chatId>` key survives its 24 h TTL, and they can no longer switch it off themselves (`/log off` is now silent for them). Self-healing within a day and scoped to their own chat, so no forced cleanup is proposed — but it is a real 24 h window. A one-off `SCAN verbose:* → DEL` would clear it immediately if wanted. |
| O-185 | **`isOwnerChat` returns `true` for everyone when `TELEGRAM_OWNER_CHAT_ID` is unset.** Pre-existing and deliberate for local dev, but it means that if the var is missing in Railway, *every* admin command — `/cost`, `/ver`, `/oversight`, `/ftlog`, `/training`, `/v`, `/hidden`, and now `/log` — is open to all users. **Not verifiable from this sandbox**, which has no access to the deployed environment's variables. Worth the operator confirming once directly. |
| O-186 | **Sandbox `node_modules` was wiped mid-session and the working tree reset to `main`**, silently discarding the local (already-pushed) commit `86ac0b9`. Recovered by re-fetching from origin. Not a repo defect, but a reminder that in this environment local state is not durable: **push early, and re-verify `git log` after any unexplained tooling failure** rather than assuming the checkout is where you left it. |

## Accepted (carried forward, unchanged this entry)

| # | Item | Ruling |
| :--- | :--- | :--- |
| O-126 | 15 IATA codes asserted, not verified. | Operator: *"i don't think need to trim to 21 cities."* All 33 Australia cities stay. Recorded in full in № 174. |

## Note

Small change, and the interesting part is not the guard clause. It is that the gap was found by
*answering a question* — the operator asked what the admin commands were, and assembling an honest
answer surfaced that one item on the admin menu was not actually an admin command. Enumerating a
surface to describe it is a decent way to audit it.

The second thing worth carrying forward is O-186. This session lost its working tree once and its
`node_modules` twice. Both were recoverable only because the work had already been pushed. The habit
that saved it — push as soon as a unit of work is coherent, rather than batching — is worth keeping.
