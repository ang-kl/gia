(№ 156 - 31-07 '26 18:53 SGT)

# Register — soleat / Gia4lunch v0.62.678 (typography/screen-size audit, 5 phases)

> **Document type:** Register (per `doc/CLAUDE.md` §3; §4.1 — items move between sections, never deleted).
> **Continues** `register-0_62_677-31_07_26-1459.md` (№ 155). **Scope:** the full typography/
> screen-size plan (`/root/.claude/plans/typed-launching-ripple.md`), Phases A/B/D/C(confirmed)/E.
> **Reflects:** project state at **v0.62.678**. **Targeted addition.**

---

## Completed (new this entry)

| # | Item | Shipped | PR(s) |
| :--- | :--- | :--- | :--- |
| C-84 | **Phase A** — removed the dead `text-[12px]` from Cuisine's 2 folio tabs (never actually rendered; `.folio-tab{font-size:11px}` always won the cascade) + documenting comment; added the missing iOS auto-zoom guard (`input,textarea,select{font-size:16px}`, mirroring clipboard's v0.62.442 fix) to Cuisine/Hawker/Transport. | v0.62.678 | pending |
| C-85 | **Phase B** — new `web/_shared/lib/type-tokens.js` (`typeScale`: caption/meta/body/body-lg/label/title, naming today's de facto sizes), spread into all 6 non-`about` apps' `tailwind.config.js`. Zero-byte vocabulary until consumed, same posture as M3 Tier 1. | v0.62.678 | pending |
| C-86 | **Phase D** — `classify-viewport.js` gained `COMPACT_MAX_WIDTH` (390px) + `isCompact` (short-edge, `mobile`-only), so layout code can branch on the iPhone-11-Pro-class phone tier the operator asked for. 5 new tests. | v0.62.678 | pending |
| C-87 | **Phase C (confirmed part)** — Cuisine's category-grid card label is now responsive: 11px on compact phones, 12px everywhere else (tablet/iPad-mini+), exactly the operator's stated rule. | v0.62.678 | pending |
| C-88 | **Phase E** — Hawker's centre card gained the same collapse/expand ("⌄ details"/"⌃ less") mechanism Cuisine and Train already have (per-card independent state, content-sized height in both states, 8 locales). Confirmed with the operator before building (Hawker toggle: yes; card height: stays content-sized, no fixed value reinstated). | v0.62.678 | pending |

## Open (new this entry)

| # | Item | Note |
| :--- | :--- | :--- |
| O-97 | **Phase C's header-title and primary-card-name-size reconciliation** (Cuisine 16px / Hawker 14-16px inconsistent across its 3 layouts / Transport 16px for titles; 12/13/14px drift for primary card-name labels) is NOT done this entry — needs a presented option-list decision round with the operator before any pixel changes, per the plan's explicit posture (mirrors the still-open O-88 round). |
| O-98 | **Phase D's targeted wrap-risk pass** (chip rows, footer button clusters, truncated station/venue names at ~375pt width) was not done this entry — only the `isCompact` breakpoint tier itself shipped. Starting point: the already-flagged v0.61.281 5-chip-row risk. |

## Open (carried forward, unchanged this entry)

| # | Item | Note |
| :--- | :--- | :--- |
| O-88 | M3 audit Tier 2 per-item decision round — still awaits presentation to the operator. Unchanged. |
| O-89 | Non-adoption recommendation (M3 literal type scale + literal elevation shadows) — still awaits sign-off. Unchanged. Note: this entry's Phase B/C work is explicitly SEPARATE from and narrower than this item (see Journal). |
| O-91 | Year-filter chips + client-side year/category sort (Michelin) — FILTER half answered by O-94/C-82; SORT half stays open. Unchanged. |
| O-92 | Back/End button cursor overlap with the Michelin pager. Unchanged. |
| O-93 | No React component render-test infrastructure — this entry's UI changes are verified by build/compiled-CSS inspection only, same standing gap. |
| O-95 | Hawker's "Copy" button still closes the WebApp on success, unlike Cuisine's. Unchanged. |
| O-96 | Glass-effect parity investigation (Hawker/Train) — root cause not found in-sandbox, operator's answer on whether Hawker shows the same symptom still awaited. Unchanged. |

## Decisions (new this entry)

| # | Decision | Rationale | Where recorded |
| :--- | :--- | :--- | :--- |
| D-51 | Cuisine's folio-tab labels stay 11px (the cascade-collision size, not the JSX's unused 12px). | Operator: "I like Cuisine's tab labels at 11px. I thought is just nice." | Journal `journal-0_62_678-31_07_26-1853.md` |
| D-52 | Cuisine's category-grid card labels: 11px on compact phones, 12px minimum on tablet/iPad-mini+. | Operator: "I feel the Cuisine groups should be 11px especially for iPhone 11 Pro... For Tablet phone or iPad Mini or bigger, 12px is minimum." | same |
| D-53 | Compact-phone viewport tier added at ≤390px width, scoped to the `mobile` deviceClass only. | Operator confirmed via plan-mode AskUserQuestion. | same |
| D-54 | Hawker's centre card gets the same collapse/expand toggle as Cuisine/Train; card height stays content-sized everywhere (no fixed height reinstated). | Operator confirmed via AskUserQuestion (both answers: recommended default). | same |

## Note

This entry closes out the plan at `/root/.claude/plans/typed-launching-ripple.md` for its
CONFIRMED scope (Phases A, B, D, C's category-grid part, E). O-97 and O-98 carry forward the
plan's explicitly deferred parts (header/card-name-size decision round; wrap-risk audit pass) —
neither was silently dropped, both are tracked for a future pass.
