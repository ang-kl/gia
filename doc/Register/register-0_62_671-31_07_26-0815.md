(№ 149 - 31-07 '26 08:15 SGT)

# Register — soleat / Gia4lunch v0.62.671 (M3 audit, Tier 0)

> **Document type:** Register (per `doc/CLAUDE.md` §3; §4.1 — items move between sections, never deleted).
> **Continues** `register-0_62_670-31_07_26-0620.md` (№ 148). **Scope:** M3 structural-conformance audit,
> Tier 0 only. **Reflects:** project state at **v0.62.671**. **Targeted addition.**

---

## Completed (new this entry)

| # | Item | Shipped | PR(s) |
| :--- | :--- | :--- | :--- |
| C-76 | **M3 audit Tier 0** — deleted 4 confirmed-dead `BackFab.jsx` files (verified via import-statement grep, then re-verified against a corrected operator premise about scroll-hide behaviour); reconciled BottomSheet's 280ms/340ms duration drift; fixed cuisine's Widen-switch 1.6px travel slip; fixed MapControls' dropdown-row vs wrapper radius contradiction. **The latter three shipped one commit later than intended** (`883fda8`, not `1ad8aa4`) after a git-add pathspec bug silently dropped them — see Journal amendment. | v0.62.671 | pending |

## Open (new this entry)

| # | Item | Note |
| :--- | :--- | :--- |
| O-88 | **M3 audit Tiers 1–2 await a decision round.** Tier 1 (define a shared shape/elevation/motion/spacing vocabulary — zero visible change by construction) and Tier 2 (apply it: consolidate ~11 chip padding combos to 2-3 named sizes; pick one button radius for CTA + one for secondary; standardize circular FABs to one diameter, recommend 40px; upsize both switches to real M3 track/thumb proportions 52×32/24px; fix clipboard's missing-shadow sheets + 18px-vs-16px sheet-corner mismatch without importing M3's literal shadows over cuisine's glass / hawker-transport-menu's skeuomorphic surfaces; add a subtle press state-layer; unify text-input radius) — presented as a pre-implementation report, same shape as Phase 2b's O-85, awaiting operator answers per item. |
| O-89 | **Explicit non-adoption recommendation, itself needs sign-off**: M3's literal type scale (too large for this dense mobile chrome) and M3's literal elevation shadow values (would overwrite documented deliberate design in cuisine's glass system and hawker/transport/menu's skeuomorphic system) — proposed as permanent exclusions from any M3 pass, not yet operator-confirmed as standing. |
| O-90 | **No tooling catches "the commit doesn't contain what its own message says it contains."** Found the hard way (see Journal amendment 31-07 '26 08:19 SGT): a multi-path `git add -A` silently dropped 3 of 4 stated changes when one path in the list matched nothing. Caught only by the repo's `stop-hook-git-check.sh` flagging uncommitted changes, not by any build/test step — same failure SHAPE as D-22–D-28 (something matches nothing, fails silently, degrades to indistinguishable-from-normal), different layer (git plumbing, not Tailwind/DOM). No fix proposed yet; recorded as a gap, not an action item. |

## Decisions (new this entry)

| # | Decision | Rationale | Where recorded |
| :--- | :--- | :--- | :--- |
| D-46 | **M3 alignment proceeds in two authorization tiers**: Tier 0 (bugs/dead-code, ship on audit alone) may proceed without a per-item round; anything touching a rendered control's size/shape/colour/motion (Tiers 1-2) requires explicit per-item operator approval before any code change. | Operator, first "apply them immediately after audited," then corrected to "Only applied if I approve" — this narrows the earlier authorization specifically for visible/structural UI changes, distinct from the invisible-tier precedent set in Phase 1. | Journal `journal-0_62_671-31_07_26-0815.md` |
| D-47 | **`BackFab.jsx` deletion confirmed** after the operator's memory of a "listing-mode, hide-while-scrolling" behaviour was checked against the actual v0.62.213 retirement history and found not to match either the current footer (always-on, no scroll-hide) or the old deleted files themselves (no scroll-hide logic ever existed in them). | Operator, after review: "okay, tier 0. proceed as planned." | Journal `journal-0_62_671-31_07_26-0815.md` |

## Note

Tiers 1–2 of the M3 audit (O-88) are drafted but unshipped; the next entry in this arc will either present
them as a formal decision round or record the operator's direct answers, following the same protocol as
the Phase 2b (O-85) colour-decision round.
