(№ 155 - 31-07 '26 14:59 SGT)

# Register — soleat / Gia4lunch v0.62.677 (Hawker "Copy" relabel)

> **Document type:** Register (per `doc/CLAUDE.md` §3; §4.1 — items move between sections, never deleted).
> **Continues** `register-0_62_676-31_07_26-1215.md` (№ 154). **Scope:** Hawker button wording fix.
> **Reflects:** project state at **v0.62.677**. **Targeted addition.**

---

## Completed (new this entry)

| # | Item | Shipped | PR(s) |
| :--- | :--- | :--- | :--- |
| C-83 | **Hawker "Save to chat" → "📋 Copy"** — `btn.saveToChat`'s value (all 8 locales) now matches Cuisine's `btn.copyOne` wording exactly. Label-only; underlying save-pick mechanism and app-close-on-success behaviour unchanged. | v0.62.677 | pending |

## Open (new this entry)

| # | Item | Note |
| :--- | :--- | :--- |
| O-95 | **Hawker's relabelled "Copy" button still closes the WebApp on success**, unlike Cuisine's "Copy" which stays open for multiple copies. Not asked for this pass (operator asked for wording only) — flagged as a possible follow-up, not actioned. |
| O-96 | **Glass-effect parity investigation, Hawker + Train**: both apps already implement the identical `.liquid-glass`/`bg-tg-card/60` mechanism as Cuisine (verified in source + built CSS), but a device screenshot of Train's carousel showed the "peeking" edge cards rendering fully opaque, not glass. Root cause NOT found in-sandbox (live repro needs the real backend + Google Maps API data). Operator asked whether Hawker shows the same symptom, to localise the bug — answer awaited. |

## Note

This entry ships a small, low-risk label fix (Tier 0) alongside flagging — but not yet resolving —
two harder items (O-95, O-96) surfaced by the same operator message. Per this session's standing
rule, the harder items await either operator confirmation (O-96) or a future explicit ask (O-95)
before any code changes.
