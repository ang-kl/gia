(№ 154 - 31-07 '26 12:15 SGT)

# Register — soleat / Gia4lunch v0.62.676 (Michelin Year + Bib Gourmand ticks)

> **Document type:** Register (per `doc/CLAUDE.md` §3; §4.1 — items move between sections, never deleted).
> **Continues** `register-0_62_675-31_07_26-1152.md` (№ 153). **Scope:** O-94 resolution.
> **Reflects:** project state at **v0.62.676**. **Targeted addition.**

---

## Completed (new this entry)

| # | Item | Shipped | PR(s) |
| :--- | :--- | :--- | :--- |
| C-82 | **Michelin Year + Bib Gourmand ticks** — 3 independent tick boxes (2026 / 2025 / Bib Gourmand, all default ON) rendered under the Michelin chip in `CuisineDrawer.jsx`; server-side filter applied once to `allEntries` in `handleMichelinSearch` (union semantics — Bib Gourmand is its own bucket, years gate star entries); `computeCriteriaHash` extended with `michYears` so a tick change resets the walk. Closes O-94. | v0.62.676 | pending |

## Open (resolved this entry)

| # | Item | Resolution |
| :--- | :--- | :--- |
| O-94 | Michelin year sub-criterion, placement/defaults — raised in `register-0_62_675-31_07_26-1152.md`. | **Closed.** Operator answered both design questions directly (chip structure: one Michelin chip + 3 ticks below it, not a chip split; walk-reset: yes). Built exactly as answered — see C-82. |

## Open (carried forward, unchanged this entry)

| # | Item | Note |
| :--- | :--- | :--- |
| O-88 | M3 audit Tier 2 per-item decision round — still awaits presentation to the operator. Unchanged. |
| O-89 | Non-adoption recommendation (M3 literal type scale + literal elevation shadows) — still awaits sign-off. Unchanged. |
| O-91 | Year-filter chips + client-side year/category sort (Michelin spec §1-§2) — the FILTER half is now materially answered by O-94/C-82; the SORT half (§2) was never re-raised and stays out of scope. Left open, narrowed. |
| O-92 | Back/End button (footer right) can also step the Michelin pager's cursor backward, overlapping with the centre pager's `‹`. Deliberately left untouched. Unchanged. |
| O-93 | No React component render-test infrastructure exists anywhere in this repo — this entry's tick-row UI is unverified beyond a successful build, same standing gap. Unchanged. |

## Decisions (new this entry)

| # | Decision | Rationale | Where recorded |
| :--- | :--- | :--- | :--- |
| D-49 | Michelin stays ONE cuisine chip; Year (2026/2025) + Bib Gourmand become 3 independent ticks revealed below it once selected — NOT a chip split into "Michelin Star" / "Bib Gourmand". Union semantics: Bib Gourmand is its own bucket, not cross-filtered by year. | Operator, resolving the placement/structure question directly: "One 'Michelin' chip, 3 ticks below it." | Journal `journal-0_62_676-31_07_26-1215.md` |
| D-50 | Changing any Michelin tick resets the walk-through pagination (via `computeCriteriaHash`'s new `michYears` field), same as any other search criterion. | Operator: "Yes, reset (Recommended)." | Journal `journal-0_62_676-31_07_26-1215.md` |

## Note

This entry lands on the SAME still-open, still-draft PR #1678 as v0.62.674/675 — the session's single
designated working branch had not been merged yet when this feature was ready, so per this session's
branch-restart convention (rebuild the branch fresh from `origin/main` only AFTER a merge, never before)
it stacks as a third round of commits rather than opening a second PR off the identical unmerged branch.
