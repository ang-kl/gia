(№ 153 - 31-07 '26 11:52 SGT)

# Register — soleat / Gia4lunch v0.62.675 (Michelin pager divisor correction)

> **Document type:** Register (per `doc/CLAUDE.md` §3; §4.1 — items move between sections, never deleted).
> **Continues** `register-0_62_674-31_07_26-1137.md` (№ 152). **Scope:** pager divisor correction +
> a new Open item for the Michelin year sub-criterion.
> **Reflects:** project state at **v0.62.675**. **Targeted addition.**

---

## Completed (new this entry)

| # | Item | Shipped | PR(s) |
| :--- | :--- | :--- | :--- |
| C-81 | **Michelin pager divisor corrected 12 → 9** — `michelinTotalPages` now divides `michelinRemaining.total` by 9 (display-only; the actual server fetch batch stays 12, unchanged). Operator's own worked example ("140 / 9 = 15 pages") is the source of the 9. | v0.62.675 | pending (same PR as v0.62.674, #1678) |

## Open (new this entry)

| # | Item | Note |
| :--- | :--- | :--- |
| O-94 | **Michelin year sub-criterion** (2026 / 2025 / Both, default Both) — requested by the operator alongside the divisor correction, but not built this entry: real new scope (new UI control + request param + server-side `awardYears` filtering + `walkHash` change), and its UI placement is a genuine design decision, not an obvious slot. Awaiting operator input on placement/defaults before implementation — see Journal `journal-0_62_675-31_07_26-1152.md`. |

## Open (carried forward, unchanged this entry)

| # | Item | Note |
| :--- | :--- | :--- |
| O-88 | M3 audit Tier 2 per-item decision round — still awaits presentation to the operator. Unchanged. |
| O-89 | Non-adoption recommendation (M3 literal type scale + literal elevation shadows) — still awaits sign-off. Unchanged. |
| O-91 | Year-filter chips + client-side year/category sort (Michelin spec §1-§2) — superseded in spirit by O-94 (the operator has now asked for the year filter directly); kept open pending O-94's resolution rather than closed outright, since O-91 also covered sort, which O-94 does not. |
| O-92 | Back/End button (footer right) can also step the Michelin pager's cursor backward, overlapping with the new centre pager's `‹`. Deliberately left untouched. Unchanged. |
| O-93 | No React component render-test infrastructure exists anywhere in this repo. Unchanged. |

## Note

This entry and v0.62.674's both landed on the same still-open, still-draft PR #1678 — the operator's
correction arrived before that PR had even reached CI. No new Decision recorded: the divisor change is
a numeric correction, not a design-rule change; O-94 is raised as an Open item precisely because it DOES
need a decision that hasn't been made yet.
