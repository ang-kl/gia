(№ 133 - 17-07 '26 09:36 SGT)

# Register — soleat / Gia4lunch v0.62.560 (Hawker responsive tablet/desktop arc + Bib Gourmand + embedded-map interaction)

> **Document type:** Register (per `doc/CLAUDE.md` §3; §4.1 — items move between sections, never deleted).
> **Continues** `register-0_62_543-17_07_26-0150.md` (№ 132). **Scope:** the v0.62.544 → v0.62.560 Hawker
> responsive tablet/desktop arc (PRs #1553–#1569) — the three layout modes, the Cuisine-style cards, Bib
> Gourmand on Hawker, and the card↔map interaction model.
> **Reflects:** project state at **v0.62.560, 17-07 '26 (HEAD `7a87072`)**.
> **Targeted addition** — does not restate the live O-/D-/DF- items from № 130–№ 132, which remain as recorded
> (incl. O-52/O-53 pin-precedence/imperial-set and DF-6 ResultDrawer left-peek — still live).

---

## Completed (new this entry)

| # | Item | Shipped | PR(s) |
| :--- | :--- | :--- | :--- |
| C-35 | **Responsive Hawker TMA — three layout modes.** Landscape (tablet/desktop) = full-bleed map + bottom carousel (3/2/1 cards in focus by width, glass peek); portrait tablet = anchored map + separate scrollable list panel with ⇲ expand→carousel; phone = stacked (untouched). Shared `useViewport()` + fullscreen re-read; footer device/orientation cue; map⇄cards toggle folded into FooterNav. | v0.62.544–552, 554 | #1553–#1555, #1559, #1561, #1563 |
| C-36 | **iPad fullscreen for touch tablets** (reverses v0.62.286 no-auto-fullscreen for tablets only; `platform === 'ipados'` gate). | v0.62.545 | #1554 |
| C-37 | **Cuisine-style centre cards** (name header, address, stall/status chip, codeHex MRT chips + station, bus pills w/ description, pill actions), one renderer across all modes. | v0.62.547–548 | #1556, #1557 |
| C-38 | **Michelin Bib Gourmand on Hawker** — macaron-red pin `#C6282D` + ✳️ marker (CVD-safe) + `✳️ Bib Gourmand · <stall>` row on card/InfoWindow/chat, stall names as Google-Maps links. Join reuses `SG-michelin.js BIB_GOURMAND[]` (number-stripped token match + digit guard; 19/122). | v0.62.553, 557–558, 560 | #1562, #1566–#1567, #1569 |
| C-39 | **Card ↔ pin embedded-map interaction** — tap card → highlight pin (3 s pulse) + active ring; tap pin → scroll matching card into focus + ring; station/bus taps (card **and** InfoWindow) drop the marker on THIS map (bus = live-arrivals); 2nd tap → external Maps. | v0.62.549, 551, 556–559 | #1558, #1560, #1565–#1568 |
| C-40 | **Map pop-up polish** — strip Google InfoWindow chrome (single clean border); widen + tiny-pad the card; `ensureInfoVisible` pans clear of the top bar; restore the lost quick-toggle row + greyed unwired expand; kill the carousel horizontal band; drop the double 🍳; **no zoom-adjust on card close** (all embedded-map TMAs). | v0.62.554–555, 558, 560 | #1563–#1564, #1567, #1569 |

---

## Open — awaiting operator go (new this entry)

| # | Item | Detail | Default in place |
| :--- | :--- | :--- | :--- |
| O-54 | **Port the responsive pattern to the next TMA** | Plan agreed (operator: "just plan, don't build yet"): **Cuisine next** (highest reuse — already has `MapPanel` + `ResultCarousel`), then Transport. Steps: promote primitives to `_shared` (layout dispatch, result carousel, InfoWindow chrome CSS, tablet-fullscreen gate); wire `useViewport` + fullscreen into Cuisine; landscape full-bleed + bottom carousel / portrait anchored panel; map-interaction parity; toggle into `FooterNav`. Two sub-decisions pending: extract-shared-first vs copy-then-refactor; Cuisine-first vs Transport-first. | Hawker only, live. Build on "go". |

---

## Deferred (new this entry)

| # | Item | Reason | Reactivate when |
| :--- | :--- | :--- | :--- |
| DF-7 | **Hawker stall-count coverage (37/122)** | `🍳 N stalls · <status>` shows only for the ~37 centres present in the data.gov.sg "Hawker Centres (GEOJSON)" join (`data/hawker-stalls.json`); Chinatown Complex, Hong Lim etc. are absent so their cards omit the row. Widening via looser name-matching risks a **wrong stall count** (worse than a missing one), so held. | Operator wants wider coverage AND accepts the wrong-count risk (or a better-keyed dataset lands). |
| DF-8 | **Hawker CLEANING-closure feature (K.I.V.)** | data.gov.sg dataset `d_bda4baa634dd1cc7a6c7cad5f19e2d68` (cleaning start/end per centre) → grey pin + closed tab/strip. Shelved by operator ("Put on K.I.V."); also blocked — the sandbox egress-blocks data.gov.sg (403) and the retired parser is below the shallow-clone graft, so the dataset column names need an operator-pasted sample. | Operator un-K.I.V.s AND pastes one sample record (column names). |
| DF-9 | **Q4 — carpark tap "closes the card"** | Operator saw the centre card close when tapping a carpark (layer on). Per the code a carpark pin opens the controller's OWN popup (separate InfoWindow from the centre card); no force-close found. | Operator provides a repro (card open → carpark on → tap carpark). |

---

## Decisions (append-only)

| # | Decision | Rationale | Date |
|---|---|---|---|
| D-135 | **Responsive layout = dispatch by device × orientation with ONE shared card renderer**, gated on `useViewport` (`isWide` + orientation); phones stay on the existing stacked layout via `display:contents`. Fullscreen is requested for **touch tablets only** (`ipados`/`ios`/`android`). | Only iPad-in-a-modal broke; keeping the phone path provably untouched + one card renderer avoids drift across four surfaces. Verified live per PR (no in-sandbox TMA driving). | 17-07 '26 (#1553–#1564) |
| D-136 | **Closing an embedded-map info card does NOT adjust the zoom**, across all embedded-map TMAs (Hawker, Cuisine, Transport). Reverses v0.62.115 (return-to-prior-zoom). Tap-to-zoom-IN (17) is kept. | Operator: closing should leave the map where the user left it. Baked into all embedded-map TMAs per the operator's "all TMA" instruction. | 17-07 '26 (#1569) |
| D-137 | **Bib→centre join is deterministic (number-stripped token key + digit guard), NOT fuzzy `findByName`.** | `findByName` edit-distance produced false positives (Jurong West 505 → Jurong West Hawker Centre; both Redhill → one Blk 79). A wrong tag on a centre is worse than a missing one. Residual: "Redhill Market"/"Redhill Food Centre" (no block number in the bib data) tag both Blk 79 + 85 — an accepted mild over-tag. | 17-07 '26 (#1562) |

---

## Amendments

- **AU-1:** Continues `register-0_62_543-17_07_26-0150.md` (№ 132). Appends Completed C-35…C-40, Open O-54,
  Deferred DF-7…DF-9, Decisions D-135…D-137. No prior items amended, closed, or removed — O-52/O-53, DF-6,
  D-133/D-134 and all earlier lines remain live as recorded.

## Reference
- Prior: `doc/Register/register-0_62_543-17_07_26-0150.md` (№ 132)
- Journals: `journal-0_62_544-…` … `journal-0_62_560-…` (#901–#917)
- Feature/Technical: `doc/Feature/feature-0_62_560-17_07_26-0936.md`,
  `doc/Technical/technical-0_62_560-17_07_26-0936.md` (this pass)
