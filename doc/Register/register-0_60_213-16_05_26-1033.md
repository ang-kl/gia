(№ 113 - 16-05 '26 10:33 SGT)

# Register — soleat / Gia4lunch v0.60.213 (consolidated master)

> **Document type:** Register (per `doc/CLAUDE.md` §3 — *open items, known issues, deferred, decisions, completed*; §4.1 — items move between sections, never deleted).
> **Status:** Consolidated master. Supersedes the reproduce-by-reference chain `register-0_60_157 → register-0_60_172` by *consolidation* (operator instruction № 1,001) — every prior versioned Register file remains on disk; this file is the complete current-state ledger, readable cold. It closes the long-standing Register catch-up debt (the Register had lagged at v0.60.172 while the Journal ran to v0.60.213).
> **Reflects:** project state at **v0.60.213, 16 May 2026 10:33 SGT**.

---

## Open Items

> Genuinely open at v0.60.213. The `DF-NN` deferred backlog was deliberately emptied across v0.60.210 → v0.60.212; what remains are operator-side / cost-watch items, not code defects.

| # | Item | Owner | Priority | Notes |
| :--- | :--- | :--- | :--- | :--- |
| O-1 | Michelin top-12-fresh Places-cost watch (DF-94) | operator | low | Monitor Places billing now that the Michelin 24 h result-cache is dropped; re-introduce caching only if cost warrants. |
| O-2 | `doc/VibeCodingRecord/` PR-ledger refresh (DF-62) | operator | low | `data/prs.ndjson` + `data/pr-files.tsv` need PRs appended and the generator re-run; cosmetic, lags the Journal. |
| O-3 | Security hardening — remaining items | operator | medium | Auth chokepoint on `/api/menu|hawker|transport|oversight/*` (DF-53); per-chatId sliding-window limits on all hot endpoints (DF-54); API-key budget caps in the Google/Gemini consoles (DF-55, operator-side). |
| O-4 | TMA component-test infrastructure | operator | low | No React component-level test harness yet; several integration tests (DF-49, DF-51, DF-59) await it. |

---

## Deferred

> Items intentionally not done, each with a reactivation condition. Carried forward from `register-0_60_172` + the v0.60.173 → v0.60.213 arc.

| # | Item | Reason | Reactivate when |
| :--- | :--- | :--- | :--- |
| DF-47 | Unit tests for the `insideSG` bbox helper + `/\bjohor\b/i` post-filter | Behavioural integration coverage judged sufficient | a JB geo-fence regression appears |
| DF-49 / DF-51 / DF-59 | Fixture / component / integration tests for the petFriendly filter + LocationField `onSelect` contract | No React component-test harness for the Cuisine TMA | O-4 (test infra) lands |
| DF-50 | Conditional `places.allowsDogs` field-mask request (only when the filter is on) | Marginal cost optimisation | Places billing review |
| DF-52 | Lint / checklist for new filter keys missing `filterCount` / `stateSig` / criteria-preview wiring | A class of bug, not an active one | a new filter key is added |
| DF-53 / DF-54 / DF-55 / DF-56 | Auth chokepoint for the other `/api/*` namespaces; sliding-window rate limits; console budget caps; inline-`verifyInitData` cleanup | Security priorities #2–#4; chokepoint already defence-in-depth covered | the next security-hardening pass |
| DF-57 | Broad FR audit across `i18n` + inline `lang === 'fr'` branches | Operator declined the full audit | operator requests a localisation sweep |
| DF-74 / DF-75 / DF-88 | Places `priceRange` / `addressComponents` cost monitor; Frankfurter FX robustness; sparse `priceRange` coverage | External-data sparsity + marginal cost | coverage or cost becomes material |
| DF-94 | Michelin top-12-fresh Places-cost watch | See O-1 | billing review |
| DF-97 / DF-98 / DF-99 | `michelin:enrich` stale-cache rotation; LLM dish-name semantic allow-list; cold-cache first-search latency | Acceptable at current scale | a user-visible symptom appears |

---

## Decisions

> Append-only. The most recent project decisions; earlier `D-NNN` decisions live in the prior versioned Register files.

| # | Decision | Rationale | Date |
|---|---|---|---|
| D-167 | `/api/cuisine/*` gets a single chokepoint auth middleware; per-handler inline verify becomes defence-in-depth. | Single source of truth; new routes inherit the gate. | 14-05 '26 |
| D-170 | LocationField `onSelect` is commit-only (updates both `locationAnchor` and `searchCenter`, no `runSearch`). | Separates "I picked a place" from "I want to search". | 14-05 '26 |
| D-171 | `/legal` + `/privacy` bodies are operator-curated i18n copy (EN supplied, FR translated paragraph-for-paragraph). | Content control + localisation unified. | 14–15-05 '26 |
| D-173 | The Michelin search mode is top-N freshness, not walk-through. *(Later refined — see D-185.)* | Rate-limit-layer rethink. | 14-05 '26 |
| D-182 | The 🐾 Pet filter is promoted to the primary filter row; Price moves to the Filters popover. | Surface the most-used filter. | 12-05 '26 |
| D-183 | Venue-card price uses a country-currency prefix (S$ / M$ / US$), never a 💲 emoji; cross-border amounts show an FX conversion in parentheses. New payload fields must reach every render path (`formatVenueBlock` + TMA card + Copy-All). | Clarity over decoration; close the "payload ≠ render" gap. | 12-05 '26 |
| D-185 | Michelin walk-through uses operator-approved heuristics — criteria-hash-scoped seen-set, 1 h idle TTL, top-12 per tap after reset. | Variety across taps without re-paying the full Places bill. | 13-05 '26 |
| D-192 | Copy-All uses the T1 "detail-with-sanctuary" template when a Sanctuary block is available. | Richer copy output. | 13-05 '26 |
| D-201 | Curated-dataset dedup keys derive from stable fields (`name`\|`address`), never schema-optional fields like `slug`. | A missing optional field silently broke the Michelin walk. | 13-05 '26 |
| D-202 | The location-saved "ready for X" command list is curated — phasing focus toward `/cuisine` and `/s`. | Deliberate surface curation. | 13-05 '26 |
| D-203 | Product copy uses the literal noun form ("2 hours") and hyphenates only the adjective form ("2-hour forecast"). | Copy precision. | 13-05 '26 |
| D-207 | Google-Maps InfoWindow content pins an explicit dark text colour so it stays legible under Telegram's dark theme. | The white InfoWindow bubble cascaded a light body colour. | 13-05 '26 |
| D-208 | Cooking-method cards show a country flag + a Gemini explainer + a "verify" caveat; the "Try" line carries a real dish only. | A method is not a dish. | 14-05 '26 |
| D-209 | The "Try" recommendation line carries only a genuine dish/dessert name (shared `dish-name.js` guard); omitted when none is known. The Sanctuary read is two headerless `🌿` fields (Quiet / Seating). | One criteria, applied everywhere. | 15-05 '26 |
| D-211 | Single-query technique/ingredient search headers follow the v0.60.208 fan-out style — a flag glyph + a verify caveat, not a "Searching…" status line. | Consistency across render paths. | 16-05 '26 |
| D-212 | DF-backlog closure pass — DF-84 dropped, DF-100 kept (not a bug), DF-101 left as-is, DF-103/106 closed as external/infra. | Empty the backlog with an explicit decision per item. | 16-05 '26 |
| D-213 | Every TMA footer carries an "Experimental · Singapore · v…" tag line; the Cuisine TMA additionally leads with a how-to line. | Consistent, honest framing across the app family. | 16-05 '26 |
| D-1001 | Documentation is consolidated into standalone master versions per domain (this Register and its five siblings), overriding the thin reproduce-by-reference cadence for this pass. Prior versioned files remain on disk so the authenticity trace is preserved. | Operator instruction № 1,001 — a coherent suite readable by external stakeholders. | 16-05 '26 |

---

## Completed

> Shipped items, by arc. The exhaustive per-PR record is in `doc/Journal/` (181 entries); PR numbers are in the Journal and `doc/VibeCodingRecord/`.

| # | Arc | Outcome | Versions |
|---|---|---|---|
| C-A | `[object Object]` root-cause kill | Places-v1 `review.text.text` unwrap helper across 5 sites | v0.60.154–.156 |
| C-B | Cuisine TMA navigation + zero-results UX | Client-side page history (`⇠ Back`/`⇢ Next`); silent auto-retry + reset CTA | v0.60.154 / .157 |
| C-C | JB region maturation | Whole-Johor geo-fence; SG/JB radius rebalance (20/30 km) | v0.60.164–.166 |
| C-D | 🐾 Pet-allowed filter | `allowsDogs` field-mask + filter chip + full state wiring | v0.60.165–.166 |
| C-E | Security hardening #1 | `initData` HMAC auth chokepoint on `/api/cuisine/*` + 17 tests | v0.60.167 |
| C-F | `/legal` + `/privacy` rewrites | Operator-curated copy, EN + FR | v0.60.169–.172 |
| C-G | Rate-limit layer | `makeRateLimiter`; per-chatId 15-min fixed window; caps tuned | v0.60.170–.176 |
| C-H | Vibe Journal framework | Tabbed UI + dashboard + PR ledger, hosted on soleat.net; contract → v0.0.4 | v0.60.177–.193 |
| C-I | `/s` assistance sub-menu | Cooking-methods / authentic-dishes / others; cuisine + dish-type drill-down | v0.60.173 / .181 / .189 / .193 |
| C-J | Venue-card render parity | Cuisine-type label, country-currency price + FX, 🐾 line, emoji map pins, ♿️ marker | v0.60.183–.192 / .201 |
| C-K | Michelin walk-through | Drop-caches → restore → `entryKey` fix; criteria-scoped seen-set, 1 h TTL | v0.60.195–.201 |
| C-L | `sanctuaryRead` everywhere (DF-87) | Sanctuary block populated on every search-response venue | v0.60.197 |
| C-M | Performance | TMA load-speed audit → immutable static cache headers | v0.60.203–.204 |
| C-N | CI scope fix | Syntax-check narrowed 2028 → 115 files; build split; diagnostics | v0.60.205 |
| C-O | Cooking-method card rework | Flag title + Gemini explainer + real-dish "Try" line | v0.60.208 |
| C-P | Dish-name guard | Shared `dish-name.js`; 2-field Sanctuary read | v0.60.209 |
| C-Q | DF-109 / DF-110 / DF-111 | Train-map FR localisation; single-query header glyph; `/hidden` Try-line guard | v0.60.210–.211 |
| C-R | DF-backlog closure | DF-84/100/101/103/106 closed by decision (D-212) | v0.60.212 |
| C-S | TMA footers | Standardised "Experimental · Singapore · v…" + Cuisine how-to line | v0.60.213 |
| C-T | GitHub Actions CI outage | Diagnosed as a billing block (jobs dead in 2–3 s, no execution); resolved operator-side | v0.60.205–.212 |

**Resolved deferred items** (closed across the arc): DF-80, DF-81, DF-82, DF-83, DF-84, DF-87, DF-90, DF-91, DF-95, DF-96, DF-100, DF-101, DF-103, DF-105, DF-106, DF-108, DF-109, DF-110, DF-111, DF-CI, DF-60, DF-29.

---

## Removed Features

> AU-EOF: this table sits at end of file. See `doc/Feature/feature-0_60_213-…md` §Removed Features for the canonical Removed table (RF-1 the `/s` "Cooking Methods" sub-menu entry; RF-2 the single-step `⇠ Prev` FAB). No Register-tracked item has been deleted — items only move between sections.

| # | Item | Original Version | Removed in Version | Removal Date (DD-MM 'YY) | Reason | Replacement | Rule Reference | Serial № |
|---|------|------------------|--------------------|--------------------------|--------|-------------|----------------|----------|
| — | (see `doc/Feature/feature-0_60_213-…md`) | — | — | — | — | — | AU-2 | (№ 113 — 16-05 '26 10:33 SGT) |

---

## Rule reference

- **AU-1:** Register items move between sections, never deleted; prior versioned Register files remain on disk as the authenticity trace.
- **AU-2:** removals move to the Removed table at EOF.
- **AU-EOF:** the Removed table sits last.

## Amendments

(None — this is a consolidated master; the per-PR amendment trace lives in `doc/Journal/`.)
