(№ 136 - 29-07 '26 20:00 SGT)

# Register — soleat / Gia4lunch v0.62.657 (O-56 closed by device check — which found real bugs)

> **Document type:** Register (per `doc/CLAUDE.md` §3; §4.1 — items move between sections, never deleted).
> **Continues** `register-0_62_655-29_07_26-1812.md` (№ 135). **Scope:** PR #1667 — the Cuisine drawer
> device-check fix. **Reflects:** project state at **v0.62.657**.
> **Targeted addition** — restates only the two items this entry moves; O-57–O-60, the Deferred items and
> Decisions from № 135 remain as recorded there.

---

## Completed (new this entry)

| # | Item | Shipped | PR(s) |
| :--- | :--- | :--- | :--- |
| C-53 | **Cuisine drawer no longer drags the whole Mini App.** `web/cuisine/src/api/tg.js` gained the `disableVerticalSwipes()` init step Hawker/Transport already had — Telegram's own vertical swipe-to-minimise was competing with the drawer's drag handle for the same gesture and, being native, always won. | v0.62.657 | #1667 |
| C-54 | **Cuisine drawer now actually collapses to a card height.** The peek-measurement selector matched nothing (`[data-venue-card]`, `article`, `.skeuo-card` — none exist in `ResultCard.jsx`), so the collapsed height was permanently the 0.80-fraction fallback, not "1 card". Fixed to `[data-pid]` (the selector `ResultPanel.jsx` already relies on) and the multiplier dropped 1.5 → 1 per the operator's revised ask. | v0.62.657 | #1667 |

## Open — items moved from № 135

| # | Item | Disposition |
| :--- | :--- | :--- |
| O-56 | ~~Cuisine drawer UNVERIFIED on a real client~~ | **CLOSED → C-53/C-54 above.** The device check happened and found two real, independent bugs — confirming the caution recorded at v0.62.655 was warranted, not excess process. See caveat below: the check was targeted at the two mechanisms, not a full end-to-end render, so a **third** defect surfacing would warrant reopening rather than a fresh number. |
| O-61 | ~~Cuisine card selector for the peek measurement is best-effort~~ | **CLOSED → C-54 above.** The "silent miss" this item warned about is exactly what happened; it is now fixed and regression-tested. |

## Open — carried forward unchanged from № 135

| # | Item | Note |
| :--- | :--- | :--- |
| O-57 | Bus-stop descriptions render blank (unpopulated Redis stop-metadata hash — infra/data gap, not client). | unchanged |
| O-58 | Exits have no description in the dataset; the normalised label is the honest maximum. | unchanged |
| O-59 | Two-line vs one-line station card height parity is approximate. | unchanged |
| O-60 | `gia-list-dense` is class-name-targeted; a new `text-[Npx]` step needs a matching line. | unchanged |

## Open (new this entry)

| # | Item | Note |
| :--- | :--- | :--- |
| O-62 | **The Cuisine drawer is still not end-to-end verified.** This fix corrected the two specific mechanisms a static read could prove broken (the swipe conflict; the selector). It did **not** run a live venue search and drag the real handle in a real client — that round trip still cannot be stubbed cheaply in-sandbox. The operator's own screenshot remains the only evidence this drawer has ever rendered at all. |

## Decisions (new this entry)

| # | Decision | Rationale | Where recorded |
| :--- | :--- | :--- | :--- |
| D-28 | **A feature gaining its first draggable surface must also gain `disableVerticalSwipes()`**, not assume the platform default is fine. | Telegram's native vertical swipe always wins against a JS drag handle; Hawker and Transport learned this at v0.62.610, and Cuisine had to relearn it independently at v0.62.655 because the fix was never generalised into a checklist. | Journal `journal-0_62_657-29_07_26-2000.md` |

## Note on this arc's recurring failure shape

Bug 2 above (C-54) is the **fourth** occurrence this arc of the same shape already logged at
Decisions D-22–D-24 (register-0_62_655): a declaration or selector that matches nothing, fails
**silently**, and degrades to a fallback indistinguishable from a deliberate design choice. Like the three
Tailwind cases, it passed every automated check and was found only because a human touched the feature.
