(№ 160 - 01-08 '26 09:55 SGT)

# Register — soleat / Gia4lunch v0.62.682 (O-92 completed; card-gap constant decided)

> **Document type:** Register (per `doc/CLAUDE.md` §3; §4.1 — items move between sections, never deleted).
> **Continues** `register-0_62_681-01_08_26-0945.md` (№ 159). **Scope:** completion of O-92 after a
> Codex review catch; the carousel-card gap constant, quantified then decided by the operator.
> **Reflects:** project state at **v0.62.682**. **Targeted addition.**

---

## Completed (new this entry)

| # | Item | Shipped | PR(s) |
| :--- | :--- | :--- | :--- |
| C-96 | **O-92 actually completed.** C-93 (v0.62.680) switched only the `‹` pager button to `.gia-hit-y`; the `›` button kept omnidirectional `.gia-hit` and its 8px horizontal reach — and `›` is the rightmost pager control, i.e. the one adjacent to the Back/End cluster, so the reported collision would have survived the "fix". Both buttons now carry `.gia-hit-y` (`App.jsx:5782`, `:5793`, re-grepped post-edit). Caught by the Codex review bot on `5de6136`, not by this session's own verification. See the C-93 amendment in `journal-0_62_682-01_08_26-0955.md`. | v0.62.682 | #1680 |
| C-97 | **Carousel-card gap set to a measured, operator-chosen 5px.** Quantified the two old states first (exact from CSS: `4.5rem+2px` = 74px on first open vs `6rem+2px` = 98px after a search, a 24px delta; derived: ~−16px overlap vs ~+8px clear of the strip). That table showed a literal 2px would be *tighter* than the state the operator had identified as correct, so the constant was put to them rather than assumed. Answer: "split it — ~4-5px" → `STRIP_GAP_PX = 5` (module-level named constant in `ResultDrawer.jsx`), applied to the measured offset so it is a true 5px on every device. | v0.62.682 | #1680 |

## Decisions (new this entry)

| # | Decision | Rationale | Where recorded |
| :--- | :--- | :--- | :--- |
| D-50 | **The carousel card holds a 5px gap above the recommendation strip**, measured from the strip's real top edge, identical on first open and after a search. Supersedes the v0.62.650 "2px" ask, which predates the measurement and would have been tighter than the state the operator preferred. | Operator, shown the measured before/after table and asked directly: "Split it — ~4-5px." | `ResultDrawer.jsx` (`STRIP_GAP_PX`); Journal `journal-0_62_682-01_08_26-0955.md`. |

## Open (new this entry)

| # | Item | Note |
| :--- | :--- | :--- |
| O-103 | **A fix can pass this repo's whole verification suite while covering only some of the sites its own root cause identifies.** O-92's analysis correctly named "the pager's `‹`/`›` buttons", but only one was edited; build and tests are both blind to which touch-target class a button carries, so nothing flagged it — an external reviewer did. Variant of O-90 ("no tooling catches the commit not containing what its message says"), one layer up: *the fix not covering what its own root cause says it covers.* No fix proposed; recorded as a gap. A cheap partial mitigation would be re-grepping the root-cause pattern after the edit and confirming the match count went to zero — done manually this entry. |

## Open (carried forward, unchanged this entry)

| # | Item | Note |
| :--- | :--- | :--- |
| O-88 | M3 audit Tier 2 per-item decision round — still awaits its formal round. Unchanged. |
| O-93 | No React component render-test infrastructure. Unchanged — directly relevant to O-101 and O-103. |
| O-99 | O-98's "no separate compact-phone template" recommendation — no concrete wrap-risk list yet. Unchanged. |
| O-100 | O-96's glass-effect fix verified by code-comparison only; real-device recheck still open. Unchanged. |
| O-101 | v0.62.681's two fixes verified by construction only; device recheck outstanding. Unchanged — now also covers the 5px constant. |
| O-102 | The 900 ms first-load dwell is a judgement call, not operator-specified. Unchanged. |

## Note

C-96 is a **correction to a Completed item from two entries ago**, which is why it lands as its own
C-number plus an AU-7 amendment against C-93 rather than a silent edit of C-93's row — the ledger
should read "claimed resolved at v0.62.680, actually completed at v0.62.682," not as though it had
been right the first time. C-97 is the opposite shape and worth keeping visible as a precedent: the
operator asked for the numbers *before* the decision, the numbers contradicted the older standing ask
(v0.62.650's "2px"), and the contradiction was surfaced rather than resolved silently in either
direction.
