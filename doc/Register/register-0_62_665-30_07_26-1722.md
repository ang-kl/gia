(№ 143 - 30-07 '26 17:22 SGT)

# Register — soleat / Gia4lunch v0.62.665 (MICHELIN Guide Singapore 2026 Bib Gourmand update)

> **Document type:** Register (per `doc/CLAUDE.md` §3; §4.1 — items move between sections, never deleted).
> **Continues** `register-0_62_664-30_07_26-1428.md` (№ 142). **Scope:** the Michelin 2026 Bib Gourmand
> update. **Reflects:** project state at **v0.62.665**.
> **Targeted addition** — restates only the items this entry touches.

---

## Completed (new this entry)

| # | Item | Shipped | PR(s) |
| :--- | :--- | :--- | :--- |
| C-65 | **MICHELIN Guide Singapore 2026 Bib Gourmand update, verified and applied.** 89 → 97 (2 dropped, 10 new, 87 retained gain `'26` alongside `'25`). Every SG record now carries a compact `awardYears` field; the render pipeline (chat line + both TMA card components) reads it directly. | v0.62.665 | pending |
| C-66 | **Fixed a pre-existing rendering bug affecting all 11 non-SG Michelin countries**: `index.js`'s flattening step computed the correct latest category but silently dropped the real award year, so already-correct 2025/2026 data in `michelin-data.js` always displayed as a hardcoded "2025" on both TMA card components. Now threads the real compact year(s) through via the new `retainedAwardYears()` helper. | v0.62.665 | pending |

## Open (new this entry)

| # | Item | Note |
| :--- | :--- | :--- |
| O-72 | **The 10 new Bib Gourmand entries' addresses are best-effort from secondary sources.** Every direct fetch against guide.michelin.com and local food-news sites returned 403 (bot-blocked, confirmed not a proxy issue). Stall names + 2026 award status are corroborated 3+ sources deep each; exact hawker-centre address strings could drift from Michelin's own formatting. Worth a spot-check next time the hawker Bib Gourmand join (`hawker-vault.js`) is touched. |
| O-73 | **"King of Laksa" has no confirmed street number** — recorded as `address: 'Aljunied Avenue 2'`, matching this file's existing convention for imprecise addresses (`Anglo Indian` → `'Shenton Way'` already sets this precedent). |
| O-74 | **The 11 non-SG countries' underlying 2025/2026 award data was not re-verified this entry** — only the rendering bug that discarded already-correct data was fixed (C-66). If Michelin has since revised any of those 11 countries' editions in a way not yet reflected in `michelin-data.js`, that's a separate, still-open gap. |

## Decisions (new this entry)

| # | Decision | Rationale | Where recorded |
| :--- | :--- | :--- | :--- |
| D-32 | **SG-michelin.js's records now carry an `awardYears` field**, a deliberate, narrow exception to the earlier decision (pinned by `__tests__/SG-michelin.test.js`'s "reverted to standalone form" comment) that kept SG year-less and decoupled from the unified venue-centric loader. | The operator's Michelin 2026 update spec explicitly permits "the existing award-year field OR the smallest compatible addition to the existing restaurant record" — read as license for exactly one new field on the same flat shape, not a re-coupling into `michelin-data.js`'s venue-centric system. SG stays exactly as decoupled as the earlier decision intended; it just isn't year-less any more. | Journal `journal-0_62_665-30_07_26-1722.md` |
| D-33 | **`venue.michelinYear` (single number) is retired as a setter, everywhere, in favour of `venue.michelinAwardYears` (array)** — but kept as a read-only fallback in Clipboard's `VenueCard.jsx` for already-saved clip records. | A single year field cannot represent a venue retaining its award across multiple consecutive editions ("'26, '25") — this is the "required to fix an existing defect" case the operator's own spec allows for renaming/removing a field, but old persisted data (Clipboard clips saved before this change) still needs the old field to keep rendering, so it's kept read-only rather than deleted. | Journal `journal-0_62_665-30_07_26-1722.md` |

## Note

New PR (not riding on a prior open one — #1668 and #1669 both merged earlier this session).
