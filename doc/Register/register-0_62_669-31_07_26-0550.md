(№ 147 - 31-07 '26 05:50 SGT)

# Register — soleat / Gia4lunch v0.62.669 (Material-alignment Phase 2a)

> **Document type:** Register (per `doc/CLAUDE.md` §3; §4.1 — items move between sections, never deleted).
> **Continues** `register-0_62_668-30_07_26-2322.md` (№ 146). **Scope:** token-bridge invisible tier +
> ledger catch-up. **Reflects:** project state at **v0.62.669**. **Targeted addition.**

---

## Completed (new this entry)

| # | Item | Shipped | PR(s) |
| :--- | :--- | :--- | :--- |
| C-73 | **Phase 2a token bridge, invisible tier** — 435-site colour audit; `tg-warn` advisory role token (11 borders), `.gia-map-btn` map-chrome role class (16 buttons), accent-text FAB rings (2), Sketchbook dead theme plumbing removed (P2-a). Zero rendering change on stock themes. | v0.62.669 | pending |
| C-74 | **VibeCodingRecord catch-up + generator join fix** — ledger backfilled #1666–#1671 (now 1,644 rows); the numeric/string key mismatch that silently dropped ~122 rows' file lists is fixed. | v0.62.669 | pending |

## Open (new this entry)

| # | Item | Note |
| :--- | :--- | :--- |
| O-85 | **Phase 2b decision list — visible colour migrations (201 B-visible sites; NONE changed yet).** Operator calls needed, by category: **(1) BackFab** — cuisine's v0.60.91 comment says inverse-theme colours, but all four apps ship identical fixed teal `#7FDBDB`; which is right — standardized teal (fix the stale comment) or the documented inverse-theme intent (visible change ×4 apps)? **(2) Dark-illegible one-offs** — `text-blue-900` streamed-name on dark bg (cuisine App.jsx:4739), the 12 `-100`-fill dish badges (CuisineCategoryDrawer), `#DCEBFF` selected category card: tokenise (visible fix in dark mode) or keep? **(3) Map chrome stays light** — now encoded as `.gia-map-btn`; confirm as a standing decision so audits stop flagging it. **(4) Status green/orange pairs** (transport open/closed, hawker status pill): keep as fixed semantics or derive CVD-safe tokens? **(5) Fixed-light surfaces** (ResultCard focused card, folio/manila, insight banner, InfoWindows): all carry deliberate-design comments — confirm as standing so they're out of scope permanently. |
| O-86 | Skeuo/glass shadow stacks byte-identical across hawker/transport/menu styles.css — Phase 3 shared-stylesheet extraction candidate (values stay fixed; deduplication only). |

## Decisions (new this entry)

| # | Decision | Rationale | Where recorded |
| :--- | :--- | :--- | :--- |
| D-40 | **A stale comment is not a mandate: A-contradict sites get a DECISION, not an auto-fix, when sibling apps show a later standard.** Cuisine's BackFab comment contradicts 4-app byte-identical reality; changing code on comment strength alone would visibly fork one app. | The invisible-tier contract ("no visual change without permission") outranks comment archaeology. | Journal `journal-0_62_669-31_07_26-0550.md` |
| D-41 | **Role tokens may be deliberately theme-independent** — `tg-warn` is a fixed hex, not a `--tg-*` var: the no-red advisory colour must not follow the Telegram theme. Same for `.gia-map-btn` (map tiles are its background, not the app theme). | The bridge's job is NAMING roles, not forcing every colour through themeParams. | Journal `journal-0_62_669-31_07_26-0550.md` |

## Note

Feature doc: no v0.62.669 entry — nothing user-visible changed (logged per the same-pass-or-log rule).
