(№ 141 - 30-07 '26 13:42 SGT)

# Register — soleat / Gia4lunch v0.62.663 (desktop auto-fullscreen parity: Menu, Clipboard, Oversight)

> **Document type:** Register (per `doc/CLAUDE.md` §3; §4.1 — items move between sections, never deleted).
> **Continues** `register-0_62_662-30_07_26-1154.md` (№ 140). **Scope:** the Telegram Desktop auto-maximize
> request. **Reflects:** project state at **v0.62.663**.
> **Targeted addition** — restates only the items this entry touches; O-70/O-71 from № 140 remain as
> recorded there.

---

## Completed (new this entry)

| # | Item | Shipped | PR(s) |
| :--- | :--- | :--- | :--- |
| C-63 | **Menu, Clipboard, and Oversight now auto-fullscreen on Telegram Desktop on load**, matching the behaviour Cuisine/Hawker/Transport have had since v0.62.624. Menu's gate was iPad-only; Clipboard and Oversight had no fullscreen step at all. | v0.62.663 | #1668 |

## Note

**No new Open items.** This entry closes a straightforward parity gap using the exact, already-proven
pattern from the three map apps — no new technical unknowns were introduced. The standing caveat that
Telegram's Bot API has no way to detect or control the OS-level window's minimized/maximized state (only
`requestFullscreen()`, which expands the Mini App's rendering area within whatever window Telegram already
has) applies here exactly as it already did for Cuisine/Hawker/Transport; not re-logged as a new item since
it's the same known constraint, not a new discovery.

Rides on the still-open **#1668** (v0.62.659 through 663 all on one PR/branch).
