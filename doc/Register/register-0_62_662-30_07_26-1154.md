(№ 140 - 30-07 '26 11:54 SGT)

# Register — soleat / Gia4lunch v0.62.662 (full 8-locale i18n coverage, three map TMAs + the station-search field)

> **Document type:** Register (per `doc/CLAUDE.md` §3; §4.1 — items move between sections, never deleted).
> **Continues** `register-0_62_661-30_07_26-1121.md` (№ 139). **Scope:** the confirmed full-repo linguistic
> catch-up. **Reflects:** project state at **v0.62.662**.
> **Targeted addition** — restates only the items this entry touches; O-67/O-68/O-69 from № 139 remain as
> recorded there.

---

## Completed (new this entry)

| # | Item | Shipped | PR(s) |
| :--- | :--- | :--- | :--- |
| C-62 | **All en/fr-only UI strings across Transport, Hawker, and Cuisine now have full 8-locale coverage** (id/ru/de/zh/ja/es added). 506 new translated entries in the three apps' `i18n.js` files + 36 in the new shared `StationLocationField.jsx`, verified by script to leave zero remaining gaps. | v0.62.662 | #1668 |

## Open (new this entry)

| # | Item | Note |
| :--- | :--- | :--- |
| O-70 | **Pre-existing duplicate key found, not fixed:** Cuisine's `RU_STRINGS` block defines `cat.sweetsFusion` twice with two DIFFERENT Russian values ("Десерты и фьюжн" vs "Сладости и фьюжн") — JS object-literal semantics mean only the second silently wins. Confirmed via `git diff` to predate this session entirely. Not fixed here: choosing which of two already-shipped translations is "correct" is a different, more subjective call than filling in a missing one, and this entry's mandate was completeness, not correctness review. |
| O-71 | **Translation quality is not native-speaker-reviewed.** All 542 new entries (id/ru/de/zh/ja/es across 3 apps + 1 shared component) were produced by model-based translation, calibrated against already-shipped entries for terminology consistency and mechanically verified for placeholder-token correctness and key completeness — but not reviewed by a human fluent in any of the six languages. |

## Note

Rides on the still-open **#1668** (v0.62.659 through 662 all on one PR/branch, since none of the operator's
follow-up messages arrived after that PR was reviewed or merged).
