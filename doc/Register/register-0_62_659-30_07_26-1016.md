(№ 137 - 30-07 '26 10:16 SGT)

# Register — soleat / Gia4lunch v0.62.659 (Train onboarding + collapsed cards + 1/4 peek + station search, Train & Hawker)

> **Document type:** Register (per `doc/CLAUDE.md` §3; §4.1 — items move between sections, never deleted).
> **Continues** `register-0_62_657-29_07_26-2000.md` (№ 136). **Scope:** the five-part Train/Hawker request
> (map zoom + onboarding popup, card default state, drawer peek fraction, station-search location row).
> **Reflects:** project state at **v0.62.659**.
> **Targeted addition** — restates only the items this entry touches; O-57–O-60, O-62 and all Decisions from
> № 136 remain as recorded there.

---

## Completed (new this entry)

| # | Item | Shipped | PR(s) |
| :--- | :--- | :--- | :--- |
| C-55 | **Train's first-load map zoom is now 10** (whole-network overview tier), not 12 (a partial zoomed-in slice). Matches the operator's own screenshot pairing of the two zoom levels. | v0.62.659 | pending |
| C-56 | **A first-visit "select a line" popup + a blinking East-West Line chip** replace the old auto-shown status popup on Train's first load. Reuses the app's own `Modal` shell (the operator's "common pop-up window"). | v0.62.659 | pending |
| C-57 | **Train station cards default to collapsed ("less")**, reversing the v0.62.645 decision — see D-29. | v0.62.659 | pending |
| C-58 | **Train's list-mode drawer opens at a 1/4-visible peek** (`snaps=[0.14,0.48,0.75]`, `initialSnap=2`) instead of the shared component's stock half-screen default. | v0.62.659 | pending |
| C-59 | **A shared station-search "📍 location + nearest station" row** (`StationLocationField.jsx`), searching all ~209 MRT/LRT stations by name or code (operating and future alike), wired into both Train's header and both of Hawker's live header layouts. | v0.62.659 | pending |

## Open (new this entry)

| # | Item | Note |
| :--- | :--- | :--- |
| O-63 | **None of the five v0.62.659 changes are device-verified.** Same caution as O-56/O-62: a sandboxed build + full test suite passing does not prove correct behaviour on a real Telegram client — this arc has already found three Tailwind opacity drops, a dead selector, and a native-gesture conflict that all passed CI clean. Watch for the operator's next screenshot. |
| O-64 | **Hawker's station search doesn't filter or navigate anything yet.** `StationLocationField` renders and works (nearest station + typeable search) in Hawker, but `onSelectStation` is unwired there — "apply this to Hawker TMA as well" was read as UI parity, not a request to filter hawker centres by proximity to a searched station. Would need an explicit ask before building that integration. |
| O-65 | **`pickline.title`/`pickline.body` (and the new component's own strings) are en/fr only** — the other six transport locales fall back to English via the existing `t()` chain. Functional, not fully localised. |

## Decisions (new this entry)

| # | Decision | Rationale | Where recorded |
| :--- | :--- | :--- | :--- |
| D-29 | **StationCard's `bodyOpen` reverts to collapsed-by-default**, superseding the v0.62.645 in-code decision ("by default show the station details, not collapse" — recorded only as a code comment at the time, not a formal Register entry). | Operator's later, more specific instruction ("All cards must be in 'less' state... let user open the card") is dated after and takes precedence over the earlier one; both are kept on the record (the v0.62.645 comment stays in `StationCard.jsx`, annotated) rather than the earlier one being silently erased. | Journal `journal-0_62_659-30_07_26-1016.md` |
| D-30 | **Transport regains a geolocation read**, superseding the v0.62.641 removal ("remove distance walking in train" — deliberately dropped the location permission prompt because nothing consumed the fix). | The new station-search row gives geolocation a fresh, distinct justification (nearest-station display); the OLD `userLoc`/walk-time plumbing this replaced stays removed — this is a new, separate one-shot read inside `StationLocationField`, not a revival of the old distance row. | Journal `journal-0_62_659-30_07_26-1016.md` |

## Note on this arc's recurring failure shape

C-56/C-57/C-58/C-59 are all state-default or UI-wiring changes verified only by build + test-suite green, the
exact same verification depth that shipped the two real O-56 bugs at v0.62.655/657. O-63 above is this
Register's explicit flag that the pattern repeats here, not a claim that it's already been checked.
