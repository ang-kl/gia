(№ 152 - 31-07 '26 11:37 SGT)

# Register — soleat / Gia4lunch v0.62.674 (device-reported bug fixes on v0.62.673)

> **Document type:** Register (per `doc/CLAUDE.md` §3; §4.1 — items move between sections, never deleted).
> **Continues** `register-0_62_673-31_07_26-1107.md` (№ 151). **Scope:** two real-device bugs found on
> the just-shipped Michelin pagination feature.
> **Reflects:** project state at **v0.62.674**. **Targeted addition.**

---

## Completed (new this entry)

| # | Item | Shipped | PR(s) |
| :--- | :--- | :--- | :--- |
| C-79 | **Footer-overlap peek gap fix** — first-load `BottomSheet` peek height now also measures the footer's own real rendered height (new `footerRef` + `ResizeObserver`) and reserves that space, so the collapsed peek no longer sits flush against the fixed footer stack. Card-derived height logic unchanged; dependency array deliberately left untouched to avoid the file's known TDZ trap. | v0.62.674 | pending |
| C-80 | **Michelin pager denominator fix** — replaced `pages.length` (always equal to the numerator by construction) with a new `michelinTotalPages`, derived from `michelinRemaining.total ÷ 12` (server-reported Michelin/Bib Gourmand count ÷ `ResultPanel.jsx`'s own per-batch `PAGE_SIZE`), floored against `pages.length`. Pager visibility gate switched to the same real total, so it now appears from the very first batch instead of only after stepping forward once. | v0.62.674 | pending |

## Open (carried forward, unchanged this entry)

| # | Item | Note |
| :--- | :--- | :--- |
| O-88 | M3 audit Tier 2 per-item decision round — still awaits presentation to the operator. Unchanged. |
| O-89 | Non-adoption recommendation (M3 literal type scale + literal elevation shadows) — still awaits sign-off. Unchanged. |
| O-91 | Year-filter chips + client-side year/category sort (Michelin spec §1-§2) — still not built; scope decision still open. Unchanged. |
| O-92 | Back/End button (footer right) can also step the Michelin pager's cursor backward, overlapping with the new centre pager's `‹`. Deliberately left untouched. Unchanged. |
| O-93 | No React component render-test infrastructure exists anywhere in this repo — both this entry's fixes are verified by construction/code-reading only, same standing gap. Unchanged. |

## Note

Both fixes originated from the operator's own real-device check on v0.62.673 (two screenshots, two
explicit bug reports) rather than from audit or spec work — see Journal `journal-0_62_674-31_07_26-1137.md`
for the full root-cause trace. No new Decisions this entry: both were bugs against the v0.62.673 feature's
own stated intent, not open design questions.
