(№ 118 - 24-05 '26 17:54 SGT)

# Register — soleat / Gia4lunch v0.61.127 (Fruits/Durian + JB geocode arc — incremental)

> **Document type:** Register (per `doc/CLAUDE.md` §3 / §4.1).
> **Status:** Incremental update to the v0.61.83 master (`register-0_61_83-21_05_26-1645.md`), layered on top of the v0.61.124 increment (`register-0_61_124-24_05_26-1636.md`). Records the deltas from the v0.61.125 / v0.61.126 / v0.61.127 arc (3 squash-merged PRs to `main`). Prior Open / Known Issues / Deferred / Decisions / Completed / Removed entries remain authoritative; this file appends per AU-1.
> **Reflects:** project state at **v0.61.127, 24 May 2026 17:54 SGT**, after PR #625 → `e7fa92f`, PR #626 → `1c7c4cb`, PR #627 → `86711da`. No vault snapshot; no master-doc refresh (per-arc Register increment).

---

## Open Items (delta only — see v0.61.83 + v0.61.124 register increments for the full carry-forward list)

| # | Item | Owner | Priority | Notes |
| :--- | :--- | :--- | :--- | :--- |
| O-23 | **Progressive radius widening on weak Fruits/Durian results** | agent | low | `scripts/Create_2_buttons.MD` "Fallback behaviour" section asks for the search radius to widen progressively when fewer than the target count is found. Current v0.61.126 behaviour surfaces the limitation honestly via the `specialModeLimited: true` response flag + "🍉/🥥 Limited matches nearby" amber card in the TMA — but it does NOT auto-widen the `pipeline.discover` radius. The widening loop is a follow-up; the current behaviour is spec-compliant in that it never pads with unrelated cuisines. |
| O-24 | **Sarawak / Petra Jaya as a real Malaysian-East anchor** | operator | low | The v0.61.127 spec correction clarifies that the original "Petra Jaya, Sarawak" zone was a typo for Putrajaya. If the operator later wants the **actual** Petra Jaya (Kuching suburb, Sarawak) as a separate anchor, that needs: new entry in `precincts.js` (e.g. `petra-jaya-sarawak`, coords ~1.58°N, 110.34°E, region `MY-SRW`), Menu TMA dropdown entry, server-side region clamp updates, and probably radius / language tuning. Not on the immediate roadmap. |

### Updates to prior Open items (status change only — items stay on the ledger per AU-1)

- **O-20 (v0.61.124) Cuisine TMA "Tell me" box does not run place detection** — **unchanged**. Still applies; not addressed in the v0.61.125-127 arc.
- **O-21 (v0.61.124) vitest 2.1.9 + Node 24 incompatibility** — **unchanged**. The full vitest suite eventually ran during the v0.61.120 push (~3 hours, 1951/1951 pass) but the dev env still hangs on first launch.
- **O-22 (v0.61.124) Working tree on iCloud Drive throttles git + vite + esbuild** — **unchanged**. Worked around again in v0.61.125-127 via plumbing commits (`git read-tree origin/main` + `git update-index --cacheinfo` + `git commit-tree` + `git update-ref`) and CI for builds.

---

## Decisions (append-only)

| # | Decision | Rationale | Date |
|---|---|---|---|
| D-61.125 | **Region-aware geocode helper** — new `vibe-suggest.geocodeQueryRegion(text, { region, biasCenter })` is the new SoT for Places `textQuery` lookups that need a country / locality bias. The legacy `geocodeQuery` (Singapore-hardcoded suffix) is preserved for back-compat but new endpoints should prefer the region-aware variant. The helper sends a Places `locationBias` circle around the user's existing anchor + filters out hits outside the expected country bbox. | Sheraton-in-Johor bug: typing "Sheraton Hotel" with a JB anchor active resolved to Sheraton Towers Orchard because the legacy helper always appended " Singapore" to the textQuery. | 24-05 '26 |
| D-61.125b | **Menu TMA Location section** — the LocationFieldMenu lives in its own top-level section (`section.id === 'location'`) rendered AFTER the PLAN section. The summary line ("Anchored at <X> · N km cap.") is bumped to `text-[13px]`; the disabled-tiles list is split onto its own next line at `text-[11px]`. All other text-size classes in the Menu TMA bumped one Tailwind step up. | Operator instruction (24-05 '26): "Move this search anchor place after the plan as its own section 'Location'. Anchored at Johor Bahru · 30 km cap be larger font size. Next line stated - Hawker, Train, Incidents, Bus stops, Weather disabled. All in the Menu be one font size bigger." | 24-05 '26 |
| D-61.126 | **Special-mode exclusive search class** — Fruits + Durian are a new modifier class where `cuisineQueries` is fully overridden by mode-specific seeds and downstream Tell-me / dessert / home-based blocks are bypassed via `if (… && !specialMode)`. The per-mode keyword + reject-type lists live in `special-mode.js` (single SoT). The CuisineDrawer greys out the catalogue grid + QuickFilters chip row + dessert/Michelin toggles when a mode is active. Future special modes (seafood-only, vegetarian-only, etc.) should follow the same pattern. | `scripts/Create_2_buttons.MD` operator spec — must NOT mix special modes with normal cuisine / Michelin / dessert filters; must keep results strongly mode-relevant; must surface the "Limited matches nearby" message rather than padding with unrelated results. | 24-05 '26 |
| D-61.127 | **Spec correction precedent** — when an operator-supplied spec doc carries a factual error (e.g. wrong city, wrong coordinates), the correction lands as its own PATCH (no code change) that edits the spec doc in-repo + adds an Amendments-section entry to the relevant journal entry per AU-7. The prior text is acknowledged in the corrected paragraph, NOT silently erased. This preserves the trace ("we deferred Sarawak; the operator later said Putrajaya; here's how the existing Putrajaya anchor already covers it"). | First instance: v0.61.127 corrects the Malaysia zone in `Create_2_buttons.MD` from "Petrajaya / Petra Jaya, Sarawak" to "Putrajaya (IOI City Mall / IOI Resort City)". Recipe AU-2 / AU-7 inform the pattern. | 24-05 '26 |

---

## Completed (delta — earlier `C-NNN` arcs live in the v0.61.83 / v0.61.124 register entries)

| # | Arc | Outcome | Versions |
|---|---|---|---|
| C-AX | Menu TMA Location section + JB geocode fix + font bump | PR #625 — Sheraton-in-Johor bug fixed via new `geocodeQueryRegion` (region-aware Places API suffix + locationBias circle + bbox filter); Menu TMA every text-size bumped one Tailwind step; LocationFieldMenu summary split into bigger "Anchored at" line + standalone next-line disabled-tiles list; LocationFieldMenu moved from PLAN section into own "Location" section after PLAN. | v0.61.125 |
| C-AY | Fruits + Durian exclusive special modes | PR #626 — new `special-mode.js` module (SEED_TEMPLATES + multi-language KEYWORDS + REJECT_PRIMARY_TYPES + `buildSeeds` + `filterByMode`); CuisineDrawer 2 amber-pill toggles above catalogue (mutually exclusive, grey out catalogue + QuickFilters when active); `/api/cuisine/search` reads `specialMode`, overrides cuisineQueries, short-circuits Tell-me / dessert / home-based, post-filters by mode keywords, sets `specialModeLimited` when count < 8; 35-spec test file. Region-suffix swap for JB / Putrajaya seeds works via `state.region`. | v0.61.126 |
| C-AZ | Spec correction — Putrajaya not Sarawak | PR #627 — `scripts/Create_2_buttons.MD` 4 textual fixes (Malaysia zones list, Important note, test scenarios 5+6, Acceptance criteria) acknowledging the operator's spec typo: the intended Malaysia zone was Putrajaya (IOI City Mall / IOI Resort City, West Malaysia, 15 km cap), NOT Petra Jaya in Sarawak. v0.61.122's existing `ioi-resort-putrajaya` anchor + v0.61.126's region-suffix swap already cover the Putrajaya zone tests; the v0.61.126 PR's "NOT YET SUPPORTED Sarawak" caveat was overly defensive. No code change. | v0.61.127 |

---

## Removed Features

> AU-EOF. No Register-tracked item has been deleted in the v0.61.125-127 arc — items only move between sections.

| # | Item | Original Version | Removed in Version | Removal Date | Reason | Replacement | Rule | Serial № |
|---|------|------------------|--------------------|--------------|--------|-------------|------|----------|
| — | (none in this arc — see prior register entries' Removed tables) | — | — | — | — | — | AU-2 | (№ 118 — 24-05 '26 17:54 SGT) |

---

## Rule reference

- **AU-1:** Register items move between sections, never deleted. This incremental update appends to the v0.61.83 master + the v0.61.124 increment.
- **AU-7:** D-61.127 establishes the spec-correction precedent — the corrected paragraph in `scripts/Create_2_buttons.MD` acknowledges the prior wording rather than silently erasing it.

## Amendments

(None — incremental delta; the per-PR amendment trace lives in `doc/Journal/journal-0_61_{125,126,127}-*.md`.)
