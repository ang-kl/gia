(№ 159 - 01-08 '26 09:45 SGT)

# Register — soleat / Gia4lunch v0.62.681 (first-load wait message; carousel-card gap)

> **Document type:** Register (per `doc/CLAUDE.md` §3; §4.1 — items move between sections, never deleted).
> **Continues** `register-0_62_680-01_08_26-0835.md` (№ 158). **Scope:** two device-reported Cuisine
> regressions (first-load overlay visibility; carousel card offset vs. the recommendation strip).
> **Reflects:** project state at **v0.62.681**. **Targeted addition.**

---

## Completed (new this entry)

| # | Item | Shipped | PR(s) |
| :--- | :--- | :--- | :--- |
| C-94 | **First-load wait message restored to "Finding eateries… → results."** The operator's hypothesis (an audit removed it) was **disproved before any code changed** — the overlay condition is unchanged since v0.62.484 and v0.62.671's only Cuisine edit was the Widen-switch thumb offset. Real cause: nothing guaranteed the message stayed on screen long enough to read — the splash gate opens and fires the boot load in one commit, so a fast-settling boot search flips `loading` false within a frame or two. Added `bootOverlayHold`, a 900 ms minimum dwell armed only on the gate's boot-load branch (mismatch branch untouched, still opens an empty TMA), cleared by a timer that always fires and by 🛑 Stop. | v0.62.681 | #1680 |
| C-95 | **Carousel card now sits a consistent 2 px above the recommendation strip.** Root cause: `ResultDrawer`'s bottom offset was the hardcoded guess `hasFilters ? '6rem' : '4.5rem'`, left from v0.62.186 when filter chips were their own full-width row; since v0.62.192/281 they are the inline "Criteria (N) ▾" pill, so the footer height no longer moves with filters — yet the drawer still lifted 1.5 rem (24 px) whenever criteria existed (first open = no criteria = flush against the strip). Replaced with a measured `stripLiftPx` (`useLayoutEffect` in `App.jsx`, reading `.insight-glass`'s real top edge scoped to the footer ref), same measure-don't-guess fix as v0.62.674's footer reserve. Old `calc()` retained as first-paint fallback only. | v0.62.681 | #1680 |

## Open (new this entry)

| # | Item | Note |
| :--- | :--- | :--- |
| O-101 | **Both v0.62.681 fixes are verified by construction only** — C-94's root cause (a fast boot search out-racing the overlay) is inferred from the code path and cannot be reproduced in-sandbox, and C-95's measured path is unreachable without a render harness (O-93). Operator device-recheck is the genuine confirmation for both. If C-94's symptom persists after this ships, the next suspect is the splash gate itself, not the overlay. |
| O-102 | **The 900 ms first-load dwell is a judgement call**, not an operator-specified number. Trivially tunable; worth confirming against a real device before treating it as settled. |

## Open (carried forward, unchanged this entry)

| # | Item | Note |
| :--- | :--- | :--- |
| O-88 | M3 audit Tier 2 per-item decision round — still awaits its formal round. Unchanged. |
| O-93 | No React component render-test infrastructure. Unchanged — directly relevant to O-101 above. |
| O-99 | O-98's "no separate compact-phone template" recommendation — no concrete wrap-risk list yet. Unchanged. |
| O-100 | O-96's glass-effect fix verified by code-comparison only; real-device recheck still open. Unchanged. |

## Note

Both items this entry are the same underlying failure shape, in two different places: a **hardcoded
guess standing in for a measurement** (C-95's `4.5rem`/`6rem` footer estimate) and an **implicit
assumption about timing standing in for an explicit guarantee** (C-94's reliance on the boot search
being slow enough to see). The repo already has the corrective precedent for the first (v0.62.674's
footer-height reserve, v0.62.657's stable-selector rule), and C-95 follows it directly. Worth noting
for the record that C-94's reported cause — "the audit removed it" — was **not** what happened; the
audits were checked commit-by-commit first, and the real cause was found somewhere else entirely.
