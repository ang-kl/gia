(№ 146 - 30-07 '26 23:22 SGT)

# Register — soleat / Gia4lunch v0.62.668 (Material-alignment Phase 1)

> **Document type:** Register (per `doc/CLAUDE.md` §3; §4.1 — items move between sections, never deleted).
> **Continues** `register-0_62_667-30_07_26-1924.md` (№ 145). **Scope:** Material-alignment Phase 1
> (invisible accessibility repairs). **Reflects:** project state at **v0.62.668**.
> **Targeted addition** — restates only the items this entry touches.

---

## Completed (new this entry)

| # | Item | Shipped | PR(s) |
| :--- | :--- | :--- | :--- |
| C-71 | **Material-alignment Phase 1 — all six approved slices** (lang metadata, reduced motion, focus-visible, dialog/keyboard contract, names/labels, invisible touch targets) across _shared + all six TMAs, driven by a 166-finding two-agent audit. Zero visible change for a default touch user; no new dependencies; bundle delta ≤ +8K per app. | v0.62.668 | pending |
| C-72 | **Repo-wide dialog primitive** — `web/_shared/lib/use-dialog.js`, lifting the vanilla bottom-sheet's proven focus-trap/Escape/restore contract into React; ~20 surfaces wired. Also fixed a pre-existing CuisineCategoryDrawer double-close bug found during wiring. | v0.62.668 | pending |

## Open (new this entry)

| # | Item | Note |
| :--- | :--- | :--- |
| O-79 | **Device verification for the whole phase** — real Telegram iOS/Android/Desktop, real screen reader, on-device keyboard walk-through. Sandbox verified builds/DOM only. Highest-priority operator check: dialogs (focus + Escape), LocaleToggle menus, drawer handle keys, focus ring visibility on both themes. |
| O-80 | **Background `inert` + scroll locking** excluded from `useDialog` by design (portals/toasts need per-app auditing). Phase 3 shared-primitive scope. |
| O-81 | **MapControls extended pill hit areas may clip** where the pill strip scrolls (`overflow-x-auto` clips vertical overflow) — partial benefit; device-check with O-79. |
| O-82 | Two clipboard strings still hard-coded English (Shell chip-clear "clear"; DrawerRow "Move up/down") — found outside the audit list, queued. |
| O-83 | **LocationSheet's unannounced 60 s auto-dismiss** (audit 3.7) — a behaviour change beyond this phase's "invisible repairs" scope; decide whether to remove or announce it. |
| O-84 | **Phases 2–5 of the governance prompt not started**: token bridge (the ~110 hard-coded-colour findings wait on it), shared primitives, optional Material Web pilot, CI a11y protection. Each needs its own §18-style report + approval. |

## Decisions (new this entry)

| # | Decision | Rationale | Where recorded |
| :--- | :--- | :--- | :--- |
| D-37 | **Clipboard/Sketchbook keeps its fixed light palette**; Phase 1 fixed only its behavioural a11y defects. Its dead theme-var writes remain (removal deferred — see O-84/Phase 2 rather than churn now). | Operator, explicitly, on the pre-implementation report: "keep clipboard palette" — reaffirming the recorded Sketchbook sample-palette decision over Telegram theming. | Journal `journal-0_62_668-30_07_26-2322.md` |
| D-38 | **Touch targets grow invisibly only** (`.gia-hit*` transparent ::after; directional variants prevent overlap in clusters); the compact visible design — including the 9px one-row MapControls pills — stands untouched. | Operator, explicitly: "invisible hit areas only". A stolen tap being worse than a small target drove the directional variants. | Journal `journal-0_62_668-30_07_26-2322.md` |
| D-39 | **LocaleToggles use the ARIA menu-button pattern** (role=menu + menuitemradio), not a corrected listbox. | A listbox must not contain buttons and would need aria-activedescendant machinery; a menu of menuitemradio buttons matches the actual widget (choose-one action list), keeps real buttons focusable, and needed no visual change. | Journal `journal-0_62_668-30_07_26-2322.md` |

## Note

New PR (branch restarted from `origin/main` after #1670 merged). Operator pre-authorized merge on green CI
("auto continue, auto audit, auto merge").
