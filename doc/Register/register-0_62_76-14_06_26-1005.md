(№ 129 - 14-06 '26 10:05 SGT)

# Register — soleat / Gia4lunch v0.62.76 (outstanding-snapshot)

> **Document type:** Register (per `doc/CLAUDE.md` §3; §4.1 — items move between sections, never deleted).
> **Status:** **Incremental outstanding snapshot.** Continues `register-0_62_69-13_06_26-1545.md`. **Scope:** the **v0.62.70 → v0.62.76** arc (PRs #1023 → #1029, 13–14 '06 '26) — the loading/fun-fact overlay polish, the cuisine relevance/geofence hardening, and the enrichSlow timing instrumentation. Per-PR detail: `doc/Journal/journal-0_62_53-13_06_26-0610.md` (amendments № 736–753) + the VibeCodingRecord (#1023–#1029).
> **Reflects:** project state at **v0.62.76, 14-06 '26 10:05 SGT** (PR #1029 merged at `3803a6c`). Operator-facing TMAs: Cuisine + Menu + Hawker + Transport + Oversight. Branch protection on `main` unchanged (4 required checks; enforce_admins=true; log collector uses the unprotected `railway-logs` branch).

---

## Open Items (live as of v0.62.76)

| # | Item | Owner | Priority | Notes |
| :--- | :--- | :--- | :--- | :--- |
| O-45 | **Cuisine search 20s timeout → "Load failed"** | agent | **high** | Operator-reported (13-06). `log/log-2026-06-13T14-12-10Z` style window: `D706 deadline-degraded (>20000ms)`, `cuisine-search-exit ms=22562`. `enrichSlow()` runs a **sequential** chain (translate → review-cache → Gemini dishes → travel → sanctuary/Claude → BestTime footfall) that sums past the 20s D706 deadline → client gives up. **Instrumented** in v0.62.76 (D-62.76, the D707 line). NEXT: read a post-deploy D707 breakdown → parallelize the worst offenders and/or gate BestTime to SG. |
| O-46 | **BestTime footfall resolves 0/N for non-SG venues** | agent | medium | `[footfall] besttime resolved=0/6` on Putrajaya searches — BestTime has poor MY coverage, so every non-SG search spends footfall latency for zero result. Gate `attachFootfallSignals` to SG region (or skip when region!=SG). Folds into the O-45 fix. |
| O-47 | **Stale "Results: N" count on a new search** | agent | medium | Operator screenshot: "Results: 6 · Showing first 1 · Russian" — `knownTotal` (`App.jsx:2355`, a running MAX) is only refreshed when a response carries `cumulativeEnd`; a 1-result search left the previous search's total (6) on the header. Fix: reset `knownTotal` / range counters when a new search is submitted, so "Results" reflects the current search. |
| O-48 | **Curated-picks (operator favourites) source** | agent + operator | low | Operator: *"Add it to a curated source"* for **97 Durian (Sri Petaling)** (~11 km from Putrajaya, in-range). No existing per-venue curated slot fits a KL durian stall (recognised "Hall of Fame" is SG-only; durian = Places + Gemini). Proposed: `curated-picks.js` (`{name, placeId, lat, lng, cuisines[], country, area, note}`) injected into in-range results, deduped by placeId, gate-exempt. **Blocked** on the venue's `place_id` + lat/lng (short Maps link `maps.app.goo.gl/pkmEuyYBVsJZAp9S9` is 403 to automated fetch; awaiting the app card or the expanded URL). |

> **Closed/dropped in the prior arc** (recorded in `register-0_62_69-…`): **O-42** durian all-seen-criteria (won't-fix), **O-43** AI-credit alerting (dropped), **O-44** v0.61.301→v0.62.52 doc backfill (resolved by the `-0_62_52` rollups).

---

## Decisions (append-only, v0.62.70 → v0.62.76)

> Continues from D-62.69 in `register-0_62_69-13_06_26-1545.md`.

| # | Decision | Rationale | PR | Date |
|---|---|---|---|---|
| D-62.70 | **First-load overlay copy**: "Please wait while loading random eateries…" → **"Finding eateries"** + a **blinking U+2026 ellipsis** (`animate-blink` keyframe, glyph rendered as a separate span); "Rating reset:" → "Rating reset **to** Good+ ≥ 3.7⭐"; stop button → "🛑 Stop search". en+fr. | Operator copy spec on the (now opaque) first-load pop-up. | #1023 | 13-06 '26 |
| D-62.71 | **Fun-fact card layout**: header "Did you know?" → **"💡 Food fact"**; footer collapses to one 3-column row — `••• Finding eateries…` · `Source: …` · `🛑 Stop` (Stop moved up from its own line). Fact-selection logic untouched. | Operator layout spec ("don't change the logic"). | #1023 | 13-06 '26 |
| D-62.72 | **Drop the rating-reset body sub-line** ("Showing eateries with generally good Google ratings.") on the loading overlay — title-only. The standalone bottom rating-reminder toast keeps its body line. | Operator: "Want it removed". | #1023 | 13-06 '26 |
| D-62.73 | **Loading-overlay text left-justified; 🛑 Stop right-justified.** Card `text-center` → `text-left`; "Finding eateries…" row `justify-center` → `justify-start`; Stop pill keeps `justify-end`. | Operator: "It has to be left justified / The stop search is right justified". | #1024 | 13-06 '26 |
| D-62.74 | **Australian / New-Zealand relevance gate.** `australian` + `new-zealand` added to `GEMINI_CACHE_KEYS` + `durian-gemini-verifier.js` `PROMPT_CONFIG` (prompts explicitly reject the generic-Western / steakhouse / Iberico-deli false positive). | An "Australian" search in Putrajaya returned a Spanish/Iberico "Southern European Deli" (Cheras) — australian had no D703e gate. | #1027 | 13-06 '26 |
| D-62.75 | **OTHER geofence is FAIL-CLOSED + floor-free.** The OTHER branch (and JB→OTHER fallback) of `cuisine-geo-scope.js` now requires a **finite, in-range** `distanceM` (was `withinCap = distanceM==null \|\| ≤cap`, fail-open) and applies **no never-empty floor** — an honest empty beats a cross-country result. SG/JB keep the permissive `withinCap` + floor. | A "Russian" search in Putrajaya returned **Kapitan Restaurant, Singapore (~330 km)** — it reached the cap coordless (`distanceM=null`) and the floor would resurface it. | #1028 | 13-06 '26 |
| D-62.76 | **`enrichSlow` per-phase timing (D707).** A one-line breakdown `[Cuisine-Enrich] D707 enrichSlow timings (Nv): crowd=… translate=… reviewCache=… dishes=… finalise=… travel=… sanctuary=… footfall=… total=…ms`. Instrumentation only. | Diagnose the 20s "Load failed" before restructuring (operator chose "add timing first"). | #1029 | 13-06 '26 |

---

## Completed in this arc (v0.62.70 → v0.62.76, PRs #1023 → #1029)

- **Loading / fun-fact overlay polish** — opaque first-load card (v0.62.69) → "Finding eateries" + blinking ellipsis + "Stop search" copy (D-62.70) → "Food fact" single-row footer (D-62.71) → drop the rating-reset body line (D-62.72) → left-justify text (D-62.73). End state: a clean, consistent, left-aligned busy indicator with a right-flush Stop.
- **Cuisine relevance & geofence hardening** — Australian/NZ relevance gate (D-62.74) + OTHER geofence fail-closed against coordless cross-country leaks (D-62.75). End state: foreign-city searches no longer surface wrong-cuisine or wrong-country venues; honest "none nearby" when genuinely empty. Verified live in the operator's logs (Russian gate dropped 6/7; 15 km cap `cap_m:15000`; AI credits restored).
- **Doc-system + vault** — VibeCodingRecord caught up to #1022 then #1029; Register/Technical v0.62.69 + the v0.61.301→v0.62.52 backfill rollups; vault/v0.62.69 (this snapshot adds vault/v0.62.76).
- **Performance diagnosis opened** — enrichSlow D707 timing (D-62.76) instruments the 20s "Load failed"; the targeted fix is O-45.

---

## Amendments

- First Register snapshot since `register-0_62_69-13_06_26-1545.md`. Appends D-62.70 → D-62.76 and Open items O-45 → O-48; prior ledgers unchanged.

## Reference

- Prior incremental: `doc/Register/register-0_62_69-13_06_26-1545.md`
- Backfill rollups: `doc/Register/register-0_62_52-13_06_26-1830.md` (+ technical / feature `-0_62_52`)
- Arc journal: `doc/Journal/journal-0_62_53-13_06_26-0610.md` (amendments № 736–753)
- Companion Technical: `doc/Technical/technical-0_62_76-14_06_26-1005.md`
- VibeCodingRecord (regenerated to #1029): `doc/VibeCodingRecord/vibe-coding-record.md` + hosted `public/doc/vibe-journal.html`
- Vault snapshot: `vault/v0.62.76/`
