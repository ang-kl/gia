(№ 132 - 17-07 '26 01:50 SGT)

# Register — soleat / Gia4lunch v0.62.543 (Distance-ring overlay arc + map pin colours)

> **Document type:** Register (per `doc/CLAUDE.md` §3; §4.1 — items move between sections, never deleted).
> **Continues** `register-0_62_526-10_07_26-1345.md` (№ 131). **Scope:** the v0.62.537 → v0.62.543 map-UI
> arc (distance-ring overlay on Hawker + Cuisine TMAs, the Cuisine result-card white-on-white fix, and the
> result-pin colour-coding), plus the two open operator-decisions it surfaced and one deferred layout item.
> **Reflects:** project state at **v0.62.543, 17-07 '26 (HEAD `55f0606`)**.
> **Targeted addition** — does not restate the live O-/D-/DF- items from № 130/№ 131, which remain as recorded.

---

## Completed (new this entry)

| # | Item | Shipped | PR(s) |
| :--- | :--- | :--- | :--- |
| C-30 | **Distance-ring overlay** — dashed concentric rings on the Hawker + Cuisine TMA maps, centred on the chosen hawker (Hawker) / set-location (Cuisine). Ring 1 = 🚶 750 m walk; in SG ring 2 = 🚆 computed 2-MRT-stops + ring 3 = 🚆 capped "reach" (only when a result is beyond ring 2). Self-contained shared helper `web/_shared/lib/distance-rings.js` (Polyline circles + dash symbol; one copy both TMAs import — sidesteps the mapOverlays duplication). | v0.62.537–538 | #1548, #1549 |
| C-31 | **Cuisine result-card name white-on-white fix** — the focused horizontal card's name row inherited the app-root's light colour over its forced-white background (bug since v0.62.285). Added `text-tg-text` to the name row + Maps/Copy buttons. | v0.62.539 | #1550 |
| C-32 | **Region-gated rings** — outside SG (no MRT network / SG-only station list) rings 2 + 3 become plain distance rings (2 km fixed + farthest-result, no glyph); ring 3 draws only when a result falls outside ring 2. Also closes the SG–JB-border edge case (a JB anchor near an SG station no longer borrows a train ring). | v0.62.540–541 | #1550 |
| C-33 | **Result-pin colour-coding** (Cuisine) — closed → grey `#8E8E93`, closing within the hour → pink `#DB2777`, Michelin star/Bib → red `#C6282D`, open → green `#34C759`. Precedence grey > pink > red > green. | v0.62.542 | #1551 |
| C-34 | **Non-SG ring-3 auto-frame + 4-side labels + km/miles** — a new non-SG search fit-bounds to the (uncapped, farthest-result) ring 3 so all three rings show; a ring wider than 8 km carries its distance label on all four sides (N/E/S/W); labels are unit-aware (miles for US/GB/MM/LR, km elsewhere), keyed to the **searched** country (`state.countryPref`). | v0.62.542–543 | #1551 |

---

## Open — awaiting operator confirmation (new this entry)

| # | Item | Detail | Default in place |
| :--- | :--- | :--- | :--- |
| O-52 | **Pin-colour precedence** | Current order is grey (closed) > pink (closing-soon) > red (Michelin) > green (open) — so a **closed or closing-soon Michelin** venue shows grey/pink, **not** red. Availability/urgency was chosen to win over prestige; the card still shows ✳️ Michelin + the Closed/Closing tab. | grey > pink > red > green (v0.62.542). One-line flip in `pinColor()` if Michelin red should win. |
| O-53 | **Imperial-unit country set** | `MILES_COUNTRIES = {US, GB, MM, LR}` (road-distance imperial). Everywhere else (incl. AU, SG) = km. Tunable — e.g. add Bahamas/Belize, or drop GB if UK-metric is preferred. | `{US, GB, MM, LR}` (v0.62.543) in `MapPanel.jsx`. |

---

## Deferred (new this entry)

| # | Item | Reason | Reactivate when |
| :--- | :--- | :--- | :--- |
| DF-6 | **Cuisine `ResultDrawer` left-peek/overlap** | Operator (IMG_0665/0666) saw a black bar over the focused card's left edge — the peeking neighbour (translucent glass over the dark monochrome map) reads as near-black and visually clips the card's left column. The white-on-white *text* bug (C-31) is fixed and separate; this is a carousel layout/overlap issue in `ResultDrawer.jsx`, not touched by the ring work. | Operator asks for the layout fix (offered; not yet greenlit). |

---

## Decisions (append-only)

| # | Decision | Rationale | Date |
|---|---|---|---|
| D-133 | **Distance-ring "2 MRT stops away" radius is COMPUTED from the operational station list, not a fixed nominal**; the outer non-SG "reach" ring sits at the **farthest result** (bounded by the results, only when outside the 2 km ring); rings are **dashed** and the walk ring is **750 m**. | Operator picks via AskUserQuestion across v0.62.537–541 (computed / dashed / 750 m; then "keep at farthest result + auto-frame"). | 15–16-07 '26 (#1548–#1551) |
| D-134 | **`vault/` snapshot mechanism stays RETIRED** — no re-committed `vault/v<N>/` dir. Each git commit sha is already a full source tree; `vault/` is `.gitignored` since v0.62.508 (#1514, 27 snapshots removed). The effective "v0.62.543 snapshot" is HEAD `55f0606`, recoverable via `git show 55f0606:<path>`. | Re-affirmed on this "vault, document update" request — creating a committed vault would re-introduce the redundancy #1514 removed. Doc-update proceeds via the eight templates + VCR instead. | 17-07 '26 |

---

## Amendments

- **AU-1:** Continues `register-0_62_526-10_07_26-1345.md` (№ 131). Appends Completed C-30…C-34, Open
  O-52/O-53, Deferred DF-6, and Decisions D-133/D-134. No prior items amended, closed, or removed — the
  DF-4/DF-5 (`ms`/`vi` locales), D-132, and the earlier O-47…O-51 / D-A1…D-A8 lines remain live as recorded.

## Reference

- Prior: `doc/Register/register-0_62_526-10_07_26-1345.md` (№ 131)
- Journals: `journal-0_62_537-15_07_26-0821.md` … `journal-0_62_543-16_07_26-1235.md` (#893–#899)
- Feature/Technical: `doc/Feature/feature-0_62_543-17_07_26-0150.md`,
  `doc/Technical/technical-0_62_543-17_07_26-0150.md` (this pass)
- Vault retirement: `.gitignore` (v0.62.508 note), PR #1514
