# Register — v0.62.924 · 03-09 '26 11:50 SGT

> Incremental snapshot. The previous one is `register-0_62_69-13_06_26-1545.md`, dated
> **13-06 '26** — **855 patch versions** and nearly three months earlier. This file does not
> pretend to close that gap; it records what is **verifiable today**, measures what is not, and
> installs the check that makes the next gap fail loudly instead of accumulating in silence.

---

## ⚠ What this snapshot does not do, stated first

`register-0_62_69` recorded open items **O-37 → O-44** and decisions **D-62.53 → D-62.69**.
Measured against the repo today:

| | last snapshot | today | source of the measurement |
| :--- | ---: | ---: | :--- |
| distinct `O-` ids referenced anywhere | ~8 | **200** (max **O-349**) | `__tests__/`, root `*.js`, `doc/Journal/` |
| distinct `D-` ids referenced anywhere | ~17 | **78** (max **D-209**) | same, plus `CLAUDE.md` |
| `O-` ids with a **live test** | — | **40**, across **34** test files | `__tests__/` |
| `O-` ids referenced in shipped code | — | **42** | root `*.js`, `web/**` |

**Roughly 155 open items and 60 decisions were opened, and in many cases closed, without ever
reaching this folder.** Reconstructing them from ~200 journal entries would mean assigning
statuses I cannot verify, and **a Register that invents a status is worse than one that is
merely late** — the stale file at least announces its own date. So they are named here as an
unreconstructed backlog with a measurement attached, and the sections below carry only what can
be checked by running something.

---

## Open Items — the 40 with a live guard

Every row below is an item some test in this repo names. That is the whole selection rule: if a
test references the id, the item is real and its guard can be run. Items whose only trace is a
journal paragraph are in the backlog above, not here.

| # | Guarded by |
| :--- | :--- |
| O-22 | `parse-address-components.test.js` |
| O-91 | `michelin-sort.test.js` |
| O-93 | `temp-pin.test.js` |
| O-103, O-104 | `ios-input-zoom-guard.test.js` |
| O-124 | `cuisine-michelin-hash.test.js` |
| O-130 | `z-stack-registry.test.js` |
| O-131 | `cuisine-michelin-years.test.js` |
| O-144 | `clip-store.test.js` |
| O-185 | `owner-gate.test.js` |
| O-187, O-189, O-191 | `i18n-coverage.test.js` |
| O-192 | `copy-all-html.test.js` |
| O-208 | `michelin-roster-policy.test.js` |
| O-215 | `count-interactions-scope.test.js` |
| O-219 | `prepush-guard.test.js` |
| O-228, O-331 | `michelin-paris-cuisine.test.js` |
| O-305, O-335 | `hours-locale-tier1.test.js` |
| O-307, O-315, O-317, O-330 | `classics-city-plates-i18n.test.js` |
| O-316, O-344 | `hawker-names-i18n.test.js` |
| O-318 | `classics-provenance-contradiction.test.js` |
| O-320, O-329 | `mrt-stations-i18n.test.js` |
| O-321 | `transport-i18n-locale-blocks.test.js` |
| O-323, O-327 | `tma-i18n-coverage.test.js` |
| O-324, O-326 | `vibe-journal-feature-area.test.js` |
| O-336 | `locale-coverage-and-address.test.js` |
| O-337 | `prompt-locale.test.js` |
| O-338 | `michelin-dish-forcefill.test.js` |
| O-345 | `locale-hydration-gate.test.js` |
| O-348 | `pronounce-endpoint.test.js` |

## Deferred / not settled by any test

| # | Item | Why it is here rather than Open |
| :--- | :--- | :--- |
| R-1 | **Nobody has looked at anything rendered across #1846–#1857.** Twelve PRs. | No test can settle it. Named in six journal entries. |
| R-2 | **The dish taxonomy is complete (1,598 hand-authored rows) and unreviewed by a human.** | Every guard checks consistency; none checks whether a native speaker agrees. |
| R-3 | **6,591 Korean cells and 72 Korean fun-fact bodies, no native speaker has read any.** | Same shape as R-2, recorded from `[AMD-131]` onward. |
| R-4 | `log/` carries 50 `[set-location]` lines with GPS at ~11 m precision. | Offered to the operator in #1845; no answer, and not to be acted on unasked. |
| R-5 | The 111 hawker Street View links are a third party's. | They resolve today (measured, all 123). If Google retires `goo.gl/maps` the button breaks visibly. |

## Decisions in force — the four `CLAUDE.md` pins

Selection rule, again a checkable one: these are the decisions the repo's own orchestration file
carries with the operator's verbatim approval and a journal reference. Every other `D-` id in the
78 lives in prose only.

| # | Decision | Where it is stated |
| :--- | :--- | :--- |
| D-123 | Verify a merge **by content, from the merged tree**. | `CLAUDE.md`, and every `[AMD-…]` merge record. |
| D-199 | Repo state is evidence about the **past**, not the present. An invariant checked against a comment or a prior entry is *Not Verifiable*, not *Passed*. | `CLAUDE.md` § standing rules. |
| D-203 | An anchor is `max(sensor reading, latest known event time)`, computed with a deterministic tool. | `CLAUDE.md`, approved 22-08 '26. |
| D-205 | Merge records **fold forward**; never open a PR whose only content is one. | `CLAUDE.md`, approved 22-08 '26. |

## ⚠ Owed records — the home D-205 rule 3 never had

D-205's rule 3 says a held merge record with no successor PR must be logged as *still due* in the
Journal's `[KNOWN GAPS]` **and in `doc/Register/`**. The second half has never been executable:
`Register.md` is an empty template and the newest snapshot predated the rule by two months. This
section is that home.

| Record | For | Status |
| :--- | :--- | :--- |
| `[AMD-195]` | #1854 | Carried by #1855. Was held with no successor for one release; a *stale check-in* happened to produce a carrier. |
| `[AMD-197]` | #1855 | Carried by #1856. |
| `[AMD-200]` | #1856 | Carried by #1857. |
| `[AMD-201]` | #1857 | Carried by this PR. |
| `[AMD-203]` | #1857's merge | Carried by this PR — **the first in four not to need the still-due treatment**, because a substantive PR was already in flight. |

**Three held records in one arc, and not one of them was carried by a mechanism.** Two rode a
request the operator happened to make; one rode a stale trigger. That is D-205's cost, and it is
recorded as a cost rather than as three routine notes.

## Completed in this arc (v0.62.798 → v0.62.923, PRs #1845 → #1857)

- **Korean reaches every surface** — 1,612 dish names, 1,684 + 1,670 dish notes, 6,591 cells,
  126 command descriptions, 66 tourist explainers, 72 fun-fact bodies, and the locale flip.
- **The dish-taxonomy backfill closes** — 99 → **1,697 of 1,697**, nine batches, 1,598 rows.
- **Singapore place composition** — 75-term vocabulary, 195 proper nouns, the harvester promoted
  into the repo with its three defects fixed.
- **The hawker card's plumbing gaps** — `marketStalls`, `description_myenv`, 204 closure windows,
  the nine-centre join, and now the Street View link.
- **`/forgetme` erases what it promised** — 7 of 29 keys before, all of them after.
- **The taste scorer** — graph, context, two-stage scoring, weather and period terms.

## Rule reference

- Items move between sections; nothing is deleted (§4.1).
- This file is append-only in the AU-1 sense: a later snapshot corrects it **by standing next to
  it**, never by editing it.

## Reference

- Previous snapshot: `doc/Register/register-0_62_69-13_06_26-1545.md`
- Arc journal: `doc/Journal/journal-0_62_798-26_08_26-2019.md`
- Staleness guard: `__tests__/register-current.test.js`
- Orchestration and the four pinned decisions: `CLAUDE.md`
