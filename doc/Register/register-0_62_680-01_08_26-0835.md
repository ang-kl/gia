(№ 158 - 01-08 '26 08:35 SGT)

# Register — soleat / Gia4lunch v0.62.680 (O-89 confirmed, O-91 SORT + O-92 resolved)

> **Document type:** Register (per `doc/CLAUDE.md` §3; §4.1 — items move between sections, never deleted).
> **Continues** `register-0_62_679-31_07_26-2206.md` (№ 157). **Scope:** O-89 sign-off, O-91 SORT
> half, O-92 fix. O-88 was presented/discussed but not resolved this entry.
> **Reflects:** project state at **v0.62.680**. **Targeted addition.**

---

## Completed (new this entry)

| # | Item | Shipped | PR(s) |
| :--- | :--- | :--- | :--- |
| C-92 | **O-91 SORT half resolved** — built `michelin-sort.js` (`sortMichelinPool`), implementing `instr/GIA_Michelin_Footer_Pagination_AI_Prompt.md` §2's deterministic order (newest applicable SELECTED award year → Michelin category rank 3★/2★/1★/Bib → alphabetical fallback). Wired into `index.js`'s `handleMichelinSearch`, replacing the old `TIER_ORDER`-sort-plus-Fisher-Yates-shuffled-Bib pipeline (the shuffle only existed to vary bib exposure across taps — the spec's determinism supersedes it). 9 new tests in `__tests__/michelin-sort.test.js`. | v0.62.680 | pending |
| C-93 | **O-92 resolved** — root cause: the Michelin pager's `‹`/`›` buttons used the omnidirectional `.gia-hit` touch-target class (8px invisible extension on every side), reaching horizontally into the adjacent Back/End button's grid cell in the same slim footer row. Switched to `.gia-hit-y` (`styles.css`'s own documented variant for "tightly-packed rows" — 8px vertical / 2px horizontal), per the P1-f convention already in the codebase. Zero visible-pixel change. | v0.62.680 | pending |

## Decisions (new this entry)

| # | Decision | Rationale | Where recorded |
| :--- | :--- | :--- | :--- |
| D-49 | **O-89 confirmed as a standing decision**: M3's literal type scale and literal elevation-shadow values are permanent exclusions from any future M3 pass — will not be added to `m3-tokens.js` on the strength of an audit alone; reopening either needs its own explicit sign-off. | Operator: "Build O-89" (treated as sign-off on the O-89 recommendation first raised in `register-0_62_671-31_07_26-0815.md`). | `m3-tokens.js` (new comment block after `m3TouchTarget`); Journal `journal-0_62_680-01_08_26-0835.md`. |

## Open (carried forward, unchanged this entry)

| # | Item | Note |
| :--- | :--- | :--- |
| O-88 | M3 audit Tier 2 per-item decision round — presented/discussed in-chat this session per the operator's "show me where is O-88," but no per-item answers were given. Still awaits presentation-and-answer as a formal round. Unchanged. |
| O-99 | O-98's "no separate compact-phone template" recommendation — still no concrete wrap-risk component list to act on. Unchanged. |
| O-100 | O-96's fix is verified by construction/code-comparison only — real-device recheck still the only way to close it out. Unchanged. |
| O-93 | No React component render-test infrastructure. Unchanged — directly relevant to O-100 above and to O-92's own construction-only verification this entry. |

## Note

O-91 and O-92 move to Completed in full this entry — O-91's FILTER half was already resolved by
O-94/C-82, and this entry resolves the SORT half exactly per the original (already-approved) spec,
so O-91 itself is now fully closed (not carried forward as "open, narrowed" any further). O-89
moves from Open to Decisions — it was a recommendation awaiting sign-off, not an action item, so it
lands in Decisions (D-49) rather than Completed (C-xx), matching how O-85's colour-decision round
was recorded in Phase 2b. O-88 stays open and unchanged: displaying it in chat on request is not the
same as running its decision round, which still needs the operator's per-item answers the way O-85's
own round did before it could resolve.
