(№ 139 - 30-07 '26 11:21 SGT)

# Register — soleat / Gia4lunch v0.62.661 (landscape-iPhone static split, shipped across Cuisine/Hawker/Transport)

> **Document type:** Register (per `doc/CLAUDE.md` §3; §4.1 — items move between sections, never deleted).
> **Continues** `register-0_62_660-30_07_26-1027.md` (№ 138). **Scope:** the confirmed landscape-iPhone
> list-mode revert. **Reflects:** project state at **v0.62.661**.
> **Targeted addition** — restates only the items this entry touches; O-66/O-67 (from № 138) close here;
> O-63–O-65 (from № 137) remain as recorded there.

---

## Completed (new this entry)

| # | Item | Shipped | PR(s) |
| :--- | :--- | :--- | :--- |
| C-61 | **iPhone landscape + list mode now uses a static split** (map anchored on top, list scrolling independently below, no over-the-map drawer) in all three map TMAs — Cuisine, Hawker, Transport. Every other device/orientation is unchanged. | v0.62.661 | #1668 |

## Open — items closed this entry

| # | Item | Disposition |
| :--- | :--- | :--- |
| O-66 | ~~Landscape-iPhone list mode: recommended, not built~~ | **CLOSED → C-61 above.** Operator confirmed the recommendation ("please to revert back"); built across all three apps. |

## Open (new this entry)

| # | Item | Note |
| :--- | :--- | :--- |
| O-67 | **Carried forward, unchanged.** The three device-scaled friction values (0.35/0.28/0.21, v0.62.660) are still not device-verified. |
| O-68 | **Cuisine's portrait-tablet `frameHeight` has been dead code since v0.62.655.** Discovered while building C-61: `MapPanel`'s `fill` prop was made unconditionally `true` for every device at v0.62.655, but `frameHeight` (the compact ~40vh framed-map override) only takes effect when `!fill` — so portrait tablets have actually been getting the full-bleed-behind-drawer treatment, identical to every other device, for two weeks, despite in-code comments describing a "compact framed map" that hasn't been reachable. Not fixed — no operator report against current tablet behaviour, and this entry's mandate was landscape-phone specifically. Needs an explicit go-ahead before changing a currently-shipped tablet layout. |
| O-69 | **The map expand/collapse button's `fill`-branch logic goes from dead to live for landscape phones.** Same `MapPanel` code (`if (fill) onCollapse() else onExpandFull()`) that O-68 shows has been effectively single-branch for tablets (fill never varied) now genuinely branches for the new landscape-phone case (fill does vary there). Should behave as originally designed; flagged because it's new-territory code path, not because a specific defect is known. |

## Note

Rides on the still-open **#1668** (v0.62.659/660/661 all on one PR/branch, since the operator's follow-up
messages arrived before that PR was reviewed or merged).
