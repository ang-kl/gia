(№ 157 - 31-07 '26 22:06 SGT)

# Register — soleat / Gia4lunch v0.62.679 (O-95/O-96/O-97 resolved)

> **Document type:** Register (per `doc/CLAUDE.md` §3; §4.1 — items move between sections, never deleted).
> **Continues** `register-0_62_678-31_07_26-1853.md` (№ 156). **Scope:** O-95/O-96/O-97 fixes,
> O-98 recommendation.
> **Reflects:** project state at **v0.62.679**. **Targeted addition.**

---

## Completed (new this entry)

| # | Item | Shipped | PR(s) |
| :--- | :--- | :--- | :--- |
| C-89 | **O-95 resolved** — Hawker's "📋 Copy" no longer closes the WebApp on success; shows "✓ Sent" for 3s per card (new `sentNames` Set + `card.sent` i18n key, 8 locales), matching Cuisine's copy UX exactly. | v0.62.679 | pending |
| C-90 | **O-96 resolved** — root cause found and fixed: Hawker's `CentreCarousel` and Transport's `StationCarousel` drove `glass` purely off IntersectionObserver on every device, unlike Cuisine's `ResultDrawer.jsx` which bypasses IO on phone in favour of scroll-driven `getBoundingClientRect()` geometry detection (cannot get "stuck," unlike IO callback timing in Telegram's WebView during scroll-snap). Ported Cuisine's dual-mode pattern (IO on tablet/desktop, geometry on phone) into both carousels via a new `isWide` prop. | v0.62.679 | pending |
| C-91 | **O-97 resolved** — Hawker's header title unified to 16px across all 3 responsive layouts (was 14/16px split). Hawker's centre-card name and Transport's station-card name now use the same `isCompact`-responsive rule (11px compact phone / 12px else) Phase C already applied to Cuisine's category-grid label — Transport's `StationCard` gained a new `isCompact` prop. | v0.62.679 | pending |

## Open (new this entry)

| # | Item | Note |
| :--- | :--- | :--- |
| O-99 | **O-98's "no separate compact-phone template" recommendation** was presented in the plan and not objected to, but has no concrete wrap-risk component list to act on yet. If/when a specific wrap-risk list is scoped (chip rows, footer clusters, truncated names — the still-open O-98 predecessor concern), build against this recommendation rather than a new template. |
| O-100 | **O-96's fix is verified by construction/code-comparison only** — no render-test harness exists (O-93) to reproduce the original scroll-snap/IntersectionObserver timing bug or confirm the geometry-based fix live. Real-device recheck (Hawker + Train carousels, does glass correctly show on peeking cards now) is the only way to close this out with confidence. |

## Open (carried forward, unchanged this entry)

| # | Item | Note |
| :--- | :--- | :--- |
| O-88 | M3 audit Tier 2 per-item decision round — still awaits presentation to the operator. Unchanged. |
| O-89 | Non-adoption recommendation (M3 literal type scale + literal elevation shadows) — still awaits sign-off. Unchanged. |
| O-91 | Year-filter chips + client-side year/category sort (Michelin) — SORT half stays open. Unchanged. |
| O-92 | Back/End button cursor overlap with the Michelin pager. Unchanged. |
| O-93 | No React component render-test infrastructure. Unchanged — directly relevant to O-100 above. |

## Note

O-95, O-96, and O-97 were all direct, unambiguous operator answers from one message and are fully
resolved this entry. O-98 got a recommendation (adopted implicitly by proceeding with O-97's
inline-class approach rather than a new template) but has no concrete follow-up list yet — tracked
as O-99 rather than closed, since "no separate template" is a stance, not a completed action.
