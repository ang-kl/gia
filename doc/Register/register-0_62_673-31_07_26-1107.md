(№ 151 - 31-07 '26 11:07 SGT)

# Register — soleat / Gia4lunch v0.62.673 (Michelin footer-centre pagination)

> **Document type:** Register (per `doc/CLAUDE.md` §3; §4.1 — items move between sections, never deleted).
> **Continues** `register-0_62_672-31_07_26-0842.md` (№ 150). **Scope:** the Michelin pagination feature.
> **Reflects:** project state at **v0.62.673**. **Targeted addition.**

---

## Completed (new this entry)

| # | Item | Shipped | PR(s) |
| :--- | :--- | :--- | :--- |
| C-78 | **Michelin footer-centre pagination** — `‹ N / total ›` indicator, batch-based ("page" = already-fetched server batch, per operator decision), reusing the app's existing `pages`/`cursor`/`runSearch` machinery with zero new fetch mechanism. Footer row 1 converted to a true 3-column grid for correct centring. Redundant "Next list" button hidden specifically when the new pager is showing. | v0.62.673 | pending |

## Open (new this entry)

| # | Item | Note |
| :--- | :--- | :--- |
| O-91 | **Year-filter chips + client-side year/category sort** (instruction spec §1-§2) were NOT built this pass — the operator's clarifying message scoped v0.62.673 to the pagination mechanism only. Awaiting a decision on whether these are wanted as a follow-up, and if so whether they should filter/sort within the current batch only (the only architecturally consistent option, since a complete Michelin result set is never fetched client-side) or something else. |
| O-92 | **Back/End button (footer right) can also step the Michelin pager's cursor backward**, overlapping with the new centre pager's `‹`. Deliberately left untouched (see D-48) rather than risk that control's separate app-close-on-oldest-page behaviour. Revisit if the operator finds the overlap confusing in practice. |
| O-93 | **No React component render-test infrastructure exists anywhere in this repo** (confirmed repo-wide during this feature's verification — zero `@testing-library/react`/`render(` usage across all 152 test files). This blocks writing genuine tests for any interactive Cuisine-TMA UI behaviour, not just this feature. Introducing one is a separate, standalone infrastructure decision — not bundled into this entry. |

## Decisions (new this entry)

| # | Decision | Rationale | Where recorded |
| :--- | :--- | :--- | :--- |
| D-48 | **"Page" = an already-fetched server batch**, not a client-side slice of a hypothetical complete Michelin result set. The existing "Next list"/cursor-history mechanism is reused and extended (fresh-fetch at the cache tip) rather than building a new fetch-everything-then-paginate model. | Operator, resolving the pre-implementation conflict directly: "'page' is already-fetched server batch. I need to just show then tap to next page to fetch." | Journal `journal-0_62_673-31_07_26-1107.md` |

## Note

This feature arrived as a direct, fully-specified instruction (`instr/GIA_Michelin_Footer_Pagination_AI_Prompt.md`,
"Do this") rather than an open design question — once the one genuine architectural fork was resolved,
it proceeded through the standard verify → PR → auto-merge pipeline, not the M3 Tier-2 hold-for-preview
posture (reserved for still-undecided visible changes).
