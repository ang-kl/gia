(№ 134 - 20-07 '26 08:42 SGT)

# Register — soleat / Gia4lunch v0.62.613 (pre-recode checkpoint: Transport TMA port + Cuisine/Hawker tablet + iPad-mini responsive arc)

> **Document type:** Register (per `doc/CLAUDE.md` §3; §4.1 — items move between sections, never deleted).
> **Continues** `register-0_62_560-17_07_26-0936.md` (№ 133). **Scope:** the v0.62.561 → v0.62.613 arc
> (PRs #1578–#1621) — the Transport (Train) TMA responsive port + rich StationCard, the Google-Maps-style
> draggable **BottomSheet drawer** (shared), the Cuisine/Hawker tablet fixes, and the physical-screen
> tablet-detection + **iPad-mini gate** fixes. Recorded as the **checkpoint before the code-weight recode**.
> **Reflects:** project state at **v0.62.613** (checkpoint tag **`vault-v0.62.613`**).
> **Targeted addition** — does not restate live O-/D-/DF- items from № 130–№ 133, which remain as recorded.

---

## Completed (new this entry)

| # | Item | Shipped | PR(s) |
| :--- | :--- | :--- | :--- |
| C-41 | **O-54 delivered — responsive pattern ported to Cuisine + Transport.** Cuisine tablet/desktop two-panel + carousel, shared `useViewport`, picker phone-width cap, portrait two-panel with frozen header + independent columns. | v0.62.561–594 | #1578–#1603 |
| C-42 | **Transport (Train) TMA responsive port** — header pills moved into the card, rich Google-Maps-style `StationCard` (first/last train, crowd, amenities), carousel-first default across devices, status/source popups, live clock. | v0.62.597–606 | #1606–#1614 |
| C-43 | **Shared Google-Maps-style draggable `BottomSheet` drawer** — Hawker phone drawer then Train phone drawer; `web/_shared/components/BottomSheet.jsx`; `disableVerticalSwipes()` so the drawer drag doesn't dismiss the Mini App. | v0.62.608–610 | #1616, #1617, #1618 |
| C-44 | **Train 2K carousel** uniform + centered; **Hawker NEA ↗** moved off the header into the footer (own row, plain text). | v0.62.611, 613 | #1619, #1621 |
| C-45 | **Physical-screen tablet detection** — `readViewport` falls back to `window.screen` short edge so a tablet whose Telegram webview is a partial-height sheet stops "reverting" to the phone layout. | v0.62.612 | #1620 |

---

## Open — awaiting operator go (new this entry)

| # | Item | Detail | Default in place |
| :--- | :--- | :--- | :--- |
| O-55 | **Code-weight recode / de-duplication** | Operator asked whether the TMA code is too heavy. Measured: cuisine `App.jsx` 5,456 lines (34% comments), 2,807 `vX.Y.Z` changelog annotations repo-wide, cross-TMA duplication (`tg.js ×4`, `i18n.js ×4`, `LocaleToggle ×4`, `BackFab ×4`, `mapOverlays ×3`, data files ×2). **Comments do NOT ship** (esbuild strips them) — this is a maintainability, not runtime, issue. **Plan (slices, low-risk first):** A) consolidate pure data/util duplicates → `_shared/`; B) small shared UI (`LocaleToggle`/`BackFab`/`MapControls`/`tg.js` core); C) unify `mapOverlays.js` (×3, diverged); D) split cuisine `App.jsx` (last, incremental); E) comment hygiene (operator policy call — cuts against the traceability culture). | Nothing changed yet. **Recode starts only after this checkpoint (`vault-v0.62.613`) is tagged.** Recommended start: **slice A**. |

---

## Decisions (append-only)

| # | Decision | Rationale | Date |
|---|---|---|---|
| D-136 | **Tablet gate keys off the physical screen, threshold `TABLET_MIN_EDGE = 700`.** A coarse-pointer device whose `window.screen` short edge ≥ 700 is a tablet regardless of the transient Telegram webview size. 700 covers the 744 px iPad mini and stays above the largest phones (~480 px). | v0.62.612 keyed detection to `window.screen` but gated at 768; the iPad mini is 744 → fell through to phone. 700 fixes the mini without promoting phones. | 20-07 '26 (#1620, #1621) |
| D-137 | **Pre-recode checkpoint = the git tag `vault-v0.62.613`, not a committed `vault/` folder.** `vault/` was de-tracked at v0.62.508 (#1514) because git history already is a full-tree snapshot; the tag names the exact known-good tree as the recode rollback target (`git archive vault-v0.62.613`). | A git-ignored `vault/` folder in an ephemeral container can't be durable; a pushed tag is. Matches the post-v0.62.508 convention. | 20-07 '26 (this entry) |

---

## Carried (unchanged, still live)

- **DF-7** Hawker stall-count coverage (37/122); **DF-8** Hawker CLEANING-closure (K.I.V.); **DF-9** Q4 carpark-closes-card repro — all as recorded in № 133.
- **UNVERIFIED on real device** (sandbox has no Telegram client): v0.62.610 `disableVerticalSwipes` drawer-drag; v0.62.613 iPad-mini tablet layout. Both await an operator eyeball.
- Not addressed (not reported): Telegram-Desktop narrow-window and tablet-with-mouse (fine-pointer) device classes.
