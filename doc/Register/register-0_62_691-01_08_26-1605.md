(№ 167 - 01-08 '26 16:05 SGT)

# Register — soleat / Gia4lunch v0.62.691 (carousel card line spacing + the spec audit)

> **Document type:** Register (per `doc/CLAUDE.md` §3; §4.1 — items move between sections, never deleted).
> **Continues** `register-0_62_690a-01_08_26-1540.md` (№ 166). **Scope:** the operator-requested
> carousel-card spec audit, the line-spacing reduction it produced, and four findings deliberately
> reported rather than built.
> **Reflects:** project state at **v0.62.691**. **Targeted addition.**

---

## Completed (new this entry)

| # | Item | Shipped | PR(s) |
| :--- | :--- | :--- | :--- |
| C-120 | **Carousel-card line spacing normalised.** Six collapsed rows carried no `leading-*` class and silently inherited Tailwind preflight's `1.5`, against 1.25/1.375 on the rows that had been styled — three line-heights on one card, by accident. One `leading-snug` on the horizontal card ROOT fixes all six; explicit per-row values still win on specificity. | v0.62.691 | #1687 |
| C-121 | **The `mt-*` that doubled the flex gap removed** on the horizontal card (`gap-0.5` already separates every row). 💵 price `mt-0.5`→0, 🍲 Try / Michelin / address `mt-1`→`mt-0.5`, toggle `mt-1.5`→`mt-1`. Every trim gated on `horizontal`; the vertical list is byte-identical. | v0.62.691 | #1687 |
| C-122 | **Standard tier `h-[13rem]` → `h-[12rem]`** (−16px, −7.7%). Typical-card content 178 → 165px; worst-case overrun 44 → 41px. | v0.62.691 | #1687 |

## Decisions (new this entry)

| # | Decision | Rationale |
| :--- | :--- | :--- |
| D-59 | **Vertical rhythm is set on the card root, not per row.** The defect was six rows inheriting `1.5` because nobody gave them a class. Fixing them one by one would have left the same trap for the seventh row added later; a parent default makes the rule explicit and lets deliberate per-row values override it. | v0.62.691 |
| D-60 | **`leading-snug` (1.375), not `leading-tight` (1.25).** Tight buys ~6px more, but the app ships 8 locales including zh/ja and 1.25 risks clipping full-height CJK glyphs. Legibility across every locale outranks 6px on one. | v0.62.691 |
| D-61 | **12rem is the floor for a spacing-only reduction.** It is the largest cut that leaves every case no worse than before: at 11.5rem the worst-case card overruns by 49px against today's 44px, i.e. the "improvement" would make the bad case worse. Going below 12rem requires removing a row, not tightening space. | v0.62.691 |

## Open (new this entry)

| # | Item | Note |
| :--- | :--- | :--- |
| O-115 | **The SHORT tier is already over-subscribed.** Arithmetic puts ~156px of content in the 120px (`7.5rem`) box **today** — an overrun of ~36px, which can clip the ▸ toggle off entirely, leaving no way to expand the card on phone landscape. v0.62.691 improves it to ~26px over but does not resolve it, and the tier was deliberately **not** shrunk. Needs more height or fewer rows — a spec decision. **Awaiting operator.** |
| O-116 | **The `≈ A$…` conversion row orphans on the short tier.** The 💵 price row it converts is gated `!isShort` (`ResultCard.jsx:501`); the conversion (`:520`) is not. A converted price appears with nothing above it. Not fixed in isolation because this same row is the one nominated for the deferred move to the expanded area — fixing it now would likely be discarded. |
| O-117 | **Cuisine hides the ⇲ map button that Hawker and Train show.** `MapPanel.jsx:1108` gates it on `(!fill || onCollapse)`; on a phone `fill` is true and `onCollapse` undefined, so it disappears. The in-code justification ("it would be a no-op button") is the exact state **Transport was bug-reported for in v0.62.626 and Hawker in v0.62.627** — both fixed by giving the button a real full-viewport-overlay action rather than hiding it. Third instance of one bug; the fix pattern already exists in `HawkerMapPanel.jsx` (`canOverlay` / `overlayFull`). Operator-reported, deliberately unbuilt this pass. |
| O-118 | **The row-move is deferred, not dropped.** Operator: *"before we move the latest row… can we reduce line spacing first"*. The audit nominated the `≈` conversion row as the candidate (least decision-relevant; the full `S$20–80 (≈A$…)` string already exists expanded and in Copy-All). Still open. |

## Open (carried forward, unchanged this entry)

| # | Item | Note |
| :--- | :--- | :--- |
| O-88 | M3 audit Tier 2 per-item decision round. Unchanged. |
| O-93 | No React render-test infrastructure. Unchanged — and the direct cause of the caveat on every number in this entry. |
| O-99 | O-98's "no separate compact-phone template". Unchanged. |
| O-100 / O-101 | Device rechecks outstanding. Unchanged. |
| O-102 | The 900 ms first-load dwell is a judgement call. Unchanged. |
| O-103 | A fix can pass the whole suite while covering only some of the sites its root cause names. Unchanged — O-117 is a fresh instance. |
| O-104 | The v0.62.678 iOS auto-zoom guard is inert. Unchanged. |
| O-107 | Height numbers are arithmetic, not measurement. **Unchanged and directly relevant** — every figure in C-120–C-122 and O-115 is computed from the class list. |
| O-108 | Transport phone-landscape 164px tile outlier. Unchanged. |
| O-109 | Hawker's height tiers differ from Cuisine's. Unchanged — and now further apart, since Cuisine moved to 12rem while Hawker stayed at 8rem. |
| O-111 | The inspection overlay has never been seen on a device. Unchanged. |
| O-112 | `/api/geo/road-search` never executed end-to-end in-sandbox. Unchanged. |
| O-113 | `titleCase` mangles unknown brand initialisms. Unchanged. |
| O-114 | `data/prs.ndjson` mixed `n` column type. Unchanged. |

## Note

The audit is the substantive part of this entry, and it inverted the framing. The operator's report
was *"there's wasted space, shrink the card"* — true, and now fixed. But the same arithmetic showed
the opposite problem on the cards nobody screenshots: a Michelin venue with wrapped rows already
overruns the box by ~44px, and the entire short tier overruns by ~36px. A fixed height with
`overflow-hidden` cannot be right for both, and the row that gets clipped first is the **toggle** —
the one control that would let the user see what was hidden.

That is why the reduction stopped at 12rem instead of the requested 10%+: it is the exact point where
helping the visible case stops making the invisible one worse. The remaining cut is available, but it
costs a row rather than whitespace, and that is the operator's call to make.

Worth noting alongside O-117: three separate operator reports (Transport v0.62.626, Hawker v0.62.627,
Cuisine today) have now been filed against the same "hide the control when it would be a no-op"
pattern. Each was fixed locally. The pattern itself was never named, so it survived in the third app.
