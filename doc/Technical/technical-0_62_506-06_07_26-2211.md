(№ 56 - 06-07 '26 22:11 SGT)

# Technical — soleat / Gia4lunch v0.62.506 (catch-up rollup snapshot)

> **Document type:** Technical (per `doc/CLAUDE.md` §3; §4.1 — add + amendment + Deprecated table at EOF).
> **Status:** **Catch-up rollup.** Continues `technical-0_62_153-17_06_26-1730.md` (№ 55). **Scope:** the
> **v0.62.154 → v0.62.506** arc (~430 merged PRs, ≈ #1106 → #1512, 17-06 → 06-07 '26). This is a
> **state snapshot**, not a per-PR log — per-PR detail lives in the arc journals + the
> VibeCodingRecord. Companion to `register-0_62_502-05_07_26-1500.md` (№ 130), which carries the
> Open/Deferred/Decisions ledger for the same span.
> **Reflects:** project state at **v0.62.506, 06-07 '26 22:11 SGT** (PR #1512 merged, `02e461f0`).
> Verified figures below come from the 4-auditor strategic code audit run 06-07 '26 against main.

---

## 1. Architecture shape (measured, 06-07 '26)

| Surface | State | Figures |
|---|---|---|
| `index.js` (trunk) | Monolithic glue: Express server + bot handlers + schedulers in one file | **19,256 LOC**; 537 `require`s; 92 HTTP routes; 45 bot handlers; **11 `setInterval` schedulers with no distributed lock** (single-replica Railway masks this; multi-replica would double-fire crons) |
| TMAs (`web/`) | 7 independently-forked React apps (cuisine v2, menu, hawker, transport, clipboard, oversight, + dead cuisine v1) | **~11k LOC duplicated**: `mapOverlays` ×3 (7,611 LOC); `tg.js` ×6; `i18n.js` ×5 (same merge loop, drifted overlays); `MapControls` hawker↔transport byte-identical; `BackFab` forked with **incompatible i18n keys** (`btn.fab*` vs `fab.*`); transport removed auto-fullscreen, hawker didn't; `cuisine/src/v2/App.jsx` = **5,171 LOC** |
| State layer | Redis SSOT (user prefs, location, vault index, review caches) — solid, no change needed | `gia:vault`, `place-reviews:*`, `vibe-summary:*` keys; `vault-index.js`/`vault.js` never read the `vault/` directory |
| Tests | ~2,672 tests, **all leaf-helper level**; `index.js` exports nothing and is untested; 0 frontend tests | TDZ AST scan (`tma-hook-deps-tdz.test.js`, ~210s) relies on **undeclared transitive deps** esbuild/acorn |
| Logging | pino/Sentry ~10% adopted | 744 `console.*` call sites vs 14 files on pino, 2 on Sentry |
| CI | 4 required checks (Syntax, Vitest, TMA build, Empty-blob guardrail), enforce_admins | No lint, no typecheck, no e2e, no post-deploy smoke |
| Generated artifacts in git | `vault/` = **27 snapshots** (~25k files, incl. new `v0.62.504`); `doc/.serial-state.yml` ~950 KB; `public/doc/vibe-journal.html` ~1.0 MB tracked | Cleanup approved-in-principle by operator (vault snapshot taken first — #1511); gitignoring `vibe-journal.html` needs a build-time generation hook (Express serves it from `public/`) |

## 2. Functions / Modules (headline changes this arc)

| Module / symbol | Change | Ref |
|---|---|---|
| `web/cuisine/src/v2/` (picker + cards) | White-folio 5-bucket picker; line-icon Phase 1 (cobalt); 131-line `criteriaOpen` duplicate removed; two result-paths collapsed; status folder-tab moved **outside** the card border; separator-wrap glue; TDZ crash fixes | D-A1/D-A2 (№ 130) |
| `web/*/i18n.js` ×5 + `user-prefs.js` + `index.js` gates | Locale program **5→8 langs** (en/fr → +id/ru/de → +zh/ja/es); per-TMA `*_STRINGS` overlays; `t() ??` fix; ~45 App.jsx ternaries → keys | D-A3 |
| `getActiveLocale` (all 5 TMAs) | **Device-locale precedence**: `navigator.language` now beats Telegram-app `language_code`; explicit `gia.locale` stays highest | D-A4, v0.62.501 |
| `nation-overlay.js` + `nation-overlay-dishnotes{,-i18n}.generated.js` | Grounded dish-note backfill → **100% coverage** (~1.65k notes ×6 langs); 7 fabricated dishes pruned; iconicDishes 1689→1682 | D-A5, v0.62.502 |
| `FR-michelin.js` + city registry | France scaffold (12 cities, Paris+Lyon Michelin 2025/26) — **gated** pending FR BBOX geo-guard (O-51) | D-A6 |
| `web/menu/src/App.jsx` auto-detect | Fixed 800 ms boot race → gate on `anchorLoading` (no timer; Menu has no location cache by design) | v0.62.498 |
| `vault-index.js` + `index.js` `updateTransitStatus` | Heartbeat logs quieted: Vault-Index gated on non-zero refresh; Pulse edge-triggered on health-label change (`lastPulseStatus`) | v0.62.504 |

## 3. Performance / operational notes (this arc)

- **Railway runtime log** now quiet at idle (was 2 lines/5 min; verified live post-deploy 05-07 23:50 UTC).
- **O-45 carried**: `enrichSlow` remains a sequential await-chain vs the 20s D706 deadline; D707 timings instrumentation in place; parallelization still the candidate fix.
- **Scheduler risk (new, from audit)**: the 11 unguarded `setInterval`s are safe only while Railway runs one replica. Any scale-out needs a Redis job lock first (cheap: `SET NX EX` wrapper).
- **Vitest wall-clock**: full run dominated by `tma-hook-deps-tdz.test.js` (~210 s); its esbuild/acorn deps are undeclared (works via transitive install — declare them or the scan silently dies on a lockfile change).

## 4. Build Constraints (restated + deltas)

- `main` protected (4 checks; enforce_admins); log collector pushes to unprotected `railway-logs`.
- `web/*` Vite builds emit to gitignored `public/*/assets/`; `public/doc/vibe-journal.{html,json}` are tracked and regenerated by `doc/VibeCodingRecord/generate.mjs` (regen shipped through #1512, ledger at 1,483 rows).
- iCloud working tree is **not** a reliable base — ship via gh Git Data API from `origin/main` (standing rule; used for every PR this arc).

---

## Amendments

- **AU-1:** First Technical snapshot since № 55 (`technical-0_62_153`). Rollup, not per-PR; adds the
  measured architecture-shape table (§1) sourced from the 06-07 '26 4-auditor audit. Prior entries
  unchanged.

## Deprecated / Removed (this arc)

| Item | Where | Note |
|---|---|---|
| `criteriaOpen` duplicate (131 LOC) | cuisine v2 | Removed; single result-path |
| 7 fabricated `F()` dish entries | `nation-overlay.js` | Pruned, never invented replacements |
| `[Vault-Index]`/`[Pulse]` idle heartbeats | `vault-index.js` / `index.js` | Gated/edge-triggered (v0.62.504) |
