# Feature — soleat v0.60.172

(№ 36 - 15-05 '26 07:30 SGT)

> Per CLAUDE.md §3 / §4 (AU-1 / AU-3): adds to — never compresses — the prior versioned Feature file `feature-0_60_157-14_05_26-1400.md`. §§1–N are reproduced *by reference* (open the prior file to read them verbatim — every prior feature listed there remains a live feature unless a Removed-table row says otherwise). New features since v0.60.157 are listed in **§8 New features (v0.60.158 → v0.60.172)** below. The Removed-Features table is unchanged (no features removed in this arc).

---

## §§1–7 — reproduced by reference

> _(open `doc/Feature/feature-0_60_157-14_05_26-1400.md` for the full prior feature catalogue — TMA family, bot commands, search pipeline, /hidden / /recognised / /transport / /carpark, copy-all + copy-syntax, Cuisine TMA v2 page-history, etc. All live unless explicitly Removed.)_

---

## §8 — New features (v0.60.158 → v0.60.172)

| # | Feature | Shipped in | PR | Notes |
| :--- | :--- | :--- | :--- | :--- |
| F-158.1 | Cuisine TMA — per-Cuisine-session seen-set memory key + LocationField name display polish | v0.60.158 | #400 | Operator post-#398. |
| F-159.1 | Cuisine TMA — result-card price tier badge restored after v0.60.155 sort refactor | v0.60.159 | #401 | Regression fix surfaced as a feature restoration. |
| F-160.1 | Cuisine TMA — zero-results CTA copy refined ("Reset filters & retry" — bilingual EN/FR) | v0.60.160 | #402 | UI polish; user-visible. |
| F-161.1 | Cuisine TMA — /copy-one bilingual fallback when the cached strict-format card is missing | v0.60.161 | #403 | Robustness improvement; no UI change unless cache misses. |
| F-162.1 | Cuisine TMA — venue-name dedup tightened to substring match across cuisines | v0.60.162 | #404 | Stricter dedup. |
| F-163.1 | Cuisine TMA — /copy-all body chunking when assembled > 4096 chars + ResultCard substring dedup tightening | v0.60.163 | #405 | Stops the 4096-char Telegram overflow. |
| F-164.1 | **Cuisine TMA — JB-region pick-respect**. SG-bbox-aware fallback in `/api/cuisine/warm-start` + `/api/cuisine/search`; post-filter loosened from `/johor bahru/i` to `/\bjohor\b/i` (word-boundary) so Pontian / Desaru / Kulai / Iskandar Puteri / Muar / Mersing / Batu Pahat / Kota Tinggi survive the gate while KL / Selangor / Pahang still fall out. | v0.60.164 | #406 cherry-picked into #407 | Operator: *"Can I set the location at Pontian or Desaru?"* — yes, after this. |
| F-165.1 | **Cuisine TMA — SG/JB radius defaults rebalanced**. SG `50000 → 20000` (near-me focus); JB `18000 → 30000` (full JB + Iskandar Puteri + Pasir Gudang + Senai/Kulai edges). Slider max 100 km unchanged. | v0.60.165 / v0.60.166 first-pass | #407 | Operator: *"reduce back to 20 KM, only if Johor Bahru is selected then focus … up to 100 KM"*. |
| F-165.2 | **Cuisine TMA — 🐾 Pet allowed filter chip**. `places.allowsDogs` added to Places `DISCOVER_FIELD_MASK`; new filter key `petFriendly` in TMA state; chip rendered in Filters OVERFLOW; strict mode post-filter + text-query fallback when strict yields < 3 venues. EN `"Pet allowed"` / FR `"Animaux autorisés"` (v0.60.168). Wired into `filterCount` / `stateSig` / criteria-card preview so Clear button + dirty-state + header preview all honour it. | v0.60.165 + v0.60.166 wiring fixes | #407, #409 | Operator: *"Build It. Add a new paw print '🐾 Pet-allow' chip…"*. |
| F-166.1 | Cuisine TMA — LocationField pick no longer auto-fires search (commit anchor only); v0.60.170 fix re-syncs `setSearchCenter` so the map + the criteria-card Search button's fallback also follow the pick. | v0.60.166 + v0.60.170 | #409, #415 | Two-step fix per operator's two bug reports. |
| F-166.2 | Cuisine TMA — first-load greyed-off overlay (`fixed inset-0 z-50 bg-tg-bg/60 cursor-wait`). Absorbs pointer events during warm-start so the Edit-search / Search-criteria / cuisine chips are non-interactive until the first results paint. v0.60.168 tightened the FR copy to `"Chargement de la liste…"` (was 53-char). | v0.60.166 + v0.60.168 | #409, #412 | Operator: *"should grey off any selection until it is loaded"*. |
| F-167.1 | **Cuisine API — Telegram WebApp `initData` auth chokepoint** on `/api/cuisine/*`. Single `app.use('/api/cuisine', requireInitDataFromBodyOrHeader)` mounted before all routes. 17 routes + 2 formerly-anonymous GETs now 401 anonymous callers. Constant-time HMAC compare. Dev bypass via `SKIP_INIT_DATA_AUTH=true`. 17 new unit tests. | v0.60.167 | #411 | Operator security-hardening priority #1. |
| F-169.1 | **`/legal` command i18n migration (EN + FR)** + Google-Places-accuracy filter disclaimer + JB-geographic-coverage clause. v0.60.171 then fully replaced the body per operator paste (3-paragraph "as is" form with transport + takedown + fullest-extent-of-law clauses). | v0.60.169 + v0.60.171 | #414, #416 | Operator: *"Should privacy or legal be update? … Yes, in FR and EN"*. Then: *"Change the text in /legal to this text below"*. |
| F-172.1 | **`/privacy` body rewrite** — operator paste, 3-paragraph tighter form. Substance preserved (24h location cache, ≤90-day retention, hashed aggregate counters, no trackers, `/forgetme` erasure). | v0.60.172 | #417 | Operator: *"Here is a tighter version for /privacy"*. |

### §8.1 — Cross-cutting UX-quality items

| # | Item | Versions touched |
| :--- | :--- | :--- |
| F-Q.1 | FR copy quality pass — 🐾 chip first-pass "Pet-allow" → review-pass "pet allowed" → "Pet allowed" → "Animaux autorisés"; overlay length tightening; filter-key wiring (filterCount / stateSig / criteria preview) | v0.60.165 → v0.60.168 |

---

## Removed Features

| # | Item | Original Version | Removed in Version | Removal Date (DD-MM 'YY) | Reason | Replacement | Rule Reference | Serial № |
|---|------|------------------|--------------------|--------------------------|--------|-------------|----------------|----------|

> _(table unchanged from `feature-0_60_157-14_05_26-1400.md` — no features were removed across the v0.60.158 → v0.60.172 arc. The "Pet-allow" → "pet allowed" → "Pet allowed" → "Animaux autorisés" label evolution within F-165.2 is an in-place wording iteration, not a removal.)_
