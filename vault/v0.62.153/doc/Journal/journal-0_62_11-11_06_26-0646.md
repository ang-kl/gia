(№ 657 - 11-06 '26 06:46 SGT) v0.62.11

# Journal — soleat v0.62.11 (first-tap slice 5 → 6; KL "<6" diagnosed, radius widen declined for non-overlap)

> Operator: *"still seeing less than six for Japanese food in Kuala Lumpur"* + *"how does [the
> v0.62.10 pagination merge] affect the concentric circle results?"* Investigated end-to-end; the
> pagination merge does NOT affect this path (the cuisine pill already passed `maxPages: 3`). The
> KL shortfall traced to the client `cityRadiusCapM` half-sibling heuristic capping KL at ~10 km
> (Shah Alam ~20 km away) → clamps the 20 km base fetch to 10 km → drops Mont Kiara/Subang/Ampang.
> BUT: widening KL necessarily overlaps Putrajaya, which the operator's v0.61.419 rule forbids.
> Operator chose (AskUserQuestion) **"keep strict non-overlap"** — so the radius widen is declined.
> The literal "<6" is the deliberate first-tap taste (`FIRST_TAP_SLICE = 5`, v0.61.239); operator
> confirmed the re-tap already yields a full 12. Fix: bump the first-tap slice 5 → 6.

> Commit 674 / journal 657 / build 416 (server-only — no web build). `index.js` one constant.
> PATCH bump 0.62.10 → 0.62.11.

## New

### [HDR] #657 | 06:46 SGT 11-06-26 | v0.62.11 — first-tap slice 5 → 6 (KL "<6") | index.js | 1 PR | 0 build |

- **[INTENT]** Resolve the literal "less than six" on the KL Japanese first tap. The full diagnosis
  (below) showed the genuine radius lever (the ~10 km `cityRadiusCapM` cap) can't be pulled without
  breaching the operator's v0.61.419 KL↔Putrajaya non-overlap rule, which the operator chose to
  keep. Since the re-tap already serves a full 12, the only thing reading as "<6" is the
  deliberate first-tap taste — so bump it to 6.

- **[DELTA]** `index.js:15851` — `FIRST_TAP_SLICE` **5 → 6**. First 🔍 tap now serves 6 (was 5);
  follow-up taps unchanged at 12. Single constant; no change to search radius, ranking, the rating
  floor, dedup, the concentric ladder, or pagination.

- **[DIAGNOSIS — kept for the record, no code change]** Why KL Japanese looked thin:
  `cityRadiusCapM` (`web/cuisine/src/v2/lib/cities.js:252`) caps a city at half the distance to its
  nearest sibling (floor 8 km). KL→Shah Alam ~20 km → **~10 km** cap (the file comment says so),
  persisted as `radiusCapM` → read server-side as `anchorCap`, clamping the 20 km base fetch to
  10 km (`index.js:14400`); the concentric ladder then filters to `[5 km, 10 km]`
  (`cuisine-nearby-widen.js:78`). KL's Japanese venues at Mont Kiara (13 km) / Subang (12 km) /
  Ampang (15 km) fall outside 10 km. **Declined fix:** raising KL's cap to ~25 km would fix the pool
  but overlap Putrajaya (~24 km) + Shah Alam (~20 km) — operator kept v0.61.419 strict non-overlap.
  The v0.62.10 pagination merge is unrelated (the pill already paged `maxPages: 3`).

- **[VERIFICATION (sandbox)]** `node --check index.js` clean. Full suite **3289/3289** green —
  including `city-radius-cap.test.js`'s non-overlap invariant (untouched, still passing, because the
  radius widen was reverted). Server-only change — no web build, no new test (a single internal UX
  constant; the existing first-tap/​pagination tests cover the slice mechanics).

- **[STATUS]** PR (rides the session branch — retitled from the #970 doc-record PR; carries this fix
  + the standing-rule v0.62.10 merge record). Deploys via Railway on merge.

- **[TEST — operator, post-deploy]** Any first 🔍 tap with a healthy pool now shows **6** cards (was
  5); tapping 🔍 again still loads 12. KL Japanese no longer reads as "less than six" on the first
  tap. (The KL pool itself is unchanged — strict non-overlap retained.)

- **[KNOWN GAPS]** KL (and any metro whose own catchment exceeds half the gap to its nearest
  sibling) remains radius-capped by the non-overlap rule; a genuinely sparse cuisine there can still
  yield a short follow-up page. Lifting that needs the operator to relax v0.61.419 (declined this
  round). The `cityRadiusCapM` `capM`-override mechanism explored this round was reverted (unused).

- **Confirmation Gates**

  | Gate | Resolution | Date |
  |---|---|---|
  | G1 — version bump | PATCH `0.62.10 → 0.62.11` (UX constant; 0.62.x patch-line policy). | 11-06 '26 |
  | G2 — destructive action | None — single-constant edit; the explored radius/capM changes were reverted (net-zero on cities.js). | 11-06 '26 |
  | G3 — new decision rule | First-tap taste is 6 (was 5). The v0.61.419 KL↔Putrajaya non-overlap rule is REAFFIRMED (radius widen declined). | 11-06 '26 |
  | G4 — paid external API call | None — first-tap 6 vs 5 slices the SAME fetched pool; no extra Places calls. | 11-06 '26 |

## Amendments

- **`index.js`** — `FIRST_TAP_SLICE` 5 → 6.
- **`package.json`** `0.62.10 → 0.62.11`; **`doc/.serial-state.yml`** counters + anchor.

## Deleted

- (none) — the radius/capM experiment was reverted in-session before commit; nothing shipped to
  remove. `cityRadiusCapM` and the v0.61.419 non-overlap invariant are unchanged.
