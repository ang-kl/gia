(№ 783 - 17-06 '26 17:25 SGT) v0.62.153

# Journal — soleat v0.62.142 → v0.62.153 (map-first layout: floating FABs · permanent list · rating persists · 📍 Current resolves · cluster-zoom)

> Operator (live screenshot-driven session, paraphrased + quoted where it matters): "Leaks again occasionally
> show the ratings set to 3.7" (rating must persist); in horizontal mode the Location + "Local food picks"
> fields "should only appear … when user tap on different mode … and these two fields disappear" once set /
> on scroll; "Current need resolutions"; "Why … the whole background white" + "make it as a floating free
> text above the edit search"; "Why the right/end, down/top, search are no longer FAB … create more space
> for google map", "the footer should only have … enter location … / Experimental … v0.62.144"; the
> "same '↑ top' FAB to include back/end" as one two-line hyperlink FAB; "change the 'edit search' to icon
> '🍲 Cuisine'"; the list FAB "two choices: 📖 open / 📘 close … ⮲ horizontal / ⮷ vertical"; "i change from
> johor to Singapore, but the search results are still Malaysia"; then "the list … be permanent available
> like other FAB as the search results will always be there"; word-wrap + IATA code on the address; a
> horizontal "Last card"; cluster-aware tap-zoom (15 if >4 pins, else 14) + a learned zoom preference; and
> the 🍲 Cuisine button "beside the '💬 What are you craving for?'".

> Counters this arc: journal 742 → 743, feature 55 → 56, technical 54 → 55, commit 800 → 801, build 450 →
> 451. App version PATCH chain **0.62.142 → 0.62.153** (12 PRs; #1094 doc-only). All squash-merged to `main`;
> Railway redeploys + rebuilds the TMAs via `postinstall: npm run build`.

> **Build-environment note (recorded per AU-7):** as in the prior arc, the operator's iCloud working tree
> has a corrupted `.git` (sync collision dups) and git-over-HTTPS hangs; all of this arc's code was built,
> tested and pushed from a clean clone outside iCloud (`/tmp/gia-build`).

## New

### [HDR] #783 | 17:25 SGT 17-06-26 | v0.62.142 → v0.62.153 — map-first floating layout + rating-persist + Current-resolve + cluster-zoom | web/cuisine · web/menu · doc/* | 12 PRs | 0 explicit build |

- **[INTENT]** A long screenshot-driven session iterating the Cuisine TMA toward a **map-first** layout
  (footer reduced to two text lines; every control floats as a FAB; results default to a horizontal strip
  with a permanent list FAB), plus three correctness fixes the operator kept flagging: a custom **rating**
  that wouldn't stick (reset to 3.7 on idle/entry), **📍 Current** anchoring under the literal word instead
  of resolving the GPS, and a **mode switch** leaving the wrong region's results on screen.

- **[DELTA]** (file-by-file, per PR; version in parentheses; all `web/cuisine/src/v2/` unless noted)

  - **#1094 — docs catch-up (v0.62.142).** Journal/serial-state/VibeCodingRecord for #1085–#1093.
  - **#1095 — rating persists (v0.62.143).** `App.jsx`: removed the entry/idle reset to 3.7
    (`resetRatingToDefault` + the ≥2-min `visibilitychange` reset); the pill seeds from the saved
    `r.ratingPref`; only the first-device `intro` toast remains. **AU-7 reversal of G3.**
  - **#1096 — hide Location/food-picks fields except on mode-tap (v0.62.144).** `App.jsx`: the fields
    wrapper is `hidden` unless `modePeek`; a downward-scroll gesture (`lastScrollYRef` direction gate)
    clears `modePeek`. Fixes horizontal mode (a short page) where they never scrolled away.
  - **#1097 — 📍 Current resolves to a real place (v0.62.145).** `App.jsx` `pickCurrentLocation`:
    `reverseGeocode(coords)` → commit the resolved name as the anchor label (fallback "Current").
  - **#1098 — footer = 2 text lines; controls float as FABs (v0.62.146).** `App.jsx` + `ResultDrawer.jsx`:
    fixed bottom cluster with corner FAB stacks + a floating free-text bar (own card → kills the
    white-flash) + the 2-line footer. Strip bottom 5.5→6.5rem.
  - **#1099 — remark above + combined top/back FAB + 🍲 Cuisine (v0.62.147).** `TellMePanel.jsx` (remark
    moved above the input); `App.jsx` (one white FAB, two lines of `text-tg-link no-underline` — top/down +
    back/end; "Edit search" → 🍲 Cuisine; `BackFab` import retired).
  - **#1100 — mode switch clears stale results (v0.62.148).** `App.jsx`: `setVenues([])` etc. on a region
    change. *(Superseded by #1102.)*
  - **#1101 — list FAB icon toggles (v0.62.149).** `App.jsx`: 📖/📘 (open/close) · ⮲/⮷ (horizontal/vertical).
  - **#1102 — results persist + permanent list FAB (v0.62.150).** `App.jsx`: reverted #1100's clear (results
    persist until re-search); the list FAB is no longer gated on having results.
  - **#1103 — address wrap + country→code + Last card (v0.62.151).** `ResultCard.jsx` (`break-words` +
    `shortenCountry()` via `OTHER_COUNTRIES`); `ResultDrawer.jsx` (terminal "Last card").
  - **#1104 — 🍲 Cuisine beside the free-text (v0.62.152).** `App.jsx`: moved the button beside the bar.
  - **#1105 — cluster-aware tap-zoom + learned pref + TMA scrollbar (v0.62.153).** `MapPanel.jsx` (15 when
    >4 pins within ~150 m else 14; `prefZoomRef` learned from two manual zoom-outs to 13/14 within 3 s,
    `progZoomRef` ignoring programmatic `setZoom`; resets on remount); `styles.css` (`html` thin scrollbar).

- **[VERIFICATION (sandbox)]** Every code PR: `npm run build:cuisine` (and `build:menu` for #1089's tail)
  green; full `vitest run` **3458/3458 pass** (137 files) on each. Sequential `package.json` version
  conflicts on the parallel branches were resolved keeping the higher version.

- **[STATUS]** PROD. 12 PRs squash-merged to `main`; `main` @ **v0.62.153**. Railway redeploys per PR.

- **[TEST]** No new unit tests this arc (FE layout / CSS / interaction). The standing 3458-test suite held
  green throughout as a regression guard.

- **[KNOWN GAPS]**
  - **On-device verification owed** (flagged on every visual PR): the floating FAB layout + positions, the
    free-text-on-focus background, the ⮲/⮷ glyph rendering in the Telegram WebView, the cluster-zoom 15/14 +
    learned-pref behaviour (the ~150 m radius / ~3 s window are tunable), and the whole-TMA scrollbar intent.
  - **Bug A trade-off:** mode-switch results now persist (operator's later request); the previous region's
    results show until the user re-searches (no auto-fire, per the operator's confirmed choice).
  - **Register refresh** for this arc is still due (logged here).

| Gate | Question | Verdict |
|------|----------|---------|
| **G1** | Operator request captured verbatim where it matters? | ✅ — quotes in the header block. |
| **G2** | Source-of-truth = current `main` code? | ✅ — DELTA from the merged diffs / built files. |
| **G3** | Journal recorded per the standing per-PR rule? | ✅ — this HDR + VibeCodingRecord #1094–#1105. |
| **G4** | Reversals disclosed (AU-7), not silently rewritten? | ✅ — rating G3 reversal (#1095/RF-27/DT-12); #1100 clear superseded by #1102 (RF-29); in-footer row → FABs (RF-28/DT-13). |
