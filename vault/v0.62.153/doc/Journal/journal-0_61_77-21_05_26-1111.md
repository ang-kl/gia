(№ 284 - 21-05 '26 11:11 SGT) v0.61.77

# Journal — soleat v0.61.77 (map controls — ⋯ menu placement + nav-button polish)

> Operator: *"move '.../⋮' button to the left next to the 'monochrome'.
> change it to square than pill button. '.../⋮' button and 4 navigation
> button be brighter font. '.../⋮' button and '⛶ ⟳' and '↘' be larger
> size. remove 24hrs from the dropdown list."*
>
> Four UI-polish tweaks to the in-map control surface, applied uniformly
> across the Cuisine, Transport and Hawker TMA maps.
>
> This entry is commit 284 / journal 277 / build 84.

## New

### [HDR] #277 | 11:11 SGT 21-05-26 | v0.61.77 — map controls: ⋯ menu placement + nav-button polish | ~2 kb | 7 files | 1 PR | 1 build |

- **[INTENT]** Apply four operator-requested tweaks to the in-map
  control surface: relocate + reshape the `⋯/⋮` overflow-menu button,
  brighten the menu + navigation glyphs, enlarge the smaller nav
  glyphs, and drop the disabled "24hrs" placeholder from the dropdown.

- **[DELTA]**
  - **`web/{cuisine,transport,hawker}/.../MapControls.jsx`** (byte-identical ×3) —
    - The `⋯/⋮` overflow-menu button moves from **last** in the quick-
      toggle row to **directly after the Colour ("Monochrome") pill**.
    - The button changes from a circle (`rounded-full`) to a **square**
      (`rounded-md`); its glyph is brighter + larger
      (`text-gray-900` `text-sm` → `text-black` `text-base`, keeping
      `font-bold`).
    - The dropdown is re-anchored `right-0` → `left-0` so it stays
      on-screen below the relocated (now left-side) button.
    - Header + inline layout comments updated to v0.61.77.
  - **`MrtMapPanel.jsx` / `MapPanel.jsx` / `HawkerMapPanel.jsx`** —
    - The 4 navigation-button glyphs (Reset / + / − / expand) →
      `text-black font-bold` (were `text-gray-900`, weights normal /
      `font-semibold`).
    - The Reset (`⛶⟲`) and expand (`⇱`/`⇲`) glyphs enlarged
      `text-sm` → `text-base`, matching the `＋` / `－` zoom buttons —
      all 4 nav buttons are now visually uniform.
    - The disabled `open24` ("24hrs") placeholder row removed from each
      panel's `menuToggles` overlay-dropdown array.
  - **`package.json`** — `0.61.76` → `0.61.77` (PATCH).

- **[VERIFICATION (sandbox)]**

  ```
  web/cuisine    vite build     pass   ~2.1s
  web/transport  vite build     pass   ~2.3s
  web/hawker     vite build     pass   ~1.6s
  npm test -- --run             1928/1928 passing, 65 files
  MapControls.jsx ×3            byte-identical (md5 match) after the edit
  ```

- **[STATUS]** DRAFT — PR #576 opened on
  `claude/review-gia4lunch-readme-3s798`. On merge, deploys via the
  Railway auto-deploy on `main`; the TMA bundles rebuild in CI.

- **[TEST]** Open each TMA map (Cuisine / Transport / Hawker): the
  `⋯/⋮` button sits immediately right of the Colour pill, is a square,
  and its dropdown opens on-screen (left-aligned). The `⋯/⋮` button and
  the 4 nav buttons show crisp bold-black glyphs; Reset and expand are
  the same size as `＋` / `－`. The overflow dropdown no longer lists
  "24hrs".

- **[KNOWN GAPS]**
  - The `layer.open24` i18n string is left in the three `i18n.js` files
    as a now-unused key — harmless, and retained in case the 24-hour-
    places layer (the v0.61.69 pipeline scaffold) is shipped later.
  - `doc/VibeCodingRecord/` regen still due (carried from journal #276).
  - Verification is sandbox-only this pass; CI is the merge gate.

- **Confirmation Gates**

  | Gate | Resolution | Date |
  |------|-----------|------|
  | G1 — version bump | PATCH `0.61.76` → `0.61.77` | 21-05 '26 |
  | G2 — destructive action | _n/a_ — button relocation + style changes + removal of a *disabled* placeholder dropdown row; no file or data deletion | 21-05 '26 |
  | G3 — new decision rule | _n/a_ — operator UI-polish tweaks, no new rule | 21-05 '26 |
  | G4 — paid external API call | _n/a_ | — |

## Amendments

_None._

## Deleted

_None._
