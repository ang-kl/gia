(№ 615 - 09-06 '26 11:21 SGT) v0.61.415

# Journal — soleat v0.61.415 (Recents "Clear all" → "Clear all except current": keep the active location)

> Operator: *"clear locations include current location which should not, please all 'Clear all except current'"*
>
> Commit 628 / journal 615 / **build 397**. recent-locations.js + index.js + web/cuisine. Revert-ready.

## New

### [HDR] #615 | 11:21 SGT 09-06-26 | v0.61.415 — recents Clear-all keeps the current row | recent-locations.js + index.js + LocationField.jsx + api.js + test | 1 PR | 1 build |

- **[INTENT]** The recents-drawer **🗑 Clear all** wiped the **whole** LRU — the **current** location included — so it disappeared from the drawer. Keep the current; clear the rest; relabel to **"Clear all except current"**.

- **[DELTA]**
  - **`recent-locations.js`** — new `clearRecentLocationsExcept(redis, chatId, lat, lng)`: keeps the entry matching `(lat,lng)` within ~11 m (`_sameCoord`); **falls back to the most-recent row `[0]`** (= the current set-location) when no coord is given or none matches; no-op + `null` on an empty list.
  - **`index.js`** `/api/cuisine/recent-locations/clear` — reads `keepLat` / `keepLng` from the body → `clearRecentLocationsExcept`; no coords → the legacy full wipe (back-compat).
  - **`index.js`** chat `/lrecent` **clear** callback — now keeps the current set-location (`getUserLocation` coords); message → *"Recent locations cleared — kept your current spot."*
  - **`web/cuisine/src/v2/lib/api.js`** — `clearRecentLocationsRemote(keep)` forwards `keepLat` / `keepLng`.
  - **`web/cuisine/src/v2/components/LocationField.jsx`** — `handleClearRecents` computes the current row (the **✓** entry matching the anchor, else `recents[0]`), passes its coords, and mirrors locally (`setRecents([current])`). Button relabelled **"Clear all" → "Clear all except current"** (`Tout effacer sauf l'actuel`).
  - **`__tests__/recent-locations.test.js`** — +4 cases (keep-match, `[0]` fallback when no coord / no match, empty → `null`).
  - **`package.json`** `0.61.414 → 0.61.415`; **`doc/.serial-state.yml`** commit 627 → 628, journal 614 → 615, **build 396 → 397**; re-anchored 11:21 SGT.

- **[VERIFICATION (sandbox)]** `node --check` on index.js / recent-locations.js / api.js clean. recent-locations suite **17 green** (+4). Cuisine **vite build green**. Full suite **3121/3121 green**.

- **[STATUS]** PR-**OPEN**, **auto-merge armed (squash)**. Railway redeploys (server + TMA) on merge.

- **[TEST]** Operator: open the 📍 recents drawer → **🗑 Clear all except current** → every row clears **except the current one** (the ✓ row stays). The chat `/lrecent` clear keeps the current spot too.

- **[KNOWN GAPS]**
  - The set-location cache `loc:<chatId>` was never touched by clear (only the LRU) — so the active search location was always safe; this fix is purely about the **drawer list** keeping its current row.
  - "Current" = the row matching the active anchor, else the most-recent — if the active anchor isn't a saved row (e.g. raw device GPS), the most-recent row is kept.
  - Eyes-on-deployed pending (operator to confirm on the live TMA).

- **Confirmation Gates**

  | Gate | Resolution | Date |
  |---|---|---|
  | G1 — version bump | PATCH `0.61.414 → 0.61.415`. | 09-06 '26 |
  | G2 — destructive action | Makes clear LESS destructive (keeps the current); legacy full-wipe still available with no coords. | 09-06 '26 |
  | G3 — new decision rule | Recents clear-all keeps the current location row. | 09-06 '26 |
  | G4 — paid external API call | None. | 09-06 '26 |

## Amendments

- **`recent-locations.js`** — `clearRecentLocationsExcept` + export.
- **`index.js`** — TMA clear endpoint (keepLat/keepLng) + chat clear keeps current.
- **`web/cuisine/src/v2/lib/api.js`** — `clearRecentLocationsRemote(keep)`.
- **`web/cuisine/src/v2/components/LocationField.jsx`** — `handleClearRecents` keep-current + button relabel.
- **`__tests__/recent-locations.test.js`** — +4 cases.
- **`package.json`** — `0.61.414 → 0.61.415`.
- **`doc/.serial-state.yml`** — counters (commit 628 / journal 615 / build 397) + anchor 11:21 SGT 09-06-26 + `last_anchor_source` prepend.

## Deleted

- The "Clear all wipes everything incl. the current location" behaviour (superseded by keep-current).
