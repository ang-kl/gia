(№ 578 - 07-06 '26 21:30 SGT) v0.61.378

# Journal — soleat v0.61.378 (Durian / Durian-pastry per-picker-city coverage probe)

> Operator: *"do a 3 fire for each picker list country+city for these two — Durian fruit + Durian pastry. I like to know which city gives zero. plan first."* Decisions: local language + EN · scan-once then confirm-zeros 3× · build AND run now · scan-first then report confirm cost.
>
> **Left OPEN for review — auto-merge to be armed on the operator's word.** Commit 591 / journal 578 / build 373 (held — server-only). Server.

## New

### [HDR] #578 | 21:30 SGT 07-06-26 | v0.61.378 — durian city-coverage probe + RESULT | 2 code files + 1 test | 1 PR | 0 build |

- **[INTENT]** Find which picker country+city returns **zero** durian / durian-pastry venues — a coverage audit of the v0.61.377 local-locale matcher.

- **[MECHANISM]**
  - **`durian-variance-runner.js`** — 2 additive opts: **`langs`** (restrict the seed fan-out to given language keys) + **`seedsOverride`** (custom seed map). Existing callers unaffected (defaults = full SEEDS, all langs).
  - **`scripts/durian-city-coverage.js`** (NEW) — dynamic-imports `CITIES_BY_COUNTRY` (the 138 real picker cities = single source of truth); `localLangsFor(cc)` (e.g. JP→`ja`+`en`, TW/HK/MO→`zh-TW`+`en`, TH→`th`+`en`); a cheap **CORE-seed scan** (`maxPages 1`) to find `kept===0` candidates (`runVariance` already applies the v0.61.377 `sm.isRelevant`, so `totals.kept` is the real post-filter count); then a **full-seed 3-fire confirm** only on candidates — a city is **ZERO only if all 3 fires return 0**. `--dry-run` prints a cost estimate with no API; `--scan-only` stops after the scan.
  - **`__tests__/durian-city-coverage.test.js`** (NEW) — 4 mock-driven tests prove the scan→confirm→zero-flag logic with **no live calls**.

- **[RESULT — live run, ~US$28]** Scan 826 calls (~$26) + confirm 54 calls ($1.73). Of **276** city×mode combos, only **2** scanned zero — **both false zeros**:
  - 🇦🇺 Brisbane / durian-pastry — confirm `[7, 7, 6]` → **has venues**
  - 🇦🇺 Perth / durian — confirm `[1, 1, 0]` → **has venues** (the lone `0` is seen-set rotation)
  - **Conclusion: NO picker city gives zero.** Every country+city has ≥ 1 durian *and* durian-pastry venue. Dense CN/HK/TW counts (30–58) confirm the v0.61.377 local-script keywords are pulling local venues.

- **[DELTA]**
  - **`durian-variance-runner.js`** — `langs` + `seedsOverride` opts.
  - **`scripts/durian-city-coverage.js`** (NEW) · **`__tests__/durian-city-coverage.test.js`** (NEW).
  - **`package.json`** — `0.61.377 → 0.61.378`. **`doc/.serial-state.yml`** — commit 590 → 591, journal 577 → 578 (**build held 373**); re-anchored 21:30 SGT.

- **[VERIFICATION (sandbox)]** `node --check durian-variance-runner.js scripts/durian-city-coverage.js` clean; full `npx vitest run` → **110 files / 3055 tests green**. Live run completed (cost above).

- **[STATUS]** PR-**OPEN**, **auto-merge to arm on the operator's word**. Server-only probe script. Re-runnable: `node scripts/durian-city-coverage.js --dry-run` (estimate) / `GOOGLE_MAPS_API_KEY=… node scripts/durian-city-coverage.js` (full).

- **[TEST]** Operator: every picker city has durian + durian-pastry coverage; the probe is now a re-runnable script if the picker grows.

- **[KNOWN GAPS]**
  - **Probe, not product** — this is an audit script, not a user-facing change.
  - **Coverage is a moment-in-time** read of Google Places; re-run after picker/keyword changes.
  - The scan uses a tiny core seed set (cheap) → it can flag false-zero *candidates* (Brisbane/Perth did); the full-seed 3-fire confirm is what makes the final verdict reliable.

- **Confirmation Gates**

  | Gate | Resolution | Date |
  |---|---|---|
  | G1 — version bump | PATCH `0.61.377 → 0.61.378`. | 07-06 '26 |
  | G2 — destructive action | New script + 2 additive runner opts; no product/behaviour change; full suite green. | 07-06 '26 |
  | G3 — new decision rule | A per-picker-city durian coverage probe (scan + 3-fire confirm) keyed off the real `CITIES_BY_COUNTRY`. | 07-06 '26 |
  | G4 — paid external API call | One-off **operator-approved** live Places run ≈ US$28 (scan $26 + confirm $1.73). | 07-06 '26 |

## Amendments

- **`durian-variance-runner.js`** — `langs` + `seedsOverride` options.
- **`scripts/durian-city-coverage.js`** (NEW) — per-city coverage probe.
- **`__tests__/durian-city-coverage.test.js`** (NEW) — mock-driven logic tests.
- **`package.json`** — `0.61.377 → 0.61.378`.
- **`doc/.serial-state.yml`** — counters (commit 591 / journal 578 / build held 373) + anchor 21:30 SGT 07-06-26.

## Deleted

- _None._
