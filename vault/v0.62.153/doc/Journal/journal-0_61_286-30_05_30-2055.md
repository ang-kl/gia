(№ 487 - 30-05 '26 20:55 SGT) v0.61.286

# Journal — soleat v0.61.286 (FunFactModal dark-mode border contrast)

> Operator (30-05 '26 ~20:50 SGT, post-v0.61.285 deploy): *"for dark mode, the contrast in border for this wait message must be obvious."*
>
> The v0.61.285 FunFactModal used the Telegram default `border-tg-border` (1px) which resolves to a near-`tg-bg` colour in dark mode — barely visible against the `tg-card` body. The modal read as a floating panel only in light mode.
>
> Single-line CSS fix: `border-2 border-tg-accent` (2px cyan/blue frame) plus a subtle `ring-1 ring-tg-accent/30 ring-offset-2` halo. Shadow bumped from `shadow-xl` to `shadow-2xl` to reinforce the "floating" cue per the v0.61.285 brief. No JSX structural change.
>
> Commit 500 / journal 487 / build 289, on branch
> `claude/v0.61.286-funfactmodal-dark-mode-border` off `main` at v0.61.285.

## New

### [HDR] #487 | 20:55 SGT 30-05-30 | v0.61.286 — FunFactModal dark-mode border contrast | 3 files | 1 PR | 1 build |

- **[INTENT]** Make the v0.61.285 fun-fact wait-modal visually pop in dark mode. One-shot Tailwind className edit; no logic change.

- **[DELTA]**

  **1. `web/cuisine/src/v2/components/FunFactModal.jsx`** — single className change on the modal card:
  ```
  - className="… border border-tg-border bg-tg-card shadow-xl …"
  + className="… border-2 border-tg-accent bg-tg-card shadow-2xl ring-1 ring-tg-accent/30 ring-offset-2 ring-offset-transparent …"
  ```
  Net visual delta:
  - Border thickness 1px → 2px.
  - Border colour `tg-border` (theme-subtle) → `tg-accent` (theme-coloured, auto-flips between light/dark).
  - New 1px outer accent ring at 30% opacity with 2px transparent offset → subtle halo that reads as a "lift" rather than a hard double-line.
  - Shadow `shadow-xl` → `shadow-2xl` for stronger floating cue.

  **2. `package.json`** — `0.61.285 → 0.61.286`.

  **3. `doc/.serial-state.yml`** — commit 499→500 (the millennial!), build 288→289, journal 486→487; re-anchored 30-05 '26 20:55 SGT.

- **[VERIFICATION (sandbox)]**

  - `esbuild.transform` (loader: jsx) of `FunFactModal.jsx` — clean.
  - Vitest still BLOCKED locally by the recurring iCloud `ERR_INVALID_PACKAGE_CONFIG` (Register O-22). No test contract changed; CI runs the v0.61.285 14-assertion sweep.

- **[STATUS]** PR-OPEN on push. Railway redeploys the Cuisine TMA on merge.

- **[TEST]** Operator (post-merge):
  1. **Dark mode.** Open Cuisine TMA in Telegram with dark theme. Force a rotating search. The FunFactModal frame should now read as a clear 2px cyan/blue border with a soft halo against the dark background — no more "ghost panel" feel.
  2. **Light mode.** Same flow in light theme. The accent-coloured border should still look intentional (not loud) — `tg-accent` resolves to the same blue Telegram uses for action buttons, so it harmonises with the rest of the chrome.
  3. **No layout shift.** The 2px border adds 1px per side vs the v0.61.285 1px — negligible. Card position and max-width unchanged.

- **[KNOWN GAPS]**
  - **The inner separator** (`border-t border-tg-border` between body and source row) stays subtle by design — it's a visual divider inside the card, not the frame.
  - **Ring offset** uses `ring-offset-transparent` because the modal floats over a `bg-black/30 backdrop-blur` overlay; a solid-coloured offset would clash with the backdrop tint.

- **Confirmation Gates**

  | Gate | Resolution | Date |
  |---|---|---|
  | G1 — version bump | PATCH `0.61.285 → 0.61.286`. Surgical visual fix. | 30-05 '26 |
  | G2 — destructive action | _n/a_. Tailwind className update only. | 30-05 '26 |
  | G3 — new decision rule | _n/a_. Refines the v0.61.285 modal styling. | 30-05 '26 |
  | G4 — paid external API call | _n/a_. | 30-05 '26 |

## Amendments

- **`web/cuisine/src/v2/components/FunFactModal.jsx`** — modal card className: `border-tg-border` → `border-2 border-tg-accent`, `shadow-xl` → `shadow-2xl`, added `ring-1 ring-tg-accent/30 ring-offset-2 ring-offset-transparent`.

## Deleted

_None._
