(№ 620 - 09-06 '26 13:49 SGT) v0.61.420

# Journal — soleat v0.61.420 (Johor as a STATE row: "Johor state" / code JOHOR / whole-state coverage)

> Operator (issue "1b", confirmed the IATA-rule exception): *"Johor should be display as 'Johor state' (italics) and the city code is 'JOHOR' and wired to boundary on entire state of Johor."*
>
> Commit 633 / journal 620 / **build 401**. web/cuisine + web/menu. Revert-ready.

## New

### [HDR] #620 | 13:49 SGT 09-06-26 | v0.61.420 — Johor whole-state row (JHB→JOHOR, italic "Johor state", 120 km covers the state) | cities.js (×2) + LocationField.jsx + tests | 1 PR | 1 build |

- **[INTENT]** In the OTHER picker, Johor is a **state**, not a city: display **"Johor state"** (italic), code **`JOHOR`**, and search the **entire Johor state**.

- **[DELTA]** (web/cuisine + web/menu)
  - **`cities.js` MY row** — `{ name:'Johor', code:'JHB', lat:1.4927, lng:103.7414 }` → **`{ name:'Johor state', code:'JOHOR', lat:1.93, lng:103.34 }`**. The centre moved to the **Johor state centroid** so the 120 km cap reaches every far corner — Muar/Segamat (NW), Mersing (NE), Desaru (SE) all **< 120 km** (the old JB-CBD centre missed Muar at ~145 km).
  - **`cityRadiusCapM`** keeps the **120 km whole-state cap** — the Johor check broadened to `code === 'JOHOR' || name ∈ {johor, johor state}` (the rename would otherwise have dropped it to the nearest-neighbour cap).
  - **`LocationField.jsx` `CityDropdown`** — italicises the `code === 'JOHOR'` row, so it reads *"Johor state"*. The closed dropdown shows the code **`JOHOR`**.
  - **`web/menu/src/cities.js`** mirrored (data only; its dropdown italic is out of scope).
  - **IATA-rule exception** — **`__tests__/iata-cities.test.js`** ("every code is a real IATA code") now `continue`s on `code === 'JOHOR'` (operator-sanctioned; **all other codes must still be real IATA** — recorded, not silently rewritten).
  - **`__tests__/city-radius-cap.test.js`** — +1 case asserting the JOHOR row is *"Johor state"* / 120 km / covers the 5 far corners; the no-overlap-invariant skip updated from name `'Johor'` → code `'JOHOR'`.
  - **`package.json`** `0.61.419 → 0.61.420`; **`doc/.serial-state.yml`** commit 632 → 633, journal 619 → 620, **build 400 → 401**; re-anchored 13:49 SGT.

- **[VERIFICATION (sandbox)]** Cuisine **+ menu vite builds green**. Full suite **3131/3131 green**.

- **[STATUS]** PR-**OPEN**, **auto-merge armed (squash)**. Railway rebuilds the TMAs on deploy.

- **[TEST]** Operator: region **Others → Malaysia** → the city list shows ***Johor state*** (italic) with code **JOHOR**; picking it searches the whole Johor state (120 km from the centroid — Muar to Desaru).

- **[KNOWN GAPS]**
  - The "boundary" is the **120 km radius** from the centroid (no GeoJSON polygon for Johor exists; the rectangular Johor bbox in `coords-to-country.js` is unchanged). A radius can't perfectly trace an irregular state border, but 120 km from this centroid covers every Johor district.
  - The **JB region pill** (SG | JB | OTHER) is **separate and unchanged** — this is the OTHER-picker Johor row only.
  - The Menu TMA shows "Johor state" / JOHOR but **not italic** (only the Cuisine dropdown was italicised, per the operator's screenshot focus).
  - Eyes-on-deployed pending.

- **Confirmation Gates**

  | Gate | Resolution | Date |
  |---|---|---|
  | G1 — version bump | PATCH `0.61.419 → 0.61.420`. | 09-06 '26 |
  | G2 — destructive action | Renames one row + breaks the IATA-code rule **for Johor only** (operator-sanctioned, test-exempted). Reversible. | 09-06 '26 |
  | G3 — new decision rule | Johor is a STATE entry: code `JOHOR`, "Johor state" (italic), 120 km whole-state cap. | 09-06 '26 |
  | G4 — paid external API call | None. | 09-06 '26 |

## Amendments

- **`web/cuisine/src/v2/lib/cities.js`** + **`web/menu/src/cities.js`** — Johor row → state; `cityRadiusCapM` JOHOR check.
- **`web/cuisine/src/v2/components/LocationField.jsx`** — italic "Johor state" in `CityDropdown`.
- **`__tests__/iata-cities.test.js`** — JOHOR exempted from the IATA-code rule.
- **`__tests__/city-radius-cap.test.js`** — Johor-state coverage case + invariant skip.
- **`package.json`** — `0.61.419 → 0.61.420`.
- **`doc/.serial-state.yml`** — counters (commit 633 / journal 620 / build 401) + anchor 13:49 SGT 09-06-26 + `last_anchor_source` prepend.

## Deleted

- The "Johor / JHB city row" (superseded by the "Johor state / JOHOR" whole-state row).
