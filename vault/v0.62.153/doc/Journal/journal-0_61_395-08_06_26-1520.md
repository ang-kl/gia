(№ 595 - 08-06 '26 15:20 SGT) v0.61.395

# Journal — soleat v0.61.395 (Track A/B seed-localisation: durian-pastry + New pill recall in non-English cities)

> Operator: **"build the Track A/B seed-localisation fix"** — the deliverable from the 08-06 durian / New-pill diagnostic.
>
> **Left OPEN for review — auto-merge to be armed on the operator's word.** Commit 608 / journal 595 / build 384 (held). Server-only.

## New

### [HDR] #595 | 15:20 SGT 08-06-26 | v0.61.395 — local-language seeds for OTHER countries | special-mode.js · index.js | 1 PR | 0 build |

- **[DIAGNOSIS RECAP]** No Algolia — search is **Google Places (New)** + `special-mode.js` filter + seeds.
  - **Track A** (durian-pastry = 0 in Tokyo) = **H-A2 localisation / seed-language**: the live TMA `buildSeeds` uses **English-only** `SEED_TEMPLATES` + empty OTHER suffix (`index.js:14233`), while the durian-variance probe that "passed 138 cities" searched with **local-language** seeds (`ドリアンパフ` via `localLangsFor`) — it found the data but never validated the production English query. The v0.61.377 **filter already accepts** JP names, so **recall** was the only gap.
  - **Track B** (New pill works in SG only) = **B2 (language)** — B0 (freshness field) + B1 (SG-only config) both **rejected**: the New pill is a **review-time refute** (`index.js:15029`) with no data field + no region gate. The English `"newly opened"` modifier under-recalls in non-English cities.

- **[DELTA]**
  - **`special-mode.js`** — NEW `LOCAL_SEED_LANG_BY_CC` (JP→ja, KR→ko, CN→zh-CN, TW/HK/MO→zh-TW, TH→th); `LOCAL_SEEDS` (per mode × lang — the **cost-guarded ~2-per-lang subset of the probe's VALIDATED** durian/durian-pastry seeds; fruits + `LOCAL_NEWLY_OPENED` authored simply = **PROVISIONAL**, flag for native review); `_localSeedLang`. `buildSeeds(mode, { country })` now **appends** the country's local seeds (NO suffix — script + Places `regionCode` bind them) for **foreign-script OTHER only**; SG/JB/Latin-script unchanged (cost guard). NEW `localNewlyOpened(cc)` for Track B.
  - **`index.js`** — Track A: `buildSeeds(specialMode, { regionSuffix, country: requestCountry })` (`:14237`). Track B: after the modifier block, when `filters.newlyOpened`, **OR in** `localNewlyOpened(requestCountry)` as a **separate** query (not mixed into the English one → avoids over-constraining; the review-time refute still decides newness language-agnostically).
  - **`__tests__/special-mode.test.js`** — +5 (JP appends `ドリアンパフ` keeping English; TH/CN/TW/KR; SG/MY/FR/ID no-op; local seeds carry no suffix; `localNewlyOpened` map).
  - **`package.json`** `0.61.394 → 0.61.395`; **`doc/.serial-state.yml`** commit 607 → 608, journal 594 → 595 (**build held 384**); re-anchored 15:20 SGT.

- **[VERIFICATION (sandbox)]** `node --check` clean (special-mode.js, index.js). Full suite **113 files / 3093 tests** (+5). Server-only — no web build.

- **[STATUS]** PR-**OPEN**, **auto-merge to arm on the operator's word**. Railway redeploys on merge.

- **[TEST]** Operator: a durian-pastry search in **Tokyo** now also fires the Japanese seeds (`ドリアンパフ` etc.) → the Japanese-named shops should surface (filter already accepts them). The "New" pill in a foreign-script city gets a local-language recall query.

- **[KNOWN GAPS]**
  - **Provisional terms:** the **fruits** local seeds + the `LOCAL_NEWLY_OPENED` phrases are author-written (the durian/durian-pastry ones are probe-validated) — flag for a native-speaker review.
  - **Cost:** +~2 Places seeds per foreign-script special-mode search (cost-guarded subset, not the full probe set).
  - **138-city probe is not production-trustworthy** (localised seeds ≠ production English seeds) — to trust it, run it through the production seed path.
  - Not live-E2E tested (no live Places call in sandbox).

- **Confirmation Gates**

  | Gate | Resolution | Date |
  |---|---|---|
  | G1 — version bump | PATCH `0.61.394 → 0.61.395`. | 08-06 '26 |
  | G2 — destructive action | None — additive seeds; SG/JB/Latin-script paths unchanged. | 08-06 '26 |
  | G3 — new decision rule | Foreign-script OTHER countries get local-language seeds appended (durian modes) + a local "newly opened" recall query (New pill). | 08-06 '26 |
  | G4 — paid external API call | Marginally more Places `searchText` calls (+~2 seeds per foreign-script special-mode search). | 08-06 '26 |

## Amendments

- **`special-mode.js`** — `LOCAL_SEEDS` / `LOCAL_SEED_LANG_BY_CC` / `localNewlyOpened` + `buildSeeds` country augmentation.
- **`index.js`** — Track A buildSeeds country; Track B local newly-opened query.
- **`__tests__/special-mode.test.js`** — +5 tests.
- **`package.json`** — `0.61.394 → 0.61.395`.
- **`doc/.serial-state.yml`** — counters (commit 608 / journal 595 / build held 384) + anchor 15:20 SGT 08-06-26.

## Deleted

- _None_ (additive).
