(№ 637 - 10-06 '26 07:34 SGT) v0.61.437

# Journal — soleat v0.61.437 (Code-review fixes B: Michelin holes — findings 5, 6, 7, 12, 13)

> Operator: *"fix all 15"* (the /code-review findings over PRs #942–#952). **PR B of 3** — the Michelin five. PR A (rating, v0.61.436 / #953) merged.

> Commit 650 / journal 637 / **build 410 (web/cuisine changed)**. index.js + App.jsx + i18n.js. Revert-ready.

## New

### [HDR] #637 | 07:34 SGT 10-06-26 | v0.61.437 — Michelin: coords-first country, honest empty states, fail-open chip strip, combo honesty | index.js + web/cuisine | 1 PR | 1 build |

- **[F6+F13 — country resolution (CONFIRMED)]** The OTHER branch resolved the Michelin country as `requestCountry || cached-location.country` — a **stale ≤24 h cached 'MY'** beat a fresh Hanoi anchor (wrong national list), and a cached **'SG'/missing** country left it null → silent `'unresolved'` empty for a user standing in KL. **Fix:** derive the country from the **request's own anchor coords first** (`nearestCityForAnchor` ≤300 km → `CITY_CENTROIDS[].country`; 'SG' skipped — the v0.61.435 `insideSG` branch owns SG anchors), then fall back to the cache. Resolution source logged.

- **[F5 — dropdown kept Michelin for no-list countries (CONFIRMED)]** Switching the OTHER country dropdown to AU/NZ/ID/BN with the ✳️ chip selected guaranteed an unexplained zero. **Fix:** `onCountryChange` now strips the chip via the new shared rule (below) when the target country **provably** lacks a curated list.

- **[F15 — region-pill strip race (CONFIRMED)]** The pill's inline `michelinOk` stripped a valid selection whenever the catalogue was still loading (5–10 s cold) or `countryPref` hadn't resolved. **Fix:** new module-scope **`michelinAllowedFor(region, countryPref, catalogue)`** — 3-state (`true`/`false`/`null`=unknown); both the pill and the dropdown strip **only on `false`** (fail-open on unknown; MY-PUT maps to 'MY'). One rule, two former implementations unified (also addresses the altitude-review note).

- **[F5/F6 display]** `handleMichelinSearch`'s empty returns now carry a machine **`reasonCode`** (`michelin_no_list` / `michelin_unresolved`, + `reasonCountry`); the TMA renders an **amber ✳️ notice** (new `michelinNotice` state holding an i18n KEY + `michelin.noList` / `michelin.unresolved` strings en/fr) instead of an unexplained blank list. Amber per the operator's no-red rule.

- **[F7+F12 — combo honesty (CONFIRMED)]** The v0.61.432 pin never empties, but the veg/halal filters after it could leave **only wrong-cuisine venues**, silently; and combo pagination walks the full national pool. **Fix:** `michelinSummary` gains **`cuisineMatched`** — the count of layered-cuisine matches on the FINAL page (post dietary/price filters, via the hoisted `comboIsMatch` predicate). `0` → the TMA shows **`michelin.comboMiss`**: "No match for your selected cuisine in this Michelin list — showing the full Michelin list instead." The full-pool pagination itself stays (documented operator intent: Michelin primary, never zero) — it is now *announced* rather than silent.

- **[DELTA files]** `index.js` (coords-first country block, 4 reasonCode returns, `comboIsMatch` + summary field); `web/cuisine/src/v2/App.jsx` (`michelinAllowedFor`, pill + dropdown strips, `michelinNotice` state/set/render); `web/cuisine/src/v2/lib/i18n.js` (3 `michelin.*` keys en/fr).

- **[VERIFICATION (sandbox)]** `node --check` clean; full suite **3176/3176 green**; `npm run build:cuisine` green.

- **[STATUS]** PR-**OPEN**, **auto-merge armed (squash)**. Railway redeploys (server + cuisine bundle) on merge.

- **[TEST — operator, deployed]** (1) Cold-launch in KL with no countryCode → Michelin returns the **MY** list (coords win; log `[Michelin] country from anchor coords: MY`). (2) Dropdown MY→AU with ✳️ selected → chip strips; if forced anyway, the result shows the amber "No Michelin Guide covers this country yet" notice instead of a blank. (3) Tap OTHER during cold catalogue load with ✳️ selected → selection survives. (4) Michelin + Japanese + Vegetarian in SG → amber "no match for your selected cuisine" notice over the full list.

- **[KNOWN GAPS]** PR C (findings 2, 10, 11, 14, 15-client races + custom box) follows. The combo pagination count (`total`/`remaining` national for combos) is unchanged by design — `cuisineMatched` is the honesty signal.

- **Confirmation Gates**

  | Gate | Resolution | Date |
  |---|---|---|
  | G1 — version bump | PATCH `0.61.436 → 0.61.437`. | 10-06 '26 |
  | G2 — destructive action | Resolution-order change (coords before cache) + fail-open strips + additive notice fields. Reversible. | 10-06 '26 |
  | G3 — new decision rule | Michelin country = request coords before cached pick; chip strips only on provable no-list; empty/miss states are always explained to the user. | 10-06 '26 |
  | G4 — paid external API call | None (centroid table is local). | 10-06 '26 |

## Amendments

- **`index.js`** — coords-first michelinCountry; reasonCode on the 4 empty returns; `comboIsMatch` + `michelinSummary.cuisineMatched`.
- **`web/cuisine/src/v2/App.jsx`** — `michelinAllowedFor` (3-state, shared); pill + dropdown strips; `michelinNotice` state/set/render.
- **`web/cuisine/src/v2/lib/i18n.js`** — `michelin.noList` / `michelin.unresolved` / `michelin.comboMiss` (en/fr).
- **`package.json`** — `0.61.436 → 0.61.437`.
- **`doc/.serial-state.yml`** — counters (commit 650 / journal 637 / build 410) + anchor 07:34 SGT 10-06-26 + `last_anchor_source` prepend.

## Deleted

- The cache-before-coords Michelin country order; the per-call-site `michelinOk` rule (superseded by the shared 3-state rule); the silent empty/wrong-cuisine Michelin states (superseded by reasonCode + notices).
