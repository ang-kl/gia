(№ 185 - 16-05 '26 01:45 SGT) v0.60.210

# Journal — soleat v0.60.210

> Operator: *"knock off DF-111, 100, 109 … rescan older gaps … report first, don't fix or build."* After the report, operator: *"proceed."*
> Report verdict (recorded last turn): DF-111 + DF-109 are actionable; DF-100 is **not a bug** (the bib-gourmand shuffle is a deliberate variety feature — recommended against touching); DF-110 is a one-glyph cosmetic, deferred. This PR ships DF-111 + DF-109.

## New

### [HDR] #178 | 01:45:00 SGT 16-05-26 | v0.60.210 — DF-111 /hidden Try-line dish guard + DF-109 train-map panel FR | 7 files | 1 PR | 1 build |

- **[INTENT]** Two carried gaps closed.
  - **DF-111** — the `/hidden` gems card's `🍴 Try ·` line is generated by Gemini prose; the prompt says "FOOD dishes only" but nothing enforced it, so a category word could leak (same class as the v0.60.209 "Try: dishes" bug, on a path v0.60.209 didn't cover).
  - **DF-109** — `MrtMapPanel.jsx` (the opt-in Google-Map view of the train TMA) was English-only: the station InfoWindow popup *and* the panel chrome never received a `lang` prop.

- **[DELTA]**

  **DF-111 — `hidden-verify.js`:**
  - New `filterTryLine(line)` — matches `🍴 Try ·` / `🍴 Essayez ·`, splits the comma-separated dish list, runs it through `filterDishNames` (shared `dish-name.js` from v0.60.209). Returns the rewritten line, or `null` to drop the line entirely when no genuine dish survives.
  - `applyVerified` restructured: the rating-rewrite branch is now gated on `ratingOk`, and the Try-line filter (`lines.map(filterTryLine).filter(l => l !== null)`) runs on the way out **whether or not** the Places rating lookup succeeded. `require('./dish-name')` added at the top.
  - This is the existing per-block post-processor — no new parse pass; `parseBlocks` already captures the Try line in `block.lines`.

  **DF-109 — train-map panel localisation:**
  - `web/transport/src/i18n.js` — 14 new keys (`mrt.opens`, `mrt.openInMap`, `mrt.status.*` ×5, `mrt.showing`, `mrt.overview`, `mrt.counts`, `mrt.err.*` ×3, `mrt.aria.map`), each EN + FR.
  - `web/transport/src/components/MrtMapPanel.jsx` — takes a `lang` prop; imports `t`/`tn`. Localised: the InfoWindow popup (`Opens …`, per-line status labels, `Open 📍 in a map ↗`) and the panel chrome (focus banner, `Overview ↺`, operational/future counts, the three error states, the map `aria-label`). The popup now uses `s.opensDateFr` when `lang==='fr'` (the FR opening-date field v0.60.207 half-wired). `lang` added to the pin-render `useEffect` deps so a locale flip rebuilds the markers and their click handlers close over the current language.
  - `web/transport/src/App.jsx` — passes `lang={lang}` to `<MrtMapPanel>`.

  **`__tests__/hidden-verify.test.js`** — 5 new DF-111 tests (drops a category word, removes the whole line when nothing survives, FR `Essayez` label, runs alongside rating verification, leaves a clean line untouched).

  **`package.json`** — `0.60.209 → 0.60.210` (PATCH).

  **`doc/Journal/journal-0_60_210-16_05_26-0145.md`** — this entry (#178). **`doc/.serial-state.yml`** — `journal 177→178`, `commit 184→185`, `build 0→1`; re-anchored.

- **[VERIFICATION (sandbox)]**

  ```bash
  $ node --check hidden-verify.js                          → OK
  $ cd web/transport && npm run build                      → built in 2.25s (42 modules)
  $ npm test -- --run                                      → 1865 passed (1865)   # 1860 → 1865, +5
  ```

- **[STATUS]** STAGED → PROD on next Railway deploy. The transport TMA bundle under `public/transport/` is gitignored and rebuilt on deploy; the local `npm run build` here was verification only.

- **[TEST]** Post-deploy:
  1. `/hidden` → on any gem card, the `🍴 Try ·` line shows only real dish names; if a card had a category word it's dropped from the list (or the line vanishes).
  2. Train TMA → toggle to the Google-Map view → tap a station pin → popup shows `Opens …` / status labels / `Open … in a map` in the active locale.
  3. FR locale → popup + the focus banner / `Overview` / counts / errors all render in French; a future CCL6 station shows its FR opening date.
  4. Switch locale while the Google-Map view is open → pins rebuild and the next popup is in the new language.

- **[KNOWN GAPS]**
  - **DF-100** — bib-gourmand slice shuffled per Michelin tap. **Not a bug** — deliberate tap-to-tap variety. Left untouched per the report; revisit only if the operator wants deterministic bib ordering (and accepts losing the variety).
  - **DF-110** — single-query `intent==='tool'` path still uses the `🔧` header glyph. Its venue cards' Try line is already guarded (v0.60.209 `formatTechniqueVenueBlock`); only the cosmetic glyph remains. Deferred — the right replacement glyph is unclear (the header is an explainer sentence, not a `cuisine · method` pair like the fan-out).
  - **DF-84** — `generate.mjs` HTML-emission redundancy is **resolved** (the script no longer writes `.html`); the separate `.vibe-journal/` migration sub-issue still needs an operator call.
  - **DF-101 / 103 / 106** — no code action: operator decision / external Places data / Railway infra respectively.

## Confirmation Gates

| Gate | Authority | Stamp |
|---|---|---|
| G1 — version bump | _PATCH 0.60.209 → 0.60.210._ | auto |
| G2 — destructive action | _None._ | n/a |
| G3 — new decision rule | _Operator: "knock off DF-111, 109" — apply the dish-only Try-line criteria to the /hidden path; finish the train-map panel's FR localisation. DF-100 deliberately NOT changed (not a bug)._ | operator |
| G4 — paid external API call | _No new external calls. `hidden-verify.js` already calls Places for rating verification; only its in-process post-processing changed._ | n/a |

## Amendments

(None. v0.60.209 (#177) is the prior entry.)

## Deleted

(Nothing physically deleted. The `applyVerified` rating branch is wrapped in an `if (ratingOk)` guard in place; the hardcoded EN strings in `MrtMapPanel.jsx` are replaced by `t()`/`tn()` calls.)
