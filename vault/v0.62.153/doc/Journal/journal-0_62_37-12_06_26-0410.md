(№ 690 - 12-06 '26 04:10 SGT) v0.62.37

# Journal — soleat v0.62.37 (⭐ Recommend filter — overlay classics on the plate + the post-search city-dish tie-in)

> Operator, three messages, verbatim where it matters:
> 1. *"so few. are you sure? i think there are many dishes in singapore and johor bahru, please
>    also use the national_dish table and overlay table to check."* → verified: NATION_OVERLAY has
>    **164 SG iconic + 27 shared** vs the plate banner's 4. Operator picked **A** (overlay-fed
>    "More local classics" section).
> 2. *"after the search result then check if there are special dishes ties to these cuisine or
>    city's unique dishes… it is both cuisine special dishes as well as cities' unique dishes."*
> 3. *"the recommend with be a checkbox 'Recommend' in search criteria filter next to vegetarian.
>    uncheck default. if check, wired to the dishes, overlay, cities unique dishes"* + *"capital
>    'Recommend'. when tap, it will show in few 7 seconds what is this 'Recommend' means."*

> Commit 707 / journal 690 / **build 433 (web/ changed — cuisine)**. Revert-ready.

## New

### [HDR] #690 | 04:10 SGT 12-06-26 | v0.62.37 — classicsForCountry + plate classics section + findCityPlateDish + D792 (gated on ⭐ Recommend) + the 7-s explainer | city-plates.js + discovery-dish.js + index.js + web/cuisine (state, QuickFilters, i18n, App, ResultCard, ResultPanel, ArrivalPlate) | 1 PR | 1 build |

- **[A — "More local classics" on the Arrival Plate]** `classicsForCountry(cc)`
  (`COUNTRY_OVERLAY_SLUG`: SG/MY/TH/JP/VN/AU/NZ → overlay slug; lazy `require('./nation-overlay')`;
  fail-open) rides `platesForCity`/`platesNear` as `classics: string[]`, deduped against the
  city's own curated rows (diacritic-fold containment both ways). ArrivalPlate renders a
  collapsible **"▸ More {country} classics (n)"** chip-wrap below the 📜 rows — names only, no
  fact-card (curated-only rule), labelled by **country** honestly (these are national, not
  city-unique). Tap a chip → the same freeTextOverride dish search. SG surfaces **163** names.

- **[B — ⭐ Recommend (the operator's checkbox)]**
  - `state.js`: `recommend` in QUICK_FILTERS + both default blocks — **default OFF**.
  - `QuickFilters.jsx`: ⭐ chip in OVERFLOW, **next to Vegetarian** (operator's placement).
  - `i18n.js`: label **"Recommend"** (capital R) / "Recommander" + the explainer string.
  - `App.jsx`: checking the box shows a **7-second** explainer card above the filters
    (auto-dismiss timer; cleared on uncheck); `recommend` added to **stateSig + filterCount +
    criteriaSummary + the copy-syntax parts** — the v0.60.166 "forgot the signature" lesson
    applied in all four places up front.
  - Server: `computeCriteriaHash` hashes the whole `filters` object → the seen-set and page
    rotation fork automatically; no cache-key edits needed (verified: the discover-pool cache
    sits BEFORE the tag pass, so tags never leak across modes).

- **[C — D792, the city half of the tie-in]** `discovery-dish.findCityPlateDish(venue,
  plateDishes)` → `{ dish, tier, evidence: 'name'|'reviews' } | null` — the D790b honest ladder
  (venue **name** first, then its own reviews/editorial; romanised + native script; CJK ≥2 /
  Latin ≥4 floors). `index.js` D792 block right after D786: **gated on
  `filters.recommend === true`** + `!specialMode`; `platesNear(lat,lng)` in-memory; tags
  `v.cityDish = { dish, tier }`; logs `D792 city-classic city=… tagged=n/N`. With the box
  UNCHECKED the search behaves **exactly** as before this PR.

- **[D — surfacing]** `ResultCard`: `⭐ {tier in words} · {dish}` line (en/fr; City icon /
  Regional classic / National classic — words, never colour). `ResultPanel`: `⭐ n of N places
  here serve a local classic` summary above the list — suppressed on dish-search lists (the
  v0.62.32 E-split owns those) via the `dishEvidence !== undefined` probe.

- **[DELIBERATE SCOPE CALL — flagged to the operator]** **D786 (the cuisine-dish Try-line) stays
  always-on** — it is shipped v0.62.29–31 behaviour; gating it behind the unchecked-by-default box
  would silently remove the Try-line for every user. If the operator wants D786 inside the
  ⭐ Recommend gate too, it is a one-line change.

- **[AU-7 CORRECTION — journal № 689]** Prior entry stated *"Registry now 21 cities / 83
  dishes"* (kept verbatim there). The 83 counted searchable **names** (romanised + local script
  per row). Correct figures: **50 dish rows / ~84 searchable names**. Recorded here per AU-7 —
  the original is not rewritten.

- **[VERIFICATION (sandbox)]** `node --check` clean (city-plates / discovery-dish / index);
  **full suite 3392 / 132 files green** (+5: classics counts, dedupe, honestEmpty+classics,
  findCityPlateDish ladder incl. null-guards, guard spot-checks); `build:cuisine` green.
  Pre-merge adversarial review run on the diff (verdict recorded in the PR).

- **[STATUS]** PR-OPEN → squash-merge via gh REST. Railway redeploys server + cuisine bundle.

- **[TEST — operator, deployed]**
  1. SG anchor → banner → "▸ More Singapore classics (163)" → tap "bak chor mee" → dish search.
  2. Search criteria → ⚙ Filters → **⭐ Recommend** next to 🥗 Vegetarian, unchecked; tap it →
     the explainer shows for ~7 s.
  3. Kyoto + Japanese + ⭐ checked → 🔍: cards whose reviews mention obanzai/yudofu show
     `⭐ City icon · Obanzai`; header shows `⭐ n of 12 places…`; log `D792 city-classic`.
  4. Same search with ⭐ unchecked → no ⭐ lines, no D792 log — unchanged behaviour.

- **[KNOWN GAPS]** The ⭐ card line has no 📜 popup (v1 — the banner carries the history); the
  classics section is country-level by design; D786 not gated (see the scope call above).

- **Confirmation Gates**

  | Gate | Resolution | Date |
  |---|---|---|
  | G1 — version bump | PATCH `0.62.36 → 0.62.37`. | 12-06 '26 |
  | G2 — destructive action | None — default-off filter; unchecked = pre-PR behaviour byte-for-byte on the search path. | 12-06 '26 |
  | G3 — new decision rule | The dish layer (overlay + city plates) is opt-in via ⭐ Recommend; overlay classics are country-labelled, never city-claimed; D786 stays always-on until the operator says otherwise. | 12-06 '26 |
  | G4 — paid external API call | None — overlay + plates are in-memory; D792 is text matching on already-fetched data. | 12-06 '26 |

## Amendments

- **`city-plates.js`** — COUNTRY_OVERLAY_SLUG, `_fold`, `classicsForCountry`, `_classicsForEntry`,
  classics on `platesForCity`; export.
- **`discovery-dish.js`** — `findCityPlateDish` (exported).
- **`index.js`** — D792 block (gated, fail-open) after D786.
- **`web/cuisine/src/v2/lib/state.js`** — `recommend` in QUICK_FILTERS + defaults.
- **`QuickFilters.jsx`** — ⭐ chip; **`i18n.js`** — label + hint strings.
- **`App.jsx`** — recommendHint (7 s timer), stateSig/filterCount/criteriaSummary/copy-syntax.
- **`ResultCard.jsx`** — ⭐ tier-word line; **`ResultPanel.jsx`** — ⭐ summary line.
- **`ArrivalPlate.jsx`** — COUNTRY_LABEL + the "More classics" collapsible chip-wrap.
- **`__tests__/city-plates.test.js`** — +5 tests.
- **`package.json`** `0.62.36 → 0.62.37`; **`doc/.serial-state.yml`** — 707/690/433 + anchor
  04:10 SGT 12-06.

## Deleted

- Nothing.
