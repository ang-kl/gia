(№ 683 - 11-06 '26 20:30 SGT) v0.62.30

# Journal — soleat v0.62.30 (Location lock: the explicit-pick latch arms on every boot; Try-line: patin tempoyak + full-list verify + D786 visibility)

> Operator: *"we have problem again to lock the location. The try line didn't surface"* — with a
> Telegram transcript ("Telegram special dishes.txt") and a committed Railway log
> (`log/logs.1781179792568.json`). Chose **1 — both fixes**.

> Commit 700 / journal 683 / **build 427 (web/cuisine + web/menu changed)**. App.jsx (cuisine + menu) +
> nation-overlay.js + discovery-dish.js + index.js + tests. Revert-ready.

## New

### [HDR] #683 | 20:30 SGT 11-06-26 | v0.62.30 — explicit-pick latch on initData boots + Menu auto-detect labelled-anchor guard; Malaysian patin tempoyak + findVerifiedDish full-list scan + D786 always-log | web/cuisine/src/v2/App.jsx + web/menu/src/App.jsx + nation-overlay.js + discovery-dish.js + index.js + __tests__/discovery-dish.test.js | 1 PR | 1 build |

- **[BUG 1 — location won't lock (root cause, from the committed log)]** Bare **label-less device
  re-saves** (`set-location → 1.2722,103.8112`, no label) at 11:38 and **11:52 (mid-Sapporo)** overwrote
  the operator's deliberate Putrajaya / Sapporo picks. Two writers:
  - **Cuisine TMA (the 20-s follow sync):** on an initData-GPS boot, `tryServerCache()` is skipped, so
    the **v0.61.438 explicit-pick latch never arms** (it was only set inside tryServerCache) →
    `shouldFollowDevice` sees `explicitPick=false` → the sync saves device GPS over the labelled pick.
    **Fix:** the v0.61.203 defensive fetch (`App.jsx:~920`) — which already reads the cached location on
    exactly those boots — now **arms `explicitPickRef` whenever the cached location carries a label**.
    Coords/UX unchanged; the **v0.61.409 no-popup / no-auto-load policy stands** — the sync simply can
    no longer silently clobber a deliberate pick.
  - **Menu TMA (auto-detect, 11:35):** POSTed `label="Singapore"` over the Putrajaya pick. Its own
    step-4 comment says *"skip when the cached anchor says I'm somewhere else explicitly"*, but the code
    only skipped **SG** anchors. **Fix:** a **labelled** cached anchor is never auto-overwritten (same
    "explicit pick wins" rule, `Menu App.jsx runAutoDetect`).

- **[BUG 2 — "the Try line didn't surface" (it half-worked)]** The log shows `D786 … filled=7/12` twice
  for **japanese@Kyoto** — the v0.62.29 fill worked there. It could NOT fire for the operator's
  Putrajaya searches: (a) most had **`cuisines=[]`** (the fill needs exactly one cuisine — by design);
  (b) **malaysian@Putrajaya verified 0** because **patin tempoyak was missing** from the Malaysian
  overlay table; (c) `filled=0` logged **nothing**, making the zero undiagnosable. Fixes:
  - **`nation-overlay.js` (malaysian):** + **`patin tempoyak`** (substring form — matches reviews
    writing either "patin tempoyak" or "ikan patin tempoyak"), replacing **`beef rendang malaysian`** —
    a literal duplicate of the existing `rendang` entry — to stay inside the **≤30 cap**.
  - **`discovery-dish.js`:** `findVerifiedDish` now scans the **full unshared list** (`max:200`) — the
    default `max=8` silently skipped deeper entries (patin would have been #17); methods cap 50.
  - **`index.js` D786:** **always logs**, with the breakdown
    `filled=n/N (reviewDishes=… noVerifiedMention=…)` so a silent zero is evident.

- **[DELTA — misc]** `package.json` `0.62.29 → 0.62.30`; `doc/.serial-state.yml` commit 699→700,
  journal 682→683, **build 426→427**; re-anchored 20:30 SGT.

- **[VERIFICATION (sandbox)]** `node --check` clean (index, nation-overlay, discovery-dish); +3 patin
  tests (both review phrasings verify; cap ≤30 holds); **full suite 3376/3376** green;
  `build:cuisine` + `build:menu` green.

- **[STATUS]** PR-**OPEN**, squash-merge via gh REST. Railway redeploys server + both TMA bundles.

- **[TEST — operator, deployed]** (1) Pick **Putrajaya / Sapporo**, leave the TMA, relaunch from SG:
  the pick **holds** (no bare `set-location → 1.27,103.81` in the log; instead
  `explicit-pick latch armed from cached label`). Menu TMA no longer auto-snaps a labelled pick to
  "Singapore". (2) Search **Malaysian in Putrajaya**: venues whose reviews mention *patin tempoyak* show
  **🍴 patin tempoyak**; the log always shows `D786 … (reviewDishes=… noVerifiedMention=…)`.

- **[KNOWN GAPS]**
  - Criteria-less searches (`cuisines=[]`) still skip the Try fill by design (no unambiguous cuisine →
    no wrong-cuisine claim). A per-venue cuisine-resolution fill is a possible v2.
  - Releasing the latch: picking any new location or tapping 📍 use-current re-anchors as before — the
    latch only blocks the *silent* follow-sync overwrite.
  - The transcript's "Malaysian Yudofu" query (Malaysian chip + Japanese free-text in Kyoto) is a comb
    the fill correctly skipped; noted, not changed.

- **Confirmation Gates**

  | Gate | Resolution | Date |
  |---|---|---|
  | G1 — version bump | PATCH `0.62.29 → 0.62.30` (0.62.x line). | 11-06 '26 |
  | G2 — destructive action | One curated row swapped (a literal duplicate → patin tempoyak) inside the ≤30 cap — recorded here per AU; all other changes additive/guarded. Reversible. | 11-06 '26 |
  | G3 — new decision rule | A LABELLED cached location is an explicit pick on EVERY boot path (cuisine latch + menu auto-detect guard) — device GPS never silently overwrites it. D786 always logs its breakdown. | 11-06 '26 |
  | G4 — paid external API call | None. | 11-06 '26 |

## Amendments

- **`web/cuisine/src/v2/App.jsx`** — explicit-pick latch armed from the cached label on non-serverCache
  boots (v0.61.203 fetch site).
- **`web/menu/src/App.jsx`** — auto-detect skips labelled cached anchors ("explicit pick wins").
- **`nation-overlay.js`** — malaysian: + `patin tempoyak`, − `beef rendang malaysian` (duplicate; cap).
- **`discovery-dish.js`** — findVerifiedDish scans the full unshared list (max 200 / methods 50).
- **`index.js`** — D786 always logs with `reviewDishes=` / `noVerifiedMention=` breakdown.
- **`__tests__/discovery-dish.test.js`** — +3 patin/cap tests.
- **`package.json`** `0.62.29 → 0.62.30`.
- **`doc/.serial-state.yml`** — commit 700 / journal 683 / build 427 + anchor 20:30 SGT + prepend.

## Deleted

- The silent follow-sync overwrite of labelled picks on initData boots; the Menu auto-detect overwrite
  of labelled foreign anchors; the `beef rendang malaysian` duplicate row; the silent `filled=0` D786.
