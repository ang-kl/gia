(№ 559 - 07-06 '26 11:00 SGT) v0.61.359

# Journal — soleat v0.61.359 (Native-script venue names — "Name (local script)" across Card / Copy / Copy-To / free-text)

> Operator: add a `{local script}` line in "( )" after the venue name — RULE A (en + foreign script → append native, e.g. **Tokyo Station (東京駅)**), RULE B (skip when the script == the user's display language). Decision: **option 2 — per-venue local fetch** (exact, ~2× Places cost).
>
> **Left OPEN for review — auto-merge NOT armed.** Commit 572 / journal 559 / build 358. Server + TMA.

## New

### [HDR] #559 | 11:00 SGT 07-06-26 | v0.61.359 — `(local script)` name line on every venue surface (RULE A/B) | 5 code files | 1 PR | 1 build |

- **[INTENT]** Surface each foreign venue's native-script name beside the English one for local clarity, without redundancy for users already reading that script.

- **[MECHANISM]**
  - **`local-name.js`** (NEW) — `LOCAL_LANG_BY_CC` (JP→ja, KR→ko, CN→zh-CN, TW/HK/MO→zh-TW, TH→th); `localLangForCountry(cc, displayLang)` returns the local languageCode **only** when the country has a foreign script **and** it isn't the user's display language (**RULE B**); `fetchLocalName(placeId, lang)` = Places **Details** GET (`fields=displayName`, `languageCode=local`); `attachLocalNames(venues, cc, displayLang, apiKey)` fetches in parallel and attaches `v.nameLocal` **only** when the result carries CJK/Hangul/Thai script and differs from the name (**RULE A** — adds info, not a dup).
  - **Server wiring** — called on the general `/api/cuisine/search` response (`searchRegionCode`, `csLang`) and in `handleMichelinSearch` (`michCC`, `csLang`) before the JSON.
  - **Render** — `venue-templates.formatVenueBlock` (which powers **Copy / Copy-To / free-text**) and the TMA **`ResultCard`** both add a `(nameLocal)` line after the name. `nameLocal` is threaded through the **Copy-all** (`ResultPanel` enriched) and **Copy-one** (`ResultCard`) payloads → the server's `slim` list passes it straight to `formatVenueBlock` (no whitelist).

- **[DELTA]**
  - **`local-name.js`** (NEW) · **`index.js`** (2 wire-ins) · **`venue-templates.js`** (name-line render) · **`web/cuisine/src/v2/components/ResultCard.jsx`** (render + copy-one payload) · **`web/cuisine/src/v2/components/ResultPanel.jsx`** (copy-all payload).
  - **`package.json`** — `0.61.358 → 0.61.359`. **`doc/.serial-state.yml`** — commit 571 → 572, journal 558 → 559, build 357 → 358; re-anchored 11:00 SGT.

- **[VERIFICATION (sandbox)]** `node --check` clean; `npm run build` clean; `npx vitest run` → **106 files / 2992 tests green**. The Copy path passes the venue through `slim` unchanged, so the patched payloads reach `formatVenueBlock`.

- **[STATUS]** PR-**OPEN**, **auto-merge NOT armed**. Server + TMA. Field test: search e.g. Japan/Korea/Thailand on the en UI → each card/Copy shows "Name (native script)".

- **[TEST]** Operator: foreign venues now show their native name in "( )" after the English name, on the card and in Copy / Copy-To / free-text. A user reading that script sees no redundant block.

- **[KNOWN GAPS]**
  - **RULE B uses `csLang`** (the resolved UI language, en/fr) — not the raw `device_display_lang`. So a Japanese **device** user on the en UI may still see the Japanese block; the refinement is to read `initData.user.language_code`. (For the common en/fr case it behaves correctly — always appends.)
  - **Onboarding flow** (first-time → welcome in device language) — **not in this PR** (separate; you didn't scope it in — say the word).
  - **~2× Places cost** for foreign-country searches (one Details lookup per venue) — the accepted tradeoff for exact native names.
  - **Not live-E2E tested** (no browser / no foreign Places call here).
  - A `doc/Feature/` / `doc/Technical/` / `doc/Register/` update + a `vault/v0.61.359/` snapshot are still due.

- **Confirmation Gates**

  | Gate | Resolution | Date |
  |---|---|---|
  | G1 — version bump | PATCH `0.61.358 → 0.61.359`. | 07-06 '26 |
  | G2 — destructive action | Additive — a helper + 2 server wires + 2 render lines + 2 copy-payload fields; English-only output unchanged when no local name resolves; full suite green. | 07-06 '26 |
  | G3 — new decision rule | Foreign-script venues show "Name (native script)" (RULE A) via a per-venue Places local-language fetch; suppressed when the script matches the user's display language (RULE B). | 07-06 '26 |
  | G4 — paid external API call | Adds one Places **Details** (displayName, local language) call per venue **only in foreign-script countries** — ~2× Places for those searches (operator-approved). | 07-06 '26 |

## Amendments

- **`local-name.js`** (NEW) — RULE A/B native-name resolver (Places Details).
- **`index.js`** — `attachLocalNames` on the general search + Michelin responses.
- **`venue-templates.js`** — `(nameLocal)` line in `formatVenueBlock`.
- **`web/cuisine/src/v2/components/ResultCard.jsx`** / **`ResultPanel.jsx`** — render + Copy payloads carry `nameLocal`.
- **`package.json`** — `0.61.358 → 0.61.359`.
- **`doc/.serial-state.yml`** — counters (commit 572 / journal 559 / build 358) + anchor 11:00 SGT 07-06-26.

## Deleted

- _None._
