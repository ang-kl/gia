(№ 606 - 08-06 '26 23:11 SGT) v0.61.406

# Journal — soleat v0.61.406 (Location mismatch: never ask — auto-use the current device location)

> Operator chose **"1"**: *never ask — auto-use where you are.*
>
> Commit 619 / journal 606 / **build 390**. web/cuisine. **Revert-ready** (detection effect only).

## New

### [HDR] #606 | 23:11 SGT 08-06-26 | v0.61.406 — auto-use device, drop the keep/use modal | App.jsx | 1 PR | 1 build |

- **[INTENT]** The keep/use modal *("Your saved spot is X, but you appear to be at Y")* was a full-screen overlay that **blocked the results until the user picked** — the root of both *"why didn't build the result"* and *"why still ask"* (the shown log was the **v0.61.403** container, pre-v0.61.404). Operator: **never ask** — just use where you are.

- **[DELTA]** (`web/cuisine/src/v2/App.jsx` — the anchor/device coherence **detection** effect only, `~1046`)
  - When a saved anchor is **>150 km from the device** (caught **once per mount**), it no longer raises the modal (`setAnchorMismatch`). It **silently auto-uses the DEVICE location**: `setLocationAnchor(null)` → `setFlyTarget(device)` + `setSearchCenter(device)` → region delta (SG / JB / OTHER+MY) → `saveUserLocation` + `saveCountryPref` → a brief toast *"Using your current location: {label} · 📍 to change"* → `runSearch(device)`.
  - Claims `initialLoadFiredRef` + `initialSearchDone` so the gate-opener doesn't double-fire; **does not** set `modalPendingRef` (the gate-opener bails **only** on that — `App.jsx:1127` — so the gate opens normally).
  - `anchorMismatch` is now always `null` → the keep/use modal + `applyAnchorCoherenceChoice` are **dead code** (left in, harmless).
  - Fires **once** (`anchorCoherenceCheckedRef`) so a deliberate mid-session pick isn't clobbered; the re-render after `setLocationAnchor(null)` re-runs the effect but it bails on the guard (no loop).
  - **`package.json`** `0.61.405 → 0.61.406`; **`doc/.serial-state.yml`** commit 618 → 619, journal 605 → 606, **build 389 → 390**; re-anchored 23:11 SGT.

- **[VERIFICATION (sandbox)]** Cuisine build green. Full suite **113 files / 3107 tests**.

- **[STATUS]** PR-**OPEN**, **auto-merge armed (squash)**. Railway redeploys on merge.

- **[TEST]** Operator: open the Cuisine TMA with a saved spot far from where you are → **no modal**; the map flies to your current location, a brief *"Using your current location…"* toast shows, and the 5 stream in. To browse the saved spot, use **📍 recents** / the location field. **No spinner hang.**

- **[KNOWN GAPS / SAFETY]**
  - **Contained to the detection effect** — fires once per mount, no `modalPendingRef` hold, `initialLoadFiredRef` owns the boot load (no double-search), guard prevents loops. The gate-opener + the rest of the gate are untouched. **Revert-ready.**
  - The old saved spot remains in **📍 recents** (reversible) — auto-use-device is not destructive.
  - The country-level `coherenceMismatch` modal is **not** touched (it didn't fire for this SG-anchor / MY-device case — `countryPref` MY == device MY). If a country-level prompt ever resurfaces, the same treatment can extend there.
  - Not unit-tested (React gate flow). **Operator must verify NO hang on the live TMA.**

- **Confirmation Gates**

  | Gate | Resolution | Date |
  |---|---|---|
  | G1 — version bump | PATCH `0.61.405 → 0.61.406`. | 08-06 '26 |
  | G2 — destructive action | Auto-resolve replaces the modal; the saved spot stays in recents (reversible); gate-opener untouched. | 08-06 '26 |
  | G3 — new decision rule | A stale far anchor at mount → **silently use the device location**; never ask. | 08-06 '26 |
  | G4 — paid external API call | One `runSearch` (the boot load that previously waited on the modal) — no net increase. | 08-06 '26 |

## Amendments

- **`web/cuisine/src/v2/App.jsx`** — anchor/device detection auto-uses the device location instead of raising the keep/use modal.
- **`package.json`** — `0.61.405 → 0.61.406`.
- **`doc/.serial-state.yml`** — counters (commit 619 / journal 606 / build 390) + anchor 23:11 SGT 08-06-26.

## Deleted

- The keep/use anchor-mismatch modal path is now unreachable (left as dead code; not deleted to keep the diff small + revert trivial).
