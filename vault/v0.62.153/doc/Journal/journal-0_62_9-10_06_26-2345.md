(№ 654 - 10-06 '26 23:45 SGT) v0.62.9

# Journal — soleat v0.62.9 (Revert the cuisine corpus — operator: "too heavy / won't scale")

> Operator, after reviewing the Phase-1 corpus (#972, v0.62.8): *"i think this solution doesn't work"*
> → reason (AskUserQuestion): **"Too heavy / won't scale"** — a per-city harvest (22 min + 7.2 MB for
> ONE city, × the world = millions of rows + storage + monthly refresh) is too much machinery for the
> recall problem. Decision: **remove the corpus.**

> Commit 671 / journal 654 / **build 416 (HELD — server/data only, no `web/`)**. Pure removal of the
> v0.62.8 additions. The corpus was **unwired**, so this is a clean no-op on the live search.

## New

### [HDR] #654 | 23:45 SGT 10-06-26 | v0.62.9 — remove the Phase-1 cuisine corpus (unwired; operator: too heavy/won't scale) | -corpus-cuisine-map.js -corpus-store.js -scripts/build-cuisine-corpus.py -data/corpus/* -2 tests | 1 PR | 0 builds |

- **[INTENT]** v0.62.8 (#972) shipped the corpus **data foundation only** (harvester + map + Redis-Geo
  store + the 7.2 MB Bangkok shard), explicitly **unwired** (zero behaviour change). On review the
  operator judged the whole corpus direction too heavy to scale. Since nothing consumed it, the
  cleanest response is to remove it entirely rather than carry dead weight.

- **[DELTA — removed]** `corpus-cuisine-map.js`, `corpus-store.js`, `scripts/build-cuisine-corpus.py`,
  `data/corpus/` (`bangkok.json.gz` 7.2 MB + `manifest.json` + `NOTICE.txt`),
  `__tests__/corpus-cuisine-map.test.js`, `__tests__/corpus-store.test.js`.
- **[DELTA — kept]** `doc/Journal/journal-0_62_8-*.md` is **retained as history** (the corpus PR did
  happen; AU-7 = preserve the record, don't erase it). `package.json` `0.62.8 → 0.62.9`;
  `doc/.serial-state.yml` commit 670→671, journal 653→654, **build held**; re-anchored 23:45.

- **[VERIFICATION (sandbox)]** Nothing in `index.js` / the live search referenced the corpus modules
  (they were never imported), so removal cannot regress the running app. Full suite expected green
  (the 2 removed test files were self-contained); `node --check` on the tree clean.

- **[STATUS]** PR-**OPEN**, **auto-merge via gh REST** (gh GraphQL is 401 this session; `gh api -X PUT
  …/merge` works, as does the GitHub MCP for create). **Zero runtime behaviour change.**

- **[KNOWN GAPS / what's still open]** The original symptom stands: "Japanese in Bangkok" returns **5**
  because the live pipeline compresses the ~60 Places results (rating-floor 3.7 + geo-ring collapse to
  ~5 km + first-tap-5). The **light fix that remains on the table** (no corpus, no storage, no spend):
  tune the live search — loosen the geo-ring for dense cities, keep nearest-first, raise the page size
  so users browse ~20–60 of the already-fetched pool. Deferred pending operator go-ahead. (Research
  trail in the plan file; Google's hard 60/query cap means "literal hundreds" needs per-search
  multi-area queries, which the operator can opt into separately.)

- **Confirmation Gates**

  | Gate | Resolution | Date |
  |---|---|---|
  | G1 — version bump | PATCH `0.62.8 → 0.62.9` (0.62.x line). | 10-06 '26 |
  | G2 — destructive action | Removal of an UNWIRED, never-consumed subsystem → no behaviour change; the journal-0_62_8 record is preserved. Reversible (the code is in git history at #972). | 10-06 '26 |
  | G3 — new decision rule | Curated per-city corpus is abandoned as too heavy to scale; recall improvements (if any) will be live-search tuning, not a stored corpus. | 10-06 '26 |
  | G4 — paid external API call | None. | 10-06 '26 |

## Amendments

- **Removed:** `corpus-cuisine-map.js`, `corpus-store.js`, `scripts/build-cuisine-corpus.py`,
  `data/corpus/*`, `__tests__/corpus-cuisine-map.test.js`, `__tests__/corpus-store.test.js`.
- **`package.json`** `0.62.8 → 0.62.9`.
- **`doc/.serial-state.yml`** — counters (commit 671 / journal 654 / build 416 held) + anchor 23:45 SGT
  + `last_anchor_source` prepend.

## Deleted

- The entire Phase-1 cuisine-corpus subsystem (superseded by the decision to not pursue a stored corpus).
  Its journal entry (`journal-0_62_8-*.md`) remains as the historical record.
