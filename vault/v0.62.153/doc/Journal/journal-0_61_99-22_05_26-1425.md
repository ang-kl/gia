(№ 306 - 22-05 '26 14:25 SGT) v0.61.99

# Journal — soleat v0.61.99 (widen station-detail bus/taxi range)

> Operator — *"The selected station only show the station inform and
> exits, don't show the taxi stand or bus stop in Train MTA."*
>
> This entry is commit 306 / journal 299 / build 105, off `main` at
> v0.61.98 (#591).

## New

### [HDR] #299 | 14:25 SGT 22-05-26 | v0.61.99 — station bus/taxi range | ~2 kb | 5 files | 1 PR | 1 build |

- **[INTENT]** The Transport-map station-detail view showed exits but
  no bus stop or taxi pin. Best-effort fix on the rendering path.

- **[DELTA]**
  - **`index.js`** — `/api/transport/station-context`: the bus-stop
    and taxi search radius widens 400 m → 600 m so a station's nearby
    amenities reliably fall in range.
  - **`web/cuisine/src/v2/lib/mapOverlays.js`** (+ the byte-identical
    `web/hawker/src/lib/mapOverlays.js` and
    `web/transport/src/lib/mapOverlays.js`) — `attachAmenityPins` no
    longer filters out taxi features classified `kind:'stop'`.
    `data/geo-taxis.json` names features `"Taxi Stand"` /
    `"Taxi Pick Up / Drop Off"` / `"Taxi Stop"`; the endpoint's
    name-regex only tags the first two with a `kind`, so the generic
    `"Taxi Stop"` points were dropped from the station-detail pins.
  - **`package.json`** — `0.61.98` → `0.61.99` (PATCH).

- **[VERIFICATION (sandbox)]**

  ```
  node --check index.js   clean
  npm run build (5 TMAs)  all green
  npm test                1944/1944 passing, 67 files
  ```

- **[STATUS]** DRAFT — PR #592 on `claude/v0.61.99-station-amenity-fix`
  (off `main` at v0.61.98, `a5188be`). On merge deploys via the Railway
  auto-deploy on `main`.

- **[TEST]** Transport TMA: tap a station → the detail view shows
  bus-stop + taxi pins where data exists within 600 m of the station.

- **[KNOWN GAPS]**
  - **The bus side is a best-effort fix.** `transport.nearestStops`
    works for the `/transport` bot flow, so if bus stops still return
    empty from `/api/transport/station-context` after the radius
    widen, the cause is the Redis bus-stop GEO index (or the
    endpoint's 30-min response cache holding a stale-empty payload),
    not the radius — that needs a runtime check this sandbox can't do.
  - **Still due — the `/app/map` (`public/app.js`) parity overhaul**
    (operator spec items #3–#7: station taps → info card, working
    dropdown, top-edge toggle row, the zoom-integer location pin,
    carpark Proper-Case + overlap→icon, the train toggle showing
    tappable stations).
  - No in-sandbox visual verification (no Maps webview).
  - `doc/VibeCodingRecord/` regen still due (carried forward).

- **Confirmation Gates**

  | Gate | Resolution | Date |
  |------|-----------|------|
  | G1 — version bump | PATCH `0.61.98` → `0.61.99` | 22-05 '26 |
  | G2 — destructive action | _code-only_ — two radius literals widened; one over-tight taxi filter removed; no doc/data file deleted | 22-05 '26 |
  | G3 — new decision rule | _n/a_ — bug fix | 22-05 '26 |
  | G4 — paid external API call | _n/a_ — the endpoint already calls LTA; no new call added | 22-05 '26 |

## Amendments

_None._

## Deleted

_None._
