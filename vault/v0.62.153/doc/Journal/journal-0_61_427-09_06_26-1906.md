(№ 627 - 09-06 '26 19:06 SGT) v0.61.427

# Journal — soleat v0.61.427 (/rating promoted into the public slash menu)

> Operator chose **"1" = yes, add /rating to the slash menu** (offered at the end of the v0.61.426 ship). The `/rating` (`/ra`) handler already existed; this surfaces it in Telegram's command autocomplete.
>
> Commit 640 / journal 627 / **build 405 (holds — server-only)**. index.js only. Revert-ready.

## New

### [HDR] #627 | 19:06 SGT 09-06-26 | v0.61.427 — /rating in setMyCommands (EN + FR) | index.js | 1 PR | 0 build |

- **[INTENT]** v0.61.426 shipped `/rating` (`/ra`) as a handler-only command (like `/hidden`), not in the slash menu — flagged as a known gap with an offer to promote it. Operator answered **"1"** (yes). Add the row so users discover it via Telegram's `/` autocomplete.

- **[DELTA]** (server-only)
  - **`index.js`** — `registerCommandsMenu()`: a `'rating'` entry added to **both** `enCommands` and `frCommands`, positioned **between `search` and `clipboard`** (it modifies search results, so it sits with the find-an-eatery cluster).
    - EN: `Min rating filter · /rating 0–5 (0 = any), shared with Cuisine (or /ra)`
    - FR: `Filtre de note min. · /rating 0–5 (0 = toutes), partagé avec Cuisine (ou /ra)`
  - `setMyCommands` is re-issued at startup (EN default + `language_code: 'fr'`), so the row appears on next bot boot. No other code path touched.
  - **`package.json`** `0.61.426 → 0.61.427`; **`doc/.serial-state.yml`** commit 639 → 640, journal 626 → 627, **build 405 holds**; re-anchored 19:06 SGT.

- **[VERIFICATION (sandbox)]** `node --check index.js` clean. Full suite **3167/3167 green** (no test delta — pure menu-string addition).

- **[STATUS]** PR-**OPEN**, **auto-merge armed (squash)**. Railway redeploys (server) on merge; the new row registers when the bot re-runs `registerCommandsMenu()` at boot.

- **[TEST — operator, deployed]** Type `/` in the chat → the autocomplete list shows **rating** with the description above (English; French for fr-locale chats). Tapping it inserts `/rating`; sending bare shows the current setting.

- **[KNOWN GAPS]**
  - The command surface is now **14 entries** (was the "canonical 12" + `/rating`). If it feels crowded, the operator can drop a lower-value row — say which.
  - VibeCodingRecord regen still due (covers #942 + this) — log/do post-merge.

- **Confirmation Gates**

  | Gate | Resolution | Date |
  |---|---|---|
  | G1 — version bump | PATCH `0.61.426 → 0.61.427`. | 09-06 '26 |
  | G2 — destructive action | Adds one menu row; no behaviour change to any handler. Reversible. | 09-06 '26 |
  | G3 — new decision rule | `/rating` is a publicly-listed command (operator-sanctioned promotion from handler-only). | 09-06 '26 |
  | G4 — paid external API call | None. | 09-06 '26 |

## Amendments

- **`index.js`** — `registerCommandsMenu` EN + FR lists gain a `rating` row (between `search` and `clipboard`).
- **`package.json`** — `0.61.426 → 0.61.427`.
- **`doc/.serial-state.yml`** — counters (commit 640 / journal 627 / build 405 holds) + anchor 19:06 SGT 09-06-26 + `last_anchor_source` prepend.

## Deleted

- The "/rating exists but is hidden from the slash menu" state (superseded — it's now listed).
