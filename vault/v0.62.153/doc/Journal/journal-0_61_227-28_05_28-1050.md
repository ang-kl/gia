(№ 428 - 28-05 '26 10:50 SGT) v0.61.227

# Journal — soleat v0.61.227 (Menu TMA: per-country "common city" cascading dropdown)

> Operator (28-05 '26): *"in other words we may need another field
> for common cities of the country (adjust to keep to top 8 cities
> of each countries by tourism destination then by population size)
> next to the country, an child dropdown button related to the
> country dropdown button. It should affect Menu TMA and Cuisine TMA.
> Only Malaysia have all the capital of the state listed in the
> child dropdown button."*
>
> This PR ships the Menu TMA half. Cuisine TMA cascade + `/s` search
> wiring + Menu→Cuisine propagation are tracked as v0.61.228+.
>
> v0.61.226 (social-profiles arc) is held local pending its own PR —
> this PR doesn't touch any of those files.
>
> Commit 440 / journal 428 / build 230, on branch
> `claude/v0.61.227-menu-city-cascade` off `main` at v0.61.225.

## New

### [HDR] #428 | 10:50 SGT 28-05-28 | v0.61.227 — Menu TMA city cascade | 4 files | 1 PR | 1 build |

- **[INTENT]** Let the user pick a destination city from a short
  curated list (top-8 tourism / population per country) instead of
  having to type a free-text query — and for Malaysia, expose all
  state / federal-territory capitals so any state can be set with
  one tap.

- **[DELTA]**

  **1. NEW `web/menu/src/cities.js`** — per-country city catalogue.

  | Country | Cities |
  |---|---|
  | **MY** (15 — operator exception) | KL, Putrajaya, Shah Alam, JB, Alor Setar, Kota Kinabalu, Kuching, Kuantan, Kota Bharu, Kuala Terengganu, George Town, Ipoh, Seremban, Malacca City, Kangar |
  | ID (8) | Jakarta, Bali, Yogyakarta, Bandung, Surabaya, Medan, Semarang, Makassar |
  | TH (8) | Bangkok, Chiang Mai, Phuket, Pattaya, Hua Hin, Krabi, Ayutthaya, Koh Samui |
  | VN (8) | HCMC, Hanoi, Da Nang, Hoi An, Nha Trang, Hue, Phu Quoc, Dalat |
  | PH (8) | Manila, Cebu, Boracay, Palawan, Davao, Tagaytay, Baguio, Iloilo |
  | BN (6) | BSB + 5 districts |
  | KH (8) | Phnom Penh, Siem Reap, Sihanoukville, Battambang, Kampot, Kep, Koh Rong, Kratié |
  | LA (8) | Vientiane, Luang Prabang, Vang Vieng, Pakse, Savannakhet, Phonsavan, Champasak, 4000 Islands |
  | MM (8) | Yangon, Mandalay, Naypyidaw, Bagan, Inle, Mawlamyine, Pyin Oo Lwin, Hpa-An |
  | AU (8) | Sydney, Melbourne, Brisbane, Perth, Adelaide, Gold Coast, Canberra, Cairns |
  | NZ (8) | Auckland, Wellington, Christchurch, Queenstown, Rotorua, Dunedin, Hamilton, Napier |
  | JP (8) | Tokyo, Osaka, Kyoto, Yokohama, Fukuoka, Sapporo, Hiroshima, Nara |
  | KR (8) | Seoul, Busan, Incheon, Jeju, Daegu, Daejeon, Gwangju, Gyeongju |
  | CN (8) | Shanghai, Beijing, Guangzhou, Shenzhen, Chengdu, Hangzhou, Xi'an, Suzhou |
  | HK (8) | TST, Central, Causeway Bay, Mong Kok, Wan Chai, Sha Tin, Aberdeen, Tung Chung |
  | TW (8) | Taipei, Kaohsiung, Taichung, Tainan, Hsinchu, Keelung, Jiufen, Sun Moon Lake |

  Total **133 cities across 16 countries**. Each entry is `{ name, lat, lng }` with a 5-decimal centroid (sourced from public OneMap / city-government open data). The lat/lng is used to set the Menu TMA anchor directly, bypassing the geocode round-trip.

  Exports: `CITIES_BY_COUNTRY`, `citiesForCountry(code)`, `findCity(code, name)`.

  **2. `web/menu/src/components/LocationFieldMenu.jsx`** — three edits:

  - Import `citiesForCountry`.
  - New `cityPick` state + an effect that clears `cityPick` on every `countryPref` change.
  - New `onCityPick(name)` handler — finds the city in the catalogue, calls `postSetLocation({ lat, lng, label, country })`, clears the picker.
  - In the OTHER-mode form, a `<select>` rendered between the country flag and the free-text input, gated on `citiesForCountry(countryPref).length > 0`. SG mode doesn't render the city select (SG has no city catalogue — its precinct quick-pick remains the canonical control).

  **3. `web/menu/src/i18n.js`** — new key `'loc.other.city'` → `'City'` / `'Ville'`.

  **4. `package.json`** — `0.61.226 → 0.61.227` (operator had pre-bumped to .226 for the held social-profiles arc; this PR claims the next number).

  **5. `doc/.serial-state.yml`** — commit 439→440, build 229→230, journal 427→428; re-anchored 28-05 '26 10:50 SGT.

- **[VERIFICATION (sandbox)]**

  ```
  $ node --input-type=module -e "import { CITIES_BY_COUNTRY, … } from './web/menu/src/cities.js'; …"
      countries: 16
      MY cities: 15
      JP cities: 8
      SG cities (expected 0): 0
      TOTAL cities: 133
      Kuching: { name: 'Kuching', lat: 1.5535, lng: 110.3593 }
  ```

  Vite build verification deferred to CI (local has the recurring iCloud / Node-24 PostCSS quirk).

- **[STATUS]** PR-OPEN on push.

- **[TEST]** Operator (post-merge + Railway redeploy):
  1. Open Menu TMA from any OTHER anchor (e.g. KL).
  2. Pick country = 🇲🇾 — new city `<select>` should appear between the flag and the input, listing all 15 capitals (KL, Putrajaya, Shah Alam, JB, …).
  3. Pick "Kuching" → anchor jumps to Kuching (`1.5535, 110.3593`) — verify the map / nearby tiles re-render.
  4. Flip country to 🇯🇵 → city `<select>` swaps to the 8 JP cities; the prior MY pick is cleared.
  5. Flip country to 🇸🇬 → city `<select>` disappears (SG mode); precinct quick-pick still works.

- **[KNOWN GAPS]**
  - Cuisine TMA cascade + Menu→Cuisine flag/city propagation + `/s` search wiring are tracked as v0.61.228+.
  - Centroid lat/lng is a single point per city. For very large cities (e.g. Tokyo, Sydney) the centroid is downtown — search around it still finds the wider metro. Future PR could let users tap a sub-precinct after the city.
  - SG is intentionally absent from `CITIES_BY_COUNTRY`. Its precinct quick-pick already covers SG. If the operator later wants SG to also use the cascade format, add an `SG` entry mapping to the 10 STB precincts.

- **Confirmation Gates**

  | Gate | Resolution | Date |
  |---|---|---|
  | G1 — version bump | PATCH `0.61.226 → 0.61.227`. New file + 3 small edits. | 28-05 '26 |
  | G2 — destructive action | _n/a_. The social-profiles draft files in the working dir are left untouched. | 28-05 '26 |
  | G3 — new decision rule | YES — the per-country city catalogue lives in `web/menu/src/cities.js` (Menu TMA), with a parallel copy to be added for Cuisine TMA in v0.61.228. Both lists must stay in sync (same shape as the existing `countries.js` duplication pattern). | 28-05 '26 |
  | G4 — paid external API call | _n/a_. City coords are baked-in, no geocode call. | 28-05 '26 |

## Amendments

- **`web/menu/src/components/LocationFieldMenu.jsx`** — OTHER-mode form gains a city `<select>` between the flag dropdown and the text input.

## Deleted

_None._
