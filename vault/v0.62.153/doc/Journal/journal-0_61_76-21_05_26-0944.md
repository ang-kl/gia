(№ 283 - 21-05 '26 09:44 SGT) v0.61.76

# Journal — soleat v0.61.76 (docs + vault pass)

> Operator: *"Vault."* — take a frozen reproducible snapshot of the repository
> and ship it alongside a freshly regenerated master document set. Scope
> confirmed by the operator as **snapshot + fresh master docs** — the heavier
> of the two options, matching the v0.61.28 vault pass.
>
> This is a documentation + vault pass: no source-code change, no version bump.
> The branch additionally folds a routine `origin/main` sync so the snapshot
> captures the true current state (v0.61.76).
>
> This entry is commit 283 / journal 276 / build 83.

## New

### [HDR] #276 | 09:44 SGT 21-05-26 | v0.61.76 — docs + vault pass | ~154 MB | 873 vault files + 7 doc files | 1 PR | 1 build |

- **[INTENT]** Capture a frozen, reproducible, append-only snapshot of soleat
  at v0.61.76 (`vault/v0.61.76/`) and produce a fresh, non-incremental master
  document set consolidating the v0.61.29 → v0.61.76 arc — the same ceremony
  as the v0.61.28 vault pass (journal #231).

- **[DELTA]**
  - **`vault/v0.61.76/`** — new frozen snapshot, **873 files / ~154 MB**. A
    faithful mirror of the repository minus the `doc/CLAUDE-FULL.md` §16.1
    exclusions (`node_modules/` root + nested incl. `.vibe-journal/`, `.git/`,
    `vault/`, `dist/` + `web/*/dist/` + `public/{cuisine,menu,hawker,transport,oversight}/`
    build artefacts, `log/` + `*.log`, `.env`, `.claude/settings.local.json`,
    `tmp/`). `geoloc/*.geojson` open data IS kept (source input to
    `build-geo-overlays.js`).
  - **`vault/v0.61.76/VAULT_README.md`** — headline, captured timestamp,
    exclusion table, file count + size, boot instructions, and the
    v0.61.28 → v0.61.76 arc table (per §16.1 required sections).
  - **Fresh master document set** (non-incremental consolidation at v0.61.76):
    - `doc/Builder/builder-0_61_76-21_05_26-0944.md` — Builder #3 (138 lines)
    - `doc/Persona/persona-0_61_76-21_05_26-0944.md` — Persona #4 (151 lines)
    - `doc/Feature/feature-0_61_76-21_05_26-0944.md` — Feature #47 (130 lines)
    - `doc/Technical/technical-0_61_76-21_05_26-0944.md` — Technical #47 (185 lines)
    - `doc/Legal/legal-0_61_76-21_05_26-0944.md` — Legal #9 (179 lines)
    - `doc/Register/register-0_61_76-21_05_26-0944.md` — Register #115 (142 lines)
  - **`doc/.serial-state.yml`** — counters bumped: commit 282→283, build
    82→83, journal 275→276, builder 2→3, persona 3→4, feature 46→47,
    technical 46→47, legal 8→9, register 114→115. `last_anchor_*` re-anchored
    to 21-05 '26 09:44 SGT. A `# journal #276` comment block was added.
  - **`origin/main` sync** — merged into the working branch so the snapshot
    reflects v0.61.76. Merge conflicts on `package.json` (resolved to
    `0.61.76`) and `doc/.serial-state.yml` (resolved to `main`'s side, which
    already carried journal #275 for PR #570).
  - **No source-code change. No version bump** — consistent with the v0.61.28
    vault pass (journal #231: *"Operator instruction; no code change, no
    version bump"*).

- **[VERIFICATION (sandbox)]**

  ```
  node --check index.js / i18n.js        clean   (post origin/main merge)
  npm test -- --run                      1928/1928 passing, 65 files
  vault/v0.61.76 integrity               873 files, 154 MB; 0 node_modules,
                                         0 nested vault, 0 *.log, 0 stray dist
  master-doc serial lines                Builder №3 · Persona №4 · Feature №47
                                         · Technical №47 · Legal №9 · Register
                                         №115 — all present, format correct
  ```

- **[STATUS]** DRAFT — to be opened as a draft PR on
  `claude/review-gia4lunch-readme-3s798`. No deploy: docs + vault only, zero
  runtime code change, so the Railway auto-deploy on `main` is a no-op for
  this pass.

- **[TEST]** Not a runtime feature — verification is the snapshot-integrity
  + test-suite checks above. To validate the vault is bootable:
  `cp -a vault/v0.61.76 /tmp/soleat-v0.61.76 && cd /tmp/soleat-v0.61.76 &&
  npm install && npm test -- --run` (expect 1,928 green).

- **[KNOWN GAPS]**
  - **Rule V-2** — the prior vault `vault/v0.61.28/` is **KEPT, not
    superseded**. Each vault is an append-only point-in-time snapshot
    (Rule V-1); `v0.61.29 → v0.61.76` is the delta arc between the two.
  - **Rule V-3** — `vault/v0.61.76/doc/` is re-synced *after* the fresh
    master set + this journal entry were written, so the snapshot DOES
    contain its own self-referential journal addendum. The rule permits
    omitting it; it is included here for completeness.
  - **v0.61.74 / v0.61.75 / v0.61.76** (PRs #572 / #573 / #574) were not
    individually journaled — they are covered collectively in this entry's
    `[DELTA]` arc and the VAULT_README arc table. A doc-lag, consistent with
    the doc-system catch-up cadence (`CLAUDE-FULL.md` §17.3).
  - `doc/VibeCodingRecord/` regen still due (append #570–#574, regenerate the
    ledger + hosted page) — logged here per the standing rule; to be picked
    up next pass.
  - Verification is sandbox-only this pass (no Railway deploy, by design).

- **Confirmation Gates**

  | Gate | Resolution | Date |
  |------|-----------|------|
  | G1 — version bump | _n/a_ — docs + vault pass, no version bump (v0.61.28 precedent) | 21-05 '26 |
  | G2 — destructive action | _n/a_ — purely additive (new `vault/v0.61.76/` tree + 7 new doc files + counter bumps); the `origin/main` sync is a standard non-destructive merge | 21-05 '26 |
  | G3 — new decision rule | _n/a_ — the fresh non-incremental master set follows the existing decision **D-1028** (AU Recipe overridden for vault-pass master sets); no new rule | 21-05 '26 |
  | G4 — paid external API call | _n/a_ | — |

## Amendments

_None._

## Deleted

_None._
