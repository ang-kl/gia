(№ 610 - 09-06 '26 09:04 SGT) v0.61.410

# Journal — soleat v0.61.410 (Cuisine TMA: country-strict "saved ≈ device" — a different country is a mismatch even under 150 km)

> Operator (AskUserQuestion follow-up to v0.61.409): chose **country-strict** — *"Treat a different COUNTRY as a mismatch even if it's under 150 km (e.g. saved Johor MY but device Singapore → no auto-load)."*
>
> Commit 623 / journal 610 / **build 393**. web/cuisine. Revert-ready (one check).

## New

### [HDR] #610 | 09:04 SGT 09-06-26 | v0.61.410 — country-strict mismatch on the anchor↔device check | App.jsx | 1 PR | 1 build |

- **[INTENT]** v0.61.409 gated *"saved ≈ device"* on the existing **150 km** distance threshold (so the daily SG↔JB crossing stayed "same"). I asked which rule the operator wanted; they chose **country-strict**: a different country is a mismatch even under 150 km.

- **[DELTA]** (`web/cuisine/src/v2/App.jsx` — the `anchorMismatch` check only)
  - The `if (km <= 150) return` early-out now ALSO considers country:
    ```js
    const anchorCC = coordsToCountry(a);        // 'SG' / 'MY' / null
    const deviceCC = coordsToCountry(userLoc);
    const countryDiffers = !!anchorCC && !!deviceCC && anchorCC !== deviceCC;
    if (km <= 150 && !countryDiffers) { …return; }   // only "same" when BOTH agree
    ```
  - So a **Johor (MY) anchor + Singapore device** (~30 km — previously "same" under the v0.61.276/409 150 km rule) is now a **mismatch** → boot load **suppressed**, empty TMA, *tap 🔍* (reuses the v0.61.409 `haltBootLoadForMismatch` path — no popup, no new wait).
  - `null` country (outside the SG/MY bbox) is uncomparable, so **distance alone** decides there — overseas anchors are unaffected.
  - The suppress log + halt reason now name the country delta when that's the trigger.
  - **`package.json`** `0.61.409 → 0.61.410`; **`doc/.serial-state.yml`** commit 622 → 623, journal 609 → 610, **build 392 → 393**; re-anchored 09:04 SGT.

- **[VERIFICATION (sandbox)]** Cuisine **vite build green**. Full suite **3107/3107 green**.

- **[STATUS]** PR-**OPEN**, **auto-merge armed (squash)**. Railway rebuilds the TMA on deploy.

- **[TEST]** Operator: with a saved **Johor** spot, open the Cuisine TMA while physically in **Singapore** → no auto-load, empty TMA + *tap 🔍* (was: auto-loaded because <150 km). Same-country / same-area still auto-loads.

- **[KNOWN GAPS]**
  - This **reverses** the v0.61.276 *"JB crossing stays same"* intent — by **explicit operator choice**.
  - Country resolution is SG/MY only (the `coordsToCountry` bbox); cross-border cases outside SG/MY still fall back to the 150 km distance test.
  - Eyes-on-deployed still pending (operator to confirm on the live TMA).

- **Confirmation Gates**

  | Gate | Resolution | Date |
  |---|---|---|
  | G1 — version bump | PATCH `0.61.409 → 0.61.410`. | 09-06 '26 |
  | G2 — destructive action | One added clause on the anchor check; reversible. | 09-06 '26 |
  | G3 — new decision rule | "Saved ≈ device" requires SAME country (when both resolvable) AND ≤ 150 km. | 09-06 '26 |
  | G4 — paid external API call | None. | 09-06 '26 |

## Amendments

- **`web/cuisine/src/v2/App.jsx`** — `anchorMismatch` check: added the country-strict clause + log/halt-reason naming.
- **`package.json`** — `0.61.409 → 0.61.410`.
- **`doc/.serial-state.yml`** — counters (commit 623 / journal 610 / build 393) + anchor 09:04 SGT 09-06-26 + `last_anchor_source` prepend.

## Deleted

- The distance-only *"saved ≈ device"* rule (superseded: now country-strict AND distance).
