(№ 528 - 06-06 '26 09:41 SGT) v0.61.327

# Journal — soleat v0.61.327 (OTHER cities: rename Malaysia 'Johor Bahru' → 'Johor')

> Operator (06-06 '26, approved data rename): in the OTHER-mode Malaysia cities table, rename the entry **'Johor Bahru' → 'Johor'** so the entry represents the wider Johor (state), not just the city. Change ONLY the `name`; keep `code: 'JHB'` and `lat: 1.4927 / lng: 103.7414` exactly. Do not touch any other entry, any other country, the iata-cities table, or the i18n `region.johor` label (the separate JB region pill stays as-is). Mirror the same rename into the gia-web browser port.
>
> Tiny, surgical data rename — one literal per file, no logic, no UI, no API change. Commit 541 / journal 528, branch off main @ v0.61.326 (sha 3127b72).

## New

### [HDR] #528 | 09:41 SGT 06-06-26 | v0.61.327 — OTHER cities: rename Malaysia 'Johor Bahru' → 'Johor' | 4 files | 1 PR | 1 build |

- **[INTENT]** The OTHER-mode Malaysia cities table listed the Johor anchor as `'Johor Bahru'`. The operator wants the label to read `'Johor'` so it represents the wider Johor. Rename the `name` only; `code` and coordinates unchanged. Pure data — no logic, no UI, no API change. The JB region pill + the i18n `region.johor` string are a separate surface and are deliberately left untouched.

- **[DELTA]**

  **1. `web/cuisine/src/v2/lib/cities.js`** — Malaysia block: the single row
    `{ name: 'Johor Bahru',      code: 'JHB', lat: 1.4927,  lng: 103.7414 }`
  became
    `{ name: 'Johor',            code: 'JHB', lat: 1.4927,  lng: 103.7414 }`
  Only the `name` literal changed; `code: 'JHB'`, `lat: 1.4927`, `lng: 103.7414` preserved byte-for-byte. Padding re-balanced (+6 spaces after the comma) so the `code:` column stays aligned with the neighbouring `Kuala Lumpur` row.

  **2. `web/menu/src/cities.js`** — the parallel copy (header says "keep in sync"). Same single-row rename, byte-identical alignment.

  **3. `package.json`** — `0.61.326 → 0.61.327`.

  **4. `doc/.serial-state.yml`** — commit 540 → 541, journal 527 → 528; re-anchored 09:41 SGT 06-06-26 with `last_anchor_source` describing the Johor Bahru → Johor rename.

- **[VERIFICATION (sandbox)]**
  - `node --check web/cuisine/src/v2/lib/cities.js` → **clean**.
  - `node --check web/menu/src/cities.js` → **clean**.
  - Pre-edit assertion: each file contained the literal `name: 'Johor Bahru'` exactly **once**.
  - Post-edit assertion: each file now contains `name: 'Johor'` exactly once and no remaining `Johor Bahru` substring.
  - `code: 'JHB'`, `lat: 1.4927`, `lng: 103.7414` confirmed unchanged on that row; no other Malaysia row, no other country block touched.
  - iata-cities table NOT touched. i18n `region.johor` label NOT touched.

- **[STATUS]** PR-OPEN; auto-merge squash armed (bot repo auto-merges on green CI). Railway redeploys on merge.

- **[TEST]** Operator (post-merge): in OTHER mode, the Malaysia city picker now shows the entry as **Johor** (was "Johor Bahru"); the pick still anchors to the same JHB coordinates.

- **[KNOWN GAPS]**
  - **i18n `region.johor` / JB region pill** — left as-is by design; this rename is only the OTHER-mode cities-table label.
  - **gia-web mirror** shipped as a SEPARATE, review-first PR (`lib/cities.ts`, version 0.1.114) — see that repo; it does NOT use this journal protocol and is left OPEN for review.

- **Confirmation Gates**

  | Gate | Resolution | Date |
  |---|---|---|
  | G1 — version bump | PATCH `0.61.326 → 0.61.327`. Pure data rename. | 06-06 '26 |
  | G2 — destructive action | _n/a_. One-literal rename; no removal, no other entry/block touched, code+coords preserved. | 06-06 '26 |
  | G3 — new decision rule | _n/a_. Follows the existing OTHER-mode cities-table conventions. | 06-06 '26 |
  | G4 — paid external API call | _n/a_. No API surface; static table data only. | 06-06 '26 |

## Amendments

- **`web/cuisine/src/v2/lib/cities.js`** — Malaysia row name `'Johor Bahru' → 'Johor'` (code + lat/lng unchanged).
- **`web/menu/src/cities.js`** — same rename (parallel sync copy).
- **`package.json`** — `0.61.326 → 0.61.327`.
- **`doc/.serial-state.yml`** — counters (commit 541 / journal 528) + anchor re-set 09:41 SGT 06-06-26.

## Deleted

_None._