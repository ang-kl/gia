(№ 802 - 30-06 '26 20:20 SGT) v0.62.448

# Journal — soleat v0.62.448 (Transport — normalize station-detail close to ✕)

> Operator: "proceed with Train TMA and Hawker TMA" (continuing the subtle-close-✕ consistency pass).

## New

### [HDR] #802 | 20:20 SGT 30-06-26 | v0.62.448 — Transport close ✕ normalize | 3 files | PR #(pending) |
- **`web/transport/src/components/LineStatusPanel.jsx`** — the selected-station detail card's
  dismiss used `×` (U+00D7) at `text-sm` without `flex-shrink-0` and an `aria-label="×"`.
  Normalized to **`✕` (U+2715)** + `flex-shrink-0` + a proper `Close` / `Fermer` aria-label —
  matching the Cuisine pattern (v0.62.446/447). Anchor unchanged (flex header row, `ml-auto`).
- **Hawker** — audited (App.jsx + 6 components + mapOverlays.js): **no pill/back/modal close
  exists**. Hawker-centre detail renders as inline cards; the map InfoWindows already use
  `✕` (U+2715, ~13px). The only pop-overs are the self-dismissing **LocaleToggle** (outside-tap /
  select) and **MapControls** (outside-tap / Esc) dropdowns — left as-is (same treatment as
  Cuisine's LocaleToggle). **No Hawker code change this PR.**
- **[VERIFICATION (sandbox)]** `esbuild` JSX parse OK; full transport/cuisine/hawker builds gated
  by CI on the PR.
- **[STATUS]** PROD — transport TMA rebuild on merge.
- **[KNOWN GAPS / OPEN Q]** Whether the self-dismissing LocaleToggle + MapControls dropdowns
  should also gain a visible ✕ across **all three** TMAs — pending operator confirm. Note
  `MapControls.jsx` is byte-identical across cuisine/transport/hawker (header-comment invariant),
  so any ✕ there must be mirrored to all three.

## Confirmation Gates
| Gate | Status | Note |
|------|--------|------|
| G1 | ✅ | esbuild parse; CI builds transport |
| G2 | ➖ | no unit tests touch this view |
| G3 | ✅ | serial 801→802 |
| G4 | ⏳ | redeploy + operator eyeball |
