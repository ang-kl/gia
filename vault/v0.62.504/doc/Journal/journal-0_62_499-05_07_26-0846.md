(№ 853 - 05-07 '26 08:46 SGT) v0.62.499

# Journal — soleat v0.62.499 (Cuisine TMA: 🔙 back FAB gets the 💬 liquid-glass at 85% opacity) — FAB visual harmony

> Operator: "the '🔙 Back' FAB should have opaque 85% or similar like '💬' fab when turn on".
> Read as: the back FAB looks too see-through; give it the same frosted-solid look the 💬 composer FAB has, at ~85% opacity.

## New

### [HDR] #853 | 08:46 SGT 05-07-26 | v0.62.499 — 🔙 back FAB liquid-glass @ 85% | 1 web file (1 className) | PR #(pending) |

- **[INTENT].** The 🔙 back FAB read as weaker / see-through over the map next to the 💬 composer FAB.
  Trace (`web/cuisine/src/v2/App.jsx` ~5033): the back FAB used `bg-tg-card/95` with **no** `liquid-glass`
  class, while the 💬 FAB (~5072) uses `bg-tg-card/75 liquid-glass`. Despite the *higher* opacity number,
  the back FAB looked less solid because it lacked the frosted `backdrop-blur` layer that gives the 💬 FAB
  its presence. The fix is not lowering opacity — it is adding the same glass treatment.

- **[DELTA] (1 code file, 1 className).** `web/cuisine/src/v2/App.jsx`
  - Back FAB `bg-tg-card/95` → **`bg-tg-card/85 liquid-glass`** — matches the 💬 FAB's glass treatment at the
    operator's requested **85%** opacity.
  - Border (`border-2 border-tg-hint/60`), `shadow-lg`, size (`h-10`, `w-10`/`px-3`), and the emoji-only-EN
    vs localised-word logic are all unchanged.

- **[VERIFICATION (sandbox)]** esbuild esm JSX parse **clean** (179.9 kB bundle, only react + `./tg.js`
  externals — not syntax). `.liquid-glass` confirmed defined in `web/cuisine/src/styles.css:68` (+ dark-theme
  variant at :79). `web/cuisine` production build via CI.

- **[STATUS]** PROD — Cuisine TMA redeploys on merge (Railway). G4: operator eyeball on the deployed back FAB
  (frosted-solid, visually matches the 💬 FAB).

- **[TEST]** esbuild parse + `web/cuisine` build in CI (pure presentational className change; no test harness
  for FAB styling).

- **[KNOWN GAPS]** None for this change. Standing: MRT CCL6 go-live flip (task #17, 12 Jul — operator chose
  to do it on the day); set-location TZ bot line (`index.js:5739` hard-coded SGT); PR #1456 journal-record
  rebase (conflicting, next); G4 CJK/ru/de live eyeball; FR Michelin BBOX geo-guard.

## Confirmation Gates
| Gate | Status | Note |
|------|--------|------|
| G1 | ✅ | esbuild parse clean; single className change; liquid-glass class verified present |
| G2 | ✅ | web/cuisine build green in CI |
| G3 | ✅ | serial commit 970→971, build 609→610, journal 852→853; anchor re-set 05-07 '26 08:46 SGT |
| G4 | ⏳ | redeploy + operator eyeball on deployed 🔙 FAB (frosted-solid, matches 💬) |
