(№ 764 - 28-06 '26 14:05 SGT) v0.62.388→406

# Journal — soleat v0.62.388→406 (BACKLOG catch-up: Clipboard TDZ fix, hawker-first, location-display/anchor fixes, Telegram LocationManager, Phase-3 zh/ja/es locale, native-script A1 + B1)

> AU-7 / standing-rule catch-up. Journal #760 (v0.62.387) covered through the doc-catchup+vault
> anchor; #761–#763 covered v0.62.407→409. This entry back-fills the **gap arc v0.62.388→406**
> (PRs #1365–#1383), recorded after the fact from the session record + the merge log. Times are
> approximate (PR merge order is authoritative); PR numbers are exact.

## New

### [HDR] #764a | Clipboard TMA black-screen — diagnose → fix | web/clipboard/src/* | PRs #1365, #1367 |
- **[INTENT]** Operator: "look into clipboard TMA still crashed" → "1" (diagnose-then-decide,
  not a ground-up rewrite).
- **[DELTA]** v0.62.388 (#1365) added a bulletproof diagnostic boot that surfaced the real cause:
  a **TDZ** — `const initial = { route: parseRoute() }` in `web/clipboard/src/lib/state.js` ran
  before the `let startParamConsumed` it reads. v0.62.390 (#1367, URGENT) fixed it by hoisting the
  `let` above `initial`.
- **[STATUS]** PROD. Lesson saved to memory (TDZ from declaration order).

### [HDR] #764b | SG hawker-first ranking | PR #1366 |
- **[INTENT]** Operator: "this is for Singapore — if user taps a local food pick, try hawker
  centre first." Chose "Hawker-first, then fall back."
- **[DELTA]** v0.62.389 — local-classic dish taps bias to hawker-centre venues first, falling back
  to general results when none.
- **[STATUS]** PROD.

### [HDR] #764c | Location display / anchor thrash + native location | PRs #1368–#1370, #1373, #1374 |
- **[INTENT]** Operator: Sunshine Coast resets to AU›Canberra; "results are correct, just the
  display is thrashed"; first-launch Telegram "Allow Once" not registering; "tap/press map to set
  location."
- **[DELTA]**
  - v0.62.391 (#1368) — added **Sunshine Coast** (AU) to cuisine+menu city pickers; added MCY to
    iata-cities; fixed iata + city-centroids tests (count 139→140, allow 'manual' source).
  - v0.62.392 (#1369) — **display follows the search anchor** (the Other-city label stops drifting
    back to the committed capital).
  - v0.62.393 (#1370) — use **Telegram native LocationManager (Bot API 8.0)** via
    `getTelegramLocation()` (cuisine + menu tg.js), gated `isVersionAtLeast('8.0')`; replaces the
    flaky first-launch navigator.geolocation path.
  - v0.62.396 (#1373) — **divergence guard**: `reassertPickAfterSearch` no longer re-pushes a
    drifted `locationAnchor` (skip when searched-center coords diverge >0.01) → stops the
    AU›Canberra server-cache reset.
  - v0.62.397 (#1374) — **long-press the map** (≥550ms, no-drag) to pin a location.
- **[STATUS]** PROD. (Cuisine location reconciliation is the most-thrashed area — these were
  ASK-first changes, no timers added to the boot gate.)

### [HDR] #764d | "Try: Rich" — adjective-only dish names rejected | PR #1372 |
- **[INTENT]** Operator (screenshot): "why 'Try Rich' in the card."
- **[DELTA]** v0.62.395 — `dish-name.js` `isDishName()` adds `isAllQualityWords()` guard
  (QUALITY_WORDS rejects rich/creamy/nice/best/signature… when the WHOLE candidate is adjectives);
  fixes the review-scrape (`cuisine-enrich.js`) stranding an adjective as a dish.
- **[STATUS]** PROD.

### [HDR] #764e | Phase-3 locale: zh, then ja + es (Cuisine TMA) | PRs #1371, #1375, #1376 |
- **[INTENT]** Operator: "proceed phase 3" / "proceed Ja/es."
- **[DELTA]** v0.62.394 (#1371) zh locale infra (ZH_STRINGS overlay, SUPPORTED_LOCALES,
  LocaleToggle 🇨🇳); v0.62.398 (#1375) zh → `open-hours.js` (fmtTime/DAY_LABELS/OH_PHRASES);
  v0.62.399 (#1376) ja + es overlays (JA/ES_STRINGS, 🇯🇵/🇪🇸, App.jsx ternaries). Locales now
  en/fr/id/ru/de/zh/ja/es (8).
- **[STATUS]** PROD.

### [HDR] #764f | Translation gaps: cuisine cards, fun-fact, cuisine names | PRs #1377, #1378 |
- **[INTENT]** Operator: "fun fact + cuisine choices (sweets, michelin/bib) not translated at
  all" → "take that."
- **[DELTA]** v0.62.400 (#1377) — `cat.sweetsFusion/cat.michelinBib/cat.setMeal` added to ALL
  overlays (ru/de/zh/ja/es were falling to English); fun-fact now prefers the **selected** lang
  (`FunFactModal` was using deviceFactLang only). v0.62.401 (#1378) — `cuisine-i18n.js`
  `cuisineName()` localises all 69 cuisine NAMES zh/ja/es; wired in CuisineCategoryDrawer + folio
  tab.
- **[STATUS]** PROD.

### [HDR] #764g | Dish-name audit → A1 native-script + B1 specific drinks | PRs #1379–#1383 |
- **[INTENT]** Operator: respect the original expression (e.g. tori no karaage 鶏の唐揚げ); do a
  slow audit; "Australian wines" is ambiguous. Chose "A1 + B1 first."
- **[FINDINGS (read-only audit)]** native-script coverage ~3%; ~84 English-description names;
  ~12 ambiguous drinks; Wikipedia image availability ~88–96%.
- **[DELTA]**
  - **B1** (v0.62.402, #1379) — ambiguous drinks made specific in `nation-overlay.js`:
    german beer→**hefeweizen**, british ale→**cask ale**, american craft beer→**ipa**,
    australian wine→**shiraz**.
  - **A1** native-script overlay `nation-overlay-local.generated.js` (keyed
    `slug::dish.toLowerCase()` → `dish.local`, merged at load when absent; ArrivalPlate already
    renders `d.local`): batch 1 Chinese (#1379), batch 2 Korean/Thai/Vietnamese (#1380), batch 3
    Cantonese/Sichuan/Hong-Kong (#1381), batch 4 Hokkien/Teochew/Hainanese/Hakka (#1382), batch 5
    Shanghainese/Taiwanese/Hunan/Northeastern/Northwestern (#1383). (Japanese carries inline
    `local` in nation-overlay.js, not repeated.) End-of-arc overlay = 364 entries. [Continued
    post-arc: SG +103 (#1386, v0.62.408), Peranakan +6 (#1387, v0.62.409) → 473.]
- **[STATUS]** PROD.

- **[VERIFICATION (sandbox)]** Each PR in-arc: `node --check` / `esbuild` parse on changed files;
  test suite green in CI (auto-merge gate); per-PR version bump. (Retrospective entry — not
  re-run now.)
- **[KNOWN GAPS]** Native-script non-CJK scripts (Indian/Tamil, Arabic, Cyrillic, Greek,
  Georgian — ~240 dishes) still pending → grounded pipeline. Wikipedia image pipeline (audit
  thread C) not built. Live-TMA eyeballs for the locale/native-script surfaces ongoing.

## Confirmation Gates

| Gate | Status | Note |
|------|--------|------|
| G1 build/parse | ✅ | per-PR in-arc (CI) |
| G2 tests | ✅ | per-PR in-arc (CI auto-merge gate) |
| G3 journal/serial | ✅ | this back-fill; serial 763→764 |
| G4 live deploy | ✅ | all 19 PRs merged + deployed |
