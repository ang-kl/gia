(№ 782 - 17-06 '26 13:20 SGT) v0.62.141

# Journal — soleat v0.62.134 → v0.62.141 (map/results/footer rework: remove Google nav cluster · floating result drawer → horizontal-default strip · Menu neo-skeuomorphism · footer rebuild)

> Operator (live screenshot-driven session, paraphrased + quoted where it matters): "remove the Google
> navigation button from all TMA"; the header "doesn't contrast during light mode" and on a mode tap should
> show the Location + "Local food picks" fields "as opaque"; make the floating results "a Google Map phone
> App drawer" that can "switch to horizontal scroll"; "change the layout of MENU TMA, it isn't
> neo-skeuomorphism. the train area looks awful"; then a reversal — "the result should default to horizontal
> scroll … without showing the vertical result", "when I scroll in horizontal mode … just blink the google
> map pin (don't auto-pop up the card unless is in vertical mode)", "close list / vertical-horizontal buttons
> should be stacked", "move the common FAB (end/back, down/top, search) into [the blank footer]"; the
> footer "search card and free-text search to move out of the current TMA" (blue 🔍 opens the criteria
> sheet); then refinements — "the blank footer is taking too much height, reduce by 50%", cards "in a
> collapsed mode with a scroll bar … scroll down … without having to expand" at "5 rows", "tap on the card,
> the Google map should zoom to 14 and then zoom out when I tap out", and the list + vertical/horizontal
> switches "should be in the footer".

> Counters this arc: journal 741 → 742, commit 799 → 800, build 449 → 450. App version PATCH chain
> **0.62.133 → 0.62.141** (8 app bumps; #1085 doc-only, no bump). 9 PRs (#1085–#1093) all squash-merged to
> `main`; Railway redeploys + rebuilds the TMAs via `postinstall: npm run build`.

> **Build-environment note (operational, recorded per AU-7):** the operator's working tree lives on iCloud
> Drive, whose file-sync had corrupted `.git` with collision duplicates (`refs/heads/main 2`,
> `refs/remotes/origin/HEAD 2`, `HEAD 2`, `index 2…5`) — `git fetch` failed with "did not send all
> necessary objects" and `git checkout`/`fetch` hung on iCloud I/O. All of this arc's code was therefore
> built, tested and pushed from a **clean clone outside iCloud** (`/tmp/gia-build`), then the iCloud `.git`
> was repaired (junk dups removed, `origin/main` re-fetched). The iCloud working copy is still parked on the
> stale `claude/v0.61.316-vibe-journal-regen` branch pending an operator `git checkout main && git pull`.

## New

### [HDR] #782 | 13:20 SGT 17-06-26 | v0.62.134 → v0.62.141 — map/results/footer rework (remove Google nav cluster · drawer → horizontal-default strip · Menu neo-skeuomorphism · footer rebuild) | web/cuisine · web/hawker · web/transport · web/menu · doc/VibeCodingRecord · public/doc/* | 9 PRs | 0 explicit build |

- **[INTENT]** A screenshot-driven UI session across the Telegram Mini Apps. Three asks the operator opened
  with turned out to be **already shipped on `main`** (the centre-map ↹ button @ v0.62.133 and the result-map
  zoom 11→12 @ v0.62.132); the genuinely-new work was: (a) **remove Google's native camera/pan nav cluster**
  from every map; (b) a **light-mode-opaque floating header** that reveals the Location + Local-food-picks
  fields on a mode-pill tap; (c) a **floating result drawer**; (d) restyle the **Menu TMA** into the
  Cuisine TMA's neo-skeuomorphic language (the flat "train area" being the worst offender). The operator then
  **reversed the drawer default** to a horizontal floating card strip (vertical hidden until toggled) with
  blink-only pin feedback, **rebuilt the footer** to carry the free-text composer + Edit-search + the common
  FABs with the blue 🔍 opening a criteria bottom sheet, and finally **refined** the strip (compact 5-row
  scroll cards, tap→zoom-14, footer-hosted list + vertical/horizontal switches, a ~50%-shorter footer band).

- **[DELTA]** (file-by-file, per PR; version in parentheses)

  - **#1085 — docs catch-up (no bump).** Recorded the prior v0.62.114→v0.62.133 arc:
    `doc/Journal/journal-0_62_133-…md`, `doc/Feature/feature-0_62_133-…md`, VibeCodingRecord +27 rows
    regenerated, `doc/.serial-state.yml` (journal 740→741, feature 54→55, commit 798→799, build 448→449).
  - **#1086 — remove Google native camera nav cluster (v0.62.134).** `web/cuisine/src/v2/components/MapPanel.jsx`
    + `web/hawker/src/components/HawkerMapPanel.jsx` + `web/transport/src/components/MrtMapPanel.jsx`:
    `cameraControl: true → false` (and dropped the now-unused `cameraControlOptions`) in all three map-init
    blocks. `disableDefaultUI:true` + `zoomControl:false` already strip the rest; the custom top-right cluster
    (Reset / + / ↹ centre / − / expand, the ↹ from v0.62.133) now owns all on-map navigation.
  - **#1087 — opaque header in light mode + reveal on mode-tap (v0.62.135).** `web/cuisine/src/v2/App.jsx`:
    resting header frosting `bg-tg-bg/85 → /90` + border + `shadow-sm` (AA separation; liquid glass kept on
    scroll). New `modePeek` state — tapping a *different* region pill (or 📍 Current) scrolls to top and turns
    the header + the Location + "Local food picks" fields fully **opaque** (a staging area); `triggerSearch`
    clears it. Fields stay in normal flow (no reflow jump) so scroll-to-top always shows them.
  - **#1088 — floating result drawer (v0.62.136).** New `web/cuisine/src/v2/components/ResultDrawer.jsx`
    (replacing the `ResultCarousel` mount) + `App.jsx`: a Google-Maps-style bottom-sheet over the FAB cluster,
    **vertical by default** with a ↰/↴ toggle to the horizontal swipe layout. *(Superseded two PRs later — see
    #1090; the vertical default was reversed to horizontal.)*
  - **#1089 — Menu TMA neo-skeuomorphism (v0.62.137).** `web/menu/src/styles.css` (new theme-agnostic
    `.skeuo-card` / `.skeuo-pill` + `:active` press / `.skeuo-inset` via `color-mix` over the Telegram theme
    vars + neutral rgba shadows — light + dark, luminance-only = colour-blind safe; Menu's `tg.js` sets no
    `data-theme`) + `components/Tile.jsx` (raised glass tiles, debossed press) + `components/TrainPanel.jsx`
    (rebuilt as a `.skeuo-card` with a recessed `.skeuo-inset` status strip + raised `.skeuo-pill` Map/More
    buttons; the deliberate 2-row layout from v0.60.58 preserved).
  - **#1090 — results default to horizontal floating strip; vertical on toggle (v0.62.138).** `App.jsx` +
    `ResultDrawer.jsx` + `MapPanel.jsx`. **Reverses #1088's vertical default** per operator. `drawerMode`
    defaults `'horizontal'`; the strip auto-shows whenever a result set exists (no pin tap), ✕ dismisses it
    (`drawerDismissed`, reset on new results). The vertical `ResultPanel` is hidden (`display:none`) but kept
    **mounted** so its pagination still feeds the map's visible-page markers. New `MapPanel` `blinkOnly` prop —
    horizontal-mode focus pans + **flashes the pin only** (no zoom, no info pop-up). *(zoom behaviour changed
    again in #1093.)*
  - **#1091 — horizontal cards bottom-aligned + scroll-to-expand (v0.62.139).** `ResultDrawer.jsx`
    (track `items-end`; each card a `card-scroll` scroll panel) + `ResultCard.jsx` (new `defaultExpanded`
    prop) + `web/cuisine/src/styles.css` (`.card-scroll` thin **visible** scrollbar, `--tg-border` thumb —
    the "more below" affordance, two ways to view: scroll or ⌄/⌃).
  - **#1092 — Cuisine footer rebuild (v0.62.140).** `App.jsx`: the footer becomes one fixed ROW —
    `⇠ end · Edit search ▾ · 💬 free-text · ↑ top` — with the **blue 🔍 floating above** (blue 🔍 and
    "Edit search ▾" both open the criteria sheet). `TellMePanel` (free-text) moved out of page flow into the
    row; the Search-criteria card became a footer-docked **bottom sheet** (open = fixed/opaque/scrollable with
    the existing Collapse header + in-sheet "🔍 Search" bar still firing + loading more via `triggerSearch`;
    closed = just the active-filter chips in flow). `triggerSearch` closes the sheet on fire; the old
    standalone 🔍 fire/load-more FAB + "More eats?" bubble retired; page bottom-padding reserves the footer.
  - **#1093 — results/footer refinements (v0.62.141).** `App.jsx` + `ResultDrawer.jsx` + `MapPanel.jsx`.
    (1) Blank footer ~**50% shorter** (offset 0.5→0.35rem, gap-1.5→1, blue 🔍 w-11→w-9, reserved padding
    7→5rem, footer-text margins/padding trimmed). (2) Cards default to a **compact ~5-row** scroll panel
    (`max-h 46vh → 10.5rem`); full content renders so scrolling reveals detail without expanding.
    (3) **Tap → map `setZoom(14)`** + blink; tap-out restores via the existing `prevFocusZoom` path (was
    blink-only). (4) The **list on/off switch + vertical/horizontal toggle moved into the footer** top row —
    the list switch persists so the strip can be turned back on after ✕; the v/h toggle shows only while the
    list is open ("when active"); the strip's stacked ✕/↴ and the old ↰ FAB removed; the vertical
    `ResultPanel` now also respects the list switch.

- **[VERIFICATION (sandbox)]** Every code PR: the affected `web/*` app built green via `vite build`
  (`npm run build:cuisine|hawker|transport|menu`), and the full `vitest run` suite passed **3458/3458**
  (137 files) on each. The two sequential merges that touched `package.json` (#1087, #1088 vs the
  already-merged version bump) were resolved keeping the higher version; #1088's App.jsx auto-merged with
  #1087's cleanly (disjoint hunks).

- **[STATUS]** PROD. All 9 PRs squash-merged to `main`; `main` @ **v0.62.141**. Railway redeploys per PR and
  rebuilds the TMAs through `postinstall: npm run build`.

- **[TEST]** No new unit tests this arc — the work is FE layout / CSS / interaction, outside the suite's
  reach (the suite covers server search/geo/format logic). The standing 3458-test suite stayed green
  throughout as a regression guard.

- **[KNOWN GAPS]**
  - **On-device verification still owed** (flagged on every visual PR): the opaque-header reveal (#1087),
    the horizontal strip + compact 5-row scroll + zoom-14 (#1090/#1091/#1093), the **footer rebuild**
    (#1092 — footer-row fit on a narrow phone; free-text input + keyboard behaviour inside a *fixed* footer
    in the Telegram WebView), and the **Menu neo-skeuomorphism** (#1089, light + dark). Build + tests green
    is necessary, not sufficient — these need eyes on the deployed TMA.
  - **Active-filter chips placement (deferred):** picture 3 shows the active-filter chips at the very top;
    #1092 currently renders them in-flow when the criteria sheet is collapsed. Noted in the PR for a
    follow-up relocation.
  - **Doc set:** this catch-up updates Journal + serial-state + VibeCodingRecord only. A `doc/Feature/`
    and `doc/Technical/` refresh for the v0.62.134→141 UI surface, and a `vault/v0.62.141/` snapshot, are
    **still due** (logged here and in `doc/Register/`).

| Gate | Question | Verdict |
|------|----------|---------|
| **G1** | Operator request captured verbatim where it matters? | ✅ — quotes in the header block. |
| **G2** | Source-of-truth = current `main` code (not PR prose)? | ✅ — DELTA written from the merged diffs / built files. |
| **G3** | Journal recorded per the standing per-PR rule? | ✅ — this HDR + VibeCodingRecord rows #1085–#1093. |
| **G4** | Reversals / supersessions disclosed, not silently rewritten? | ✅ — #1088 vertical-default superseded by #1090; #1090 blink-only superseded by #1093 zoom-14; both flagged in DELTA (AU-7). |
