(№ 545 - 06-06 '26 20:16 SGT) v0.61.345

# Journal — soleat v0.61.345 (Mainland China Michelin — fill `CN-michelin.js`; 475 venues + native hanzi name/address; +9 CITY_IATA cities; CN manifest — last stub filled)

> Operator: *"china"* → `instruction/China.js`. China fills the **last empty stub** — 475 venues (2nd-biggest after Japan), 15 cities, native `nameZh`/`addressZh` on every row. JP/KR shape: **2025 partial** (64, no three-star) + **2026 full** (411). **Milestone:** all 10 country tables are now non-empty.
>
> **Left OPEN for review — auto-merge NOT armed.** Commit 558 / journal 545 / build 344, landed via the GitHub Git Data API on origin/main @ v0.61.344.

## New

### [HDR] #545 | 20:16 SGT 06-06-26 | v0.61.345 — Mainland China Michelin live: `CN-michelin.js` = 475 curated venues (hanzi name + address); +9 loader CITY_IATA cities; CN manifest (64/411) | 2 code files | 1 PR | 1 build |

- **[INTENT]** Fill the China stub (CN was already in `COUNTRY_TABLES` + the empty-tables guard) with the operator's curated `instruction/China.js` — the full venue-centric Mainland China dataset (475 venues, 15 cities, 2025 + 2026, plus native `nameZh` + `addressZh`). With CN filled, **no empty stubs remain**.

- **[MECHANISM]** Bare array literal of 475 venue objects (venue-award-schema.v0_1 + `nameZh`/`addressZh`/`postal`). Copied **byte-for-byte** into `CN-michelin.js` — hanzi (`潮上潮 (朝阳)`, `朝阳区金和东路`) preserved. The loader validates each `city`; 6 of 15 cities (Shanghai/Beijing/Guangzhou/Chengdu/Hangzhou/Suzhou) were mapped, **9 were not** (Xiamen, Nanjing, Taizhou, Fuzhou, Wenzhou, Quanzhou, Yangzhou, Changzhou, Ningde) → added to `CITY_IATA` with their nearest-airport codes (XMN/NKG/HYN/FOC/WNZ/JJN/YTY/CZX; Ningde→FOC, no own airport). `cities.js`/`iata-cities.js` untouched. `venueToVenue` projects `nameZh`/`addressZh` out (store-now / display-later, same as JP/KR). Because CN was the last empty table, the test's empty-tables guard + the empty-country false-gate are retired/repointed.

- **[DELTA]**

  **1. `CN-michelin.js`** (root) — empty v0.61.333 stub → `const COUNTRY='CN'; const ENTRIES=[ …475 venues verbatim… ]; module.exports={COUNTRY,ENTRIES}`. Header → v0.61.345 (source, 15 cities, CN-locale fields, 2025-partial manifest). 475 unique ids, 475 awards.

  **2. `michelin-data.js`** —
  - `CITY_IATA`: China block extended with 9 cities (Xiamen→XMN, Nanjing→NKG, Taizhou→HYN, Fuzhou→FOC, Wenzhou→WNZ, Quanzhou→JJN, Yangzhou→YTY, Changzhou→CZX, Ningde→FOC).
  - `COUNTRY_MANIFEST` + `CN: { 2025:{two:3,one:17,bib:44,total:64}, 2026:{three:3,two:22,one:104,bib:282,total:411} }`. 2025 omits three-star (none in source).

  **3. `__tests__/michelin-data.test.js`** —
  - **Removed** the `it('the empty guide country tables ship zero rows …')` block (CN was its last member — no empty stubs remain) and the file-header bullet that listed it.
  - **Flipped** the false-gate `it('is false for the empty guide cities/countries')` (Shanghai/CN — now both true) → `it('is false for countries with no Michelin table (not curated)')` asserting India/IN + France/FR false.
  - Added a full CN load suite (475 venues / 475 awards, 64@2025 / 411@2026, per-tier manifest `{two:3,one:17,bib:44}` + `{three:3,two:22,one:104,bib:282}`, unique ids, every `city` curated incl. the 9 new keys, **a source-table assertion that `nameZh` `潮上潮 (朝阳)` / `addressZh` hanzi are preserved**, Xiamen + Ningde resolve), a `CN-michelin.js is venue-centric with 475 rows` require test, and a `hasMichelinData` CN-true case (CN / cn / Shanghai / Xiamen / Ningde).

  **4. `package.json`** — `0.61.344 → 0.61.345`.

  **`doc/.serial-state.yml`** — commit 557 → 558, journal 544 → 545, build 343 → 344; re-anchored 20:16 SGT 06-06-26.

- **[VERIFICATION (sandbox)]**
  - `node --check` clean: `CN-michelin.js`, `michelin-data.js`, `__tests__/michelin-data.test.js`.
  - Real-loader temp-harness (all 10 `{CC}-michelin.js` + patched `michelin-data.js` in `/tmp/cntest`): loads **without throwing**. `venuesForCountry('CN')===475`; global unique ids **1925/1925** (MY 70 + TH 180 + JP 589 + KR 117 + TW 196 + VN 83 + HK 147 + MO 34 + PH 34 + CN 475, no dup); CN 2025=64 / 2026=411; tiers `{two:3,one:17,bib:44}` (2025) + `{three:3,two:22,one:104,bib:282}` (2026); `sum(CN awards)===475`. CN manifest passes. 9 new CITY_IATA keys resolve. `hasMichelinData` true for CN/Shanghai/Xiamen/Ningde; false for India/France.
  - **16/16** explicit assertions green.
  - Full `npm test` runs in CI on merge.

- **[STATUS]** PR-**OPEN**, **auto-merge NOT armed**. Landed via the GitHub Git Data API on a branch off origin/main @ v0.61.344. Railway redeploys on merge.

- **[TEST]** Operator: Mainland China is now in the venue loader — 475 venues across 15 cities, the full 2026 edition (411) + the partial 2025 (64). Native Chinese name + address stored on every row (hanzi preserved). **This was the last empty country table — all 10 are now loaded** (MY/TH/VN/JP/KR/CN/HK/TW/MO/PH), 1925 venues total in `michelin-data.js`.

- **[KNOWN GAPS]**
  - **2025 is PARTIAL** — 64 venues, no three-star. A fuller 2025 China list is **still due** when supplied.
  - **`nameZh` / `addressZh` not surfaced** — stored but `venueToVenue` projects them out; shared display follow-up with JP (`nameJa`) / KR (`nameKo`).
  - **CN cities not in the OTHER-mode picker** — loader-side (`CITY_IATA`) only.
  - **Unified loader still off the live search path** — all 10 countries via `michelin-data.js` not yet wired into the bot's Michelin search (SG standalone). This is now the **headline next step** (all data is in place; nothing surfaces it yet).
  - A `doc/Feature/` / `doc/Technical/` / `doc/Register/` update + a `vault/v0.61.345/` snapshot are still due.

- **Confirmation Gates**

  | Gate | Resolution | Date |
  |---|---|---|
  | G1 — version bump | PATCH `0.61.344 → 0.61.345`. | 06-06 '26 |
  | G2 — destructive action | None. Empty `CN-michelin.js` stub filled; append-only CITY_IATA + manifest edits; the empty-tables test retired (its subject no longer exists) + false-gate repointed. Curated `instruction/China.js` untouched. Loader clean (1925/1925 unique). | 06-06 '26 |
  | G3 — new decision rule | Mainland China loads from `instruction/China.js` (475 venues) VERBATIM; 9 missing cities added to loader CITY_IATA; CN manifest 2025 64 (partial) / 2026 411; `nameZh`/`addressZh` stored now, displayed later. All 10 country tables now non-empty. | 06-06 '26 |
  | G4 — paid external API call | _n/a_. Data load + CITY_IATA/manifest registration only. | 06-06 '26 |

## Amendments

- **`CN-michelin.js`** — empty stub → 475 curated venues from `instruction/China.js` (verbatim, hanzi intact); header → v0.61.345.
- **`michelin-data.js`** — `CITY_IATA` +9 China cities; `COUNTRY_MANIFEST` +`CN` (2025 64 / 2026 411).
- **`__tests__/michelin-data.test.js`** — empty-tables test removed (no empty stubs left); false-gate repointed Shanghai/CN → India/France; +CN load suite + `CN-michelin.js` 475-row require test + `hasMichelinData` CN case.
- **`package.json`** — `0.61.344 → 0.61.345`.
- **`doc/.serial-state.yml`** — counters (commit 558 / journal 545 / build 344) + anchor re-set 20:16 SGT 06-06-26.

## Deleted

- The `it('the empty guide country tables ship zero rows …')` test — its subject (empty stub tables) no longer exists now that CN is filled.
