(№ 813 - 01-07 '26 14:05 SGT) v0.62.459

# Journal — soleat v0.62.459 (Cuisine — Pick-local-classic hint: own line + translated)

> Operator: "(1) put the 'Tap a dish to learn more, then 🔍 Find eateries.' on the next line in
> Pick Local Classic. (2) also this sentence isn't translated."

## New

### [HDR] #813 | 14:05 SGT 01-07-26 | v0.62.459 — plate tap-hint line + i18n | 3 files | PR #(pending) |
- **`web/cuisine/src/v2/components/ArrivalPlate.jsx`** — the stage-2 hint used to trail inline after
  the 📍 city on the same line, and was hard-coded **fr/en only**. Now:
  - **Own line** — geo-mode (`geoStage===2`) renders it as a block
    (`<span className="block mt-0.5 text-tg-hint">…</span>`) beneath the city.
  - **Translated** — both the geo-mode and cuisine-mode hints now use `t('plate.tapHint', lang)`
    (imported `t` from `../lib/i18n.js`; ArrivalPlate was otherwise fr/en-inline).
- **`web/cuisine/src/v2/lib/i18n.js`** — new **`plate.tapHint`** across **all 8 locales** (base
  en/fr + id/ru/de/zh/ja/es maps).
- **[VERIFICATION (sandbox)]** `esbuild` parse ArrivalPlate; `node --check` i18n (7 `plate.tapHint`
  = base + 6 maps). Cuisine build + vitest via CI.
- **[STATUS]** PROD — cuisine TMA rebuild on merge.
- **[KNOWN GAPS]** Operator request — fold **dish-name** translation into the long-tail program —
  still owed (separate larger PR).

## Confirmation Gates
| Gate | Status | Note |
|------|--------|------|
| G1 | ✅ | esbuild + node --check; CI builds cuisine |
| G2 | ⏳ | full vitest via CI |
| G3 | ✅ | serial 812→813 |
| G4 | ⏳ | redeploy + operator eyeball |
