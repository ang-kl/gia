(№ 846 - 04-07 '26 11:30 SGT) v0.62.492

# Journal — soleat v0.62.492 (Transport TMA: extend UI locales from 5 → 8, adding zh · ja · es)

> Operator (task 2 — TMA language-set audit):
> *"audit the language set across all TMA, Menu TMA doesn't have the full language listing like Cuisine."*
> Read as: bring every TMA up to Cuisine's 8-language UI set (en/fr/id/ru/de/**zh/ja/es**), one PR per TMA.
> **This is PR 3/4 — Transport.** (Menu = v0.62.490 #1493, Hawker = v0.62.491 #1494, both merged.)

## New

### [HDR] #846 | 11:30 SGT 04-07-26 | v0.62.492 — Transport TMA gains zh · ja · es | 2 web files + 3 doc/meta | PR #(pending) |

- **[INTENT].** Close the Transport-vs-Cuisine locale gap. Add **Chinese (zh)**, **Japanese (ja)**,
  **Spanish (es)** so the MRT/LRT map hub — map layers, header status, view tips, ticker, station
  cards, crowd/exit/carpark labels, FABs, error strings — renders in all 8 languages.

- **[DELTA] (2 code files).**
  - `web/transport/src/i18n.js` — `SUPPORTED_LOCALES` `['en','fr','id','ru','de']` → **`+['zh','ja','es']`**.
    Appended three flat `ZH_STRINGS` / `JA_STRINGS` / `ES_STRINGS` overlays (**72 keys each** — the exact
    `ID_STRINGS` key set, **parity-validated** by script: no missing, no extra) + **3 merge loops** after
    the DE loop, same overlay pattern as id/ru/de (base `en`/`fr` win, overlay fills the gap).
  - `web/transport/src/components/LocaleToggle.jsx` — **3 dropdown rows**: `🇨🇳 中文` / `🇯🇵 日本語` / `🇪🇸 Español`.

- **Translation provenance.** Agent-drafted per language from the EN base. Singapore-standard terms
  (`地铁` / `德士站`). zh header `地铁地图与状态`, ja header `鉄道路線図と運行状況`. All emoji, `{n}`/`{code}`/
  `{ops}`/`{future}`/`{when}` tokens and glyphs (`·`, `→`, `↗`, `↩`, `⇆`, `⇡`, `⇣`) preserved in place.
  Brand/proper nouns (`MRT`, `LRT`, `Google Map`) verbatim. ja overlay verified all-Japanese (no stray
  Korean); es overlay written apostrophe-free (single-quoted JS).

- **[VERIFICATION (sandbox)]** Key-parity assertion: each overlay covers exactly the 72 `ID_STRINGS`
  keys (no missing, no extra) — the es agent's self-reported "76" was a miscount; the script is
  authoritative. `esbuild` bundle-parse **clean** on both files (only `./tg.js` + `react` module-resolve
  warnings — not syntax). A node merge-harness confirmed **all 8 locales resolve** for `header.title`,
  `footer.tag`, `mrt.status.normal`, `mrt.counts`. Full vitest + `web/transport` build in CI.

- **[STATUS]** PROD — Transport TMA redeploys on merge (Railway). Verify on Telegram: the language
  dropdown lists 8; picking 中文 / 日本語 / Español re-renders the map hub. **CJK render eyeball** (font +
  pill fit for zh/ja) is the G4 gate.

- **[TEST]** Full vitest via CI.

- **[KNOWN GAPS]** (1) **Clipboard** TMA still at 5 locales (task-2 PR 4/4, still due — Clipboard has
  no LocaleToggle; follows shared `gia.locale` pref via `SUPPORTED` Set + `SUPPORTED_LOCALES` array).
  (2) Transport **data-driven** strings (live station/line names from LTA) pass through untranslated by
  design. (3) CJK live-render eyeball outstanding. (4) Search auto-fire investigation (A) remains
  separate. G4 gates for v0.62.483–492 remain open.

## Confirmation Gates
| Gate | Status | Note |
|------|--------|------|
| G1 | ✅ | key-parity 72/72 per lang (script-validated); esbuild parse clean (i18n + LocaleToggle); node merge harness — 8/8 locales resolve |
| G2 | ⏳ | full vitest via CI |
| G3 | ✅ | serial 845→846, commit 963→964, build 602→603; anchor re-set 04-07 '26 11:30 SGT |
| G4 | ⏳ | redeploy + operator eyeball (8-language dropdown; zh/ja/es render + CJK pill fit) |
