(№ 804 - 01-07 '26 09:30 SGT) v0.62.450

# Journal — soleat v0.62.450 (Sketchbook — intelligent Pick-Local-Dish filter)

> Operator: "I need something better for 'Pick Local Dish' — it ought to be intelligent to know
> these places have significant dishes, or from the 'Try' in the eatery card. 'Cuisine & Filter'
> should give the cuisines saved in the cards + filters representing the card."

## New

### [HDR] #804 | 09:30 SGT 01-07-26 | v0.62.450 — data-driven dish filter | 4 files | PR #(pending) |
- **`App.jsx`** — new **`dishOptions`** derived from each saved card's venue: `signatureDish`,
  `cityDish`, `dishes[]` (the exact source of the card's **🍲 Try** line), deduped and **ranked
  by frequency**. Only cards that actually carry a notable dish contribute — so the picker knows
  which places have significant dishes. `cardMatches` dish branch now matches the venue dish
  fields first (text fallback).
- **`Shell.jsx`** — the **Pick Local Dish** dropdown now renders those dish chips (2-col grid,
  `🍲 {dish} · count`, an **All** reset) instead of a blind free-text keyword box; empty-state
  `filter.noDishes`. Header chip shows `🍲 {dish}` when active.
- **Cuisine & Filter** — already card-derived (`cuisineOptions` from `c.cuisines`) + the
  rating/price/crowd/open/Michelin facets over the stored venue; unchanged this PR.
- **`i18n.js`** — `filter.noDishes` (en/fr/id/ru/de).
- **[VERIFICATION (sandbox)]** `esbuild` parse (App, Shell) + `node --check` i18n; real `vite
  build` OK (50 modules).
- **[STATUS]** PROD — clipboard TMA rebuild on merge.

## Confirmation Gates
| Gate | Status | Note |
|------|--------|------|
| G1 | ✅ | vite build + parse |
| G2 | ➖ | no unit tests touch this view |
| G3 | ✅ | serial 803→804 |
| G4 | ⏳ | redeploy + operator eyeball |
