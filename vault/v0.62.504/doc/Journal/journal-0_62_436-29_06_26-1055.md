(№ 790 - 29-06 '26 10:55 SGT) v0.62.436

# Journal — soleat v0.62.436 (Sketchbook item 8 — 📍 All locations list)

> Operator item 8: location line like Cuisine; tap 📍 → saved eatery locations grouped by
> country/city, 12-nearby style with +, auto-close after 1 minute.

## New

### [HDR] #790 | 10:55 SGT 29-06-26 | v0.62.436 — location list | 3 files | PR #(pending) |
- **NEW `components/LocationSheet.jsx`** — bottom sheet grouping the user's saved-card eatery
  locations by **Country (region SG/JB) › City (parsed from `venue.area`)**; "12-nearby" style:
  each city is a row with a **count + "+" expand** to the eatery names; tapping a city filters the
  Clipboard to it; **"All locations"** clears. **Auto-closes after 60s** (operator spec).
- **`CatchAllStrip.jsx`** — a **📍 {city | All locations}** line at the top (tap → sheet; ✕
  clears); the catch-all list is filtered by the chosen city. Frontend-only (derives from the
  cards' existing venue data — no backend).
- **`lib/i18n.js`** — `loc.all`, `loc.title`, `loc.none`.
- **[VERIFICATION (sandbox)]** real `vite build` OK (51 modules).
- **[STATUS]** PROD — clipboard TMA rebuild.
- **[REMAINING]** 4 (cuisine filter = Criteria-Search layout + smaller margins), 14 (bot
  /clipboard → last 5 + Open Sketchbook).

## Confirmation Gates
| Gate | Status | Note |
|------|--------|------|
| G1 | ✅ | vite build (51 modules) |
| G2 | ⏳ | CI |
| G3 | ✅ | serial 789→790 |
| G4 | ⏳ | redeploy |
