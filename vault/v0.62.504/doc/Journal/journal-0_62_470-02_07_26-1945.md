(№ 824 - 02-07 '26 19:45 SGT) v0.62.470

# Journal — soleat v0.62.470 (Michelin — new country France: scaffold + Paris/Lyon stars)

> Operator: *"urgent fix: new country: France, Top 12 cities in France. Micheln Stars (2025/26)
> Bib Gournamnd (if any). plan and then let me know"*. Agreed scope: ship the France scaffold +
> fully-sourced Paris & Lyon **star** venues now; Paris 1★ and Bib Gourmand land in a
> browser-sourced follow-up PR.

## New

### [HDR] #825 | 19:45 SGT 02-07-26 | v0.62.470 — France scaffold + Paris/Lyon Michelin stars | 9 files | PR #(pending) |
- **`FR-michelin.js`** (NEW) — 29 **verified** Michelin 2025/26 star venues on the venue-centric
  `michelin-data.js` path (France is a new country, so it uses that loader, not the SG legacy flat
  file). Paris 3★ ×9 (both years) + **L'Ambroisie** (3★ 2025 → 2★ 2026 demotion); Paris 2★ ×16
  (2026); Lyon 2★ ×3 (both years). Every row carries `vegetarian`/`halal` booleans and a
  `venue-award-schema.v0_1` id.
- **`michelin-data.js`** — added `require('./FR-michelin')` to `COUNTRY_TABLES` and a **France
  block to `CITY_IATA`** (Paris→PAR, Lyon→LYS, +10 more, real IATA metro codes).
- **`web/cuisine/src/v2/lib/countries.js`** + **`web/menu/src/countries.js`** — registered
  `{ code: 'FR', flag: '🇫🇷', name: 'France' }` in the OTHER-country picker (both TMAs kept in sync).
- **`web/cuisine/src/v2/lib/cities.js`** + **`web/menu/src/cities.js`** — France block, **top 12
  cities**: Paris, Lyon, Marseille, Nice, Bordeaux, Toulouse, Strasbourg, Nantes, Montpellier,
  Lille, Rennes, Reims.
- **`city-centroids.js`** — 12 FR centroids (`source: manual:city-centre`, 40 km radius cap,
  `fallback: FR`).
- **`index.js`** — `MICH_CC_NAME` gains `FR: 'France'` so the Michelin country label renders.
- **No `COUNTRY_MANIFEST` for FR yet** — the table ships as an un-asserted curated subset so CI
  doesn't load-throw on the incomplete tier counts. **Nothing invented**: unverified 1★/Bib rows
  were omitted, not padded.
- **[VERIFICATION (sandbox)]** `node --check` on all 8 changed `.js`; `michelin-data.js` load-test
  → 1954 venues, 29 FR, cities [Paris, Lyon], `hasMichelinData('FR') === true`, no throw.
- **[STATUS]** PROD — server + both TMAs rebuild on merge.
- **[KNOWN GAPS]** Paris 1★ + Bib Gourmand across all 12 cities still due (guide.michelin.com 403s
  the fetch tool) — follow-up PR will scrape them via the real Chrome browser. Paris 2★ 2025 not
  re-verified (only 2026 asserted). Remaining 10 cities have registry entries but no venues yet.

## Confirmation Gates
| Gate | Status | Note |
|------|--------|------|
| G1 | ✅ | node --check all 8; michelin-data load-test passes |
| G2 | ⏳ | full vitest via CI |
| G3 | ✅ | serial 823→824 |
| G4 | ⏳ | redeploy + operator eyeball (France in picker, Paris/Lyon stars render) |
