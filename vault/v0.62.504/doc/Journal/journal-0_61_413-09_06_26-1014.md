(№ 613 - 09-06 '26 10:14 SGT) v0.61.413

# Journal — soleat v0.61.413 (Add Vietnam to the durian belt → seven countries)

> Operator: *"add vietnam to the list of six belt countries"*
>
> Commit 626 / journal 613 / **build 396**. special-mode.js + web/cuisine. Revert-ready.

## New

### [HDR] #613 | 10:14 SGT 09-06-26 | v0.61.413 — durian belt: +Vietnam (VN), now seven | special-mode.js + cuisine-selection.js + i18n + tests + comments | 1 PR | 1 build |

- **[INTENT]** Add **Vietnam (VN)** to the SE-Asian durian belt. The belt becomes **seven**: 🇸🇬 SG · 🇲🇾 MY · 🇮🇩 ID · 🇹🇭 TH · 🇵🇭 PH · 🇧🇳 BN · 🇻🇳 **VN**.

- **[DELTA]** Both gates updated **together** — otherwise the chip would enable in VN but the server search would still return 0 (inconsistent).
  - **Server — `special-mode.js`** — `SPECIAL_MODE_COUNTRIES` `+= 'VN'`. Now durian / durian-pastry **searches** run in Vietnam.
  - **Client — `web/cuisine/src/v2/lib/cuisine-selection.js`** — `DURIAN_BELT_COUNTRIES` `+= 'VN'` (kept in sync with the server; tests assert both). Now the **chips** are enabled in Vietnam.
  - **User-facing strings — `web/cuisine/src/v2/lib/i18n.js`** — the three `special.*.blocked` messages (en + fr) now end *"…the Philippines, Brunei **and Vietnam**"* / *"…aux Philippines, au Brunei **et au Vietnam**"*; the `special.beltOnly` tooltip → *"…PH, BN, **VN**"*.
  - **Tests** — `__tests__/special-mode.test.js` (belt loop +VN, outside loop −VN, size 6 → **7**, sorted array +VN) and `__tests__/cuisine-selection.test.js` (belt loop +VN, size 6 → **7**, inside +VN, outside −VN).
  - **Comments** — every `SG/MY/ID/TH/PH/BN` enumeration (index.js, CuisineDrawer, CuisineCategoryDrawer, App.jsx, ResultPanel, i18n) → `…/VN` so the docs don't drift.
  - **Fruits** stays global (unchanged — only durian + durian-pastry are belt-gated).
  - **`package.json`** `0.61.412 → 0.61.413`; **`doc/.serial-state.yml`** commit 625 → 626, journal 612 → 613, **build 395 → 396**; re-anchored 10:14 SGT.

- **[VERIFICATION (sandbox)]** `node --check` on special-mode.js / index.js / cuisine libs clean. Cuisine **vite build green**. Full suite **3115/3115 green** (belt tests now assert the seven-country set).

- **[STATUS]** PR-**OPEN**, **auto-merge armed (squash)**. Railway redeploys (server + TMA) on merge.

- **[TEST]** Operator: region **Others → Vietnam** → **Durian Fruits** + **Durian Pastry** are now **selectable** and a durian search returns results (was greyed / blocked). All other non-belt countries (JP, KR, US…) stay disabled.

- **[KNOWN GAPS]**
  - The client `DURIAN_BELT_COUNTRIES` must stay **in sync** with the server `SPECIAL_MODE_COUNTRIES` (noted in code; both now `{SG,MY,ID,TH,PH,BN,VN}`).
  - Eyes-on-deployed pending (operator to confirm on the live TMA).

- **Confirmation Gates**

  | Gate | Resolution | Date |
  |---|---|---|
  | G1 — version bump | PATCH `0.61.412 → 0.61.413`. | 09-06 '26 |
  | G2 — destructive action | One country added to both belt sets + strings/tests; reversible. | 09-06 '26 |
  | G3 — new decision rule | Durian belt = **seven** countries incl. Vietnam. | 09-06 '26 |
  | G4 — paid external API call | None. | 09-06 '26 |

## Amendments

- **`special-mode.js`** — `SPECIAL_MODE_COUNTRIES` += `VN`.
- **`web/cuisine/src/v2/lib/cuisine-selection.js`** — `DURIAN_BELT_COUNTRIES` += `VN`.
- **`web/cuisine/src/v2/lib/i18n.js`** — blocked messages (en+fr) + `special.beltOnly` now name Vietnam.
- **`__tests__/special-mode.test.js`**, **`__tests__/cuisine-selection.test.js`** — belt assertions updated to seven.
- Comment enumerations across `index.js`, `CuisineDrawer.jsx`, `CuisineCategoryDrawer.jsx`, `App.jsx`, `ResultPanel.jsx` → `/VN`.
- **`package.json`** — `0.61.412 → 0.61.413`.
- **`doc/.serial-state.yml`** — counters (commit 626 / journal 613 / build 396) + anchor 10:14 SGT 09-06-26 + `last_anchor_source` prepend.

## Deleted

- The six-country belt (superseded by the seven-country belt incl. Vietnam).
