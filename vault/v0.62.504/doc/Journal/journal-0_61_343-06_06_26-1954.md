(№ 543 - 06-06 '26 19:54 SGT) v0.61.343

# Journal — soleat v0.61.343 (Macau Michelin — NEW country table `MO-michelin.js`; 34 venues; loader registration + revert HK's no-Macau assertions)

> Operator: *"macau"* → `instruction/Macau.js` (just uploaded, alongside `China.js`). Macau is a **new ISO-2 country (`MO`)** — the first brand-new country **table** (the others filled existing stubs). 34 venues, single city "Macau". JP/KR shape: **2025 partial** (8, stars only) + **2026 full** (34). This also **reverts the v0.61.342 HK PR's "no Macau" assertions** (Macau is now loaded as its own table).
>
> **Left OPEN for review — auto-merge NOT armed.** Commit 556 / journal 543 / build 342, landed via the GitHub Git Data API on origin/main @ v0.61.342.

## New

### [HDR] #543 | 19:54 SGT 06-06-26 | v0.61.343 — Macau Michelin live: NEW `MO-michelin.js` (34 venues); +COUNTRY_TABLES entry; +CITY_IATA `macau`; MO manifest (8/34) | 3 code files | 1 PR | 1 build |

- **[INTENT]** The v0.61.342 Hong Kong load flagged that Macau was missing from the HK dataset (the "Hong Kong / Macau" guide). The operator supplied `instruction/Macau.js`. Macau is administratively distinct (ISO-2 `MO`), so it gets its **own** country table rather than folding into HK — keeping the schema's one-country-per-table invariant.

- **[MECHANISM]** Unlike MY/TH/JP/KR/TW/VN/HK (which filled pre-existing empty stubs), **no `MO-michelin.js` stub existed** — this PR **creates** the file and **registers** it. The loader change is three-fold: (1) a new `MO-michelin.js` exporting `{COUNTRY:'MO', ENTRIES:[…34…]}` (verbatim from `instruction/Macau.js`); (2) `require('./MO-michelin')` appended to `COUNTRY_TABLES` so the load loop ingests it + the global dedup covers it; (3) `'macau':'MFM'` added to `CITY_IATA` (the loader gate — `MFM` already exists in `iata-cities.js` as Macau). MO manifest registered.

- **[DELTA]**

  **1. `MO-michelin.js`** (root, **NEW**) — `const COUNTRY='MO'; const ENTRIES=[ …34 venues verbatim… ]; module.exports={COUNTRY,ENTRIES}`. Header documents the source, the single "Macau" city, the new-ISO-2 split from HK, and the 2025-partial manifest. 34 unique ids, 42 awards.

  **2. `michelin-data.js`** —
  - `COUNTRY_TABLES`: `require('./MO-michelin')` added after `require('./HK-michelin')`.
  - `CITY_IATA`: `'macau':'MFM'` added (under a new "Macau" comment, after the HK territory key).
  - `COUNTRY_MANIFEST` + `MO: { 2025:{three:2,two:6,total:8}, 2026:{three:2,two:6,one:13,bib:13,total:34} }`. 2025 omits one-star + Bib (none in source).

  **3. `__tests__/michelin-data.test.js`** — **reverted the HK PR's no-Macau assertions** (Macau is now loaded):
  - HK suite's `it('has NO Macau venues …')` (which asserted `venuesForCountry('MO').length===0`) → reworded to `it('is Hong Kong only — Macau is a separate MO table, not in HK')` (HK venues are all country HK + city Hong Kong; no HK venue has city Macau).
  - `it('is false for Macau (NOT in the Hong Kong dataset)')` → replaced with `it('is true where MO venues exist …')`.
  - The empty-guide false-gate's `Macau`/`MO` lines removed (Shanghai/CN retained).
  - Added a full MO load suite (34 venues / 42 awards, 8@2025 / 34@2026, per-tier manifest `{three:2,two:6}` + `{three:2,two:6,one:13,bib:13}`, unique ids, every `city`==='Macau' resolving to MFM, Jade Dragon 3★ both editions) and an `MO-michelin.js is venue-centric with 34 rows` require test.

  **4. `package.json`** — `0.61.342 → 0.61.343`.

  **`doc/.serial-state.yml`** — commit 555 → 556, journal 542 → 543, build 341 → 342; re-anchored 19:54 SGT 06-06-26.

- **[VERIFICATION (sandbox)]**
  - `node --check` clean: `MO-michelin.js`, `michelin-data.js`, `__tests__/michelin-data.test.js`.
  - Real-loader temp-harness (all 9 `{CC}-michelin.js` incl. the new MO + patched `michelin-data.js` in `/tmp/motest`): loads **without throwing**. `venuesForCountry('MO')===34`; global unique ids **1416/1416** (MY 70 + TH 180 + JP 589 + KR 117 + TW 196 + VN 83 + HK 147 + MO 34, no dup); MO 2025=8 / 2026=34; tiers `{three:2,two:6}` (2025) + `{three:2,two:6,one:13,bib:13}` (2026); `sum(MO awards)===42`. MO manifest passes. `hasMichelinData` true for MO/Macau; HK remains HK-only (no Macau under HK); Shanghai/CN still false.
  - **13/13** explicit assertions green.
  - Full `npm test` runs in CI on merge.

- **[STATUS]** PR-**OPEN**, **auto-merge NOT armed**. Landed via the GitHub Git Data API on a branch off origin/main @ v0.61.342. Railway redeploys on merge.

- **[TEST]** Operator: Macau is now in the venue loader as its own country (`MO`) — 34 venues, the full 2026 edition (34) + the partial 2025 stars (8). Hong Kong (`HK`) and Macau (`MO`) are now both loaded, split as Michelin lists them. With Macau done, only **China** remains an empty stub.

- **[KNOWN GAPS]**
  - **2025 is PARTIAL** — 8 venues, stars only (no one-star, no Bib). A fuller 2025 Macau list is **still due** when supplied.
  - **No native-name / postal fields** — none stored (source has none).
  - **MO not in the OTHER-mode picker** — `cities.js` has no `MO` country block; Macau is loader-side (`CITY_IATA`) only. Add a picker entry later if OTHER-mode navigation to Macau is wanted.
  - **Unified loader still off the live search path** — MY/TH/JP/KR/TW/VN/HK/MO via `michelin-data.js` not yet wired into the bot's Michelin search (SG standalone).
  - A `doc/Feature/` / `doc/Technical/` / `doc/Register/` update + a `vault/v0.61.343/` snapshot are still due.

- **Confirmation Gates**

  | Gate | Resolution | Date |
  |---|---|---|
  | G1 — version bump | PATCH `0.61.342 → 0.61.343`. | 06-06 '26 |
  | G2 — destructive action | None. New `MO-michelin.js` created; append-only COUNTRY_TABLES + CITY_IATA + manifest edits; test assertions corrected (Macau now loaded). Curated `instruction/Macau.js` untouched. Loader clean (1416/1416 unique). | 06-06 '26 |
  | G3 — new decision rule | Macau loads as its own ISO-2 country table `MO-michelin.js` (NOT folded into HK); registered in COUNTRY_TABLES + CITY_IATA (`macau`→MFM); MO manifest 2025 8 (partial) / 2026 34. | 06-06 '26 |
  | G4 — paid external API call | _n/a_. Data load + loader registration only. | 06-06 '26 |

## Amendments

- **`MO-michelin.js`** (NEW) — 34 curated venues from `instruction/Macau.js` (verbatim); header v0.61.343.
- **`michelin-data.js`** — `COUNTRY_TABLES` +`require('./MO-michelin')`; `CITY_IATA` +`'macau':'MFM'`; `COUNTRY_MANIFEST` +`MO` (2025 8 / 2026 34).
- **`__tests__/michelin-data.test.js`** — reverted HK PR's no-Macau assertions (HK "no Macau venues"→"HK-only/MO separate"; "Macau false"→"MO true"; false-gate Macau/MO lines removed); +MO load suite + `MO-michelin.js` 34-row require test.
- **`package.json`** — `0.61.342 → 0.61.343`.
- **`doc/.serial-state.yml`** — counters (commit 556 / journal 543 / build 342) + anchor re-set 19:54 SGT 06-06-26.

## Deleted

- _None._
