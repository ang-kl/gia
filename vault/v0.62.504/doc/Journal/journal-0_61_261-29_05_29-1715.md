(№ 462 - 29-05 '26 17:15 SGT) v0.61.261

# Journal — soleat v0.61.261 (Durian variance baseline — 70.6% cross-region precision)

> Operator (29-05 '26): pasted `/Users/akla/Downloads/durian-variance-2026-05-29.json` — the JSON file from the v0.61.260 `/ver` → `🥥 Durian variance` → `Durian only` run.

> Commit 474 / journal 462 / build 264, on branch
> `claude/v0.61.261-durian-variance-data` off `main` at v0.61.260.

## New

### [HDR] #462 | 17:15 SGT 29-05-29 | v0.61.261 — Durian variance baseline | 5 files | 1 PR | 1 build |

- **[INTENT]** Land the operator's `/ver` variance JSON in the repo
  so the precision number is committed alongside the v0.61.257
  code that produced it, and the v0.61.259 summary generator has
  a real `durian-latest.json` to read.

- **Headline result**: **cross-region precision 70.6%** (525 kept /
  744 places) — a massive jump from the operator-reported <20%
  baseline. v0.61.257's strong-haystack-only + strict-name-types
  tightening validated.

  | Region | n | Kept | Precision |
  |---|---:|---:|---:|
  | Singapore | 191 | 141 | **73.8%** |
  | Johor Bahru | 184 | 119 | **64.7%** ← weakest |
  | Putrajaya | 175 | 129 | **73.7%** |
  | Kuala Lumpur | 194 | 136 | **70.1%** |
  | **ALL** | **744** | **525** | **70.6%** |

  Top primaryTypes returned (merged across regions):
  `wholesaler` 297 · `food_store` 160 · `food` 81 ·
  `grocery_store` 78 · `dessert_shop` 31 · `restaurant` 28 ·
  `farm` 12 · `cafe` 10 · `market` 10 · `store` 7.

- **[DELTA]**

  **1. NEW `data/durian-variance/durian-latest.json`** — exact
     copy of the operator's `/ver`-produced JSON. Canonical name
     the v0.61.259 `scripts/durian-variance-summary.js` reads.

  **2. NEW `data/durian-variance/durian-2026-05-29.json`** —
     dated archive copy. Future runs land alongside under their
     own date stamp.

  **3. NEW `doc/durian-variance-summary.md`** — regenerated via
     `node scripts/durian-variance-summary.js`. Three sections:
     - Cross-region rollup (markdown table above)
     - `durian` primaryType frequency top-30
     - `durian` per-query precision (every lang × seed × region
       row — long, copy-pasteable for follow-up investigation)
     - `durian-pastry` sections render the
       `_(no JSON file present for durian-pastry — workflow likely
       failed mid-run)_` note since the operator hasn't sent
       that JSON yet.

  **4. `package.json`** — `0.61.260 → 0.61.261`.

  **5. `doc/.serial-state.yml`** — commit 473→474, build 263→264,
     journal 461→462; re-anchored 29-05 '26 17:15 SGT.

- **[VERIFICATION (sandbox)]**

  - `node scripts/durian-variance-summary.js` ran clean and wrote
    a 24-line file (durian section only; pastry placeholder).
  - JSON validated by Python `json.load` round-trip during
    summary computation.

- **[STATUS]** PR-OPEN on push.

- **[TEST]** Operator (post-merge):
  1. Open `doc/durian-variance-summary.md` on GitHub — table
     renders cleanly.
  2. Compare the per-query precision rows (mode `durian`,
     region × lang × seed). The lowest rows highlight Places
     queries that returned mostly non-durian venues — those
     can drive the next round of accept-list / keyword
     tightening.
  3. Run the v0.61.260 `/ver` → `🍰 Durian Pastry` flow to fill
     in the second mode; paste its JSON path next and I'll land
     it as v0.61.262.

- **[KNOWN GAPS]**
  - Johor Bahru is the weakest at 64.7%. Drilling into the
    per-query table will likely show whether it's a single
    failing seed or a uniform issue across languages.
  - The dated archive path lets us keep historical snapshots,
    but the summary generator currently reads only
    `durian-latest.json`. If we want diffs (e.g. "v0.61.261 vs
    v0.61.265 after the next tightening"), the generator
    should be extended to read both files and emit a delta
    column. Deferred.

- **Confirmation Gates**

  | Gate | Resolution | Date |
  |---|---|---|
  | G1 — version bump | PATCH `0.61.260 → 0.61.261`. Data-only PR + regenerated summary. | 29-05 '26 |
  | G2 — destructive action | _n/a_. | 29-05 '26 |
  | G3 — new decision rule | _n/a_ — this PR captures the v0.61.260 G3 result (canonical /ver-driven baseline storage). | 29-05 '26 |
  | G4 — paid external API call | _n/a in this PR_ (the paid call happened during the v0.61.260 `/ver` run; this PR just archives the resulting JSON). | 29-05 '26 |

## Amendments

- **NEW** `data/durian-variance/durian-latest.json` +
  `data/durian-variance/durian-2026-05-29.json` +
  `doc/durian-variance-summary.md`.

## Deleted

_None._
