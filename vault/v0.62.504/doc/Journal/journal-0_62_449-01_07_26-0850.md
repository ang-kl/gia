(№ 803 - 01-07 '26 08:50 SGT) v0.62.449

# Journal — soleat v0.62.449 (Menu — fix Sketchbook tile navy-block icon)

> Operator: "the sketchbook icon looks awful, why" (screenshot: solid navy square).

## New

### [HDR] #803 | 08:50 SGT 01-07-26 | v0.62.449 — transparent Sketchbook icon | 3 files | PR #(pending) |
- **`web/menu/public/sketchboard_clip_icon.png`** — was **RGB with an opaque near-white
  background** (corners `(253,254,254)`, opaque-fraction 1.0). The Menu tile applies the
  **`icon-navy`** CSS filter, which tints every opaque pixel navy → the whole square rendered as
  a **solid navy block**. (Cuisine/Hawker tiles look right because their icons are transparent
  line-art, so only the strokes tint.)
- **Fix** — regenerated the PNG as **transparent line-art** (near-white → alpha 0, ink →
  near-black `#14141e`) with Pillow, trimmed to 782×1166. `icon-navy` now tints only the
  clipboard drawing, matching the other tiles.
- **[VERIFICATION (sandbox)]** rendered a white-bg + navy-bg preview and eyeballed it (transparent,
  strokes only). Menu TMA build gated by CI.
- **[STATUS]** PROD — Menu TMA rebuild on merge.
- **[KNOWN GAPS]** Unshipped dropdown-✕ pass (LocaleToggle ×3 + shared MapControls) still staged,
  not in this PR.

## Confirmation Gates
| Gate | Status | Note |
|------|--------|------|
| G1 | ✅ | asset preview eyeballed; CI builds menu |
| G2 | ➖ | no unit tests touch this asset |
| G3 | ✅ | serial 802→803 |
| G4 | ⏳ | redeploy + operator eyeball |
