(№ 759 - 25-06 '26 16:10 SGT) v0.62.332

# Journal — soleat v0.62.332 (revert: remove the Clipboard entry-point from the Cuisine TMA panels)

> Operator (screenshot IMG_2576, 25-06 '26): the "Sketchbook / Clipboard → 📋 Open Clipboard"
> panel that Clipboard PR #3 (#1303, v0.62.330) inserted into the Cuisine TMA — between the
> header and the Cuisine / "Pick local classic" panels — is unwanted. *"revert back to original
> how the panels (Cuisine, Local food pick) works as we worked so hard to get these panels
> working."* Reversed.

## New

### [HDR] #759 | 16:10 SGT 25-06-26 | v0.62.332 — remove Clipboard entry-point from Cuisine TMA (restore panels) | 1 file | 1 PR |

- **[INTENT]** Operator reversed the in-Cuisine-TMA Clipboard launcher. The Cuisine + "Pick
  local classic" panels must read exactly as before the Clipboard cycle.
- **[DELTA]** `web/cuisine/src/v2/App.jsx` — removed the 38-line **additive** block
  (`v0.62.330 Clipboard TMA entry point`: the bordered "Sketchbook / Clipboard" panel +
  "📋 Open Clipboard" button) that sat between `</header>` and the criteria/plate panels.
  #1303 never modified the panels themselves — this is a **pure removal**; the
  `</header>` → panel adjacency is restored byte-identical to pre-v0.62.330.
- **[VERIFICATION (sandbox)]** esbuild parse OK; 0 clipboard refs remain; 8 panel refs intact.
- **[STATUS]** PROD — squash to main (v0.62.332). Host redeploys per merge.
- **[AU-7 amendment — reverses a prior "locked" decision]** Clipboard PR #3 (#1303) + journals
  #328/#330 recorded a *"Locked UI decision (doc/v0.70_.MD § Locked UI decisions for the
  Clipboard cycle): a Sketchbook/Clipboard panel directly below the Location field in the
  Cuisine TMA."* The operator has now **REVERSED** that placement (IMG_2576). Prior decision
  preserved here verbatim; new decision: **no Clipboard launcher inside the Cuisine TMA.** The
  separate Clipboard Mini App (`web/clipboard/`) + backend (`clipboard-routes.js`, storage/API)
  are **untouched** — only the Cuisine entry-point is removed. The Clipboard app should be
  reached by some other surface (operator to decide), not the Cuisine panels.
- **[KNOWN GAPS]**
  - `doc/v0.70_.MD` (which carries the "locked" wording) lives on the **other agent's open PR
    #1289** (`claude/tender-rubin-mxh87w`), **not on main** — it cannot be amended from main
    without touching that PR. The other agent must update its v0.70 "Locked UI decisions"
    before #1289 merges, or the launcher may be re-inserted. **Operator to coordinate.**
  - The Clipboard cycle was 4 PRs (#1301–#1303 + a polish PR, v0.62.328–331) by a parallel agent.
- **Confirmation Gates**

  | Gate | Resolution | Date |
  |---|---|---|
  | G1 — version bump | PATCH `0.62.331` → `0.62.332`. | 25-06 '26 |
  | G2 — destructive action | Removal of an additive UI block only; the Clipboard app + backend preserved; rationale (operator reversal) recorded above (AU-7). | 25-06 '26 |
  | G3 — new decision rule | **No Clipboard launcher inside the Cuisine TMA** (reverses the v0.70 "locked" in-Cuisine placement). | 25-06 '26 |
  | G4 — paid external API call | _n/a_. | 25-06 '26 |

## Amendments

- `doc/.serial-state.yml` — journal 758→759, commit 864→865, build 519→520; re-anchored
  16:10 SGT 25-06-26.

## Deleted

_None_ — the Clipboard entry-point removal has its rationale preserved above (AU-7); no data removed.
