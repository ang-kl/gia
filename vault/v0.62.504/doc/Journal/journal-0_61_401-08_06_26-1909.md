(№ 601 - 08-06 '26 19:09 SGT) v0.61.401

# Journal — soleat v0.61.401 (Cuisine returns 0 on a country/pin mismatch: floor the OTHER country-text-filter)

> Operator: *"did you gate the cuisine, now so many returns zero except no filter"* + *"are the codes over-compress … unblocked only in six countries?"* + *"walk through the logic …"* (evidence: `log/logs.1780911862357.json`); then **"continue"** = ship fix 1.
>
> Commit 614 / journal 601 / **build held 386** (server-only). Revert-ready.

## New

### [HDR] #601 | 19:09 SGT 08-06-26 | v0.61.401 — OTHER country-text-filter floors instead of zeroing | index.js | 1 PR | 0 build |

- **[INTENT]** With a cuisine selected, *"so many return zero except no filter."* Operator suspected a cuisine gate / over-compression.

- **[ROOT (traced from the operator's log — NOT a gate)]** The anchor was pinned at **VivoCity, Singapore** (`1.2647,103.8232`) but **country-pref = MY** → region **OTHER** → `D703d OTHER country-text-filter cc=MY`. `country-text-match.filterVenuesByCountry` keeps only venues whose `area + name` text carries **Malaysia** keywords; every **SG** venue fails:
  - `durian` 9 → **0**, `american` 9 → **0** (small pools wiped);
  - no-cuisine 54 → **4** (big pool leaks a few) — hence *"no filter works."*
  - The durian/fruits here arrived as **cuisine chips** (`cuisines:["durian"]`, normal path), **not** the `specialMode` pill, so the **v0.61.397 6-country belt** (applies only to `req.body.specialMode`, `index.js:13732`) was **not** involved.

- **[DELTA]** (`index.js`) — **floor** the OTHER country-text-filter. The Places `locationBias.circle` already pins the pool to the user's **real coords**, and this keyword filter is **belt-and-braces** (fail-open by design), so when it would **empty** the page (`filtered.length === 0 && beforeOther > 0`) it now **keeps the coord-pinned pool** instead of returning 0 (logs `FLOOR: kept coord-pinned pool`). Mirrors the JB-fallback (`D703b`) and the **v0.61.399** review-refute floor.
  - **`package.json`** `0.61.400 → 0.61.401`; **`doc/.serial-state.yml`** commit 613 → 614, journal 600 → 601, **build held 386**; re-anchored 19:09 SGT.

- **[VERIFICATION (sandbox)]** `node --check index.js` clean. Full suite **114 files / 3104 tests** — **no regression**.

- **[STATUS]** PR-**OPEN**, **auto-merge armed (squash)**. Railway redeploys on merge (server only — no rebuild).

- **[TEST]** Operator: pin in **SG** with country still set to **MY**, pick **durian** (or any cuisine) → a list now loads from the SG coords instead of 0. Legit **MY** searches (pin in KL) are unchanged.

- **[KNOWN GAPS / SAFETY]**
  - Behaviour change is **only** the all-or-nothing zero case. **Legit OTHER** (pin in KL, country MY → filter keeps the MY venues, non-zero → floor never fires) and **cross-border-bleed** removal (mixed pool → keeps the MY ones) are **unchanged**.
  - The cleaner long-term fix is to derive the filter country from the **pin's reverse-geocoded country** rather than the stored pref — deferred (extra geocode + more surface). Flagged for the operator.
  - The **Fruits/Durian pill** belt-gate (6 countries) is untouched — separate, operator's call.
  - **Not** unit-tested (inline express-route logic, no harness seam). **Revert-ready.**

- **Confirmation Gates**

  | Gate | Resolution | Date |
  |---|---|---|
  | G1 — version bump | PATCH `0.61.400 → 0.61.401`. | 08-06 '26 |
  | G2 — destructive action | Additive floor; the filter still applies whenever it leaves ≥1 venue. | 08-06 '26 |
  | G3 — new decision rule | The OTHER country-text-filter **demotes, never empties** — falls back to the coord-pinned pool at 0. | 08-06 '26 |
  | G4 — paid external API call | None new. | 08-06 '26 |

## Amendments

- **`index.js`** — OTHER country-text-filter floors to the pre-filter coord-pinned pool when it would zero the list.
- **`package.json`** — `0.61.400 → 0.61.401`.
- **`doc/.serial-state.yml`** — counters (commit 614 / journal 601 / build held 386) + anchor 19:09 SGT 08-06-26.

## Deleted

- _None_ (additive floor).
