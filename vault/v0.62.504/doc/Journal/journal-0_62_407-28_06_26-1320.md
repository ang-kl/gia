(№ 761 - 28-06 '26 13:20 SGT) v0.62.407

# Journal — soleat v0.62.407 (dish-explanation modal: smaller search pill, 🔍-only chip, [ picture ] → Commons photo source)

> Operator session 28-06 '26 (screenshot IMG_1101, Tsukemen card): "for dish description, make
> the search pill small font size -1, background on the search icon only and not the word but
> ensure whole icon+word is tappable. add the '[ picture ]' and link to the earlier search for the
> authentic / wiki picture. when click shows on the browser to the exact picture place source and
> not the whole page. read and ask if you dont know." Clarified the picture target via a numbered
> ask → operator chose **Commons file page (source)**: the photo plus its author + licence.

## New

### [HDR] #761 | 13:20 SGT 28-06-26 | v0.62.407 — dish-card pill restyle + [ picture ] affordance | 3 files | PR #1384 |

- **[INTENT]** Tidy the dish-explanation card's action row and give it a way to see the dish's
  authentic photo, opening the picture's SOURCE page (not the prose article).
- **[DELTA]**
  - `web/cuisine/src/v2/components/ArrivalPlate.jsx` — both dish-card render sites (the headliner
    rows AND the folio-panel list). Pill font `text-[10px]` → `text-[9px]`. The glass background +
    `border-tg-accent/70` now wrap ONLY the 🔍 (an 18px circular `glass-pill` chip); the label
    ("Find eateries" / "Trouver des adresses") sits bare. The whole icon+word remains ONE
    `<button>` (one tap target). Added `[ picture ]` / `[ photo ]` (blue `text-tg-link`,
    bracket shape signals affordance — colour-blind safe) in a right-aligned group next to
    `[ close ]`, calling `openDishPicture(d.dish)`.
  - `web/cuisine/src/v2/lib/dish-picture.js` (NEW) — runtime resolver: `fetch` the en.wikipedia
    REST summary for the (qualifier-stripped) dish, read `originalimage`/`thumbnail` source,
    derive the bare File name (handles thumb URLs where the filename sits one segment before the
    size suffix), open `https://commons.wikimedia.org/wiki/File:<name>`. No lead image → fall back
    to a Commons MediaSearch image query (still picture-scoped, never the article page).
  - `web/cuisine/src/api/tg.js` — new `openExternal(url)`: Telegram `WebApp.openLink` when present
    (a host-API call, so it survives the async fetch / is not popup-blocked), else `window.open`.
- **[VERIFICATION (sandbox)]** `esbuild` parse OK on all three (ArrivalPlate.jsx, dish-picture.js,
  tg.js). Verified `/tmp/ap.jsx` byte-identical to `origin/main` before editing (stale-iCloud-tree
  guard). Shipped via GitHub Git Data API (blobs → tree off main's base_tree → commit → branch).
- **[STATUS]** PROD — squash-merged to main (PR #1384, auto-merge after CI green: syntax + TMA
  build + Vitest). Bot redeploys per PR.
- **[TEST]** No new unit test (UI-only + external-link helper); covered by the existing TMA build
  + esbuild parse. Live eyeball on the Cuisine TMA dish modal still pending (final gate).
- **[KNOWN GAPS]**
  - **Live verification** of the restyled pill + `[ picture ]` on the deployed Telegram TMA not
    yet done (quality-bar final gate).
  - `[ picture ]` resolves against **en.wikipedia** lead image → Commons; dishes whose photo is a
    non-Commons local upload (~few %) will 404 the File: page → user lands on the Commons search
    fallback. Acceptable; flagged honestly.
  - **Journal backlog:** v0.62.388→406 are NOT yet journaled (#760 stops at v0.62.387). That arc =
    Clipboard TDZ diagnostic+fix (#388/#390), hawker-first ranking (#389), Sunshine Coast city
    (#391), Canberra-reset divergence guard (#392/#396), Telegram LocationManager 8.0 (#393),
    Phase-3 zh/ja/es locale + cuisine-name i18n (#394/#398–401), translation-quality audit +
    B1 drink renames + A1 native-script overlay batches (#402–406). Still due — log in Register.

## Confirmation Gates

| Gate | Status | Note |
|------|--------|------|
| G1 build/parse | ✅ | esbuild OK ×3 |
| G2 tests | ✅ | CI Vitest green (auto-merge gate) |
| G3 journal/serial | ✅ | this entry; serial 760→761 |
| G4 live deploy | ⏳ | TMA redeploy in flight; eyeball pending |
