(№ 801 - 30-06 '26 19:55 SGT) v0.62.447

# Journal — soleat v0.62.447 (Cuisine — shrink the group-drill-in close ✕)

> Operator: "the close 'x' is bigger than the one use in the ✕."

## New

### [HDR] #801 | 19:55 SGT 30-06-26 | v0.62.447 — smaller close glyph | 3 files | PR #(pending) |
- **`web/cuisine/src/v2/components/CuisineCategoryDrawer.jsx`** — the close added in v0.62.446
  used `×` (U+00D7) at `text-xl` (20px), which read oversized next to the `text-sm` header title.
  Swapped to **`✕` (U+2715) at `text-sm`** (~14px) to match the ✕ used elsewhere (MapPanel's
  in-card close is `font-size:14px`). Same `text-tg-hint` subtle grey, same right-anchored slot.
- **[VERIFICATION (sandbox)]** `esbuild` JSX parse OK; full cuisine build gated by CI.
- **[STATUS]** PROD — cuisine TMA rebuild on merge.

## Confirmation Gates
| Gate | Status | Note |
|------|--------|------|
| G1 | ✅ | esbuild parse; CI builds cuisine |
| G2 | ➖ | no unit tests touch this view |
| G3 | ✅ | serial 800→801 |
| G4 | ⏳ | redeploy + operator eyeball |
