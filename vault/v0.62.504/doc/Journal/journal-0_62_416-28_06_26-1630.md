(№ 770 - 28-06 '26 16:30 SGT) v0.62.416

# Journal — soleat v0.62.416 (Sketchbook TMA reconcile — P1 backend top-ups)

> Operator: port the Sketchbook prototype (UI/sketchbook/*.dc.html) into web/clipboard per
> UI/sketchbook/HANDOFF.md; "show me a plan before large edits." Plan approved (card=Classic,
> chips→deep-link Cuisine). This is **P1 of 5** (backend), the only phase that touches Redis.

## Finding

`web/clipboard` is **already wired to real APIs** (api.js → clipboard-routes.js → Redis), not
in-memory like the prototype — so HANDOFF Task 2 is mostly done. Build chain (`build:clipboard`),
`/app/clipboard` mount, 16 endpoints, drawer reorder (moveTo), share/fork, TDZ boot fix: all present.
Remaining work is UI parity (P2–P5) + the backend gaps below.

## New

### [HDR] #770 | 16:30 SGT 28-06-26 | v0.62.416 — Sketchbook P1 backend | 5 files | PR #(pending) |
- **[INTENT]** Add the backend the spec needs: delete→clipboard reflow, default cabinet, duplicate.
- **[DELTA]**
  - **`clip-store.js`** — new `returnToClipboard(redis, chatId, cardId)` (dedupe to front, cap 50,
    recompute TTL → 30-day, PERSIST if favourite); exported.
  - **`clipboard-store.js`** — **[AU-7]** cascade-delete rule REVERSED: an orphaned card now
    RETURNS to the catch-all clipboard (was: `softDeleteCard` for non-favourites; prior rule kept
    verbatim in the header comment). `cascadeRemoveDrawerCards` returns the count; `deleteDrawer`
    / `deleteCabinet` surface `{ returned }`. Default cabinet: `cabDefaultKey`,
    `getDefaultCabinetId`, `setDefaultCabinet`; `deleteCabinet` reassigns the default to the first
    remaining cabinet (clears when none). `duplicateCabinet` (name + " copy", drawers + placements)
    and `duplicateDrawer` (copies its cards). All exported.
  - **`clipboard-routes.js`** — `/state` returns `defaultCabinetId`; delete responses echo
    `returned` (+ `reassignedDefault`); new POST `/cabinet/:id/default`, `/cabinet/:id/duplicate`,
    `/cabinet/:id/drawer/:n/duplicate`.
  - **`web/clipboard/src/lib/api.js`** — client wrappers `setDefaultCabinet`, `duplicateCabinet`,
    `duplicateDrawer`.
- **[VERIFICATION (sandbox)]** `node --check` clean. Vitest: `clip-store` 42 + `clipboard-store`
  74 (incl. rewritten cascade return-to-clipboard + new default/duplicate describes) +
  `clipboard-routes` 69 = **185 passing**.
- **[STATUS]** PROD — server + clipboard TMA rebuild; redeploys per PR.
- **[TEST]** 185 clipboard-suite tests green.
- **[KNOWN GAPS]** P2 chrome (sticky header + fixed 4-tab footer + hamburger; chips→Cuisine),
  P3 VenueCard (classic), P4 2-step File sheet + edit pills (incl. Duplicate) + 📍 location & 🧭
  Recents (reuse cuisine `/api/cuisine/recent-locations`), P5 Settings + default ★ + delete→clip
  toasts. Model note: catch-all stays the master list (filing is additive) per the locked
  vocabulary — "return to clipboard" = orphaned cards are no longer hard-deleted.

## Confirmation Gates

| Gate | Status | Note |
|------|--------|------|
| G1 build/parse | ✅ | node --check ×3 |
| G2 tests | ✅ | 185 clipboard-suite tests green |
| G3 journal/serial | ✅ | serial 769→770 |
| G4 live deploy | ⏳ | redeploy; full flow verify after P2–P5 |
