(№ 808 - 01-07 '26 12:10 SGT) v0.62.454

# Journal — soleat v0.62.454 (Cuisine — Dishes pop-up title + label polish)

> Operator (screenshots on v0.62.453): title should be "Dishes · 🇸🇬 Singaporean"; the per-cuisine
> "Dishes" link should be smaller (not bold, not underlined); "why no dishes?"

## New

### [HDR] #808 | 12:10 SGT 01-07-26 | v0.62.454 — Dishes pop-up polish | 3 files | PR #(pending) |
- **`web/cuisine/src/v2/components/CuisineCategoryDrawer.jsx`**
  1. **Pop-up title** now reads **`Dishes · {flag} {Cuisine}`** (was just the cuisine name) — the
     flag is threaded through `openDishes` → `dishModal`.
  2. The per-cuisine **"Dishes"** link is **smaller + not bold + not underlined**
     (`text-[11px] font-semibold` → `text-[10px] font-normal ... no-underline`). Kept the
     theme-aware `text-tg-link` colour (not literal red) per the operator's red-green colour rule.
- **"No dishes" was deploy timing, not a bug** — verified `getNationOverlay('singaporean')` = 164
  dishes and the catalogue slug is `singaporean`; `/api/cuisine/dishes` simply hadn't redeployed
  when the screenshot was taken. Populates after the Railway rebuild.
- **[VERIFICATION (sandbox)]** `esbuild` parse OK; cuisine build + vitest via CI.
- **[STATUS]** PROD — cuisine TMA rebuild on merge.

## Confirmation Gates
| Gate | Status | Note |
|------|--------|------|
| G1 | ✅ | esbuild parse; CI builds cuisine |
| G2 | ⏳ | full vitest via CI |
| G3 | ✅ | serial 807→808 |
| G4 | ⏳ | redeploy + operator eyeball |
