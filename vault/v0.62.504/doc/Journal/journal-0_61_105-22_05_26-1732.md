(№ 312 - 22-05 '26 17:32 SGT) v0.61.105

# Journal — soleat v0.61.105 (carpark zoom-size ladder + overlap demotion)

> Operator — the last of the four-item map spec (after the v0.61.103
> monochrome-lines fix and the v0.61.104 bus-tier / parity / readout
> PR). The carpark overlay marker shrinks by zoom and on overlap.
>
> This entry is commit 312 / journal 305 / build 109, off the
> v0.61.104 branch (#597); the PR is stacked on #597.

## New

### [HDR] #305 | 17:32 SGT 22-05-26 | v0.61.105 — carpark zoom-size ladder | ~4 kb | 5 files | 1 PR | 1 build |

- **[INTENT]** The carpark overlay marker should shrink two sizes (a
  "size" = 2 px) per zoom level below z17, and two sizes more when it
  overlaps another carpark.

- **[DELTA]**
  - **`web/cuisine/src/v2/lib/mapOverlays.js`** (+ the byte-identical
    Hawker / Transport copies) —
    - New `carparkSize(zoom)` → 22 px (z17+) / 18 (z16) / 14 (z15) /
      10 (z14 and below).
    - `dotNode` gains an optional numeric `size`; `buildMarkers` still
      passes the feature object as the 3rd arg, so only a number sizes
      the dot — anything else falls back to 20 px.
    - `applyVisibility`'s marker branch re-renders the carpark layer's
      markers at `carparkSize(zoom)` on every pass; a carpark whose box
      collides with one already placed drops 4 px (two sizes). The
      `zoom_changed` listener now also re-applies the carpark layer.
  - **`public/app.js`** — the same ladder (`carparkSize` / `carparkPin`)
    in `renderClippedLayer`, with a metres→px overlap check (`mppAt`).
  - **`package.json`** — `0.61.104` → `0.61.105` (PATCH).

- **[VERIFICATION (sandbox)]**

  ```
  node --check public/app.js   clean
  npm run build (5 TMAs)       all green
  npm test                     1944/1944 passing, 67 files
  ```

- **[STATUS]** DRAFT — PR #598 on `claude/v0.61.105-carpark-tiers`,
  **stacked on #597** (off the v0.61.104 branch). #597 should merge
  first. On merge deploys via the Railway auto-deploy on `main`.

- **[TEST]** Toggle Carpark and zoom — the 🅿️ markers shrink as the
  map zooms out; clustered carparks render smaller still.

- **[KNOWN GAPS]**
  - The carpark size ladder + the `2 px = 1 size` reading are picked
    to the operator's loose "two size" deltas.
  - No in-sandbox visual verification (no Maps webview).
  - `doc/VibeCodingRecord/` regen still due (carried forward).

- **Confirmation Gates**

  | Gate | Resolution | Date |
  |------|-----------|------|
  | G1 — version bump | PATCH `0.61.104` → `0.61.105` | 22-05 '26 |
  | G2 — destructive action | _code-only_ — additive carpark sizing; `dotNode` gains an optional param; no deletion | 22-05 '26 |
  | G3 — new decision rule | _n/a_ — operator UI spec | 22-05 '26 |
  | G4 — paid external API call | _n/a_ — pure client-side rendering | 22-05 '26 |

## Amendments

_None._

## Deleted

_None._
