(№ 760 - 27-06 '26 16:04 SGT) v0.62.387

# Journal — soleat v0.62.376→387 (Search-Insights strip relocation arc, degraded-AI notice, Clipboard ErrorBoundary, folio/header border fixes, doc catch-up + vault)

> Operator session 27-06 '26: (1) "proceed with 1" — add a throttled "AI busy" notice when the
> chat search classifier fails open; (2) a long visual iteration moving the Search-Insights strip
> out of the header and into the bottom dock as a neo-glass pill between the 💬/🔍 FABs; (3)
> "look into clipboard TMA still crashed" — investigation (read-only); (4) "vault, journal,
> vibe-journal, document first, then 1" — this doc catch-up + a full vault snapshot.

## New

### [HDR] #760 | 16:04 SGT 27-06-26 | v0.62.376 — throttled degraded-AI notice on chat free-text search | 2 files | PR #1353 |

- **[INTENT]** When the Gemini search-intent classifier fails open during an outage, still run the
  search on raw text AND reassure the user (once, throttled) that results are the direct-match
  fallback. Operator chose "option 1".
- **[DELTA]** `gemini-client.js` — both `classifySearchIntent` fail-open returns (no-key ~L1872,
  all-models-down ~L2011) now carry `degraded: true`; the normal/model-fallback return is
  unchanged so the flag never fires on routine fallback. `index.js` — new
  `sendDegradedNotice(redis, chatId, lang)` (Redis `SET NX EX 180` → once / 3 min per chat,
  en/fr, never throws/blocks), wired into the chat free-text handler after `runFreeTextSearch`
  when `cls.degraded`.
- **[VERIFICATION (sandbox)]** `node --check` clean on both; flag scoped to the two fail-open
  paths only.
- **[STATUS]** PROD — squash to main (v0.62.376).

### [HDR] #760b | Search-Insights strip relocation arc | web/cuisine/src/v2/* + styles.css | PRs #1354–#1363 |

Operator iterated the strip placement/skin across 10 PRs (v0.62.377→386). Net end-state:

- **Placement** — strip moved out of the under-header mount into the bottom command dock,
  rendered as a CENTRE element BETWEEN the 💬 and 🔍 FABs (`App.jsx` FAB row).
- **Skin** — neo-glassmorphism pill, 75% opaque (`bg-tg-card/75 backdrop-blur-md`), near-square
  corners (`rounded-sm`), no border outline; horizontally scrollable both ways with no scrollbar
  (`overflow-x-auto no-scrollbar`, inner `w-max`, full venue name scrolls, no truncation); pulled
  flush onto the footer (`self-end -mb-2`); fills the FAB gap (`flex-1`, row `gap-0`).
- **Data fix** — strip now reads `visibleVenues.length ? visibleVenues : venues` (was `venues`),
  so its ★ best-value pick is always one of the result cards shown (#1360).
- **`InsightStrip.jsx`** gained an `inline` variant + a `className` override prop.
- Intermediate states (full-width tinted band above the footer with a +2.25rem card lift, #1355/
  #1358) were SUPERSEDED by the between-FABs pill; the card bottom offset was reverted to
  6/4.5rem (cards unshifted).

### [HDR] #760c | v0.62.382→386 — header line removal + folio folder-tab framing | App.jsx + styles.css | PRs #1359,#1361–#1363 |

- **[INTENT]** Operator (remove-this-line.png / missing-line.png): remove the horizontal line
  under the header; make the Cuisine / "Pick local classic" pickers read as folder tabs.
- **[DELTA + ROOT CAUSE]** The persistent header "line" was the header's drop-**shadow**
  (`shadow-sm`/`shadow-md`), NOT a border — six border edits never touched it. Removed the shadow
  + the `border-b`. The folio framing is now self-contained in CSS: `web/cuisine/src/styles.css`
  `.folio--manila .folio-tab` gets `border-bottom: 1px solid #d1cec9` (the INACTIVE tab's seam),
  `.folio-tab--active` gets `border-bottom: none` (opens into the drawer), and the two
  `.folder-drawer` mounts in `App.jsx` use full `border` (was `border-t-0`).
- **[STATUS]** PROD (v0.62.386).

### [HDR] #760d | Clipboard TMA "still crashed" — investigation (READ-ONLY) | _no code change_ |

- **[INTENT]** Operator: "look into clipboard TMA still crashed."
- **[FINDINGS]** Deployed bundle is current (`v0.62.386`, built 07:45 UTC) and CONTAINS the
  ErrorBoundary ("Clipboard hit a snag"). All render paths are crash-guarded (`state.js` empty
  inits, `App.jsx` root renders header + empty strips, `VenueCard`/`CabinetCard`/`CatchAllStrip`/
  `CabinetGrid` null-check inputs, `tg.js` fully try/caught, `api.js` clean, Vite
  `__BUILD_VERSION__`/`__BUILD_TIME__` defines present). A real render crash would now surface the
  dark snag box, not a blank — so a pure black screen ⇒ React never mounts ⇒ likely a STALE
  CACHED bundle in the Telegram webview, not a code bug.
- **[NEXT]** Operator chose: docs first, then "option 1" = ship a bulletproof diagnostic boot
  (always paints a version banner + exact error) to capture the on-device cause. PENDING.

## Amendments

- `doc/.serial-state.yml` — journal 759→760, commit 865→878 (PRs #1304,#1352–#1363), build
  520→521; re-anchored 16:04 SGT 27-06-26.
- `vault/v0.62.386/` — full repo snapshot (built via the Git Data API referencing existing blob
  SHAs; prior vault was v0.62.302).
- `doc/VibeCodingRecord/` — appended PR rows #1353–#1363 + regenerated the ledger/page.

## Deleted

_None._

## Known Gaps

- **Journal gap v0.62.333→374** (~40 patches, parallel locale / dish-corpus / Search-Insights
  build PRs by other agents) remain UNJOURNALED — out of this session's verified scope; not
  reconstructed here to avoid invention.
- Clipboard black-screen root cause unconfirmed pending the diagnostic-boot PR ("option 1").

- **Confirmation Gates**

  | Gate | Resolution | Date |
  |---|---|---|
  | G1 — version bump | PATCH bumps `0.62.375`→`0.62.387` across PRs #1353–#1363 + this doc PR. | 27-06 '26 |
  | G2 — destructive action | Vault snapshot is additive; intermediate insight-strip layouts superseded (rationale above); no data removed. | 27-06 '26 |
  | G3 — new decision rule | Search-Insights strip = neo-glass pill BETWEEN the FABs (supersedes the under-header banner + the full-width dock band). Header has NO bottom line/shadow; folio framing lives in the tab/panel CSS. | 27-06 '26 |
  | G4 — paid external API call | _n/a_ (degraded notice is Redis-only; classifier already existed). | 27-06 '26 |
