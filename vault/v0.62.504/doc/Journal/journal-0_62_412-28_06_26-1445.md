(№ 766 - 28-06 '26 14:45 SGT) v0.62.412

# Journal — soleat v0.62.412 (dish explanation → pop-up modal + auto-close)

> Operator: "when tap on the dish, pop-up window with the same dimension and style of current
> explanation"; "when user didn't close the pop-up and clicks another dish or other place, it
> will close." (Same thread also confirmed: keep the 5 meal buckets as-is; community
> sub-grouping = next PR.)

## New

### [HDR] #766 | 14:45 SGT 28-06-26 | v0.62.412 — dish explanation pop-up modal | 2 files | PR #(pending) |
- **[INTENT]** Float the dish-explanation card as a pop-up over a scrim instead of pushing the
  list inline; dismiss on tap-outside (or when another dish is tapped).
- **[DELTA]** `web/cuisine/src/v2/components/ArrivalPlate.jsx` — new `DishModal` wrapper
  (`fixed inset-0 z-40 bg-black/50`, centered `max-w-[420px] max-h-[80vh]` scroll container;
  scrim `onClick` → `onClose`; inner `stopPropagation` so taps inside don't dismiss). Wrapped all
  **four** dish-card render sites (headliner card, folio-panel card, geo main 📜 card,
  More-classics `clCard`). The card markup is unchanged → identical dimensions/style. Auto-close
  is free: the scrim closes on outside tap, and tapping another dish sets `factIdx`, swapping the
  open card. `package.json` 0.62.411 → 0.62.412.
- **[VERIFICATION (sandbox)]** `esbuild` parse OK; 4 `<DishModal>` open / 4 close balanced.
  Verified against `origin/main` before editing.
- **[STATUS]** PROD — cuisine TMA rebuild in CI; bot redeploys per PR.
- **[TEST]** No new unit test (UI). Live eyeball pending.
- **[KNOWN GAPS]** Community sub-grouping within each meal section (Straits Chinese / Malay /
  Chinese / Indian / Indonesian for SG/MY/TH/PH/ID) is the NEXT PR — will derive community from
  the existing origin / `sharedWith` tags (never invent). Non-CJK native script + Wikipedia image
  pipeline still pending.

## Confirmation Gates

| Gate | Status | Note |
|------|--------|------|
| G1 build/parse | ✅ | esbuild OK; modal tags balanced |
| G2 tests | ⏳ | CI Vitest + TMA build |
| G3 journal/serial | ✅ | this entry; serial 765→766 |
| G4 live deploy | ⏳ | redeploy; eyeball pending |
