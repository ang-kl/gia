(№ 607 - 09-06 '26 07:46 SGT) v0.61.407

# Journal — soleat v0.61.407 (The shared set-location is authoritative: Menu TMA → Cuisine TMA aligns; stop the device-override)

> Operator: *"i selected Malaysia, Kuala Lumpur in Menu TMA, the mode in Cuisine TMA should change to Others first … before loading/building the Cuisine TMA check the location … set it align with Menu TMA then do the rest."*
>
> Commit 620 / journal 607 / **build 391**. web/cuisine. **Revert-ready** (detection effect only).

## New

### [HDR] #607 | 07:46 SGT 09-06-26 | v0.61.407 — set-location wins; revert the v0.61.406 device-override | App.jsx | 1 PR | 1 build |

- **[INTENT]** Set **Malaysia / Kuala Lumpur** in the **Menu TMA** → open the **Cuisine TMA** → it should be **OTHER (Others)** at **KL**, aligned, **before** loading. The shared set-location must win.

- **[ROOT — my own v0.61.406 was the conflict]** `tryServerCache` (`App.jsx:670`) **already** reads the cached set-location, installs it as `locationAnchor`, and auto-flips region (KL → OTHER) + countryPref (→ MY); `runInitialLoad` (`:1549`) centres the boot load on the anchor. **But** my **v0.61.406** anchor-coherence check then **overrode** the Menu-set KL anchor with the **device GPS (SG)** — KL is >150 km from the SG device → it forced the device. That's exactly the conflict the operator flagged.

- **[DELTA]** (`web/cuisine/src/v2/App.jsx` — the anchor/device detection effect only)
  - The override is **removed**. The >150 km branch is now a **no-op that keeps the set-location** (`anchorCoherenceCheckedRef = true` and nothing else — no `setLocationAnchor(null)`, no device `runSearch`, no region→device `setState`). The keep/use modal **stays gone** (`anchorMismatch` is still never set; the gate-opener fires the one boot load at the anchor).
  - Net: Menu sets KL → shared cache → Cuisine `tryServerCache` aligns **region = OTHER + anchor = KL + country = MY** → boot load centres **KL** → loads KL. (Reverts v0.61.406's *device-wins* in favour of *set-location-wins*.)
  - **`package.json`** `0.61.406 → 0.61.407`; **`doc/.serial-state.yml`** commit 619 → 620, journal 606 → 607, **build 390 → 391**; re-anchored 07:46 SGT.

- **[VERIFICATION (sandbox)]** Cuisine build green. Full suite **113 files / 3107 tests**.

- **[STATUS]** PR-**OPEN**, **auto-merge armed (squash)**. Railway redeploys on merge.

- **[TEST]** Operator: set **KL** in Menu TMA → open Cuisine TMA → region pill = **Others**, map + search at **KL** (not SG), **no device-override, no modal, no hang**.

- **[SCOPE — the operator's 4 parts]**
  - **#1 (Menu KL → Cuisine Others)** + **#2 (align before load)** → **done** here.
  - **#3 (each change aligns the other TMA)** → already served by the **shared server-side set-location cache** (both TMAs write set-location + read it on open / visibility-refresh). No code change needed; verify.
  - **#4 (on exit to free chat, fire "Search area set to: {name}. Use /location to change it or return to your previous search area.")** → **deferred** — needs the bot side **and a reliable Telegram TMA-close trigger** (`WebApp.close()` doesn't webhook the bot). Flagged to the operator for the trigger decision.

- **[KNOWN GAPS / SAFETY]**
  - **Contained to the detection effect** — a *removal* of logic (the override), not new gate timing. The gate-opener + the rest are untouched. **Revert-ready.**
  - A genuinely stale anchor (e.g. an old overseas pick) now **persists** instead of being auto-corrected to the device — that is the operator's intent (the set-location is the search area; change it via 📍 / `/location`). The banner still reverse-geocodes the right name.
  - Not unit-tested (React gate flow). **Operator must verify on the live TMA.**

- **Confirmation Gates**

  | Gate | Resolution | Date |
  |---|---|---|
  | G1 — version bump | PATCH `0.61.406 → 0.61.407`. | 09-06 '26 |
  | G2 — destructive action | Removes the v0.61.406 override; no new behaviour, no modal; gate-opener untouched. | 09-06 '26 |
  | G3 — new decision rule | The **shared set-location wins** over device GPS on Cuisine open; device GPS only seeds when there is no set-location. | 09-06 '26 |
  | G4 — paid external API call | **Fewer** — no extra device search; the boot load runs once at the anchor. | 09-06 '26 |

## Amendments

- **`web/cuisine/src/v2/App.jsx`** — anchor/device detection no longer overrides the set-location with the device (set-location authoritative).
- **`package.json`** — `0.61.406 → 0.61.407`.
- **`doc/.serial-state.yml`** — counters (commit 620 / journal 607 / build 391) + anchor 07:46 SGT 09-06-26.

## Deleted

- The v0.61.406 auto-use-device override block (superseded by the keep-set-location no-op).
