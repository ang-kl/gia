(№ 844 - 04-07 '26 10:40 SGT) v0.62.490

# Journal — soleat v0.62.490 (Menu TMA: extend UI locales from 5 → 8, adding zh · ja · es)

> Operator (task 2 — TMA language-set audit):
> *"audit the language set across all TMA, Menu TMA doesn't have the full language listing like Cuisine."*
> Read as: the Cuisine TMA already ships en/fr/id/ru/de/**zh/ja/es** (8); Menu/Hawker/Transport/Clipboard
> are stuck at 5 (en/fr/id/ru/de). Bring them up to parity, one PR per TMA. **This is PR 1/4 — Menu.**

## New

### [HDR] #844 | 10:40 SGT 04-07-26 | v0.62.490 — Menu TMA gains zh · ja · es | 2 web files + 3 doc/meta | PR #(pending) |

- **[INTENT].** Close the Menu-vs-Cuisine locale gap. Cuisine carries the full 8-language UI set;
  Menu was 2 years behind at 5. Add **Chinese (zh)**, **Japanese (ja)**, **Spanish (es)** to the
  Menu hub UI so the language dropdown and every hub string render in all 8.

- **[DELTA] (2 code files).**
  - `web/menu/src/i18n.js` — `SUPPORTED_LOCALES` `['en','fr','id','ru','de']` → **`+['zh','ja','es']`**.
    Appended three flat `ZH_STRINGS` / `JA_STRINGS` / `ES_STRINGS` overlays (**68 keys each** — the exact
    `ID_STRINGS` key set) + **3 merge loops** after the existing DE loop, same overlay pattern as
    id/ru/de: base `en`/`fr` win, overlay fills the gap (`if (STRINGS[k] && STRINGS[k].xx == null) …`).
  - `web/menu/src/components/LocaleToggle.jsx` — **3 dropdown rows** added after `id`:
    `🇨🇳 中文` / `🇯🇵 日本語` / `🇪🇸 Español`.

- **Translation provenance.** Agent-drafted per language against the EN/ID source. Shared keys
  (`loc.other.*`, the FAB back/end/top/down labels + arias) **reused verbatim from the Cuisine
  zh/ja/es overlays** for cross-TMA consistency. All emoji, `{label}`/`{km}`/`{country}`/`{flag}`/`{cap}`
  interpolation tokens, and `<b>…</b>` tags preserved in place. Brand/proper nouns
  (`Soleat`, `MRT`, `LRT`, `LTA`, `STB`, `UNESCO`, `Google`) kept verbatim; `50+` / `3.7` unchanged.
  One agent slip corrected pre-ship: ja `tile.train.live.offline` had rendered "offline" as Korean
  `오프라인` — fixed to `オフライン` (`🟡 LTAセンサーオフライン`).

- **[VERIFICATION (sandbox)]** `esbuild` bundle-parse **clean** on both files (external `react`/`../i18n.js`).
  A node eval-merge harness stripped the imports and confirmed **all 8 locales resolve** for
  `tile.cuisine.label` and `location.currentSet`, with `<b>{label}</b>{cap}` intact across zh/ja/es.
  Full vitest + `web/menu` build in CI.

- **[STATUS]** PROD — Menu TMA redeploys on merge (Railway). Verify on Telegram: the Menu hub language
  dropdown lists 8 languages; picking 中文 / 日本語 / Español re-renders every hub tile, chip, hint and the
  location panel in that language. **CJK render eyeball** (font stack + wrapping for zh/ja pill fit) is
  the G4 gate.

- **[TEST]** Full vitest via CI.

- **[KNOWN GAPS]** (1) Only the **Menu** TMA is done here — **Hawker, Transport, Clipboard** still
  sit at 5 locales (task-2 PRs 2–4, still due). (2) Menu **prose/content** beyond the hub UI strings
  (dish notes etc.) follows the shared translation pipeline, not this UI overlay. (3) CJK live-render
  eyeball on the deployed TMA outstanding. (4) Search auto-fire investigation (A) remains a separate
  open item. G4 gates for v0.62.483–490 remain open.

## Confirmation Gates
| Gate | Status | Note |
|------|--------|------|
| G1 | ✅ | esbuild parse clean (i18n.js + LocaleToggle.jsx); node merge harness — 8/8 locales resolve, `<b>` intact |
| G2 | ⏳ | full vitest via CI |
| G3 | ✅ | serial 843→844, commit 961→962, build 600→601; anchor re-set 04-07 '26 10:40 SGT |
| G4 | ⏳ | redeploy + operator eyeball (8-language dropdown; zh/ja/es render + CJK pill fit) |
