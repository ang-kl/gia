(№ 800 - 30-06 '26 19:40 SGT) v0.62.446

# Journal — soleat v0.62.446 (Cuisine — subtle top-right × on the group drill-in)

> Operator: "can you have a close x button subtle and not a pill type like 'Cuisine & Filter'
> when tap the right top has a x … in all the overlay like tap on Cuisine Group 'Sweets & Fusion'
> and all sub-overlay in-front."

## New

### [HDR] #800 | 19:40 SGT 30-06-26 | v0.62.446 — subtle close × on CuisineCategoryDrawer | 3 files | PR #(pending) |
- **`web/cuisine/src/v2/components/CuisineCategoryDrawer.jsx`** — the cuisine-group drill-in
  modal (opened by tapping a group card like "Sweets & Fusion") previously closed via a **←
  back arrow** in the header **and** a **full-width "Done" pill** in the footer. Cuisine
  selection applies **live** (toggling updates the parent immediately), so the Done pill was a
  redundant pill-style dismiss. Replaced **both** with **one subtle top-right ×** in the header —
  reusing the exact reference markup from the QuickFilters "Cuisine & filters" dropdown the
  operator pointed at (literal `×` U+00D7, `text-tg-hint text-xl leading-none px-1 flex-shrink-0`,
  pushed to the right via the title's `flex-1`).
- **[VERIFICATION (sandbox)]** `esbuild` JSX parse OK; the full cuisine-TMA build is gated by CI
  on the PR (CI builds cuisine/hawker/menu/transport; only clipboard/about are skipped).
- **[STATUS]** PROD — cuisine TMA rebuild on merge.
- **[KNOWN GAPS]** Other surfaces left as-is pending operator confirm which else need the ×:
  **FunFactModal** (a loading overlay — already has 🛑 Stop + 3s auto-dismiss);
  **ResultPanel / MapPanel / TellMePanel / CuisineDrawer / ActiveFilters** are inline page
  regions (not dismissible modals) with their own toggles / per-chip ×. QuickFilters already
  carries the reference ×.

## Confirmation Gates
| Gate | Status | Note |
|------|--------|------|
| G1 | ✅ | esbuild JSX parse; CI builds cuisine |
| G2 | ➖ | no unit tests touch this view |
| G3 | ✅ | serial 799→800 |
| G4 | ⏳ | redeploy + operator eyeball |
