(№ 285 - 21-05 '26 13:55 SGT) v0.61.74 → v0.61.77

# Journal — soleat v0.61.73 → v0.61.77 (four-PR code-fix catch-up: #572 / #573 / #574 / #577)

> Catch-up pass for the four **code-fix PRs** shipped this session that the
> two intervening **docs PRs** (already journaled) did not cover:
> - journal **#276** (`journal-0_61_76-…-0944.md`) = the v0.61.76 vault/docs
>   pass (PR #575) — its `[KNOWN GAPS]` explicitly flagged #572/#573/#574 as
>   un-journaled;
> - journal **#277** (`journal-0_61_77-…-1111.md`) = v0.61.77 map controls
>   (PR #576).
>
> The four entries below run journal **#278–#281** / commit **285–288** /
> build **85–88**. Sequencing note: PRs #572/#573/#574 merged (08:49 / 09:04 /
> 09:29 SGT) *before* the already-journaled #575 (#276) and #576 (#277), so
> these journal/commit/build numbers run **after** despite the earlier merge
> times — standard concurrent-PR sequencing (cf. the v0.60.165/166 and
> v0.60.230/231 precedents in `.serial-state.yml`).
>
> **Version-number collisions** (concurrent PRs, recorded here for the trace):
> `v0.61.76` is shared by **#574** (this catch-up, journal #280) and **#575**
> (vault, journal #276); `v0.61.77` is shared by **#576** (map controls,
> journal #277) and **#577** (this catch-up, journal #281). All four code
> bodies are merged to `main`; no files overlap. `package.json` on `main` is
> `0.61.77`. Renumbering is deferred to the operator.

## New

### [HDR] #278 | 08:49:55 SGT 21-05-26 | v0.61.74 — Bus menu title + Train nearest-stations radius fix | ~1 kb | 3 files | 1 PR | 1 build |

- **[INTENT]** Two `/transport` fixes: reword the Bus sub-menu title, and fix
  the Train reply listing only **1** station under a "Nearest 3" header.

- **[DELTA]**
  - **`i18n.js`** — `transport.bus.menu.title` `🚌 Bus — pick what you need`
    → `🚌 Bus Information` (EN); FR → `🚌 Informations bus`.
  - **`index.js`** — `runTransportTrain` MRT search radius widened
    **1500 m → 5000 m**. From MRT-sparse spots (operator at VivoCity — only
    HarbourFront within 1.5 km) the tighter radius returned 1 station while
    the header promised 3. `rankPreference:DISTANCE` + `slice(0,3)` still
    yields the true nearest 3; the wider circle only guarantees 3 are found.
    `pipeline.js`'s separate 1500 m venue-MRT enrichment call left as-is.
  - **`package.json`** — `0.61.73` → `0.61.74` (PATCH).

- **[VERIFICATION (sandbox)]** `node --check index.js i18n.js` clean;
  CI green — syntax check, Vitest 1928/1928, TMA build.

- **[STATUS]** PROD — PR #572 squash-merged to `main` as `006cff6`.

- **[TEST]** `/transport` → Bus → title reads `🚌 Bus Information`. `/transport`
  → Train (saved location near few MRTs) → 3 stations listed, distance-sorted.

- **[KNOWN GAPS]** The `transport.train.nearestHeader` string still hardcodes
  "3" — always satisfied at a 5 km radius in Singapore; an adaptive "Nearest
  {n}" was noted but not done.

- **Confirmation Gates** — G1: PATCH `0.61.73`→`0.61.74` (21-05 '26).
  G2/G3/G4: _n/a_ (copy + bug fix; no deletion, no new rule, no paid API).

### [HDR] #279 | 09:04:45 SGT 21-05-26 | v0.61.75 — Menu hub FAB always shows "End" | ~1 kb | 2 files | 1 PR | 1 build |

- **[INTENT]** Fix the `/menu` hub's bottom-left FAB showing `⇠ back` instead
  of `🔚 end` after `menu → Cuisine → close`.

- **[DELTA]**
  - **`web/menu/src/components/BackFab.jsx`** — the FAB now unconditionally
    renders `🔚 end` and calls `WebApp.close()`. It previously chose
    Back-vs-End from `window.history.length > 1`; `history.length` only ever
    grows (`history.back()` never shrinks it), so once `menu → Cuisine`
    navigated it stayed `≥2` permanently. The menu hub is always the webview
    entry point (nothing navigates *into* `/app/menu`) and has no sub-views,
    so "Back" is never meaningful there.
  - **`package.json`** — `0.61.74` → `0.61.75` (PATCH).

- **[VERIFICATION (sandbox)]** `web/menu` Vite build clean (37 modules);
  CI green — syntax check, Vitest, TMA build.

- **[STATUS]** PROD — PR #573 squash-merged to `main` as `d34ec5a`.

- **[TEST]** `/menu` → tap Cuisine → close → the hub's bottom-left FAB reads
  `🔚 end`; tapping it closes the Mini App.

- **[KNOWN GAPS]** `web/hawker/.../BackFab.jsx` and `web/transport/.../BackFab.jsx`
  still carry the same `window.history.length` heuristic — left untouched
  (they may have genuine in-app sub-views); flagged for a separate audit.

- **Confirmation Gates** — G1: PATCH `0.61.74`→`0.61.75` (21-05 '26).
  G2/G3/G4: _n/a_.

### [HDR] #280 | 09:29:44 SGT 21-05-26 | v0.61.76 — station-info link: normalised name lookup | ~1 kb | 2 files | 1 PR | 1 build |

- **[INTENT]** Fix `/transport` Train hyperlinking only 2 of 3 station names
  to their SMRT/SBS station-info page.

- **[DELTA]**
  - **`index.js`** — `stationMoreInfoUrl()` now does a **normalised fallback
    lookup**. It was an exact, case-sensitive key lookup into
    `data/stations.json`; the JSON keys the station `HarbourFront` (camelCase)
    but Google Places returns `Harbourfront`, so the exact match missed and
    the name rendered as plain text. A second index, keyed by a normalised
    name (lowercased, `MRT`/`LRT`/`Station` + punctuation stripped), is built
    on first load; an exact-key miss falls back to it.
  - **`package.json`** — `0.61.75` → `0.61.76` (PATCH).

- **[VERIFICATION (sandbox)]** `node --check index.js` clean; lookup
  simulation resolved `Harbourfront` / `HarbourFront` / `Telok Blangah` /
  `Tiong Bahru` / `Harbourfront MRT Station`; CI green.

- **[STATUS]** PROD — PR #574 squash-merged to `main` as `ba94546`.
  Version `0.61.76` collides with PR #575 (vault, journal #276) — see header.

- **[TEST]** `/transport` → Train (saved location near VivoCity) → all 3
  station names are hyperlinks, Harbourfront included.

- **[KNOWN GAPS]** This fixes the *matching*, not data coverage — the
  `more_info_url` presence across all 180 `stations.json` keys is not audited.

- **Confirmation Gates** — G1: PATCH `0.61.75`→`0.61.76` (21-05 '26).
  G2/G3/G4: _n/a_.

### [HDR] #281 | 13:52:40 SGT 21-05-26 | v0.61.77 — SBS Transit station-info deep links | ~6 kb | 3 files | 1 PR | 1 build |

- **[INTENT]** Make SBS Transit station-info links station-specific. SMRT
  stations deep-linked; all 80 SBS station-line rows in `data/stations.json`
  pointed at the generic `TrainInformation` page — `build-station-info.js`
  flagged this as a deferred "Phase 2" task.

- **[DELTA]**
  - **`scripts/build-station-info.js`** — `moreInfoUrl()` now builds the SBS
    query URL `?TrainLine=<line>&Station=<code>`. New `SBS_TRAINLINE`
    (`NEL` / `DTL` / `SKG+LRT` / `PGL+LRT`) and `SBS_STATION_CODE` (80
    stations) maps, **scraped from the live SBS site** — the
    `TrainInformation` page (NEL/DTL server-render their `<select>`) and the
    `/Ajax/StationDropdown` endpoint (LRT) — then joined against
    `data/stations.json` by line + station name: **80/80 matched**. LRT
    interchange centrals (Sengkang `STC`, Punggol `PTC`) resolve to the
    East-loop entry (`STCE` / `PTCE`), per the operator's `PTCE`-for-Punggol
    example. Quality/schema notes updated.
  - **`data/stations.json`** — regenerated; all 80 SBS rows now carry a
    specific URL; the obsolete "SBS more_info_url is generic" quality note
    removed.
  - **`package.json`** — `0.61.76` → `0.61.77` (PATCH).

- **[VERIFICATION (sandbox)]** `node --check scripts/build-station-info.js`
  clean; 80/80 join, 0 unmatched; spot-checks match the operator's reference
  URLs (HarbourFront→HBF, Telok Ayer→TLA, Compassvale→SE1, Punggol→PTCE);
  CI green.

- **[STATUS]** PROD — PR #577 squash-merged to `main` as `8f65ecc`.
  Version `0.61.77` collides with PR #576 (map controls, journal #277) —
  see header.

- **[TEST]** `/transport` → Train → tap an SBS-line station name (e.g.
  HarbourFront) → opens that station's SBS Transit info page, not the
  generic one.

- **[KNOWN GAPS]** Hume (`DT4` / SBS `HUM`) is in the `SBS_STATION_CODE` map
  but not yet in `data/stations.json` — no Hume link ships until Hume is
  added to our station data.

- **Confirmation Gates** — G1: PATCH `0.61.76`→`0.61.77` (21-05 '26).
  G2: _n/a_ — regeneration + note removal, no file/data deletion.
  G3: _n/a_ — the SBS URL format is operator-supplied reference data, not a
  new decision rule. G4: _n/a_ — the SBS scrape is a public, unauthenticated
  web fetch (no paid API).

## Amendments

_None._

## Deleted

_None._
