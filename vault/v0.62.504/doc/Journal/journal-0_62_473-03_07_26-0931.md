(№ 827 - 03-07 '26 09:31 SGT) v0.62.473

# Journal — soleat v0.62.473 ("Closed" tab now visible + France search gated off)

> Operator (IMG_1102): the v0.62.472 "Closed" strip *"didn't wrap down like a tab"* — only the red
> word showed. Root cause: the tab was `bg-white` on a white card, so the tab shape was invisible.
> Second message: *"Disabled the search for France country first until we can settle the rest."*

## New

### [HDR] #827 | 09:31 SGT 03-07-26 | v0.62.473 — visible "Closed" tab + FR picker gate | 5 files | PR #(pending) |
- **`web/cuisine/src/v2/components/ResultCard.jsx` — visible tab.** The v0.62.472 corner tab kept
  `bg-white text-red-600`; on the white card only the red word rendered (no tab). Now a **solid tab**
  matching the "… & Nearby Flavours" banner: **`bg-red-600 text-white`**, `text-[10px]`,
  `rounded-br-lg`, `-mt-2.5 -ml-2.5` flush into the card's **top-left corner** so it **wraps down**
  like a folder tab. CVD-safe — the WORD "Closed" carries the state (no green counterpart; open
  venues show no strip).
- **`web/cuisine/src/v2/lib/countries.js` + `web/menu/src/countries.js` — France gated off.** The
  `{ code: 'FR', … 'France' }` picker entry is **commented out** in both TMAs, so France is no longer
  user-searchable until the catalogue is settled (**Paris 1★ + Bib Gourmand + the other 11 cities
  still due**). **Scaffold retained** — `FR-michelin.js`, the FR cities, centroids and Michelin
  loader registration all stay; only the picker entry is gated, so re-enabling is a one-line revert.
- **[VERIFICATION (sandbox)]** `node --check` on both `countries.js` (pass); `esbuild --loader=jsx`
  parse of `ResultCard.jsx` clean (exit 0).
- **[STATUS]** PROD — Cuisine + Menu TMAs rebuild on merge. Verify on the live TMA: closed venue card
  shows a solid red "Closed" tab wrapping down; France absent from the country picker.
- **[TEST]** Full vitest via CI.
- **[KNOWN GAPS]** France catalogue completion parked on the operator's browser-scrape go/no-go; the
  pink "Closing in N min" strip left full-width (scoped to the Closed tab only). Taxonomy re-dispatch
  run 28632339821 in flight (checkpointing to `bot/draft-dish-taxonomy`).

## Confirmation Gates
| Gate | Status | Note |
|------|--------|------|
| G1 | ✅ | node --check both countries.js; esbuild jsx parse clean |
| G2 | ⏳ | full vitest via CI |
| G3 | ✅ | serial 826→827, commit 944→945, build 583→584; anchor re-set 09:31 SGT |
| G4 | ⏳ | redeploy + operator eyeball (visible "Closed" tab; France gone from picker) |
