(№ 524 - 05-06 '26 18:35 SGT) v0.61.323

# Journal — soleat v0.61.323 (Cuisine TMA: defer the boot venue load to the splash gate)

> Operator (05-06 '26, follow-up to v0.61.322): live bug. Entered the Cuisine TMA via the Menu TMA, the anchor-coherence modal appeared, chose **"Keep JP"** — but the result list showed the first 5 venues in SINGAPORE. Railway log: a `cuisine-search region=SG center=1.2820,103.8525` fired at 10:25:04 (device GPS / SG) and returned 5 SG venues, BEFORE `/api/cuisine/user-location` (the saved JP anchor) even loaded at 10:25:19. Operator intent (recorded v0.61.322): *"check BEFORE load"*.
>
> Root cause: v0.61.322 gated only the **visuals** (`locationGateOpen` hides the body). The **initial venue load still fired at raw device GPS underneath the splash** — from the auto-detect effect's `runSearch` and the warm-start/deep-link effect — racing the anchor-coherence check. The v0.61.322 KNOWN GAP ("the auto-detect / warm-start searches may fire underneath during the splash — harmless") turned out NOT to be harmless: the SG search populated/raced the list before the Keep-JP search.
>
> Commit 537 / journal 524 / build 324, on branch `claude/v0.61.323-vibe-journal-regen` off `main` at v0.61.322 (sha 0af44c4).

## New

### [HDR] #524 | 18:35 SGT 05-06-26 | v0.61.323 — defer boot venue load until the gate opens (no SG-under-splash) | 3 files | 1 PR | 1 build |

- **[INTENT]** No venue list is populated until `locationGateOpen` is true. On the no-mismatch boot, fire EXACTLY ONE initial load at the confirmed location. On a mismatch boot, the `apply*` choice already fired the correct search — do NOT double-fire (and never fire the stray SG/device load).

- **[DELTA]**

  **1. `web/cuisine/src/v2/App.jsx`**

  - **New ref `initialLoadFiredRef` (`useRef(false)`)** — declared with the v0.61.322 `locationGateOpen` / `modalPendingRef` block. Ensures the boot venue list is populated exactly once, and only after the gate opens.
  - **Auto-detect effect** — REMOVED its direct `runSearch(snap, {target})` call (was ~line 1198). The effect now only RESOLVES + persists location state (region / countryPref / locationAnchor / searchCenter) and logs "auto-detect applied (load deferred to gate)". `autoDetectedRef` / `initialSearchDone` stay claimed so warm-start remains suppressed on this path.
  - **Warm-start / deep-link effect → extracted into a function `runInitialLoad()`** (function declaration, hoisted). It is no longer an eager `useEffect` keyed on `userLoc`. It reads the CURRENT resolved center (`locationAnchor` → else committed `searchCenter` → else `userLoc`) and the committed `state`. All prior behaviour preserved: deep-link `runSearch(state, {anchor})`; `warmStart()` seeding with `setVenues` / `setFirstLoadPending(false)` / `setWarmStartSeed` / `setPages([...])` / `setCursor(0)` / `setLastRunSnap` / edit-search pulse; the generic `runSearch(state)` fallback on empty/error. Re-entry guarded by `initialSearchDone.current`.
  - **Gate-opener effect** — after `setLocationGateOpen(true)`, if `!initialLoadFiredRef.current` it sets the ref true and calls `runInitialLoad()`. This is now the SINGLE place a boot load is fired on the no-mismatch path (and the deep-link path: `runInitialLoad` searches the hash anchor as before).
  - **`applyAnchorCoherenceChoice`** — sets `initialLoadFiredRef.current = true` up-front (it fires its own `runSearch` in BOTH branches), so the subsequent gate-open does NOT also fire `runInitialLoad()`. `applyCoherenceChoice` / `applyRegionCoherenceChoice` do NOT fire their own search (they only stage state), so they intentionally leave the ref false → the gate-opener fires the ONE load at the reconciled location. No empty-list edge.
  - **bfcache `pageshow` (persisted)** — already reset `initialSearchDone.current = false`; now also resets `initialLoadFiredRef.current = false` and `setLocationGateOpen(false)` so the gate-opener re-fires `runInitialLoad()` to refill the list cleared on `pagehide`. Coherence `*CheckedRef`s persist, so with `userLoc` resolved the gate re-opens next pass (no modal → no splash flash).

  **2. `package.json`** — `0.61.322 → 0.61.323`.

  **3. `doc/.serial-state.yml`** — counters (commit 536→537, build 323→324, journal 523→524) + `last_anchor_*` re-anchored 05-06 '26 18:35 SGT.

- **[VERIFICATION (sandbox)]**
  - `web/cuisine/src/v2/App.jsx` passes **esbuild** JSX parse (`node_modules/.bin/esbuild --loader:.jsx=jsx --bundle=false --format=esm src/v2/App.jsx` → clean, no error).
  - No `index.js` change. `npm test -- --run` unaffected (no source under test touched; TMA React code is not in the vitest suite).
  - Local `npm run build` still hits the iCloud `vite/dist` corruption (`does not provide an export named 'B'`) — an env artifact, NOT this code; **CI TMA-build is the authoritative gate** (v0.61.319–322 precedent).
  - Logic trace of the three boot paths confirms: (a) NO venue search fires until the gate opens; (b) no-mismatch boot fires exactly one load at the device/confirmed location; (c) Keep-JP / Use-current fire exactly one load at the chosen location with NO SG load.

- **[STATUS]** PR-OPEN on push; auto-merge squash armed. Railway redeploys on merge.

- **[TEST]** Operator (post-merge, live):
  1. Cold launch, clean SG location → splash → gate opens → ONE search at the device SG location (no earlier/stale search).
  2. Enter via Menu TMA with a saved JP anchor (Naka Ward / Downtown Cove) → splash → anchor modal → **Keep JP** → list shows JP venues, NO SG search at all.
  3. Same → **Use my current location** → list shows the device-location venues, fired once.
  4. Deep-link (`/cuisine` hash anchor) → splash → gate opens → ONE search at the deep-link anchor.
  5. No-GPS-no-cache pathological path → splash → gate opens → warm-start / fallback runSearch fires once.

- **[KNOWN GAPS]**
  - `runInitialLoad` reads the closure `state` / `searchCenter` / `locationAnchor` at the render in which the gate-opener effect runs. Because the auto-detect effect commits those via `setState`/`setSearchCenter`/`setLocationAnchor` BEFORE the gate-opener's `*CheckedRef` gate is satisfied (the anchor-coherence check waits on `userLoc` + `locationAnchor`), the values are committed by the time the gate opens. If a future change lets the gate open before the auto-detect commit lands, `runInitialLoad` would read a partially-resolved center — guard with the `*CheckedRef`s as today.
  - Edge special-cased: bfcache restore re-shuts the gate to re-fire the load; the `*CheckedRef`s persist so the modal does not re-prompt and the splash is a one-frame flash, not a stall.

- **Confirmation Gates**

  | Gate | Resolution | Date |
  |---|---|---|
  | G1 — version bump | PATCH `0.61.322 → 0.61.323`. Boot-load timing fix; no API contract change. | 05-06 '26 |
  | G2 — destructive action | _n/a_. Moves a `runSearch` call + extracts an effect into a function; no data deletion. | 05-06 '26 |
  | G3 — new decision rule | Boot venue load is deferred to the splash-gate opener — no search fires until location is confirmed. | 05-06 '26 |
  | G4 — paid external API call | _n/a_ at gate time; `runInitialLoad` fires the same warm-start / search calls already made on a normal boot — now once, after confirmation. | 05-06 '26 |

## Amendments

- **`web/cuisine/src/v2/App.jsx`** — v0.61.323 defers the boot venue load to the splash gate. Amends the v0.61.322 KNOWN GAP (quoted verbatim: *"The auto-detect / warm-start searches may fire underneath during the splash — harmless, the body is hidden, and the apply* path fires a fresh correct search."*). Correction with rationale: the underneath search was NOT harmless — the device/SG `runSearch` populated/raced the result list before the apply\* (Keep-JP) search, so the operator saw 5 SG venues after choosing Keep-JP. Fix: removed the auto-detect effect's direct `runSearch`; extracted the warm-start/deep-link/fallback body into `runInitialLoad()`; added `initialLoadFiredRef`; the gate-opener now fires the single boot load after the gate opens; `applyAnchorCoherenceChoice` claims the ref so it isn't double-fired.
- **`package.json`** — version `0.61.322 → 0.61.323`.
- **`doc/.serial-state.yml`** — counters + last_anchor_time re-anchored 05-06 '26 18:35 SGT.

## Deleted

_None._
