(№ 858 - 06-07 '26 07:26 SGT) v0.62.504

# Journal — soleat v0.62.504 (log-noise cleanup: silence the two 5-min heartbeats)

> Operator: "1" — chose the log-noise cleanup from the outstanding list, after pasting a Railway
> runtime-log dump with "why is there a need to refresh and waste my token".
> Read as: the two every-5-minute heartbeat lines spam the Railway runtime log with no signal —
> make them edge-triggered (log on change), not level-triggered (log every tick).

## New

### [HDR] #858 | 07:26 SGT 06-07-26 | v0.62.504 — quiet the Vault-Index + Pulse heartbeats | 4 files | PR #(pending) |

- **[INTENT].** The Railway **runtime** (Deploy) log showed two lines repeating every ~5 min with
  no state change:
  - `[Vault-Index] Refreshed: 0 review caches, 0 summaries` (`vault-index.js`) — fired on every
    refresh tick even when nothing was cached.
  - `[Pulse] Status updated at <SGT>` (`index.js`, `updateTransitStatus()`) — fired on every LTA
    poll regardless of whether the health status changed.

- **[DELTA] (4 files).**
  - `vault-index.js` — the refresh log is now **gated on content**: only prints when
    `reviews.size || summaries.size` (the idle 0/0 tick is silent). Error path unchanged.
  - `index.js` (`updateTransitStatus`) — added a module-scope `let lastPulseStatus = null;`
    tracker; the Pulse log is now **edge-triggered**: it prints `[Pulse] Status → <label> at <SGT>`
    only when the health label (`🟢 Healthy` / `🔴 Disruption`) **changes** from the last tick.
    Hoisted the label into `statusLabel` (reused by `writeStatus` + the compare). The `writeStatus`
    call still fires every tick (Redis SSOT stays fresh); only the console line is throttled. The
    LTA-disabled stub log and the error/degraded logs are unchanged.
  - `doc/Journal/journal-0_62_504-06_07_26-0726.md` — this entry.
  - `doc/.serial-state.yml` — commit 975→**976**, build 614→**615**, journal 857→**858**; anchor
    re-set `06-07 '26 07:26 SGT`; prior anchor demoted to `…_superseded_1500`.
  - `package.json` — 0.62.503 → **0.62.504**.

- **[VERIFICATION (sandbox)]** `node --check index.js` clean; `node --check vault-index.js` clean.
  Both edits confirmed to apply on current `origin/main` HEAD `fd621ca6` (blob shas of both files
  identical between the download base `f843ef2d` and HEAD — PR #1505 was docs-only). `npm test`
  green (see [TEST]).

- **[STATUS]** PROD — merges to `main`; Railway redeploys `node index.js` on merge. Behaviour is
  log-only: no functional change to the LTA sniffer or the vault index; Redis writes unchanged.

- **[TEST]** Full vitest suite green (no test targets the two console lines; the change is
  observable only in the runtime log). No web build (no `web/` change).

- **[KNOWN GAPS]** None new. Standing items unchanged: O-49 (`uv`, external/blocked on operator),
  O-50 (`index.js:5739` SGT bot forecast), O-51 (FR Michelin BBOX); DF-1 MRT flip (12 Jul);
  companion `doc/Technical/` arc entry + `vault/v0.62.502` snapshot still due.

## Confirmation Gates
| Gate | Status | Note |
|------|--------|------|
| G1 | ✅ | code touched (index.js, vault-index.js); node --check clean both; version 0.62.503→0.62.504 |
| G2 | ➖ | no doc under the AU append-only Recipe amended (journal + serial are additive) |
| G3 | ✅ | serial commit 975→976, build 614→615, journal 857→858; anchor re-set 06-07 '26 07:26 SGT |
| G4 | ➖ | no user-facing surface; runtime-log-only change |
