(№ 773 - 28-06 '26 18:05 SGT) v0.62.419

# Journal — soleat v0.62.419 (Sketchbook P3 — collapsible VenueCard + vertical Clipboard list)

> Operator: "continue everything." P3 = the eatery card (EateryCard.dc.html → VenueCard.jsx).

## New

### [HDR] #773 | 18:05 SGT 28-06-26 | v0.62.419 — VenueCard | 3 files | PR #(pending) |
- **[INTENT]** Port the eatery card to a clean, expandable card honest to the real data.
- **[DATA NOTE]** A saved card is a copied-text snapshot (`cuisines[]` tag + `body`/`preview`/
  `note`), NOT the prototype's structured venue (rating/crowd/dishes). So the card surfaces what
  exists rather than fabricating fields.
- **[DELTA]**
  - **`components/VenueCard.jsx`** — collapsible: collapsed shows ⭐ name · cuisines · 1-line
    preview · ▸; expanded shows the full copied `body` (whitespace-pre-wrap, scroll-capped) + note
    + an actions row (📋 Copy, ✎ Edit; ＋ File in clipboard context / ✕ Remove in drawer context
    via optional `onFile`/`onRemove`). Tap toggles expand; long-press still drags (dragProps on
    root, `stopPropagation` on actions). Copy uses `navigator.clipboard` + haptic.
  - **`components/CatchAllStrip.jsx`** — Clipboard list changed from a horizontal 200px strip to a
    **vertical list** (needed for in-place expand; matches the prototype's list layout).
  - **`lib/i18n.js`** — `card.copy/copied/edit/file/remove` (en/fr/id/ru/de).
- **[VERIFICATION (sandbox)]** Real `vite build` OK (50 modules → public/clipboard).
- **[STATUS]** PROD — clipboard TMA rebuild; redeploys per PR.
- **[TEST]** No unit test (UI). Live eyeball after P4–P5.
- **[KNOWN GAPS]** ＋File / ✕Remove buttons render only when the parent passes `onFile`/`onRemove`
  — wired in P4 (File sheet) and the drawer context. P4 (File sheet + edit pills + 📍/🧭 Recents)
  and P5 (Settings) still to come.

## Confirmation Gates

| Gate | Status | Note |
|------|--------|------|
| G1 build/parse | ✅ | real vite build (50 modules) |
| G2 tests | ⏳ | CI |
| G3 journal/serial | ✅ | serial 772→773 |
| G4 live deploy | ⏳ | redeploy |
