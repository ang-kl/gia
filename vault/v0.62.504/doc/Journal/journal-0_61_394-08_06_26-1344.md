(№ 594 - 08-06 '26 13:44 SGT) v0.61.394

# Journal — soleat v0.61.394 (First load runs the FULL search, not the lean warm-start)

> Operator (**urgent**): *"remove the 15 seconds limitation for first load, there isn't the full card."*
>
> **Left OPEN for review — auto-merge to be armed on the operator's word.** Commit 607 / journal 594 / build 384. web/cuisine.

## New

### [HDR] #594 | 13:44 SGT 08-06-26 | v0.61.394 — first load = full search | App.jsx | 1 PR | 1 build |

- **[INTENT]** The first load came up **without the full card** (no review / type / price / social). Operator wants the full template on first load.

- **[ROOT (traced)]** `runInitialLoad` (`App.jsx:1469`) ran the **lean warm-start** endpoint on a non-anchored first load (an anchored boot already used the full `runSearch`, `:1490`). `/api/cuisine/warm-start` **"reduces Places-enrichment fan-out"** (`index.js:12553`) — it deliberately **skips** the review/type/price/social enrichment for speed, so the first card had no full template. (The "15s" is the perception of this lean-then-slow behaviour — confirmed **again** there is no literal 15s cutoff in the search/enrichment; the only `15000`s are the bot please-wait nudge, the iata-snap Gemini per-attempt, and the client fact-card rotation.)

- **[DELTA]** (`web/cuisine/src/v2/App.jsx`)
  - The non-anchored first load now runs the **full `runSearch`** instead of `warmStart`. Replaced the entire warm-start-first async block (warmStart call + setPages history seeding + edit-search pulse) with `setSearchCenter(center)` + `runSearch(state, { center })`.
  - `runSearch` was **already** the boot path for an explicit anchor (`:1490`) and the warm-start empty-fallback (`:1559`), and it clears `firstLoadPending` the same way (`:1717/1786/1960`) — so it's **proven boot-safe**.
  - **`package.json`** `0.61.393 → 0.61.394`; **`doc/.serial-state.yml`** commit 606 → 607, journal 593 → 594, **build 383 → 384**; re-anchored 13:44 SGT.

- **[SAFETY]** This change is in **`runInitialLoad` only** — it does **NOT** touch the splash-gate / coherence effects (the v0.61.391 hang area), so it **cannot** reintroduce that hang.

- **[VERIFICATION (sandbox)]** Cuisine build green. Full suite **113 files / 3088 tests**.

- **[STATUS]** PR-**OPEN**, **auto-merge to arm on the operator's word**. Railway redeploys on merge.

- **[TEST]** Operator: the very first card now shows the full template (review 💬 / type / price / social). First load is **slower** (it's the full search now — you accepted "first load just ask user to wait").

- **[KNOWN GAPS]**
  - **First-load latency rises** — the lean warm-start fast path is gone for non-anchored boots; the boot now pays the full enrichment cost.
  - `warmStartSeed` stays wired but is always `null` now (the seed badge just doesn't show); the `warmStart` import is unused (harmless). A later cleanup can drop both.
  - The `/api/cuisine/warm-start` server endpoint is unchanged (still used by nothing on the cuisine boot now).
  - Not live-E2E tested — watch first-load latency on the deployed TMA.

- **Confirmation Gates**

  | Gate | Resolution | Date |
  |---|---|---|
  | G1 — version bump | PATCH `0.61.393 → 0.61.394`. | 08-06 '26 |
  | G2 — destructive action | Replaced the warm-start boot branch with `runSearch`; the gate/coherence effects + the warm-start endpoint are untouched. | 08-06 '26 |
  | G3 — new decision rule | The first load runs the full search (full card), not the lean warm-start. | 08-06 '26 |
  | G4 — paid external API call | None new — `runSearch` already hits Places; it now also runs on the first load (was warm-start, also Places). | 08-06 '26 |

## Amendments

- **`web/cuisine/src/v2/App.jsx`** — `runInitialLoad` non-anchored boot → `runSearch` (full card).
- **`package.json`** — `0.61.393 → 0.61.394`.
- **`doc/.serial-state.yml`** — counters (commit 607 / journal 594 / build 384) + anchor 13:44 SGT 08-06-26.

## Deleted

- The warm-start-first boot branch (warmStart call + its setPages/pulse) — superseded by `runSearch`.
