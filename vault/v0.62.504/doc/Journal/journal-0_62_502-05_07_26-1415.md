(№ 856 - 05-07 '26 14:15 SGT) v0.62.502

# Journal — soleat v0.62.502 (dish-note backfill tail: 10 grounded notes + 6-lang translations; 7 fabricated entries pruned) — 100% iconicDishes coverage

> Operator: after the coverage audit surfaced 17 un-noted iconicDishes (4 drinks + 13 thin/odd), "do these" — with the two grouped bullet lists quoted back verbatim.
> Read as: fill every remaining gap — write a grounded, sourced EN note + all-6-language translations for what is real; be honest and prune what is fabricated. Never invent.

## New

### [HDR] #856 | 14:15 SGT 05-07-26 | v0.62.502 — dish-note backfill tail (last 17 un-noted) | 4 data/gen files + 3 doc/meta | PR #(pending) |

- **[INTENT].** The v0.62.501 coverage audit found **17 of 1,689** `iconicDishes` still had no explanation
  (hand or generated). Operator: "do these." Everything else in the corpus was already 100% translated
  (1,647 notes × id/ru/de/zh/ja/es, 0 partial rows).

- **[METHOD] (grounded verify, honest split).** Three parallel research agents web-searched all 17 and
  returned a per-item verdict `REAL | UNCERTAIN | FABRICATED` with a sourced ≤160-char EN note. Result:
  **10 real/mappable**, **7 fabricated/unidentifiable**. Nothing was invented for the fabricated names.

- **[DELTA] (4 data/generated files).**
  - `nation-overlay-dishnotes.generated.js` — **+10** `note.en` entries with grounding `source:`
    (Entries 1643→**1653**): `german::hefeweizen`, `british::cask ale`, `american::ipa`,
    `australian::shiraz`, `egyptian::lamb sayadia rice` (→ sayadieh), `australasia::faiakai chop`
    (→ Tongan faikakai), `macau::serradura sago` (→ serradura), `macau::chilli crab macanese`
    (→ Macau curry crab, honest correction — no Macanese "chilli crab" exists),
    `northwestern::xibei dumpling soup` (→ Xibei lamb dumplings), `goan::coconut-vinegar curry`
    (→ Goan ambot tik).
  - `nation-overlay-dishnotes-i18n.generated.js` — **+10** entries × **6 languages** (id/ru/de/zh/ja/es),
    keys 1647→**1657**. (Dish notes carry no `fr` — consistent with all 1,647 prior entries; `note.en`
    serves en/fr surfaces.)
  - `nation-overlay.js` — **pruned 7 fabricated** `F('…')` entries: `kaipake plate`,
    `fish suckling pacific`, `antipodean cafe brunch`, `pasifika fusion plate` (australasia),
    `caca-mato` (macau), `changchun braised duck`, `three rice porridge` (northeastern). Verdicts:
    not attested as real dishes (Changchun/Jilin specialities are game/Korean-cold-noodle/pickle, not
    braised duck; no "three rice porridge" dish; the Pacific/Macau names are garbled or generic).
  - `package.json` — 0.62.501 → **0.62.502**.

- **[RESULT].** iconicDishes 1689 → **1682** (−7 pruned); un-noted **17 → 0** — **100% of remaining
  iconicDishes now carry an explanation**.

- **[VERIFICATION (sandbox)]** `node --check` clean on all 3 `.js`; overlay load-test clean; every new
  i18n entry has all 6 langs (0 missing); the 7 pruned names absent from `NATION_OVERLAY`.
  **`npm test` — 3697/3697 green** (incl. `nation-overlay.test.js` 448/448; SG `chilli crab` alias
  unaffected — that is a different dish from the pruned/edited macau entry).

- **[STATUS]** PROD — TMAs redeploy on merge (Railway). Dish notes surface in the 📜 pop-up.

- **[TEST]** `npm test` full suite + `node --check` + overlay require-load.

- **[KNOWN GAPS]** None for coverage — corpus is now fully noted + translated. `serradura sago` is a
  redundant variant of the existing `serradura` (kept + noted, honest mapping, not pruned). Standing:
  MRT CCL6 flip (task #17, 12 Jul); `index.js:5739` hard-coded-SGT bot line; FR Michelin BBOX geo-guard.

## Confirmation Gates
| Gate | Status | Note |
|------|--------|------|
| G1 | ✅ | node --check clean; npm test 3697/3697; overlay load-test + 6-lang completeness verified |
| G2 | ✅ | no destructive infra; 7 pruned entries are unverifiable/fabricated (grounded verdicts), not real data |
| G3 | ✅ | serial commit 973→974, build 612→613, journal 855→856; anchor re-set 05-07 '26 14:15 SGT |
| G4 | ⏳ | operator eyeball: 📜 pop-up shows the 10 new notes in device language on the deployed TMA |
