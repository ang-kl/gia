(№ 629 - 09-06 '26 21:30 SGT) v0.61.429

# Journal — soleat v0.61.429 (Michelin disables the rating control · "No rating" = new-or-null, >0)

> Operator feedback on v0.61.428:
> 1. *"if I select 'Michelin , Bib Gourmand', the rating (No rating | Any rating | ≥3.7 | Custom 1.0–5.0) should be disable and not included in search."*
> 2. *"if user select No rating, the display should be 'No rating'."*
> 3. *"'No rating' should be searching for new nor null rating, still appear >0."*
>
> Commit 642 / journal 629 / **build 407 (web/cuisine)**. venue-filters.js + rating-pref.js + App.jsx + QuickFilters.jsx + tests. Revert-ready.

## New

### [HDR] #629 | 21:30 SGT 09-06-26 | v0.61.429 — Michelin greys the rating pill · "No rating" broadened to new-or-null (>0) | venue-filters.js + web/cuisine + test | 1 PR | 1 build |

- **[INVESTIGATION — Michelin already bypasses the floor]** Selecting "Michelin, Bib Gourmand" adds cuisine **slug `'michelin'`** to `state.cuisines`. Server-side, `/api/cuisine/search` detects `cuisines.includes('michelin')` and **early-returns to `handleMichelinSearch` (index.js:14231) BEFORE the rating-floor seam (15539)** — so the floor was **never applied** to a Michelin search. Request 1's "not included in search" is therefore already true on the server; the missing half was the **UI** (the pill still looked active).

- **[DELTA]**
  - **`web/cuisine` — Michelin disables the rating pill (req 1):**
    - `QuickFilters.jsx` gains a **`ratingDisabled`** prop. When true the ⭐ pill greys (`opacity-40 pointer-events-none`, `aria-disabled`), `openRating()` no-ops, and a `useEffect` force-closes the panel if it was open — mirroring the v0.61.149 Halal special-mode greying.
    - `App.jsx` passes `ratingDisabled={(state.cuisines || []).includes('michelin')}`.
  - **`web/cuisine` — panel layout re-arrange + smaller fonts (mid-build operator asks):**
    - Row 1 `No rating | Any rating ⭐`, row 2 `⭐ ≥ 3.7 | ⭐ ≥ [custom box]`. The ⭐ marks the star-rating options; "No rating" (the no-star choice) carries none. The Custom option is now **just the input box** (a `<label>` wrapping `⭐ ≥ [field]`; tap / focus selects it — no separate radio dot / "Custom" word).
    - Fonts reduced: option labels `text-sm → text-xs`, hints `0.65rem → 0.6rem`, custom field + "⭐ ≥" `→ text-xs`.
    - **Save button** slimmed: `text-sm`/`px-4 py-1.5`/`min-h-[40px]` → `text-xs`/`px-3 py-1` (operator: "too big").
  - **`venue-filters.js` — "No rating" = new OR null (req 3):** `applyRatingFloor` mode **`'unrated'`** now keeps a venue when it has **no rating (null / 0)** OR is **new / barely-reviewed** (`userRatingCount < UNRATED_MODE_MAX_REVIEWS = 10`) — so a brand-new place showing a small provisional rating > 0 **still appears**. Still **never empties** (a thin area with none falls back to the input → results stay **> 0**).
  - **`rating-pref.js`** — `describeRatingPref('unrated')` log label `'unrated-only' → 'new-or-unrated'` (accuracy).
  - **"No rating" pill label (req 2):** already correct — `ratingPillLabel('unrated')` resolves `rating.pillNoRating` = **"No rating"** (fr "Sans note"). Verified, no change needed.
  - **Tests** — `__tests__/venue-filters.test.js`: updated the `'unrated'` cases (keeps null + new, incl. a new venue with rating > 0; +1 case). `__tests__/rating-pref.test.js`: `describeRatingPref` label.
  - **`package.json`** `0.61.428 → 0.61.429`; **`doc/.serial-state.yml`** commit 641 → 642, journal 628 → 629, **build 406 → 407**; re-anchored 21:30 SGT.

- **[VERIFICATION (sandbox)]** `node --check` venue-filters.js / rating-pref.js / index.js clean. Full suite **3168/3168 green** (3167 + 1). `npm run build:cuisine` green. Functional spot-check: `'unrated'` keeps null + new(any rating), drops established, never-empties — all PASS.

- **[STATUS]** PR-**OPEN**, **auto-merge armed (squash)**. Railway redeploys (server + rebuilt cuisine bundle) on merge.

- **[TEST — operator, deployed]** Select **Michelin, Bib Gourmand** → the ⭐ rating pill greys out and can't be tapped; results are the curated awards list (no floor). Deselect → the pill is live again. Set **No rating** → the pill reads **"No rating"**; the next 🔍 returns brand-new / unrated venues (incl. new places with a small rating), and the list is never empty.

- **[KNOWN GAPS]**
  - "New" here is proxied by **review count** (`< 10`), not the review-TIME signal the 🆕 New pill uses (121 days, needs per-venue Places Details). It's the cheap, no-extra-API approximation of "new/unreviewed"; bump `UNRATED_MODE_MAX_REVIEWS` if the operator wants a wider net.
  - When a Michelin search is active the TMA still *sends* `ratingPref` in the body, but `handleMichelinSearch` ignores it (the floor seam isn't reached) — harmless; left as-is to keep the diff minimal.
  - VibeCodingRecord regen still due (covers #942–#944 + this) — log/do post-merge.

- **Confirmation Gates**

  | Gate | Resolution | Date |
  |---|---|---|
  | G1 — version bump | PATCH `0.61.428 → 0.61.429`. | 09-06 '26 |
  | G2 — destructive action | UI disable + a broader (more inclusive) unrated filter. Never empties. Reversible. | 09-06 '26 |
  | G3 — new decision rule | Michelin/Bib Gourmand disables the rating control; "No rating" = new-or-null (>0). | 09-06 '26 |
  | G4 — paid external API call | None. | 09-06 '26 |

## Amendments

- **`venue-filters.js`** — `applyRatingFloor` mode `'unrated'` broadened to new-or-null (`UNRATED_MODE_MAX_REVIEWS`).
- **`rating-pref.js`** — `describeRatingPref('unrated')` → `'new-or-unrated'`.
- **`web/cuisine/src/v2/components/QuickFilters.jsx`** — `ratingDisabled` prop greys + locks the rating pill, force-closes its panel.
- **`web/cuisine/src/v2/App.jsx`** — pass `ratingDisabled` when `'michelin'` is selected.
- **`__tests__/venue-filters.test.js`** + **`__tests__/rating-pref.test.js`** — updated.
- **`package.json`** — `0.61.428 → 0.61.429`.
- **`doc/.serial-state.yml`** — counters (commit 642 / journal 629 / build 407) + anchor 21:30 SGT 09-06-26 + `last_anchor_source` prepend.

## Deleted

- The "rating pill is interactive while Michelin/Bib Gourmand is selected" behaviour — superseded (greyed + locked).
- The strict "unrated-only = rating null/0 ONLY" semantics — superseded by new-or-null (a new venue with a small rating > 0 now counts).
