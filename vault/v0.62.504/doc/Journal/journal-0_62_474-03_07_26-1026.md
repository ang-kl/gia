(№ 828 - 03-07 '26 10:26 SGT) v0.62.474

# Journal — soleat v0.62.474 (Cuisine card: "Closing in N min" pink corner-tab + ru/de i18n)

> Operator (IMG_2664 — a full-width pink-text-on-white "Closing in 17 min" strip above
> "7 · Po (Robertson Quay)"): *"The closing strip 'Closing in ## minutes' should also look
> like the 'Closed' style, make it pink background. also ensure all other languages are
> incorporated."*

## New

### [HDR] #828 | 10:26 SGT 03-07-26 | v0.62.474 — pink "Closing" corner-tab + ru/de closingSoon | 2 files | PR #(pending) |
- **`web/cuisine/src/v2/components/ResultCard.jsx` — "Closing in N min" strip → pink corner tab.**
  The v0.62.466 closing-soon strip was still a **full-width** `-mx-2.5 rounded-t-lg bg-white
  text-pink-600 text-[12px]` block (pink *text* on white). Restyled to **match the v0.62.473
  "Closed" red tab**: a **solid pink corner tab** — `bg-pink-600 text-white`, `text-[10px]`,
  `rounded-br-lg`, `-mt-2.5 -ml-2.5` bleeding flush into the card's **top-left corner** so it
  **wraps down** like a folder tab; `self-start` so it hugs the word. CVD-safe — pink is
  distinct from the "Closed" red at a glance **and** the WORD "Closing in …" carries the state
  (never colour alone).
- **`web/cuisine/src/v2/lib/i18n.js` — `card.closingSoon` for ru + de.** The key existed for
  **en/fr/id/zh/ja/es** but was **missing for ru and de**, so Russian/German users saw the
  English fallback "Closing in {n} min". Added **ru = "Закрывается через {n} мин"** and
  **de = "Schließt in {n} Min"**. All **8 supported locales** (`en fr id ru de zh ja es`) now
  carry it. Placed beside `card.closed` in each locale's `_STRINGS` block; `{n}` interpolation
  handled by the existing `tn()` formatter.
- **[VERIFICATION (sandbox)]** `esbuild` parse of the edited `ResultCard.jsx` (`--loader=jsx`)
  and `i18n.js` (esm) both clean (exit 0); `grep -c card.closingSoon` = 7 lines / 8 locales.
- **[STATUS]** PROD — Cuisine TMA rebuilds on merge. Verify on the live TMA: a closing-soon
  venue card shows a **solid pink corner tab** wrapping down (same shape as the red "Closed"
  tab); ru/de UI shows the translated closing text, not English.
- **[TEST]** Full vitest via CI (ResultCard is presentational; i18n is a data table).
- **[KNOWN GAPS]** First-load open-now meal-window bias (+ per-anchor local-time display and
  the timezone guard for out-of-hours cities) still in design — investigation done, awaiting
  operator go. France catalogue completion parked on the browser-scrape go/no-go.

## Confirmation Gates
| Gate | Status | Note |
|------|--------|------|
| G1 | ✅ | esbuild parse clean: ResultCard.jsx (jsx) + i18n.js (esm) |
| G2 | ⏳ | full vitest via CI |
| G3 | ✅ | serial 827→828, commit 945→946, build 584→585; anchor re-set 10:26 SGT |
| G4 | ⏳ | redeploy + operator eyeball (pink corner tab; ru/de closing text) |
