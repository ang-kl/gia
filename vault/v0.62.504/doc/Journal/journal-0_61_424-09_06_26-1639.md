(№ 624 - 09-06 '26 16:39 SGT) v0.61.424

# Journal — soleat v0.61.424 ("New" pill: 121-day window (days-precise), via review-time refute)

> Operator: *"are we wiring 'new' as last 121 days that Google can reply?"* → chose **change to 121 days**; will verify on the deployed TMA.
>
> Commit 637 / journal 624 / **build 404 (holds — server-only)**. index.js + hidden-verify.js. Revert-ready.

## New

### [HDR] #624 | 16:39 SGT 09-06-26 | v0.61.424 — "New" = no Google review older than 121 days | hidden-verify.js + index.js + test | 1 PR | 0 build |

- **[INTENT]** Wire the cuisine **"New"** pill to a **121-day** window (was 3 months / ~91 days), days-precise.

- **[CONTEXT — how "New" works]** It is **not** an opening-date filter (Google has no opening date). It **refutes** newness from **review `publishTime`**: a venue is dropped if its **oldest** of the (≤5) returned reviews is older than the window (a review can only post after opening). Recent-only / no-review venues are **kept** (refute-only — Google can't *confirm* newness). OTHER countries use the **same language-agnostic refute** + a **local-language "newly opened"** recall seed (v0.61.395) + the **v0.61.399 floor** (never returns 0).

- **[DELTA]** (server-only)
  - **`hidden-verify.js`** — new **`oldestReviewDays(reviews)`** (oldest review age in **days**, via `MS_PER_DAY`); exported. `oldestReviewMonths` **unchanged** (still backs `/hidden`'s month rule).
  - **`index.js`** cuisine "New" block — now uses `oldestReviewDays` + **`NEW_MAX_DAYS = 121`** (keep when `_oldestReviewDays == null || ≤ 121`). The floor + the "newly opened" query bias + the OTHER-country local-language seed are all preserved.
  - **`__tests__/hidden-verify.test.js`** — +5 `oldestReviewDays` cases (null / oldest-wins / 60 d in / 200 d out / months-unchanged).
  - **`package.json`** `0.61.423 → 0.61.424`; **`doc/.serial-state.yml`** commit 636 → 637, journal 623 → 624, **build 404 holds**; re-anchored 16:39 SGT.

- **[VERIFICATION (sandbox)]** `node --check` on index.js / hidden-verify.js clean. Full suite **3136/3136 green** (was 3131 + 5).

- **[STATUS]** PR-**OPEN**, **auto-merge armed (squash)**. Railway redeploys (server) on merge. No TMA rebuild.

- **[TEST — operator, on the deployed TMA]** **Others → a city → New → 🔍**: results should be venues whose oldest Google review is **within 121 days** (or that have no reviews); the Railway log line `[Cuisine-New] review-time refute: N → M kept (≤121d …)` shows the cut (and notes the floor when nothing is proven-new).

- **[KNOWN GAPS]**
  - Still a **heuristic** — "no review older than 121 days", **not** a true opening date (Google can't supply one). It can only **refute** (an old review ⇒ not new), never confirm.
  - `/hidden`'s newness stays **month-based** (its own rule) — only the cuisine "New" pill moved to 121 days.
  - Eyes-on-deployed verification is the operator's (no live API in the sandbox).

- **Confirmation Gates**

  | Gate | Resolution | Date |
  |---|---|---|
  | G1 — version bump | PATCH `0.61.423 → 0.61.424`. | 09-06 '26 |
  | G2 — destructive action | New helper + a constant change (3 mo → 121 d); `/hidden` untouched. Reversible. | 09-06 '26 |
  | G3 — new decision rule | "New" = no Google review older than 121 days (days-precise). | 09-06 '26 |
  | G4 — paid external API call | Unchanged — the per-candidate reviews fetch already happened on the pill. | 09-06 '26 |

## Amendments

- **`hidden-verify.js`** — `oldestReviewDays` + export.
- **`index.js`** — cuisine "New" block: `oldestReviewDays` + `NEW_MAX_DAYS = 121`.
- **`__tests__/hidden-verify.test.js`** — +5 cases.
- **`package.json`** — `0.61.423 → 0.61.424`.
- **`doc/.serial-state.yml`** — counters (commit 637 / journal 624 / build 404 holds) + anchor 16:39 SGT 09-06-26 + `last_anchor_source` prepend.

## Deleted

- The 3-month (`NEW_MAX_MONTHS = 3`) window for the cuisine "New" pill (superseded by 121 days).
