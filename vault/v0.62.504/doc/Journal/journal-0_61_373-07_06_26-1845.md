(№ 573 - 07-06 '26 18:45 SGT) v0.61.373

# Journal — soleat v0.61.373 (Decompress, stage 1: region-coherent search-centre single source of truth)

> Operator: **"fix the over-compress"** — decompress the tangled Cuisine location layer.
>
> **Left OPEN for review — auto-merge to be armed on the operator's word.** Commit 586 / journal 573 / build 371. Cuisine TMA.

## New

### [HDR] #573 | 18:45 SGT 07-06-26 | v0.61.373 — search-centre single source (region-coherent) | 3 files | 1 PR | 1 build |

- **[INTENT]** Stop the search **centre** and the **region** from being resolved separately (the root of "Other mode, still searching Singapore"). Make the centre one tested, region-coherent decision.

- **[ROOT — the over-compression]** `runSearch` (~:1600) and `runInitialLoad` (~:1451) each resolved the centre as `anchor || searchCenter || locationAnchor || userLoc` — the **device GPS was the unconditional last fallback even in OTHER/JB mode** — while the region came separately from `state.region`. So centre and region could diverge: `region=OTHER`, `centre=SG device`.

- **[MECHANISM]**
  - **`web/cuisine/src/v2/lib/search-location.js`** (NEW, pure) — `resolveSearchCenter({ region, anchor, searchCenter, locationAnchor, userLoc })` + `isValidPoint` + `regionUsesDeviceFallback`. Explicit picks win in **every** region; the device GPS is a valid centre **only** in SG (and `__NONE__`). In **OTHER / JB / MY-PUT** it returns **null** — the centre can never silently be the device location.
  - **`__tests__/search-location.test.js`** (NEW) — 12 cases (explicit-pick priority, SG device fallback, OTHER/JB → null, device-followed anchor still centres, invalid → null).
  - **`web/cuisine/src/v2/App.jsx`** — `runSearch` uses it (null → the existing visible "pick a country + city" error); `runInitialLoad` uses it (null → **don't** boot-load the device's city; the `initialSearchDone` flag now moves *after* the null check).

- **[DELTA]**
  - **`web/cuisine/src/v2/lib/search-location.js`** (NEW) · **`__tests__/search-location.test.js`** (NEW) · **`web/cuisine/src/v2/App.jsx`** (runSearch + runInitialLoad centre).
  - **`package.json`** — `0.61.372 → 0.61.373`. **`doc/.serial-state.yml`** — commit 585 → 586, journal 572 → 573, build 370 → 371; re-anchored 18:45 SGT.

- **[VERIFICATION (sandbox)]** `npm run build` (cuisine) clean; full `npx vitest run` → **106 files / 3029 tests green** (+12).

- **[STATUS]** PR-**OPEN**, **auto-merge to arm on the operator's word**. Cuisine TMA. Field test: in OTHER mode, the search centre is the picked city (never your physical SG location); SG mode still uses the device pin.

- **[TEST]** Operator: an "Others" search can no longer fall back to your Singapore location — it uses the city you picked, or asks you to pick one.

- **[HONEST SCOPE — what's done vs remaining]**
  - **Done:** the centre/region divergence bug class (the most impactful tangle) is closed by one tested source of truth.
  - **Remaining decompression stages** (flagged, **not** in this PR, to keep blast radius bounded with no live E2E): the mount-resolution precedence (hash / initData / serverCache / GPS), the **three** coherence checks + their `apply*` functions, the 20 s sync, and the splash gate. Each needs its own careful, tested pass.
  - **Not live-E2E tested** — no Telegram WebView here.

- **Confirmation Gates**

  | Gate | Resolution | Date |
  |---|---|---|
  | G1 — version bump | PATCH `0.61.372 → 0.61.373`. | 07-06 '26 |
  | G2 — destructive action | New pure module replaces two inline centre chains; SG behaviour unchanged; OTHER/JB now refuse the device fallback (the fix); **unit-tested** (+12); full suite green. | 07-06 '26 |
  | G3 — new decision rule | The search centre is a single region-coherent source of truth; OTHER/JB never centre on the device GPS. | 07-06 '26 |
  | G4 — paid external API call | None. | 07-06 '26 |

## Amendments

- **`web/cuisine/src/v2/lib/search-location.js`** (NEW) — region-coherent centre resolver.
- **`__tests__/search-location.test.js`** (NEW) — 12 cases.
- **`web/cuisine/src/v2/App.jsx`** — runSearch + runInitialLoad route through `resolveSearchCenter`.
- **`package.json`** — `0.61.372 → 0.61.373`.
- **`doc/.serial-state.yml`** — counters (commit 586 / journal 573 / build 371) + anchor 18:45 SGT 07-06-26.

## Deleted

- The two inline `anchor || searchCenter || locationAnchor || userLoc` centre chains (replaced by the tested `resolveSearchCenter`).
