(№ 171 - 15-05 '26 21:10 SGT) v0.60.197

# Journal — soleat v0.60.197

> Operator: *"DF-87"* — close the carry-forward by populating `sanctuaryRead` on every search-response venue (not just `/api/cuisine/copy-one`). The v0.60.190 T1 "detail-with-sanctuary" Copy-All variant has been emitting the 🌿 block sparsely since v0.60.192; this PR makes it dense.

## New

### [HDR] #164 | 21:10:00 SGT 15-05-26 | v0.60.197 — DF-87 close: populate sanctuaryRead on every search-response venue | 2 files, ~30 LOC net | 1 PR |

- **[INTENT]** Operator picked DF-87 + DF-82 in the outstanding-issues review. DF-82 routed to a zero-code path (operator sets the `VIBE_JOURNAL_KEY` Railway env var; existing v0.60.144 gate at `index.js:8108` engages on next request). This PR addresses DF-87 only: the 🌿 Sanctuary block in the T1 Copy-All variant + the TMA ResultCard's sanctuary section was only populated on the `/api/cuisine/copy-one` endpoint — every other search response left `venue.sanctuaryRead` empty, so the operator's preferred T1 Copy-All format degraded silently to T2 ("no sanctuary"). DF-87 was opened in v0.60.191's plan and carried for 6 PRs.

- **[DELTA]**

  **`index.js`** — one new helper + six call-site wirings (all best-effort try/catch, parallel `Promise.allSettled`):

  *(a) New `enrichSanctuaryRead(venues, lang)` helper* near `enrichPriceRangeDisplay` at line ~290:
  - Skips venues missing `placeId` (e.g. Michelin entries that didn't resolve to Places).
  - Skips venues already carrying a populated `sanctuaryRead` (idempotent — upstream paths can pre-fill).
  - No-ops cleanly when Redis is down (the `getOrCacheSummary` helper requires the cache layer).
  - Per-venue failure caught silently (card just omits the 🌿 block).
  - Reuses the existing `getOrCacheSummary` from `vibe-summary.js` (7-day Redis cache key `vibe:summary:<placeId>:<lang>` — warm venues are ~1 ms, cold venues cost one Gemini call).

  *(b) Six call sites wired*, each next to the existing `enrichPriceRangeDisplay` call so the enrichment cluster stays co-located:
  | Site | Line | Path |
  |---|---|---|
  | Telegram `/s` cuisine search | ~5099 | `handleSearchTurn` → cuisine-chip variant |
  | `/s` fan-out (combo) | ~5619 | `runTechniqueFanOut` |
  | Nation-iconic | ~5740 | `nationOverlay` path |
  | Cooking-method | ~5862 | `cooking-methods` matcher |
  | Michelin TMA + Telegram | ~6543 | `handleMichelinSearch` |
  | Cuisine TMA non-Michelin | ~10515 | `/api/cuisine/search` non-michelin branch |

  Result: every venue that lands in a search response now has a chance to carry `sanctuaryRead`. The T1 Copy-All variant's 🌿 block fills automatically; the TMA ResultCard's sanctuary section renders without a separate `/copy-one` round-trip.

  **`package.json`** — `0.60.196 → 0.60.197` (PATCH; behaviour-additive).

  **`doc/Journal/journal-0_60_197-15_05_26-2110.md`** — this entry (#164).

  **`doc/.serial-state.yml`** — `journal 163 → 164`, `commit 170 → 171`; re-anchored `15-05 '26 21:10 SGT`.

- **[VERIFICATION (sandbox)]**

  ```bash
  $ node --check index.js
  syntax OK
  $ npm test -- --run
   Tests  1794 passed (1794)
  ```

- **[STATUS]** STAGED → PROD on next Railway deploy.

- **[TEST]** Post-deploy:
  1. Cuisine TMA → tap any cuisine chip → ResultCard now shows a 🌿 Sanctuary block on venues that have prior sanctuary data warmed (or on subsequent taps after Gemini fills the cold cache).
  2. ✳️ Michelin chip → every venue card shows the 🌿 block (Michelin entries are the most-summarized, so cache-warm rate is highest).
  3. Telegram `/s pizza` → reply text includes the 🌿 block in T1 sections.
  4. Cold-venue first-tap: card initially missing 🌿, second tap shows it (Gemini call completes on first tap, cached for next).
  5. Railway log per search: no `enrichSanctuaryRead failed` warnings under normal load. The helper is idempotent, so repeat taps stay cheap.

- **[KNOWN GAPS]**
  - **DF-99 NEW** — first-search cold-cache latency: a fresh chat-session searching an unfamiliar cuisine may see ~5 Gemini calls fire serially against `getOrCacheSummary`'s rate-limited path. The helper is best-effort and won't block the response, but cards may show the 🌿 block only on the second tap. Acceptable trade-off; revisit if operator reports first-tap UX feels slow.
  - **DF-94** (Michelin Places cost-watch) carried.
  - **DF-95** / **DF-96** (dead `michelinDedupKey` / unused `michelinPageDepth`) carried.
  - **DF-97** (stale `michelin:enrich:<slug>` cache rotation) carried.
  - **DF-98** (cuisine-aware allow-list) carried.
  - **DF-82** **CLOSED** by operator-side action (set `VIBE_JOURNAL_KEY` Railway env var; no PR needed).
  - **DF-87** **CLOSED** by this PR.
  - DF-84, DF-92, DF-V1–4 carried.

## Confirmation Gates

| Gate | Authority | Stamp |
|---|---|---|
| G1 — version bump | _PATCH 0.60.196 → 0.60.197 (additive enrichment, no contract change)_ | auto |
| G2 — destructive action | _None._ | n/a |
| G3 — new decision rule | _Operator: every search response should carry `sanctuaryRead`, not just `/copy-one`. Future search-response paths (e.g. surprise.js, dessert/drink dispatcher) should adopt the same `enrichSanctuaryRead(...)` call next to their `enrichPriceRangeDisplay`._ | operator |
| G4 — paid external API call | _Yes: cold-cache venues trigger one Gemini call each (via `getOrCacheSummary` → `summarizeVibe`). Cap is ~12 venues × ~5 cold = ~5 Gemini calls per search worst case; 7-day cache TTL keeps recurring searches cheap. Operator-authorised by selecting "All venues" in the DF-87 scoping question._ | operator |

## Amendments

(None. v0.60.196 (#163) is the prior entry.)

## Deleted

(Nothing physically deleted.)
