(№ 791 - 29-06 '26 11:20 SGT) v0.62.437

# Journal — soleat v0.62.437 (Menu TMA — launch Sketchbook tile)

> Operator: "how can I launch Sketchbook — include it in the Menu TMA below the location."

## New

### [HDR] #791 | 11:20 SGT 29-06-26 | v0.62.437 — Sketchbook tile in Menu hub | 2 files | PR #(pending) |
- **`web/menu/src/App.jsx`** — new `sketchbook-app` section placed **after the Location section**;
  a navigate tile → **`/app/clipboard`** (emoji 📋 icon for now — no dedicated Sketchbook PNG yet).
- **`web/menu/src/i18n.js`** — `tile.sketchbook.label` ("Sketchbook") + `.sub` (en/fr; other
  locales fall back to en).
- **[ICON SIZES — answer to the operator]** Menu `Tile` default icon box = 64×64 (`w-16 h-16`),
  image capped ~48px; **Cuisine** overrides to 74×74 box / icon ~74px; **Hawker** 64 box / icon
  ~64px; **Train** 64 box, landscape width-bound. (A future Sketchbook PNG can drop into
  `web/menu/public/` and replace the emoji via `iconImage`.)
- **[VERIFICATION (sandbox)]** `esbuild` parse OK on both menu files; the CI "TMA build" job builds
  the menu TMA as the gate.
- **[STATUS]** PROD — menu TMA rebuild; redeploys per PR.
- **[REMAINING Sketchbook items]** 4 (cuisine filter = Criteria-Search layout), 14 (bot /clipboard).

## Confirmation Gates
| Gate | Status | Note |
|------|--------|------|
| G1 | ✅ | esbuild parse (menu) |
| G2 | ⏳ | CI builds menu TMA |
| G3 | ✅ | serial 790→791 |
| G4 | ⏳ | redeploy |
