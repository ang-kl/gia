(№ 782 - 28-06 '26 23:40 SGT) v0.62.428

# Journal — soleat v0.62.428 (Sketchbook E — Cabinets list: badges, counts, touched)

> Operator: sample parity (Cabinets_view.png). Footer/E from the C–G batch — split out because it
> needs backend per-cabinet counts.

## New

### [HDR] #782 | 23:40 SGT 28-06-26 | v0.62.428 — Cabinets list parity | 5 files | PR #(pending) |
- **[DELTA]**
  - **`clipboard-store.js`** — `listCabinets` now attaches **`drawerCount`** (`lLen` drawer-meta)
    and **`eateryCount`** (sum of `lLen` across drawer lists; placements) per cabinet. Both guarded
    (`.catch(()=>0)`), bounded by caps (12×20×10). Flows to the TMA via `/state`.
  - **`CabinetCard.jsx`** — redesigned from a grid tile to a full-width **list row**: 🗄️ tile ·
    name · **★ DEFAULT / OPEN** badges · "N drawers · M eateries" · "touched … · 1-year TTL" ·
    chevron. `touchedAgo()` derives today / Nd / Nw from `modifiedAt`.
  - **`CabinetGrid.jsx`** — 2-col grid → **vertical list**; "Cabinets" heading + "Trips & events
    you've filed · 1-year TTL on touch" subtitle; passes `defaultCabinetId` + `activeCabinetId`
    (→ DEFAULT/OPEN badges). Sort pills retained.
  - **`App.jsx`** — passes `defaultCabinetId` + `currentCabinetId` to the grid.
  - **`lib/i18n.js`** — `root.cabinetsSub`, `cab.default/open/counts/ttl/touched*` (en/fr/id/ru/de).
- **[VERIFICATION (sandbox)]** `clipboard-store` 74/74 tests green (count fields additive, guarded);
  real `vite build` OK (50 modules).
- **[STATUS]** PROD — server + clipboard TMA rebuild; redeploys per PR.
- **[KNOWN GAPS]** **B** — Clipboard cards → Cuisine `ResultCard` (store structured venue on Copy):
  the last big piece. Header WeatherBadge/LocaleToggle/LocationField reuse also still pending.

## Confirmation Gates

| Gate | Status | Note |
|------|--------|------|
| G1 build/parse | ✅ | node --check + real vite build |
| G2 tests | ✅ | clipboard-store 74/74 |
| G3 journal/serial | ✅ | serial 781→782 |
| G4 live deploy | ⏳ | redeploy + eyeball |
