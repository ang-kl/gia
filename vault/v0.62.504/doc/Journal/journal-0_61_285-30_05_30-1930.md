(№ 486 - 30-05 '26 19:30 SGT) v0.61.285

# Journal — soleat v0.61.285 (NLB-sourced fun-facts modal during search wait)

> Operator (30-05 '26 ~17:55 SGT): *"the idea of Fun-facts JSON is to replace the current wait messages while curating results even in free-text chat. … a food-wiki on the city that the user are searching and the popup in the middle window … should look like floating and fun to read."* — followed by *"can you curate from NLB history for food history in Singapore push for 40 in Singapore"* and then *"1 ok / 2 approve / 3 yes / 4 good"* (sign-off on the curated 40, tagging plan, source-URL in modal, and FR translations).
>
> Phase 1 ships: 40 NLB-sourced facts, a floating modal, selector + anti-repeat, and the wiring to surface a fact 1.5 s into a rotating cuisine search. Chat surface deferred to Phase 2 (Telegram message-edit constraints).
>
> Commit 499 / journal 486 / build 288, on branch
> `claude/v0.61.285-nlb-funfacts-modal` off `main` at v0.61.284.

## New

### [HDR] #486 | 19:30 SGT 30-05-30 | v0.61.285 — NLB fun-facts modal | 7 files | 1 PR | 1 build |

- **[INTENT]** Convert the cuisine-search wait window from "still loading…" anxiety into a NLB-sourced food-history micro-read. Zero added latency (modal fires in parallel with the existing search), curated by hand from NLB Infopedia + curiocity digital stories + BiblioAsia.

- **[DELTA]**

  **1. `web/cuisine/src/v2/data/fun-facts.json` (NEW, ~16 KB).**
  40 entries, each `{id, tags[], en, fr, source, sourceUrl}`. Tagging plan (operator-approved):
  - Facts 1–10: cuisine-keyed (`hainanese-chicken-rice`, `laksa`, `nasi-lemak`, `satay`, `roti-prata`, `teh-tarik`, `kaya`, `hainanese-curry-rice`, `yong-tau-foo`, `popiah`).
  - Facts 11–22: hawker policy milestones tagged `hawker`, `history`, `policy`, `SG`.
  - Facts 23–32: immigrant-community tags (`hokkien`, `teochew`, `cantonese`, `hockchia`, `malay`, `javanese`, `indian`, `hainanese`, `peranakan`).
  - Facts 33–40: named places + Michelin Labyrinth.
  - Sources: 10 distinct NLB URLs (Infopedia, curiocity, BiblioAsia). Test asserts every `sourceUrl` contains `nlb.gov.sg`.

  **2. `web/cuisine/src/v2/lib/fun-facts.js` (NEW, ~80 lines).**
  - `_pickFact({ctxTags, lastSeen, factsList, rng})` — pure selector exported for tests. Tag-overlap first, fallback to non-seen pool, fallback again to full list.
  - `pickFunFact(ctx)` — public; reads `state.cuisines + state.region + state.countryPref`, threads through `_pickFact`, persists ID to `localStorage['gia.funfact.lastSeen']` (max 10).
  - `factBody(fact, lang)` — EN/FR with EN fallback.
  - `totalFunFacts()`, `clearFunFactHistory()` — utility / future operator-tooling.

  **3. `web/cuisine/src/v2/components/FunFactModal.jsx` (NEW, ~95 lines).**
  Floating centred card (`max-w-[360px]`, `bg-tg-card`, `rounded-2xl`, `shadow-xl`, light `bg-black/30 backdrop-blur-sm` backdrop). 💡 header + body + dot-pulse "Still curating…" + linked "Source: NLB" footer. Min display 3 s (default; configurable via prop) so a fast search can't yank mid-sentence — local `shown` state holds the modal past `visible=false` until the floor elapses.

  **4. `web/cuisine/src/v2/App.jsx`** — two changes:
  - Import `FunFactModal` + `pickFunFact` (lines 9–10 area).
  - New `useState(null)` for `funFact` + a 1.5 s `setTimeout` `useEffect` keyed on `loading + loadingReason + state.cuisines + state.region + state.countryPref`. Picks a fact only when `loadingReason === 'rotating'` (skips `'initial'` boot + `'refresh'` same-criteria reruns).
  - Render `<FunFactModal fact={funFact} visible={loading && !!funFact} />` near the top of the main return, alongside the v0.61.274/v0.61.276 coherence modals.

  **5. `web/cuisine/src/v2/lib/i18n.js`** — three new EN/FR keys:
  - `funfact.header` — "Did you know?" / "Le saviez-vous ?"
  - `funfact.curating` — "Still curating…" / "Recherche en cours…"
  - `funfact.sourceLabel` — "Source" / "Source"

  **6. `__tests__/fun-facts.test.js` (NEW, ~140 lines).**
  - Data contract: 40 facts, all have id+tags+EN+FR+source URL, IDs unique, every URL contains `nlb.gov.sg`.
  - `_pickFact` purity: tag-match first, fallback path, lastSeen exclusion, pool reset when everything seen, deterministic with fixed rng.
  - `factBody`: EN, FR, unknown-lang fallback, null fact.
  - `pickFunFact`: localStorage stub + tag-overlap + persistence + non-repeat across consecutive calls + `clearFunFactHistory` wipe.

  **7. `package.json`** — `0.61.284 → 0.61.285`.

  **8. `doc/.serial-state.yml`** — commit 498→499, build 287→288, journal 485→486; re-anchored 30-05 '26 19:30 SGT.

- **[VERIFICATION (sandbox)]**

  - `node --check fun-facts.js + i18n.js` — clean.
  - `JSON.parse(fun-facts.json)` — clean.
  - `esbuild.transform` (loader: jsx) of `App.jsx` + `FunFactModal.jsx` — clean.
  - Vitest still BLOCKED locally by the recurring iCloud `ERR_INVALID_PACKAGE_CONFIG` (Register O-22). CI will run authoritatively. Expected: `fun-facts.test.js` ~14 assertions, all green.

- **[STATUS]** PR-OPEN on push. Railway redeploys the Cuisine TMA on merge (no server change).

- **[TEST]** Operator (post-merge):
  1. **Modal appears mid-search.** Open Cuisine TMA. Change a filter to force a `rotating` search. After ~1.5 s, the floating "💡 Did you know?" card appears centred with a NLB fact. When results arrive, modal fades (after the 3 s minimum on-screen).
  2. **Modal skipped on fast search.** If the cached search returns in <1.5 s, no modal. (Avoids flash.)
  3. **Modal skipped on boot / refresh.** Initial mount loading doesn't trigger. Tapping 🔍 with unchanged criteria (`'refresh'`) doesn't trigger.
  4. **Tag matching.** Search "laksa" — modal preferentially picks one of the laksa-tagged facts (cap-1 laksa fact in v0.61.285). Search "durian" — picks any SG/hawker fact (no durian-tagged fact in this curation pass; falls through to the SG pool).
  5. **Anti-repeat.** Run 5 consecutive `rotating` searches. The modal should show 5 different facts. Then 10+ — confirm IDs cycle without immediate repeats.
  6. **FR locale.** Switch locale to FR. Modal renders the FR body + "Le saviez-vous ?" header + "Recherche en cours…" footer.
  7. **Source link.** The "Source: NLB Infopedia / NLB curiocity / NLB BiblioAsia" footer is clickable (opens the source URL in a new tab via Telegram's external browser). No styling clutter from the dotted underline.

- **[KNOWN GAPS]**
  - **No durian / fruits tagged fact yet** — the curation focused on classical SG dishes + hawker culture. Durian could be added in a future pass; for now durian searches fall through to the SG pool.
  - **No chat-surface fun-fact** — Phase 2 work. Telegram message-edit doesn't allow rich animation; needs a different approach (e.g. append `💡 [fact]` to the cuisine result-list message).
  - **Modal display doesn't measure reading time per-fact** — uses a flat 3 s minimum. Could tune to ~3 s per 8 words if it ever feels rushed; the curated facts cluster around 25–35 words so the floor handles them all.
  - **Source URL points to NLB even for content drawn from BiblioAsia / curiocity** — all three are nlb.gov.sg domains and the user-facing label says "NLB Infopedia / NLB BiblioAsia / NLB curiocity" verbatim from the JSON `source` field.
  - **JB / OTHER searches still get SG-tagged facts** — that's the Phase 1 scope per operator's "push for 40 in Singapore" wording. A future Phase could add JB / MY / Penang / regional facts.
  - **Telegram WebApp's external-link handling** — clicking the source URL opens in the system browser via `target="_blank"`. Tested behaviour in Telegram iOS/Android is to defer to the in-app browser; no popup-blocker issue expected.
  - **Removed Features / amendments table** — `doc/Feature/cuisines_js.MD` should gain a v0.61.285 entry for the new modal + i18n keys per AU-2. Flagged here.

- **Confirmation Gates**

  | Gate | Resolution | Date |
  |---|---|---|
  | G1 — version bump | PATCH `0.61.284 → 0.61.285`. Net-new optional surface (modal) + supporting data/lib/test. No API contract change. Could justify MINOR given the user-visible feature, but operator's pattern is PATCH for incremental UX work. | 30-05 '26 |
  | G2 — destructive action | _n/a_. All additive. No code paths replaced or deleted. | 30-05 '26 |
  | G3 — new decision rule | YES — *"during the cuisine-search rotating wait window we surface a NLB-sourced food-history micro-read; 1.5 s gate skips fast searches; 3 s floor protects mid-read; localStorage-backed anti-repeat per session"*. Operator approved the 40 facts + tagging + source-link + FR-translation autonomy 30-05 '26 18:55 SGT. | 30-05 '26 |
  | G4 — paid external API call | _n/a_. Curated JSON; no Gemini / Wikipedia / external fetch. | 30-05 '26 |

## Amendments

- **`web/cuisine/src/v2/App.jsx`** — `FunFactModal` + `pickFunFact` imported; `funFact` useState + 1.5 s rotating-only `useEffect`; modal render at the top of the main return.
- **`web/cuisine/src/v2/lib/i18n.js`** — three new keys (`funfact.header`, `funfact.curating`, `funfact.sourceLabel`) at the end of the EN/FR table.

## Deleted

_None._
