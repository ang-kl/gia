(№ 591 - 08-06 '26 13:06 SGT) v0.61.391

# Journal — soleat v0.61.391 (Kill the "mismatch modal over loading" double-state — deterministic countryPref gate)

> Operator (IMG_2333) *"this problem again"*: the **Location-mismatch modal** showing **at the same time** as **"Please wait while loading random eateries…"** — the v0.61.364 double-state, knowingly left after the v0.61.368 hang-revert.
>
> **Left OPEN for review — auto-merge to be armed on the operator's word.** Commit 604 / journal 591 / build 381. web/cuisine. **Revert-ready** (this area's prior hang history).

## New

### [HDR] #591 | 13:06 SGT 08-06-26 | v0.61.391 — countryPref-resolved splash gate | App.jsx | 1 PR | 1 build |

- **[INTENT]** The "Location mismatch" modal and "loading random eateries…" rendered **simultaneously** (+ "Showing 0 results · Loading…"). The v0.61.368 revert left this cosmetic in (a hung TMA was worse) and flagged the fix: *"a deterministic countryPref-loaded gate."*

- **[ROOT (re-traced, `App.jsx`)]** The coherence-mismatch effect (`:880`) bails at `if (!state.countryPref) return;` (`:883`) on the **transient null** while `countryPref` is still loading from the server (`fetchCountryPref` `:837`) → `modalPendingRef` stays false → the splash gate-opener (`:1049`) opens + fires `runInitialLoad` → **then** `countryPref` lands, the check re-runs, detects the mismatch, and the modal pops **over** the already-loading body.

- **[DELTA]** (`web/cuisine/src/v2/App.jsx`) — the **documented** deterministic gate, **not** the reverted v0.61.365 settle timer
  - NEW `countryPrefResolved` state, set `true` the moment `fetchCountryPref` **resolves** (value OR null, even on error via `try/catch`), **plus a 3 s `setTimeout` backstop** so it flips true even on a slow/failed fetch — a **one-shot** flag that can only *unblock* the gate, never stall it. (The reverted settle had a cleanup + churning deps that deadlocked.)
  - The gate-opener gains `if (!countryPrefResolved) return;` + `countryPrefResolved` in its deps → the boot-load **waits** until the coherence check has `countryPref` to compare, so a mismatch sets `modalPendingRef` **before** the gate opens (effects run in declaration order: coherence `:880` before gate `:1049`).
  - **`package.json`** `0.61.390 → 0.61.391`; **`doc/.serial-state.yml`** commit 603 → 604, journal 590 → 591, **build 380 → 381**; re-anchored 13:06 SGT.

- **[BEHAVIOUR]** Mismatch → **modal alone**, no loading underneath. No mismatch → gate opens + loads as before. `countryPref` null/SG → no modal, gate opens. During the wait the splash shows **"Confirming your location…"**, not "loading random eateries".

- **[VERIFICATION (sandbox)]** Cuisine build green. Full suite **113 files / 3088 tests**.

- **[STATUS]** PR-**OPEN**, **auto-merge to arm on the operator's word**. Railway redeploys on merge.

- **[TEST]** Operator: enter the TMA on a country mismatch — you should now see **only** the "Location mismatch" prompt (no "loading random eateries" underneath); answer it, then results load.

- **[KNOWN GAPS / SAFETY]**
  - **Deadlock-safe:** `countryPrefResolved` is guaranteed true within 3 s (fast fetch or backstop), so the gate can never hang — fundamentally different from the v0.61.365/367 settle timer that was reverted.
  - **Revert-ready:** this is the gate area that caused ~6 PRs + a revert this session. If anything hangs, revert this PR immediately.
  - Not unit-tested (a React effect gate in `App.jsx`); not live-E2E tested — needs the operator's eyeball on a real mismatch.

- **Confirmation Gates**

  | Gate | Resolution | Date |
  |---|---|---|
  | G1 — version bump | PATCH `0.61.390 → 0.61.391`. | 08-06 '26 |
  | G2 — destructive action | None — additive gate condition + a resolved flag; no existing path removed. | 08-06 '26 |
  | G3 — new decision rule | The splash gate waits for `countryPref` to resolve (one-shot flag + 3 s backstop) before opening, so a mismatch modal never appears over a loading body. | 08-06 '26 |
  | G4 — paid external API call | None. | 08-06 '26 |

## Amendments

- **`web/cuisine/src/v2/App.jsx`** — `countryPrefResolved` flag + 3 s backstop on the countryPref loader; gate-opener waits on it.
- **`package.json`** — `0.61.390 → 0.61.391`.
- **`doc/.serial-state.yml`** — counters (commit 604 / journal 591 / build 381) + anchor 13:06 SGT 08-06-26.

## Deleted

- _None_ (the v0.61.368 synchronous open is preserved; only a deadlock-safe wait condition is added).
