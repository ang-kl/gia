(№ 556 - 07-06 '26 09:30 SGT) v0.61.356

# Journal — soleat v0.61.356 (Location-sync rollout — wire the 20 s follow-the-device poller into the Menu TMA)

> Operator: *"Go"* — roll the v0.61.355 location-sync poller into the other TMAs. This PR wires **Menu** (the second flag-bearing TMA). Now both Cuisine + Menu auto-correct the flag/location on load.
>
> **Left OPEN for review — auto-merge NOT armed.** Commit 569 / journal 556 / build 355. TMA.

## New

### [HDR] #556 | 09:30 SGT 07-06-26 | v0.61.356 — Menu TMA follows the device on load (shared `location-sync.js`) | 2 code files | 1 PR | 1 build |

- **[INTENT]** Extend the stale-flag fix to the Menu TMA so the flag/location self-corrects whichever TMA the user opens first.

- **[MECHANISM]** `location-sync.js` is copied **verbatim** into `web/menu/src/`. The Menu `App` mount gains an `anchorRef` mirror + a once-on-mount effect that runs `startLocationSync({ current: anchor })`. On a **> 1.5 km** device move it **follows the device**: `setAnchor({ lat, lng, country: coordsToCountry, region })` where `region = SG | JB (MY & lat<1.55) | OTHER` (mirroring the existing `applyCoherenceChoice` derivation, but **automatic** — no coherence prompt) and persists via `POST /api/menu/set-location` `{lat,lng,country,region,initData}`. The 1.5 km gate suppresses jitter.

- **[DELTA]**
  - **`web/menu/src/location-sync.js`** (**NEW**, copy of the cuisine module).
  - **`web/menu/src/App.jsx`** — import + `anchorRef` + the follow-the-device mount effect.
  - **`package.json`** — `0.61.355 → 0.61.356`.
  - **`doc/.serial-state.yml`** — commit 568 → 569, journal 555 → 556, build 354 → 355; re-anchored 09:30 SGT 07-06-26.

- **[VERIFICATION (sandbox)]**
  - `tg()` confirmed callable (matches the existing `const w = tg()` usage). `npm run build` clean (menu bundle compiled); `npx vitest run` → **106 files / 2992 tests green**.
  - **NOT live-E2E tested** (no browser / no real movement).

- **[STATUS]** PR-**OPEN**, **auto-merge NOT armed**. TMA; Railway rebuilds the menu bundle on merge. Field test: open Menu somewhere new → within ~20 s the flag/location corrects.

- **[TEST]** Operator: the Menu TMA now does the same 20 s device-follow as Cuisine — whichever you open first, the flag catches up to where you are.

- **[KNOWN GAPS]**
  - **SG-only TMAs (hawker / transport / oversight)** — no country flag, so the flag-follow is moot; their location already syncs via the shared server `set-location`. Wiring them is optional/low-value — **not done** (noted).
  - **`coordsToCountry` only resolves SG/MY** — the flag-follow is precise for the common **SG ↔ JB ↔ MY** cross-border case; other countries keep `region` via the OTHER picker.
  - **Not live-E2E tested.**
  - A `doc/Feature/` / `doc/Technical/` / `doc/Register/` update + a `vault/v0.61.356/` snapshot are still due.

- **Confirmation Gates**

  | Gate | Resolution | Date |
  |---|---|---|
  | G1 — version bump | PATCH `0.61.355 → 0.61.356`. | 07-06 '26 |
  | G2 — destructive action | Additive — a copied module + one mount effect; 1.5 km guarded; full suite green. | 07-06 '26 |
  | G3 — new decision rule | The Menu TMA follows the device on load (shared `location-sync.js`) — same 20 s / fresher-wins / >1.5 km rule as Cuisine; SG/JB/OTHER region derived + persisted. | 07-06 '26 |
  | G4 — paid external API call | _n/a_. | 07-06 '26 |

## Amendments

- **`web/menu/src/location-sync.js`** (NEW) — shared poller copy.
- **`web/menu/src/App.jsx`** — follow-the-device mount effect.
- **`package.json`** — `0.61.355 → 0.61.356`.
- **`doc/.serial-state.yml`** — counters (commit 569 / journal 556 / build 355) + anchor 09:30 SGT 07-06-26.

## Deleted

- _None._
