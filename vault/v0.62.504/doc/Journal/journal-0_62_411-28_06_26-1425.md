(№ 765 - 28-06 '26 14:25 SGT) v0.62.411

# Journal — soleat v0.62.411 ([ picture ] + pill restyle reach the "Pick local classic" geo plate)

> Operator [IMG_2623]: "Where is the picture selection hyperlink in the local food pick?" — the
> Hainanese Chicken Rice card in the geo plate still showed the OLD blue "Find eateries" pill and
> NO [ picture ]. Bundle was already v0.62.410, so not a cache issue — a missed render site.

## New

### [HDR] #765 | 14:25 SGT 28-06-26 | v0.62.411 — picture/pill fix on the geo plate's two dish cards | 2 files | PR #(pending) |
- **[INTENT]** Make the v0.62.407 dish-card change (font -1, glass bg on the 🔍 chip only,
  [ picture ] → Commons photo source) appear in the "Pick local classic" view too.
- **[ROOT CAUSE]** `ArrivalPlate.jsx` has **four** dish-card render sites across **two** return
  blocks: the cuisine-mode block (headliner card + folio-panel card) and the geo "Local Food
  Classic" block (the main 📜 card + the "More classics" `clCard`). v0.62.407 patched only the
  first two; the geo plate (which "Pick local classic" actually renders) was untouched — hence the
  old blue pill + no [ picture ] in the screenshot.
- **[DELTA]** `web/cuisine/src/v2/components/ArrivalPlate.jsx` — applied the same chip-pill +
  [ picture ] treatment to the two geo-plate sites (~L420 main card, ~L487 clCard). All four sites
  now identical. `package.json` 0.62.410 → 0.62.411.
- **[VERIFICATION (sandbox)]** `esbuild` parse OK; 4 `openDishPicture` call sites confirmed.
  Verified `/tmp/ap_now.jsx` against `origin/main` before editing.
- **[STATUS]** PROD — TMA rebuild (cuisine) in CI; bot redeploys per PR.
- **[TEST]** No new unit test (UI). Live eyeball on the geo plate still the final gate.
- **[KNOWN GAPS]** Non-CJK native script (~240) + Wikipedia image pipeline still pending.
  Note: the folio meal-TABS strip (☕/🍽/🥢/🧁/🥤) is the cuisine-mode view; the geo plate splits
  "More classics" into stacked icon-labelled meal SECTIONS (categoriseClassics) — by design.

## Confirmation Gates

| Gate | Status | Note |
|------|--------|------|
| G1 build/parse | ✅ | esbuild OK |
| G2 tests | ⏳ | CI Vitest + TMA build (auto-merge gate) |
| G3 journal/serial | ✅ | this entry; serial 764→765 |
| G4 live deploy | ⏳ | TMA redeploy; eyeball pending |
