(№ 829 - 03-07 '26 12:17 SGT) v0.62.475

# Journal — soleat v0.62.475 (Cuisine card: status corner-tab no longer overlaps the cuisine banner)

> Operator (IMG_2664 — a red "Fermé" corner tab colliding with the green "Eurasian et
> saveurs voisines" banner): *"also the close strip and flavour nearby strip doesn't look
> nice."* Offered A (both strips full-width, stacked) or B (keep the corner tab, move the
> banner so they don't overlap). Operator chose **B**.

## New

### [HDR] #829 | 12:17 SGT 03-07-26 | v0.62.475 — strips no longer overlap (option B) | 1 file | PR #(pending) |
- **`web/cuisine/src/v2/components/ResultCard.jsx` — status tab vs cuisine banner overlap.**
  Root cause: the status **corner tab** (red "Closed" / pink "Closing", `-mt-2.5 -ml-2.5
  self-start rounded-br-lg`) and the full-width cuisine **"& Nearby Flavours" banner**
  (`-mt-2.5 -mx-2.5 rounded-t-lg`) **both** pulled up by `-mt-2.5`, so the banner rode up
  into the tab — a messy collision, and **red directly touching green** (the operator's
  worst CVD pairing). **Fix (option B):** a new `hasStatusTab` flag — when a status tab is
  drawn, the cuisine banner **drops its top-flush `-mt-2.5` bleed** (and takes `rounded-lg`
  instead of `rounded-t-lg`) so it sits **cleanly beneath** the tab; the tab now floats alone
  in the **top-left corner** over empty space. The **first dish-hint strip** also drops its
  `firstFlush` top-bleed when a status tab is present (same collision class). Net: no overlap,
  red no longer abuts green, corner-tab look retained per the operator's choice.
- **[VERIFICATION (sandbox)]** `esbuild --loader=jsx` parse of the edited `ResultCard.jsx`
  clean (exit 0). `hasStatusTab` wired at 3 sites (definition + banner + dish-hint).
- **[STATUS]** PROD — Cuisine TMA rebuilds on merge. Verify on the live TMA: a closed/closing
  venue that also has a cuisine banner shows the corner tab top-left with the banner sitting
  below it, no overlap, no red-on-green touch.
- **[TEST]** Full vitest via CI (ResultCard is presentational).
- **[KNOWN GAPS]** HK→Bangladesh "location bleed": audit found the search path resolves HK
  correctly on 0.62.474 (Railway logs), every country lookup is key-based (not FR-index-
  shifted), and France is already out of the picker — most likely already fixed by the 473
  picker gate; awaiting operator re-test. One real latent FR bug logged: `scripts/build-
  michelin-meta.mjs` `BBOX` has no FR key, so FR Michelin venues skip the anti-leak geo-guard
  (fix when France is re-enabled). First-load open-now meal-window bias + per-anchor local-
  time display still in design.

## Confirmation Gates
| Gate | Status | Note |
|------|--------|------|
| G1 | ✅ | esbuild jsx parse clean |
| G2 | ⏳ | full vitest via CI |
| G3 | ✅ | serial 828→829, commit 946→947, build 585→586; anchor re-set 12:17 SGT |
| G4 | ⏳ | redeploy + operator eyeball (no overlap; corner tab floats top-left) |
