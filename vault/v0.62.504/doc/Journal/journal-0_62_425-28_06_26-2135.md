(№ 779 - 28-06 '26 21:35 SGT) v0.62.425

# Journal — soleat v0.62.425 (Sketchbook card — strip raw HTML; ResultCard is next)

> Operator: "the copy card should be the same as search result's card; currently it's HTML within
> the card — why?" Why: the clip only stores the copied Telegram message *body* (HTML markup), with
> no structured venue, and VenueCard printed it verbatim.

## New

### [HDR] #779 | 21:35 SGT 28-06-26 | v0.62.425 — strip HTML in the card | 1 file | PR #(pending) |
- **[INTENT]** Immediate fix for the raw `<b>…</b>` showing in the card.
- **[DELTA]** `components/VenueCard.jsx` — `plainText()` strips tags + decodes entities; applied to
  the body, preview, label, and the Copy payload. Clean text now, for ALL existing clips.
- **[VERIFICATION (sandbox)]** Real `vite build` OK (50 modules).
- **[STATUS]** PROD — clipboard TMA rebuild; redeploys per PR.
- **[NEXT — the real fix (B), confirmed]** Store the **structured venue** at Copy
  (`index.js` copy-one ~L13326 already has the `venue` object) → persist in clip-store
  (`venue` JSON field) → render the Sketchbook card with the **Cuisine `ResultCard`** (port its
  deps: tg / api / i18n / dish-category / SocialButtons / countries). Old text-only clips keep the
  cleaned-text card. This is the larger cross-cutting piece.

## Confirmation Gates

| Gate | Status | Note |
|------|--------|------|
| G1 build/parse | ✅ | real vite build (50 modules) |
| G2 tests | ⏳ | CI |
| G3 journal/serial | ✅ | serial 778→779 |
| G4 live deploy | ⏳ | redeploy |
