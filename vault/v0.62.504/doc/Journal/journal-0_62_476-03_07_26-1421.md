(№ 830 - 03-07 '26 14:21 SGT) v0.62.476

# Journal — soleat v0.62.476 (Cuisine card: French cuisine names + banner localiser wiring + redundant-close cleanup + thinner strips)

> Operator (French "Fermé" card screenshot — "Quentin's Bar and Restaurant", green
> "Eurasian et saveurs voisines" banner, pink "Ferme dans 54 min" tab over an
> "Ouvert · Ferme 15h00 · Rouvre 17h30" clock row): *"looks wrong."* Pinned to three
> confirmed defects (operator multi-select): stacked strips too heavy, "Eurasian"
> untranslated inside French text, redundant closing info. Operator chose **keep
> stacked but thinner** for the strips and **all ~60 French cuisine names now**.

## New

### [HDR] #830 | 14:21 SGT 03-07-26 | v0.62.476 — FR cuisine names + banner localiser + close de-dup + thinner strips | 3 files | PR #(pending) |
- **`web/cuisine/src/v2/lib/cuisine-i18n.js` — French names for all cuisine slugs.**
  The overlay carried **zh/ja/es only** (by design, en/fr/id/ru/de fell back to the
  English catalogue label). Added **`fr`** to **all ~60 slugs** — feminine adjective
  form (agree with the implicit "cuisine …"): `eurasian → Eurasienne`,
  `singaporean → Singapourienne`, `japanese → Japonaise`, `french → Française`, etc.
  `cuisineName()` unchanged (still falls back for id/ru/de). Header comment updated.
- **`web/cuisine/src/v2/App.jsx` — banner label now routed through the localiser.**
  Root cause of "Eurasian et saveurs voisines": the `nearbyFlavours` memo built its
  label from `cuisineNameBySlug.get(slug)` **directly** — the raw English name — while
  the folio tab correctly localises via `cuisineName(slug, en, lang)`. Added a
  **`labelOf(slug)`** that localises the label; **`nameOf(slug)` still returns the
  English name for the combo-strip KEY** (ResultCard looks up `nearbyStrips[venue.
  matchedCuisine]` where `matchedCuisine` is server-set English — localising the key
  would break the lookup in non-EN locales). Single-cuisine label + combo-strip labels
  now localise; keys stay English. **Fixes zh/ja/es banners immediately** (they were
  covered in the overlay but never reached) **and French** (new names).
- **`web/cuisine/src/v2/components/ResultCard.jsx` — redundant close + thinner strips.**
  - **(#3 redundant close)** The pink "Closing in N min" tab already states the close,
    so the clock row's "· Closes {t}" was doubled ("Ferme dans 54 min" + "Ferme 15h00").
    When `openNow === true && closingSoonMinutes >= 0`, splice out the **index-1**
    segment of the server-built `Open · Closes {t}[ · Reopens {t}]` label
    (locale-agnostic, by position) → leaves "Ouvert · Rouvre 17h30".
  - **(#1 thinner strips)** Operator chose *keep stacked, thinner*: the red "Closed" tab,
    pink "Closing" tab, and green "& Nearby Flavours" banner drop from `text-[10px]
    py-0.5 mb-1 leading-tight` to **`text-[9px] py-0 mb-0.5 leading-snug`** so the two
    stacked bars read lighter. Stacking (post-#1476 `hasStatusTab`) is unchanged.
- **[VERIFICATION (sandbox)]** `esbuild` parse of `App.jsx` + `ResultCard.jsx` (jsx) and
  `cuisine-i18n.js` (js) all clean (exit 0); `node --check cuisine-i18n.js` clean.
- **[STATUS]** PROD — Cuisine TMA rebuilds on merge. Verify on the live TMA in French: a
  closing-soon Eurasian venue shows "**Eurasienne** et saveurs voisines" (not "Eurasian"),
  the pink countdown tab, and a clock row **without** the duplicate "Ferme {t}"; strips
  read thinner. zh/ja/es banners now show their localised cuisine names too.
- **[TEST]** Full vitest via CI (ResultCard is presentational; App.jsx memo is display
  metadata; cuisine-i18n is a data table + a pure lookup fn).
- **[KNOWN GAPS]** id/ru/de cuisine names still fall back to English (French only this
  batch, per operator's small-batch rule for the others). Combo-strip keys remain English
  by necessity (server `matchedCuisine`). HK→Bangladesh re-test still awaiting operator.
  FR Michelin BBOX geo-guard gap still deferred until France search is re-enabled.

## Confirmation Gates
| Gate | Status | Note |
|------|--------|------|
| G1 | ✅ | esbuild jsx parse clean (App.jsx, ResultCard.jsx) + node --check cuisine-i18n.js |
| G2 | ⏳ | full vitest via CI |
| G3 | ✅ | serial 829→830, commit 947→948, build 586→587; anchor re-set 14:21 SGT |
| G4 | ⏳ | redeploy + operator eyeball (FR "Eurasienne"; no dup close; thinner strips) |
