(№ 805 - 01-07 '26 10:20 SGT) v0.62.451

# Journal — soleat v0.62.451 (Sketchbook — Cuisine TMA grouped picker port, grey-out)

> Operator: "the Sketchbook (Cuisine & Filter, Pick Local Dish) should be identical to Cuisine
> TMA … but you grey out those cuisine or filters not in the eatery cards." (Delivery: cuisine
> panel first, dishes next.)

## New

### [HDR] #805 | 10:20 SGT 01-07-26 | v0.62.451 — grouped cuisine picker + grey-out | 4 files | PR #(pending) |
- **`web/clipboard/src/components/CuisineGroupPicker.jsx`** (NEW) — the Cuisine TMA
  `CuisineDrawer` + `CuisineCategoryDrawer` ported into one self-contained component: the same
  2-col group grid + drill-in modal (with the subtle top-right ✕). Self-contained (no
  cuisine-selection/cuisine-i18n deps); `lang` is a prop; labels come from the server catalogue
  (`cat.label` / `cu.name`); multi-select ≤5. **Grey-out:** groups/cuisines whose slug is NOT in
  `availableSlugs` render `opacity-50 cursor-not-allowed` and ignore taps.
- **`App.jsx`** — fetches **`/api/cuisine/catalogue`** (same-origin GET) for the group structure;
  computes **`availableSlugs`** (catalogue slug/name matched against the saved cards' cuisine
  strings; `+michelin` when any saved venue has a Michelin category). Cuisine filter switched from
  a single string to a **multi-select `cuisineSel` array**; `cardMatches` passes a card whose
  cuisines intersect the selected slugs' names (or, for Michelin, whose venue has a Michelin
  category).
- **`Shell.jsx`** — the "Cuisine & filters" dropdown now renders `CuisineGroupPicker` above the
  existing rating/price/crowd/open/Michelin facets; header chip shows `🍜 {N}` when active.
- **[VERIFICATION (sandbox)]** `esbuild` parse (App, Shell, CuisineGroupPicker); real `vite build`
  OK (51 modules).
- **[STATUS]** PROD — clipboard TMA rebuild on merge.
- **[KNOWN GAPS]** **Pick local classic** (dish grid) is the NEXT PR — mirror the Cuisine TMA
  dish grid, deriving the city from the saved cards, greying dishes not saved. Facet pills are the
  card-representable set (rating/price/crowd/open/Michelin), not the Cuisine TMA New/Halal/Pet
  pills (no such flags in saved-card venue data).

## Confirmation Gates
| Gate | Status | Note |
|------|--------|------|
| G1 | ✅ | vite build 51 modules |
| G2 | ➖ | no unit tests touch this view |
| G3 | ✅ | serial 804→805 |
| G4 | ⏳ | redeploy + operator eyeball |
