(№ 792 - 29-06 '26 11:45 SGT) v0.62.438

# Journal — soleat v0.62.438 (/clipboard → last-5 + Open Sketchbook; icon move)

> Operator: /clipboard should show the last 5 + "Open Sketchbook" (drop the 5 action rows +
> clear-all); and move sketchboard_clip_icon.png into data/icon/.

## New

### [HDR] #792 | 11:45 SGT 29-06-26 | v0.62.438 — /clipboard rework + Sketchbook icon | 5 paths | PR #(pending) |
- **item 14** `index.js` `runClipCommand` (list path) — keeps the last-5 text list but the inline
  keyboard is now just **📋 Open Sketchbook** (web_app → /app/clipboard). Removed the per-clip
  rows (resend/copy/rename/remove) and the **Clear all** button (those actions live in the
  Sketchbook TMA: file / amend / archive / restore).
- **icon move** `sketchboard_clip_icon.png` moved **repo-root → `data/icon/`** (same blob, root
  copy deleted). Also placed in **`web/menu/public/`** and wired as the Menu **Sketchbook tile**
  `iconImage` (max-h-16, like Hawker) — replaces the 📋 emoji.
- `web/menu/src/App.jsx`.
- **[VERIFICATION (sandbox)]** `node --check` index.js OK; `esbuild` parse menu App OK; CI builds
  the menu TMA.
- **[STATUS]** PROD — bot + menu TMA rebuild; redeploys per PR.
- **[REMAINING]** item 4 (cuisine filter = Criteria-Search layout + smaller margins) — the last
  open Sketchbook spec item.

## Confirmation Gates
| Gate | Status | Note |
|------|--------|------|
| G1 | ✅ | node --check + esbuild |
| G2 | ⏳ | CI (menu build) |
| G3 | ✅ | serial 791→792 |
| G4 | ⏳ | redeploy |
