(№ 130 - 05-07 '26 15:00 SGT)

# Register — soleat / Gia4lunch v0.62.502 (catch-up rollup snapshot)

> **Document type:** Register (per `doc/CLAUDE.md` §3; §4.1 — items move between sections, never deleted).
> **Status:** **Catch-up rollup.** Continues `register-0_62_76-14_06_26-1005.md` (№ 129). **Scope:** the
> **v0.62.77 → v0.62.502** arc — **113 journal files** (`journal-0_62_133-…` … `journal-0_62_502-…`),
> ~470 merged PRs (≈ #1030 → #1503, 14-06 → 05-07 '26). This is a **state snapshot**, not a per-PR log:
> granular per-PR detail lives in the arc journals + the VibeCodingRecord. This entry carries the ledger
> forward — resolves/defers the live Open items and records the **major decision arcs** (grouped, not the
> ~470 individual D-numbers).
> **Reflects:** project state at **v0.62.502, 05-07 '26 15:00 SGT** (HEAD `f843ef2`). Operator-facing TMAs:
> Cuisine + Menu + Hawker + Transport + Clipboard + Oversight. Branch protection on `main` unchanged
> (4 required checks; enforce_admins=true; log collector uses the unprotected `railway-logs` branch).

---

## Open Items (live as of v0.62.502)

| # | Item | Owner | Priority | Notes |
| :--- | :--- | :--- | :--- | :--- |
| O-47 | **Stale "Results: N" count on a new search** | agent | low | Carried forward from v0.62.76. `knownTotal` (`App.jsx`) still grows as a monotonic MAX of `cumulativeEnd` (reset only on snapshot/Prev-nav replay). No journal in the v0.62.77→502 arc records an explicit new-search reset. **[UNVERIFIED whether incidentally resolved by later streaming/counter refactors]** — re-confirm against a live 1-result search before closing. |
| O-49 | **`uv: command not found` — external source** | agent + operator | low | Operator: *"settle the github — Failed with non-blocking status code: /bin/sh: uv: command not found"*. Exhaustive READ-ONLY search (05-07): `uv` is **absent** from all 8 workflow YAMLs (disk + `origin/main`), CI run logs, check-run annotations, `package.json`, git hooks, `.husky`, `.claude` settings; no Python/Railway build markers (`pyproject.toml`/`uv.lock`/`nixpacks.toml`/`Dockerfile`). All GitHub checks green. Producer is **outside the repo** (local git tool / a GitHub App step / Railway) — **blocked** pending operator on *where* the message surfaces. |
| O-50 | **`index.js:5739` hard-coded `Asia/Singapore` bot 2-hour forecast line** | agent | low | Flagged across recent journals; not operator-requested. Standing cleanup — the bot forecast row assumes SGT regardless of venue timezone. |
| O-51 | **FR Michelin BBOX geo-guard gap** | agent | medium | `build-michelin-meta.mjs` BBOX has no FR key, so FR venues skip the anti-leak guard. Deferred while France stays gated; **reactivate when France is re-enabled**. |

> **O-48 → CLOSED** (superseded then resolved in the prior arc): the "curated-picks for 97 Durian"
> injection was dropped (operator *"dont hardcode, find why the zero results can appear"*); the durian
> zero-results root fix landed as D-62.79 (`durianSoftRating` on the plain cuisine + the D793 small-pool
> recycle). No hardcoded pick shipped.

---

## Deferred

| # | Item | Reason | Reactivate when |
| :--- | :--- | :--- | :--- |
| DF-1 | **MRT go-live flip — Keppel / Cantonment / Prince Edward Road** | CCL6 stations not yet operational | **12 Jul 2026**: flip `status:"future"`→`"operational"` in `data/mrt-coords.json`, drop `opensYear`/`opensDate`/`opensDateFr`, update `_meta` counts, re-run `scripts/build-station-info.js`. |
| DF-2 | **Dish-explanation city-expansion (Phase 4)** | Operator dropped from the current program (*"Only Phase 1, 2 and 3. DROP Phase 4"*) | Operator re-opens city expansion (JP/KR/VN/TH/MY/ID/PH). |
| DF-3 | **FR Michelin BBOX geo-guard (O-51)** | France gated | France re-enabled. |

---

## Decisions (append-only, grouped arcs — v0.62.77 → v0.62.502)

> Continues from D-62.79 in `register-0_62_76-14_06_26-1005.md`. This arc spans ~470 PRs; per-PR decision
> lines live in the 113 arc journals. Recorded here as **thematic decision groups** with representative
> anchors.

| # | Decision (arc) | Rationale | Representative refs |
|---|---|---|---|
| D-A1 | **Cuisine TMA MVP folio + line-icon system.** White-folio cuisine picker (5 buckets, in-flow folio panels); emoji→line-art icon migration (Phase 1, cobalt line-icons); removed the 131-line `criteriaOpen` architectural duplicate + collapsed the two result-paths. | Operator MVP direction; de-dup the picker; consistent iconography. | journals `-0_62_133…`; recent VCR |
| D-A2 | **Result-card polish.** Corner status folder-tab (Closed/Closing) rendered **outside** the card border; separator-wrap fix; street-name resolve; stop-loading closes pickers; temporal-dead-zone crash fix (`startParamConsumed` / state-ref-before-decl). | Operator visual-harmony bar; TDZ boot crash. | card-polish journals |
| D-A3 | **Locale program 5→8 languages.** en/fr → +id/ru/de → +**zh/ja/es** across Cuisine/Menu/Hawker/Transport/Clipboard; `t() ?? ` fix; ~45 App.jsx ternaries migrated to keys; per-TMA `*_STRINGS` overlays; LocaleToggle (🇨🇳/🇯🇵/🇪🇸); CJK font/wrap check. | Multilingual SG/SE-Asia audience (CLAUDE.md). | tasks #1–#4,#9,#10,#16 |
| D-A4 | **Device-locale precedence (v0.62.501).** `getActiveLocale` now prefers `navigator.language` (device) over Telegram-app `language_code`, keeping supported-or-skip fall-through; `localStorage 'gia.locale'` stays highest. | French phone + English Telegram showed English UI. | `journal-0_62_501` |
| D-A5 | **Dish-explanation backfill → 100% coverage.** Grounded draft+verify pipeline (`nation-overlay-dishnotes.generated.js` + `-i18n` for id/ru/de/zh/ja/es); ~1.6k notes; taxonomy classifier (type/mealTime/dietary/course). Final tail (v0.62.502): 10 grounded notes+translations, **7 fabricated entries pruned** (never invented). iconicDishes 1689→1682; un-noted 17→0. | Locale work sat on ~3% content coverage; fill EN first, honestly. | tasks #8,#9; `journal-0_62_502` |
| D-A6 | **France scaffold + Michelin 2025/26.** Registered FR + 12 cities; Paris+Lyon Michelin sourced; `FR-michelin.js` built + shipped (France stays gated pending BBOX geo-guard, O-51). | Regional expansion groundwork. | tasks #11–#13 |
| D-A7 | **Boot / FAB / strip polish.** Menu boot `anchorLoading` gate (no cache/localStorage — server-Redis SSOT); Back-FAB liquid-glass styling; Nearby-Flavours strip full-bleed; clock row when `openNow` is null. | Operator visual + boot-correctness specs. | tasks #14,#15; FAB journal |
| D-A8 | **Doc-system rhythm held.** Per-PR PATCH bump + Journal + serial + re-anchor sustained across the arc; VibeCodingRecord regenerated (catch-up to ~#1503); journal rebase-and-land for stale PR #1456 (v0.62.500). | Standing doc contract (CLAUDE.md). | `journal-0_62_500`; VCR |

---

## Completed in this arc (v0.62.77 → v0.62.502)

- **Cuisine TMA MVP** — white-folio picker, line-icon Phase 1, `criteriaOpen` de-dup, two-result-path collapse (D-A1).
- **Result-card system** — folder-tab outside border, separator-wrap, street-name, stop-loading, TDZ crash fix (D-A2).
- **Locale 5→8** — id/ru/de then zh/ja/es across all operator TMAs; device-locale precedence (D-A3, D-A4).
- **Dish content to 100%** — grounded backfill + 6-lang translation + taxonomy; honest prune of 7 fabricated dishes (D-A5).
- **France scaffold** — 12 cities + Michelin 2025/26, gated (D-A6).
- **Boot/FAB/strip polish** — Menu boot gate, Back-FAB glass, full-bleed strip, null-openNow clock row (D-A7).
- **Doc system** — VibeCodingRecord regen, PR #1456 rebase-land, per-PR journal/serial rhythm sustained (D-A8).

---

## Amendments

- **AU-1:** First Register snapshot since `register-0_62_76-14_06_26-1005.md` (№ 129). Appends Open items
  O-49 → O-51, Deferred DF-1 → DF-3, and thematic Decisions D-A1 → D-A8; carries O-47 forward
  (`[UNVERIFIED]`); records O-48 CLOSED. Prior ledgers unchanged. This is a **rollup** — it does not
  restate the ~470 per-PR decisions (they live in the 113 arc journals + VCR); it captures **state**.

## Reference

- Prior incremental: `doc/Register/register-0_62_76-14_06_26-1005.md`
- Arc journals: `doc/Journal/journal-0_62_133-17_06_26-1200.md` … `doc/Journal/journal-0_62_502-05_07_26-1415.md` (113 files)
- Recent decision journals: `journal-0_62_500` (PR #1456 rebase), `journal-0_62_501` (device locale), `journal-0_62_502` (dishnote tail)
- VibeCodingRecord: `doc/VibeCodingRecord/` + hosted `public/doc/vibe-journal.html`
- Still due (logged, not in this pass): companion `doc/Technical/` entry for this arc; `vault/v0.62.502/` snapshot.
