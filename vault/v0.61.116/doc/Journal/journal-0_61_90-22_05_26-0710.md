(№ 297 - 22-05 '26 07:10 SGT) v0.61.90 — vault pass

# Journal — soleat v0.61.90 (vault snapshot `vault/v0.61.90/`)

> Operator instruction (22-05 '26): *"vault"* — take a fresh
> `vault/<version>/` reproducible snapshot at the current head.
>
> This entry is commit 297 / journal 290 (no build, no version bump —
> a vault pass copies the existing tree, it does not change code).
> Journal #290 follows #289 (the v0.61.90 feature entry); both overlap
> the deferred PR #579 branch renumber (#282-285 → #288-291) — the
> renumber stays deferred to the operator per `doc/.serial-state.yml`.

## New

### [HDR] #290 | 07:10 SGT 22-05-26 | v0.61.90 — vault snapshot `vault/v0.61.90/` | ~155 MB | 901 files | 1 PR | 0 build |

- **[INTENT]** Capture a frozen, bootable, append-only snapshot of the
  repository at v0.61.90 — rollback safety + reproducibility + audit
  trail (`doc/CLAUDE-FULL.md` §16.1).

- **[DELTA]**
  - **`vault/v0.61.90/`** (new, ~155 MB, 901 files) — a faithful mirror
    of the repo at v0.61.90: `index.js` + ~80 root modules, `web/<app>/
    src`, `scripts/`, `data/`, `data_realtime/`, `geoloc/`, the
    `__tests__/` suite, and the full `doc/` system. Built with `tar`
    (no `rsync` in the sandbox), applying the §16.1 strict exclusions:
    `node_modules/` (root + nested), `.git/`, `vault/`, `dist/` +
    `web/*/dist/`, `public/{cuisine,menu,hawker,transport,oversight}/`
    (Vite build output — gitignored), `*.log`, `.env`,
    `.claude/settings.local.json`, `tmp/`. `.env.example`,
    `public/app.js`, `public/index.html`, `public/doc/` and
    `geoloc/*.geojson` are kept.
  - **`vault/v0.61.90/VAULT_README.md`** (new) — the required §16.1
    sections: headline blurb, captured timestamp, exclusion table,
    file count + size, boot instructions, and the v0.61.77 → v0.61.90
    arc table.
  - **`doc/.serial-state.yml`** — `commit` 296 → 297, `journal` 289 →
    290 (`build` unchanged — no build); `last_anchor_*` re-anchored to
    22-05 '26 07:10 SGT; a v0.61.90-vault comment added.
  - No code file changed; `package.json` is NOT bumped — a vault pass
    snapshots the existing v0.61.90 tree.

- **[VERIFICATION (sandbox)]**

  ```
  find vault/v0.61.90 -type f | wc -l            901 files
  du -sh vault/v0.61.90                          ~155 MB
  exclusion guard (node_modules/.git/vault)      none present
  spot check                                     index.js, package.json,
                                                 doc/, web/<5 TMAs>/src,
                                                 public/{app.js,index.html,doc}
                                                 present; public/<tma>/ absent
  vault/v0.61.90/package.json version            0.61.90
  ```

- **[STATUS]** DRAFT — PR opened on `claude/vault-v0.61.90` (branched
  off `main` at v0.61.90, squash `c9872f3`). The vault is committed
  data; nothing deploys.

- **[TEST]** Boot test (per `VAULT_README.md`): `cp -a vault/v0.61.90
  /tmp/soleat-v0.61.90 && cd … && npm install && npm test` reproduces
  the 1,932-test green suite. Not run in-sandbox (a full `npm install`
  in `/tmp` is outside this pass's scope) — the snapshot is a verbatim
  `tar` copy of the working tree, which already builds + tests green.

- **[KNOWN GAPS]**
  - **Rule V-3** — this snapshot's `vault/v0.61.90/doc/Journal/` does
    NOT contain this self-referential addendum (it documents the vault
    that contains it). Recorded here and in the creating commit message.
  - **Rule V-2** — `vault/v0.61.76/` is **kept**, not superseded: each
    vault is an append-only point-in-time snapshot (Rule V-1). This one
    captures the v0.61.77 → v0.61.90 delta; both coexist.
  - No fresh master document set (Builder / Persona / Feature /
    Technical / Legal / Register) was produced — the operator scoped
    the instruction to the snapshot only, and the master set is recent
    (v0.61.83: Feature #48 / Technical #48 / Register #116).
  - `doc/VibeCodingRecord/` regen still due (carried forward).
  - The v0.61.84-87 journal-number / v0.61.86-.87 version-label
    collisions remain deferred to the operator's renumber pass.

- **Confirmation Gates**

  | Gate | Resolution | Date |
  |------|-----------|------|
  | G1 — version bump | _n/a_ — a vault pass; `package.json` stays 0.61.90 | 22-05 '26 |
  | G2 — destructive action | _n/a_ — purely additive (`vault/v0.61.90/` new); Rule V-1 — no prior vault deleted or rewritten | 22-05 '26 |
  | G3 — new decision rule | _n/a_ — operator instruction, no new rule | 22-05 '26 |
  | G4 — paid external API call | _n/a_ | — |

## Amendments

_None._

## Deleted

_None._
