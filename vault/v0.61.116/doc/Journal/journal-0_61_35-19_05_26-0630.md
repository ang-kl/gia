(№ 246 - 19-05 '26 06:30 SGT) v0.61.35

# Journal — soleat v0.61.34 → v0.61.35 (/privacy + /legal copy reassessment)

> Operator: *"Reassess this privacy message '🔒 Privacy & Data Handling' and 'Legal'
> … Render in FR as well."* — supplying new, shorter EN copy for both. On reassessment
> two substantive removals were flagged and confirmed by the operator: drop the
> 90-day retention disclosure from /privacy, and drop the IMDA AI Governance line
> from /legal. New copy applied verbatim; FR freshly translated.
>
> Sequenced after PR #519 (v0.61.34 / commit 245) — this PR is commit 246 / journal 239.

## New

### [HDR] #239 | 06:30:00 SGT 19-05-26 | v0.61.35 — /privacy + /legal copy rewrite (EN + FR) | ~2 kb | 3 files | 1 PR | 0 build |

- **[INTENT]** Replace the `/privacy` and `/legal` message bodies with the operator's
  shorter, friendlier copy, and provide the matching French translations.

- **[DELTA]**
  - **`i18n.js`** — `privacy.body` rewritten: header `🔒 Privacy & Data` (was
    `Privacy & Data Handling`); four short paragraphs (only-what-is-needed; 24 h
    location memory + a "clipboard / food-travel journal" metaphor for saved places;
    no personal profile / no trackers / no data sale / no cross-bot profiles;
    `/forgetme`). The prior 90-day search/usage retention + hashed-aggregate disclosure
    was **intentionally dropped** (operator-confirmed; the formal Legal record still
    documents the TTLs).
  - **`i18n.js`** — `legal.body` rewritten: header `🔖 Legal & Disclaimer`; "as is"
    + AI / Google Places / SG public data sources; verify-with-venues paragraph with
    emoji spacing normalised (one space after each of 🟢 🕌 🥗 🐾 🏠); SG / Johor Bahru
    scope; not-professional-advice + fullest-extent-of-Singapore-law liability clause;
    LinkedIn takedown contact (kept as a tappable Markdown link); `/privacy` pointer;
    `2026`. The prior "IMDA Model AI Governance Framework is followed" line was
    **intentionally dropped** (operator-confirmed).
  - **`i18n.js`** — fresh FR translations of both bodies, paragraph-for-paragraph
    (formal "vous"; headers `Confidentialité et données` / `Mentions légales et
    avertissement`).
  - **`__tests__/privacy-html.test.js`** — the EN/FR end-to-end assertions retargeted
    to the new copy (`Privacy & Data`, "only keeps what is needed", "does not use
    trackers" / `Confidentialité et données`, "ne conserve que", "traceurs").
  - **`package.json`** — `0.61.34` → `0.61.35` (PATCH).

- **[VERIFICATION (sandbox)]**

  ```bash
  $ node --check i18n.js  → OK
  $ npm test  → 65 files / 1928 tests passed
  ```
  No `web/` change → no TMA build. `/privacy` still renders to its HTML page via
  `renderPrivacyHtml` (header `*bold*` only, no bullets/links in the privacy body).

- **[STATUS]** DRAFT → PR (base `main`, branch `claude/review-gia4lunch-readme-3s798`).

- **[TEST]** Send `/privacy` and `/legal` in EN and after `/language` → FR: each shows
  the rewritten body; the /legal LinkedIn contact is tappable; `/privacy` web page
  (`/privacy?lang=en|fr`) renders the new paragraphs.

- **[KNOWN GAPS]** The formal `doc/Legal/` record (legal-0_60_172-…md, cited in the
  `i18n.js` comment as the source of truth for retention TTLs / hash scheme) is **not**
  updated in this pass — a `doc/Legal/` revision is still due to reconcile it with the
  dropped 90-day disclosure and IMDA line. Map-controls redesign Phases C / D / E remain.

## Amendments

(none)

## Deleted

(none — the removed /privacy and /legal clauses are preserved verbatim in this entry's
`[DELTA]` and in the prior journal/Legal records.)

## Confirmation Gates Satisfied

| Gate | Authority | Stamp |
| :--- | :--- | :--- |
| G1 — version bump (PATCH: 0.61.34 → 0.61.35) | _operator: 0.61.x PATCH sequence_ | 19-05 '26 |
| G2 — destructive action (dropped 2 legal/privacy clauses) | _operator: confirmed both removals via AskUserQuestion_ | 19-05 '26 |
| G3 — new decision rule | _n/a_ | — |
| G4 — paid external API call | _n/a_ | — |
