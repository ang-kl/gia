(№ 307 - 22-05 '26 14:43 SGT) v0.61.100

# Journal — soleat v0.61.100 (/app/map — first parity pass)

> Operator — *"Proceed"* / *"just work to get done"* on the `/app/map`
> overhaul. `/app/map` (`public/app.js`) is the older, non-React map
> page that the bot's "View *N* Train Stations / Compare all *N*
> carpark / Show all *N* bus stop" buttons open.
>
> This entry is commit 307 / journal 300 / no new build (public/app.js
> is served raw — no Vite build), off `main` at v0.61.99 (#592).

## New

### [HDR] #300 | 14:43 SGT 22-05-26 | v0.61.100 — /app/map first pass | ~3 kb | 2 files | 1 PR | 0 builds |

- **[INTENT]** Bring the `/app/map` page toward parity with the React
  TMA maps — first pass covering the tractable operator items.

- **[DELTA]**
  - **`public/app.js`** —
    - The toggle row moves to the top edge (`top:56px` → `top:8px`).
    - New bottom-right zoom-level readout (`buildZoomPin`): a white
      circle showing the integer zoom; tapping it recenters on the
      user's GPS location.
    - The Train Line layer now also draws tappable station markers
      (`renderTrainStations` — a 🚉 pin per operational station,
      viewport-clipped + zoom-gated z≥13 like the bus / carpark
      layers); a tap opens a station info popup (`stationInfoHtml`).
      Previously the layer drew line polylines only, so a station tap
      did nothing. `ensureTrainLayer` now also fetches
      `/api/transport/stations`.
    - Carpark popup names are Proper-Cased (`toTitleCase`) — LTA
      returns them ALL CAPS.
  - **`package.json`** — `0.61.99` → `0.61.100` (PATCH).
  - Covers operator items #4, #5, #6, and the top-edge + zoom-pin
    parts of #7.

- **[VERIFICATION (sandbox)]**

  ```
  node --check public/app.js   clean
  npm test                     1944/1944 passing, 67 files
  ```
  (No Vite build — `public/app.js` is served raw, not a TMA bundle.)

- **[STATUS]** DRAFT — PR #593 on `claude/v0.61.100-appmap-overhaul`
  (off `main` at v0.61.99, `ddf5f37`). On merge deploys via the Railway
  auto-deploy on `main`.

- **[TEST]** `/app/map`: the toggle row sits at the top edge; the zoom
  circle shows the integer zoom + recenters on tap; Train Line draws
  tappable 🚉 station pins; carpark popup names read in Proper Case.

- **[KNOWN GAPS]**
  - **The `⋯` overflow dropdown on `/app/map` is still inert** — the
    remaining part of operator item #7. Wiring it to the parks /
    attractions / clinic / police / hospital layers is a larger port
    of the React overlay controller into plain DOM; tracked as a
    follow-up PR.
  - No in-sandbox visual verification (no Maps webview).
  - `doc/VibeCodingRecord/` regen still due (carried forward).

- **Confirmation Gates**

  | Gate | Resolution | Date |
  |------|-----------|------|
  | G1 — version bump | PATCH `0.61.99` → `0.61.100` | 22-05 '26 |
  | G2 — destructive action | _code-only_ — additive page features; one CSS offset changed; no deletion | 22-05 '26 |
  | G3 — new decision rule | _n/a_ — operator UI spec | 22-05 '26 |
  | G4 — paid external API call | _n/a_ — reuses existing endpoints | 22-05 '26 |

## Amendments

_None._

## Deleted

_None._
