(№ 580 - 08-06 '26 07:25 SGT) v0.61.380

# Journal — soleat v0.61.380 (Global weather — Open-Meteo follows the country expansion)

> Operator: **"build weather"** — make the live weather chip follow the country expansion so a Tokyo / Seoul / Bangkok user gets **their** weather, not Singapore's.
>
> **Left OPEN for review — auto-merge to be armed on the operator's word.** Commit 593 / journal 580 / build 374. Server + web/cuisine.

## New

### [HDR] #580 | 07:25 SGT 08-06-26 | v0.61.380 — global weather via Open-Meteo | weather.js · index.js · WeatherBadge · App | 1 PR | 1 build |

- **[INTENT]** gia's live weather was **Singapore-only**. `weather.js` reads NEA / `data.gov.sg` (nearest NEA station), and the TMA `/api/weather/summary` was **hardcoded** to `weather.summary(1.3521, 103.8198)` under one global cache key `weather:tma-summary` — so a user anywhere abroad got **Singapore's** reading. NEA cannot cover anywhere outside SG. Operator: make weather follow the country expansion.

- **[SOURCE CHOICE]** **Open-Meteo** for non-SG — free, **no API key**, global, returns WMO weather codes; fits the cost-hygiene posture. **NEA stays the SG source** (no regression for the home market).

- **[DELTA]**
  - **`weather.js`** — added `OPEN_METEO_BASE`; `wmoToText(code)` (0=Clear … 95–99=Thunderstorm, **bounded** `95<=c<=99` so an out-of-range `999 → null`); `wmoToEmoji(code)` (☀️ ⛅ ☁️ 🌫️ 🌦️ 🌧️ 🌨️ ⛈️, else 🌡️); `fetchOpenMeteo(lat,lng,_getFn=axios.get)` (`current=temperature_2m,relative_humidity_2m,weather_code&timezone=auto`); `summaryGlobal(lat,lng,_getFn)` → `{ tempC, humidityPct, condition, emoji, source:'open-meteo' }` **or `null`** (no `current` block / null temp / network error all collapse to `null` so the chip hides rather than blanks). Exports extended: `summaryGlobal, fetchOpenMeteo, wmoToText, wmoToEmoji`.
  - **`index.js`** (`/api/weather/summary`) — rewritten from the fixed SG call to read `req.query.lat/lng`; `useSG = !hasCoords || weather.inSgBounds(qLat,qLng)` → **SG path** `weather.summary` (NEA), **else** `weather.summaryGlobal` (Open-Meteo). Per-grid cache key `weather:tma-summary:${lat.toFixed(1)}:${lng.toFixed(1)}` (TTL 300, **only** cached when `payload.ok`). Both paths add a `source` field.
  - **`web/cuisine/src/v2/components/WeatherBadge.jsx`** — signature gains `lat`/`lng` props; builds `?lat=&lng=` **only when both finite**; re-fetches on `[lat,lng]`.
  - **`web/cuisine/src/v2/App.jsx`** — passes `locationAnchor ?? searchCenter ?? userLoc` coords to the badge, so the chip tracks the **picked** place.
  - **`__tests__/weather-global.test.js`** (NEW) — 8 tests via `createRequire` + a mocked `_getFn` (**no live network**): `wmoToText` / `wmoToEmoji` incl. the 95–99 bound (`999 → null` / 🌡️); `summaryGlobal` Tokyo shape (18.4 °C / 63 % / Partly Cloudy / ⛅ / `open-meteo`) + no-current / network-error / null-temp → `null`.
  - **`package.json`** — `0.61.379 → 0.61.380`. **`doc/.serial-state.yml`** — commit 592 → 593, journal 579 → 580, **build 373 → 374** (web/cuisine rebuilt); re-anchored 07:25 SGT 08-06.

- **[VERIFICATION (sandbox)]** `node --check index.js weather.js` clean. `web/cuisine` build green. Full suite **111 files / 3063 tests green** (+8). **Live-verified** Open-Meteo returns real readings: **Tokyo 18.9 °C ⛅ · Seoul 18.9 °C ☀️ · Bangkok 28 °C ☁️**.

- **[STATUS]** PR-**OPEN**, **auto-merge to arm on the operator's word**. Railway redeploys on merge (rebuilds `public/cuisine/` server-side — `public/` is gitignored).

- **[TEST]** Operator: open the Cuisine TMA in / anchored to an overseas city — the header weather chip now shows **that city's** live temperature + condition (Open-Meteo), while a Singapore anchor still shows the NEA reading.

- **[KNOWN GAPS]**
  - Only the **cuisine `WeatherBadge` header chip** was globalized. The **Menu Weather TILE** stays `SG_ONLY_TILES`-gated — a separate follow-up if the operator wants the tile global too.
  - Open-Meteo current-conditions only (no forecast / no humidity surfaced in the chip yet — `humidityPct` is returned but unused by the badge).
  - Not live-E2E tested inside the Telegram WebView (verified via direct Open-Meteo calls + unit mocks).

- **Confirmation Gates**

  | Gate | Resolution | Date |
  |---|---|---|
  | G1 — version bump | PATCH `0.61.379 → 0.61.380`. | 08-06 '26 |
  | G2 — destructive action | None — additive (new fns + new endpoint routing + new test); NEA SG path unchanged. | 08-06 '26 |
  | G3 — new decision rule | Non-SG weather sourced from **Open-Meteo** (free / no-key / global); NEA reserved for SG. | 08-06 '26 |
  | G4 — paid external API call | None — Open-Meteo is free + keyless; live verification used its public endpoint. | 08-06 '26 |

## Amendments

- **`weather.js`** — Open-Meteo client + WMO→text/emoji + `summaryGlobal` (additive; exports extended).
- **`index.js`** — `/api/weather/summary` routes SG→NEA / else→Open-Meteo, per-grid cache.
- **`web/cuisine/src/v2/components/WeatherBadge.jsx`** — `lat`/`lng` props, location-aware fetch.
- **`web/cuisine/src/v2/App.jsx`** — passes anchor/search/device coords to the badge.
- **`__tests__/weather-global.test.js`** (NEW) — 8 mocked-network tests.
- **`package.json`** — `0.61.379 → 0.61.380`.
- **`doc/.serial-state.yml`** — counters (commit 593 / journal 580 / build 374) + anchor 07:25 SGT 08-06-26.

## Deleted

- _None_ (additive; the SG NEA path is preserved unchanged).
