(№ 196 - 16-05 '26 15:15 SGT) v0.60.221

# Journal — soleat v0.60.221

> Operator multi-area request — area A, item 1: *"Remove the one line spacing above the 🌿 lines"* in the `/s` / Cuisine-TMA copy / copy-to / free-text venue cards.

## New

### [HDR] #189 | 15:15:00 SGT 16-05-26 | v0.60.221 — drop the blank line above the 🌿 Sanctuary block | 1 file | 1 PR |

- **[INTENT]** The Sanctuary read block (`🌿 Quiet:` / `🌿 Seating:`) in the T1 "detail-with-sanctuary" venue card was preceded by a blank line, leaving a one-line gap above it. Operator wants the block flush with the row above.

- **[DELTA]** `venue-templates.js` — `formatVenueBlock`'s sanctuary branch dropped its leading `lines.push('')`. The `🌿` block now renders immediately after the preceding row (stats / order / footfall / etc.) with no gap. This is the single render site for the `🌿` block across every surface the operator named — Cuisine TMA Copy / Copy-to (the T1 `formatVenueBlock` template) and the free-text `deliverPicks` cards. (`formatTechniqueVenueBlock`, the `/s` card renderer, does not emit a sanctuary block, so there is no `/s`-side gap to remove.)

- **[VERIFICATION (sandbox)]**

  ```bash
  $ node --check venue-templates.js   → OK
  $ npm test -- --run                 → 1889 passed (1889)
  ```

- **[STATUS]** STAGED → PROD on next Railway deploy. Server-only (`venue-templates.js`) — no `web/` build.

- **[TEST]** Post-deploy: a venue card with a Sanctuary read (Cuisine Copy / Copy-to, free-text picks) → the `🌿 Quiet:` / `🌿 Seating:` lines sit directly under the row above, no blank line.

- **[KNOWN GAPS]** Remaining operator areas: D (map pins — 4a Cuisine white pin background, 4b Hawker custom tiny pins), E (Train TMA polylines 5a-5d, then the 178-station exit dataset 5e).

## Confirmation Gates

| Gate | Authority | Stamp |
|---|---|---|
| G1 — version bump | _PATCH 0.60.220 → 0.60.221._ | auto |
| G2 — destructive action | _None._ | n/a |
| G3 — new decision rule | _Operator: the 🌿 Sanctuary block renders flush with the row above (no leading blank line)._ | operator |
| G4 — paid external API call | _None._ | n/a |

## Amendments

(None. v0.60.220 (#188) is the prior entry.)

## Deleted

(Nothing deleted — a single `lines.push('')` spacing call was removed from `formatVenueBlock`; the change is described in the New section above and in the in-file v0.60.221 comment.)
