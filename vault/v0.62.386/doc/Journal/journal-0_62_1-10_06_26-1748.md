(№ 645 - 10-06 '26 17:48 SGT) v0.62.1

# Journal — soleat v0.62.1 (Michelin metadata snapshot populated: `michelin-meta.json` {} → 1,888 entries)

> Operator ran the refresh job (`npm run michelin:refresh`) with the shared `GOOGLE_MAPS_API_KEY`
> after switching keys mid-task (*"can i use gia-web google map id instead which is free now"* — it
> turned out to be the **same** key/project; validated with one free `searchText` call before the
> full run). This commits the populated snapshot, **closing the v0.62.0 / PR #960 KNOWN GAP** that
> shipped `michelin-meta.json` empty (`{}`).

> Commit 661 / journal 645 / **build 414 (HELD — server-only data file, no `web/` change)**.
> michelin-meta.json only. Data regen, no code change. Revert-ready.

## New

### [HDR] #645 | 17:48 SGT 10-06-26 | v0.62.1 — populated `michelin-meta.json` from the offline refresh (1,888/1,922 resolved) | michelin-meta.json + package.json + serial-state | 1 PR | 0 builds |

- **[INTENT]** v0.62.0 (#960) shipped the refresh job + the by-placeId Michelin resolution path but
  with an **empty** `michelin-meta.json` (`{}`) — so the handler stayed on the live text-search
  fallback (name+city, ambiguous) and the populate was logged as *still-due* in #960's `[KNOWN GAPS]`.
  Operator: ship the populated snapshot now (chose **"1 — ship it"**).

- **[DELTA]**
  - **`michelin-meta.json`** — `{}` (0 entries) → **1,888 entries, every one carrying a `placeId`**
    (1.4 MB; minified single-line, matching the #960 format). Produced by
    `GOOGLE_MAPS_API_KEY=<shared> npm run michelin:refresh` → `scripts/build-michelin-meta.mjs`,
    which resolved each curated venue against Places **once** (offline) for
    placeId/lat-lng/rating/price/hours/accessibility/Maps-URL/website + social URLs.
    **Resolved 1,888 / 1,922; 34 unresolved** (those simply absent from the snapshot — the handler
    falls back to live text-search for them, per the script's one-directional design).
  - **`package.json`** `0.62.0 → 0.62.1` (PATCH — data regen, no behaviour/code change).
  - **`doc/.serial-state.yml`** — commit 660 → 661, journal 644 → 645, **build 414 (held)**;
    re-anchored to 17:48 SGT 10-06-26 (also corrected the prior 16:39/11:23 `iso8601`/`utc` drift to
    a single consistent anchor); `last_anchor_source` prepended (chained `PRIOR:`).

- **[VERIFICATION (sandbox)]** Key validated first with a single live `searchText` (HTTP 200, Les
  Amis resolved) — confirms Places API (New) enabled + key not referer-restricted (server-side OK),
  *before* spending the full batch. `michelin-meta.json` parses as JSON with **1,888** keys, **1,888**
  carrying `placeId`. `node --check` n/a (no `.js` touched). No `web/` change → no build.

- **[STATUS]** PR-**OPEN**, **auto-merge armed (squash)**. Server-only: Railway redeploys the server
  on merge and the Michelin handler immediately begins resolving curated venues **by placeId**
  (reliable, no text-search ambiguity) with the restored 24 h `michelin:place:<id>` cache. No client
  bundle change.

- **[TEST — operator, deployed]** Post-merge, KL / SG / JB Michelin taps resolve via the snapshot
  (by-ID) instead of the `{}`-fallback text-search — faster warm taps, correct venue matches, no
  bib-gourmand/star mis-resolution from name+city ambiguity. A curated venue in the 34 unresolved
  still appears via the live text-search fallback.

- **[KNOWN GAPS]**
  - **34 unresolved** curated venues are not in the snapshot (name/closure/relocation drift); they
    use the live text-search fallback. A targeted re-run or a curated-id fix could reclaim them —
    deferred; the script does not currently log *which* 34.
  - Refresh cadence is **manual / ~monthly** (ratings/price/hours drift slowly) — `fetchedAt` stamps
    each record; no auto-refresh scheduled.
  - The 1.4 MB minified single-line file makes the PR diff one large line (the #960 format; not
    re-flowed).

- **Confirmation Gates**

  | Gate | Resolution | Date |
  |---|---|---|
  | G1 — version bump | PATCH `0.62.0 → 0.62.1` (data regen, no code change). | 10-06 '26 |
  | G2 — destructive action | Snapshot `{}` → 1,888 entries is purely additive to the lookup; the text-search fallback is retained for absent records. Reversible (revert the file). | 10-06 '26 |
  | G3 — new decision rule | None — executes the v0.62.0/#960 design (snapshot-by-id). | 10-06 '26 |
  | G4 — paid external API call | The refresh IS the paid call — 1 `searchText` (+ optional social) per curated venue, **offline / one-off**, on the shared key the operator authorised; NOT per user-search. ~1,922 calls. | 10-06 '26 |

## Amendments

- **`michelin-meta.json`** — `{}` → 1,888 placeId-keyed entries (from `npm run michelin:refresh`).
- **`package.json`** — `0.62.0 → 0.62.1`.
- **`doc/.serial-state.yml`** — counters (commit 661 / journal 645 / build 414 held) + anchor 17:48
  SGT 10-06-26 (+ iso8601/utc drift corrected) + `last_anchor_source` prepend.

## Deleted

- The empty (`{}`) `michelin-meta.json` placeholder state — superseded by the populated snapshot.
  The text-search fallback path it implied is retained for the 34 unresolved records.
