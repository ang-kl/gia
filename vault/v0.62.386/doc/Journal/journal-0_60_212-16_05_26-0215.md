(№ 187 - 16-05 '26 02:15 SGT) v0.60.212

# Journal — soleat v0.60.212

> Operator: *"Complete the rest of the DF."* After the v0.60.210 rescan + the v0.60.211 DF-110 fix, five DF gaps remained: DF-84, DF-100, DF-101, DF-103, DF-106. They are **not uniform** — one is a small code task, one a UX choice, one a large migration, two are non-code (external data / infra). Scoped each, then put three to the operator via AskUserQuestion. All five resolve **by decision, with no code change**. This entry records the closures.

## New

### [HDR] #180 | 02:15:00 SGT 16-05-26 | v0.60.212 — DF backlog closure (DF-84 / 100 / 101 / 103 / 106) | docs-only | 1 PR |

- **[INTENT]** Close out the residual DF backlog with explicit, recorded decisions so nothing lingers as an untracked "carried" gap.

- **[DELTA]** No source files changed. Documentation only — `package.json` PATCH bump (`0.60.211 → 0.60.212`) to keep the journal-file version naming consistent, `doc/.serial-state.yml` (`journal 179→180`, `commit 186→187`, re-anchored), and this Journal entry.

- **DF resolutions (operator decisions — Confirmation Gate G3):**

  | DF | Gap | Scoping finding | Decision | Status |
  |---|---|---|---|---|
  | **DF-84** | Migrate `doc/VibeCodingRecord/` into the standalone `.vibe-journal/` framework | Concrete but **~500–800 LOC** (a new `.vibe-journal/lib/parsers/` module folding in the rework/churn/failure-mode analytics + retiring `generate.mjs`). `.vibe-journal/` is a working v0.1.0 package; `VibeCodingRecord/generate.mjs` works fine standalone. | **Dropped.** Accept the two as separate tools; no migration. The HTML-emission redundancy sub-issue was already resolved (generate.mjs no longer writes `.html`). | CLOSED — won't do |
  | **DF-100** | Bib-gourmand slice shuffled per Michelin tap (`index.js` ~6213, Fisher-Yates) | Deliberate variety feature — *"each click surfaces a different slice without re-paying the full Places bill."* Making it deterministic = ~4 lines but every tap would then show the identical bib slice (a UX regression). | **Keep the shuffle.** Closed as not-a-bug. | CLOSED — not a bug |
  | **DF-101** | Michelin walk auto-recycle — an exhausted curated pool keeps returning empty until the seen-set resets (1h idle / filter change) | Implementable in ~7 lines (a `resetWalkState` export in `michelin-walk.js` + one call after `recordWalk`). Behaviour change: exhausted → next tap restarts at venue 1. | **Leave as-is.** The seen-set self-resets on 1h idle or any filter/price/radius change; auto-recycle not wanted. | CLOSED — leave as-is |
  | **DF-103** | `♿️ accessibilityOptions` sparse on hawker stalls / older venues | Google Places API data coverage — not addressable in code; improves only as Google enriches their data. | **Closed as external.** No code action possible. | CLOSED — external |
  | **DF-106** | Railway dormancy cold-start (3–8 s after ~30 min idle) | Infrastructure — addressable only via Railway config (a paid tier feature). Not a code issue. | **Closed as infra.** Operator-side Railway decision if ever wanted. | CLOSED — infra |

- **[VERIFICATION (sandbox)]**

  ```bash
  $ node --check  …            → n/a (no source files changed)
  $ npm test -- --run          → 1865 passed (1865)   # unchanged — confirms the docs-only change touched no code
  ```

- **[STATUS]** DRAFT → docs-only PR. No deploy impact (no source change).

- **[TEST]** None — documentation only.

- **[KNOWN GAPS]**
  - **DF backlog — now empty.** DF-84/100/101/103/106 closed here; DF-109/110/111 closed in v0.60.210–211; older DF-### resolved earlier. No carried DF gap remains.
  - **Register catch-up still due** — `doc/Register/` was last refreshed at `register-0_60_172` (15-05-26). The DF items logged in Journals across v0.60.173 → v0.60.212 (including these five closures) are **not yet reflected in a Register file**. Per `CLAUDE-FULL` §17.3 (doc-system catch-up cadence) a fresh `register-0_60_212-*.md` should fold in the Open/Decisions/Deferred/Completed deltas. Not done here — it is a separate, larger doc-maintenance pass. Logged so it is not lost.

## Confirmation Gates

| Gate | Authority | Stamp |
|---|---|---|
| G1 — version bump | _PATCH 0.60.211 → 0.60.212 (docs-only; keeps journal-file naming consistent)._ | auto |
| G2 — destructive action | _None. DF-84 is "dropped" as a backlog decision — nothing deleted; `doc/VibeCodingRecord/` and `.vibe-journal/` both remain in place._ | n/a |
| G3 — new decision rule | _Operator (via AskUserQuestion): DF-84 dropped (no migration); DF-100 shuffle kept (not a bug); DF-101 left as-is (no auto-recycle); DF-103 / DF-106 closed as external / infra._ | operator |
| G4 — paid external API call | _None._ | n/a |

## Amendments

(None. v0.60.211 (#179) is the prior entry.)

## Deleted

(Nothing deleted. DF-84 "dropped" is a decision recorded above — no files were removed.)
