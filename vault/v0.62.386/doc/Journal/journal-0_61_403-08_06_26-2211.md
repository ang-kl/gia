(№ 603 - 08-06 '26 22:11 SGT) v0.61.403

# Journal — soleat v0.61.403 (Bot TMA first-load: progressive one-card streaming + bottom toast, parity with gia-web v0.1.151)

> Operator: **"1"** — port the gia-web v0.1.151 first-load treatment to the bot Cuisine TMA.
>
> Commit 616 / journal 603 / **build 387**. web/cuisine. **Revert-ready** (ResultPanel-only; the hang-prone gate is untouched).

## New

### [HDR] #603 | 22:11 SGT 08-06-26 | v0.61.403 — stream the first impression + bottom toast (ResultPanel only) | ResultPanel.jsx | 1 PR | 1 build |

- **[INTENT]** Bring the gia-web v0.1.151 first-load felt-experience to the bot TMA: cards stream one-by-one, "more coming" cue, unobtrusive bottom loading toast.

- **[KEY FINDING — what NOT to touch]** The bot TMA **already satisfies brief #1/#2** (no raw-first-paint fetch; deferred to **location confirmed**): the `App.jsx` **location gate** fires `runInitialLoad()` only once `userLoc` resolves + no modal is pending (`~1117-1126`). That is the **exact hang-prone area reverted twice this session** (v0.61.365/367 settle-timer, v0.61.391 gate) — its own comment warns *"a hung TMA is far worse."* So the **gate / trigger was left untouched**. Also: the bot's batch search **legitimately runs 12–16 s** (`cuisine-search-exit ms`), so a gia-web-style **10 s abort would break normal searches** → **no search-timeout** added.

- **[DELTA]** (`web/cuisine/src/v2/components/ResultPanel.jsx` — presentation only)
  - **Progressive reveal** — `revealCount` walks `0→N` once the batch lands (first card immediate, each next ~220 ms). **First batch only** (paging / re-search render in full). `cardsToShow = firstBatch ? pagedVenues.slice(0, revealCount) : pagedVenues`; resets via a `placeId`-key ref on each new batch.
  - **"loading more…" cue** — subtle inline line while `streamingMore`.
  - **Bottom toast** — the inline *"Loading…"* block is now a **narrow, bottom-anchored, non-blocking toast** (`pointer-events-none` wrapper); self-dismisses when the first card paints (`loading` → false).
  - `App.jsx`, the location gate, and `searchCuisine` are **untouched**.
  - **`package.json`** `0.61.402 → 0.61.403`; **`doc/.serial-state.yml`** commit 615 → 616, journal 602 → 603, **build 386 → 387**; re-anchored 22:11 SGT.

- **[VERIFICATION (sandbox)]** Cuisine build green. Full suite **113 files / 3107 tests**.

- **[STATUS]** PR-**OPEN**, **auto-merge armed (squash)**. Railway redeploys on merge.

- **[TEST]** Operator: open the Cuisine TMA → after the location settles, the 5 cards **stream in one-by-one** (not all at once) with a small **bottom toast** during the wait + a quiet *"loading more…"* line as they fill. Paging / re-search render instantly (no stagger).

- **[KNOWN GAPS / SAFETY]**
  - **Contained to ResultPanel** — does **not** touch the boot load / location gate (the v0.61.391 hang area). Revert = revert this one file.
  - The bot keeps its **existing** location-gate trigger (the gia-web "suppress auto-load + GPS-confirm" was unnecessary here — the gate already defers to location).
  - The batch search still takes 12–16 s; streaming improves **perceived** progress once the batch lands, not the batch latency itself.
  - Not unit-tested (presentational React).

- **Confirmation Gates**

  | Gate | Resolution | Date |
  |---|---|---|
  | G1 — version bump | PATCH `0.61.402 → 0.61.403`. | 08-06 '26 |
  | G2 — destructive action | Additive — reveal slice + cue + toast; the gate + search are untouched. | 08-06 '26 |
  | G3 — new decision rule | Bot TMA first impression **streams one-by-one** with a bottom toast (parity with gia-web v0.1.151); the location-gate trigger is unchanged. | 08-06 '26 |
  | G4 — paid external API call | None new. | 08-06 '26 |

## Amendments

- **`web/cuisine/src/v2/components/ResultPanel.jsx`** — progressive reveal + "loading more…" cue + bottom-anchored loading toast.
- **`package.json`** — `0.61.402 → 0.61.403`.
- **`doc/.serial-state.yml`** — counters (commit 616 / journal 603 / build 387) + anchor 22:11 SGT 08-06-26.

## Deleted

- _None_ (the inline "Loading…" block became the bottom toast).
