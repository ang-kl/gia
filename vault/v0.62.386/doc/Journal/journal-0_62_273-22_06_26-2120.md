(№ 752 - 22-06 '26 21:20 SGT) v0.62.273

# Journal — soleat v0.62.268 → v0.62.273 (Menu/Cuisine/map UI polish arc: bigger Cuisine icon · tighter dock · Reset-as-zoom-readout · vertical ⋮ · thin scrollbar · Hawker pill fit)

> Operator: a rapid run of UI polish across the Menu hub, the Cuisine TMA and the
> shared map controls (six PRs, #1235–#1240). Built + shipped via the GitHub
> Contents API because the operator's iCloud `.git` was lock-jammed by the
> auto-commit tool (no local build this arc — every change is surgical CSS / text /
> small JSX). One ARC entry, one line per PR (non-inventive, from the PR set).
> Commit 812 / journal 752 / build 467.

## New

### [HDR] #752 | 21:20 SGT 22-06-26 | v0.62.268→v0.62.273 — 6-PR Menu/Cuisine/map UI arc (#1235–#1240) | ~13 files | 6 PRs | 6 builds |

- **[INTENT]** Operator polish pass after the v0.62.267 Menu/Cuisine batch: enlarge the Cuisine hub icon, tighten the Cuisine command dock, fold the map zoom readout into the Reset button, force a vertical overflow glyph, make the page scrollbar unobtrusive, and stop the Hawker map-layer pill from being scrolled off-screen.

- **[DELTA]** (one line per PR)
  - **#1235 · v0.62.268** — Menu **Cuisine tile icon +15%** (64→74px), isolated via a new per-tile `boxClass` prop on `Tile.jsx` so only Cuisine grows (Hawker/Train unchanged). `web/menu/src/{components/Tile.jsx,App.jsx}`.
  - **#1236 · v0.62.269** — **Cuisine dock control row pulled closer to the footer**: dock `gap-1.5→gap-1`, control buttons `py-2→py-1.5`. `web/cuisine/src/v2/App.jsx`.
  - **#1237 · v0.62.270** — **Reset button doubles as the live zoom readout** (square `rounded-md`, shows the zoom number, tap still resets; same 28px size); the separate **🔭 readout removed** from all 3 map TMAs; **Cuisine ↹ now GPS-recentres** (the old 🔭 action), disabled when no fix. `web/cuisine/.../MapPanel.jsx`, `web/hawker/.../HawkerMapPanel.jsx`, `web/transport/.../MrtMapPanel.jsx`.
  - **#1238 · v0.62.271** — map layers menu glyph is **always the vertical ⋮** (dropped the platform `overflowGlyph()` that showed ⋯ on iOS/macOS); button stays a 28px rounded rectangle. All 3 shared `MapControls.jsx` copies.
  - **#1239 · v0.62.272** — **thin, unobtrusive page scrollbar** (7px, thumb transparent until hover) across **all 4 TMAs**; REVERSES v0.62.158 ("always on, obvious, thick" — prior rule preserved in a CSS comment, AU-7). `web/{cuisine,hawker,transport,menu}/src/styles.css`.
  - **#1240 · v0.62.273** — **🍚 Hawker layer pill fits one row**: reclaimed the dead `right-12` controls margin (was reserved for the 🔭 removed in #1237) → ~40px more width, plus pill padding `px-1.5→px-1`. All 3 `MapControls.jsx` copies.

- **[VERIFICATION (sandbox)]** No local build this arc (iCloud `.git` lock-jammed → pushed file-by-file via the GitHub Contents API). All changes are CSS-class / text / small-JSX, no logic paths touched. Each PR was a clean multi-file Contents-API commit; #1235–#1239 squash-merged, #1240 auto-merge-armed at entry time.

- **[STATUS]** PROD — #1235–#1239 merged to `main` (v0.62.268→272); #1240 (v0.62.273) auto-merge armed (lands on green checks). Host redeploys per merge.

- **[TEST]** Operator to eyeball on the live TMAs: (1) Cuisine hub icon size; (2) Cuisine dock row spacing; (3) Reset button shows the zoom number + still resets, ↹ recentres on GPS; (4) ⋮ vertical; (5) scrollbar thin/unobtrusive; (6) Hawker pill visible on a narrow phone.

- **[KNOWN GAPS]**
  - **No local build** — visual/build verification deferred to the deploy (operator gate). The iCloud `.git` lock (auto-commit tool) remains the reason the whole arc went via the GitHub API.
  - **Navy icon tone + Reset-as-number legibility** flagged for operator eyeball; tunable in one line each if off.
  - **Hawker pill fallback** — if it still overflows on the narrowest viewports, the next step is moving overflow pills into the ⋮ menu (deferred).

- **Confirmation Gates**

  | Gate | Resolution | Date |
  |---|---|---|
  | G1 — version bump | PATCH ×6 (`0.62.268`→`0.62.273`), one per PR. | 22-06 '26 |
  | G2 — destructive action | _n/a_ — additive/edits only; 🔭 + the v0.62.158 thick-scrollbar rule removed but their rationale is preserved in comments (AU-7). | 22-06 '26 |
  | G3 — new decision rule | Reset button = zoom readout; scrollbar thin/auto-hide (reverses v0.62.158); per-tile `boxClass`. | 22-06 '26 |
  | G4 — paid external API call | _n/a_. | 22-06 '26 |

## Amendments

- **v0.62.158 reversed** (#1239): the "always-on, obvious, thick" page scrollbar → thin + hover-reveal. Prior rule quoted verbatim in `styles.css` (AU-7).
- **🔭 zoom-readout button removed** (#1237): its zoom number moved onto the Reset button; its GPS-recentre moved to the Cuisine ↹ button.
- **`doc/.serial-state.yml`** — commit 812 / journal 752 / build 467; re-anchored 21:20 SGT 22-06-26.

## Deleted

_None._ Removed UI elements (🔭, thick scrollbar) have their rationale preserved in code comments.
