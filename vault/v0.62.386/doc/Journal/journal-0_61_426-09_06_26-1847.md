(№ 626 - 09-06 '26 18:47 SGT) v0.61.426

# Journal — soleat v0.61.426 (Per-chat rating preference: TMA "≥3.7" pill + /rating command)

> Operator: *"Let's but this '≥3.7' as a pill after '⚙Filters'. Defaulted as toggled. … four toggle fields … 'no rating', 'any rating', '≥3.7', and final field … number with 1 decimal from '1.0' to '5.0' … a save button. Once saved. The button '≥3.7' will change to the new rating … Create a '/rating' equal to '/r' … zero means anything from null … saved per chatid."*
>
> Confirmed semantics: **no rating = unrated-only**, **any rating = no minimum (off)**, custom range **1.0–5.0** for both surfaces. Alias: **/rating = /ra** (/r stays bound to /recognised — operator pick, since /r was already taken).
>
> Commit 639 / journal 626 / **build 405 (web/cuisine changed)**. New rating-pref.js + venue-filters.js + index.js + Cuisine TMA + tests. Revert-ready.

## New

### [HDR] #626 | 18:47 SGT 09-06-26 | v0.61.426 — per-chat rating pref (TMA pill + /rating), one Redis key drives both | rating-pref.js + venue-filters.js + index.js + web/cuisine + tests | 1 PR | 1 build |

- **[INTENT]** Make the v0.61.425 guarded 3.7 floor **user-configurable** and **shared** across the chat + the Cuisine TMA. Two surfaces, one setting:
  - **Cuisine TMA** — a **"≥3.7" pill after ⚙ Filters**, default toggled-on. Tap → a panel just below with **four mutually-exclusive choices** (① *no rating* = unrated-only, ② *any rating* = no minimum, ③ *≥3.7*, ④ *custom* 1.0–5.0, one decimal) + **Save**. The pill **relabels** to the saved value.
  - **`/rating` (alias `/ra`)** — `0` = any (no floor), `1.0–5.0` = that floor, anything else → suggest 3.7. Saved **per chatId**, **in tandem** with the TMA (same Redis key).

- **[DELTA]**
  - **`rating-pref.js`** (NEW, mirrors `country-pref.js`) — per-chat `rating-pref:<chatId>` (+ `:dev:<id>` per-device) in Redis. Stores ONE token: `'unrated' | 'any' | '1.0'…'5.0'` (default `'3.7'`). Helpers: `normalizeRatingPref` / `parseRatingCommand` (0→any, 1.0–5.0→floor, else null), `ratingPrefToFloorOpts` (→ applyRatingFloor opts), `describeRatingPref` (log label), `getUserRatingPref` / `setUserRatingPref`, and bilingual (en/fr) `/rating` messages.
  - **`venue-filters.js`** — `applyRatingFloor(venues, opts)` gained **`opts.mode`**: `'floor'` (default — the v0.61.425 guarded ≥floor, **unchanged** for legacy no-mode callers), `'off'`/`'any'` (keep all), `'unrated'` (keep ONLY unrated venues, still **never empties**). Additive — the 6 existing v0.61.425 cases stay green.
  - **`index.js`** — (1) `require('./rating-pref')`; (2) **`deliverPicks`** (bot renderer) reads the per-chat pref → floors with the chosen mode; (3) **`/api/cuisine/search`** floor seam reads the pref (prefer a validated request override, else Redis, else default); (4) **`/rating` (`/ra`)** `bot.onText` handler (status / set / invalid→suggest 3.7); (5) **`/api/cuisine/rating-pref`** GET + POST (mirror country-pref endpoints, initData-gated, rate-limited).
  - **Cuisine TMA** (`web/cuisine/src/v2`) — `QuickFilters.jsx`: ⭐ rating pill after ⚙ Filters + the 4-option **radiogroup** panel (44px rows, selection shown by **filled-dot shape + border**, not colour — colour-blind-safe) + custom 1.0–5.0 field + Save. `App.jsx`: dedicated `ratingPref` `useState` (NOT in `state`, so filter-resets can't clobber it) + mount-seed from the server + `onRatingSave` → relabel + persist. `api.js`: `fetchRatingPref` / `saveRatingPref`. `i18n.js`: `rating.*` keys (en/fr).
  - **Tests** — `__tests__/rating-pref.test.js` (NEW, 18 cases) + `__tests__/venue-filters.test.js` (+7 mode cases).
  - **`package.json`** `0.61.425 → 0.61.426`; **`doc/.serial-state.yml`** commit 638→639, journal 625→626, **build 404→405**; re-anchored 18:47 SGT.

- **[VERIFICATION (sandbox)]** `node --check` on index.js / venue-filters.js / rating-pref.js clean. Full suite **3167/3167 green** (was 3142 + 25). `npm run build:cuisine` green. Functional spot-check of `normalizeRatingPref` / `parseRatingCommand` / `applyRatingFloor` modes via `node -e` all PASS.

- **[STATUS]** PR-**OPEN**, **auto-merge armed (squash)**. Railway redeploys (server + the rebuilt cuisine bundle) on merge.

- **[TEST — operator, deployed]** In the Cuisine TMA: tap the ⭐ "≥3.7" pill → pick *any rating* / *no rating* / a *custom* value → Save → the pill relabels and the next 🔍 search respects it. In chat: `/rating` shows the current setting; `/rating 0` = any; `/rating 4.3` = floor; `/rating xyz` → suggests 3.7. Setting one surface is reflected on the other (shared `rating-pref:<chatId>`). Railway logs show `[deliverPicks] rating-floor …` / `[Cuisine-Search] rating-floor …` and `[rating-pref] chat=… → …`.

- **[KNOWN GAPS]**
  - **`/rating` is not in the public slash menu** (handler + `/ra` alias only) — same pattern as `/hidden` / `/ver`. The TMA pill is the primary discovery surface. Can be promoted into `setMyCommands` if the operator wants it listed.
  - **`/rating` can't set *unrated-only*** by design — the command covers off (0) + floor (number); unrated-only is a TMA-panel-only choice (operator's command spec only named 0 + number + invalid).
  - **`'unrated'` mode keeps the never-empty guard** — a city with zero unrated venues falls back to the full pool (logged) rather than returning 0, honouring the operator's standing "never zero" rule. Flip if a true-empty is preferred.
  - **`/hidden` / `/recognised` (Michelin) are NOT routed** through the per-chat floor (parity with v0.61.425 — Michelin is a curated awards list; /hidden has its own ≥3.9 gate).
  - **VibeCodingRecord** regen still due (append PR row + regenerate) — log/do post-merge.
  - Couldn't measure live over-compression (no Places API key in sandbox); the guard + deployed log lines are the safety net — eyes-on-deployed pending.

- **Confirmation Gates**

  | Gate | Resolution | Date |
  |---|---|---|
  | G1 — version bump | PATCH `0.61.425 → 0.61.426`. | 09-06 '26 |
  | G2 — destructive action | Additive: new module + new opts.mode (legacy unchanged) + new endpoints/command + TMA pill. Floor still guarded (never empties). Reversible. | 09-06 '26 |
  | G3 — new decision rule | The minimum-rating floor is now **per-chat configurable** (TMA pill + /rating, shared via `rating-pref:<chatId>`); default stays the guarded ≥3.7. | 09-06 '26 |
  | G4 — paid external API call | None (uses the rating already on each venue; Redis only). | 09-06 '26 |

## Amendments

- **`rating-pref.js`** — NEW per-chat rating-preference module (mirrors country-pref.js).
- **`venue-filters.js`** — `applyRatingFloor` gains `opts.mode` (off / any / unrated / floor); default behaviour unchanged.
- **`index.js`** — rating-pref require; per-chat floor in `deliverPicks` + `/api/cuisine/search`; `/rating` (`/ra`) command; `/api/cuisine/rating-pref` GET/POST.
- **`web/cuisine/src/v2/components/QuickFilters.jsx`** — ⭐ rating pill + 4-option panel + Save.
- **`web/cuisine/src/v2/App.jsx`** — `ratingPref` state + mount-seed + `onRatingSave`.
- **`web/cuisine/src/v2/lib/api.js`** — `fetchRatingPref` / `saveRatingPref`.
- **`web/cuisine/src/v2/lib/i18n.js`** — `rating.*` keys (en/fr).
- **`__tests__/rating-pref.test.js`** (NEW, 18) + **`__tests__/venue-filters.test.js`** (+7).
- **`package.json`** — `0.61.425 → 0.61.426`.
- **`doc/.serial-state.yml`** — counters (commit 639 / journal 626 / build 405) + anchor 18:47 SGT 09-06-26 + `last_anchor_source` prepend.

## Deleted

- The hardcoded single 3.7 floor as the ONLY behaviour (superseded by the per-chat configurable floor; ≥3.7 remains the default).
