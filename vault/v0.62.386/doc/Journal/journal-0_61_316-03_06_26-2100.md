(№ 517 - 03-06 '26 21:00 SGT) v0.61.316

# Journal — soleat v0.61.316 (VibeCodingRecord catch-up regen #748-#823)

> Operator (03-06 '26): *"merge then vibe-journal"*. Doc-system upkeep per CLAUDE.md standing rule — regenerate `doc/VibeCodingRecord/` so the ledger + hosted page (`/doc/vibe-journal`) reflect main.
>
> Backfills **73 PR rows** (#748-#823 minus 3 closed-without-merge: #759, #762, #767) since the prior regen on 29-05-26 (PR #747 / v0.61.244). Bumps `GEN_DATE` 2026-05-29 → 2026-06-03. Regenerates the four generator outputs.
>
> Commit 530 / journal 517 / build 319, on branch `claude/v0.61.316-vibe-journal-regen` off `main` at v0.61.315 (sha `98dac61`).

## New

### [HDR] #517 | 21:00 SGT 03-06-26 | v0.61.316 — VibeCodingRecord catch-up regen | 9 files | 1 PR | 0 build |

- **[INTENT]** Bring `doc/VibeCodingRecord/` + the hosted `/doc/vibe-journal.html` + `/doc/vibe-journal.json` ledger into sync with main. 76-PR drift since the last regen (29-05 catch-up at v0.61.244 ledger PR #747); each PR carries its own journal entry, but the cross-PR ledger + the queryable hosted view were stale.

- **[DELTA]**

  **1. `doc/VibeCodingRecord/data/prs.ndjson`** — appended **73 JSON rows** (#748 → #823, ASC, JSON-Lines). Each row: `{ n, title, state, merged, body }` with body truncated to 600 chars. Source: `gh pr list --state merged --limit 200 --json number,title,state,mergedAt,body --jq 'select(.number >= 748 and .number <= 823)'`. 3 closed-without-merge PRs (#759 *v0.61.256 Menu picker fix*, #762 *v0.61.258 durian-variance JSON output*, #767 *v0.61.262 Durian precision/recall push*) correctly excluded by the `state="merged"` filter.
  - File grew 738 → 811 rows.

  **2. `doc/VibeCodingRecord/data/pr-files.tsv`** — appended **73 TSV rows** (one per PR, ASC by PR #). Each row: `<PR#>\t<comma-separated-file-list-from-squash-commit>`. Built by walking `git log origin/main --pretty=format:"|commit|%H|%s|" --name-only`, regexing `(#\d+)\s*$` from the subject to associate files-per-merge-commit. PR #821 (one of the `ang-kl-patch-1` uploads) had `git show --name-only` empty (the file was first added by #822 which merged earlier the same minute — git deduplicated); the row was patched in manually via `gh pr view 821 --json files`.
  - File grew 528 → 601 rows.

  **3. `doc/VibeCodingRecord/generate.mjs`** — `GEN_DATE` bumped `2026-05-29` → `2026-06-03`. Comment updated to describe this regen window (#748-#823, 3 closed-without-merge skipped).

  **4. `doc/VibeCodingRecord/records.tsv`** *(regenerated)*. 810 rows. Category breakdown: feature=521 · fix=107 · docs=60 · copy=40 · prompt-tune=39 · refactor=22 · test=11 · infra=10. Rework-flagged=69.

  **5. `doc/VibeCodingRecord/vibe-coding-record.md`** *(regenerated)*. Auto-generated Markdown ledger consumed by the hosted page.

  **6. `public/doc/vibe-journal.html`** *(regenerated)*. The hosted queryable view at `https://soleat.net/doc/vibe-journal.html`.

  **7. `public/doc/vibe-journal.json`** *(regenerated)*. The machine-readable JSON twin of the HTML, served alongside.

  **8. `package.json`** — `0.61.315 → 0.61.316`.

  **9. `doc/.serial-state.yml`** — commit 529→530, build 318→319, journal 516→517; re-anchored 03-06 '26 21:00 SGT.

- **[VERIFICATION (sandbox)]**

  - **Generator clean**: `node doc/VibeCodingRecord/generate.mjs` exits 0, prints `Vibe-Coding Record: wrote records.tsv (810 rows), vibe-coding-record.md, public/doc/vibe-journal.html, public/doc/vibe-journal.json`.
  - **Sanity-check the 73 appended rows**:
    - First: `{"n":748,"title":"v0.61.245: Menu TMA location field + JB / OTHER region pill (PR #2 of 4)","state":"merged","merged":"2026-05-29T05:35:24Z"}`. Last: `{"n":823,"title":"v0.61.315: canonical feature spec @ PR #818 cutoff (v0.61.312 / sha 207f1e63)","state":"merged"}`.
    - Gaps verified: `seq 748 823 - present-numbers = [759, 762, 767]` ⇒ all three closed-without-merge.
  - **Ledger row count math**: ndjson 738 + 73 = 811; generator processes the merged subset (`state == "merged"`) → 810 rows in records.tsv. One PR's `merged` field is null / state ≠ merged → excluded by the generator's filter; aligns with the existing schema.
  - **No code changes**: this PR touches only docs + data + the doc-system version log. Bot redeploys at v0.61.316 with **zero runtime behaviour change** vs v0.61.315.

- **[STATUS]** PR-OPEN on push. Railway redeploys; the new `/doc/vibe-journal.html` becomes the served version (the prior was generated 2026-05-29 against PR #747).

- **[TEST]** Operator (post-merge):
  1. Browse `https://soleat.net/doc/vibe-journal.html` — header should read *"Auto-generated … on 2026-06-03 from a snapshot of all 810 pull requests (#1–#810)"*.
  2. Filter by "feature" tab: 521 rows expected.
  3. Filter by "rework-flagged": 69 rows expected.
  4. The new entries (#748-#823) cover the recents-drawer / region-flip / MAP_ID-wire / `/about` / canonical-spec arcs.

- **[KNOWN GAPS]**
  - **#821 (the second `ang-kl-patch-1` upload)**'s pr-files.tsv row is the gh-supplied file list (`scripts/gia-feature-spec-and-gia-web-audit-prompts.md`) — same file as #822, but git's squash commit showed empty because #822 had merged first.
  - **The 3 closed-without-merge PRs** (#759, #762, #767) are NOT in the ledger — their work was redone in subsequent PRs. If you ever want them recorded as `state="closed"`, append manually.
  - **Body truncation at 600 chars**: matches the prior catch-up's choice. Longer bodies are visible in the GitHub PR itself.
  - **`vibe-journal.html` is served via `/doc/`**: the existing route mount + Vibe-Journal-key gate (`VIBE_JOURNAL_KEY` env) carries through unchanged.

- **Confirmation Gates**

  | Gate | Resolution | Date |
  |---|---|---|
  | G1 — version bump | PATCH `0.61.315 → 0.61.316`. Doc-system upkeep; no code change. | 03-06 '26 |
  | G2 — destructive action | _n/a_. Additive — appends to ndjson + tsv, regenerates 4 outputs. No deletions. | 03-06 '26 |
  | G3 — new decision rule | _n/a_. Routine catch-up per CLAUDE.md standing rule. | 03-06 '26 |
  | G4 — paid external API call | _n/a_. `gh pr list` is the GitHub API (free); no Places / Gemini calls. | 03-06 '26 |

## Amendments

- **`doc/VibeCodingRecord/data/prs.ndjson`** — 738 → 811 rows (+73 for #748-#823).
- **`doc/VibeCodingRecord/data/pr-files.tsv`** — 528 → 601 rows (+73).
- **`doc/VibeCodingRecord/generate.mjs`** — GEN_DATE 2026-05-29 → 2026-06-03 + comment refresh.
- **`doc/VibeCodingRecord/records.tsv`**, **`doc/VibeCodingRecord/vibe-coding-record.md`**, **`public/doc/vibe-journal.html`**, **`public/doc/vibe-journal.json`** — regenerated outputs.
- **`package.json`** — `0.61.315 → 0.61.316`.
- **`doc/.serial-state.yml`** — counters + last_anchor_time re-anchored 03-06 '26 21:00 SGT.

## Deleted

_None._
