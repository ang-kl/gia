(№ 617 - 09-06 '26 13:00 SGT) v0.61.417

# Journal — soleat v0.61.417 (Review date "X ago" after the closing quote — card + copy + chat)

> Operator: *"the result card, copy to, copy, free-text's result if review field row is included - it must have the date/day past ... a few spacing after the closed quote ... or day past based on google map review. check first and be 99% sure before building."* Chose: **Google "X ago"**.
>
> Commit 630 / journal 617 / **build 398**. pipeline.js + index.js + venue-templates.js + ResultCard.jsx. Revert-ready.

## New

### [HDR] #617 | 13:00 SGT 09-06-26 | v0.61.417 — review "X ago" date rendered after the quote | pipeline.js + index.js + venue-templates.js + ResultCard.jsx | 1 PR | 1 build |

- **[INTENT]** Show the review's **date** ("3 months ago" — Google's own relative time) as dated evidence, a **few spaces after the closing quote** of the review, on the result card + the copy / copy-to / free-text outputs.

- **[FINDING — investigated first (agent, file:line)]** The date **is** fetched (Places `places.reviews` → `publishTime` + `relativePublishTimeDescription`) but **dropped** before the card: `pipeline.js` conflated `publishTime || relativePublishTimeDescription`, then `index.js` set `recentReview` text-only and `delete v.reviews`. Operator chose Google's **"X ago"** → no device-date formatting needed.

- **[DELTA]** (server + client)
  - **`pipeline.js`** (review flatten) — keep **`relative: relativePublishTimeDescription`** as its **own** field (no longer conflated with `publishTime`).
  - **`index.js`** — capture **`v.recentReviewAgo`** **before** each `delete v.reviews` (the cuisine path + the Michelin/`/s` path): match the review whose text became `recentReview` by 40-char prefix, else the most-recent review, and take its `.relative`.
  - **`web/cuisine/src/v2/components/ResultCard.jsx`** — render `recentReviewAgo` (`not-italic ml-2`, i.e. the "few spaces") right after the closing quote; **forward it in the copy-one POST body** (→ `formatVenueBlock` via the whole-body pass-through).
  - **`venue-templates.js`** (`formatVenueBlock`) **+ `index.js`** bot-detail block — append the ago after the closing quote (these chat review lines render only when `recentReviewTranslatedFlag` is set — respects *"if review field row is included"*).
  - **`package.json`** `0.61.416 → 0.61.417`; **`doc/.serial-state.yml`** commit 629 → 630, journal 616 → 617, **build 397 → 398**; re-anchored 13:00 SGT.

- **[VERIFICATION (sandbox)]** `node --check` on index.js / pipeline.js / venue-templates.js clean. Cuisine **vite build green**. Full suite **3125/3125 green**.

- **[STATUS]** PR-**OPEN**, **auto-merge armed (squash)**. Railway redeploys (server + TMA) on merge.

- **[TEST]** Operator: a result card with a review now reads `💬 "great laksa…"  3 months ago`; the per-card 📋 Copy + chat free-text carry the same after the quote.

- **[KNOWN GAPS]**
  - The **device-region date format** path was **not** built (operator chose "X ago"); the `/hidden` `toLocaleDateString` precedent remains if they switch later.
  - The chat copy/detail review line only renders when the review is a **nationality-translated** one (pre-existing gate) — the ago shows there, not on plain chat copies. Relaxing that gate is a separate ask.
  - No new unit test — the review flatten is deep in the pipeline (not unit-isolable without mocking Places); verified end-to-end by reading the path.
  - Eyes-on-deployed pending.

- **Confirmation Gates**

  | Gate | Resolution | Date |
  |---|---|---|
  | G1 — version bump | PATCH `0.61.416 → 0.61.417`. | 09-06 '26 |
  | G2 — destructive action | Additive (keeps a field that was discarded); render-only. Reversible. | 09-06 '26 |
  | G3 — new decision rule | Review row shows Google's "X ago" a few spaces after the closing quote. | 09-06 '26 |
  | G4 — paid external API call | None (the date already came back in the existing `places.reviews` fetch). | 09-06 '26 |

## Amendments

- **`pipeline.js`** — review flatten keeps `relative`.
- **`index.js`** — `recentReviewAgo` capture (×2, before each review strip) + bot-detail render.
- **`venue-templates.js`** — `formatVenueBlock` review line appends the ago.
- **`web/cuisine/src/v2/components/ResultCard.jsx`** — render after quote + copy POST forward.
- **`package.json`** — `0.61.416 → 0.61.417`.
- **`doc/.serial-state.yml`** — counters (commit 630 / journal 617 / build 398) + anchor 13:00 SGT 09-06-26 + `last_anchor_source` prepend.

## Deleted

- The "review text shown without its date" behaviour (superseded — the "X ago" now follows the quote).
