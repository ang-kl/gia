(№ 275 - 20-05 '26 18:40 SGT) v0.61.64

# Journal — soleat v0.61.63 → v0.61.64 (bus-stop popup legibility + row layout)

> Operator: *"the words font is very light cannot be seen. secondly place the
> timing in front instead of at the end of the row"* — with a sample template
> showing each arrival row leading with its `≤N min` label, a `Bus Stop №`
> prefix on the stop code, and a `Google Map ➚` footer.
>
> Sequenced after the v0.61.63 nearest-bus-stop enrichment — this entry is
> commit 275 / journal 268 / build 75. Rides PR #564's branch
> (`claude/review-gia4lunch-readme-3s798`) — the GitHub REST API is
> rate-limited, so no separate PR could be opened.

## New

### [HDR] #268 | 18:40:00 SGT 20-05-26 | v0.61.64 — bus-stop popup legibility + timing-first rows | ~2 kb | 4 files | rides PR #564 | 1 build |

- **[INTENT]** Make the bus-stop arrivals popup readable: the arrival text was
  too light to see on the white card, and the timing label sat at the *end* of
  each row instead of leading it.

- **[DELTA]**
  - **`web/transport/src/lib/mapOverlays.js`** (canonical) — `busArrivalRows()`
    + `busInfoHtml()`:
    - `busArrivalRows()` — each bucket row now leads with the timing label:
      `- ≤5 min ‧ Bus № 8, 31, …` (was `№ 8, 31, … — ≤5 min`). Text forced dark
      (`color:#1c1c1f`) + `font-weight:600`; row spacing `margin-top:2px` → `5px`.
    - `busInfoHtml()` — stop-code line is now `🚏 Bus Stop № <code>` (was
      `🚏 № <code>`) and recoloured from the faint `c.sub` (#5a5a5a) to `c.fg`
      (#1c1c1f) + `font-weight:600`; title forced to `c.fg` + `font-weight:700`;
      `Loading arrivals…` / `No live arrivals` lines recoloured `c.sub` → `c.fg`;
      footer link text `<road> ↗` → `Google Map ➚` (U+279A).
  - **`web/hawker/src/lib/mapOverlays.js`** + **`web/cuisine/src/v2/lib/mapOverlays.js`**
    — byte-identical copies of the canonical file (`md5sum` ×3 confirmed equal).
  - **`package.json`** — `0.61.63` → `0.61.64` (PATCH — UI legibility fix).

- **[VERIFICATION (sandbox)]**

  ```bash
  $ node --check web/transport/src/lib/mapOverlays.js  → OK
  $ md5sum web/{transport/src,hawker/src,cuisine/src/v2}/lib/mapOverlays.js
    99287633ba845aef0fe9a5d4ff3b15bf  (×3 identical)
  $ npm test  → 65 files / 1928 tests green
  $ npm run build:transport / build:hawker / build:cuisine  → all green
  ```

- **[STATUS]** DRAFT → rides PR #564 (base `main`, branch
  `claude/review-gia4lunch-readme-3s798`). Deploys via the Railway auto-deploy
  on `main` once merged.

- **[TEST]** Popup renders road name (bold dark), `🚏 Bus Stop № <code>`,
  then one row per non-empty bucket — each `- ≤N min ‧ Bus № <list>` — and a
  `Google Map ➚` link. No logic change to the arrival bucketing.

- **[KNOWN GAPS]**
  - Visual check is sandbox-blind — the build is the only gate; the colour /
    weight change is verified by code inspection, not a browser preview.
  - GitHub REST API rate-limited this session → this commit lands on PR #564's
    branch rather than its own PR. Merge #564 to ship both.

- **Confirmation Gates**

  | Gate | Resolution | Date |
  |------|-----------|------|
  | G1 — version bump | PATCH `0.61.63` → `0.61.64`, UI legibility fix | 20-05 '26 |
  | G2 — destructive action | _n/a_ | — |
  | G3 — new decision rule | _n/a_ | — |
  | G4 — paid external API call | _n/a_ | — |

## Amendments

_None._

## Deleted

_None._
