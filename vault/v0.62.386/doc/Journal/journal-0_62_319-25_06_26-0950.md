(№ 754 - 25-06 '26 09:50 SGT) v0.62.319

# Journal — soleat v0.62.303 → v0.62.319 (Multi-locale program: Indonesian finish · Russian + German · keyed-i18n refactor · Gemini curated-prose translation by PR)

> Operator: a multi-session locale push — finish **Indonesian (id)** across the 4 TMAs +
> chat, add **Russian (ru) + German (de)**, migrate inline `lang==='fr'` ternaries to the
> keyed i18n overlay, and stand up a **Gemini-API mechanism that translates curated prose
> (tourist explainers) by PR**. Proper nouns (dish/cuisine/place names) never translated.
> "Maximal" localisation: UI chrome + curated prose + (gate opened for) runtime review
> translation; EN stays the genuine last-resort fallback, never forced. Built + shipped via
> the GitHub Contents API (iCloud `.git` lock-jammed; per-PR squash + auto-merge).
> Commit 843→860 / journal 753→754 / build 498→515.

## New

### [HDR] #754 | 09:50 SGT 25-06-26 | v0.62.303→v0.62.319 — multi-locale arc: id finish + ru/de + keyed-i18n refactor + Gemini prose translation (#1275–#1288, #1290–#1292) | ~20 files | 17 PRs |

- **[INTENT]** Operator: *"improve the ID and FR further… I also want to add RU and GE"* →
  *"add fun facts + dish explanations… translate to the 5 languages, via Gemini API,
  committed by PR/deployment."* Reach rationale: ru = Telegram's #1 market, de = large
  tourist segment; id already SE-Asia priority. Keep curated **proper-noun content in
  English/native** (dish names = identity, not copy). Default **EN only when the device
  language isn't supported** — "don't force these language to EN to discourage user".

- **[DELTA]** (grouped by theme)
  - **Indonesian finish** (#1275–#1279) — `id` strings across Cuisine result-card +
    open-hours, the Hawker/Transport/Menu TMAs, and App.jsx pop-up dialogs; completes the
    curated id pass started pre-arc.
  - **Ternary → keyed i18n + `t()` fix** (#1280) — migrated inline `fr:id:en` ternaries to
    `t('key', lang)` keys; fixed the `t()` empty-string fallback `entry[l] || entry.en` →
    **`entry[l] ?? entry.en ?? key`** (nullish) across all 4 TMA `i18n.js`, so an
    intentionally-empty locale value (fr `card.distAway = ''`) is honoured instead of
    falling back to English.
  - **ru/de infrastructure + static-translation tooling** (#1281) — `SUPPORTED(_LOCALES)`
    `['en','fr','id']` → `['en','fr','id','ru','de']` across the 4 TMAs + `user-prefs.js`;
    EN-default invariant locked (`detectFromTelegram`/`detectFromNavigator` gate on
    `SUPPORTED`; chain ends `|| 'en'`; test asserts zh/ja/es/ar/th/vi/ko/it → `en`).
  - **ru/de UI overlays + agent QA** (#1282–#1285) — `RU_STRINGS`/`DE_STRINGS` flat overlays
    merged via the same loop as `ID_STRINGS`; Hawker 64, Transport 72, Menu 69, Cuisine
    207×2 keys. Each batch vetted by a **ru/de linguist + pill-fit review agent** (operator
    can't read these langs) — caught real errors ("rebuilt" vs "replaced", German anglicism,
    dangling RU adjective, control-label overflow → natural abbreviations e.g. de "Sehensw.",
    "Bushalt.").
  - **5-flag → dropdown toggle** (#1286) — the 5-flag header row (space-eating) became a
    compact 📍-style dropdown: collapsed = flag + ▾; open = native-name rows (English 🇬🇧 /
    Français 🇫🇷 / Deutsch 🇩🇪 / Русский 🇷🇺 / Bahasa Indonesia 🇮🇩). Cuisine + Menu + the
    added Hawker/Transport toggles.
  - **Cross-TMA + chat language sync** (#1287) — the server persist gate for
    `/api/cuisine/user-language` `['en','fr']` → `['en','fr','id','ru','de']`; all 4 TMAs
    POST on change (shared `gia.locale` localStorage + `gia:locale` event already wired) so a
    switch in any TMA reflects everywhere + in free-text/chat.
  - **open-hours ru/de** (#1288) — `fmtTime` 24h `HH:MM`, `DAY_LABELS`, `OH_PHRASES` for
    ru/de; EN output byte-identical (regression-guarded); tests 36/36.
  - **Gemini curated-prose translation mechanism** (#1290) — `scripts/translate-content.mjs`
    reads `nation-overlay.js` `touristExplainer.en`, calls Gemini (REST `generateContent`,
    **no `googleSearch` tool** — translation must not be grounded), writes a **generated
    overlay** `nation-overlay-i18n.generated.js` (NEVER mutates hand-authored source; same
    overlay-merge convention as `ID_STRINGS`). `.github/workflows/translate-content.yml` =
    `workflow_dispatch`; injects `secrets.GEMINI_API_KEY` into the runner env only; the merge
    in `nation-overlay.js` is a guarded `require` (en/fr win; absent overlay = no-op).
  - **66 tourist-explainer translations id/ru/de** (#1291 → 24, #1292 → all 66) — generated +
    native-reviewed by PR. Pipeline hardened across three real failure modes: **batched
    calls** (6→14/req) to dodge per-minute 429; **frugal retry** (no model-cascade on 429 —
    models share the project quota); **cascade to a fallback model on 503** (overload is
    per-model). Refined prompt fixed a systemic bug — "preserve proper nouns" was too blunt,
    so nationality **adjectives** ("Japanese cuisine") were left in English; now adjectives +
    common nouns translate, dish names stay Latin (incl. Russian — no Cyrillic
    transliteration), with a factual-fidelity guard.

- **[VERIFICATION (sandbox)]** No local web build (iCloud `.git` lock → GitHub Contents API).
  Per PR: `node --check` on changed `.js`, `esbuild` parse on `.jsx`; `nation-overlay.js`
  load-test (66 `touristExplainer.en`; guarded merge swallows `MODULE_NOT_FOUND`); overlay
  `require` + completeness assertion (66/66, none incomplete). Translation drafts vetted by 3
  per-language native-linguist review agents (id/ru/de) over both halves; reviewer-specified
  fixes applied (🇩🇪 pakistani "Schienenschnitzel"→Beinscheibe, portuguese Klippfisch/
  Eiertörtchen; 🇷🇺 dish names → Latin, hairy crab, ukrainian gloss; 🇮🇩 hainanese order,
  thai/vietnamese phrasing). Full Vitest + TMA build run in CI on each PR.

- **[STATUS]** PROD — #1275–#1288 + #1290–#1292 squash-merged to `main`
  (v0.62.303→v0.62.319). Host redeploys per merge. This doc-catch-up PR carries the next PATCH.

- **[TEST]** Operator eyeball on the live TMAs: language dropdown (native names) on Cuisine/
  Menu/Hawker/Transport; switch reflects across TMAs + chat; ru/de UI + open/closed labels
  fit pills; an id/ru/de device sees translated tourist explainers (66 cuisines) with dish
  names intact; an unsupported device language (zh/ja/…) falls back to EN, not forced.

- **[KNOWN GAPS]**
  - **fun-facts.js NOT yet translated** — locale code `id` collides with the entry `id`
    **identifier** field; needs a `text:{en,fr,id,ru,de}` nest-refactor (renderer
    `factBody` `fact[lang]` → `fact.text[lang]`) before id/ru/de can be added. Its locale set
    is also the older `en/fr/zh/ms/ta/ja/ko/th` (out of sync with the curated-5).
  - **cooking-methods.js excluded by design** — it is a deterministic **search-routing token
    index** (matched against user input, never displayed); bulk-translating it would break the
    router + duplicate tests. Multilingual support there = query *aliases* (separate feature).
  - **Runtime review translation** — the `resolveLang`/`SUPPORTED` gate is open for id/ru/de
    (#1281/#1287); `enrichVenuesWithTranslatedReview` can target them — live verification +
    Redis cache-cost check still queued (Phase 4).
  - **Chat-side reply localisation** — ~143 inline `=== 'fr'` reply ternaries + `dpLang`/
    `ftLang` gates still en/fr; staged Phase 5 (open-hours ru/de = stage 1, #1288).
  - **App.jsx ~45 dialog ternaries → keys** — deferred (Phase 0b).
  - **VibeCodingRecord** — still owes the #1275–#1292 rows (`data/prs.ndjson` +
    `pr-files.tsv` append + `generate.mjs`); regenerate when convenient.
  - **Gemini billing note** — free-tier daily quota exhausted mid-fill (24/66); operator
    enabled billing; a transient project `403 PERMISSION_DENIED` cleared on retry; 503-cascade
    fix then completed all 66.

- **Confirmation Gates**

  | Gate | Resolution | Date |
  |---|---|---|
  | G1 — version bump | PATCH ×17 (`0.62.303`→`0.62.319`), one per PR; this doc PR → next PATCH. | 25-06 '26 |
  | G2 — destructive action | _n/a_ — additive overlays + a generated file; no hand-authored prose mutated (guarded merge; en/fr win). | 25-06 '26 |
  | G3 — new decision rule | tiered locale strategy (curated-5 hand+agent-verified vs auto long-tail); EN = genuine fallback only; proper nouns never translated; generated overlay (not under AU append-only). | 25-06 '26 |
  | G4 — paid external API call | Gemini `generateContent` for static prose translation, by PR, key via GitHub secret on the runner (never committed); operator enabled billing. | 25-06 '26 |

## Amendments

- **`doc/.serial-state.yml`** — commit 843→860, journal 753→754, build 498→515; re-anchored
  09:50 SGT 25-06-26 (evidence: PR #1292 merge `2026-06-25T01:50:07Z`).
- **Tiered locale strategy (decision, AU-7)** — curated **en/fr/id/ru/de** get hand- +
  agent-verified UI/prose; long-tail device languages (zh/ja/es/ar/th/vi/ko/it/…)
  auto-translate **flowing text only**, controls stay English (pill-overflow), EN last-resort.
  Supersedes the prior implicit "en/fr only" gate; recorded verbatim here.
- **`t()` fallback `||` → `??`** (#1280) — prior `entry[l] || entry.en || key` returned EN for
  intentionally-empty locale values; corrected to nullish coalescing. Kept here per AU-7.

## Deleted

_None._ The 5-flag toggle row was replaced by the dropdown (#1286) — rationale (header space)
preserved in code + this entry. No prose or data removed.
