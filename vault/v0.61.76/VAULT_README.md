# VAULT — soleat / Gia4lunch v0.61.76

> Frozen reproducible snapshot of **soleat** (*Gia4lunch* / "Gia") at **v0.61.76** —
> a production Telegram bot + five Telegram Mini Apps + an Express server for
> Singapore F&B discovery, transport, and logistics.

**Captured:** 21 May 2026, 09:44 SGT (serial № 283 — see `doc/.serial-state.yml`).
**Created by:** operator instruction (21-05 '26) — *"Vault."* (Snapshot + fresh master document set.)
**Snapshot size:** ~154 MB, 873 files.
**Prior vault:** `vault/v0.61.28/`. This vault is **kept alongside** it, not superseding it — each vault is an append-only point-in-time snapshot (Rule V-1). The v0.61.29 → v0.61.76 arc is the delta since the prior vault.

---

## What this snapshot is

A faithful mirror of the repository at v0.61.76 — all source (`index.js` + ~80 modules, `web/<app>/src`, `scripts/`), the curated datasets (`data/`, `data_realtime/`), the open-data sources (`geoloc/`), the `__tests__/` suite (1,928 passing), and the full `doc/` documentation system **including the fresh v0.61.76 master document set** (`doc/{Builder|Persona|Feature|Technical|Legal}/…-0_61_76-…md`, `doc/Register/register-0_61_76-…md`) produced in the same pass.

It is bootable: a clean `npm install` reproduces the service.

## Exclusions (per `doc/CLAUDE-FULL.md` §16.1 — strict)

| Excluded | Reason |
|---|---|
| `node_modules/` (root + every nested, incl. `web/*/node_modules`, `.vibe-journal/node_modules`) | Reinstalled by `npm install` |
| `.git/` | History lives in the parent repository |
| `vault/` | Nested vaults forbidden (recursion) |
| `dist/`, `web/*/dist/`, `public/{cuisine,menu,hawker,transport,oversight}/` | Vite build artefacts — gitignored, regenerated by `npm run build` |
| `log/`, `*.log` (incl. `migration_audit.log`) | Transient logs |
| `.env` | Secrets (`.env.example` IS kept) |
| `.claude/settings.local.json` | Local session state |
| `tmp/` | Transient scratch |

`public/app.js`, `public/index.html`, `public/doc/` are kept (not build output). `geoloc/*.geojson` (LTA/URA/NParks/STB/NLB/HPB open data) IS kept — it is the source input to `scripts/build-geo-overlays.js`.

## Boot instructions

```bash
cp -a vault/v0.61.76 /tmp/soleat-v0.61.76
cd /tmp/soleat-v0.61.76
npm install
npm test -- --run                       # 1,928 tests, expect 100% green
npm run build:geo                        # rebuild data/geo-*.json from geoloc/ (optional)
(cd web/cuisine && npm install && npx vite build)   # repeat per web/<app>/ to rebuild the TMAs
# provide a .env from .env.example, then: node index.js
```

## Arc since the prior vault (v0.61.28 → v0.61.76)

| Version range | Headline | Reference |
|---|---|---|
| v0.61.29 – v0.61.32 | Cuisine TMA editable LocationField relocated above the map; "Google Map ↗" pin-popup standard across all three map TMAs; Clinics + Police POI overlay layers (map-controls redesign Phases A–B). | PRs #514–#516 + arc; journals #233–#236 |
| v0.61.33 – v0.61.41 | Map-controls redesign Phases C–G — floating `MapControls` toggle row + "⋯/⋮" overflow dropdown, Reset nav button, the working 🎨 Colour greyscale toggle, the vertical semi-transparent nav-button column; 🏥 Hospital overlay layer + one-shot fetcher; clinic "+" pin; `/ver` admin-command inline menu. | journals #237–#245 |
| v0.61.42 – v0.61.49 | Bus Stop overlay layer enabled (`transport.allStops` → `/api/geo/bus-stops`, live-arrivals popup); Singapore street/building abbreviation expansion (`sg-address.js`); bus-stop popup redesign; greyscale toggle fixes; clinic-hours Google Places data run (→ 88.8 %); map-pin tweaks; high-contrast popup palette. | journals #246–#253 |
| v0.61.50 – v0.61.54 | Cuisine location-field search trigger + loading-overlay rework; map-control tidy (operator change-requests CR1–CR10 arc begins); bus-stop de-emphasis away from the map focus; CR5 zoom-aware train layer. | journals #254–#258 |
| v0.61.55 – v0.61.62 | CR6 station-info system — Phase 1 data layer (`scripts/build-station-info.js` → `data/stations.json`, 180 stations), Phase 2 bus services per exit + first/last-train timings, Phase 3 station-info card UI; CR5 v2 selected-station polyline emphasis; two-colour station pins + `exit_centroid`; bus-stop popup legibility; `/healthz` crash HOTFIX. | PRs #543–#562; journals #259–#273 |
| v0.61.63 – v0.61.70 | CR6 per-station nearest-bus-stop enrichment (`scripts/enrich-nearest-bus-stops.js`); station-card bus stops; 24-hour-places fetch pipeline (`scripts/fetch-24hr-places.js`); map UI redesign — toggles, pins, flashes. | PRs #565–#569; journals #267–#274 |
| v0.61.71 – v0.61.76 | `/transport` nearest-bus-stops message rework (weather leads · live stop count · road-name lead · per-stop distance sentence · "🚌 Bus №" rows); "📍 Refresh location" on the Bus menu + `/carpark`; carpark/bus map-button label tweaks; Bus menu title `🚌 Bus Information`; Train nearest-stations radius fix (1500 m → 5000 m); Menu hub FAB always shows "End"; station-info link normalised-name lookup. | PRs #570–#574; journal #275 + this pass |

## Notes

- **Rule V-1 / V-2** — `vault/v0.61.28/` is kept, not superseded. Each vault is an append-only point-in-time snapshot; this one captures the v0.61.29 → v0.61.76 delta.
- **Rule V-3** — this snapshot's `doc/Journal/` may omit the self-referential journal addendum that documents this very vault pass; the omission is recorded in the creating commit message and the journal entry's `[KNOWN GAPS]`.
- The fresh v0.61.76 master document set (Builder #3 / Persona #4 / Feature #47 / Technical #47 / Legal #9 / Register #115) was produced in the same pass — a fresh, non-incremental consolidation, following the v0.61.28 vault-pass precedent (decision D-1028, AU Recipe overridden for vault-pass master sets).
