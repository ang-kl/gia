(№ 232 - 18-05 '26 18:30 SGT) v0.61.23

# Journal — soleat v0.61.22 → v0.61.23 (taxi-pin words + Nearby↔Details slider)

> Operator review of the Cuisine TMA map: standardise the taxi pins as words, and replace
> the Nearby/All dropdown with a slider that widens every chip overlay layer's radius.

## New

### [HDR] #225 | 18:30:00 SGT 18-05-26 | v0.61.23 — standardised taxi pins + Nearby↔Details overlay-radius slider | ~7 kb | 9 files | 1 PR | 1 build |

- **[INTENT]** Operator, on the TMA maps: (1) *"Standardize all 'taxi' and 'pickup' as
  words for the pins"* — the overlay Taxis layer drew a `🚕` emoji dot, the amenity pins
  used the words "Taxi"/"Pick-up"; (2) *"Remove the 'nearby' and 'all' buttons; instead
  have a slider… defaults to nearby, slide right 'details'"* — a slider widening every
  chip overlay layer to a wider radius. Operator-set radii: Nearby 550 m, Details 7 km.
  Follow-up constraint: the slider must **not** affect the train layer nor the
  tap-triggered amenity views (station-detail, hawker-centre tap, eatery tap) — each keeps
  its own criteria.

- **[DELTA]**
  - **`web/{cuisine,hawker,transport}/src/.../lib/mapOverlays.js`** (byte-identical ×3) —
    new `buildTaxiMarkers` renders the overlay Taxis layer as `amenityLabelNode` word
    pills ("Taxi" / "Pick-up", classified by feature name) instead of `🚕` `dotNode`s.
    `RADIUS_ATTRACTIONS_M` + `RADIUS_NEAR_M` replaced by `OVERLAY_RADIUS = { nearby: 550,
    details: 7000 }`; a `currentRadius()` keyed off `attractionsMode` ('nearby'|'details')
    drives the five chip layers (parks / attractions / taxis / carpark / exits) — parks
    are radius-clipped for the first time (`buildParks` now stores a representative point).
    `TRAIN_RADIUS_M` is kept; the train layer and `attachAmenityPins` are untouched.
    `setAttractionsMode` accepts `'details'` and re-applies the five chip layers only.
  - **`web/{cuisine,hawker,transport}/src/.../components/MapLayerChips.jsx`** — the
    "Nearby ▾" chip dropdown is removed; a 2-position range-input slider (Nearby ↔
    Details) is added below the chip row. (Not byte-identical across TMAs — cuisine uses
    `t as tr` / `[lang]`, hawker+transport use `t` / `lang`; edited per copy.)
  - **`web/{cuisine,hawker,transport}/src/.../i18n.js`** — new `layer.details` key.

- **[VERIFICATION (sandbox)]**

  ```bash
  $ node --check mapOverlays.js                           → OK (byte-identical ×3)
  $ npx vite build  (cuisine / hawker / transport)        → all green
  $ npm test -- --run                                     → 1924 passed (1924)
  ```

- **[STATUS]** DRAFT → PR (base `main`, branch `claude/review-gia4lunch-readme-3s798`).

- **[TEST]** Each TMA map: every taxi pin reads "Taxi"/"Pick-up" (no `🚕` dot); chip row
  has a Nearby↔Details slider below it; sliding to Details widens parks/attractions/
  taxis/carpark/exits to 7 km; train layer + tap-amenity views unchanged.

- **[KNOWN GAPS]** The slider is global to the five chip layers; per-layer radius is not
  exposed. `layer.all` i18n key is now unused (kept, not removed).

## Amendments

(none)

## Deleted

Superseded by v0.61.23:

```js
const RADIUS_ATTRACTIONS_M = 800;   // attractions
const RADIUS_NEAR_M = 400;          // carpark / taxis / exits
```

— folded into `OVERLAY_RADIUS = { nearby: 550, details: 7000 }` (slider-driven). The
overlay Taxis layer's `buildMarkers(d.taxis, '#FBC02D', '🚕', nameInfo)` emoji-dot call is
replaced by `buildTaxiMarkers` (word pills). The `MapLayerChips` "Nearby ▾" toggle chip
(`attractionsMode === 'all' ? 'nearby' : 'all'`) is replaced by the Nearby↔Details slider;
the `'all'` mode value is superseded by `'details'`. Preserved here per Recipe AU-5.

## Confirmation Gates Satisfied

| Gate | Authority | Stamp |
| :--- | :--- | :--- |
| G1 — version bump (PATCH: 0.61.22 → 0.61.23) | _operator: 0.61.x PATCH sequence_ | 18-05 '26 |
| G2 — destructive action | _code-only: radius consts + Nearby/All chip superseded; logged in Deleted_ | 18-05 '26 |
| G3 — new decision rule | _n/a_ | — |
| G4 — paid external API call | _n/a_ | — |
