(№ 244 - 19-05 '26 03:45 SGT) v0.61.33

# Journal — soleat v0.61.32 → v0.61.33 (Phase G — Transport/Hawker in-map controls)

> Phase G of the approved 7-phase map-controls redesign. The flat above-the-map
> `MapLayerChips` strip on the Transport + Hawker maps is replaced by a floating
> in-map control surface: a layer toggle-button row, a 4th navigation button
> (Reset), and a platform-detected "⋯/⋮" overflow dropdown.
>
> Operator: *"Proceed with the 4 navigation buttons and toggle buttons."* — and,
> on sequencing: *"A+B first, then G."* Sequenced after PR #517 (v0.61.32 / commit
> 243) — this PR is commit 244 / journal 237.

## New

### [HDR] #237 | 03:45:00 SGT 19-05-26 | v0.61.33 — Phase G in-map map controls (Transport + Hawker) | ~5 kb | 11 files | 1 PR | 1 build |

- **[INTENT]** Move the Transport + Hawker map's overlay-layer toggles off the flat
  chip strip above the map and into a floating in-map control surface, add a Reset
  navigation button, and group the less-used layers behind a "⋯/⋮" overflow dropdown.
  (Cuisine keeps its `MapLayerChips` strip — its equivalent redesign is Phase C.)

- **[DELTA]**
  - **`MapControls.jsx`** (new, byte-identical ×2 — `web/transport` + `web/hawker`) —
    floating in-map control. Row 1 = layer toggle pills driven by a `rowToggles` prop;
    a `menuToggles`-driven "⋯/⋮" overflow dropdown (outside-tap / Esc close).
    `overflowGlyph()` returns `⋯` on iOS/iPadOS/macOS, `⋮` elsewhere. Toggles flagged
    `disabled` (Bus Stop, Colour, 24 hrs — later phases) render greyed + inert.
  - **`MrtMapPanel.jsx`** — `ROW_TOGGLES` (Attractions, Park, Carpark, Exit; Bus Stop +
    Colour disabled — no Train, the line polylines are intrinsic) + `MENU_TOGGLES`
    (Taxi Stand, Clinics, Police; 24 hrs disabled). New `onOverlayChange` prop; renders
    `<MapControls>`; nav row gains a 4th button — **Reset** (`⟲`, recenters to the SG
    centroid + default zoom).
  - **`HawkerMapPanel.jsx`** — same, with **Train** in `ROW_TOGGLES` (the `train`
    overlay layer); Reset recenters to SG centroid + zoom 11.
  - **`App.jsx`** (Transport + Hawker) — the `<MapLayerChips>` strip removed; the panels
    now receive `onOverlayChange={setOverlayLayers}`.
  - **`MapLayerChips.jsx`** — **deleted** from `web/transport` + `web/hawker` (orphaned;
    Cuisine's copy is untouched).
  - **i18n** (Transport + Hawker) — `layer.busstop` / `layer.colour` / `layer.open24` /
    `layer.soon` / `map.reset` / `map.more` (EN + FR).
  - **`package.json`** — `0.61.32` → `0.61.33` (PATCH).

- **[VERIFICATION (sandbox)]**

  ```bash
  $ npm run build:transport / build:hawker / build:cuisine  → all ✓ built
  $ npm test  → green
  ```
  `MapControls.jsx` confirmed byte-identical across the two TMAs. No test or non-vault
  source still imports the deleted `MapLayerChips.jsx`.

- **[STATUS]** DRAFT → PR (base `main`, branch `claude/review-gia4lunch-readme-3s798`).

- **[TEST]** Open the Transport (Google Map view) or Hawker map: a floating toggle row
  sits top-left, the 4-button nav cluster (zoom ±, expand, Reset) top-right. Toggling a
  pill shows/hides that overlay; the "⋯/⋮" button opens the Taxi/Clinics/Police dropdown;
  Reset recenters the map. Bus Stop / Colour / 24 hrs appear greyed.

- **[KNOWN GAPS]** Bus Stop, Colour and 24 hrs are disabled placeholders — Bus Stop
  (live-Redis layer) and the greyscale Colour toggle (Phase E, blocked on the Google
  Cloud `MAP_ID_GREYSCALE`) and the 24 hrs convenience-store layer have no backing data
  yet. Phase C (Cuisine "⋯/⋮" dropdown) and Phase D (eatery-pin filters) remain.
  `doc/VibeCodingRecord/` regeneration is still due.

## Amendments

(none)

## Deleted

| File | Reason |
| :--- | :--- |
| `web/transport/src/components/MapLayerChips.jsx` | Superseded by the in-map `MapControls` toggle row. |
| `web/hawker/src/components/MapLayerChips.jsx` | Superseded by the in-map `MapControls` toggle row. |

## Confirmation Gates Satisfied

| Gate | Authority | Stamp |
| :--- | :--- | :--- |
| G1 — version bump (PATCH: 0.61.32 → 0.61.33) | _operator: 0.61.x PATCH sequence_ | 19-05 '26 |
| G2 — destructive action (deleted 2 orphaned components) | _operator: "remove … toggle buttons" redesign_ | 19-05 '26 |
| G3 — new decision rule | _n/a_ | — |
| G4 — paid external API call | _n/a_ | — |
