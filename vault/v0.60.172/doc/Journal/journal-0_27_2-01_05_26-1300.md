(№ 50 - 01-05 '26 13:00 SGT)

# Journal — soleat v0.27.2

> **Authenticity note (recipe AU-1):** Preserves all content from `journal-0_27_1-01_05_26-1245.md` by reference; appends v0.27.2 entry below.

---

## New

### [HDR] #50 | 13:00:00 SGT 01-05-26 | v0.27.2 | n/a kb | n/a lines
- **[INTENT]** Outstanding-list item #7: localised cuisine labels (Bahasa Melayu, Mandarin Simplified, Tamil) on the 70-cuisine accordion. State and Gemini payload remain canonical English; only rendered labels translate.
- **[DELTA]**
  - **NEW** `web/cuisine/src/state/cuisine-i18n.js` — `CATEGORY_LABELS` (8 categories × 4 langs, fully localised) and `CUISINE_LABELS` (per-cuisine translations keyed by canonical English). Coverage:
    - **Common Here** (21) — fully localised in zh / ms / ta.
    - **Southeast Asian** (2) + **China (Regional)** (10) + **South Asian Specialists** (5) — fully localised.
    - **European** / **Middle Eastern** / **Americas** / **African** — high-frequency items (Italian, Spanish, French, British, German, Portuguese, Mediterranean, Greek, Lebanese, Turkish, Persian, Israeli) localised; long-tail (Austrian, Swiss, Russian, Ukrainian, Polish, Scandinavian, Belgian, Dutch, Irish, Moroccan, Egyptian, Jordanian, Afghan, Uzbek, Georgian, Peruvian, Argentinian, Cuban, Jamaican, Ethiopian, Kenyan, Nigerian, South African) intentionally falls back to English. Native-speaker review pass deferred.
    - + helpers `localizedCuisine(name, lang)` and `localizedCategory(catId, lang)` with English fallback.
  - **NEW** `tg.js#getLanguage()` — reads `Telegram.WebApp.initDataUnsafe.user.language_code`, falls back to `navigator.language` then `'en'`. Returns ISO 639-1 two-letter code lowercased.
  - **`web/cuisine/src/components/CuisineAccordion.jsx`** —
    - + `import { localizedCuisine, localizedCategory, SUPPORTED_LANGS }` and `import { getLanguage } from '../api/tg.js'`.
    - + Detect lang at component-init; clamp to `SUPPORTED_LANGS` else 'en'.
    - + Selected pill row uses `localizedCuisine(c, lang)`.
    - + Category accordion header uses `localizedCategory(cat.id, lang)`.
    - + Chip labels use `localizedCuisine(item, lang)`.
    - State (`selected`) still carries canonical English so onToggle, search payload, and backend prompt are all unchanged. The translation is purely cosmetic.
- **[VERIFICATION (sandbox)]**
  ```
  cd web/cuisine && npm run build → vite v5.4.21 ✓ in 1.16s
    → public/cuisine/assets/index-*.js  171.71 kB (gzip 56.50 kB)
  ```
  Live language-switch test requires changing the Telegram client's preferred language; behaviour will be verified post-deploy.
- **[STATUS]** PROD — auto-deploys with the next Railway build.
- **[TEST]**
  1. Telegram language = English → labels remain "Common Here / Singaporean / Sichuan / …".
  2. Telegram language = 中文 (Mandarin) → labels become "常见 / 新加坡式 / 四川 / …". Tap Sichuan → selected pill shows "四川 ✕". Backend payload still receives `["Sichuan"]`.
  3. Telegram language = Bahasa Melayu → "Lazim Di Sini / Masakan Singapura / Sichuan / …".
  4. Telegram language = தமிழ் → "இங்கே பொதுவானது / சிங்கப்பூர் / சிச்சுவான் / …".
  5. Long-tail cuisine (Austrian, Russian, Cuban) on non-English → falls back to English label. Diagnostics records the cuisine via canonical English regardless.
- **[KNOWN GAPS]**
  - Translations done by Claude using best-effort rendering. Native-speaker review needed before public-facing claims of localisation. The pragmatic SG-relevant subset (Common Here + Southeast Asian + China Regional + South Asian) should be more reliable than the long-tail.
  - No language switcher in-TMA — follows Telegram's user preference. Could add a toggle in a future patch if user feedback warrants.
  - Tamil glyphs require system font support; on bare Android Telegram WebViews missing Tamil fonts, characters fall back to ☐ boxes. Not in our control; user-side OS configuration.

## Confirmation Gates Satisfied

| Gate | Authority | Stamp |
| :--- | :--- | :--- |
| G1 — patch 0.27.1 → 0.27.2 (purely cosmetic; no contract change) | Outstanding list item #7 | 01-05 '26 |

## Amendments
(none)

## Deleted
(none)
