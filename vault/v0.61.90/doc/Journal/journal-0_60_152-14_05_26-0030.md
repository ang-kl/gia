(№ 117 - 14-05 '26 00:30 SGT)

# Journal — soleat v0.60.152

> **Authenticity note (recipe AU-1):** Preserves all content from `journal-0_60_151-14_05_26-0000.md` by reference. One operator-led tweak: bumping the PR #125 / v0.57.8 distance gate from 80 km to 120 km. One PATCH PR. Test count 1727 → 1728.

---

## New

### [HDR] #117 | 00:30:00 SGT 14-05-26 | v0.60.152 | n/a kb | 1 PR

- **[INTENT]** Operator: *"PR #125, gate to 80 KM. Increase to 120."*

- **[DELTA]**

  Two coupled changes — the cuisine TMA's hard distance gate AND the chat free-text dish-search filter are both bumped to 120 km so a `/cuisine` search and a `/s` chat search agree on reach:

  - **`index.js:9227`** — the PR #125 / v0.57.8 hard gate `venues.filter((v) => v.distanceM == null || v.distanceM <= 80000)` → **120000**. The surrounding comment block updated to record the bump + rationale (more headroom for JB-side venues + a sliver of Iskandar / Kulai when `region: 'JB'` is opted in; the existing `formattedAddress` filter for `'Johor Bahru'` keeps JB-mode pinned).
  - **`free-text-search.js:15`** — `MAX_DISTANCE_M = 80000` → **120000**. Header comment updated to reference v0.60.152 + the cuisine-TMA-side bump for consistency.

  **`__tests__/free-text-search.test.js`** — the existing "drops venues farther than 80 km from the user" test rewritten to "drops … farther than 120 km" with a venue at ~290 km. Plus a NEW companion test that pins the v0.60.152-widened band — a venue ~100 km from SG centroid (would have been dropped at the old 80 km gate) is kept and lands with `80000 < distanceM <= 120000`.

  **`package.json` → `0.60.152`.** **`doc/Journal/journal-0_60_152-14_05_26-0030.md`** (this entry). **`doc/.serial-state.yml`** — `journal 116 → 117`, `commit 119 → 120`; re-anchored 14-05 '26 00:30 SGT.

- **[VERIFICATION (sandbox)]**

  ```bash
  $ node --check index.js free-text-search.js
  (clean)
  $ npx vitest run __tests__/free-text-search.test.js
   Test Files  1 passed (1)
        Tests  16 passed (16)
  $ npm test -- --run
   Test Files  55 passed (55)
        Tests  1728 passed (1728)
  ```

- **[STATUS]** STAGED → PROD on next Railway deploy after squash-merge. No TMA / frontend change required.

- **[TEST]** Post-deploy:
  - `/cuisine` → pick a cuisine + region:JB → 🔍 → result list now includes JB-side venues up to ~120 km from the user (was capped at 80). The JB `formattedAddress` filter keeps the results pinned to Johor Bahru (no Iskandar bleed).
  - From a south-SG location, `/s` chat free-text dish search (e.g. `/s laksa`) → matching venues up to ~120 km are kept; the previous 80 km outer ring (JB outskirts) was clipped.
  - Cuisine-TMA + chat free-text now use the same reach (120 km); the `/cuisine` ↔ `/s` consistency the operator's note implies is preserved.

- **[KNOWN GAPS]**
  - The bump applies to two distance filters; the LTA / Routes API travel-time enrichment still uses its own bounded behavior. A 120 km venue from south-SG will travel-time-out (no SG MRT spans that distance, drive is ~90 min); the card just shows a longer estimate.
  - Carried-over (still owed): `doc/Feature/` addendum for v0.60.150 / v0.60.151 / this PR + `doc/Technical/` + `doc/Register/` + `vault/v0.60.152/` + `doc/Legal/` aggregate-usage-counts clause + `doc/VibeCodingRecord/` refresh through #390 + #391 + this PR.

---

## Confirmation Gates Satisfied

| Gate | Authority | Stamp |
| :--- | :--- | :--- |
| G1 — version bump (PATCH 0.60.151 → 0.60.152) | _operator-supplied constant change; PATCH appropriate (one number + one comment + one test rewrite per file; no API surface change)_ | 14-05 '26 |
| G2 — destructive action | _n/a — additive (relaxes the filter; nothing removed)_ | — |
| G3 — new decision rule | _n/a — applies the existing "/cuisine and /s share reach" implicit invariant_ | — |
| G4 — paid external API call | _n/a_ | — |

## Amendments

### `/api/cuisine/search` hard distance gate — 80 km → 120 km

> Prior (v0.57.8 / PR #125 → v0.60.151): `venues = venues.filter((v) => v.distanceM == null || v.distanceM <= 80000);` (`index.js`). 80 km covered SG + adjacent JB even from south SG.
>
> Change (v0.60.152): `… <= 120000`. Same reasoning, wider headroom — JB-side curated venues + the south-SG → JB-east coast band. Region-mode filters (`isJB` + `formattedAddress` match for "Johor Bahru") still pin JB-mode searches to actual JB venues (no Iskandar / Kulai / KL bleed).

### `free-text-search.js MAX_DISTANCE_M` — 80 km → 120 km

> Prior (v0.57.27 → v0.60.151): `const MAX_DISTANCE_M = 80000;`. The chat free-text dish-search filter capped at 80 km.
>
> Change (v0.60.152): `120000`. Matches the cuisine-TMA-side bump so a `/cuisine` search and a `/s` chat search return the same outer band. Header comment + test updated.

## Deleted

(No content deleted. Both bumps are in-place constant edits — recorded under Amendments above per AU-7.)
