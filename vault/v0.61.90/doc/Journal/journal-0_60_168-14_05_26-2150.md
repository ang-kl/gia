(№ 141 - 14-05 '26 21:50 SGT)

# Journal — soleat v0.60.168

> Operator: *"Update FR"* → after AskUserQuestion, chose two FR copy tweaks:
> 1. **Pet allowed**: FR `'Animaux acceptés'` (literally "accepted") → `'Animaux autorisés'` (literally "allowed") — closer to EN "Pet allowed" semantically.
> 2. **First-load overlay**: FR `'Veuillez patienter pendant le chargement de la liste…'` (53 chars, twice the EN's length) → `'Chargement de la liste…'` (24 chars) — matches EN brevity while preserving the "loading list" meaning.

Two-line copy tweak. EN strings untouched.

---

## New

### [HDR] #134 | 21:50:00 SGT 14-05-26 | v0.60.168 Cuisine TMA: FR copy tweaks — `'Animaux autorisés'` + shortened first-load overlay | 2 files, ~10 LOC net (mostly comments) | 1 PR |

- **[INTENT]** Operator review pass on FR strings shipped in v0.60.165/166: the petFriendly chip's FR translation drifted from the EN's "allowed" semantics, and the first-load overlay's FR copy was disproportionately long compared with the EN.

- **[DELTA]**

  **`web/cuisine/src/v2/lib/i18n.js`** — `filter.petFriendly`:

  *Before* (v0.60.166): `{ en: 'Pet allowed', fr: 'Animaux acceptés' }`
  *After* (v0.60.168): `{ en: 'Pet allowed', fr: 'Animaux autorisés' }`

  **`web/cuisine/src/v2/App.jsx`** — first-load overlay copy:

  *Before* (v0.60.166): `'Veuillez patienter pendant le chargement de la liste…'`
  *After* (v0.60.168): `'Chargement de la liste…'`

  EN copy in the same overlay (`'Please wait while loading list…'`) is unchanged. Inline comment added explaining the FR tightening rationale per AU-7.

  **`package.json`** — `0.60.167 → 0.60.168` (PATCH — copy correction).

  **`doc/Journal/journal-0_60_168-14_05_26-2150.md`** — this entry (#134).

  **`doc/.serial-state.yml`** — `journal 133 → 134`, `commit 140 → 141`; re-anchored to `14-05 '26 21:50 SGT`.

- **[VERIFICATION (sandbox)]**

  ```bash
  $ node --check index.js
  syntax OK
  $ npm test -- --run
   Test Files  57 passed (57)
        Tests  1757 passed (1757)
  $ (cd web/cuisine && npm run build)
   ✓ built in 3.03s
  ```

- **[STATUS]** STAGED → PROD on next Railway deploy after squash-merge.

- **[TEST]** Post-deploy verification (Telegram, FR locale):

  - Toggle TMA to FR via the LocaleToggle (or set Telegram client lang to FR).
  - Cuisine TMA → [⚙ Filters] popover → 🐾 chip label reads `Animaux autorisés` (was `Animaux acceptés`).
  - Active-filters strip below the criteria card: same `Animaux autorisés` chip when toggled on.
  - Cold-open Cuisine TMA in FR locale → first-load overlay reads `⏳ Chargement de la liste…` (was the 53-char full sentence).
  - EN locale unchanged: chip still reads `Pet allowed`; overlay still reads `⏳ Please wait while loading list…`.

- **[KNOWN GAPS]**

  - **DF-57 NEW** — broader FR audit deferred (operator declined audit option on this turn). The two strings tweaked here were the v0.60.165/166-era regressions; other FR strings remain at their last-reviewed wording. Re-open if operator notices more drift.
  - **DF-53 / DF-54 / DF-55 (carried)** — security hardening follow-ups documented in `/root/.claude/plans/unified-floating-truffle.md` (auth chokepoint for other TMAs / per-chatId rate limit / cloud-console budget caps). Operator hasn't picked one yet.
  - **DF-49 / DF-50 / DF-51 / DF-52 (carried)** — petFriendly strict/fallback fixture test; conditional fieldmask; LocationField no-auto-fire integration test; filter-key wiring lint.
  - **DF-44 → DF-48 (carried)** — earlier deferrals.

---

## Confirmation Gates Satisfied

| Gate | Authority | Stamp |
| :--- | :--- | :--- |
| G1 — version bump | _PATCH 0.60.167 → 0.60.168 (FR copy tweak; no behaviour change)_ | auto |
| G2 — destructive action | _n/a — copy edits only_ | — |
| G3 — new decision rule | _n/a_ | — |
| G4 — paid external API call | _n/a_ | — |

## Amendments

(No amendments. v0.60.167 (#133) sits directly before this entry. All prior entries intact.)

## Deleted

(Nothing deleted. The prior `'Animaux acceptés'` + `'Veuillez patienter pendant le chargement de la liste…'` strings are preserved verbatim in this entry's `[DELTA]` "Before" blocks per AU-7.)
