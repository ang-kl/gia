(№ 255 - 19-05 '26 23:00 SGT) v0.61.44

# Journal — soleat v0.61.43 → v0.61.44 (bus-stop popup redesign)

> Operator: *"Change the Bus Stop pin information when tap inside the Google Map, this
> affects 3 TMA"* — a proposed template: `🚏 <road name>` / `🚏 № <code>` / bucketed
> arrival rows / a Google-Maps link whose text is the road name. On the road-name vs
> stop-name ambiguity the operator chose **road name** (AskUserQuestion).
>
> Sequenced after PR #531 — this entry is commit 255 / journal 248 / build 56.

## New

### [HDR] #248 | 23:00:00 SGT 19-05-26 | v0.61.44 — bus-stop popup redesign | ~1 kb | 4 files | 1 PR | 1 build |

- **[INTENT]** Re-shape the bus-stop tap popup on all three TMA maps to the operator's
  template.

- **[DELTA]**
  - **`mapOverlays.js`** (byte-identical ×3) — `busInfoHtml` rewritten:
    - line 1 — `🚏 <road name>` (was the stop Description; per operator decision the
      road name is now the header);
    - line 2 — `🚏 № <code>` on its own line (was a combined `📍 road · distance ·
      № code` line — the distance is dropped);
    - the bucketed arrival rows (`№ … — ≤5/≤10/≤20/20+ min`) are unchanged
      (`busArrivalRows` already renders one bucket per line);
    - footer — a Google-Maps link whose **text is the road name** (`<road> ↗`),
      replacing `infoCard`'s generic `Google Map ↗` tail (`infoCard` is now called
      without the `gmaps` arg so it appends no tail of its own).
  - **`package.json`** — `0.61.43` → `0.61.44` (PATCH).

- **[VERIFICATION (sandbox)]**

  ```bash
  $ node --check web/transport/src/lib/mapOverlays.js  → OK
  $ npm run build:cuisine / build:hawker / build:transport  → all ✓ built
  $ npm test  → 65 files / 1928 tests green
  ```
  `mapOverlays.js` confirmed byte-identical ×3.

- **[STATUS]** DRAFT → PR #532 → **MERGED** to `main` (squash `9557c4f`, 19-05 '26).
  Deploys via the Railway auto-deploy on `main`.

- **[TEST]** Tap a bus-stop pin (overlay layer or station-detail) on any TMA map: the
  popup shows `🚏 <road name>`, `🚏 № <code>`, the live-arrival buckets, then a
  road-name hyperlink to Google Maps.

- **[KNOWN GAPS]** The road name comes from the LTA catalogue via the v0.61.43
  abbreviation expander, so it reads in full ("South Bridge Road"). When a stop has no
  `roadName` the header falls back to the Description, then `Stop <code>`. The 24 hours
  layer remains disabled; the `doc/Legal/` revision (journal #239) remains.

- **Confirmation Gates**

  | Gate | Resolution | Date |
  |------|-----------|------|
  | G1 — version bump | PATCH `0.61.43` → `0.61.44`, established 0.61.x cadence | 19-05 '26 |
  | G2 — destructive action | _n/a_ | — |
  | G3 — new decision rule | _n/a_ | — |
  | G4 — paid external API call | _n/a_ | — |

## Amendments

_None._

## Deleted

_None._
