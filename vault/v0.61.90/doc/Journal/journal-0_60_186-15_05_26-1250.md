(№ 160 - 15-05 '26 12:50 SGT) v0.60.186

# Journal — soleat v0.60.186

> Operator: *"I prefer the old PR with charts and advisory while keeping the latest additional."* Restore the 10 insight panels + Lessons block from the original `doc/VibeCodingRecord/generate.mjs` HTML output (stripped during the v0.60.177 tabbed rebuild) without losing the v0.60.180 rich PR cards + search + GFM table rendering.

## New

### [HDR] #153 | 12:50:00 SGT 15-05-26 | v0.60.186 — Vibe Journal: 📊 Dashboard tab restored (all 10 insight panels + Lessons block) | 5 files, ~370 LOC | 1 PR |

- **[INTENT]** Two coexisting Vibe Journal generators competed for `public/doc/vibe-journal.html`:
  - **OLD**: `doc/VibeCodingRecord/generate.mjs` (v0.60.143–v0.60.153) emitted the original dashboard — 10 `<details>` panels (Likely rework / Churn by feature / Churn by module / PRs per release / Category mix / 📈 PRs over time / 🪶 Small-PR batching / 🔁 Indecision / 🧠 Behavioural patterns / 🧩 Hard parts) + a 📚 Lessons ordered list.
  - **NEW**: `.vibe-journal/` (v0.60.177–v0.60.180) emitted a 5-row tabbed bundle with PR rich `<details>` cards, search box, GFM table rendering. Lost all the dashboard panels.
  - Each `npm run`/`vibe-journal regen` invocation overwrote the other.

  Operator preference: charts + advisory back, without losing the new tabbed-bundle additions. Solution: integrate the dashboard as a new tab inside the `.vibe-journal/` bundle so a single generator produces both.

- **[DELTA]**

  **`.vibe-journal/lib/render.mjs`** — tab layout + record duplication.
  - `TAB_LAYOUT[0]` now reads `['dashboard', 'pr', 'register', 'third-party']` (was `['pr', 'register', 'third-party']`). Dashboard surfaces as the first chip in the sidebar's first row so it's the default landing tab.
  - `TAB_LABELS.dashboard = '📊 Dashboard'`.
  - After parsing all records: `records.dashboard = records.pr`. Same array reference — the dashboard renderer enriches each row client-side rather than carrying a separately-parsed copy. Count: 392 PRs in the current snapshot.

  **`.vibe-journal/templates/app.js`** — new `renderDashboard(panel, prRecords)` function (~250 LOC) registered in `RENDERERS.dashboard`. Ports the entire instrument logic from `doc/VibeCodingRecord/generate.mjs:507–614`:
  - **Enrichment pass**: each PR record gets `category` (categoryOf), `area` (featureAreaOf), `version`/`minor` (versionOf), `day` (from `merged_at`), `modules` (codeBucketsOf), `nFiles` (from `files.length`). Then a rework-of pass back-references the previous LOOKBACK=8 PRs in the same feature area.
  - **6 KPI cards**: PRs total / Merged / Closed unmerged / Releases / Flagged rework / Follow-up-cue titles.
  - **10 instrument panels** (all sticky-collapsed; "Likely rework" sticky-open by default):
    1. Likely rework / "we shipped, then iterated" (heuristic) — fix + same-area-prior-PR, with a Core/Docs/Infra exclusion.
    2. Churn by feature / UX area — tallyBy(area).
    3. Churn by code area / module — tallyBy(modules), top 30.
    4. PRs per release (MAJOR.MINOR) — chronological per minor line.
    5. Category mix & same-day clusters — tallyBy(category) + days with ≥4 PRs.
    6. 📈 PRs over time — chronoTally by ISO week + by day.
    7. 🪶 Small / low-effort PRs — ≤2 files OR category in {copy, prompt-tune, test}; per-area tally + scrollable table.
    8. 🔁 Indecision — 10-regex set scanning title+body for revert / re-enable / "actually" / "not taking effect" / etc.
    9. 🧠 Behavioural patterns — 8 KPI cards (PRs/active day, busiest day, fix:feature ratio, rework rate, longest same-area streak, median files/PR, most-edited code area, small-PR share).
    10. 🧩 Hard parts & recurring failure modes — fix-density per area (≥3 PRs) + 7-regex keyword scan for failure-mode signals (silent handler, missing export, HTML escaping, fuzzy over-match, resolver ordering, cache/stale bundle, regression-after-merge).
  - **📚 Lessons block** — 9 advisory entries verbatim from generate.mjs (the gia-preflight SKILL.md distillation).

  All instruments computed lazily on tab activation; no extra round-trip — same PR record set as the PR tab.

  **`.vibe-journal/templates/style.css`** — new `.vj-dash*` / `.vj-kpi*` / `.vj-barlist` / `.vj-barrow` / `.vj-scrollbox` / `.vj-mini` / `.vj-lessons` / `.vj-dash-tag` block appended. No collisions with the existing `.vj-pr-*` / `.vj-status` namespaces. Picks up the existing CSS variables (`--vj-card`, `--vj-border`, `--vj-fg`, `--vj-muted`, `--vj-accent-soft`) so light/dark/mobile parity works without per-rule overrides.

  **`public/doc/vibe-journal.html` + `public/doc/vibe-journal.json`** — regenerated via:
  ```bash
  cd .vibe-journal && npm install && node bin/vibe-journal.mjs regen --config examples/soleat-deploy/config.yaml
  ```
  Manifest now lists Dashboard (count=392) as the first chip. JSON blob includes `data.dashboard = data.pr` (same array).

  **`package.json`** — `0.60.185 → 0.60.186` (PATCH; additive UI tab, no contract change).

  **`doc/Journal/journal-0_60_186-15_05_26-1250.md`** — this entry (#153).

  **`doc/.serial-state.yml`** — `journal 152 → 153`, `commit 159 → 160`, `feature 40 → 41`, `technical 36 → 37`; re-anchored `15-05 '26 12:50 SGT`.

- **[VERIFICATION (sandbox)]**

  ```bash
  $ node --check .vibe-journal/templates/app.js
  app.js syntax OK
  $ npm test -- --run
   Tests  1790 passed (1790)
  $ cd .vibe-journal && node bin/vibe-journal.mjs regen --config examples/soleat-deploy/config.yaml
   [vibe-journal] wrote /home/user/gia/public/doc/vibe-journal.json — 1089 total records
   [vibe-journal] wrote /home/user/gia/public/doc/vibe-journal.html — self-contained single-page bundle (inlined CSS + JS)
  $ node -e 'const j=require("./public/doc/vibe-journal.json"); console.log(j.manifest.tabs[0][0])'
   { key: 'dashboard', label: '📊 Dashboard', count: 392 }
  ```

- **[STATUS]** STAGED → PROD on next Railway deploy. The regenerated `public/doc/vibe-journal.html` ships with the deploy. The old `doc/VibeCodingRecord/generate.mjs` is **NOT** removed — it still produces `records.tsv` / `vibe-journal.json` (the source-of-truth ledger) and the Markdown view (`vibe-coding-record.md`), only the HTML output is now superseded by the bundled tab framework.

- **[TEST]** Post-deploy (browser, `https://soleat.net/doc/vibe-journal.html?key=<KEY>`):
  1. First chip in the sidebar reads **📊 Dashboard · 392**.
  2. Default landing tab is Dashboard. Panels render top-to-bottom: 6 KPI cards → 10 `<details>` insight panels → 📚 Lessons ordered list.
  3. "Likely rework" panel is sticky-open with the rework table populated.
  4. Other tabs (PR / Register / Technical / Feature / Legal / Vault / Journal / Builder / Persona / 3rd Party) unchanged — PR rich cards + search + GFM tables intact.
  5. Dark mode renders cleanly (KPI cards, bar lists, scrollboxes, tag chips all picked up via CSS variables).
  6. Mobile (≤760 px) reduces the barrow grid to 120 px so labels don't crowd.

- **[KNOWN GAPS]**
  - **DF-83 NEW** — the dashboard's PR snapshot is the same one the PR tab uses, which the `vibe-journal regen` step refreshes from GitHub. After today's session lands #434 + #435, regenerate once to sync the dashboard count.
  - **DF-84 NEW** — the OLD `doc/VibeCodingRecord/generate.mjs` HTML emission is now redundant. Two options: (a) delete the HTML write at line 669 (keep records.tsv + vibe-coding-record.md + JSON), (b) accept the duplication and stop running generate.mjs's HTML path. (a) is cleaner — track as a follow-up.
  - **DF-80** (UNRESOLVED) — Michelin "cannot refresh next 12 again". Carried from v0.60.185; awaiting Railway-log evidence from operator reproduction.
  - **DF-82** — Vibe Journal auth-hardening (timingSafeEqual + per-IP rate-limit) carried.
  - DF-V1 → DF-V4, DF-49 / DF-50 / DF-53 / DF-55 / DF-60 → DF-82 carried.

## Confirmation Gates

| Gate | Authority | Stamp |
|---|---|---|
| G1 — version bump | _PATCH 0.60.185 → 0.60.186 (additive Dashboard tab; the 10 panels + Lessons block restored as a new tab, no existing tab altered)_ | auto |
| G2 — destructive action | _none (additive only; the old generate.mjs HTML emission still exists, redundancy will be cleaned up in a follow-up per DF-84)_ | — |
| G3 — new decision rule | _Explicit operator rule: dashboards / charts / advisories MUST coexist with the rich PR-card view. Two generators competing for the same output is not acceptable — fold into one bundle._ | operator |
| G4 — paid external API call | _n/a — pure client-side computation; no API hit_ | — |

## Amendments

(None. v0.60.185 (#152) is the prior entry.)

## Deleted

(Nothing deleted. The `doc/VibeCodingRecord/generate.mjs` HTML emission still exists for safety; DF-84 tracks the redundancy clean-up.)
