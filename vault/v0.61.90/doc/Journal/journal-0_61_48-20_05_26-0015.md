(№ 259 - 20-05 '26 00:15 SGT) v0.61.48

# Journal — soleat v0.61.47 → v0.61.48 (clinic-hours coverage tuning + re-run)

> Operator: *"think how to improve first [to] 95%"* on the clinic-hours coverage gap
> (176/1193 clinics still without hours), chosen via AskUserQuestion as the next PR.
>
> Rides the same branch / PR as v0.61.47 (#536) — this entry is commit 259 /
> journal 252 / build 59.

## New

### [HDR] #252 | 00:15:00 SGT 20-05-26 | v0.61.48 — clinic-hours matching tuning + re-run | ~2 kb + data | 3 files | (PR #536) | no build (script/data) |

- **[INTENT]** Lift the clinic opening-hours coverage from 85 % toward 95 % by
  improving `fetch-clinic-hours.js`'s Places matching, then re-running.

- **[DELTA]**
  - **`scripts/fetch-clinic-hours.js`** — matching tuned: `MATCH_RADIUS_M` widened
    `140 → 280 m` (CHAS coordinates drift from the Places pin) and `BIAS_RADIUS_M`
    `200 → 350 m`; `lookupHours` now tries **progressively looser queries** — name +
    street + postal, then name + postal, then name alone — and accepts the first
    within-radius match that carries opening hours (was a single name + street query).
  - **`data/geo-clinics.json`** — re-run of the 176 unmatched clinics with the tuned
    matcher: **42 newly matched**, 134 still no Places hours, 0 errors. Coverage
    **1017 → 1059 / 1193 (85.2 % → 88.8 %)**.
  - **`package.json`** — `0.61.47` → `0.61.48` (PATCH).

- **[VERIFICATION (sandbox)]**

  ```bash
  $ node --check scripts/fetch-clinic-hours.js  → OK
  $ GOOGLE_MAPS_API_KEY=… node scripts/fetch-clinic-hours.js
      → 176 looked up — 42 with hours, 134 no match, 0 errors
  $ npm test  → 65 files / 1928 tests green
  ```

- **[STATUS]** DRAFT → PR #536 (base `main`, branch `claude/review-gia4lunch-readme-3s798`),
  bundled with the v0.61.47 popup-palette fix. Deploys via the Railway auto-deploy on
  `main` once merged.

- **[TEST]** Clinic map popups: 1059 of 1193 clinics now show today's `🕒` hours line.

- **[KNOWN GAPS]** **95 % is not reachable via Google Places.** The 134 still-unmatched
  clinics have no Places listing with published `regularOpeningHours` within 280 m of
  the CHAS coordinate — the data ceiling for this source is ~89 %. Closing the last
  gap would need a different source (e.g. operator-curated hours, or the CHAS GP
  directory if it ever publishes hours). The 24 hours layer remains disabled; the
  `doc/Legal/` revision (journal #239) remains.

- **Confirmation Gates**

  | Gate | Resolution | Date |
  |------|-----------|------|
  | G1 — version bump | PATCH `0.61.47` → `0.61.48`, established 0.61.x cadence | 20-05 '26 |
  | G2 — destructive action | _n/a_ | — |
  | G3 — new decision rule | _n/a_ | — |
  | G4 — paid external API call | clinic-hours re-run (~330 Places searchText calls) on the operator-supplied key — continuation of the journal #243 G4 decision | 20-05 '26 |

## Amendments

_None._

## Deleted

_None._
