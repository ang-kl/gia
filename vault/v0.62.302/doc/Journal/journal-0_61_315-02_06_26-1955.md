(№ 516 - 02-06 '26 19:55 SGT) v0.61.315

# Journal — soleat v0.61.315 (canonical feature spec @ PR #818 cutoff)

> Operator (02-06 '26, follow-up to the multi-step archaeology brief): commission a single canonical feature specification covering every merged PR ≤ #818, with zero invention, file:line traceability, and explicit `[UNVERIFIED]` tagging for any gap. Anchor: HEAD sha `207f1e63`, cutoff = PR #818 (v0.61.312). The gia-web project will audit against this spec.
>
> Delivered: `doc/Feature/feature-0_61_312-02_06_26-1745.md` (529-line canonical spec, 13 top-level sections, RF-1 → RF-22, §V audit) + `doc/Feature/_ledger-818.md` (797-row per-PR ledger, zero PENDING; 696 FOLDED + 101 SKIPPED-irrelevant). Sub-agent + post-audit AU-7 amendment pass resolved every initial `[UNVERIFIED]` row.
>
> Commit 529 / journal 516 / build 318 / feature 53, on branch `claude/feature-spec-pr818` off `main` at v0.61.314 (sha `207f1e63`).

## New

### [HDR] #516 | 19:55 SGT 02-06-26 | v0.61.315 — canonical feature spec @ PR #818 cutoff | 4 files (incl. 2 new docs) | 1 PR | 0 build |

- **[INTENT]** Establish a single canonical specification of soleat's shipped behaviour at the PR #818 / v0.61.312 cutoff. Mandate: zero invention; every claim traceable to a `file:line` symbol on main; explicit `[UNVERIFIED]` tag where source did not give a definitive answer. Companion ledger accounts for all 797 merged PRs ≤ #818.

- **[DELTA]**

  **1. `doc/Feature/feature-0_61_312-02_06_26-1745.md`** *(new, 529 lines, ~38 KB)*:
  - Serial header line 1: `(№ 53 - 02-06 '26 17:45 SGT)` per CLAUDE.md §5.
  - §0 — Document scope + invariants.
  - §A — Surfaces overview (bot, 5 TMAs, Express server, shared module list).
  - §B — Slash-command registry: 13 public commands (verbatim from `enCommands` / `frCommands` at `index.js:10807-10841`) + 17 hidden commands with handler `index.js:N` line numbers + owner-gating marks.
  - §C — Free-text chat pipeline (intent classifier, 5 intent kinds, guided assistance, `dish-name.js` guard, `pipeline.discover()` at `pipeline.js:947-956`).
  - §D — TMA Cuisine D.1 → D.9, including recents drawer (PR #811, `LocationField.jsx:103-321`), region pill flip on pick (PR #817, `App.jsx:1675-1742`), MAP_ID env wiring (PR #816, `MapPanel.jsx:122-205`).
  - §E — TMA Hawker.
  - §F — TMA Transport F.1-F.7 (Train, Bus, Carpark, Weather, station map, refresh control, status block order).
  - §G — TMA Menu (launcher hub).
  - §H — TMA Oversight (owner-only) + **new §H.1** documenting 5 live `/admin/*` HTTP routes (added in the AU-7 amendment pass — see below).
  - §I — Google-map micro-details I.1-I.11 (overlay chips, ~550m fixed overlay radius, station info card, pins, Exit Template, `attachAmenityPins`, map chrome, popup palette).
  - §J — Result rendering per surface (exact header strings, loading-overlay copy, zero-result behaviour).
  - §K — Data, APIs, geo-build scripts.
  - §L — Governance constraints carried in code (100%-non-inventive rule, owner gating, region no-SG-leak sentinel).
  - §M — Removed Features table: RF-1 → RF-18 carried verbatim from the prior master (`feature-0_61_83-21_05_26-1645.md`); RF-19 → RF-22 NEW in this pass (`/buddy` retired from menu, `/eat /drink /groceries` consolidated into `/cuisine`, RF-21 VOIDED via AU-7 with the live `/admin/*` route inventory in §H.1, RF-22 original RSS `/lunch` vibe sensor).
  - §V — Verification audit: V.1 lists items resolved in the post-audit pass; V.2 (residual `[UNVERIFIED]`) is empty.

  **2. `doc/Feature/_ledger-818.md`** *(new, 854 lines)*:
  - 797 PRs ≤ #818 enumerated via `gh pr list --state merged --limit 1000 --json …` filtered + sorted.
  - Resolution: 696 FOLDED into spec sections + 101 SKIPPED-irrelevant with one-line reason (74 doc-only, 14 vault-snapshot, 9 ci-only, 4 test-only).
  - Zero PENDING rows. Summary tables (by status, by spec section) at the foot.

  **3. `package.json`** — `0.61.314 → 0.61.315`.

  **4. `doc/.serial-state.yml`** — commit 528→529, build 317→318, journal 515→516, feature 52→53; re-anchored 02-06 '26 19:55 SGT.

- **[VERIFICATION (sandbox)]**

  - **Source-anchor sampling (8 keystone claims, all OK on main @ 207f1e63):**
    - `isOwnerChat(chatId)` at `index.js:3791` — env-unset → gate open
    - `setMyCommands` EN list at `index.js:10807-10825` (13 entries, no `/buddy`); FR mirror at `10826-10841` (12 entries)
    - `/hidden` owner-gate at `runSurpriseCommand` (`index.js:6289-6299`) + `runSurpriseCommandWithFreeText` (`index.js:6087-6097`) — PR #818
    - `/cost` owner-gate at `index.js:4799-4815` — PR #813
    - `pipeline.discover({...})` at `pipeline.js:947-956`; `PLACES_TEXT_URL` at `pipeline.js:334`
    - MAP_ID wire: `MapPanel.jsx:122, 132, 205` + `HawkerMapPanel.jsx:121, 144, 196` + `/maps-key` at `index.js:16167-16177` + boot warning at `11041-11046`
    - Region pill flip: `App.jsx:1675-1706` (sync flip) + `App.jsx:1717-1742` (persist with inferred region)
    - Live `/admin/*` routes: `index.js:11197, 11247, 16461, 16485, 16499`
  - **Post-audit AU-7 amendment pass** (02-06 '26 18:30 SGT, same day): chased the initial 2 `[UNVERIFIED]` rows.
    - §C.1 cooking-technique render symbol → RESOLVED. `findCookingMethodMatches() @ cooking-methods.js:1083` → `runCookingMethodFanOut() @ index.js:8509`, dispatched from chat callback at `index.js:3074`.
    - §M / RF-21 `/admin/*` mounts → RESOLVED **and the original RF-21 was factually WRONG.** `grep -nE "app\\.(get|post)\\([\\\"']/admin" index.js` returned 5 LIVE routes. RF-21 has been VOIDED in-place via AU-7 amendment (prior text preserved verbatim, correction appended); the live routes are now documented in §H.1.
    - RF-19 `/buddy` removal version → TIGHTENED from "≈ 12-05 '26" to **v0.60.113**, pinned by the in-source comment at `index.js:10815`: `// v0.60.113 — /buddy removed from the command menu (feature retired).`
  - **Ledger zero-PENDING check**: `awk … | sort | uniq -c` returned 696 FOLDED + 4 SKIPPED-status-rows (totalling 797). No PENDING.

- **[STATUS]** PR-OPEN on push. Railway redeploys at v0.61.315 with **zero runtime behaviour change** (no `.js`/`.jsx`/Express-route code touched). The bot serves the same surfaces; the doc-system version log + bot-version log stay synchronised.

- **[TEST]** Operator (post-merge):
  1. `git checkout main && git pull` → see `doc/Feature/feature-0_61_312-02_06_26-1745.md` + `doc/Feature/_ledger-818.md` on disk.
  2. Sanity-spot-check §B handler line numbers against current `index.js`.
  3. Sanity-spot-check §H.1's 5 `/admin/*` route lines (11197, 11247, 16461, 16485, 16499).
  4. Use spec as the audit baseline for the **gia-web** parallel project.

- **[KNOWN GAPS]**
  - **Depth on §D.4-§I.7 micro-details**: the sub-agent verified ~80 keystone `file:line` claims; another ~80 surface-level descriptive claims (e.g. exact emoji per badge, exact i18n string for each banner) lean on the prior v0.61.83 master via the AU-1 append-trace rather than fresh greps. A second-pass deep audit could nail these — flagged but not scheduled.
  - **No technical doc refresh**: this PR doesn't touch `doc/Technical/` — the last is `technical-0_61_271-30_05_26-0830.md` (untracked, never committed) which is independent of this spec.
  - **No vault snapshot**: deferred (current vault is `vault/v0.61.308/`; next due when the operator decides).
  - **Auto-merge after CI green** matches the docs-only-PR precedent (PR #812 v0.61.306, PR #819 v0.61.313).

- **Confirmation Gates**

  | Gate | Resolution | Date |
  |---|---|---|
  | G1 — version bump | PATCH `0.61.314 → 0.61.315`. Docs-only; no code change. | 02-06 '26 |
  | G2 — destructive action | _n/a_. Additive: two new doc files + version + counter bumps. RF-21 VOIDED in-place per AU-7 (text preserved verbatim, correction appended). | 02-06 '26 |
  | G3 — new decision rule | Operator-confirmed: the canonical spec is the audit baseline for the parallel **gia-web** project. The 100%-non-inventive constraint embedded in §L is the standing rule. | 02-06 '26 |
  | G4 — paid external API call | _n/a_. No upstream calls in this PR (`gh pr list` is GitHub API, free). | 02-06 '26 |

## Amendments

- **`doc/Feature/feature-0_61_312-02_06_26-1745.md`** — new canonical spec (529 lines). Supersedes-as-current-state-catalogue the prior `feature-0_61_83-21_05_26-1645.md` (which remains on disk per AU-1).
- **`doc/Feature/_ledger-818.md`** — new 797-row PR ledger.
- **`package.json`** — version `0.61.314 → 0.61.315`.
- **`doc/.serial-state.yml`** — counters + last_anchor_time re-anchored 02-06 '26 19:55 SGT.

## Deleted

_None._ Per AU-1 the prior master remains on disk; per AU-EOF the Removed table is append-only (RF-21 voided in-place, not deleted).
