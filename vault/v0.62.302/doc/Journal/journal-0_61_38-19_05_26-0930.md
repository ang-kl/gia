(№ 249 - 19-05 '26 09:30 SGT) v0.61.38

# Journal — soleat v0.61.37 → v0.61.38 (Colour nav button + dark-mode train link + Clinic label)

> Operator bug/UI batch: (1) the 🎨 Colour toggle wasn't working in the Cuisine TMA →
> move it out of the pill row into a nav-button (its own circular button above Reset);
> (2) the blue station hyperlink in `/transport` is hard to read in dark mode; (3) the
> `⚕️ Clinics` dropdown item becomes `💊 Clinic / Pharmacy`.
>
> Sequenced after PR #521 (v0.61.37 / commit 248) — this PR is commit 249 / journal 242.

## New

### [HDR] #242 | 09:30:00 SGT 19-05-26 | v0.61.38 — Colour nav button + dark-mode /transport link + Clinic/Pharmacy label | ~3 kb | 8 files | 1 PR | 1 build |

- **[INTENT]** Make the 🎨 Colour greyscale toggle reliable by giving it its own
  navigation button; stop the `/transport` nearest-station link washing out in
  Telegram dark mode; relabel the Clinics layer.

- **[DELTA]**
  - **`MrtMapPanel.jsx` / `HawkerMapPanel.jsx` / `MapPanel.jsx`** — 🎨 Colour is removed
    from the `MapControls` toggle-pill row and re-added as a **circular nav button**,
    the top item of the vertical nav stack (above Reset `⛶⟲`). It toggles
    `overlayLayers.colour` directly via `onOverlayChange`; `bg-tg-accent` while greyscale
    is on, `bg-white/70` otherwise; `text-lg` so the 🎨 glyph reads clearly.
  - **`MrtMapPanel.jsx` / `HawkerMapPanel.jsx` / `MapPanel.jsx`** — the `⋯/⋮` dropdown's
    Clinics row icon `⚕️` → `💊`.
  - **i18n** (`web/*/…i18n.js` ×3) — `layer.clinics`: `Clinics` → `Clinic / Pharmacy`
    (FR `Clinique / Pharmacie`).
  - **`i18n.js`** (root / bot) — `transport.train.stationRow` no longer wraps the whole
    station name in the `<a>` (Telegram colours link text with its theme link colour,
    which washes out in dark mode). The name is now plain text; a trailing `🗺️`
    glyph is the tappable Google-Maps link.
  - **`package.json`** — `0.61.37` → `0.61.38` (PATCH).

- **[VERIFICATION (sandbox)]**

  ```bash
  $ node --check i18n.js  → OK
  $ npm run build:cuisine / build:hawker / build:transport  → all ✓ built
  $ npm test  → green
  ```

- **[STATUS]** DRAFT → PR (base `main`, branch `claude/review-gia4lunch-readme-3s798`).

- **[TEST]** Each TMA map: the nav stack now reads 🎨 · ⛶⟲ · ＋ · － · expand;
  tapping 🎨 greys the base map (button turns accent). `/transport` nearest-station
  rows show the station code/name in normal text with a trailing 🗺️ link — readable
  in Telegram dark mode. The `⋯/⋮` dropdown shows `💊 Clinic / Pharmacy`.

- **[KNOWN GAPS]** Bus Stop + 24 hours remain disabled placeholders. The formal
  `doc/Legal/` record revision (journal #239) + map-controls Phase D remain.

## Amendments

(none)

## Deleted

(none)

## Confirmation Gates Satisfied

| Gate | Authority | Stamp |
| :--- | :--- | :--- |
| G1 — version bump (PATCH: 0.61.37 → 0.61.38) | _operator: 0.61.x PATCH sequence_ | 19-05 '26 |
| G2 — destructive action | _n/a — UI tweak_ | — |
| G3 — new decision rule | _n/a_ | — |
| G4 — paid external API call | _n/a_ | — |
