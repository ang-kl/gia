(№ 541 - 06-06 '26 19:44 SGT) v0.61.341

# Journal — soleat v0.61.341 (Vietnam Michelin — load the curated `instruction/Vietnam.js` into `VN-michelin.js`; 83 venues; VN manifest)

> Operator: *"vietnam"* → `instruction/Vietnam.js`. Same JP/KR shape: **2025 partial** (9 venues, one-star only, no Bib) vs **2026 full** (83) → **add as-is + flag**. Vietnam has **no two-/three-star** venues (one-star + Bib only). No native-name field. All 3 cities pre-mapped.
>
> **Left OPEN for review — auto-merge NOT armed.** Commit 554 / journal 541 / build 340, landed via the GitHub Git Data API on origin/main @ v0.61.340.

## New

### [HDR] #541 | 19:44 SGT 06-06-26 | v0.61.341 — Vietnam Michelin live: `VN-michelin.js` = 83 curated venues; VN manifest (9/83) | 2 code files | 1 PR | 1 build |

- **[INTENT]** v0.61.333 shipped `VN-michelin.js` as the empty stub. The operator's curated `instruction/Vietnam.js` holds the full venue-centric Vietnam dataset (**83 venues** across Ho Chi Minh City, Hanoi, Da Nang; 2025 + 2026 editions). This PR loads it **verbatim**, registers the VN manifest, leaves `instruction/Vietnam.js` as source-of-record.

- **[MECHANISM]** Bare array literal of 83 venue objects (venue-award-schema.v0_1, no locale fields). Copied **byte-for-byte** into `VN-michelin.js`. The loader validates each `city` resolves in `CITY_IATA`; **all 3 Vietnam cities (Ho Chi Minh City→SGN, Hanoi→HAN, Da Nang→DAD) were already mapped** in `cities.js`, `iata-cities.js`, and the loader — so, like Japan/Korea, **no city / IATA / `CITY_IATA` changes** were needed. Vietnam's guide has **no two-/three-star** venues (one-star + Bib Gourmand only) — reflected in the manifest.

- **[DELTA]**

  **1. `VN-michelin.js`** (root) — empty v0.61.333 stub → `const COUNTRY='VN'; const ENTRIES=[ …83 venues verbatim… ]; module.exports={COUNTRY,ENTRIES}`. Header → v0.61.341 (source = `instruction/Vietnam.js`; 3 cities; one-star/Bib-only note; 2025-partial manifest). 83 unique ids, 92 awards.

  **2. `michelin-data.js`** — `COUNTRY_MANIFEST` + `VN: { 2025:{one:9,total:9}, 2026:{one:11,bib:72,total:83} }`. 2025 omits Bib (none in source); both years omit two-/three-star (Vietnam has none).

  **3. `__tests__/michelin-data.test.js`** — removed `VN` from the empty-tables loop; **flipped the false-gate** (the v0.61.340 TW PR had used Hanoi/VN as the still-empty example — now non-empty → swapped to Shanghai/CN); added a full VN load suite (83 venues / 92 awards, 9@2025 / 83@2026, per-tier manifest `{one:9}` + `{one:11,bib:72}`, unique ids, every VN `city` curated, La Maison 1888 one-star both editions, Da Nang count 24), a `VN-michelin.js is venue-centric with 83 rows` require test, and a `hasMichelinData` VN-true case (VN / vn / HCMC / Hanoi / Da Nang).

  **4. `package.json`** — `0.61.340 → 0.61.341`.

  **`doc/.serial-state.yml`** — commit 553 → 554, journal 540 → 541, build 339 → 340; re-anchored 19:44 SGT 06-06-26.

- **[VERIFICATION (sandbox)]**
  - `node --check` clean: `VN-michelin.js`, `michelin-data.js`, `__tests__/michelin-data.test.js`.
  - Real-loader temp-harness (all 8 `{CC}-michelin.js` + patched `michelin-data.js` in `/tmp/vntest`): loads **without throwing**. `venuesForCountry('VN')===83`; global unique ids **1235/1235** (MY 70 + TH 180 + JP 589 + KR 117 + TW 196 + VN 83, no dup); VN 2025=9 / 2026=83; tiers `{one:9}` (2025) + `{one:11,bib:72}` (2026); `sum(VN awards)===92`. VN manifest passes. `hasMichelinData` true for VN/HCMC/Hanoi/Da Nang; false for Shanghai/CN.
  - **14/14** explicit assertions green.
  - Full `npm test` runs in CI on merge.

- **[STATUS]** PR-**OPEN**, **auto-merge NOT armed**. Landed via the GitHub Git Data API on a branch off origin/main @ v0.61.340. Railway redeploys on merge.

- **[TEST]** Operator: Vietnam is now in the venue loader — 83 venues across HCMC / Hanoi / Da Nang, the full 2026 edition (83) + the partial 2025 one-stars (9). With Vietnam loaded, only **China + Hong Kong/Macau** stubs remain empty. `instruction/Vietnam.js` stays source-of-record.

- **[KNOWN GAPS]**
  - **2025 is PARTIAL** — 9 venues, one-star only (no Bib). A fuller 2025 Vietnam list is **still due** when supplied.
  - **No native-name field** — none stored (source has none).
  - **Unified loader still off the live search path** — MY/TH/JP/KR/TW/VN via `michelin-data.js` not yet wired into the bot's Michelin search (SG standalone).
  - A `doc/Feature/` / `doc/Technical/` / `doc/Register/` update + a `vault/v0.61.341/` snapshot are still due.

- **Confirmation Gates**

  | Gate | Resolution | Date |
  |---|---|---|
  | G1 — version bump | PATCH `0.61.340 → 0.61.341`. | 06-06 '26 |
  | G2 — destructive action | None. Empty `VN-michelin.js` stub filled; append-only manifest + test edits. Curated `instruction/Vietnam.js` untouched. Loader clean (1235/1235 unique). | 06-06 '26 |
  | G3 — new decision rule | Vietnam Michelin loads from `instruction/Vietnam.js` (83 venues) VERBATIM; 2025 partial (9, one-star only) as-is + flagged; VN manifest 2025 9 / 2026 83 (one-star + Bib only, no two-/three-star). | 06-06 '26 |
  | G4 — paid external API call | _n/a_. Data load + manifest registration only. | 06-06 '26 |

## Amendments

- **`VN-michelin.js`** — empty stub → 83 curated venues from `instruction/Vietnam.js` (verbatim); header → v0.61.341.
- **`michelin-data.js`** — `COUNTRY_MANIFEST` +`VN` (2025 9 / 2026 83; one-star + Bib only).
- **`__tests__/michelin-data.test.js`** — VN removed from empty-tables loop; false-gate flipped Hanoi/VN → Shanghai/CN; +VN load suite + `VN-michelin.js` 83-row require test + `hasMichelinData` VN case.
- **`package.json`** — `0.61.340 → 0.61.341`.
- **`doc/.serial-state.yml`** — counters (commit 554 / journal 541 / build 340) + anchor re-set 19:44 SGT 06-06-26.

## Deleted

- _None._
