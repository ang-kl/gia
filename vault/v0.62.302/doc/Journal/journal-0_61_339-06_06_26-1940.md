(№ 539 - 06-06 '26 19:40 SGT) v0.61.339

# Journal — soleat v0.61.339 (Michelin chip — drop the 🇸🇬 flag from the label; just "Michelin, Bib Gourmand")

> Operator first asked for a **dynamic per-country flag** on the Michelin chip (🇸🇬🇲🇾🇯🇵🇹🇭🇭🇰🇹🇼🇰🇷🇻🇳🇨🇳, greyed when no list). On learning the live Michelin search is **SG-only** (the `michelin-data.js` MY/TH/JP/KR loader isn't wired to `handleMichelinSearch` yet, so a non-SG flag would tap into SG results), the operator **withdrew the request**: *"remove this request. just 'Michelin, Bib Gourmand' without the flag."*
>
> **Left OPEN for review — auto-merge NOT armed.** Commit 552 / journal 539 / build 338, landed via the GitHub Git Data API on origin/main @ v0.61.338.

## New

### [HDR] #539 | 19:40 SGT 06-06-26 | v0.61.339 — Michelin chip label: '🇸🇬 Michelin, Bib Gourmand' → 'Michelin, Bib Gourmand' | 1 code file | 1 PR | 1 build |

- **[INTENT]** Simplify the Michelin cuisine chip — remove the leading 🇸🇬 flag from its label so it reads just "Michelin, Bib Gourmand". (Supersedes the dynamic-flag idea, which is blocked on per-country search wiring — logged as a known gap.)

- **[MECHANISM]** The chip label is emitted by the catalogue builder in `index.js` (the `id:'michelin'` category) as `cat.label`. `CuisineDrawer.labelFor()` has **no** `CATEGORY_LABEL_KEY['michelin']` entry, so it renders `cat.label` verbatim (not an i18n string). Therefore stripping the flag from the server label is sufficient — **no TMA rebuild, no i18n change**. The chip emoji (✳️) and the `regionScope:'SG'` greying (chip disabled outside SG, since the curated list is Singapore-only) are **unchanged**.

- **[DELTA]**

  **`index.js`** (catalogue builder, `id:'michelin'` category) — both label strings updated:
  - `label: '🇸🇬 Michelin, Bib Gourmand'` → `label: 'Michelin, Bib Gourmand'`
  - `categoryLabel: '🇸🇬 Michelin, Bib Gourmand'` → `categoryLabel: 'Michelin, Bib Gourmand'`
  No other field touched (`emoji:'✳️'`, `flag:'✳️'`, `regionScope:'SG'`, `searchQuery`, `description` all unchanged).

  **`package.json`** — `0.61.338 → 0.61.339`.

  **`doc/.serial-state.yml`** — commit 551 → 552, journal 538 → 539, build 337 → 338; re-anchored 19:40 SGT 06-06-26.

- **[VERIFICATION (sandbox)]**
  - `node --check index.js` — clean (edit applied to current origin/main `index.js`).
  - Exactly **2** label occurrences replaced (asserted); the chip now renders "✳️ Michelin, Bib Gourmand".

- **[STATUS]** PR-**OPEN**, **auto-merge NOT armed**. Server-side catalogue change; Railway redeploys on merge (the `/api/cuisine/catalogue` response carries the new label — no client bundle change). Eyes-on-live: open the cuisine drawer in SG, confirm the chip reads "✳️ Michelin, Bib Gourmand" with no flag.

- **[TEST]** Operator: the Michelin chip now reads "Michelin, Bib Gourmand" (no 🇸🇬). It still greys outside Singapore (the list is SG-only today).

- **[KNOWN GAPS]**
  - **Dynamic per-country Michelin chip** — withdrawn for now; would require wiring `handleMichelinSearch` to `michelin-data.js` per detected country (MY/TH/JP/KR data exist but aren't on the live search path; VN/CN/HK/TW are empty). Revisit when the search path is country-aware.
  - **gia-web mirror** — if the browser port shows a Michelin chip, the same flag-strip should mirror there (separate repo; not touched here).
  - A `doc/Feature/` / `doc/Technical/` / `doc/Register/` update + a `vault/v0.61.339/` snapshot are still due.

- **Confirmation Gates**

  | Gate | Resolution | Date |
  |---|---|---|
  | G1 — version bump | PATCH `0.61.338 → 0.61.339`. | 06-06 '26 |
  | G2 — destructive action | None. Two label strings edited in the catalogue builder; greying + emoji + search behaviour unchanged. | 06-06 '26 |
  | G3 — new decision rule | The Michelin chip label carries no country flag — just "Michelin, Bib Gourmand". (Dynamic per-country flag withdrawn pending country-aware search.) | 06-06 '26 |
  | G4 — paid external API call | _n/a_. | 06-06 '26 |

## Amendments

- **`index.js`** — Michelin chip `label` + `categoryLabel`: dropped the leading `🇸🇬 ` (both occurrences).
- **`package.json`** — `0.61.338 → 0.61.339`.
- **`doc/.serial-state.yml`** — counters (commit 552 / journal 539 / build 338) + anchor re-set 19:40 SGT 06-06-26.

## Deleted

- _None._
