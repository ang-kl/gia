(№ 622 - 09-06 '26 14:45 SGT) v0.61.422

# Journal — soleat v0.61.422 (Country change COMMITS the capital — fixes "selected Manila but location shows Osaka")

> Operator (screenshot IMG_2350 — map = Manila, location field = "Osaka", Others pill not lit): *"I selected Manila but the location shows Osaka as the last location … change to durian in manila resulted in zero results. The mode isn't locked as Others."*
>
> Commit 635 / journal 622 / **build 403**. web/cuisine. Revert-ready. Depends on v0.61.421 (region re-assert, already on main).

## New

### [HDR] #622 | 14:45 SGT 09-06-26 | v0.61.422 — country-change auto-pick commits (not previews) the capital | LocationField.jsx + App.jsx | 1 PR | 1 build |

- **[INTENT]** Switching country must make the new country's **capital the active location** — field, map, **and** search — not just fly the map.

- **[ROOT CAUSE]** The v0.61.418 country-change auto-pick used **`cityPreview: true`**, which **only flies the map** (`onLocationSelect` preview branch returns at `:2195`, **no commit**). So switching Japan → Philippines flew the map to Manila but left **`locationAnchor` + the search on Osaka (Japan)**. A "durian in Manila" search actually ran at **Osaka** → Japan is **not** in the durian belt → **0 results**. The field showed the committed anchor ("Osaka") and the OTHER pill read stale.

- **[DELTA]** (`web/cuisine/`)
  - **`LocationField.jsx`** — the country-change auto-pick now **commits** the capital: `onSelect({ noAutoFire: true, fly: true, silent: true, … })` instead of `cityPreview`. The committed branch sets `locationAnchor` (field → Manila) + `searchCenter` + the region-sync (`coordsToCountry(Manila)=null` → region **stays OTHER**) + persists the set-location; its `!noAutoFire` gate keeps the **search from firing** ("don't fire yet").
  - **`App.jsx`** — new **`fly`** flag: MapPanel only **pans on `flyTo`** (a `searchCenter` change alone doesn't move it — `MapPanel.jsx:351` is the only pan effect besides the focus pin), so the committed branch `setFlyTarget(p)` when `p.fly`. New **`silent`** flag: `notify: !p.silent`, so the auto-commit **persists without firing** the v0.61.412 *"Search area set to …"* chat message (no spam on a country browse; manual 🔍 picks still notify).
  - **`package.json`** `0.61.421 → 0.61.422`; **`doc/.serial-state.yml`** commit 634 → 635, journal 621 → 622, **build 402 → 403**; re-anchored 14:45 SGT.

- **[VERIFICATION (sandbox)]** Cuisine **vite build green**. Full suite **3131/3131 green**.

- **[STATUS]** PR-**OPEN**, **auto-merge armed (squash)**. Railway rebuilds the TMA on deploy.

- **[TEST]** Operator: region **Others → Japan**, then switch to **Philippines** → the location field shows **Manila**, the map flies to Manila, the **OTHER pill stays lit**, **nothing searches** until 🔍 — and a **durian** search now runs in **Manila (PH, in the belt)** with results, not 0.

- **[KNOWN GAPS]**
  - The 5 issues' fixes (incl. this) **stack** — this one + v0.61.421 (region re-assert) + v0.61.416 (durian fallback) together fix the Manila durian-0. They need to be deployed together.
  - The set-location now **persists** the capital on a country switch (silently) — consistent with v0.61.407 set-location-authoritative; if the operator wants country switches to NOT change the set-location until 🔍, that's a follow-up.
  - Eyes-on-deployed pending.

- **Confirmation Gates**

  | Gate | Resolution | Date |
  |---|---|---|
  | G1 — version bump | PATCH `0.61.421 → 0.61.422`. | 09-06 '26 |
  | G2 — destructive action | Changes the country-change auto-pick from preview → commit; adds 2 opt-in flags. Reversible. | 09-06 '26 |
  | G3 — new decision rule | A country switch commits the capital as the active location (field + map + search), still no fire. | 09-06 '26 |
  | G4 — paid external API call | None (still no search until 🔍). | 09-06 '26 |

## Amendments

- **`web/cuisine/src/v2/components/LocationField.jsx`** — country-change auto-pick → `noAutoFire + fly + silent` commit.
- **`web/cuisine/src/v2/App.jsx`** — `fly` → `setFlyTarget`; `silent` → `notify: !p.silent`.
- **`package.json`** — `0.61.421 → 0.61.422`.
- **`doc/.serial-state.yml`** — counters (commit 635 / journal 622 / build 403) + anchor 14:45 SGT 09-06-26 + `last_anchor_source` prepend.

## Deleted

- The v0.61.418 "country change only flies the map (preview)" behaviour (superseded by the commit).
