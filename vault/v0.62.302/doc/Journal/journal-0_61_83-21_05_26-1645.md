(№ 289 - 21-05 '26 16:45 SGT) v0.61.77 → v0.61.83

# Journal — soleat v0.61.77 → v0.61.83 (six-version session catch-up: v0.61.78–v0.61.83)

> Catch-up pass for the six version bumps shipped this session. Journal
> #277 (`journal-0_61_77-…-1111.md`) + #281 (`journal-0_61_77-…-1355.md`)
> covered through v0.61.77; the six entries below run journal
> **#282–#287** / commit **289–294** / build **89–94**.
>
> **No PRs.** Every change this session was committed **direct to `main`**,
> not via a pull request:
> - **v0.61.78** landed through the operator's `"done HH:MM"` auto-commit
>   workflow (commit `09a246b`, message `"done 2_33 PM"`).
> - **v0.61.79** and the four CR-refactor phases (**v0.61.80–v0.61.83**)
>   were committed direct by Claude Code at the operator's instruction
>   (*"please proceed auto build and merge until they are done"*).
> The standing per-PR Journal rule (decision-gate G3, `journal-0_60_144`)
> is honoured here in spirit — shipped changes recorded — with `[STATUS]`
> citing the `main` commit SHA in place of a PR number.
>
> **Arc:** #282 (map-controls reorder) and #283 (Cuisine Michelin/FAB)
> are standalone operator UI requests; **#284–#287 are Phases A–D of the
> `UI/CR_Google_Map_Design_1022.MD` refactor** (CR-1…CR-7). CR-4 (adaptive
> bus pins) was already implemented pre-arc — verified only, no entry.
>
> A seventh `main` commit, `39456da` (`chore:` — see #282), removed a
> stale stray file the v0.61.78 auto-commit had swept in; it carries no
> version bump or build and is folded into #282 rather than counted.

## New

### [HDR] #282 | 14:34:00 SGT 21-05-26 | v0.61.78 — map controls: ⋯/⋮ overflow menu first, Monochrome after | ~2 kb | 4 files | commit 09a246b | 1 build |

- **[INTENT]** Operator: *"move ⋯/⋮ to the top left as the first button,
  'Monochrome' comes after ⋯/⋮"*.

- **[DELTA]**
  - **`MapControls.jsx`** (byte-identical across `web/transport`,
    `web/cuisine`, `web/hawker`) — the floating in-map control row is
    reordered. Was `Colour pill → ⋯/⋮ menu → layer toggles`; now
    `⋯/⋮ menu → Colour pill → layer toggles`. The ⋯/⋮ overflow-menu
    (white square button) is the leftmost control; the Colour-mode
    ("Monochrome") pill renders directly after it; the Train Line /
    Carpark / Bus Stop toggle pills follow. The dropdown stays
    `left-0`-anchored below the now-leftmost button. Header + inline
    comments updated.
  - **`package.json`** — `0.61.77` → `0.61.78` (PATCH).

- **[VERIFICATION (sandbox)]** `node --check` n/a (`.jsx`); all three
  `web/` Vite builds clean; Vitest 1928/1928.

- **[STATUS]** PROD — committed to `main` as `09a246b` by the operator's
  `"done HH:MM"` auto-commit workflow (commit message `"done 2_33 PM"`).
  The same auto-commit also swept in an untracked stray file,
  `scripts/build-station-info 2.js` — a stale iCloud conflict-duplicate
  of `scripts/build-station-info.js` (dated a day older, missing the
  v0.61.77 SBS deep-links work). It was removed in the immediately
  following `chore` commit `39456da`.

- **[TEST]** Open any TMA map → the ⋯/⋮ menu is the first (leftmost)
  control; the Monochrome pill sits directly to its right.

- **[KNOWN GAPS]** `doc/Feature/`, `doc/Technical/`, `doc/Register/`
  updates and a `vault/<v>/` snapshot are still due for the whole
  v0.61.78–v0.61.83 arc (logged once here for the arc).

- **Confirmation Gates** — G1: PATCH `0.61.77`→`0.61.78` (21-05 '26).
  G2: the follow-up `chore` `39456da` `git rm`'d `scripts/build-station-info 2.js`
  — a destructive file deletion, but the file was a stale conflict-dupe
  with no unique content; operator-confirmed before removal. G3/G4: _n/a_.

### [HDR] #283 | 14:52:54 SGT 21-05-26 | v0.61.79 — Cuisine: Results (n/130) header, 3 s FAB flash + arrow, drop search-limit toast | ~3 kb | 3 files | commit d86b6f4 | 1 build |

- **[INTENT]** Operator: *"there are over 130 listed in Michelin table …
  state in the first result as 'Results (12/130)' then flash the '🔍' FAB
  3 seconds. Remove function and message of this pop up 'ℹ️ Google search
  limit · tap 🔍 again or add a criterion.' Flash a Arrow pointing at the
  '🔍' FAB"*.

- **[DELTA]**
  - **`web/cuisine/src/v2/components/ResultPanel.jsx`** — new `totalCount`
    prop. The result header reads `Results (12/130)` (current batch /
    curated-pool total) when set; plain `Results (12)` otherwise.
  - **`web/cuisine/src/v2/App.jsx`** — passes `michelinRemaining.total`
    (~130) as `totalCount`; the post-result `🔍` Search-FAB flash window
    extended **2 s → 3 s**; a flashing `👉` arrow added immediately left
    of the FAB for that 3 s window; the `ℹ️ Google search limit · tap 🔍
    again…` toast removed in full — its `searchTipShow` state, the
    `tipFirstShownRef` guard, the trigger in the search handler, and the
    rendered EN/FR toast.
  - **`package.json`** — `0.61.78` → `0.61.79` (PATCH).

- **[VERIFICATION (sandbox)]** Cuisine Vite build clean; Vitest 1928/1928.

- **[STATUS]** PROD — committed direct to `main` as `d86b6f4` (no PR).

- **[TEST]** Michelin search → header reads `Results (n/130)`; the 🔍 FAB
  pulses ~3 s with a 👉 arrow beside it; no Google-limit toast appears.

- **[KNOWN GAPS]** The header numerator is the current batch
  (`venues.length`), not a cumulative count. The 3 s flash + arrow fire
  on every result load, not Michelin-only — intentional (the flash
  already fired on every load pre-change; Michelin-gating it would
  regress non-Michelin searches).

- **Confirmation Gates** — G1: PATCH `0.61.78`→`0.61.79` (21-05 '26).
  G2: removal of the search-limit toast is operator-requested code
  removal (*"Remove function and message of this pop up"*), no file/data
  deletion — confirmed by the instruction itself. G3/G4: _n/a_.

### [HDR] #284 | 15:22:46 SGT 21-05-26 | v0.61.80 — CR-3: polyline depth-mapping constant retune | ~1 kb | 4 files | commit bcb58fa | 1 build |

- **[INTENT]** **CR-3** of `UI/CR_Google_Map_Design_1022.MD` — retune the
  shared transit-line polyline opacity constants. Phase A of the refactor.

- **[DELTA]**
  - **`mapOverlays.js`** (byte-identical ×3) — in `applyVisibility`:
    zoomed-out opacity (`z<15`) `0.5` → `0.2`; zoomed-in (`z≥15`) `0.85`
    → `0.95`; muted base lines (a station selected) `0.25` → `0.1`;
    stroke weights unchanged (`4`). The polyline creation default was
    also bumped `0.85` → `0.95` to match the zoomed-in target before
    `applyVisibility` first runs. The search-emphasis case (`0.35`/`3`)
    is outside the CR-3 table and left as-is.
  - **`package.json`** — `0.61.79` → `0.61.80` (PATCH).

- **[VERIFICATION (sandbox)]** All three `web/` builds clean; Vitest
  1928/1928. CR-3's "Previous Value (v0.61.73)" column matched the live
  constants exactly — no drift, the table applied verbatim.

- **[STATUS]** PROD — committed direct to `main` as `bcb58fa` (no PR).

- **[TEST]** Transport map — train lines render fainter zoomed out,
  crisper zoomed in; base lines near-invisible when a station is selected.

- **[KNOWN GAPS]** _None._

- **Confirmation Gates** — G1: PATCH `0.61.79`→`0.61.80` (21-05 '26).
  G2/G3/G4: _n/a_ (constant retune; no deletion, no new rule, no paid API).

### [HDR] #285 | 15:26:28 SGT 21-05-26 | v0.61.81 — CR-1 + CR-2: droplet venue pin, delete centre-station zoom bypass | ~2 kb | 5 files | commit b2848ca | 1 build |

- **[INTENT]** **CR-1 + CR-2** of `UI/CR_Google_Map_Design_1022.MD`.
  Phase B of the refactor.

- **[DELTA]**
  - **CR-1** — **`web/cuisine/src/v2/components/MapPanel.jsx`**:
    `cuisinePinNode` changes from a round circle to a droplet/teardrop —
    a 24 px box with three rounded corners + one sharp corner, rotated
    45° so the sharp tip points down at the venue coordinate. The 1-based
    rank number rides in a counter-rotated inner `<span>` so it stays
    upright; the white border + drop shadow are retained. (Note: CR-1's
    text called the prior pin "square" — it was in fact a circle; the
    droplet goal is unaffected.)
  - **CR-2** — **`mapOverlays.js`** (byte-identical ×3): the
    centre-station zoom-bypass in `applyVisibility` is deleted. A
    selected/centre station no longer force-renders as a pill below
    z15 — it strictly obeys the `z=15` threshold (9 px line-coloured
    square below, labelled pill above) like every other station. The
    now-unused `centreName` / `newCentre` / `isCentre` bindings were
    removed. `squareStationNode` (9 px, `codeHex` fill) and
    `stationPillNode` (verbatim `"<name> station"`) already met the
    CR-2 spec — unchanged.
  - **`package.json`** — `0.61.80` → `0.61.81` (PATCH).

- **[VERIFICATION (sandbox)]** All three `web/` builds clean; Vitest
  1928/1928.

- **[STATUS]** PROD — committed direct to `main` as `b2848ca` (no PR).

- **[TEST]** Cuisine map — venue pins render as numbered teardrops.
  Transport map — tap a station while zoomed out → it stays a 9 px
  square (no pill exception).

- **[KNOWN GAPS]** _None._

- **Confirmation Gates** — G1: PATCH `0.61.80`→`0.61.81` (21-05 '26).
  G2: CR-2 removed the bypass code within a refactor — no file/data
  deletion → _n/a_. G3/G4: _n/a_.

### [HDR] #286 | 16:10:44 SGT 21-05-26 | v0.61.82 — CR-5 + CR-6: adaptive exit pins, force-render focus targets | ~6 kb | 4 files | commit 4b3792b | 1 build |

- **[INTENT]** **CR-5 + CR-6** of `UI/CR_Google_Map_Design_1022.MD`.
  Phase C of the refactor.

- **[DELTA]** **`mapOverlays.js`** (byte-identical ×3):
  - **CR-5** — station-exit overlay markers become dual-state, mirroring
    the bus-stop pins. New `exitTextNode()` builds a compact
    bare-identifier node (just `A` / `12`, with a white text-halo for
    legibility on the greyscale base); at/above z15 the marker swaps to
    the full white `Exit <code>` card (`amenityLabelNode`).
    `buildExitMarkers` now returns `{ compact, full, _exit:true }`;
    `applyVisibility` swaps `marker.content` for `_exit` items alongside
    `_bus`; the `zoom_changed` listener re-applies the `exits` layer so
    the swap tracks zoom.
  - **CR-6** — `__giaStationFocus` (pan + `flashPin` halo + `setZoom(17)`)
    retained as-is. New `showStationExits()` mirrors `showStationBusStops()`
    — a station tap force-renders that station's exits as `Exit <code>`
    pins **even when the Exit overlay toggle is off**, so the card's
    `Exit #` links always have a pin to highlight. `clearStationExitPins()`
    wired into `exitStationDetail`, `closeInfo`, `destroy`, and the
    exits-layer-on dedup path.
  - **`package.json`** — `0.61.81` → `0.61.82` (PATCH).

- **[VERIFICATION (sandbox)]** All three `web/` builds clean; Vitest
  1928/1928.

- **[STATUS]** PROD — committed direct to `main` as `4b3792b` (no PR).

- **[TEST]** Transport map — exit pins show a bare identifier zoomed out,
  an `Exit A` card zoomed in; tap a station with the Exit layer off →
  its exit pins appear and a tapped `Exit #` link flashes its pin.

- **[KNOWN GAPS]** CR-6's `showStationExits` force-renders **all** of the
  station's exit pins on a station tap (matching the pre-existing
  `showStationBusStops` behaviour); if the CR intended only the single
  tapped target to appear, that is a narrowing — flagged to the operator.

- **Confirmation Gates** — G1: PATCH `0.61.81`→`0.61.82` (21-05 '26).
  G2/G3/G4: _n/a_.

### [HDR] #287 | 16:16:10 SGT 21-05-26 | v0.61.83 — CR-7: standardised station info card | ~9 kb | 4 files | commit cb50143 | 1 build |

- **[INTENT]** **CR-7** of `UI/CR_Google_Map_Design_1022.MD` — standardise
  the train-station info card. Phase D (final) of the refactor.

- **[DELTA]** **`mapOverlays.js`** (byte-identical ×3) —
  `stationInfoCardHtml` is restructured:
  - **Header leads the card.** Single-line station: code pill ·
    `"<Name> Station"`, then line code/name. Interchange: one combined
    row of every line's code pill, then `"<Name> Station"` once.
  - **Exits moved up**, directly under the header. Rendered as a row of
    tappable `Exit X` labels; tapping a label toggles a detail row below
    it (`Exit # · <street> · Bus Stop №` — `Exit #` and `Bus Stop №`
    remain `__giaStationFocus` map-focus links). Detail rows are
    collapsed by default — an inline `onclick` toggles a hidden per-exit
    `<div>` (the InfoWindow content is live DOM, so `getElementById`
    resolves).
  - **Per-line detail block(s)** follow: `More Info ↗` + First/Last
    Train. An interchange repeats each line's code pill · name · line
    above its block; a single-line station already showed those in the
    header, so its block carries just More Info + First/Last.
  - **Footer** unchanged: deduped Operator(s) + `Google Map ↗`.
  - **`package.json`** — `0.61.82` → `0.61.83` (PATCH).

- **[VERIFICATION (sandbox)]** All three `web/` builds clean; Vitest
  1928/1928. Completes CR-1…CR-7 (CR-4, adaptive bus pins, was already
  implemented — verified only).

- **[STATUS]** PROD — committed direct to `main` as `cb50143` (no PR).

- **[TEST]** Transport map — tap a station → header, then the Exits row;
  tap `Exit A` → its detail row expands/collapses; an interchange shows
  the combined code-pill header, then per-line blocks.

- **[KNOWN GAPS]** The click-to-expand toggle is inline `onclick` DOM
  logic — not unit-testable; a manual check in the running Transport TMA
  is owed. `UI/Google_Map_Design.MD` (the map-layer reference doc) now
  describes pre-refactor v0.61.73 behaviour and is due a regeneration.

- **Confirmation Gates** — G1: PATCH `0.61.82`→`0.61.83` (21-05 '26).
  G2/G3/G4: _n/a_.

## Amendments

_None._

## Deleted

_None._
