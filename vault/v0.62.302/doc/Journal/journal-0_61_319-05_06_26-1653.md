(№ 520 - 05-06 '26 16:53 SGT) v0.61.319

# Journal — soleat v0.61.319 (/hidden: render the SAME rich venue card as /search + 📝 latest-review-date, drop 💎 Why a gem)

> Operator (05-06 '26): make `/hidden` render the **same rich venue card** as `/search` free-text
> (copy / copy-to), **PLUS** a `📝` latest-review-date line, and **DROP** the "💎 Why a gem" prose
> line — the LLM can invent it, so remove the only fabricable field.
>
> Why: `/hidden`'s old card was Gemini's free text (rating + criteria + "Why a gem" prose), rewritten
> by `hidden-verify`. The "Why a gem" line is the one field with no API ground truth — it is whatever
> Gemini asserts. The fix stops sending Gemini's prose entirely and instead builds the proven
> `formatTechniqueVenueBlock` card (the shared `/search` formatter) from the live Places fields the
> verifier already fetches, adding a `📝 Latest review · DD MMM YYYY` line from the newest review
> timestamp. The Gemini-surfaced dish ("🍲 Try ·") is carried through — it is not the fabricable field.
>
> Commit 533 / journal 520 / build 320, on branch `claude/v0.61.319-hidden-rich-card` off `main` at v0.61.318.

## New

### [HDR] #520 | 16:53 SGT 05-06-26 | v0.61.319 — /hidden renders the shared rich card + 📝 latest review, − 💎 Why a gem | 5 files | 1 PR | 0 build |

- **[INTENT]** `/hidden` cards should look exactly like `/search` free-text result cards (same `formatTechniqueVenueBlock`: 🍽️ type, 📇 area, hours, 🌐, 📞, 🌟 rating, travel time, 🍲 Try, 📍 maps, 📱 socials, Michelin/Healthier/building appenders), PLUS a `📝 Latest review · <date>` line, and WITHOUT the fabricable `💎 Why a gem` prose.

- **[DELTA]**

  **1. `hidden-verify.js`** — the Places re-verification layer that already runs on every `/hidden` pick:
  - `FIELD_MASK` (`:36`): appended `places.googleMapsUri, places.primaryType, places.primaryTypeDisplayName, places.regularOpeningHours.weekdayDescriptions, places.websiteUri, places.nationalPhoneNumber, places.priceLevel, places.editorialSummary` — all ride the EXISTING `places:searchText` verifier call (no new request).
  - New `newestReviewIso(reviews)` helper: MAX `publishTime` across the returned reviews as an ISO string; `null` when none parseable (mirror of the existing `oldestReviewMonths`).
  - `lookupVenue` return object now carries: `newestReviewIso`, `googleMapsUri`, `primaryType`, `primaryTypeDisplayName`, `weekdayDescriptions`, `websiteUri`, `nationalPhoneNumber`, `priceLevel`, `editorialSummary`. `oldestReviewMonths` + the newness-refute logic are unchanged.
  - Exported `newestReviewIso` for unit tests.

  **2. `index.js`** — new renderer + wired both `/hidden` paths:
  - New `async function renderHiddenVenueCards(chatId, lang, verifiedVenues, anchorLat, anchorLng, verifiedText)` (before `runSurpriseCommandWithFreeText`):
    1. Parses the Gemini-surfaced "🍲 Try ·" / "Order this" dish per venue (via `hidden-verify.parseBlocks`, matched to `displayHeading` case-insensitively).
    2. Maps each verified venue → a venue object shaped for `formatTechniqueVenueBlock`, mirroring `searchVenuesByDish`'s key names (`name`/`placeId`/`area`/`restaurantType` via `humaniseRestaurantType`/`rating`/`lat`/`lng`/`weekdayDescriptions`/`websiteUri`/`phone`/`priceLevel`/`url`).
    3. Best-effort `enrichTravelTimes(anchor, venues)` + `attachSocialsToVenues(redis, venues, {concurrency:4})` (same surfaces as `/search`; try/catch each).
    4. Renders each card via `formatTechniqueVenueBlock(... googleMapsUrlFn: require('./maps-url').googleMapsUrl, dishPhrase ...)`, then splices a `📝 Latest review · DD MMM YYYY` line (i18n label, `Asia/Singapore` date) right after the `🌟` rating line. Skips the line when no review date.
    5. **No `💎 Why a gem`** — Gemini's prose is no longer sent; only built cards.
    6. Packs cards into ≤3800-char messages on card boundaries and `safeSend`s each with `{parse_mode:'HTML', disable_web_page_preview:true}`. (Deliberately NOT routed through `chunkHiddenGemsOutput` — that escapes/strips/re-bolds raw Gemini text and would double-escape the already-HTML cards.)
  - **GPS path** (was `:6683` chunk loop): replaced with `renderHiddenVenueCards(chatId, lang, verifiedVenues, cached.lat, cached.lng, verifiedText)`. Tier ladder, `allDropped → hidden.allClosed`, distance-rewrite step, and the one-map button (reads `verifiedVenues`, unchanged) all KEPT.
  - **Free-text path** (was `:6249` chunk loop): replaced with `renderHiddenVenueCards(..., geo.lat, geo.lng, verifiedText)`. Same surrounding logic KEPT.

  **3. `i18n.js`** — added `hidden.latestReviewLabel` (EN `📝 Latest review ·`, FR `📝 Dernier avis ·`).

  **4. `package.json`** — `0.61.318 → 0.61.319`.

  **5. `doc/.serial-state.yml`** — commit 532→533, journal 519→520; re-anchored 05-06 '26 16:53 SGT.

  Plus **`__tests__/hidden-verify.test.js`** — 4 new `newestReviewIso` cases.

- **[VERIFICATION (sandbox)]**
  - `node --check index.js hidden-verify.js i18n.js __tests__/hidden-verify.test.js` — clean.
  - `npx vitest run __tests__/hidden-verify.test.js __tests__/hidden-gems.test.js --no-file-parallelism --pool=forks --poolOptions.forks.singleFork` — **96/96** pass (hidden-verify **50/50** incl. 4 new `newestReviewIso` cases; hidden-gems **46/46**).
  - Full local suite is known-flaky under iCloud (hangs / transient `ERR_INVALID_PACKAGE_CONFIG`); CI (GitHub Actions) is the authoritative full-green gate.

- **[STATUS]** PR-OPEN on push; auto-merge squash. Railway redeploys on merge. Effect is immediate on the live (pipeline-enabled) `/hidden` path, both GPS and free-text.

- **[TEST]** Operator (post-merge, owner chat):
  1. `/hidden <area>` and `/hidden` (GPS) → each pick renders the `/search`-style rich card (🍽️/📇/hours/🌟/🚊/🍲/📍/📱…), shows a `📝 Latest review · <date>` line, and shows NO `💎 Why a gem`.
  2. Long-press a card → copy / copy-to behave like a `/search` card.
  3. The one-map button still appears for ≥2 picks.

- **[KNOWN GAPS]**
  - **OMITTED card fields:** `footfall` (BestTime), `priceRangeDisplay`/`🐾 allowsDogs`, and the `💬` translated-review quote are NOT populated — they need pre-resolved fields/extra enrichers not on the verifier call. `formatTechniqueVenueBlock` skips absent fields, so those rows simply don't render (no new paid API call was added, per the constraint). Price LEVEL ($–$$$$) IS carried via `priceLevel`.
  - **`editorialSummary`** is fetched + mapped but `formatTechniqueVenueBlock` has no editorial-summary row, so it is currently unused (harmless; available if a row is added later).
  - **Card-build not unit-tested end-to-end:** `renderHiddenVenueCards` lives inside `index.js` (a side-effecting bot entrypoint, not exported anywhere), so the "(a) has 📝 Latest review, (b) has no 💎 Why a gem, (c) reuses formatTechniqueVenueBlock" assertions are verified by construction + the pure `newestReviewIso` tests rather than a rendered-string test. `formatTechniqueVenueBlock` has no "Why a gem" concept by definition.
  - **Gemini prompt unchanged:** `gemini-client.js` still tells Gemini to emit "💎 Why a gem" — left as-is (we simply stop rendering it; `parseBlocks` does not depend on that line). No prompt edit needed.
  - **`doc/Feature/` canonical spec** still due an AU-7 amendment recording the card-parity change. Logged here; next doc pass.

- **Confirmation Gates**

  | Gate | Resolution | Date |
  |---|---|---|
  | G1 — version bump | PATCH `0.61.318 → 0.61.319`. Render change + additive verifier fields; no API contract change. | 05-06 '26 |
  | G2 — destructive action | _n/a_. Additive renderer + field fetches; replaced two render loops with a function call; no file/data deletion. | 05-06 '26 |
  | G3 — new decision rule | Operator: `/hidden` = same rich card as `/search` + 📝 latest review, − 💎 Why a gem. Recorded here. | 05-06 '26 |
  | G4 — paid external API call | None added — new card fields ride the existing `places:searchText` verifier call; `enrichTravelTimes` + social profiles already used on `/search`, both best-effort. | 05-06 '26 |

## Amendments

- **`hidden-verify.js`** — `FIELD_MASK` expanded with 8 card fields; `newestReviewIso` helper added + exported; `lookupVenue` return carries the new fields.
- **`index.js`** — new `renderHiddenVenueCards`; GPS + free-text `/hidden` render paths now build the shared `formatTechniqueVenueBlock` card (+ 📝 latest review, − 💎 Why a gem) instead of sending Gemini's chunked text.
- **`i18n.js`** — added `hidden.latestReviewLabel` (EN/FR).
- **`package.json`** — version `0.61.318 → 0.61.319`.
- **`doc/.serial-state.yml`** — counters + last_anchor_time re-anchored 05-06 '26 16:53 SGT.

## Deleted

_None._
