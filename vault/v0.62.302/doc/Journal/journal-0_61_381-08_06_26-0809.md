(№ 581 - 08-06 '26 08:09 SGT) v0.61.381

# Journal — soleat v0.61.381 (Sanctuary-read leak fix — drop Gemini refusals)

> Operator screenshot: a Seoul venue card (**종로은행나무집 시청점**) was printing Gemini's chat **refusal** as the "Sanctuary read."
>
> **Left OPEN for review — auto-merge to be armed on the operator's word.** Commit 594 / journal 581 / build 374 (held). Server-only.

## New

### [HDR] #581 | 08:09 SGT 08-06-26 | v0.61.381 — vibe-summary output guard + de-Singapore prompt | vibe-summary.js | 1 PR | 0 build |

- **[INTENT]** The venue card's "Sanctuary read" rendered Gemini's **conversational refusal** verbatim — *"I don't have any reviews provided in your message to analyze… Could you please share the actual Google reviews of the Singapore CBD restaurant? Once you do, I'll be happy to provide the two lines in the format you've requested."* — instead of a quiet/seating summary or nothing.

- **[ROOT (traced)]**
  - `vibe-summary.js` `summarizeVibe()` returned `result.response.text().trim()` with **no check** that the model produced the demanded two-line `🌿 Quiet / 🌿 Seating` shape.
  - The prompt was hardcoded *"Read these Google reviews of a restaurant in **Singapore's CBD**."* — factually wrong for a Seoul venue now that gia is global, and exactly the framing Gemini parroted back ("Singapore CBD restaurant", "the two lines").
  - With thin / foreign-script reviews the model answered conversationally; that refusal was **cached** (`getOrCacheSummary`, 7-day TTL) and rendered by `venue-templates.js:262` (copy card) + `index.js:368` (TMA) with only a truthy check.

- **[DELTA]** (`vibe-summary.js`)
  - **NEW `isValidVibe(text)`** — a sanctuary read must contain the `🌿` marker **and** not read as model meta-talk (rejects `I don't|cannot|as an AI|share the actual|the two lines|placeholder text|provide the lines`). `summarizeVibe` now returns `isValidVibe(out) ? out : null` → a refusal is **dropped** (never cached, never rendered; the card omits the line).
  - **De-Singapore both prompts** — EN *"of a restaurant in Singapore's CBD."* → *"of a restaurant."*; FR *"d'un restaurant au CBD de Singapour."* → *"d'un restaurant."* (geography-neutral).
  - **Cache key `vibe:summary:v2` → `v3`** — flush any refusal text the pre-guard build cached.
  - **NEW `__tests__/vibe-summary-guard.test.js`** — 6 tests (accepts EN/FR `🌿` shape; rejects the exact Seoul refusal, a refusal with a stray `🌿`, empty/blank/non-string, plausible prose lacking `🌿`).
  - **`package.json`** `0.61.380 → 0.61.381`; **`doc/.serial-state.yml`** commit 593 → 594, journal 580 → 581 (**build held 374**); re-anchored 08:09 SGT.

- **[VERIFICATION (sandbox)]** `node --check vibe-summary.js` clean. Full suite **112 files / 3069 tests green** (+6). The guard test feeds the operator's exact refusal string and asserts `false`.

- **[STATUS]** PR-**OPEN**, **auto-merge to arm on the operator's word**. Railway redeploys on merge.

- **[TEST]** Operator: the Seoul card no longer shows the "I don't have any reviews…" apology — when the model can't produce a proper read, the card simply omits the sanctuary line.

- **[KNOWN GAPS]**
  - This guards the **output**; it doesn't make the model *better* at foreign-script reviews — it just refuses to render garbage. A localized read for overseas venues is a separate enhancement.
  - The **name** is still in raw Korean (`종로은행나무집 시청점`) — that's the next PR (B, foreign-name reading via Gemini).
  - Not live-E2E tested (no Telegram WebView).

- **Confirmation Gates**

  | Gate | Resolution | Date |
  |---|---|---|
  | G1 — version bump | PATCH `0.61.380 → 0.61.381`. | 08-06 '26 |
  | G2 — destructive action | None — additive guard; valid reads pass through unchanged; only refusals are dropped. | 08-06 '26 |
  | G3 — new decision rule | A sanctuary read must match the demanded `🌿` two-line shape or it is discarded (not cached, not rendered). | 08-06 '26 |
  | G4 — paid external API call | None. | 08-06 '26 |

## Amendments

- **`vibe-summary.js`** — `isValidVibe` guard; de-Singapore EN/FR prompts; cache key v2 → v3.
- **`__tests__/vibe-summary-guard.test.js`** (NEW) — 6 guard tests.
- **`package.json`** — `0.61.380 → 0.61.381`.
- **`doc/.serial-state.yml`** — counters (commit 594 / journal 581 / build held 374) + anchor 08:09 SGT 08-06-26.

## Deleted

- _None_ (the "Singapore's CBD" prompt wording is amended in place, not removed wholesale; the EN/FR structure and 🌿 format are preserved).
