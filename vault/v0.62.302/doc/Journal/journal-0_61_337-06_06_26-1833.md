(№ 537 - 06-06 '26 18:33 SGT) v0.61.337

# Journal — soleat v0.61.337 (South Korea Michelin — load the curated `instruction/Korea.js` into `KR-michelin.js`; 117 venues + native hangul name/address; KR manifest)

> Operator: *"next korea"* → `instruction/Korea.js`. Same two situations as Japan, resolved by the **Japan precedent** (not re-asked): **(1)** Korea 2025 is **partial** (10 venues, **no Bib Gourmand**) vs 2026's full 117 → **add as-is + flag the gap**; **(2)** the native `nameKo`/`addressKo` on every row → **stored verbatim now, displayed in a later follow-up**.
>
> **Left OPEN for review — auto-merge NOT armed.** Commit 550 / journal 537 / build 336, landed via the GitHub Git Data API on origin/main @ v0.61.336.

## New

### [HDR] #537 | 18:33 SGT 06-06-26 | v0.61.337 — South Korea Michelin live: `KR-michelin.js` = 117 curated venues (hangul name + address verbatim); KR manifest (10/117) | 3 code files | 1 PR | 1 build |

- **[INTENT]** v0.61.333 shipped `KR-michelin.js` as the empty stub. The operator's curated `instruction/Korea.js` holds the full venue-centric Korea dataset (**117 venues** across Seoul + Busan, both 2025 + 2026 editions, **plus** a native `nameKo` + `addressKo` on every row). This PR loads it **verbatim** into `KR-michelin.js`, registers the KR manifest, and leaves `instruction/Korea.js` as source-of-record. No UI surfacing of the Korean fields yet (Japan-precedent decision).

- **[MECHANISM]** `instruction/Korea.js` is a bare array literal of 117 venue objects in venue-award-schema.v0_1 shape + two locale fields (`{id, city, country:'KR', name, nameKo, address, addressKo, postal?, cuisine?, vegetarian, halal, awards:[{year,category}]}`). The 117 rows are copied **byte-for-byte** into `KR-michelin.js` — no re-serialisation, so hangul (`밍글스`, `강남구 도산대로`) survives untouched. The loader uses each `entry.id` verbatim and validates the `city` resolves in `CITY_IATA`. **Both KR cities (Seoul→SEL, Busan→PUS) were already mapped** in `cities.js`, `iata-cities.js`, and the loader — so, like Japan, **no city / IATA / `CITY_IATA` additions** were needed (the cleanest integration yet — 2 cities, both pre-existing). `venueToVenue` projects the venue to canonical shape and **drops `nameKo`/`addressKo`** (not yet consumed) — they remain in the source table for the display follow-up.

- **[DELTA]**

  **1. `KR-michelin.js`** (root) — empty v0.61.333 stub → `const COUNTRY='KR'; const ENTRIES=[ …117 venues verbatim… ]; module.exports={COUNTRY,ENTRIES}`. Header → v0.61.337 (source = `instruction/Korea.js`; 2 cities; KR-locale fields documented; 2025-partial manifest noted). 117 unique ids, 127 awards.

  **2. `michelin-data.js`** — `COUNTRY_MANIFEST` + `KR: { 2025:{three:1,two:8,one:1,total:10}, 2026:{three:1,two:10,one:35,bib:71,total:117} }`. 2025 omits `bib-gourmand` (none in source) — same omit-absent-tier convention as MY/JP.

  **3. `__tests__/michelin-data.test.js`** — removed `KR` from the empty-tables loop; **flipped the false-gate** (the v0.61.336 JP PR had used Seoul/KR as the still-empty example — now non-empty → swapped to Taipei/TW); added a full KR load suite (117 venues / 127 awards, 10@2025 / 117@2026, per-tier manifest `{1,8,1}` + `{1,10,35,71}`, unique ids, every KR `city` curated, Mingles 3★ both editions, Busan count 24, **source-table assertion that `nameKo` `밍글스` / `addressKo` hangul are preserved verbatim**), a `KR-michelin.js is venue-centric with 117 rows` require test, and a `hasMichelinData` KR-true case (KR / kr / Seoul / Busan).

  **4. `package.json`** — `0.61.336 → 0.61.337`.

  **`doc/.serial-state.yml`** — commit 549 → 550, journal 536 → 537, build 335 → 336; re-anchored 18:33 SGT 06-06-26.

- **[VERIFICATION (sandbox)]**
  - `node --check` clean: `KR-michelin.js`, `michelin-data.js`, `__tests__/michelin-data.test.js`.
  - Real-loader temp-harness (all 8 `{CC}-michelin.js` + patched `michelin-data.js` in `/tmp/krtest`): `require('./michelin-data.js')` loads **without throwing**. `venuesForCountry('KR')===117`; global unique ids **956/956** (MY 70 + TH 180 + JP 589 + KR 117, no dup); KR 2025=10 / 2026=117; tiers `{three:1,two:8,one:1}` (2025) + `{three:1,two:10,one:35,bib:71}` (2026); `sum(KR awards)===127`. KR manifest passes. `hasMichelinData` true for KR/Seoul/Busan; false for Taipei/TW.
  - **18/18** explicit assertions green (counts, tiers, unique ids, kr- prefix, curated-city gate, Mingles 3★ both years, Busan 24, source hangul `밍글스` + `강남구`, every-row `nameKo`/`addressKo`, global 956 dedup).
  - Full `npm test` runs in CI on merge.

- **[STATUS]** PR-**OPEN**, **auto-merge NOT armed** — awaits operator review/merge. Landed via the GitHub Git Data API on a branch off origin/main @ v0.61.336. Railway redeploys on merge.

- **[TEST]** Operator: South Korea is now in the venue loader — 117 venues across Seoul + Busan, the full 2026 edition (117) + the partial 2025 upper-tiers (10). The native Korean name + address are stored on every row (hangul preserved) ready for the display follow-up. `instruction/Korea.js` stays source-of-record.

- **[KNOWN GAPS]**
  - **2025 is PARTIAL** — 10 venues, **no Bib Gourmand** and only 1 one-star (vs 2026's 117 @ 71 Bib). Loaded as-is per the Japan-precedent decision; a fuller 2025 Korea list is **still due** when supplied.
  - **`nameKo` / `addressKo` not surfaced** — stored but `venueToVenue` projects them out; the display follow-up must pass them through + render them (shared with Japan's `nameJa`/`addressJa`).
  - **Unified loader still off the live search path** — MY/TH/JP/KR via `michelin-data.js` not yet wired into the bot's Michelin search (SG standalone). Per-city Michelin gating for OTHER-mode cities remains the larger follow-up.
  - A `doc/Feature/` / `doc/Technical/` / `doc/Register/` update + a `vault/v0.61.337/` snapshot are still due.

- **Confirmation Gates**

  | Gate | Resolution | Date |
  |---|---|---|
  | G1 — version bump | PATCH `0.61.336 → 0.61.337`. | 06-06 '26 |
  | G2 — destructive action | None. Empty `KR-michelin.js` stub filled; append-only manifest + test edits. Curated `instruction/Korea.js` untouched. Loader clean (956/956 unique). | 06-06 '26 |
  | G3 — new decision rule | South Korea Michelin loads from `instruction/Korea.js` (117 venues) VERBATIM; `nameKo`/`addressKo` stored now, displayed later; 2025 partial (10, no Bib) loaded as-is + flagged; KR manifest 2025 10 / 2026 117. (Japan precedent applied without re-asking.) | 06-06 '26 |
  | G4 — paid external API call | _n/a_. Data load + manifest registration only. | 06-06 '26 |

## Amendments

- **`KR-michelin.js`** — empty stub → 117 curated venues from `instruction/Korea.js` (verbatim, hangul intact); header → v0.61.337.
- **`michelin-data.js`** — `COUNTRY_MANIFEST` +`KR` (2025 10 / 2026 117; 2025 omits Bib Gourmand).
- **`__tests__/michelin-data.test.js`** — KR removed from empty-tables loop; false-gate flipped Seoul/KR → Taipei/TW; +KR load suite + `KR-michelin.js` 117-row require test + `hasMichelinData` KR case.
- **`package.json`** — `0.61.336 → 0.61.337`.
- **`doc/.serial-state.yml`** — counters (commit 550 / journal 537 / build 336) + anchor re-set 18:33 SGT 06-06-26.

## Deleted

- _None._
