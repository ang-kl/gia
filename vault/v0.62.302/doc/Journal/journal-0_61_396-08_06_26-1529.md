(№ 596 - 08-06 '26 15:29 SGT) v0.61.396

# Journal — soleat v0.61.396 (Mismatch → non-SG country: route the map + fire the search)

> Operator (**urgent**): *"if the user choose to keep JP (where in JP isn't display) when first loading — shouldn't you pause to re-route map to JP location then fire 5."*
>
> **Left OPEN for review — auto-merge to be armed on the operator's word.** Commit 609 / journal 596 / build 385. web/cuisine. **Revert-ready** (3rd coherence-area change today).

## New

### [HDR] #596 | 15:29 SGT 08-06-26 | v0.61.396 — applyCoherenceChoice routes + fires for non-SG | App.jsx | 1 PR | 1 build |

- **[INTENT]** Resolving the location-mismatch modal to a **non-SG country (JP)** did **nothing** — no map reroute, no first-5, the JP city wasn't shown.

- **[ROOT (traced)]** `applyCoherenceChoice` (`App.jsx:904`) set `region=OTHER` + `countryPref=JP` but **no anchor / searchCenter / flyTo**. The gate re-fires `runInitialLoad` → `resolveSearchCenter(OTHER, no explicit pick)` returns **null** (device GPS is SG-only since v0.61.373) → `runInitialLoad` bails *"awaiting a country+city pick"* → nothing fires.

- **[DELTA]** (`web/cuisine/src/v2/App.jsx`)
  - When the committed country is **non-SG**, `applyCoherenceChoice` now: picks a centre (the **device coords** + `nearestIataCity` name when "Use", else the **capital centroid** `CITIES_BY_COUNTRY[cc][0]`, e.g. Tokyo); `setLocationAnchor` + `setSearchCenter` + `setFlyTarget(zoom 11)` to **reroute the map**; sets `initialLoadFiredRef` + `initialSearchDone` so the gate-opener doesn't double-fire; and fires `runSearch({ ...state, region:'OTHER', countryPref:cc }, pt)` with a **corrected snapshot** (avoids the stale-region race) to load the first 5 from the country centre. Mirrors the country-city pick flow (`~:2068`). **SG keeps its existing device-GPS anchor.**
  - **`package.json`** `0.61.395 → 0.61.396`; **`doc/.serial-state.yml`** commit 608 → 609, journal 595 → 596, **build 384 → 385**; re-anchored 15:29 SGT.

- **[CAUGHT IN REVIEW]** `isValidCoord` (`App.jsx:627`) is a **local** const inside an effect — **not in scope** at `:904`. Vite builds the out-of-scope reference without error, but it would **throw at runtime**. Replaced with an inline `_validPt`.

- **[VERIFICATION (sandbox)]** Cuisine build green. Full suite **113 files / 3093 tests**.

- **[STATUS]** PR-**OPEN**, **auto-merge to arm on the operator's word**. Railway redeploys on merge.

- **[TEST]** Operator: resolve the mismatch to JP → the map should **fly to the JP city** (named), and the **first 5** load from there.

- **[KNOWN GAPS / SAFETY]**
  - **Contained to `applyCoherenceChoice`** — does **NOT** touch the gate-opener timing (the v0.61.391 hang area), so it shouldn't reintroduce that hang. **Revert-ready** (3rd coherence change today).
  - "Use" lands on the **device coords**; "Keep saved JP" lands on the **capital** (Tokyo) — the user can then pick a specific JP city.
  - The "keep SG while physically abroad" case is unchanged (pre-existing).
  - Not unit-tested (a React modal flow); not live-E2E tested — **operator must verify the route + no hang.**

- **Confirmation Gates**

  | Gate | Resolution | Date |
  |---|---|---|
  | G1 — version bump | PATCH `0.61.395 → 0.61.396`. | 08-06 '26 |
  | G2 — destructive action | Additive — extends the apply fn; the SG path + gate are untouched. | 08-06 '26 |
  | G3 — new decision rule | Committing the mismatch to a non-SG country anchors + routes the map to that country and fires the search. | 08-06 '26 |
  | G4 — paid external API call | None new — one `runSearch` (Places) that previously failed to fire. | 08-06 '26 |

## Amendments

- **`web/cuisine/src/v2/App.jsx`** — `applyCoherenceChoice` non-SG reroute + fire (inline `_validPt`).
- **`package.json`** — `0.61.395 → 0.61.396`.
- **`doc/.serial-state.yml`** — counters (commit 609 / journal 596 / build 385) + anchor 15:29 SGT 08-06-26.

## Deleted

- _None_ (additive).
