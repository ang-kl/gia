(№ 311 - 22-05 '26 17:26 SGT) v0.61.104

# Journal — soleat v0.61.104 (bus-tier revision · /app/map parity · zoom readout)

> Operator — three of the four items from the latest map spec (the
> carpark zoom-size ladder follows as its own PR).
>
> This entry is commit 311 / journal 304 / build 108, off `main` at
> v0.61.103 (#596).

## New

### [HDR] #304 | 17:26 SGT 22-05-26 | v0.61.104 — bus tiers + parity + readout | ~3 kb | 8 files | 1 PR | 1 build |

- **[INTENT]** Revise the bus-stop zoom tiers, bring the `/app/map`
  toggle/dropdown set to parity with the Hawker TMA, and restyle the
  zoom readout.

- **[DELTA]**
  - **Bus-stop tiers** — `web/cuisine/src/v2/lib/mapOverlays.js` (+ the
    byte-identical Hawker / Transport copies) and `public/app.js`:
    `busTier` revised — z15 now returns the `🚏` glyph (was the
    `🚏 № <code>` short label), z16 keeps the short label, and z14
    gets a new `glyph-lg` tier (an 18 px `🚏`, one size larger).
  - **`/app/map` layer parity** — `public/app.js`: the toggle row
    reorders to Train Line / Car Park / Bus Stop (Hawker's order); the
    `⋯` dropdown's `MENU_LAYERS` now carries Hawker's full set in its
    order — Station Exits, Taxis, Attractions, Parks, Police, Clinics,
    Hospitals (Station Exits + Taxis were missing).
  - **Zoom readout** — `web/cuisine/src/v2/components/MapPanel.jsx`,
    `web/hawker/src/components/HawkerMapPanel.jsx`,
    `web/transport/src/components/MrtMapPanel.jsx`, `public/app.js`:
    opacity `0.3` → `0.9` (operator: "10 % transparent") and one size
    larger (`text-[10px]` → `text-[11px]`; the `/app/map` pin 10 px →
    11 px).
  - **`package.json`** — `0.61.103` → `0.61.104` (PATCH).

- **[VERIFICATION (sandbox)]**

  ```
  node --check public/app.js   clean
  npm run build (5 TMAs)       all green
  npm test                     1944/1944 passing, 67 files
  ```

- **[STATUS]** DRAFT — PR #597 on
  `claude/v0.61.104-bus-tier-readout-parity` (off `main` at v0.61.103,
  `6455212`). On merge deploys via the Railway auto-deploy on `main`.

- **[TEST]** Bus markers step correctly through z14-16; the `/app/map`
  toggle row + dropdown match the Hawker TMA; the zoom readout is
  clearly visible (90 % opacity) and one size larger.

- **[KNOWN GAPS]**
  - **Still due — the carpark zoom-size ladder** (each zoom level two
    sizes smaller than the one above, + overlap demotion) — its own
    follow-up PR.
  - No in-sandbox visual verification (no Maps webview).
  - `doc/VibeCodingRecord/` regen still due (carried forward).

- **Confirmation Gates**

  | Gate | Resolution | Date |
  |------|-----------|------|
  | G1 — version bump | PATCH `0.61.103` → `0.61.104` | 22-05 '26 |
  | G2 — destructive action | _code-only_ — tier bands + classes revised in place; no deletion | 22-05 '26 |
  | G3 — new decision rule | _n/a_ — operator UI spec | 22-05 '26 |
  | G4 — paid external API call | _n/a_ | 22-05 '26 |

## Amendments

_None._

## Deleted

_None._
