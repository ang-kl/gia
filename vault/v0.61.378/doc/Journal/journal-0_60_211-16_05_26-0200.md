(№ 186 - 16-05 '26 02:00 SGT) v0.60.211

# Journal — soleat v0.60.211

> Operator: *"do DF-110 … review and ask."* Reviewed, then asked two questions (AskUserQuestion). Operator chose: (1) **flag + verify caveat** — match the v0.60.208 fan-out treatment; (2) **fix both** — apply the misleading-italic fix to the sibling 🌿 ingredient branch too.

## New

### [HDR] #179 | 02:00:00 SGT 16-05-26 | v0.60.211 — DF-110: single-query technique/ingredient search header | 4 files | 1 PR |

- **[INTENT]** DF-110 — the single-query free-text search reply (`index.js` ~5176, the `intent==='tool'` branch) led with a `🔧` glyph and a misleading italic *"Searching for Singapore restaurants that use this technique…"*. Review finding: this is the **final** message, not an interim ack — the venue cards render directly below and the whole thing is sent once (line 5241). So the "Searching…" wording reads as in-progress when the search is already done. The v0.60.208 cooking-method fan-out had already fixed the equivalent surface (🔧 → country flag + a "verify" caveat); this single-query path is a *separate* render path that was never given the same treatment.

- **[DELTA]**

  **`index.js`** — `runFreeTextSearch` header block:
  - **Tool branch** (`intent==='tool'`): `🔧 <b>{explainer}</b>` → `${flagFor(intent.cuisine)} <b>{explainer}</b>` — a country flag (🍽 fallback when `intent.cuisine` is null, consistent with the cuisine/`why` branches). The misleading `<i>Searching for Singapore restaurants that use this technique…</i>` → `<i>These places may have it. Please verify.</i>` (FR: `<i>Ces lieux peuvent le proposer. Veuillez vérifier.</i>`) — the exact caveat strings the v0.60.208 fan-out uses.
  - **Ingredient branch** (`intent==='ingredient'`): the `🌿` glyph stays (apt for an ingredient); only the misleading `<i>Searching for Singapore restaurants that feature it…</i>` italic is replaced with the same verify caveat.
  - `flagFor` is a hoisted top-level function (declared ~5305); calling it at ~5180 is safe — `CUISINE_FLAG` (a `const`) is fully initialised long before the handler runs.

  **`package.json`** — `0.60.210 → 0.60.211` (PATCH).

  **`doc/Journal/journal-0_60_211-16_05_26-0200.md`** — this entry (#179). **`doc/.serial-state.yml`** — `journal 178→179`, `commit 185→186`; re-anchored.

- **[VERIFICATION (sandbox)]**

  ```bash
  $ node --check index.js        → OK
  $ npm test -- --run            → 1865 passed (1865)   # unchanged — copy/glyph change, no test touched these strings
  ```

- **[STATUS]** STAGED → PROD on next Railway deploy. Server-only change (`index.js`) — no `web/` build needed.

- **[TEST]** Post-deploy:
  1. `/s sous vide` (a technique that misses the `COOKING_METHODS` dictionary → single-query path) → header is `<flag> <b>{explainer}</b>` then `These places may have it. Please verify.` — no `🔧`, no "Searching…" wording.
  2. `/s` an ingredient (e.g. `burrata`) → `🌿 <b>{explainer}</b>` then the same verify caveat.
  3. FR locale → `Ces lieux peuvent le proposer. Veuillez vérifier.`
  4. A technique with a known origin cuisine → the real country flag; a generic technique with no cuisine → `🍽` fallback.

- **[KNOWN GAPS]**
  - **DF-110 — now CLOSED.** Both halves done: the venue-card Try line was guarded in v0.60.209 (`formatTechniqueVenueBlock` + `isDishName`); the header glyph + misleading italic are fixed here.
  - **DF-100** — bib-gourmand per-tap shuffle: not a bug (deliberate variety). Untouched.
  - **DF-84** — `.vibe-journal/` framework migration sub-issue still open (the HTML-redundancy half is resolved).
  - **DF-101 / 103 / 106** — no code action: operator decision / external Places data / Railway infra.

## Confirmation Gates

| Gate | Authority | Stamp |
|---|---|---|
| G1 — version bump | _PATCH 0.60.210 → 0.60.211._ | auto |
| G2 — destructive action | _None._ | n/a |
| G3 — new decision rule | _Operator (via AskUserQuestion): the single-query technique/ingredient search header follows the v0.60.208 fan-out style — a country flag (not 🔧) and a "verify" caveat (not a "Searching…" status line)._ | operator |
| G4 — paid external API call | _None._ | n/a |

## Amendments

(None. v0.60.210 (#178) is the prior entry.)

## Deleted

(Nothing deleted. The `🔧` glyph and the two "Searching…" italic strings are replaced in place.)
