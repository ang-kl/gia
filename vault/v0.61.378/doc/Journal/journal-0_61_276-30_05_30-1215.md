(№ 477 - 30-05 '26 12:15 SGT) v0.61.276

# Journal — soleat v0.61.276 (JB-coords coherence: server sanity + client modal + graceful filter fallback)

> Operator (30-05 '26 morning Railway-log investigation board, Experts A + C):
> *"select country+code, still cannot find durian"* — root-caused to JB region pill sticky from prior session while coords had moved to Putrajaya / Ipoh / KL. JB-hybrid-filter at index.js:14490 was correctly biased to actual Johor coords (1.2–2.55 N, 102.5–104.5 E) and threw away every non-Johor candidate, returning 0 silent venues.
>
> Three-part fix:
> 1. Server-side set-location sanity guard — downgrade JB→OTHER when coords aren't JB-shaped (catches chat-bot / legacy clients).
> 2. Server-side JB-hybrid-filter graceful exit — when the filter wipes all candidates AND request coords are >150 km from JB CBD, fall through to OTHER country-text-filter treatment with `ctxCountry='MY'` default.
> 3. Client-side region-coords coherence modal — sibling of the v0.61.274 country-coords modal; fires on mount when `state.region === 'JB'` AND `isJbCoords(userLoc) === false`.
>
> Commit 489 / journal 477 / build 279, on branch
> `claude/v0.61.276-jb-coords-coherence-and-graceful-exit` off `main` at v0.61.275.

## New

### [HDR] #477 | 12:15 SGT 30-05-30 | v0.61.276 — JB-coords coherence (3 surfaces) | 4 files | 1 PR | 1 build |

- **[INTENT]** Close the "JB pill at non-JB coords" bug class at every layer of the stack — prevent (client modal), correct (server set-location sanity), and recover (server filter graceful exit). After this PR a user who lands with JB sticky at Putrajaya sees a modal instead of empty results; if they tap the modal away or never see it (chat-bot path), the server still rescues them by treating the request as OTHER and showing MY-scoped results.

- **[DELTA]**

  **1. `index.js` — `/api/cuisine/search` JB-hybrid-filter graceful exit (~line 14492)**

  After the existing JB-hybrid filter, capture the pre-filter pool via `preJbVenues = venues.slice()`. If the filter wiped everything (`venues.length === 0 && beforeJb >= 5`) AND the request's coords are >150 km from JB CBD via haversine, restore the pre-filter pool and set `jbFilterFellBackToOther = true`. The else-if on the OTHER branch becomes `if (jbFilterFellBackToOther || isOther)`. Inside that branch a new defaulting line forces `ctxCountry = 'MY'` when the JB fallback path is active and no countryPref was set.

  Log line: `[Cuisine-Search] D703b JB-fallback: filter wiped 16→0 at coords 1234567m from JB CBD. Treating request as OTHER.`

  **2. `index.js` — `/api/cuisine/set-location` JB sanity guard (~line 13235)**

  Inside the existing region-validation block, when client passes `region: 'JB'`, compute haversine to JB CBD `(1.4927, 103.7414)`. If >150 km, downgrade to `'OTHER'` BEFORE writing to Redis. Log: `[set-location] sanity guard: region=JB requested at coords X,Y (Nkm from JB CBD) — downgrading to OTHER`. Belt-and-braces with the client modal — covers legacy bundles, chat-bot writes, and any future client that doesn't surface the modal.

  **3. `web/cuisine/src/v2/App.jsx`** — region-coords coherence modal + handler

  - Import gains `isJbCoords` alongside the existing `coordsToCountry`.
  - New state: `regionMismatch` + `regionCoherenceCheckedRef` (one-shot guard).
  - New `useEffect` keyed on `[userLoc?.lat, userLoc?.lng, state.region]`: when `state.region === 'JB'` AND `!isJbCoords(userLoc)`, sets `regionMismatch = { coordsCountry: 'SG'/'MY'/null }`. Mirrors the v0.61.274 country-coords pattern.
  - New `applyRegionCoherenceChoice(useCoords)`: on "Switch" path resets `state.region` to `'SG'` (coords inside SG bbox) or `'OTHER'` (anywhere else); when targeting OTHER + coords are MY, also forces `state.countryPref = 'MY'`.
  - New modal JSX (EN + FR) below the existing country-coords modal in the render tree. Two buttons:
    - `Switch to Singapore` / `Switch to Others` (depending on coords).
    - `Stay on JB` — keeps the user's pick even though it'll return 0 venues; the server-side graceful exit (#1) catches the search at the back-end.

  **4. `package.json`** — `0.61.275 → 0.61.276`.

  **5. `doc/.serial-state.yml`** — commit 488→489, build 278→279, journal 476→477; re-anchored 30-05 '26 12:15 SGT.

- **[VERIFICATION (sandbox)]**

  - `node --check index.js` — clean.
  - `@babel/parser` parse of `App.jsx` (2,526 lines) — clean.
  - Full vitest: **99 files / 2,793 tests** in 16.65 s. No tests touched (all changes are flow / UX additions; no test contracts changed).

- **[STATUS]** PR-OPEN on push. Railway redeploys both routes + the Cuisine TMA bundle on merge.

- **[TEST]** Operator (post-merge):
  1. **Modal: stale JB pill + non-JB coords** — pre-condition: prior session left `state.region = 'JB'`. Open Cuisine TMA from Putrajaya / KL / Ipoh / SG-far-north coords. Modal appears: *"Johor Bahru region is selected, but you're not in Johor"* + buttons. Tap "Switch to Others" → region flips to OTHER, countryPref → MY (if coords are MY), search runs normally.
  2. **Server graceful exit: JB pill + non-JB coords, modal dismissed** — same scenario but tap "Stay on JB". Run a durian search. Railway log shows `[Cuisine-Search] D703b JB-fallback: filter wiped 16→0 at coords … from JB CBD. Treating request as OTHER.` Results come back from the OTHER path. The TMA's banner / region pill stays on JB (the user's expressed intent), but the venues are real.
  3. **Set-location sanity guard** — chat /location to a Putrajaya coord with explicit `region: 'JB'` (curl the route directly with that body). Server logs `[set-location] sanity guard: region=JB requested at coords 2.93,101.69 (123km from JB CBD) — downgrading to OTHER`. The Redis cache stores `region: 'OTHER'`.
  4. **Real JB coords**: switch to actual JB (e.g. Mid Valley Southkey 1.4912, 103.7665). Modal does NOT appear. Search runs normally. Graceful exit does NOT trigger.
  5. **No regression in SG / OTHER coords without JB pill** — neither modal nor graceful exit fires; behaviour identical to v0.61.275.

- **[KNOWN GAPS]**
  - **No client-side notice** when the server falls back from JB → OTHER. The user sees results without knowing the system overrode their pick. Could add a small toast / banner in a follow-up UI PR.
  - **150 km threshold** is hardcoded in two places (search route + set-location route). Could be extracted to `location-mode.js` if a third caller appears.
  - **The graceful exit kicks in at 150 km** — slightly larger than the JB extent (≤ ~120 km from CBD). Pontian / Mersing / Desaru stay within the JB filter; only mainland-MY + SG-far-north hit the fallback. If a user is at Mersing (~100 km from JB CBD) with JB pill on, they still get the normal filter behaviour.
  - **The graceful-exit `preJbVenues` slice** is a shallow copy — fine for the array of references; doesn't break the venue objects themselves.

- **Confirmation Gates**

  | Gate | Resolution | Date |
  |---|---|---|
  | G1 — version bump | PATCH `0.61.275 → 0.61.276`. UX (modal) + flow (graceful exit) + sanity (set-location). No API contract change. | 30-05 '26 |
  | G2 — destructive action | _n/a_. The set-location sanity guard DOWNGRADES region=JB to OTHER when coords don't match; that's a soft correction (logs the override, persists OTHER). Operator can always re-pick JB explicitly. | 30-05 '26 |
  | G3 — new decision rule | YES — when the server sees a region/coords mismatch (JB pill at non-JB coords), prefer coords over the explicit pick. Charitable interpretation: the user wants results near their actual location, not at a pin they no longer remember setting. This is a continuation of the v0.61.274 country-coords rule extended to the region axis. | 30-05 '26 |
  | G4 — paid external API call | _n/a_. Server-side haversine is pure-function; modal is client-only. | 30-05 '26 |

## Amendments

- **`index.js:/api/cuisine/search`** — JB-hybrid-filter graceful exit (~line 14492) + OTHER-branch ctxCountry default to MY when fallback fires (~line 14546). The else-if structure becomes `if (jbFilterFellBackToOther || isOther)`.
- **`index.js:/api/cuisine/set-location`** — JB sanity guard inside the region-validation block (~line 13235).
- **`web/cuisine/src/v2/App.jsx`** — `isJbCoords` import; `regionMismatch` state + `regionCoherenceCheckedRef`; `useEffect` for the JB-coords check; `applyRegionCoherenceChoice` handler; modal JSX below the existing v0.61.274 country-coords modal.

## Deleted

_None._
