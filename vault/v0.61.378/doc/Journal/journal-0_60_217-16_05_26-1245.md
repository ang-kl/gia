(№ 192 - 16-05 '26 12:45 SGT)

# Journal — soleat v0.60.217

> Operator: *"Increase the font size by 1 point for each TMA footer (Cuisine, Train, Hawker, Menu) is framed no border, reading: Experimental · Singapore · v0.60.215."*

## New

### [HDR] #185 | 12:45:00 SGT 16-05-26 | v0.60.217 — TMA footer: no border, font +1pt, full tag text restored | 9 files | 1 PR | 4 TMA builds |

- **[INTENT]** Three footer adjustments across all four TMAs, refining the v0.60.215 bordered footer: drop the border, enlarge the font by one point, and restore the full `Experimental · Singapore · v<build>` tag wording (v0.60.215 had shortened it to `experimental · v<build>`).

- **[DELTA]**
  - **No border** — the footer `className` loses `border border-tg-border rounded-lg` on all four TMAs (`web/cuisine/src/v2/App.jsx`, `web/transport/src/App.jsx`, `web/hawker/src/App.jsx`, `web/menu/src/App.jsx`). The `mx-2 mb-2 mt-… px-3 py-2` block spacing is kept — the footer is still a distinct block ("framed"), just without the stroke.
  - **Font +1 point** — `text-[8px]` → `text-[9px]` on all four footers.
  - **Tag text restored** — the footer reads `Experimental · Singapore · v<build>` again:
    - `web/transport/src/i18n.js`, `web/hawker/src/i18n.js`, `web/menu/src/i18n.js` — `footer.tag` `experimental` → `Experimental · Singapore` (FR `Expérimental · Singapour`).
    - `web/cuisine/src/v2/lib/i18n.js` — `footer.experimental` `experimental` → `Experimental`.
    - `web/cuisine/src/v2/App.jsx` — the tag line re-adds the dynamic region segment, so it reads `Experimental · Singapore · v<build>` (or `… · Johor Bahru · …` in JB mode).
  - The version segment stays the dynamic `v{BUILD_VERSION}` (the v0.60.215 build-version fix) — it shows the deployed root version (`v0.60.217` on this build).
  - `package.json` `0.60.216 → 0.60.217`; this Journal entry (#185); `doc/.serial-state.yml` (`journal 184→185`, `commit 191→192`, `build 3→4`; re-anchored).

- **[VERIFICATION (sandbox)]**

  ```bash
  $ (cd web/{cuisine,transport,hawker,menu} && npm run build)  → all 4 built clean
  ```

- **[STATUS]** STAGED → PROD on next Railway deploy. TMA-only change; the `public/<tma>/` bundles are rebuilt on deploy.

- **[TEST]** Post-deploy — each TMA footer is a borderless block, font one point larger, reading `Experimental · Singapore · v0.60.217` (Cuisine flips the middle term to `Johor Bahru` in JB mode; Menu keeps its `Soleat … · 2026` line above).

- **[KNOWN GAPS]** None new. (The footer wording has now iterated v0.60.213 `Experimental · Singapore` → v0.60.215 `experimental` → v0.60.217 `Experimental · Singapore`; the border was added in v0.60.215 and removed here.)

## Confirmation Gates

| Gate | Authority | Stamp |
|---|---|---|
| G1 — version bump | _PATCH 0.60.216 → 0.60.217._ | auto |
| G2 — destructive action | _None._ | n/a |
| G3 — new decision rule | _Operator: the TMA footer is a borderless block, `text-[9px]`, reading `Experimental · Singapore · v<build>`._ | operator |
| G4 — paid external API call | _None._ | n/a |

## Amendments

(None. v0.60.216 (#184) is the prior entry.)

## Deleted

(Nothing deleted. The footer border + the shortened `experimental` wording from v0.60.215 are replaced in place.)
