(№ 575 - 07-06 '26 19:45 SGT) v0.61.375

# Journal — soleat v0.61.375 (Drop the hard-coded "Michelin Singapore 2025" year)

> Operator chose **fix B** (after A): de-stale the "Michelin Singapore 2025" label.
>
> **Left OPEN for review — auto-merge to be armed on the operator's word.** Commit 588 / journal 575 / build 372 (held — server-only). Server.

## New

### [HDR] #575 | 19:45 SGT 07-06-26 | v0.61.375 — drop "2025" from the Michelin SG label | 1 code file | 1 PR | 0 build |

- **[INTENT]** Remove the hard-coded year that went stale at the year roll-over.

- **[DECISION]** **Drop** the year, don't make it dynamic — a dynamic *current* year would be **wrong** (the curated data is the **2025** edition, not the live year). Omitting it is the honest fix and matches every other country ("Michelin Japan", "Michelin South Korea", …).

- **[MECHANISM]** (`index.js`)
  - `michelinEditionLabel` SG branch: `'Michelin Singapore 2025'` → **`'Michelin Singapore'`** (this is `michelinSummary.label`, shown in the result **criteria line** at `App.jsx:2894`).
  - `/api/cuisine/catalogue` michelin cuisine entry: `name` `'Michelin Singapore 2025'` → **`'Michelin, Bib Gourmand'`** (it leaked into the criteria line via the `m.name` fallback before the server label loads); `description` `'Singapore Michelin Guide 2025: …'` → `'Singapore Michelin Guide: …'`.
  - Remaining "Michelin Singapore 2025" occurrences are **code comments** + an internal `searchQuery` only — not user-facing.

- **[DELTA]**
  - **`index.js`** — SG edition label + catalogue chip `name`/`description` year-drop.
  - **`package.json`** — `0.61.374 → 0.61.375`. **`doc/.serial-state.yml`** — commit 587 → 588, journal 574 → 575 (**build held 372** — no web change); re-anchored 19:45 SGT.

- **[VERIFICATION (sandbox)]** `node --check index.js` clean; full `npx vitest run` → **108 files / 3029 tests green**.

- **[STATUS]** PR-**OPEN**, **auto-merge to arm on the operator's word**. Server-only. Field test: SG Michelin result criteria line reads "Michelin Singapore" (no "2025"); the drawer chip stays "Michelin, Bib Gourmand".

- **[TEST]** Operator: no more "2025" stamp on the Singapore Michelin label.

- **[KNOWN GAPS]**
  - **Not live-E2E tested** — no Telegram WebView here.
  - Code comments still reference the 2025 edition (accurate — that *is* the data edition); only the user-facing strings changed.

- **Confirmation Gates**

  | Gate | Resolution | Date |
  |---|---|---|
  | G1 — version bump | PATCH `0.61.374 → 0.61.375`. | 07-06 '26 |
  | G2 — destructive action | String-only edits to user-facing labels; behaviour unchanged; full suite green. | 07-06 '26 |
  | G3 — new decision rule | The Michelin SG label omits the edition year (dynamic-current-year would misstate the 2025 data); consistent with other countries. | 07-06 '26 |
  | G4 — paid external API call | None. | 07-06 '26 |

## Amendments

- **`index.js`** — SG Michelin label + catalogue `name`/`description` year-drop.
- **`package.json`** — `0.61.374 → 0.61.375`.
- **`doc/.serial-state.yml`** — counters (commit 588 / journal 575 / build held 372) + anchor 19:45 SGT 07-06-26.

## Deleted

- The hard-coded "2025" / "Michelin Singapore 2025" from the user-facing label, chip name, and description.
