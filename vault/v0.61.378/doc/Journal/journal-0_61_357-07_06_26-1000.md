(№ 557 - 07-06 '26 10:00 SGT) v0.61.357

# Journal — soleat v0.61.357 (Location-state refactor PR 4 — OTHER picker reordered capital-first, then by city size)

> Operator: *"Pr 4"* — the picker reorder ("capital first then next larger city"). Reordered every OTHER-mode country block in both `cities.js` copies. LA/KH/MM stay excluded (v0.61.347). Completes the 4-part refactor.
>
> **Left OPEN for review — auto-merge NOT armed.** Commit 570 / journal 557 / build 356. TMA.

## New

### [HDR] #557 | 10:00 SGT 07-06-26 | v0.61.357 — OTHER picker: capital first, then by population (both Mini-App copies) | 2 code files | 1 PR | 1 build |

- **[INTENT]** The OTHER-mode city picker listed cities in ad-hoc order. The operator wants each country's list to lead with its **capital**, then descend by **city size**.

- **[MECHANISM]** A permutation-safe reorder of each `CC: [ … ]` block: build `name → entry-line`, emit in the new order, assert the new order is the **same set** as before (no city dropped or duplicated). Applied identically to `web/cuisine/src/v2/lib/cities.js` and `web/menu/src/cities.js`. The capital-first rule reorders the countries where the capital isn't the biggest city: **VN → Hanoi** (over HCMC), **AU → Canberra** (over Sydney), **NZ → Wellington** (over Auckland), **CN → Beijing** (over Shanghai), **TW → Taipei**; for JP/KR/TH/ID/PH/MY/BN the capital already leads. HK (no capital) keeps the territory-level "Hong Kong" first, then districts.

- **[DELTA]**
  - **`web/cuisine/src/v2/lib/cities.js`** / **`web/menu/src/cities.js`** — every country block reordered capital-first, then by population descending (entries unchanged — only order).
  - **`package.json`** — `0.61.356 → 0.61.357`.
  - **`doc/.serial-state.yml`** — commit 569 → 570, journal 556 → 557, build 355 → 356; re-anchored 10:00 SGT 07-06-26.

- **[VERIFICATION (sandbox)]**
  - Permutation assertion passed for all 14 countries (no city lost). `node --check` clean both copies. `npm run build` clean; `npx vitest run` → **106 files / 2992 tests green**.
  - Capital-first confirmed via `citiesForCountry`: VN→Hanoi, AU→Canberra, NZ→Wellington, CN→Beijing, TW→Taipei.

- **[STATUS]** PR-**OPEN**, **auto-merge NOT armed**. TMA; Railway rebuilds both bundles on merge. Eyes-on-live: open the OTHER city dropdown — the capital is now first.

- **[TEST]** Operator: each country's city dropdown now opens with the capital at the top, then the larger cities down to the smaller ones.

- **[KNOWN GAPS]**
  - **Secondary-city size ranking is best-effort** — the capital-first ordering is exact; the population order of the *remaining* cities is sourced from public population figures and is approximate for some peripheral towns. Easy to fine-tune any specific list on request.
  - **Pre-existing picker duplicates** (not from this PR) — TH carries both `Ayutthaya`/`Phra Nakhon Si Ayutthaya` and `Koh Samui`/`Ko Samui` (same place, two names, introduced by the v0.61.348 Michelin-city expansion; the Michelin matcher uses the long names). Left in place (deduping is out of reorder scope) — flagged for a follow-up.
  - A `doc/Feature/` / `doc/Technical/` / `doc/Register/` update + a `vault/v0.61.357/` snapshot are still due.

- **Confirmation Gates**

  | Gate | Resolution | Date |
  |---|---|---|
  | G1 — version bump | PATCH `0.61.356 → 0.61.357`. | 07-06 '26 |
  | G2 — destructive action | Reorder only — permutation-checked (no city dropped); both copies in sync; full suite green. | 07-06 '26 |
  | G3 — new decision rule | The OTHER picker lists each country capital-first, then by city size; LA/KH/MM remain excluded. | 07-06 '26 |
  | G4 — paid external API call | _n/a_. | 07-06 '26 |

## Amendments

- **`web/cuisine/src/v2/lib/cities.js`** / **`web/menu/src/cities.js`** — country blocks reordered capital-first, then by population.
- **`package.json`** — `0.61.356 → 0.61.357`.
- **`doc/.serial-state.yml`** — counters (commit 570 / journal 557 / build 356) + anchor 10:00 SGT 07-06-26.

## Deleted

- _None._ (Order changed; no entries removed.)
