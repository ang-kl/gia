(№ 351 - 25-05 '26 14:25 SGT) v0.61.150

# Journal — soleat v0.61.150 (CI hotfix — stale cuisines-vault.test.js assertions broke CI for PRs #648–#650)

> Operator: GitHub email *"PR run failed: CI - v0.61.149 … (e3016ac)"* + *"Run failed: CI - main (76350f1)"*. Surfaced 5 failing CI runs across recent PRs.
>
> Root cause: PR #642 / v0.61.141 added 3 cuisines (Fruits, Durian, Durian Pastry) to the dessert category in `doc/Feature/cuisines_js.MD` — total 67 → 70; dessert 1 → 4. But the integration test in `__tests__/cuisines-vault.test.js` (last updated for v0.59.49) still asserted the pre-v0.61.141 baseline. CI on Node 20 (which checks out fresh from `main`) saw the real `cuisines_js.MD` + the stale assertions = test failure. Locally the test PASSED because of iCloud drift — the working-tree `cuisines_js.MD` was still the pre-v0.61.141 version (label `'Dessert'`, `items: ['Dessert']`).
>
> Commit 363 / journal 351 / build 153, on branch `claude/v0.61.150-ci-fix-cuisines-vault-test` off `main` at v0.61.149 (commit `76350f1`).

## New

### [HDR] #351 | 14:25 SGT 25-05-26 | v0.61.150 — CI hotfix: update cuisines-vault.test.js assertions for the v0.61.141 dessert-group additions | 4 files | 1 PR | 1 build |

- **[INTENT]** Unblock CI. The 5 failing runs since v0.61.143 are all the same root cause; this PR makes them all pass on the next build.

- **[DELTA]**
  - **`__tests__/cuisines-vault.test.js`** — two assertion blocks updated:
    - `loads exactly 67 cuisines` → `loads exactly 70 cuisines` (updated title + the `.toBe(67)` → `.toBe(70)` + new comment referencing v0.61.141 + D-61.141).
    - `groups by category with expected counts` block: `counts['dessert']` `.toBe(1)` → `.toBe(4)`; final `expect(total).toBe(67)` → `.toBe(70)`. Comment block updated to note the v0.61.141 addition.
  - **`package.json`** — `0.61.149 → 0.61.150`.
  - **`doc/.serial-state.yml`** — `commit 362 → 363`, `build 152 → 153`, `journal 350 → 351`; re-anchored 25-05 '26 14:25 SGT.
  - **`doc/Journal/journal-0_61_150-25_05_26-1425.md`** — this entry.

- **[VERIFICATION (sandbox)]**

  ```
  $ git fetch origin main && git show origin/main:doc/Feature/cuisines_js.MD | grep -A2 "id: 'dessert'"
       id: 'dessert',
       label: 'Dessert, Fruits',
       items: ['Dessert', 'Fruits', 'Durian', 'Durian Pastry']
  ```

  The PLUMBING-commit pattern is essential here — running `vitest` against the iCloud-stale working tree gives a FALSE PASS because the local `cuisines_js.MD` is the pre-v0.61.141 version. The blob shipped to `origin/main` (which is what CI sees) is the real v0.61.141 file. The plumbing-commit guarantees the test fix is built against `origin/main`'s state.

- **[STATUS]** PROD-bound on squash-merge to `main`. Docs/test only; no Railway build behaviour change. CI's next run should turn green.

- **[TEST]** Operator (after merge): the GitHub Actions CI run on the merge commit should complete with status **passed** (or at least the `Vitest unit tests` job should no longer be `failed`). 2110 tests → 2110 pass instead of 2108 pass / 2 fail.

- **[KNOWN GAPS]**
  - **iCloud-stale working tree** caught a real CI failure that local testing missed. Logged as another instance of O-22 (the iCloud Drive throttle item) — local `vitest` validation is necessary but not SUFFICIENT given the working-tree may drift from `origin/main`. The plumbing-commit pattern always builds against `origin/main`'s tree, which sidesteps the local drift for code changes; but for TEST changes, the test ASSERTIONS may be against the stale working tree, hiding genuine breakage. **Mitigation: always re-verify a test fix after pushing by checking the CI run, not just the local result.**
  - **PR 2 of the operator's two-part spec** (cuisine-nationality review-language priority + `(<flag> translated)` tagging) still pending — operator originally split it; this hotfix interrupts before PR 2 lands.

- **Confirmation Gates**

  | Gate | Resolution | Date |
  |------|-----------|------|
  | G1 — version bump | PATCH `0.61.149 → 0.61.150`. CI hotfix only — test-data refresh, no behaviour change. | 25-05 '26 |
  | G2 — destructive action | _n/a_ — pure assertion update. The prior 67/1 values were stale (last touched in v0.59.49); the new 70/4 values match the v0.61.141 cuisines_js.MD content on `origin/main`. | 25-05 '26 |
  | G3 — new decision rule | _n/a_ — test fix; no rule change. | 25-05 '26 |
  | G4 — paid external API call | _n/a_ — test code. | 25-05 '26 |

## Amendments

_None — pure follow-up test refresh._

## Deleted

_None — assertion VALUES updated; the test itself remains in place._
