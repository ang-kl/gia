(№ 569 - 07-06 '26 16:45 SGT) v0.61.369

# Journal — soleat v0.61.369 (Menu pill flag derives from anchor coords — fixes "Bukit Merah with VN flag")

> Operator [Image 7]: *"is the menu TMA behave the same as Cuisine TMA, it shows bukit merah with vn flag"*.
>
> **Left OPEN for review — auto-merge to be armed on the operator's word.** Commit 582 / journal 569 / build 367. Menu TMA.

## New

### [HDR] #569 | 16:45 SGT 07-06-26 | v0.61.369 — Menu flag from anchor coords | 1 code file | 1 PR | 1 build |

- **[INTENT]** Make the Menu location pill's flag match where the anchor actually **is**, not a stale stored country.

- **[ROOT CAUSE]** The compact-pill flag trusted `currentAnchor.region`, then `currentAnchor.country || countryPref` — all **stale** (`VN` / `OTHER` left over from the prior Hanoi session) while the anchor coords were **Bukit Merah = Singapore**. So a SG anchor rendered a 🇻🇳 flag.

- **[MECHANISM]** (`web/menu/src/components/LocationFieldMenu.jsx`, compact pill) — derive the flag from the anchor **coords** (ground truth): `coordsToCountry(anchor)` → SG → 🇸🇬; MY + `isJbCoords` → Johor; otherwise `nearestIataCity(coords).countryCode`. The stored country / `countryPref` is only the last-resort fallback (coords missing). The anchor's coords always reflect the real place — GPS-detected *or* manually picked — so the flag stays coherent.

- **[DELTA]**
  - **`web/menu/src/components/LocationFieldMenu.jsx`** — import `coordsToCountry` + `isJbCoords`; compact-pill flag derived from coords.
  - **`package.json`** — `0.61.368 → 0.61.369`. **`doc/.serial-state.yml`** — commit 581 → 582, journal 568 → 569, build 366 → 367; re-anchored 16:45 SGT.

- **[VERIFICATION (sandbox)]** `npm run build` (menu) clean; full `npx vitest run` → **106 files / 3007 tests green**.

- **[STATUS]** PR-**OPEN**, **auto-merge to arm on the operator's word**. Menu TMA. Field test: in Bukit Merah the Menu pill should show 🇸🇬, not 🇻🇳.

- **[TEST]** Operator: the Menu flag now matches the spot — Singapore in Bukit Merah — regardless of a stale saved country.

- **[KNOWN GAPS — reported, NOT in this PR]**
  - **Stale `region = OTHER` still disables the SG-only tiles** (Hawker / Train / Incidents / Bus stops / Weather) on a SG anchor — same stale-data root. A region reconciliation ("coords in SG → region SG") re-enables them but **touches search behaviour**, so it's deferred for the operator's steer.
  - **Not live-E2E tested** — no Telegram WebView here.

- **Confirmation Gates**

  | Gate | Resolution | Date |
  |---|---|---|
  | G1 — version bump | PATCH `0.61.368 → 0.61.369`. | 07-06 '26 |
  | G2 — destructive action | Display-only flag derivation; no state/persistence/search change; full suite green. | 07-06 '26 |
  | G3 — new decision rule | The Menu pill flag is derived from the anchor's coordinates, not the stored (stale-prone) region/country. | 07-06 '26 |
  | G4 — paid external API call | None — `coordsToCountry` / `nearestIataCity` are local tables. | 07-06 '26 |

## Amendments

- **`web/menu/src/components/LocationFieldMenu.jsx`** — coords-derived compact-pill flag + `coordsToCountry`/`isJbCoords` import.
- **`package.json`** — `0.61.368 → 0.61.369`.
- **`doc/.serial-state.yml`** — counters (commit 582 / journal 569 / build 367) + anchor 16:45 SGT 07-06-26.

## Deleted

- _None._
