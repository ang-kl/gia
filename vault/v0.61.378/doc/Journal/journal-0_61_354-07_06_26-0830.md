(№ 554 - 07-06 '26 08:30 SGT) v0.61.354

# Journal — soleat v0.61.354 (Location-state refactor PR 3 — city changes PREVIEW the map; the search anchor commits only on 🔍)

> Operator: *"go"* (after an honest validation showed PR 2 had only landed `mapViewLocation` + the Back button). This PR ships the **behavioural core** of the 4-location model — the city-pick **decouple** (positive control).
>
> **Left OPEN for review — auto-merge NOT armed.** Commit 567 / journal 554 / build 353. TMA.

## New

### [HDR] #554 | 08:30 SGT 07-06-26 | v0.61.354 — OTHER city pick previews (flies the map) without moving the search; 🔍 commits it | 2 code files | 1 PR | 1 build |

- **[INTENT]** Complete the aircraft-handoff model: changing city should **direct the map** but leave the **confirmed search location under positive control** until the user explicitly confirms (🔍 / "search from here"). Before this, an OTHER city pick committed the search anchor immediately.

- **[MECHANISM]** Surgical — only the **OTHER city dropdown** path changes (typed / GPS / chip picks are untouched):
  - **`LocationField.onCityPick`** now calls `onSelect({ …, cityPreview: true, radiusCapM })` (was `noAutoFire: true`).
  - **`App.onLocationSelect`** gains an early **preview branch**: on `p.cityPreview` it sets `selectedCityLocation` + `flyTarget` (fly the map) and **returns** — it never touches `locationAnchor` / `searchCenter` (the search anchor).
  - **`App.triggerSearch`** (🔍): if a city is previewed, it **commits** by re-calling `onLocationSelect` **without** `cityPreview` — running the full commit (anchor + searchCenter + region sync + server persist + auto-fire search). Otherwise it searches the existing confirmed anchor.
  - **`↩ Back to last search area`** now also **clears** the pending preview (cancel), so a later 🔍 won't commit an abandoned city.

- **[SLOT MAPPING]** `mapViewLocation` ✅ (live centre, PR 2) · `selectedCityLocation` ✅ (new — the previewed city) · `activeSearchLocation` = `locationAnchor`‖`userLoc` ✅ (the slot search uses) · `previousSearchLocation` = implicit (`locationAnchor` is unchanged during preview, so the Back button restores it). Behaviours ①–⑦ of the spec are now satisfied; safeguards: stale-centroid guard ✅ (preview never overwrites), persist-previous ✅, button-until-restored ✅, labels reflect the real (committed) search location ✅. **Debounce of rapid city changes was NOT added** — `panTo` already animates smoothly to the latest pick; noted as a minor follow-up.

- **[DELTA]**
  - **`web/cuisine/src/v2/App.jsx`** — `selectedCityLocation` state; `onLocationSelect` preview branch; `triggerSearch` commit-on-🔍; Back button clears the preview.
  - **`web/cuisine/src/v2/components/LocationField.jsx`** — `onCityPick` → `cityPreview: true`.
  - **`package.json`** — `0.61.353 → 0.61.354`.
  - **`doc/.serial-state.yml`** — commit 566 → 567, journal 553 → 554, build 352 → 353; re-anchored 08:30 SGT 07-06-26.

- **[VERIFICATION (sandbox)]**
  - `npm run build` clean (cuisine bundle compiled); `npx vitest run` → **106 files / 2992 tests green**.
  - Logic trace: OTHER → pick KL → preview (map flies to KL, `selectedCityLocation`=KL, anchor unchanged → Back button shows). Tap 🔍 → commit (anchor+searchCenter=KL, persist, search at KL). OR tap ↩ Back → map flies to the old anchor, preview cleared, 🔍 searches the old anchor.
  - **NOT live-E2E tested** — no browser; the map interaction (fly, button appearance, commit) is logic-verified only. **Operator must eyeball on the deployed TMA.**

- **[STATUS]** PR-**OPEN**, **auto-merge NOT armed**. TMA; Railway rebuilds the cuisine bundle on merge. Eyes-on-live: OTHER → change city → the map flies there, the orange ↩ helper appears, but results/anchor don't move until you tap 🔍.

- **[TEST]** Operator: changing city now previews (flies the map) without moving the search; 🔍 confirms the new city, ↩ Back cancels it. Search, labels, and titles reflect only the confirmed location.

- **[KNOWN GAPS]**
  - **Debounce** of rapid city switching — not added (panTo handles it); minor.
  - **Picker reorder** (capital-first, then size; LA/KH/MM excluded) — still a later PR.
  - **Not live-E2E tested** — needs an eyeball on the deployed TMA.
  - A `doc/Feature/` / `doc/Technical/` / `doc/Register/` update + a `vault/v0.61.354/` snapshot are still due.

- **Confirmation Gates**

  | Gate | Resolution | Date |
  |---|---|---|
  | G1 — version bump | PATCH `0.61.353 → 0.61.354`. | 07-06 '26 |
  | G2 — destructive action | Surgical — only the OTHER city-dropdown path changes from commit→preview; typed/GPS/chip untouched; full suite green. | 07-06 '26 |
  | G3 — new decision rule | An OTHER city change is a PREVIEW (fly the map via `selectedCityLocation`); the search anchor commits only on 🔍 (or is cancelled by ↩ Back). Positive control. | 07-06 '26 |
  | G4 — paid external API call | _n/a_. (The committed search still calls Places as before.) | 07-06 '26 |

## Amendments

- **`web/cuisine/src/v2/App.jsx`** — `selectedCityLocation` + preview branch + commit-on-🔍 + Back clears preview.
- **`web/cuisine/src/v2/components/LocationField.jsx`** — city pick sends `cityPreview`.
- **`package.json`** — `0.61.353 → 0.61.354`.
- **`doc/.serial-state.yml`** — counters (commit 567 / journal 554 / build 353) + anchor 08:30 SGT 07-06-26.

## Deleted

- The immediate-commit behaviour of OTHER city picks (now a preview until 🔍).
