(№ 106 - 11-05 '26 13:00 SGT)

# Register — soleat (delta entry)

> Per `doc/CLAUDE.md` §3 + AU-1: this snapshot supersedes `register-0_60_41-08_05_26-2000.md` by reference. All Open / Deferred / Decisions / Completed items in the prior snapshot remain in force — this counter only adds new entries for the v0.60.42 → v0.60.106 arc.

---

## Completed (append-only addition)

| # | Item | Completed | Version | Rationale |
| :--- | :--- | :--- | :--- | :--- |
| C-401 | `/causeway` (alias `/checkpoint`) live border-crossing camera view | 09-05 '26 | v0.60.72 | LTA Traffic-Imagesv2 + 4-camera allow-list. Photos uploaded directly; Telegram caches. |
| C-402 | `/traffic` incident radius 10 km → 20 km | 09-05 '26 | v0.60.72 | Catches Telok Blangah ⟷ Raffles ⟷ Changi corridor incidents. |
| C-403 | Bus arrivals v2 → v3 endpoint switch | 09-05 '26 | v0.60.76 | `/BusArrival` v3 fixes occasional empty arrivals payload. |
| C-404 | Square-emoji MRT line set | 09-05 '26 | v0.60.76 | 🟥🟧🟪🟩🟦🟫⬜ replaces 🔴🟠🟣🟢🔵🟤⚪. Reads cleaner at 11 px. |
| C-405 | Static MRT line headway footer in `/transport train` | 09-05 '26 | v0.60.75 | LTA-published peak/off-peak frequency table (no upstream call). |
| C-406 | Interactive Google Map view in Train TMA | 09-05 '26 | v0.60.85 | `data/mrt-coords.json` (206 stations: 177 operational + 29 future). PNG schematic remains default; user opts into Google Map view. |
| C-407 | Cuisine criteria preview line (replaces `1c · 0f` count) | 10-05 '26 | v0.60.80 | Renders selected cuisines + filters + prices verbatim under "Search criteria". |
| C-408 | Combo cuisine AND/OR search with banner | 10-05 '26 | v0.60.82 → v0.60.84 | AND single-query attempted first; if ≥1 result whose name/types contain ALL selected cuisines passes validation, surfaces as primary; otherwise OR per-cuisine round-robin + "No exact cuisine combination found" banner. |
| C-409 | Cross-TMA navigation standardisation (aqua FABs + text labels + bottom-flip) | 10-05 '26 | v0.60.92 → v0.60.96 | `#7FDBDB` aqua, ⇠ U+21E0 back glyph, text labels (`back / end / ↑ top / ↓ down`), `atBottom` flip-to-top state, all 4 TMAs aligned. |
| C-410 | Train TMA: `All Lines` reset chip + one-shot PNG → Google Map auto-switch + LRT lines in scroll + popup line status | 11-05 '26 | v0.60.99 | One-shot auto-switch gated by `useRef` (Codex P2 catch). LRT lines via `LINES.filter((l) => !l.future)`. Popup shows colored per-line `🟥 NSL · Normal service` rows. |
| C-411 | `/transport train`: surface LRT stations in nearest enrichment | 11-05 '26 | v0.60.100 | `light_rail_station` added to Google Places `includedTypes`. 35 LRT-only stops added to `STATION_LINES`. |
| C-412 | `/traffic` incident cap removed | 11-05 '26 | v0.60.103 | Renders every island-wide incident; Telegram-chunks at ≤3500 chars. |
| C-413 | `/checkpoint` cameras expanded (allow-list → lat/lng bbox) | 11-05 '26 | v0.60.103 | Two bboxes around Woodlands + Tuas (~3 km each). Header surfaces live camera count + per-checkpoint breakdown. |
| C-414 | `/causeway` alias dropped | 11-05 '26 | v0.60.103 | Only `/checkpoint` dispatches now. |
| C-415 | ICA queue scrape (best-effort) | 11-05 '26 | v0.60.104 | Cheerio + regex heuristics on ICA's public trafficupdates page. 60 s cache. Renders italic markdown with explicit `source: ICA · unofficial scrape` attribution. Graceful null-fallback on parse failure. |
| C-416 | FAB buttons shrunk + lowered across all 4 TMAs | 11-05 '26 | v0.60.105 | `h-7 / px-1.5 / text-[10px] / rounded-b-[14px] / bottom-2`. Search FAB `w-7 h-7 / text-[11px]`. |
| C-417 | FR audit pass #2 + transport TMA i18n module created | 11-05 '26 | v0.60.106 | `web/transport/src/i18n.js` (new). All 4 TMA BackFabs + 3 scroll FABs + transport App.jsx + AffectedTicker.jsx routed through `useLocale()` + `t()`. Operator-visible English-leakage zeroed. |
| C-418 | v0.60.106 vault freeze | 11-05 '26 | v0.60.106 | Reproducible-from-disk mirror at `vault/v0.60.106/`. |

## Reverted (append-only addition)

| # | Item | Reverted | Version | Rationale |
| :--- | :--- | :--- | :--- | :--- |
| R-401 | Cuisine-type label chips on combo result cards | 11-05 '26 | v0.60.102 | Operator: "Revert. Don't implement v0.60.101." The always-on `places.types` field-mask expansion was affecting initial load. `cuisine-google-types.js` deleted; AND-validator logic preserved at v0.60.84 state. |

## Decisions (append-only addition)

| # | Decision | Date | Rationale |
| :--- | :--- | :--- | :--- |
| D-401 | Telegram chunking for uncapped traffic incidents | 11-05 '26 | Telegram's 4096-char message limit. Chunks at newline boundaries with `CHUNK_LIMIT = 3500`; inline keyboard rendered only on the last chunk. |
| D-402 | ICA queue data is best-effort, not authoritative | 11-05 '26 | LTA's queue-count endpoints are deprecated; ICA doesn't publish JSON. The scrape attribution + italic styling tells the user this is an estimate. If parse fails the line is omitted (camera-only fallback). |
| D-403 | All 4 BackFabs and scroll FABs share `useLocale()` + per-TMA i18n keys | 11-05 '26 | Localisation should not depend on which TMA the user enters first. Shared `gia.locale` localStorage key + `gia:locale` CustomEvent already plumbed; just needed to wire the FAB labels through. |

---

## Open + Deferred

(All Open / Deferred from prior register snapshots remain in force. No new items added in this arc.)
