(№ 52 - 13-06 '26 15:45 SGT)

# Technical — soleat / Gia4lunch v0.62.69 (incremental snapshot)

> **Document type:** Technical (per `doc/CLAUDE.md` §3 — *stack, functions, budgets, build constraints*; §4.1 — add + amendment + Deprecated table at EOF).
> **Status:** **Incremental snapshot** (not a master refresh). Continues the v0.61.147 fresh master (`technical-0_61_147-25_05_26-1225.md`). **Scope:** the technical additions of the **v0.62.53 → v0.62.69** arc (PRs #1012 → #1022) — `[LocDrift]` telemetry, the Railway log-collection pipeline, the geo-cap tier architecture, and the loading-overlay styling contract. Every prior versioned Technical file remains on disk per AU-1.
> **Coverage gap (see Register O-44):** the v0.61.148 → v0.62.52 technical arc is **not** consolidated here; it lives in the Journals + VibeCodingRecord only.
> **Reflects:** project state at **v0.62.69, 13-06 '26 15:45 SGT** (PR #1022 merged at `a3429c0`).

---

## 1. Stack (additions this arc)

| Layer | Technology | Version | Purpose |
|---|---|---|---|
| CI / ops | GitHub Actions (`actions/*` → **v5**, D-62.59–61) | — | Scheduled Railway log fetch (15-min / hourly) committing to the `railway-logs` branch |
| Telemetry | `loc-drift-log.js` (new module) | — | Emits structured `[LocDrift]` JSON lines + `r5()` coord rounding helper; default-on, best-effort (never throws into the search path) |

No new runtime dependencies; the log pipeline is Actions + git only.

## 2. Functions / Modules (additions this arc)

| Module / symbol | Role | Introduced |
|---|---|---|
| `loc-drift-log.js` → `logLocDrift(kind, payload)`, `r5(n)` | Structured drift telemetry: `set-location` (anchor moves) + `search-anchor` (the searched center/radius/cap). | #1012 (v0.62.53) |
| `index.js` cuisine-search `capSource` thread-point (~14574) | Single place that resolves the OTHER/non-JB radius cap and tags its origin: `explicit` → `city-default` → `other-default` → `none`. Feeds the `search-anchor` log + the ladder/refetch ceiling. | #1012 |
| `city-centroids.js` `_radiusForCity(city)` → **`TIGHT_15KM` set** | New radius tier (15 km) evaluated **before** DENSE-30 / SPARSE-60 / MAJOR-45. Members: `Putrajaya`, `Cyberjaya`. Applied programmatically over the 139-row centroid literal (the literal stays a pure coordinate source). | #1021 (v0.62.68) |
| `place-search-variance.js` `nearestCityRadiusM(lat,lng)` | Unchanged signature; now returns 15 000 for coords nearest Putrajaya/Cyberjaya. Regression-guarded against the exact logged drift pin `(2.96957, 101.71218)`. | #1021 |

## 3. Budgets / cost notes (this arc)

- **AI-credit dependency is a hard single-point failure** (Register O-43). When the Gemini key (`[Extract-Dishes]`, 3-model fallback) **and** the Anthropic key (`vibe-summary`, claude-haiku-4-5) are both out of credit, every enrichment path returns empty with no user-facing signal. Observed live in `log/logs.1781315803421.json` (429 prepayment-depleted + 400 credit-too-low). No spend guardrail / alert exists yet.
- Log-collection pipeline cost: GitHub Actions minutes only; no paid-API calls (G4 not engaged).

## 4. Build Constraints (this arc)

- **`main` is protected** (4 required checks: Vitest, Syntax, TMA build, Empty-blob; `enforce_admins=true`). Automated pushes (the log collector) **cannot** target `main` → they write to the unprotected **`railway-logs`** branch (D-62.62). No protection bypass / ruleset was added.
- **Loading-overlay styling contract (D-62.69):** the busy indicator must match `FunFactModal` — opaque `bg-tg-card`, `border-2 border-tg-accent`, `ring-tg-accent/30`, no card-level `backdrop-blur` (the wrapper scrim `bg-black/30 backdrop-blur-sm` provides the page blur). Any future overlay restyle should keep these in sync to avoid the v0.62.66→.69 first-load/translucent regression.
- TMA changes (`web/cuisine/**`) still require the `build:cuisine` job green; the `web/cuisine` Vite build emits to the **gitignored** `public/cuisine/assets/` (built at deploy, not committed). `public/doc/vibe-journal.html|json` **are** tracked (the hosted Vibe Journal page) and are regenerated by `doc/VibeCodingRecord/generate.mjs`.

## 5. Diagnostic Codes Index (additions this arc)

| Code | Emitted by | Meaning |
|---|---|---|
| `[LocDrift] set-location` | `index.js` location-set path | Anchor write: `{chat,dev,from,to,moved_m,label,region,country,cap_m,ambient,kept}`. |
| `[LocDrift] search-anchor` | `index.js` cuisine-search | The searched anchor: `{chat,center,region,radius_m,cap_m,cap_src}`. `cap_src:"city-default"` + large `radius_m` = wide-radius drift signature. |
| `D777b` | `index.js` (~14607) | `anchorCap city-default <m>m (nearest-city radiusM, no explicit cap)` — the city-default fill fired (no persisted `radiusCapM`). After #1021, Putrajaya/Cyberjaya report 15000 here, not 45000. |
| `D781` | `index.js` (~14627) | `OTHER radius ceiling <a>m → <b>m` — the 40 km OTHER hard ceiling clamp (when no stricter per-anchor cap). |

---

## Deprecated Decisions

| # | Item | Original Version | Deprecated in Version | Date (DD-MM 'YY) | Reason | Replacement | Rule Reference | Serial № |
|---|------|------------------|------------------------|--------------------|--------|-------------|----------------|----------|
| DT-1 | Putrajaya / Cyberjaya inherit the **MAJOR 45 km** `city-default` radius cap | v0.61.441 (density tiers) | v0.62.68 | 13-06 '26 | A 45 km cap from a Putrajaya pin reached central KL / PJ / Shah Alam (20–23 km) — the logged "drift to KL" | `TIGHT_15KM` tier (15 km) in `_radiusForCity` | D-62.68 | (№ 52 - 13-06 '26 15:45 SGT) |
| DT-2 | First-load loading overlay card style `bg-tg-card/95` + card-level `backdrop-blur` + `border-tg-border/60` (frosted/translucent) | v0.62.66 | v0.62.69 | 13-06 '26 | First load looked translucent while the `FunFactModal` fact card was solid — visually inconsistent | Opaque `bg-tg-card` + `border-2 border-tg-accent` + `ring-tg-accent/30`, no card blur | D-62.69 | (№ 52 - 13-06 '26 15:45 SGT) |

## Rule reference

- AU-1 (add, never compress), AU-2 (Deprecated table at EOF) — `doc/CLAUDE.md` §4.
- Companion Register: `doc/Register/register-0_62_69-13_06_26-1545.md` (D-62.53 → D-62.69, O-42 → O-44).
