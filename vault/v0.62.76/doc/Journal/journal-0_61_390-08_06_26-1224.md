(№ 590 - 08-06 '26 12:24 SGT) v0.61.390

# Journal — soleat v0.61.390 (Review quote: fall back past the 4-month recency filter)

> Operator: *"where are the reviews and the full result card template when loaded? it is 15 seconds threshold that spill out the results"* → chose fix **1**.
>
> **Left OPEN for review — auto-merge to be armed on the operator's word.** Commit 603 / journal 590 / build 380 (held). Server-only.

## New

### [HDR] #590 | 12:24 SGT 08-06-26 | v0.61.390 — review snippet falls back past the 4-month filter | index.js | 1 PR | 0 build |

- **[INTENT]** The TMA cards showed **no 💬 review quote** (and looked sparse) on many SG venues. The operator suspected a "15-second threshold" spilling results early.

- **[INVESTIGATION — traced 100%, read-only]**
  - The **full card template IS coded** and runs on every TMA search — `restaurantType` (`index.js:15483`, set via assignment so an earlier `restaurantType:` grep missed it), `recentReview` (15322), `enrichPriceRangeDisplay`. `DISCOVER_FIELD_MASK` requests reviews/type/priceRange and `pipeline.discover` **maps them onto every venue** (`pipeline.js:1158/1150/1175`).
  - **No 15-second cutoff exists** — searched all server `.js` + the client (`App.jsx`/`api.js`). The only `15000`s are a bot please-wait nudge, a Gemini per-attempt in a different path, the fact-card rotation, and distance ladders. The handler `res.json`s **only after** enrichment (`15677`). The ~15s is the genuine slow search, **not a timer dropping data**.
  - **Root of the missing 💬:** `extractDishes` (`index.js ~15256`) filtered reviews to a **4-month** window (`FOUR_MONTHS_MS`) and returned `snippet: null` when none were recent — **no fallback to older reviews**. Places returns its *"most relevant"* reviews, often **>4 months old**, so popular venues (Keng Eng Kee, etc.) showed no quote.
  - type/price gaps are genuine **Google data gaps** (no `primaryTypeDisplayName` / no `priceRange` for some hawker venues).

- **[DELTA]** (operator chose fix **1**, `index.js` `extractDishes`)
  - NEW `pool = recent.length ? recent : withText` — the snippet + dish-scan fall back to the **best available** review when none are within 4 months; recent reviews are still preferred. So the **💬 quote shows whenever Google has any review**.
  - The same closure also serves the `place-reviews:` cache fallback (`15344`) → fixed there too.
  - **`package.json`** `0.61.389 → 0.61.390`; **`doc/.serial-state.yml`** commit 602 → 603, journal 589 → 590 (**build held 380**); re-anchored 12:24 SGT.

- **[VERIFICATION (sandbox)]** `node --check index.js` clean. Full suite **113 files / 3088 tests**.

- **[STATUS]** PR-**OPEN**, **auto-merge to arm on the operator's word**. Railway redeploys on merge.

- **[TEST]** Operator: the venue cards now show a 💬 review quote even when the venue's Google reviews are all older than 4 months (most popular SG venues).

- **[KNOWN GAPS]**
  - The **Michelin path** (`index.js ~9471`) has the same 4-month pattern — a separate surface, not in the screenshots; left for a follow-up.
  - **type / price** stay blank when Google genuinely has no `primaryTypeDisplayName` / `priceRange` for a venue (data gap, not fixable here).
  - The inline closure isn't separately unit-tested (the enrichment is inline in the handler); verified via `node --check` + full suite + the operator's live eyeball.
  - There is **no 15-second cutoff** — confirmed; the ~15s is search latency, addressed separately (social was the 30s; v0.61.389 removed it).

- **Confirmation Gates**

  | Gate | Resolution | Date |
  |---|---|---|
  | G1 — version bump | PATCH `0.61.389 → 0.61.390`. | 08-06 '26 |
  | G2 — destructive action | None — additive fallback; recent reviews still preferred. | 08-06 '26 |
  | G3 — new decision rule | The review quote falls back to the best available review when none is within 4 months (so it's never blank when reviews exist). | 08-06 '26 |
  | G4 — paid external API call | None. | 08-06 '26 |

## Amendments

- **`index.js`** — `extractDishes` `pool` fallback (recent → any review with text).
- **`package.json`** — `0.61.389 → 0.61.390`.
- **`doc/.serial-state.yml`** — counters (commit 603 / journal 590 / build held 380) + anchor 12:24 SGT 08-06-26.

## Deleted

- _None_ (the 4-month recency *preference* is kept; only the hard null-on-no-recent is replaced with a fallback).
