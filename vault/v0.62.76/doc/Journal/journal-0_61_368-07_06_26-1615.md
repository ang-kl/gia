(№ 568 - 07-06 '26 16:15 SGT) v0.61.368

# Journal — soleat v0.61.368 (REVERT the 0.3 s gate settle — un-hang Menu→Cuisine boot)

> Operator: *"still hung. when I load menu tma and then cuisine tma."* (after v0.61.367)
>
> **Left OPEN for review — auto-merge to be armed on the operator's word.** Commit 581 / journal 568 / build 366. Cuisine TMA.

## New

### [HDR] #568 | 16:15 SGT 07-06-26 | v0.61.368 — revert the v0.61.365 gate settle | 1 code file | 1 PR | 1 build |

- **[INTENT]** Stop the Cuisine TMA hanging on boot after the Menu TMA. A hung TMA is far worse than the cosmetic issue the settle was trying to fix.

- **[EVIDENCE]** Railway (v0.61.367, device in SG / Bukit Merah `1.2722,103.8117`): the cuisine **set-location persist fired** (`label="Bukit Merah"`) but **no cuisine search ever reached the server** — the gate/load path ran without `warmStart`/`runSearch` firing, so the splash hung. The operator had just been in Hanoi, so the **per-device** `countryPref=VN` / `region=OTHER` were stale on return to SG, compounding the state churn the timer raced against.

- **[ROOT CAUSE]** The v0.61.365 0.3 s settle (and the v0.61.367 one-shot guard) were **too fragile** around the simultaneous coherence-check + 20 s-sync + `runSearch` state churn. Two attempts to tame the timer both left a hang.

- **[MECHANISM]** Full **revert** of the v0.61.365 gate change. `web/cuisine/src/v2/App.jsx` gate-opener is back to the v0.61.323/324 **synchronous** open: `setLocationGateOpen(true)` + `runInitialLoad()` the instant `userLoc` resolves and no modal is pending; deps restored to include `state.region`. Removed the 300 ms `setTimeout`, the reverse-geocode/persist IIFE, and the `gateSettleScheduledRef`.

- **[DELTA]**
  - **`web/cuisine/src/v2/App.jsx`** — gate-opener reverted to synchronous; `gateSettleScheduledRef` removed.
  - **`package.json`** — `0.61.367 → 0.61.368`. **`doc/.serial-state.yml`** — commit 580 → 581, journal 567 → 568, build 365 → 366; re-anchored 16:15 SGT.

- **[VERIFICATION (sandbox)]** `npm run build` (cuisine) clean (App bundle back to 257.91 kB = pre-settle); full `npx vitest run` → **106 files / 3007 tests green**.

- **[STATUS]** PR-**OPEN**, **auto-merge to arm on the operator's word**. Cuisine TMA. Field test: load Menu → load Cuisine → it should open + load (or show the mismatch modal to resolve), no hang.

- **[TEST]** Operator: the Cuisine TMA no longer hangs after the Menu TMA; if there's a stale-country mismatch it shows the prompt to resolve, then loads.

- **[KNOWN GAPS]**
  - **Image-1 cosmetic returns** — on a slow async `countryPref` load the mismatch modal can again appear over a loading body. Accepted vs the hang. The proper fix is a **deterministic `countryPref`-loaded gate** (wait for the seed fetch to finish, not a timer) — a careful follow-up if the operator wants it.
  - **Stale per-device `countryPref`/`region`** across country moves (VN→SG) still triggers a coherence modal each time — pre-existing, separate from this revert.
  - **Not live-E2E tested** — no Telegram WebView here.

- **Confirmation Gates**

  | Gate | Resolution | Date |
  |---|---|---|
  | G1 — version bump | PATCH `0.61.367 → 0.61.368`. | 07-06 '26 |
  | G2 — destructive action | Revert to a prior known-good code path; full suite green; bundle back to pre-settle size. | 07-06 '26 |
  | G3 — new decision rule | The boot gate opens synchronously again (no 0.3 s settle, no boot-persist); the Image-1 fix is deferred to a deterministic-gate follow-up. | 07-06 '26 |
  | G4 — paid external API call | Removes the per-boot reverse-geocode the settle added. | 07-06 '26 |

## Amendments

- **`web/cuisine/src/v2/App.jsx`** — revert gate-opener to synchronous (v0.61.323/324); remove settle ref.
- **`package.json`** — `0.61.367 → 0.61.368`.
- **`doc/.serial-state.yml`** — counters (commit 581 / journal 568 / build 366) + anchor 16:15 SGT 07-06-26.

## Deleted

- The v0.61.365/367 0.3 s gate settle + persist-before-load (reverted).
