(№ 482 - 30-05 '26 17:05 SGT) v0.61.281

# Journal — soleat v0.61.281 (5-chip JB focus row + 8s suffix auto-hide)

> Operator (30-05 '26 16:55 SGT, screenshot annotation on a v0.61.280 build at JB Mid Valley Southkey):
> 1. *"the '5 places nearby · tap to change 🔝' should not be permanent, it shows for 8 seconds then disappear."*
> 2. *"For 'Johor Bahru' mode, replace 'Default focus:' to 3 places in this order 'Legoland', 'Bukit Indah', 'CBD', 'Southkey' 'Mt Austin'. register these five places. show as small pill font size. build together and ship as v0.61.281."*
>
> Two coordinated UI tweaks in the Cuisine LocationField:
>
> 1. **Suffix auto-hide.** New `suffixVisible` state in both `LocationField` and `OtherLocationPicker` components. Default `true` on mount / whenever `suffix` content changes (next search / new anchor); an 8 s `setTimeout` flips it `false`. The pill below the location row ("{N} places nearby · tap to change 🔝") collapses out of view once the user has had a chance to notice it.
>
> 2. **5-chip JB focus row.** The v0.61.268 2-chip row ("Default focus: Southkey · JB CBD") becomes a flat 5-chip row: `Legoland · Bukit Indah · CBD · Southkey · Mt Austin`. Order matches the operator's screenshot annotation (Legoland first, Mt Austin last). The "Default focus:" prefix is dropped. Same `text-[10px]` mobile pill font. Chip keys + short labels live in `web/cuisine/src/v2/lib/jb-focus-points.js` so the catalogue is single-source.
>
> Per-chip coords:
>
> - Legoland Malaysia: (1.4296, 103.6321)
> - Bukit Indah: (1.4773, 103.6645)
> - JB CBD: (1.4927, 103.7414) ← v0.61.277 constant
> - Mid Valley Southkey: (1.4912, 103.7665) ← v0.61.277 constant
> - Mount Austin: (1.5252, 103.7935)
>
> Four of the five lats are >1.47 (north of SG_LAT_MAX); **Legoland is at lat 1.4296** — south of the v0.61.276 flat `SG_LAT_MAX=1.47`. The pre-v0.61.281 bbox over-claimed it for SG (rectangular bbox doesn't know the SG-Johor strait drops to ~lat 1.36 in the west). v0.61.281 fixes this with a west-of-strait carve-out in `coords-to-country.js`: for `lng < 103.70`, `SG_LAT_MAX_WEST = 1.42`. Sembawang (1.45, 103.82) / Woodlands (1.44, 103.79) / Pulau Ubin (1.41, 103.96) / Pulau Tekong (1.42, 104.03) all sit at `lng >= 103.70` so the 1.47 max still applies. Tuas (1.358, 103.65) stays SG via the 1.42 cap. Legoland (1.4296, 103.6321) now correctly classifies as JB.
>
> Commit 495 / journal 482 / build 284, on branch
> `claude/v0.61.281-jb-5-chip-row-and-suffix-autohide` off `main` at v0.61.280.

## New

### [HDR] #482 | 17:05 SGT 30-05-30 | v0.61.281 — 5-chip JB row + 8s suffix auto-hide + SG bbox west carve-out | 6 files | 1 PR | 1 build |

- **[INTENT]** Two minor UX tweaks from the screenshot annotation: ① reduce visual clutter by auto-hiding the location suffix line after 8 s; ② let users one-tap any of 5 JB anchor presets (Legoland → Mt Austin reading left-to-right) instead of the v0.61.268 binary Southkey/CBD chip.

- **[DELTA]**

  **1. `web/cuisine/src/v2/lib/jb-focus-points.js`** — expanded from 2 keys (`southkey`, `cbd`) to 5 (`legoland`, `bukitIndah`, `cbd`, `southkey`, `mtAustin`). New export `JB_FOCUS_CHIP_LABELS` carries the short chip labels (`'Legoland'`, `'Bukit Indah'`, `'CBD'`, `'Southkey'`, `'Mt Austin'`) so the long-form name (e.g. `'Legoland Malaysia'`) is reserved for the resting-pill label after the chip is picked. `JB_FOCUS_DEFAULT` stays `'southkey'` (backwards-compat with the v0.61.277 pill auto-anchor).

  **2. `web/cuisine/src/v2/components/LocationField.jsx`** — three changes:
  - Import gains `JB_FOCUS_KEYS, JB_FOCUS_CHIP_LABELS`.
  - The v0.61.268-280 hardcoded 2-chip block (Southkey + CBD) is replaced with a `JB_FOCUS_KEYS.map((key) => …)` render so the order + count is data-driven.
  - The "Default focus:" prefix span is removed (chip row stands on its own).
  - New `suffixVisible` state + `useEffect` 8 s timer in `LocationField` (SG/JB path).
  - Same `suffixVisible` state + timer mirrored in `OtherLocationPicker`.
  - Both suffix render sites gain a `&& suffixVisible` guard.

  **3. `web/cuisine/src/v2/lib/coords-to-country.js`** — west-of-strait carve-out (CI follow-up after the first PR-787 vitest red). New constants `SG_LNG_WEST_THRESHOLD = 103.70` and `SG_LAT_MAX_WEST = 1.42`; new internal `_inSgBbox` that applies the tighter cap for `lng < 103.70`. `coordsToCountry` + `isJbCoords` switched from `_inBbox` to `_inSgBbox` for the SG check. Without this Legoland (1.4296, 103.6321) classified as SG and the v0.61.276 region-coords coherence modal would fire on chip tap.

  **4. `__tests__/jb-focus-points.test.js`** — rewritten for the v0.61.281 shape: 5-key order, coords for the 3 new entries (Legoland / Bukit Indah / Mt Austin), `JB_FOCUS_CHIP_LABELS` contract, and a per-focus-point `isJbCoords(...) === true` + `coordsToCountry(...) === 'MY'` sweep. The pre-v0.61.281 "all five > 1.47" check is dropped (Legoland breaks it by design); the new contract is the actual function behaviour, not the raw lat threshold.

  **5. `package.json`** — `0.61.280 → 0.61.281`.

  **6. `doc/.serial-state.yml`** — commit 494→495, build 283→284, journal 481→482; re-anchored 30-05 '26 17:05 SGT.

- **[VERIFICATION (sandbox)]**

  - `node --check web/cuisine/src/v2/lib/jb-focus-points.js` — clean.
  - `esbuild.transform` (loader: jsx) of `LocationField.jsx` — clean.
  - Vitest still BLOCKED locally by the recurring iCloud `ERR_INVALID_PACKAGE_CONFIG` throttle (Register O-22). CI will run authoritatively. Expected: `jb-focus-points.test.js` 11/11 pass (was 9/9 on v0.61.278).

- **[STATUS]** PR-OPEN on push. Railway redeploys the Cuisine TMA on merge (no server change).

- **[TEST]** Operator (post-merge):
  1. **Suffix auto-hide.** Open Cuisine TMA. Pick any location. The "{N} places nearby · tap to change 🔝" line appears below the compact pill. Wait 8 s — line fades out. Tap another location — line re-appears (because `suffix` content changed) and starts a fresh 8 s timer. Same behaviour in OTHER region.
  2. **5-chip JB row, order.** Tap JB region pill. Below the compact pill, the chip row reads left-to-right: `Legoland Bukit Indah CBD Southkey Mt Austin`. No "Default focus:" prefix.
  3. **5-chip JB row, tap-to-anchor.** Tap "Legoland" chip. Pin + label flip to `Legoland Malaysia` (1.4296, 103.6321). Tap "Mt Austin" → pin + label flip to `Mount Austin` (1.5252, 103.7935). Tap "CBD" → 1.4927, 103.7414. All five chips behave identically to the v0.61.277 anchor-commit behaviour.
  4. **5-chip JB row, active highlight.** The chip whose key matches `jbFocusKey` is rendered with `bg-tg-accent` + `font-semibold`; the other four sit in the muted `bg-tg-card / text-tg-hint` style.
  5. **Default focus on first JB tap.** From SG, tap JB region pill (no prior chip pick). Per v0.61.277, the pill auto-anchors to Southkey (the `JB_FOCUS_DEFAULT`). The Southkey chip should render in the active style; the other four in muted.
  6. **FR locale.** Both behaviours unchanged in FR (chip labels are place names, no translation).

- **[KNOWN GAPS]**
  - **Chip row may wrap on narrow phones.** 5 chips at `text-[10px]` + `px-1.5` plus `gap-1` should fit a 320 px viewport, but on a 280 px iPhone SE-era device the row may wrap. The container has `flex-wrap` so a wrap degrades gracefully.
  - **No telemetry for chip taps.** Could log which chip is most-used to inform future tuning of `JB_FOCUS_DEFAULT`.
  - **The Legoland coord (1.4296, 103.6321)** is the park entrance — close enough to the JB extent (>1.42 N) but slightly south of CBD. Search results centred on Legoland will pick up Iskandar Puteri / Pendas / Gelang Patah. Per the JB-hybrid filter (60 km from JB CBD), these still fall inside the JB envelope.
  - **The `suffix` prop is shared with the compact pill body** (`📍 <place> · <suffix>`) which is NOT gated on `suffixVisible`. Only the standalone "tap to change 🔝" line is auto-hidden. Per operator's screenshot, that's the right behaviour — the place name + suffix INSIDE the pill stays permanently visible.

- **Confirmation Gates**

  | Gate | Resolution | Date |
  |---|---|---|
  | G1 — version bump | PATCH `0.61.280 → 0.61.281`. Two UI tweaks (suffix timer + chip row expansion). No API contract change; no server change. | 30-05 '26 |
  | G2 — destructive action | _n/a_. All changes are additive / replacement-of-equivalent (the 2-chip block becomes the 5-chip block). | 30-05 '26 |
  | G3 — new decision rule | _n/a_. Codifies the operator's screenshot annotation as a one-off UX adjustment. | 30-05 '26 |
  | G4 — paid external API call | _n/a_. Pure client-side change. | 30-05 '26 |

## Amendments

- **`web/cuisine/src/v2/lib/jb-focus-points.js`** — expanded `JB_FOCUS_POINTS` from 2 → 5 keys; new `JB_FOCUS_CHIP_LABELS` export; same `JB_FOCUS_KEYS` + `JB_FOCUS_DEFAULT` + `getJbFocus` API.
- **`web/cuisine/src/v2/components/LocationField.jsx`** — `JB_FOCUS_KEYS.map` chip render; "Default focus:" prefix removed; new `suffixVisible` state + 8 s timer in both inner components.
- **`__tests__/jb-focus-points.test.js`** — rewritten for the v0.61.281 5-key shape; v0.61.278 frozen-array fix preserved via `Array.from(JB_FOCUS_KEYS)`.

## Deleted

_None._
