(№ 503 - 31-05 '26 09:35 SGT) v0.61.302

# Journal — soleat v0.61.302 (durian-variance-runner unit tests + test seam)

> Operator (31-05 '26 09:25 SGT, post v0.61.301 Register refresh): *"2"* — Register O-41 (just raised in v0.61.301): unit tests for `durian-variance-runner.js`. Would have caught the v0.61.283/.288 silent placeId-loss at write-time.
>
> Two changes in this PR:
>
> 1. **Test seam in `durian-variance-runner.js`**: `runVariance({...})` accepts an optional `_searchTextFn` parameter (defaults to the real internal `_searchText`). Tests inject a stub that returns canned Places-shaped responses → no network, no `GOOGLE_MAPS_API_KEY`. Mirrors the `_genAIFactory` pattern in `durian-gemini-verifier.js`. Also exposes `_placeToVenue` via `module.exports` for direct unit testing.
>
> 2. **New `__tests__/durian-variance-runner.test.js`** (~200 lines, ~17 assertions across 4 describe blocks):
>    - **Data exports**: REGIONS_DEFAULT shape, SEEDS_DURIAN / SEEDS_DURIAN_PASTRY language coverage, PLACES_LANG coverage.
>    - **`_placeToVenue` placeId carry** (the v0.61.283/.288 regression contract): placeId from `p.id`, fallback to `''` for missing or non-object input, full venue-shape matching.
>    - **`runVariance` input validation**: unknown mode + missing apiKey both throw.
>    - **`runVariance` end-to-end with the stub**: report shape, every kept[] row has placeId, every rejected[] row has placeId, mode='durian-pastry' works, call count, onProgress fires per query, log callback fires for region+lang+query lines (the v0.61.293 addition), precision computed correctly, empty-results path.
>
> The critical assertion — **every kept and rejected row carries a non-empty placeId** — guards against the silent regression class that bit us in v0.61.283 (CLI), v0.61.288 (bot runner), and ran undetected for 28 patches in production.
>
> Commit 516 / journal 503 / build 305, on branch
> `claude/v0.61.302-variance-runner-tests` off `main` at v0.61.301.

## New

### [HDR] #503 | 09:35 SGT 31-05-31 | v0.61.302 — variance-runner unit tests + test seam | 4 files | 1 PR | 1 build |

- **[INTENT]** Close Register O-41 (raised earlier today in v0.61.301). Add unit tests that would have caught the v0.61.275 → v0.61.287 silent placeId-loss class of bug at write-time, plus general regression coverage for the runner's data exports and `runVariance` flow.

- **[DELTA]**

  **1. `durian-variance-runner.js`** — two additions:
  - **`_searchTextFn` injection seam** in `runVariance` opts. Default: real `_searchText`. Tests pass a stub. Inline comment cross-references the `_genAIFactory` pattern in `durian-gemini-verifier.js`.
  - **`_placeToVenue` added to `module.exports`** so tests can assert on it directly without going through `runVariance`.

  **2. `__tests__/durian-variance-runner.test.js` (NEW, ~200 lines)** — 4 describe blocks, ~17 assertions:

  | Section | What it asserts |
  |---|---|
  | `data exports` | REGIONS_DEFAULT 4 cities; SEEDS_DURIAN + SEEDS_DURIAN_PASTRY 12 BCP-47 langs; every pastry-EN seed contains `"durian"`; PLACES_LANG covers all SEEDS keys |
  | `_placeToVenue` | **placeId from `p.id`**, fallback to `''` for missing / non-object, full venue shape with reviews + publishTime + rating |
  | `runVariance` validation | Unknown mode throws; missing apiKey throws |
  | `runVariance` flow | Report shape; **every kept + rejected row has placeId**; durian-pastry mode works; _searchTextFn called expected count; onProgress per query; log callback fires region+lang+query+primary-type lines (v0.61.293 contract); precision math; empty-results path |

  The bolded assertions are the v0.61.283/.288 regression contract.

  **3. `package.json`** — `0.61.301 → 0.61.302`.

  **4. `doc/.serial-state.yml`** — commit 515→516, build 304→305, journal 502→503; re-anchored 31-05 '26 09:35 SGT.

- **[VERIFICATION (sandbox)]**

  - `node --check durian-variance-runner.js` — clean.
  - `node --check __tests__/durian-variance-runner.test.js` — clean.
  - Local runtime check via `node -e "require(...)"` hit ANOTHER iCloud-stale-checkout victim (`mime-db/db.json` empty; transitive axios dep). The test file parses cleanly; CI will run the actual vitest suite with fresh `npm install` node_modules.
  - **Vitest now a required CI check** (v0.61.296 branch protection) — this PR can't merge if any assertion fails.

- **[STATUS]** PR-OPEN on push. No prod runtime impact — pure test coverage + a backward-compatible test seam in the runner.

- **[TEST]** Operator (post-merge):
  1. **CI vitest green.** All new test assertions pass. Total vitest count grows by ~17.
  2. **Future-proof.** Any PR that touches `_placeToVenue` or the `kept[]` row builder will fail CI if it drops `placeId`. Catches v0.61.283/.288-class bugs at the PR boundary.
  3. **No regression.** The new `_searchTextFn` opt is optional with default = real `_searchText`. Production calls `runVariance({ mode, apiKey, onProgress })` and continue working unchanged.

- **[KNOWN GAPS]**
  - **No matching test for `scripts/durian-variance.js`** — that file is now (v0.61.293) a 71-line CLI wrapper that just calls `runVariance`. The runner's tests cover the substance; the CLI wrapper is barely testable beyond "did argv parse work."
  - **No matching test for `durian-gemini-verifier.js`** — already has its own tests via `__tests__/durian-gemini-verifier.test.js`. Coverage there is decent.
  - **The test stub returns identical 2-place responses for every query.** Real Places returns highly variable per-query venue lists. A more realistic fixture set (e.g. SEEDS-specific responses) would be more thorough but adds complexity. The placeId-contract assertion doesn't care about variability.
  - **The "graceful failure" test currently uses an empty-array stub** (mirroring the real `_searchText`'s catch path), not a stub that throws. If a future code change makes the runner brittle to `_searchTextFn` throwing, that test won't catch it. Could add a separate "stub throws" test if the operator wants belt-and-braces.

- **Confirmation Gates**

  | Gate | Resolution | Date |
  |---|---|---|
  | G1 — version bump | PATCH `0.61.301 → 0.61.302`. Test coverage + small backward-compatible test seam in production module. | 31-05 '26 |
  | G2 — destructive action | _n/a_. Additive only — new test file + one export + one optional param. | 31-05 '26 |
  | G3 — new decision rule | _n/a_. Codifies the existing v0.61.275 placeId-carry contract via a test. | 31-05 '26 |
  | G4 — paid external API call | _n/a_. Tests use a stub; no Places API call. | 31-05 '26 |

## Amendments

- **`durian-variance-runner.js`** — `runVariance` opts gain `_searchTextFn` (default = real `_searchText`); `_placeToVenue` added to `module.exports`. Both for test injection / direct unit testing.
- **`__tests__/durian-variance-runner.test.js`** — NEW. ~17 assertions across 4 describes. The v0.61.283/.288 regression contract is now executable.

## Deleted

_None._
