(№ 440 - 28-05 '26 21:00 SGT) v0.61.239

# Journal — soleat v0.61.239 (First-load cap 24 → 5)

> Operator (Issue 4 of 5): *"The first load of opening Cuisine TmA
> is always 5, right now is 48, 84 for Others."*
>
> Commit 452 / journal 440 / build 242, on branch
> `claude/v0.61.239-first-load-cap` off `main` at v0.61.238.

## New

### [HDR] #440 | 21:00 SGT 28-05-28 | v0.61.239 — FIRST_TAP_SLICE 24 → 5 | 3 files | 1 PR | 1 build |

- **[INTENT]** A curated first-impression of 5 best matches when the user opens the Cuisine TMA, instead of a 24-venue wall (and the 48 / 84 cumulative counts in OTHER mode that operator was seeing).

- **[DELTA]**

  **`index.js`** — single constant change at the cuisine-search handler:
  ```js
  // v0.61.239 — was 24 (v0.61.170 split). Operator: "first load
  // is always 5". Subsequent taps keep the 12-per-batch ceiling.
  const FIRST_TAP_SLICE = 5;
  const FOLLOW_UP_SLICE = 12;
  ```

  The TMA's autoReset + counter copy keys on `firstBatch` /
  `finalBatch` / `cumulativeStart-End` which all continue to work
  with the new ceiling.

  **`package.json`** — `0.61.238 → 0.61.239`.

  **`doc/.serial-state.yml`** — commit 451→452, build 241→242, journal 439→440; re-anchored 28-05 '26 21:00 SGT.

- **Impact:**

  | Tap | Before (v0.61.170) | After (v0.61.239) |
  |---|---|---|
  | 1st load | 24 venues | **5 venues** |
  | 2nd tap | 12 | 12 |
  | 3rd tap | 12 | 12 |

  Saves ~$0.04 per first-load (24 → 5 = 19 fewer Routes Distance Matrix calls).

- **[VERIFICATION (sandbox)]**

  ```
  $ node --check index.js                       (OK)
  ```

  No vitest changes needed — the slice logic is keyed on a
  numeric constant, not on the value 24 specifically.

- **[STATUS]** PR-OPEN on push.

- **[TEST]** Operator (post-merge + Railway redeploy):
  1. Fresh Cuisine TMA open → first tap of any criteria → result list shows 5 venues.
  2. Tap 🔍 again → next 12 venues append.
  3. Operator sees cumulative range counter increment 1-5, then 6-17.
  4. autoReset (the v0.60.188 < 12 → resetSeen) still works at the new ceiling.

- **[KNOWN GAPS]**
  - The 24 → 5 change is server-side. If a future client-side rendering decision depends on receiving ≥ 12 venues per fetch (e.g. masonry layout settling), expect a render glitch on first tap. Verified by inspection that the TMA's ResultPanel doesn't have that dependency.
  - Operator's "48, 84 for Others" numbers likely reflected cumulative across multiple taps. The first-tap fix addresses the underlying perception.

- **Confirmation Gates**

  | Gate | Resolution | Date |
  |---|---|---|
  | G1 — version bump | PATCH `0.61.238 → 0.61.239`. One-constant change. | 28-05 '26 |
  | G2 — destructive action | _n/a_. | 28-05 '26 |
  | G3 — new decision rule | YES — first-tap slice is now 5 venues across all regions / cuisines. Future tuning should respect this curated-first-impression intent. | 28-05 '26 |
  | G4 — paid external API call | Net REDUCTION (24 → 5 candidates per first tap means fewer downstream enrichment fetches). | 28-05 '26 |

## Amendments

- **`FIRST_TAP_SLICE`** — was 24 (v0.61.170). Now 5.

## Deleted

_None._
