(№ 489 - 30-05 '26 21:25 SGT) v0.61.288

# Journal — soleat v0.61.288 (v0.61.283 follow-up: durian-variance-runner.js placeId carry)

> Operator (30-05 '26 21:13 SGT, post-PR-#793 merge): dropped two fresh JSON paths in chat —
> - `~/Downloads/durian-gemini-verified-2026-05-30.json`
> - `~/Downloads/durian-pastry-variance-2026-05-30.json`
>
> Both ran via `/ver` on the v0.61.286 live deploy (ranAtIso `2026-05-30T13:10:10.625Z` ≈ 21:10 SGT). Inspection showed every venue still had `placeId: ""` — the same root-cause behaviour the v0.61.283 fix was supposed to close.
>
> **What v0.61.283 actually fixed**: `scripts/durian-variance.js` (the CLI tool, run from a developer workstation).
> **What v0.61.283 missed**: `durian-variance-runner.js` (a *separate* module the bot's `/ver` flow calls — added in v0.61.260 as an in-bot equivalent of the CLI script). It has its own `_placeToVenue` mapper and `kept[]` row builder — both of which dropped `placeId` exactly like the CLI used to.
>
> Net: every `/ver`-triggered variance run from v0.61.260 to v0.61.287 produced placeId-less reports, even on the v0.61.283+ deploys. The Gemini-verify step downstream (`ownerDurianGeminiRun` / `_flattenKeptVenues`) read `v?.placeId` as `''` and skipped every venue at the Redis-cache write step. **`dgv:labels:<mode>` has been empty in production through 28 patches**, not just v0.61.275–282 as v0.61.283's journal claimed.
>
> v0.61.288 mirrors the v0.61.283 fix in `durian-variance-runner.js`:
> - `_placeToVenue` now returns `placeId: p?.id || ''`.
> - The per-query `kept[]` row now includes `placeId: v.placeId || ''`.
> - The hardcoded `scriptVersion: '0.61.260'` bumped to `'0.61.288'` so future JSON downloads carry the right provenance string.
>
> Once Railway redeploys v0.61.288, the *next* `/ver → 🥥 Durian variance → ...` followed by `/ver → 🤖 Gemini verify → ...` will write actual placeId → label entries into Redis. The fresh JSON download can also seed via the v0.61.282 script.
>
> Commit 502 / journal 489 / build 291, on branch
> `claude/v0.61.288-runner-placeid-fix` off `main` at v0.61.287.

## New

### [HDR] #489 | 21:25 SGT 30-05-30 | v0.61.288 — runner placeId carry (v0.61.283 follow-up) | 3 files | 1 PR | 1 build |

- **[INTENT]** Close the SAME bug class v0.61.283 was supposed to close — but in the file v0.61.283 missed. The bot's in-process `/ver` runner has been silently dropping `placeId` ever since the v0.61.260 split that introduced it.

- **[DELTA]**

  **1. `durian-variance-runner.js` — `_placeToVenue` (~line 80).**
  Add `placeId: p?.id || ''` as the first field, with an inline comment explaining the v0.61.283-missed-this-file context.

  **2. `durian-variance-runner.js` — per-query `kept[]` row (~line 189).**
  `const row = { …, placeId: v.placeId || '' }`. Comment cross-references the v0.61.283 fix in `scripts/durian-variance.js` so a future reader sees both files in one Search-In-Project hit.

  **3. `durian-variance-runner.js` — `scriptVersion` literal (~line 240).**
  `'0.61.260'` → `'0.61.288'`. The JSON output's `scriptVersion` field now matches the patch that fixed the placeId carry.

  **4. `package.json`** — `0.61.287 → 0.61.288`.

  **5. `doc/.serial-state.yml`** — commit 501→502, build 290→291, journal 488→489; re-anchored 30-05 '26 21:25 SGT.

- **[VERIFICATION (sandbox)]**

  - `node --check durian-variance-runner.js` — clean.
  - Operator's two JSON files (16:57 + 21:10 SGT runs) confirmed placeId-less via `node -e "JSON.parse(...).regions[0].queries[0].kept[0].placeId"` → `''` for both.
  - Vitest still BLOCKED locally by the recurring iCloud `ERR_INVALID_PACKAGE_CONFIG` (Register O-22 + O-37). CI will run authoritatively. No test contract changed.

- **[STATUS]** PR-OPEN on push. Railway redeploys the bot on merge.

- **[TEST]** Operator (post-merge):
  1. **Re-run `/ver` on the v0.61.288 deploy.** `/ver → 🥥 Durian variance → durian-pastry`, wait for completion, then `/ver → 🤖 Gemini verify → durian-pastry`. The post-verify chat summary should now read `dgv:labels:durian-pastry populated N placeId → label entries (30d TTL)` with **N > 0** (every run from v0.61.260 to v0.61.287 logged `populated 0 entries` silently).
  2. **Confirm Redis hash.** `redis-cli HLEN dgv:labels:durian-pastry` returns the count from #1.
  3. **JSON download has placeIds.** Download the variance + Gemini-verify JSON attachments from the chat. Open them — every `kept[]` row in the variance JSON and every `venues[]` row in the verified JSON should have a non-empty `placeId` (the previously empty string).
  4. **Cuisine-search D703e takes effect.** A durian-pastry search at any covered region now drops cached-known `unrelated` venues via the D703e Redis-cache check — Railway log shows `D703e Gemini-label post-filter (durian-pastry): N → N-K (dropped=K unrelated, unlabeled=M passed through)` with K > 0.
  5. **v0.61.282 inline D703f still works for OTHER.** Durian-pastry at Surabaya — D703f fires for any remaining unlabeled venues and writes them to the cache (unchanged from v0.61.282).

- **[KNOWN GAPS]**
  - **The four pre-v0.61.288 JSONs in `~/Downloads/`** are still unrecoverable (placeId-less). Re-running `/ver` on the v0.61.288 deploy is the only path to a placeId-bearing report.
  - **Why two variance modules?** `scripts/durian-variance.js` is the CLI tool (developer workstation); `durian-variance-runner.js` is the bot module (in-process, called by `/ver`). The two should probably share a single source-of-truth — but the runner module also threads Telegram-side progress callbacks, so a true unification would need a callback-injectable interface. Deferred.
  - **Register O-37** (iCloud-stale-checkout, shipped in v0.61.287 a few minutes ago) is one half of the explanation for why this bug class keeps re-appearing. The OTHER half is silent drift between the CLI and the in-bot copies of the same logic. Worth a follow-up Register entry on "duplicated logic in two files" as a category risk.

- **Confirmation Gates**

  | Gate | Resolution | Date |
  |---|---|---|
  | G1 — version bump | PATCH `0.61.287 → 0.61.288`. Single bug fix; mirrors v0.61.283's contract to the second file. No API change. | 30-05 '26 |
  | G2 — destructive action | _n/a_. Two additive field assignments + one literal bump. | 30-05 '26 |
  | G3 — new decision rule | _n/a_. Restores the v0.61.275 / v0.61.283 intended contract. | 30-05 '26 |
  | G4 — paid external API call | _n/a_. | 30-05 '26 |

## Amendments

- **`durian-variance-runner.js`** — `_placeToVenue` returns `placeId` from `p.id`; per-query `kept[]` row includes `placeId`; `scriptVersion` bumped to `'0.61.288'`.

## Deleted

_None._
