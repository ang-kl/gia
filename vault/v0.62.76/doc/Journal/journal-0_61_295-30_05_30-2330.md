(№ 496 - 30-05 '26 23:30 SGT) v0.61.295

# Journal — soleat v0.61.295 (Phase 2A fun-facts: 12 MY/regional facts)

> Operator (30-05 '26 23:18 SGT, post-PR-#800 merge): *"5"* — Fun-facts Phase 2 → chat-surface + MY/regional facts.
>
> **Scope decision**: ship Phase 2A (data extension — 12 MY/regional facts) as v0.61.295, defer the chat-surface integration to Phase 2B. Rationale:
> - The TMA modal already exists (v0.61.285); adding facts is a pure data extension with no new code paths to test or design.
> - Chat-surface integration is a bot-side change touching `formatVenueBlock` callsites across multiple commands (`/c`, `/s`, `/hidden`). Needs operator approval on the message format (separate "💡 Did you know?" reply vs. appended section).
> - Operator's Phase 1 journal said *"Chat surface is harder (Telegram message-edit constraints) — suggest deferring to Phase 2"*; this PR honours that incremental ship.
>
> v0.61.295 adds 12 facts:
>
> | # | ID | Region tag |
> |---|---|---|
> | 41 | penang-char-kway-teow | penang, MY |
> | 42 | kl-char-kway-teow | kl, MY |
> | 43 | klang-bak-kut-teh-origin | klang, MY |
> | 44 | asam-laksa-penang | penang, MY |
> | 45 | nasi-kandar-tamil-muslim | penang, MY |
> | 46 | my-durian-seasons | MY, **JB** |
> | 47 | jb-mamak-24-7 | **JB**, MY |
> | 48 | kl-hokkien-mee | kl, MY |
> | 49 | murtabak-yemeni-arab | MY, SG |
> | 50 | pasar-malam-tradition | MY, SG |
> | 51 | mee-goreng-mamak | MY |
> | 52 | kueh-kapit-love-letters | MY, SG |
>
> Source: **Wikipedia** (verifiable, widely-cited regional culinary articles), NOT NLB — NLB's coverage skews Singapore. The Phase 1 40 facts retain their NLB sourcing; the new 12 are clearly labelled `"source": "Wikipedia"` with the corresponding `en.wikipedia.org` URL.
>
> Total facts: 40 → **52**. The selector's existing tier-1-cuisine + tier-2-region matching handles them automatically: JB-region searches preferentially see JB-tagged facts; MY countryPref + OTHER region surfaces MY facts; cuisine searches like "char kway teow" hit the new penang/kl variants.
>
> Commit 509 / journal 496 / build 298, on branch
> `claude/v0.61.295-phase2a-my-regional-facts` off `main` at v0.61.294.

## New

### [HDR] #496 | 23:30 SGT 30-05-30 | v0.61.295 — Phase 2A 12 MY/regional fun-facts | 4 files | 1 PR | 1 build |

- **[INTENT]** Close the MY-coverage gap in the v0.61.285 fun-facts dataset (40 NLB-sourced facts, all SG-skewed). Phase 2A adds 12 Wikipedia-sourced MY/regional facts so JB / MY-countryPref / "char kway teow" / "asam laksa" / etc. searches now have culturally-appropriate facts to surface.

- **[DELTA]**

  **1. `web/cuisine/src/v2/data/fun-facts.js`** — 12 new entries appended after fact #40 (`labyrinth-michelin-chicken-rice`). Each fact:
  - `id`: stable kebab-case identifier (e.g. `penang-char-kway-teow`)
  - `tags`: includes city tag (`penang` / `kl` / `klang`) AND broader region (`MY`, optionally `JB` / `SG` for cross-border dishes) AND cuisine slug (`char-kway-teow`) AND community where relevant (`indian`, `chinese`, `hokkien`, `peranakan`)
  - `en` + `fr`: ~25–40 words each, parallel structure
  - `source`: `"Wikipedia"`
  - `sourceUrl`: `https://en.wikipedia.org/wiki/<Article>`

  **2. `__tests__/fun-facts.test.js`** — three contract assertions updated:
  - `expect(facts.length).toBe(40)` → `toBe(52)`. Test name updated to `"exports 52 facts (40 SG-NLB + 12 MY-regional)"`.
  - `expect(f.sourceUrl.includes('nlb.gov.sg')).toBe(true)` → broadened to `nlb.gov.sg || wikipedia.org` so the new Wikipedia URLs pass.
  - **NEW assertion**: `"Phase 2A: at least 10 MY-tagged facts (v0.61.295 extension)"` — guards against accidental removal of MY content in future PRs.

  **3. `package.json`** — `0.61.294 → 0.61.295`.

  **4. `doc/.serial-state.yml`** — commit 508→509, build 297→298, journal 495→496; re-anchored 30-05 '26 23:30 SGT.

- **[VERIFICATION (sandbox)]**

  - `node --check fun-facts.js` — clean.
  - Smoke test (dynamic import):
    ```
    total: 52
    MY-tagged: 14   (12 new + 2 pre-existing — nasi-lemak, teh-tarik)
    JB-tagged: 2    (both new — my-durian-seasons, jb-mamak-24-7)
    penang-tagged: 3 (penang-char-kway-teow, asam-laksa-penang, nasi-kandar-tamil-muslim)
    unique IDs: 52 / 52
    wikipedia sourced: 12
    ```
  - Selector smoke-test across 4 ctx scenarios:
    - `ctx={cuisines:['char-kway-teow'], region:'SG'}` → penang-char-kway-teow ✓ (Tier 1 cuisine match)
    - `ctx={region:'JB'}` → my-durian-seasons ✓ (Tier 2 region match — first JB-tagged fact)
    - `ctx={region:'OTHER', countryPref:'MY'}` → MY-tagged fact ✓ (Tier 2 region match)
    - `ctx={cuisines:['laksa'], region:'SG'}` → laksa-etymology ✓ (existing Phase 1 fact, no regression)
  - Vitest still BLOCKED locally by recurring iCloud `ERR_INVALID_PACKAGE_CONFIG` (Register O-22 + O-37). CI authoritative.

- **[STATUS]** PR-OPEN on push. Railway redeploys the Cuisine TMA on merge. Vite bundles the larger `fun-facts.js` (~22 KB up from ~17 KB).

- **[TEST]** Operator (post-merge):
  1. **Phase 2A facts surface for cuisine searches.** Pick "char kway teow" in the Cuisine TMA. Force a rotating search >1.5 s. FunFactModal should show one of the two new char-kway-teow facts (Penang or KL variant), NOT a generic SG fact.
  2. **Phase 2A facts surface for JB region.** Tap JB region pill, force a rotating search. Modal preferentially shows `my-durian-seasons` or `jb-mamak-24-7` (only two JB-tagged facts; rotates with anti-repeat).
  3. **Phase 2A facts surface for OTHER+MY context.** Set OTHER region + countryPref=MY (via the OTHER picker → Malaysia country). Force a rotating search. Modal picks from the 14 MY-tagged facts.
  4. **Phase 1 SG facts still surface for SG region.** Pick SG region (or default), force a rotating search. Modal preferentially picks one of the ~25 SG-only facts (not the new MY ones).
  5. **FR locale.** Switch to FR. All 12 new facts have FR translations.
  6. **Source link goes to Wikipedia.** The modal's footer "Source: Wikipedia" link opens the corresponding en.wikipedia.org page.

- **[KNOWN GAPS]**
  - **Chat surface (Phase 2B) deferred.** The bot DM cuisine result (`/c <cuisine>`) doesn't get a fun-fact appended. Per v0.61.285 journal: *"Telegram message-edit constraints; suggest deferring to Phase 2"*. A clean Phase 2B design needs operator input on:
    - separate "💡 Did you know?" reply vs. appended section to the venue list
    - which commands get facts (`/c` only, or also `/s`, `/hidden`, `/cuisine`)
    - cost: Telegram message-edit is free, but extra `sendMessage` calls eat into the 30-msg/sec rate limit
  - **No Penang TMA region pill.** The 3 penang-tagged facts only surface via cuisine context (char-kway-teow / laksa / nasi-kandar tier-1 match) or via MY countryPref. There's no way for a Penang resident to set their region as Penang specifically. Acceptable for Phase 2A; could be a future region-pill addition.
  - **Some MY facts cross-tag SG.** `pasar-malam-tradition`, `murtabak-yemeni-arab`, `kueh-kapit-love-letters` are tagged both `MY` and `SG` because they exist in both. The selector treats `SG`-tagged facts as Tier-2 region match in SG contexts, so these will appear for SG users too. Intentional — they're shared heritage.
  - **The chat surface's bot-side branding question** — should the bot say "💡 Did you know?" or use a Singlish flair like "Eh, did you know"? Phase 2B scope question for the operator.

- **Confirmation Gates**

  | Gate | Resolution | Date |
  |---|---|---|
  | G1 — version bump | PATCH `0.61.294 → 0.61.295`. Data extension, no code change beyond the data file + test assertions. | 30-05 '26 |
  | G2 — destructive action | _n/a_. Pure addition; Phase 1 40 facts unchanged byte-for-byte. | 30-05 '26 |
  | G3 — new decision rule | _n/a_. Continuation of the v0.61.285 fun-fact contract. | 30-05 '26 |
  | G4 — paid external API call | _n/a_. Static data; no API. | 30-05 '26 |

## Amendments

- **`web/cuisine/src/v2/data/fun-facts.js`** — 12 entries appended (40 → 52). Phase 1 facts unchanged.
- **`__tests__/fun-facts.test.js`** — fact-count assertion 40 → 52; sourceUrl assertion broadened to allow `wikipedia.org`; new MY-coverage assertion added.

## Deleted

_None._
