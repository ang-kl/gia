(№ 608 - 09-06 '26 08:12 SGT) v0.61.408

# Journal — soleat v0.61.408 (Default location: last set location drawer, not hardcoded Raffles Place)

> Operator: *"now in the codebase that suggest 'Raffles Place' as default, change to last set location drawer as default."*
>
> Commit 621 / journal 608 / **build 391 (holds — server-only)**. index.js only. Revert-ready (one helper + 4 call sites + 1 button relabel).

## New

### [HDR] #608 | 08:12 SGT 09-06-26 | v0.61.408 — Raffles Place default → last set location (recents drawer top) | index.js | 1 PR | 0 build |

- **[INTENT]** The bot hardcoded **Raffles Place** (`1.2839, 103.8517`) as the universal per-user location fallback. The operator wants the **last set location** — the **top of the recents drawer** — used as the default instead, keeping Raffles only as a true first-timer floor.

- **[DELTA]** (`index.js` only — server-side)
  - **New helper `resolveDefaultCenter(chatId)`** (≈ index.js:1196) returns `{ lat, lng, label, source }`, resolving the default in priority order:
    1. **live set-location cache** — `getUserLocation` (~24 h TTL, the active pin);
    2. **recents drawer top** — `listRecentLocations()[0]` — *the LAST SET LOCATION*, a **180-day LRU** that **outlives** the cache;
    3. **Raffles Place** floor only when there is *no cache **and** no recents* (genuine first-timer).
  - **`startSanctuaryFlow`** now defaults to the last set location *before* asking. Only a true first-timer (no cache **and** no recents) still sees the share-pin prompt. Previously: no fresh cache → always asked.
  - **Reply-keyboard button relabelled** `⛔ Use Raffles Place default` → **`🕘 Use my last location`** — the text, the `KEYBOARD_TEXTS` set, and the `bot.onText(/^…$/)` regex all updated in lockstep. The handler now calls `resolveDefaultCenter` (was hardcoded `1.2839, 103.8517` across the cuisine / NL / sanctuary branches).
  - **`runWeatherCommand`** no-area default: `cached?.lat ?? 1.2839` → `resolveDefaultCenter(chatId)`.
  - The v0.56.1 keyboard-dismiss comment updated to the new label.
  - **`package.json`** `0.61.407 → 0.61.408`; **`doc/.serial-state.yml`** commit 620 → 621, journal 607 → 608, **build 391 holds**; re-anchored 08:12 SGT 09-06-26.

- **[VERIFICATION (sandbox)]** `node --check index.js` clean. Full suite **3107/3107 green** (114 files). No `web/` change → no build.

- **[STATUS]** PR-**OPEN**, **auto-merge armed (squash)**. Railway redeploys on merge. Server-only; no Mini-App rebuild.

- **[TEST]** Operator: trigger a flow with no fresh shared pin (e.g. after the 24 h cache expires) → the bot now searches at your **last set location** instead of jumping to Raffles Place. The reply keyboard's second button now reads **`🕘 Use my last location`**.

- **[KNOWN GAPS — deliberate scope boundaries]**
  - **Not touched** (no per-user context to resolve recents against): `vibe.js` + `public/app.js` (anonymous surfaces), `ver.js` (API health-probe coords), `index.js:11545` (admin `test-pipeline` query default).
  - **`web/cuisine/src/App.jsx` `CBD_DEFAULT`** left as-is — that is the **v1** Cuisine app and is **UNMOUNTED dead code** (`web/cuisine/src/main.jsx` mounts `v2/App.jsx`).
  - The v2 Cuisine TMA does not hardcode Raffles (it uses `resolveSearchCenter` / `locationAnchor`), so no TMA change was needed.

- **Confirmation Gates**

  | Gate | Resolution | Date |
  |---|---|---|
  | G1 — version bump | PATCH `0.61.407 → 0.61.408`. | 09-06 '26 |
  | G2 — destructive action | Adds a resolver + reroutes 4 fallback sites; Raffles retained as the final floor. Reversible. | 09-06 '26 |
  | G3 — new decision rule | Per-user default location = last set location (set-cache → recents[0] → Raffles floor). | 09-06 '26 |
  | G4 — paid external API call | None. | 09-06 '26 |

## Amendments

- **`index.js`** — new `resolveDefaultCenter`; `startSanctuaryFlow`, the last-location button (label + `KEYBOARD_TEXTS` + `onText`), and `runWeatherCommand` rerouted through it.
- **`package.json`** — `0.61.407 → 0.61.408`.
- **`doc/.serial-state.yml`** — counters (commit 621 / journal 608 / build 391 holds) + anchor 08:12 SGT 09-06-26 + `last_anchor_source` prepend.

## Deleted

- The hardcoded `1.2839, 103.8517` per-user fallback at `startSanctuaryFlow`, the fallback-button handler, and `runWeatherCommand` (superseded by `resolveDefaultCenter`). The `⛔ Use Raffles Place default` button label (→ `🕘 Use my last location`).
