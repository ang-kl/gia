(№ 513 - 02-06 '26 09:00 SGT) v0.61.312

# Journal — soleat v0.61.312 (owner-gate `/hidden`)

> Operator (02-06 '26): *"`/hidden` be allowed for me only."*
>
> Closes the public access to the Hidden Gems flow (Gemini-grounded `googleSearch` with vault context — high per-call spend + curated owner-only feature). Silent no-op for non-owners, same gating pattern as `/ver` / `/cost` / `/ftlog`.
>
> Commit 526 / journal 513 / build 315 / feature 52, on branch `claude/v0.61.312-hidden-owner-gate` off `main` at v0.61.311.

## New

### [HDR] #513 | 09:00 SGT 02-06-26 | v0.61.312 — owner-gate /hidden | 4 files | 1 PR | 0 build |

- **[INTENT]** Restrict every reachable path of the Hidden Gems flow to the owner chat. Mirrors the v0.60.131 `/ftlog` + v0.61.307 `/cost` gating pattern.

- **[DELTA]**

  **1. `index.js`** — gate at both surprise-command entry functions so all four reachable paths inherit the gate without per-callsite duplication:
  - **`runSurpriseCommand(chatId, lang)`** (line ~6280) — gains `if (!isOwnerChat(chatId)) { console.log(`[/hidden] denied ...`); return; }` at the top.
  - **`runSurpriseCommandWithFreeText(chatId, lang, freeText)`** (line ~6087) — gains the same gate with label `[/hidden-freetext]`.

  Reachable paths (all already in tree):
  - `bot.onText(/^\/hidden ...)` slash command at `index.js:1473`
  - `pending === '/hidden'` location-resume branch at `index.js:3751`
  - `routeMenuCommand` `case 'hidden':` at `index.js:5072` (Menu TMA dispatch)
  - NL-resolver `resolved.kind === 'hidden'` branch at `index.js:9836`

  All four funnel through `runSurpriseCommand` (or the free-text variant), so a single gate per function covers the lot.

  **2. `doc/Feature/feature-0_61_83-21_05_26-1645.md`** — Removed Features table gains **RF-18**: the public-access surface for `/hidden`, replaced with the owner-only gate.

  **3. `package.json`** — `0.61.311 → 0.61.312`.

  **4. `doc/.serial-state.yml`** — commit 525→526, build 314→315, journal 512→513, feature 51→52; re-anchored 02-06 '26 09:00 SGT.

- **[VERIFICATION (sandbox)]**

  - `node --check index.js` — clean.
  - `npm test -- --run` — no test changes; existing 2879 tests stay green.
  - No build (server-side only).

- **[STATUS]** PR-OPEN on push. Railway redeploys on merge. First `/hidden` invocation from a non-owner chat post-deploy logs the deny line + returns silently. Operator's own chat unchanged.

- **[TEST]** Operator (post-merge):
  1. From the owner chat (TELEGRAM_OWNER_CHAT_ID): `/hidden` and `/hidden <free text>` run normally.
  2. From a non-owner chat (e.g. a test account, or any group/channel): `/hidden` produces no visible response. Railway log line: `[/hidden] denied chat=<N> (not TELEGRAM_OWNER_CHAT_ID)`.
  3. Menu TMA "hidden" tile dispatch from a non-owner chat: same silent deny (path funnels through `routeMenuCommand` → `runSurpriseCommand`).
  4. Pending-location resume: if a non-owner pinned `pending = '/hidden'` then shares location, the resume hits the gate at `runSurpriseCommand` entry and silently exits.
  5. `/ver` from the owner chat shows nothing different (Hidden Gems isn't part of the runHealthCheck probes).

- **[KNOWN GAPS]**
  - **No user-visible "denied" message** by design — silent no-op matches the existing `/ver` / `/cost` / `/ftlog` pattern. Bots that telegraph "you don't have access" leak the command's existence.
  - **`TELEGRAM_OWNER_CHAT_ID` unset → gate is open** per `isOwnerChat`'s existing semantics. If the operator wants strict-deny-when-env-unset, that's a separate change.
  - **Menu TMA's tile labelled "Hidden" still renders for non-owners**. The button click silently no-ops post-deploy. Cosmetic follow-up: hide the tile entirely for non-owners (`isOwner` flag in the Menu TMA's `/api/menu/live` response).
  - **i18n strings for `/hidden` busy / no-results / etc. are not removed** — preserved for the owner-side use. AU-1 (additive) preserves the prior surface; the gate is the change.

- **Confirmation Gates**

  | Gate | Resolution | Date |
  |---|---|---|
  | G1 — version bump | PATCH `0.61.311 → 0.61.312`. Access control change; no API contract break. | 02-06 '26 |
  | G2 — destructive action | _n/a_. Additive: two top-of-function guards. | 02-06 '26 |
  | G3 — new decision rule | Operator-confirmed: `/hidden` is owner-only. Stands as a standing rule for future Hidden-Gems-flavoured surfaces. | 02-06 '26 |
  | G4 — paid external API call | _n/a_. The gate prevents *more* paid calls (Gemini-grounded `googleSearch` is the most expensive surface in the bot). | 02-06 '26 |

## Amendments

- **`index.js`** — `runSurpriseCommand` + `runSurpriseCommandWithFreeText` gain `isOwnerChat` gates at function entry.
- **`doc/Feature/feature-0_61_83-21_05_26-1645.md`** — Removed Features table gains RF-18.
- **`package.json`** — version `0.61.311 → 0.61.312`.
- **`doc/.serial-state.yml`** — counters + last_anchor_time re-anchored 02-06 '26 09:00 SGT.

## Deleted

_None._ The Hidden Gems flow itself remains intact for the owner chat.
