(№ 115 - 21-05 '26 09:44 SGT)

# Register — soleat / Gia4lunch v0.61.76 (fresh master)

> **Document type:** Register (per `doc/CLAUDE.md` §3 — *open items, known issues, deferred, decisions, completed*; §4.1 — items move between sections, never deleted).
> **Status:** Fresh, non-incremental master, produced under the established vault-pass precedent (the v0.61.28 pass, journal #231 / decision D-1028). Supersedes `register-0_61_28-18_05_26-2330.md` as the current-state ledger; every prior versioned Register file remains on disk. No code change, no version bump in this pass.
> **Reflects:** project state at **v0.61.76, 21 May 2026 09:44 SGT**, captured into `vault/v0.61.76/`.

---

## Open Items

| # | Item | Owner | Priority | Notes |
| :--- | :--- | :--- | :--- | :--- |
| O-1 | Exit feature **Part B — ♿ lift availability per exit** | operator + agent | medium | No LTA dataset carries lift-per-exit accessibility; the SMRT site that does (`journey.smrt.com.sg/.../exits/`) 403s non-browser clients and is unreachable from the build sandbox. Needs a scraper run in an open-network environment (browser headers, possibly headless if the page is a JS SPA) committing a static data file — or operator-supplied data. The buildings/attractions half shipped in v0.61.28. |
| O-2 | **Building-anchored cuisine search** | agent | medium | Operator-directed (18-05 '26): a building name typed in the Cuisine TMA Search-Criteria free-text field or the free-text chat should return that building's eateries first (criteria still applied), then vicinity if none. Scoped (in-building-only-then-vicinity; filters kept); not yet built. Spans `cuisine-search.js` / `pipeline.js`, the Cuisine TMA free-text field, and `runFreeTextSearch`. |
| O-3 | `doc/VibeCodingRecord/` PR-ledger refresh | operator + agent | low | `data/prs.ndjson` + `data/pr-files.tsv` lag behind `main`; PRs #514–#574 need appending and the generator re-running. Cosmetic; lags the Journal. |
| O-4 | Observability expansion | operator | low | `doc/observability-expansion-plan.md` (a plan for latency / request-volume / venue-popularity / UX-funnel metrics) is written but unbuilt. |
| O-5 | Security hardening — remaining items | operator | medium | Auth chokepoint on `/api/{menu,hawker,transport,oversight}/*` (DF-53); per-chatId sliding-window limits on all hot endpoints (DF-54); API-key budget caps in the Google/Gemini consoles (DF-55, operator-side). |
| O-6 | Michelin top-12-fresh Places-cost watch (DF-94) | operator | low | Monitor Places billing now the Michelin 24 h result-cache is dropped. |
| O-7 | TMA component-test infrastructure | operator | low | No React component-level test harness yet; several integration tests (DF-49, DF-51, DF-59) await it. |
| O-8 | Train-station tap has no info popup | operator | low | Pre-v0.61.57 behaviour, now resolved by the CR6 station info card (a station tap opens the card — see C-AG). Kept on the ledger as the resolved precedent for the deliberate "no InfoWindow" decision; revisit only if a different station-tap interaction is requested. |
| O-9 | **Base-map station labels cannot be hidden in code** | operator | low | The 3 TMA maps are initialised with a `mapId` (required for `AdvancedMarkerElement` pins); Google Maps JS ignores the `styles` option whenever a `mapId` is set. To hide `transit.station` labels the operator must edit the styled Map ID in the Google Cloud Console (Maps Platform → Map Styles → POI → Transit → Station → labels off) and ensure that Map ID is the `MAP_ID` env var on Railway; the `DEMO_MAP_ID` fallback cannot be restyled. No code change is possible (journal #270). |
| O-10 | **`data/sg_24hr_places.json` is an empty scaffold** | operator | medium | The v0.61.69 24-hour-places fetch pipeline (`scripts/fetch-24hr-places.js`) ships, but the data file is a committed scaffold — the build sandbox has no web egress and the non-invention rule forbids fabricating place data. Operator step: `GOOGLE_PLACES_API_KEY=… npm run fetch:24hr-places`, then commit the refreshed file. Wiring the data into the map's `layer.open24` overlay toggle (currently a disabled stub) is a further follow-up. |
| O-11 | **`bus_stop_name` unresolved in the nearest-bus-stop dataset** | operator | medium | `data/stations_with_nearest_bus_stops.json` (v0.61.63) ships with all 880 `bus_stop_name` fields `null` — Stage B (the OneMap `getNearestBusStops` name pass) cannot run from the sandbox (egress allowlist). Operator step: `ONEMAP_API_KEY=… node scripts/enrich-nearest-bus-stops.js`, then commit the three refreshed data files. Wiring `nearest_bus_stops` into the CR6 station card render is a separate follow-up. |
| O-12 | **`exit.street` not sourced** | operator + agent | low | The CR6 station info card renders an optional `exit.street` slot, but `geoloc/LTAMRTStationExitGEOJSON.geojson` carries no street name (only `STATION_NA` / `EXIT_CODE`). Sourcing it needs a OneMap reverse-geocode of each exit's coords or an operator-supplied dataset; the renderer is already ready for the field once `stations.json` carries it (journal #260). |
| O-13 | **`fetch-bus-services-by-stop.js` not run** | operator | low | CR6 Phase 2a (`exits[].nearest_bus_stop`) ships the fetcher; the operator must run it locally with `LTA_ACCOUNT_KEY` to populate `data/bus-services-by-stop.json`. `build-station-info.js` joins it gracefully when present and falls back when absent (journal #260). |
| O-14 | LRT-loop & TEL-branch first/last-train gaps | operator | low | v0.61.67's first/last-train join leaves 28 of 180 stations without timings: Sengkang/Punggol LRT (the source publishes only town-centre loop departures), the EWL airport branch (Expo, Changi Airport — `data_available:false`), and TE23 Tanjong Rhu → TE29 Bayshore (open but absent from the aggregator source). A source ceiling, not a code bug; recorded in `stations.json` `data_quality_notes`. |
| O-15 | **mrt-coords.json station coordinates are coarse** | agent | low | Investigation in v0.61.65 found `mrt-coords.json` station lat/lng is imprecise (median 37 m, worst case ~1.5 km off the real station); the LTA exit GeoJSON is accurate. `build-station-info` now adds an `exit_centroid` (mean of exit coords) to 175 of 180 records and the map pin + derived polylines use the centroid; the original `lat`/`lng` are kept. Replacing the coarse coords outright is deferred. |

---

## Known Issues

| # | Issue | Severity | Notes |
| :--- | :--- | :--- | :--- |
| KI-1 | v0.61.71 / v0.61.72 never shipped as standalone versions | low | Both were folded into the v0.61.73 squash commit; `package.json` jumps `0.61.70` → `0.61.73`. The GitHub PR #570 title still reads `v0.61.71`; the squash-commit subject was set to `v0.61.73` to match `package.json`. Cosmetic version-history irregularity, recorded for traceability. |
| KI-2 | Verification is CI-only on the recent arc | low | Several late-arc PRs (v0.61.73 onward) were verified by the GitHub Actions run only, with no local sandbox run. The Actions run (syntax check + Vitest + TMA build) is the gate; visual UI changes remain sandbox-blind. |
| KI-3 | The `/healthz` `pkg`→`pkgJson` crash (v0.61.62) | resolved | A `ReferenceError` in `/healthz` returned 500 → the webhook-domain probe classified `soleat.net` unreachable → TMA URLs switched to the fallback host → all 3 Mini Apps blanked. Fixed by a one-token change on `index.js:8495` (v0.61.62 HOTFIX). Kept here as the precedent: a health-endpoint crash can cascade into a full Mini-App outage. |

---

## Deferred

| # | Item | Reason | Reactivate when |
| :--- | :--- | :--- | :--- |
| DF-47 | Unit tests for `insideSG` + the `/\bjohor\b/i` post-filter | Integration coverage sufficient | a JB geo-fence regression appears |
| DF-49 / DF-51 / DF-59 | Component / integration tests for the petFriendly filter + LocationField contract | No React component-test harness | O-7 (test infra) lands |
| DF-53 / DF-54 / DF-55 / DF-56 | Auth chokepoint for the other `/api/*` namespaces; sliding-window rate limits; console budget caps; inline-`verifyInitData` cleanup | Security priorities #2–#4 | the next security-hardening pass |
| DF-57 | Broad FR audit across `i18n` + inline `lang === 'fr'` branches | Operator declined the full audit | operator requests a localisation sweep |
| DF-74 / DF-75 / DF-88 | Places `priceRange` / `addressComponents` cost monitor; Frankfurter FX robustness; sparse `priceRange` coverage | External-data sparsity + marginal cost | coverage or cost becomes material |
| DF-94 | Michelin top-12-fresh Places-cost watch | See O-6 | billing review |
| DF-97 / DF-98 / DF-99 | `michelin:enrich` stale-cache rotation; LLM dish-name allow-list; cold-cache first-search latency | Acceptable at current scale | a user-visible symptom appears |
| DF-100 | Selective prev/current/next train-polyline emphasis (the CR5 v2 `trackBetween()` model) | Shipped in v0.61.58 — see C-AK; entry retained for the deferral trace from v0.61.53 | — (closed) |
| DF-101 | `layer.open24` map overlay toggle wiring | The 24-hour-places data file is an empty scaffold (O-10) | `data/sg_24hr_places.json` is populated by the operator's local fetch run |
| DF-102 | CR6 station-card render of `nearest_bus_stops` | The dataset (`stations_with_nearest_bus_stops.json`) is built but `bus_stop_name` is null (O-11) | the OneMap name pass is run and the data file is wired into the card |

---

## Decisions

> Append-only. Earlier `D-NNN` decisions live in the prior versioned Register files.

| # | Decision | Rationale | Date |
|---|---|---|---|
| D-213 | Every TMA footer carries an "Experimental · Singapore · v…" tag; the Cuisine TMA leads with a how-to line. | Honest, consistent framing. | 16-05 '26 |
| D-1001 | Documentation consolidated into standalone master versions per domain (v0.60.213). | Operator instruction № 1,001. | 16-05 '26 |
| D-61.0 | Map overlay layers are built from committed `geoloc/*.geojson` open data via `scripts/build-geo-overlays.js` into static `data/geo-*.json` — a build-time, zero-runtime-cost step. | Open data, no per-request API cost. | 17-05 '26 |
| D-61.17 | The map-overlay controller (`mapOverlays.js`) is one framework-agnostic module, duplicated byte-identically across the Cuisine / Hawker / Transport TMAs. | No shared package across separate Vite apps; every copy carries a `KEEP IN SYNC` header and is edited + `diff`/`md5sum`-verified together. | 17-05 '26 |
| D-61.18 | A train-station marker tap enters the station-detail focus view, with no InfoWindow popup (unlike bus/taxi amenity pins). | The detail view self-identifies; an extra popup tap is redundant. | 17-05 '26 |
| D-61.24 | MRT/LRT exit overlay pins show the exit letter/number as a line-coloured pill; a tap opens the shared Exit Template popup. | Replaces an undifferentiated emoji dot; the colour signals the line. | 18-05 '26 |
| D-61.26 | The Nearby↔Details overlay-radius slider is removed (it "doesn't work"); chip layers use one fixed radius; in a station-detail view the Exits/Taxis chips scope to the 3 visible stations; bus + carpark are dropped from the station-detail amenities. | Operator review; the slider added no value, the station-scoped chips are the wanted interaction. | 18-05 '26 |
| D-61.27 | The `/transport` nearest-stations rows show the real station code (NS21, DT11), coloured by line, via `mrt-lines.codedLinesForStation`. | "The station code must be included, not just the line." | 18-05 '26 |
| D-61.28 | The exit feature is split: Part A (line-coloured pins + Exit Template) and Part B (♿ lift + surrounding buildings/attractions). Build only what owned/reachable data supports — the nearby-attractions half — and shelve the ♿ lift half with the blocker logged (O-1). | Spike-then-scope: the SMRT lift data is unreachable from the build environment. | 18-05 '26 |
| D-1028 | A fresh, non-incremental master set of the eight-folder documents is produced at v0.61.28 and frozen into `vault/v0.61.28/`. Prior versioned files remain on disk so the authenticity trace is preserved; the AU append-only Recipe is overridden for this pass only, by explicit operator instruction. | Operator instruction (18-05 '26) — *"Create a fresh set of documents (not incremental) and vault it."* | 18-05 '26 |
| D-61.31 | Every map pin info popup ends with a `Google Map ↗` text hyperlink; the Cuisine venue popup's blue `#1a73e8` button is removed. | One consistent, low-chrome way out to Google Maps across all 3 TMAs (map-controls redesign Phase F). | 19-05 '26 |
| D-61.36 | The in-map controls are unified across the 3 TMAs as one presentational `MapControls` component (`⋯/⋮` menu + row pills + checkbox dropdown); `MapLayerChips` is retired. | One control surface, edited once and copied byte-identically. | 19-05 '26 |
| D-61.41 | The map colour toggle is default-ON with the semantics ON = colour / OFF = greyscale. | Operator-corrected semantics; colour is the expected default. | 19-05 '26 |
| D-61.55 | The CR6 station info layer is built offline: `scripts/build-station-info.js` joins `mrt-coords.json` + `station-exits.json` + rule-based URL templates into `data/stations.json`; per-exit bus services, first/last train and nearest bus stops are optional joins populated by separate operator-run fetchers. | A reproducible, mostly-offline build; paid/keyed API calls are isolated to operator-run enrichment scripts. | 20-05 '26 |
| D-61.57 | A train-station marker tap opens the CR6 station info card (`stationInfoCardHtml` / `openStationCard`), replacing the v0.61.17/.26 one-stop neighbour-detail focus view. | The card carries the station identity and links; the neighbour-detail view is superseded. | 20-05 '26 |
| D-61.58 | Selected-station polyline emphasis uses `trackBetween()` — the prev→current→next stretch of the selected station's line is sliced and emphasised while base lines mute to 0.25 opacity. | Supersedes the Cuisine search-emphasis windows; one selective-emphasis model. | 20-05 '26 |
| D-61.63 | Nearest-bus-stop triangulation is an offline Haversine join from committed LTA open data (`stations.json` + `LTABusStop.geojson`); OneMap `getNearestBusStops` is a name-only pass, operator-run with their own key. | The sandbox cannot reach OneMap; codes/coords come from open data, names from the keyed API — non-invention preserved. | 20-05 '26 |
| D-61.67 | First/last-train timings join `data/sg_train_timings.json` into `stations.json` by station code; the timing strings are stored verbatim, missing-data rows keep the source note, LRT-loop stations are left empty. | Operator-supplied scraped data; non-invention — no timing is fabricated. | 20-05 '26 |
| D-61.69 | A place is listed in `sg_24hr_places.json` only when the Google Places API (New) opening-hours data confirms 24-hour operation; all fields are verbatim from the API, missing → `null`; the live fetch is an operator-run step. | Non-invention rule; the build sandbox has no web egress. | 20-05 '26 |
| D-61.70 | Toggle ON = Singapore blue / OFF = white; the bus-stop pin is a white zoom-aware rectangle; Cuisine venue pins are numbered circles by result rank; the **Cuisine map is the standardisation reference** for all 3 TMAs. | Operator's 10-item map redesign; one reference map keeps the 3 TMAs uniform. | 20-05 '26 |
| D-61.75 | A webview hub that is always the entry point (e.g. the `/menu` hub) shows a fixed "End" FAB that calls `WebApp.close()`, never a history-derived "Back". | `history.length` only ever grows, so a Back-vs-End heuristic mis-fires after navigating away and closing; the hub has no sub-views, so Back is never meaningful. | 21-05 '26 |
| D-61.76 | `stationMoreInfoUrl` resolves `stations.json` keys via a normalised second index (lowercased, MRT/LRT/Station tokens + punctuation stripped) when the exact key misses. | Google Places returns display names ("Harbourfront") that differ in casing/spacing from the JSON keys ("HarbourFront"); a normalised lookup makes the station-info hyperlink robust to that mismatch. | 21-05 '26 |
| D-1076 | A fresh, non-incremental master set of the eight-folder documents is produced at v0.61.76 and frozen into `vault/v0.61.76/`, following the v0.61.28 vault-pass precedent. Prior versioned files remain on disk; the AU append-only Recipe is overridden for this pass only. No code change, no version bump. | Established vault-cadence precedent (D-1028); a frozen snapshot ships alongside a freshly regenerated master set. | 21-05 '26 |
| D-1077 | Standing rule — the Journal is updated every time a PR is opened and again when it merges (`[HDR]` block + `doc/.serial-state.yml` counter bump + re-anchor); a `doc/Feature/` / `Technical/` / `Register/` or `vault/<v>/` update is either done in the same pass or logged as "still due" in `[KNOWN GAPS]` and `doc/Register/`. | Operator instruction — *"Update the Journal every time a new PR is created and done."* Recorded as decision-gate G3 in `journal-0_60_144`. | 13-05 '26 |

---

## Completed

> Shipped items, by arc. The exhaustive per-PR record is in `doc/Journal/` (275 entries).

| # | Arc | Outcome | Versions |
|---|---|---|---|
| C-U | Geo-overlay system | `build-geo-overlays.js` → `data/geo-*.json` served at `/api/geo/*`; parks / attractions / carpark / exits / taxis chip layers on the Cuisine / Hawker / Transport maps | v0.61.0–.10 |
| C-V | Train overlay + line geometry | OSM/Overpass-derived, Chaikin-smoothed MRT/LRT polylines; square line-coloured station markers; `mrt-lines.js` metadata | v0.61.1 / .7 / .8 / .9 |
| C-W | Station-detail focus view | Tap a train-overlay station → it + one-stop neighbours; ported to all three map TMAs (later superseded by the CR6 station info card — C-AG) | v0.61.13–.17 |
| C-X | Amenity pins | Clickable bus-stop live arrivals; hawker-centre surrounding amenity pins; the unified `attachAmenityPins` helper; theme-aware popup cards + in-card close | v0.61.18–.23 |
| C-Y | Exit feature | Part A — line-coloured exit pins + the Exit Template (code / station / station-codes); Part B — nearby named attractions on the Template | v0.61.24 / .28 |
| C-Z | Station-scoped chips + slider removal | The Nearby↔Details slider removed; Exits/Taxis chips scope to the 3 station-detail stations | v0.61.26 |
| C-AA | `/transport` station codes | Nearest-stations chat rows show the real station code, line-coloured | v0.61.27 |
| C-AB | `/v` builder menu | An owner-only inline-keyboard hub firing `/ver`, `/oversight`, `/ftlog`, `/log on|off` | v0.61.25 |
| C-AC | Map polish | Square station pins, per-layer radius, zoom row, `clickableIcons` POI suppression, expand icons, `/train` alias, standardised taxi word-label pins | v0.61.9 / .18 / .22 / .23 |
| C-AD | v0.61.28 fresh-master doc set + vault | Fresh Builder / Persona / Feature / Technical / Legal / Register masters; `vault/v0.61.28/` snapshot | v0.61.28 |
| C-AE | Cuisine LocationField rework | Editable LocationField moved above the map with a `suffix` status prop; a 🔍 search-trigger button (icon ✏️↔🔍, edit auto-closes after 2.5 s idle); rotating/refresh loading overlay | v0.61.29 / .50 |
| C-AF | Map-controls redesign (7-phase) | Phases A–G — Clinics/Police/Hospital/Clinic-Pharmacy POI layers; the `Google Map ↗` popup standard; the unified presentational `MapControls` (`⋯/⋮` menu + pills + dropdown), `MapLayerChips` retired; vertical nav-button column; the working 🎨 Colour greyscale toggle (default-ON, ON=colour) | v0.61.31–.42 |
| C-AG | CR6 station info card | A 6-phase build — Phase 1 the offline `build-station-info.js` data layer (`data/stations.json`, 180 stations); Phase 2a per-exit bus services; Phase 2b first/last-train timings; Phase 3 the `stationInfoCardHtml` card (a station tap opens it, replacing the neighbour-detail view); the operator-template card redesign; per-exit nearest-bus-stop draw | v0.61.55–.68 |
| C-AH | Bus Stop overlay layer | `sg-address.js` Singapore street/building abbreviation expansion at the API boundary; the Bus Stop overlay layer enabled (`transport.allStops` + `/api/geo/bus-stops`, Redis-cached 24 h, `🚏` amenity-label + live-arrivals popup); bus-stop popup legibility + timing-first arrival rows | v0.61.42–.44 / .47 / .51 / .64 |
| C-AI | Greyscale-toggle fixes | Raster-base-map desaturation fix; the fixed high-contrast popup palette (no longer theme-following); the monochrome 🎨 Colour-button icon | v0.61.45 / .46 / .47 |
| C-AJ | Clinic/hospital data + pins | The 💊 clinic/pharmacy structured popup + CHAS structured fields; `fetch-clinic-hours.js` opening-hours enricher (1059/1193 clinics, ~89% source ceiling); the 🏥 Hospital overlay layer (`fetch-hospitals.js`, 67 facilities); the clinic pin as a red "+" rectangle | v0.61.39–.41 / .48 |
| C-AK | Train-layer emphasis + zoom-aware layer | The CR5 zoom-aware train layer (stations become line-coloured pills above zoom 15); CR4 bus-stop focus de-emphasis; CR5 v2 selective `trackBetween()` polyline emphasis; the v0.61.65 two-colour station pin + `exit_centroid` coordinate field | v0.61.52–.54 / .58 / .65 |
| C-AL | Map UI redesign (10-item) | Quick-toggle row on one line; toggle ON=blue/OFF=white; the `⋯` menu as a white circle; the white zoom-aware bus-stop rectangle pin; exit-flash card links; Cuisine numbered venue pins; the 🔍 FAB pulse on result load; the Cuisine map standardised across all 3 TMAs | v0.61.66 / .70 |
| C-AM | `/transport` reply layout reworks | Train reply leads with the weather + nearest-3 header and station name hyperlinks (`stationMoreInfoUrl`); nearest-bus-stops message reworked to the operator's template (weather leads, live stop count, road-name lead, per-stop distance sentence, `🚌 Bus №` rows, "minutes" ETA bands); `📍 Refresh location` on the Bus menu + `/carpark` result | v0.61.61 / .71–.73 |
| C-AN | Data pipelines (operator-run) | `fetch-24hr-places.js` (Google Places API 24-hour places, 8 types — pipeline ships, data run is operator-side); `enrich-nearest-bus-stops.js` (offline Haversine + OneMap name pass) | v0.61.63 / .69 |
| C-AO | `/healthz` outage hotfix | The `pkg`→`pkgJson` `ReferenceError` that 500'd `/healthz` and cascaded into all 3 Mini Apps blanking — one-token fix | v0.61.62 |
| C-AP | Late-arc fixes | Bus sub-menu title reword + Train nearest-stations radius widened 1500→5000 m; the `/menu` hub FAB always shows "End"; `stationMoreInfoUrl` normalised-name fallback lookup | v0.61.74 / .75 / .76 |
| C-AQ | v0.61.76 fresh-master doc set + vault | Fresh Builder / Persona / Feature / Technical / Legal / Register masters; `vault/v0.61.76/` snapshot | v0.61.76 |

---

## Removed Features

> AU-EOF: this table sits at end of file. The canonical Removed table is `doc/Feature/feature-0_61_76-…md` §Removed Features. No Register-tracked item has been deleted — items only move between sections.

| # | Item | Original Version | Removed in Version | Removal Date | Reason | Replacement | Rule | Serial № |
|---|------|------------------|--------------------|--------------|--------|-------------|------|----------|
| — | (see `doc/Feature/feature-0_61_76-…md`) | — | — | — | — | — | AU-2 | (№ 115 — 21-05 '26 09:44 SGT) |

---

## Rule reference

- **AU-1:** Register items move between sections, never deleted; prior versioned Register files remain on disk. This fresh master supersedes `register-0_61_28` under the established vault-pass precedent (D-1028 / D-1076).
- **AU-2 / AU-EOF:** removals move to the Removed table at EOF.

## Amendments

(None — fresh master; the per-PR amendment trace lives in `doc/Journal/`.)
