(№ 123 - 25-05 '26 12:25 SGT)

# Register — soleat / Gia4lunch v0.61.147 (fresh master)

> **Document type:** Register (per `doc/CLAUDE.md` §3 — *open items, known issues, deferred, decisions, completed*; §4.1 — items move between sections, never deleted).
> **Status:** Fresh, non-incremental master, produced under operator instruction (25-05 '26 — *"viberecord, journal"* → Doc master refresh dropdown choice), following the v0.61.83 master + the v0.61.124 / v0.61.127 / v0.61.132 / v0.61.136 / v0.61.137 / v0.61.144 incremental Register entries. Supersedes the prior chain as the current-state ledger; every prior versioned Register file remains on disk per AU-1.
> **Reflects:** project state at **v0.61.147, 25 May 2026 12:25 SGT**, after the v0.61.84 → v0.61.147 arc (a ~63-version sequence: place-anchored search, Menu TMA Location section, Fruits/Durian/Durian Pastry special modes, branding fix, wake-from-idle rewrite, catalogue chip refactor, per-slug image assets, name-dedup, group-card emojis). No vault snapshot in this pass — operator scoped to the doc master set only.

---

## Open Items

| # | Item | Owner | Priority | Notes |
| :--- | :--- | :--- | :--- | :--- |
| O-1 | Exit feature **Part B — ♿ lift availability per exit** | operator + agent | medium | No LTA dataset carries lift-per-exit accessibility; the SMRT site that does (`journey.smrt.com.sg/.../exits/`) 403s non-browser clients and is unreachable from the build sandbox. Needs a scraper run in an open-network environment committing a static data file — or operator-supplied data. The buildings/attractions half shipped in v0.61.28. |
| O-2 | **Building-anchored cuisine search** | agent | medium | **Partially addressed by C-BA** — the v0.61.129 O-20 Tell-me place-anchor flow handles building names typed in the Cuisine TMA Tell-me box (via `place-detector.detectPlaceName`'s geocode fallback). True "building eateries first, then vicinity" (in-building-only-then-vicinity tier) is still not built. Spans `cuisine-search.js` / `pipeline.js` / `runFreeTextSearch`. |
| O-3 | `doc/VibeCodingRecord/` PR-ledger refresh | operator + agent | low | **Routinely addressed** — the v0.61.128 (PR #629, #553-#627), v0.61.133 (PR #634, #628-#633), v0.61.143 (PR #644, #634-#643), v0.61.147 (PR #648, #644-#647) catch-up cycles keep the ledger current. The standing-rule cadence in `CLAUDE.md` root ("Regenerate when convenient") is the working pattern. Item stays on the ledger as the **convention reminder**, not an outstanding gap. |
| O-4 | Observability expansion | operator | low | `doc/observability-expansion-plan.md` (a plan for latency / request-volume / venue-popularity / UX-funnel metrics) is written but unbuilt. |
| O-5 | Security hardening — remaining items | operator | medium | Auth chokepoint on `/api/{menu,hawker,transport,oversight}/*` (DF-53); per-chatId sliding-window limits on all hot endpoints (DF-54); API-key budget caps in the Google/Gemini consoles (DF-55, operator-side). |
| O-6 | Michelin top-12-fresh Places-cost watch (DF-94) | operator | low | Monitor Places billing now the Michelin 24 h result-cache is dropped. |
| O-7 | TMA component-test infrastructure | operator | low | No React component-level test harness yet (no JSDOM scaffolding). Carried through the v0.61.131 (33 helper tests), v0.61.141 (88 helper tests), v0.61.145 (no JSX test) PRs — every chip-render test would benefit from this. The CR-7 click-to-expand exit toggle is still gated on it. |
| O-8 | Train-station tap has no info popup | operator | low | Pre-v0.61.57 behaviour, resolved by the v0.61.57 CR6 station info card (C-AG in v0.61.83 master). Kept on the ledger as the resolved precedent for the deliberate "no InfoWindow" decision; revisit only if a different station-tap interaction is requested. |
| O-9 | **Base-map station labels cannot be hidden in code** | operator | low | The 3 TMA maps use a `mapId` (required for `AdvancedMarkerElement` pins); Google Maps JS ignores the `styles` option whenever a `mapId` is set. To hide `transit.station` labels the operator must edit the styled Map ID in Google Cloud Console and ensure that Map ID is the `MAP_ID` env var on Railway. No code change is possible. |
| O-10 | **`data/sg_24hr_places.json` is an empty scaffold** | operator | medium | The v0.61.69 24-hour-places fetch pipeline (`scripts/fetch-24hr-places.js`) ships, but the data file is a committed scaffold — the build sandbox has no web egress. Operator step: `GOOGLE_PLACES_API_KEY=… npm run fetch:24hr-places`, then commit. |
| O-11 | **`bus_stop_name` unresolved in the nearest-bus-stop dataset** | operator | medium | `data/stations_with_nearest_bus_stops.json` (v0.61.63) ships with all 880 `bus_stop_name` fields `null` — Stage B (the OneMap `getNearestBusStops` name pass) cannot run from the sandbox. Operator step: `ONEMAP_API_KEY=… node scripts/enrich-nearest-bus-stops.js`, then commit the three refreshed data files. |
| O-21 | **vitest 2.1.9 + Node 24 incompatibility (dev only)** | operator | low | Local dev shell is Node 24; vitest 2.x targets 18/20/22. Symptom: `npm test -- --run` hangs at the `RUN v2.1.9` banner for hours. **Working pattern: per-file fast path** — `npx vitest run __tests__/<file>.test.js __tests__/<other>.test.js` runs in 700-900 ms with no hang (proven via 6+ test files in the v0.61.131-145 arc, totalling 121+ tests in <1 s each). CI on Node 20 runs the full suite authoritatively. Resolution paths unchanged: (a) `nvm install 20`, (b) upgrade repo to vitest 3.x. Codified in `technical-0_61_147-…` §4.8. |
| O-22 | **Working tree on iCloud Drive throttles git + vite + esbuild** | operator | low | The repo lives at `~/Library/Mobile Documents/com~apple~CloudDocs/Downloads/Github/gia`. iCloud's lazy-sync model causes silent partial-writes (the v0.61.133→134 boundary saw both local pack files become "too short to be a packfile"; the v0.61.139 + v0.61.141 sessions hit `Invalid package config` on `node_modules/rollup/package.json` and `node_modules/axios/package.json`). **Working pattern: plumbing-commits** + pack-corruption recovery, both codified in `technical-0_61_147-…` §4.5-§4.7. Used for **every PR** in the v0.61.128-147 arc (20+ PRs). Permanent fix: `mv` repo out of iCloud. |

---

## Retracted

| # | Item | Originally raised | Retracted in | Reason |
| :--- | :--- | :--- | :--- | :--- |
| O-24 | Sarawak / Petra Jaya as a real Malaysian-East anchor | v0.61.127 | v0.61.137 | **Typo-derived non-item.** The v0.61.126 spec doc's *"Petrajaya / Petra Jaya, Sarawak"* string was a typo for Putrajaya. The v0.61.127 Register opened O-24 as a hypothetical "if operator later wants the actual Sarawak Petra Jaya", but no such operator intent existed — over-defensive logging of the typo's downstream effect, not a roadmap entry. The v0.61.136 *"Deferred"* classification was corrected to **Retracted** in v0.61.137 (operator: *"this is to drop totally — it was a typo mistake"*). Any future Sarawak coverage request → fresh O-NN row citing D-61.137, NOT a re-open of O-24. |

---

## Decisions (append-only, v0.61.84 → v0.61.147)

> Prior decisions D-1028 / D-1076 / D-1083 + the v0.61.83 master's earlier decisions live in `register-0_61_83-21_05_26-1645.md`. This section continues from v0.61.120.

| # | Decision | Rationale | Date |
|---|---|---|---|
| D-61.120 | Chat free-text path gains a deterministic `<cuisine> + <food noun>` whitelist (`freetext-classify.looksLikeCuisineBrowse`) that short-circuits the v0.60.216 Gemini food-relatedness gate. | `"western cuisine nearby"` was returning the food-nudge decline because Gemini's prompt only buckets text as `dish/ingredient/tool/venue/ambiguous` and `"western"` is an umbrella with no family in `cuisine-family.js`. | 24-05 '26 |
| D-61.121 | Chat free-text gains a place-anchored search path via the new `place-detector.js`. Deterministic ladder: MRT/LRT first (`mrt-coords.json`), then hawker (`hawker-vault.findByName` fuzzy), then geocode fallback (`vibe-suggest.geocodeQuery`). Per-kind radii: hawker 150 m / MRT 400 m / geocoded 300 m / nearby 1500 m. | Operator request: *"if I type in address or building name or place, you should priority search for that place"*. Deterministic ladder keeps the path cheap. | 24-05 '26 |
| D-61.122 | Chat `/location` reply appends a 12-button inline keyboard (10 STB Singapore precincts + 2 Malaysia anchors). Malaysia anchors carry `radiusCapM` (JB 30 km, IOI Putrajaya 15 km). `precincts.js` is the new SoT for curated anchors; `setUserLocation(opts?)` carries `region / radiusCapM / label / precinctId` on the cached payload. | Operator instruction for the 10 STB precincts + JB + IOI Putrajaya quick-pick set with cap enforcement at the Places-call boundary. | 24-05 '26 |
| D-61.123 | Menu TMA hub adds a `LocationFieldMenu` (free-text input + grouped `<select>` of 17 entries — 10 STB + 5 SG region buckets + 2 Malaysia anchors) via `/api/menu/set-location` `{ text | precinctId }`. SG-only tiles (Hawker / Incidents / Bus stops / Weather / TrainPanel) flip to disabled when anchor is `'JB'` or `'MY-PUT'`. | Operator instruction for the Menu TMA location field below PLAN section. | 24-05 '26 |
| D-61.124 | Cuisine TMA `/api/cuisine/search` auto-clamps to anchor `radiusCapM` + region; v0.61.119 nearby button gains polygon-based "OUTSIDE <precinct>" exclusion for STB-anchored searches; *"showing X of Y eateries here"* copy; weak-place auto-suggest; `/location` chain → `🔍 See eateries here`; Menu TMA Places autocomplete; disabled-tile note. | Operator "do 1-7" 7-item completion against the v0.61.124 audit. | 24-05 '26 |
| D-61.125 | **Region-aware geocode helper** — new `vibe-suggest.geocodeQueryRegion(text, { region, biasCenter })`. Sends Places `locationBias` circle around the existing anchor + filters out hits outside the expected country bbox. Legacy `geocodeQuery` preserved for back-compat. | Sheraton-in-Johor bug: typing *"Sheraton Hotel"* with a JB anchor active resolved to Sheraton Towers Orchard because the legacy helper hardcoded `" Singapore"` suffix. | 24-05 '26 |
| D-61.125b | **Menu TMA Location section** moved to its own top-level section after PLAN. Summary line bumped to `text-[13px]`; disabled-tiles list on its own next line at `text-[11px]`. All other Menu TMA text-sizes bumped one Tailwind step up. | Operator instruction on layout. | 24-05 '26 |
| D-61.126 | **Special-mode exclusive search class** — Fruits + Durian replace `cuisineQueries` with per-mode seeds; downstream Tell-me / dessert / home-based blocks bypassed via `if (… && !specialMode)`. `special-mode.js` is the single SoT. CuisineDrawer greyed out the catalogue grid when a mode was active (later retired by D-61.141). | `scripts/Create_2_buttons.MD` spec — must NOT mix special modes with normal cuisines; must surface "Limited matches nearby" rather than padding. | 24-05 '26 |
| D-61.127 | **Spec correction precedent** — when an operator-supplied spec doc carries a factual error, the correction lands as its own PATCH (no code change) editing the spec doc + AU-7 amendment in the relevant journal. Prior text is acknowledged in the corrected paragraph, NOT silently erased. | First instance: v0.61.127 corrects the Malaysia zone in `Create_2_buttons.MD` from *"Petrajaya / Petra Jaya, Sarawak"* to Putrajaya. | 24-05 '26 |
| D-61.129 | **Cuisine TMA `/api/cuisine/search` is now place-aware.** Tell-me text → `searchCenter` pivot + radius tighten + freeText strip via the same `place-detector.detectPlaceName` ladder used by chat `/s`. **AND** Fruits/Durian special-mode results widen radius progressively (2× / 3× the initial, capped at `anchorCap` or 30 km) when below the 8-target. Surfaced as `payload.placeAnchor` / `payload.specialModeWidened*`. | Closes O-20 + O-23 from the v0.61.127 Register. | 24-05 '26 |
| D-61.130 | **TMA layer surfaces the v0.61.129 backend fields.** New `wake2.body` / `anchor.*` / `special.widened` i18n keys; pill above result list (`📍 Searching near …` / `📍 Showing 'X' near …`); caption appended to the existing amber `Limited matches` card; page-cache + failure-path resets. v0.61.131 followup: pill renders FIRST in the info-card stack. | The v0.61.129 backend surfaced new payload fields but the React layer didn't render them — feature was silently correct but invisible. v0.61.131 then closed a v0.61.130 layout gap. | 24-05 '26 + 25-05 '26 |
| D-61.136 → D-61.137 | **O-24 retracted as a typo-derived non-item.** D-61.136 initially classified the operator's drop as a "scope decision"; D-61.137 amends to **Retracted** per the operator's clarification that O-24 was never a real roadmap entry. Future Sarawak request → fresh O-NN row citing D-61.137, not a re-open of O-24. | The Register / Journal need to faithfully reflect what was decided. Mis-classifying a documentation typo as a scope decision inflates the trace. | 25-05 '26 |
| D-61.140 | **Wake-from-idle is a 2-step flow.** First text after 6 h idle → `promptLocationOnWake` stamps `wake:pending:<chat>` + sends `wake.intro` + `LOCATION_REQUEST_KEYBOARD`. On share → `handleWakeLocationResponse` reverse-geocodes street, reads cached anchor, sends `wake2.body` HTML with 3 inline buttons + `/l` helper text. Device GPS stashed in `wake2:offer:<chat>` for the `wake2:current` button. v0.61.84 keep/new path retained for back-compat. | Operator screenshot of the stale wake message + the only way to surface "device now near …" is to ASK (Telegram bots can't read GPS unsolicited). | 25-05 '26 |
| D-61.141 | **Special-mode toggles are catalogue chips, not amber pills.** Fruits / Durian / Durian Pastry live as regular chips inside the "Dessert, Fruits" group; symmetric mutex (special ↔ Dessert + all other cuisines) via `applyChipToggle` in `web/cuisine/src/v2/lib/cuisine-selection.js`. **Durian narrowed to "fruit only"** (pastry terms moved to a NEW `DURIAN_PASTRY` mode; `NAME_REJECT_PATTERNS[DURIAN]` regex list catches pastry-named venues). | Operator screenshot + written spec. Codifies the recipe for future special modes. | 25-05 '26 |
| D-61.142 | **Per-slug image-asset override for catalogue chips.** `IMG_FLAG_BY_SLUG` in `cuisines-vault.js`; chip renderer uses `<img src="/app/cuisine/{imgFlag}">` when set, falls back to emoji `flag` (or default 🍽️) on load error. First slug: `'durian' → 'durian.png'`. | Operator uploaded a 204×192 durian PNG via `data/IMG_2177.png`. Establishes the recipe for future per-slug assets. | 25-05 '26 |
| D-61.145 | **Special-mode name-dedup.** After the v0.61.129 widening loop, filter `venues` by `name.toLowerCase().trim()` to drop multi-outlet chains showing the same name twice (e.g. Golden Moments Durian's many outlets). Only fires for special modes — normal cuisine searches still show multiple outlets of chains (Tim Ho Wan / Toast Box are intentional). Plus: 4 new `FLAG_BY_SLUG` entries (`dessert: '🍨'`, `fruits: '🍉'`, `durian: '🥥'`, `durian-pastry: '🥮'`). | Operator screenshot of repetitive Golden Moments durian cards + emoji spec. | 25-05 '26 |
| D-61.146 | **Durian chip swaps PNG for JPEG.** Operator: *"durian emoji must use the following image and no other"* → `IMG_FLAG_BY_SLUG['durian']: 'durian.png' → 'durian.jpeg'`. The 🥥 emoji from D-61.145 carries as the on-error fallback (operator's "no other" rule is about which IMAGE, not whether to have an emoji safety net). | Operator uploaded a 204×192 JPEG via `data/IMG_2180.jpeg`. | 25-05 '26 |

---

## Completed

| # | Arc | Outcome | Versions |
|---|---|---|---|
| C-AV | **Location-anchor arc** | Chat free-text `<cuisine>+<food noun>` whitelist (v0.61.120) · chat place-anchored search + nearby button (v0.61.121) · `/location` STB/JB/IOI quick-picks with `radiusCapM` (v0.61.122) · Menu TMA location field + SG-only-tile grey-out (v0.61.123) · 7-item "do 1-7" completion + polygon exclusion + auto-suggest + autocomplete (v0.61.124). | v0.61.120 → v0.61.124 |
| C-AW | v0.61.120-124 Journal catch-up | 5 journal entries + serial-state bumps + register increment. | v0.61.124 |
| C-AX | Menu TMA Location section + JB geocode fix + font bump | Sheraton-in-Johor bug fixed via `geocodeQueryRegion`; Menu TMA text bumped one Tailwind step; LocationFieldMenu summary split. | v0.61.125 |
| C-AY | Fruits + Durian exclusive special modes | New `special-mode.js`; CuisineDrawer 2 amber-pill toggles (later retired); `/api/cuisine/search` overrides + post-filter. | v0.61.126 |
| C-AZ | Spec correction — Putrajaya not Sarawak | 4 textual fixes in `Create_2_buttons.MD`; no code change. | v0.61.127 |
| C-BA | **O-20 — Tell-me box place anchor (Cuisine TMA, full E2E)** | PR #630 backend + #631 TMA UI pill + #632 reorder + 17 inline tests (`cuisine-tellme-anchor.test.js`). | v0.61.129 → v0.61.131 |
| C-BB | **O-23 — Fruits/Durian progressive radius widening** | PR #630 widening loop + #631 TMA caption + #632 extract + 16 inline tests (`cuisine-special-mode-widen.test.js`). | v0.61.129 → v0.61.131 |
| C-BC | **Cuisine-TMA chip refactor + group emojis + asset overrides** | PR #642 special-mode chips in "Dessert, Fruits" group + mutex + 23 inline tests (`cuisine-selection.test.js`) + 65 expanded special-mode tests · PR #643 durian PNG via `IMG_FLAG_BY_SLUG` · PR #646 name-dedup + 4 FLAG_BY_SLUG entries · PR #647 durian PNG → JPEG swap. | v0.61.141 → v0.61.146 |
| C-BD | **Wake-from-idle 2-step rewrite + branding** | PR #639 user-facing `Gia → Soleat` (22 strings across i18n + index.js + Header.jsx) · PR #640 Menu TMA addr format via Places `addressComponents` (10 inline tests) · PR #641 wake-from-idle 2-step (`request_location` → reverse-geocode street → rich comparison + 3 buttons + `/l` helper text). | v0.61.138 → v0.61.140 |
| C-BE | **Michelin path + Technical doc** | PR #635 Michelin handler threads place anchor + post-strip freeText (closes v0.61.129 KNOWN GAP) · PR #636 Technical doc incremental codifying O-22 plumbing-commit + pack-corruption recovery patterns + per-file vitest fast path (`technical-0_61_135-…` §4.5-§4.8). | v0.61.134 → v0.61.135 |
| C-BF | **Register cadence (O-24 trace + decisions logging)** | PR #629 ledger catch-up #553-#627 (75 rows) · PR #633 Register increment closing O-20 + O-23 + logging D-61.129 / D-61.130 · PR #634 ledger catch-up #628-#633 · PR #637 O-24 → Deferred · PR #638 O-24 → Retracted (typo-derived) · PR #644 ledger catch-up #634-#643 · PR #645 Register increment logging D-61.140 / .141 / .142 · PR #648 ledger catch-up #644-#647 · PR #649 (this PR) — fresh master pass. | v0.61.128 → v0.61.147 |

---

## Removed Features

> AU-EOF. No Register-tracked item has been DELETED in the v0.61.84 → v0.61.147 arc. O-24 moved to Retracted per AU-1 (items move between sections).

| # | Item | Original Version | Removed in Version | Removal Date | Reason | Replacement | Rule | Serial № |
|---|------|------------------|--------------------|--------------|--------|-------------|------|----------|
| — | (none in this arc) | — | — | — | — | — | AU-2 | (№ 123 — 25-05 '26 12:25 SGT) |

---

## Rule reference

- **AU-1:** Items move between sections, never deleted. This fresh master supersedes prior register entries as the current-state ledger; the prior chain (v0.61.83 master + v0.61.124 / v0.61.127 / v0.61.132 / v0.61.136 / v0.61.137 / v0.61.144 increments) remains on disk for trace.
- **AU-7:** D-61.130 / D-61.137 are explicit AU-7 amendments to their predecessors (D-61.129 layout decision, D-61.136 retraction frame). Other Decision rows stand on their own.

## Amendments

(None in this fresh master — it consolidates, doesn't amend. Per-decision amendment history lives in the per-PR journal entries.)
