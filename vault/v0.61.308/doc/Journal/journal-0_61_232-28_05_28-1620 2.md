(№ 433 - 28-05 '26 16:20 SGT) v0.61.232

# Journal — soleat v0.61.232 (re-ship social-profile row from closed PR #729)

> Operator (28-05 '26): asked to merge PR #729 (v0.61.226 social-profile
> row). PR #729 had been open since v0.61.225 — my session shipped 13
> intervening PRs (v0.61.218 → v0.61.231), so the PR's branch was on a
> stale base and showed `mergeable: CONFLICTING`. Operator chose
> "Close #729, re-ship as v0.61.232 from current main" (recommended).
>
> 7 of 8 modified files were untouched in main since v0.61.225 → direct
> blob copy. `index.js` got a clean 3-way merge with 0 conflict markers
> against the v0.61.222 (Bug B fallback) + v0.61.230 (regionCode-aware)
> changes that landed in the meantime.
>
> Commit 445 / journal 433 / build 235, on branch
> `claude/v0.61.232-social-profile-row` off `main` at v0.61.231.

## New

### [HDR] #433 | 16:20 SGT 28-05-28 | v0.61.232 — re-ship social-profile row | 10 files | 1 PR | 1 build |

- **[INTENT]** Get the social-profile row (operator-drafted as
  v0.61.226) onto production without the stale-base merge conflicts
  PR #729 had accumulated.

- **[DELTA]**

  **All 11 files from PR #729 are reused as-is on top of current main:**

  - NEW `social-profiles.js` (~265 lines) — `getSocialProfiles`,
    `fetchSocialProfilesForVenues` (concurrency-4 fan-out),
    `attachSocialsToVenues`, `pickTopProfiles`. Per-platform URL
    regex for **Instagram / TikTok / Facebook / X / YouTube /
    Threads**. Redis `social:<placeId>` with 30-day TTL via
    `setEx`; negative caching; no caching on transient Gemini
    failures. Gemini-grounded Google Search call backed by
    `gemini-client.js`'s v0.58.42 fallback chain.
  - NEW `__tests__/social-profiles.test.js` (~190 lines, 19 tests) —
    URL pattern positive + negative cases, JSON parser (bare /
    fenced / surrounded by prose / malformed), priority ordering,
    cap-3 picker, Redis cache hit/miss/negative, Gemini total-
    failure non-caching, fan-out concurrency cap, regression guard
    on URL_PATTERNS shape.
  - NEW `web/cuisine/src/v2/components/SocialButtons.jsx` — brand-
    glyph button row on the result card (max 3, priority IG →
    TikTok → Facebook → others).
  - `venue-templates.js` — new 📱 row in `formatVenueBlock`,
    inserted immediately after the 📍 maps line, inside the
    `includeContact` gate so T3 compact / `deliverPicks` stays
    sparse. URLs space-joined with ` · ` — Telegram auto-links.
  - `web/cuisine/src/v2/components/ResultCard.jsx` — imports +
    renders SocialButtons; lazy fetch on card mount.
  - `web/cuisine/src/v2/lib/api.js` — client helper for
    `/api/cuisine/socials` lookup.
  - `index.js` — new server endpoint + venue enrichment plumbing
    (+93 additions, no deletions; 3-way merged clean against the
    v0.61.222 and v0.61.230 changes to the same file).
  - `__tests__/venue-templates.test.js` — 📱 row coverage.
  - `package.json` — `0.61.231 → 0.61.232` (was `0.61.226` in #729).
  - `doc/.serial-state.yml` — counters bumped on top of current
    main's v0.61.231 entry.
  - `doc/Journal/journal-0_61_232-28_05_28-1620.md` — this file.
    The old `journal-0_61_226-28_05_28-1045.md` from PR #729 is NOT
    re-included (the v0.61.226 entry it describes never landed).

  **Merge integrity check:**

  ```
  $ git diff v0.61.225 origin/main -- <each PR #729 file>
      7/8 modified files: UNCHANGED in main → direct blob reuse
      1/8 index.js: CHANGED in main → 3-way merge needed

  $ git merge-file ours.js base.js theirs.js
      exit 0
      conflict markers: 0
      lines: 15933
      node --check: syntax OK
  ```

- **Background — why this took a parallel PR.** PR #729 was opened
  by an earlier session as v0.61.226 with the social-profile work,
  but operator chose not to ship it during my session ("Leave it;
  my city-dropdown ships as v0.61.227"). When operator returned to
  it after v0.61.231 landed, GitHub reported `mergeStateStatus:
  DIRTY` / `mergeable: CONFLICTING` because main had advanced by
  ~6 PRs. Closing-and-reshipping cleanly side-steps the rebase /
  force-push permission gate.

- **[VERIFICATION (sandbox)]**

  ```
  $ node --check /tmp/index_merged.js                     (OK)
  $ git diff v0.61.225..origin/main -- <PR #729 files>   (7 unchanged, 1 merged)
  ```

  Vitest deferred to CI on Linux (recurring iCloud / Node-24
  PostCSS quirk on the local). Tests in PR #729 were already CI-
  green pre-conflict.

- **[STATUS]** PR-OPEN on push.

- **[TEST]** Operator (post-merge + Railway redeploy):
  1. Cuisine TMA result cards → see 📱 social-button row on cards
     that have IG / TikTok / Facebook profiles.
  2. Free-text `/s <dish>` venue card → 📱 row with full URLs
     (space-joined, Telegram auto-links).
  3. Copy / Copy-all → 📱 row included.
  4. First lookup for a new placeId triggers a Gemini call;
     subsequent lookups within 30 days hit the Redis cache.

- **[KNOWN GAPS]**
  - The original PR #729 journal entry (`journal-0_61_226-...`) is
    dropped intentionally — that v0.61.226 entry never made it to
    main. This journal (v0.61.232) supersedes it.
  - If the Gemini grounded-search returns a malformed JSON, the
    error path doesn't cache → next call retries. Operator may want
    a circuit breaker after N failures; deferred.

- **Confirmation Gates**

  | Gate | Resolution | Date |
  |---|---|---|
  | G1 — version bump | PATCH `0.61.231 → 0.61.232`. Re-shipping payload from the closed PR #729. | 28-05 '26 |
  | G2 — destructive action | YES (operator-confirmed) — closing PR #729 without merging. The payload is reproduced as v0.61.232; no work is lost. | 28-05 '26 |
  | G3 — new decision rule | When a long-running PR shows merge conflicts and the diff is mostly additive, prefer close-and-reship over force-rebase. | 28-05 '26 |
  | G4 — paid external API call | YES (existing pattern) — Gemini grounded-search call per first-lookup-per-place. Redis cache cuts repeat cost. | 28-05 '26 |

## Amendments

_None — additive PR._

## Deleted

- PR #729 closed (operator-confirmed). Payload reproduced in this PR.
