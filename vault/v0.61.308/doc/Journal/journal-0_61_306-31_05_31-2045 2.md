(№ 507 - 31-05 '26 20:45 SGT) v0.61.306

# Journal — soleat v0.61.306 (doc upkeep: feature RF-16 + post-merge confirmations for v0.61.304 + v0.61.305)

> Operator (31-05 '26, post v0.61.305 deploy verification): *"Doc-upkeep — vault / journal / features"*. After the live Railway logs confirmed both v0.61.305 fixes working end-to-end (typed "Raffles City Shopping Centre" auto-fired + persisted; "Mid Valley Southkey" picked from drawer also auto-fired), this PR closes the doc loop on the two-PR arc — feature ledger gets RF-16 (the v0.61.244 SG-only auto-fire-on-pick guard now superseded), the journal records both prior PRs as MERGED, and the in-session standing rule ("update the Journal every time a new PR is created and done") is honoured.
>
> Vault snapshot at v0.61.305 follows as a separate PR (B) due to size (~163 MB / 1100+ files per v0.61.208 precedent).
>
> Commit 520 / journal 507 / build 309 / feature 50, on branch
> `claude/v0.61.306-doc-upkeep` off `main` at v0.61.305.

## New

### [HDR] #507 | 20:45 SGT 31-05-31 | v0.61.306 — feature RF-16 + post-merge confirmations | 3 files | 1 PR | 0 build |

- **[INTENT]** Close the doc-upkeep loop on v0.61.304 + v0.61.305:
  1. Add RF-16 to the Feature ledger's Removed table — the v0.61.244 `noAutoFire: region !== 'SG'` guard on autocomplete pick was removed in v0.61.305.
  2. Record both v0.61.304 (PR #810) and v0.61.305 (PR #811) as merged on origin/main via this journal entry (replaces the implicit `[STATUS] PR-OPEN` in each prior journal — the prior status lines remain on disk for the authenticity trace).
  3. Bump version + serial-state so the doc-upkeep PR threads through the standard counter cadence.

- **[DELTA]**

  **1. `doc/Feature/feature-0_61_83-21_05_26-1645.md`** — appends RF-16 to the Removed Features table. Same shape as RF-12 → RF-15 (the v0.61.292 precedent). Captures:
  - Item: `noAutoFire: region !== 'SG'` flag in `handlePick`'s `onSelect` payload (Cuisine TMA LocationField).
  - Original version: v0.61.244.
  - Removed in: v0.61.305.
  - Reason: in-TMA recents drawer (📍 → 🧭, 20-entry scrollable list) makes wrong-pick revert one-tap-cheap, so the original v0.61.244 safety guard's cost-vs-benefit flipped.
  - Replacement: drawer + auto-fire-on-pick across SG / JB / OTHER.

  **2. `package.json`** — `0.61.305 → 0.61.306`. Docs-only PATCH bump (matches v0.61.301 master register refresh precedent).

  **3. `doc/.serial-state.yml`** — commit 519→520, build 308→309, journal 506→507, feature 49→50; re-anchored 31-05 '26 20:45 SGT.

- **[POST-MERGE CONFIRMATIONS]**

  - **PR #810 (v0.61.304)** — *Menu TMA visibility-rehydrate + tile-navigate hash-preserve.* Merged 2026-05-31T~10:03Z. All 4 CI checks passed (empty-blob, syntax, vitest, TMA build). Auto-merge squash fired on the last check completion. Live status: deployed on Railway.
  - **PR #811 (v0.61.305)** — *in-TMA recents drawer + LRU cap 20 + auto-fire flip.* Merged 2026-05-31T~12:30Z. All 4 CI checks passed. Auto-merge squash fired. Live status: deployed on Railway at 12:32:27 UTC (20:32 SGT); operator-confirmed working via two live picks (Raffles City Shopping Centre, then Mid Valley Southkey via drawer — both auto-fired with 12-venue results each).

- **[VERIFICATION (sandbox)]**

  - **Static**: no JS / JSX touched in this PR; no `node --check` or `npm test` re-run needed (CI will still execute the full suite on push).
  - **Lint check**: feature table row matches the existing RF-12 / RF-13 / RF-14 / RF-15 columnar shape (Item / Original Version / Removed in Version / Removal Date / Reason / Replacement / Rule / Serial №).
  - **Cross-link**: serial № `(№ 506 — 31-05 '26 18:30 SGT)` in RF-16 matches the journal-0_61_305 anchor.

- **[STATUS]** PR-OPEN on push. The four CI checks still execute (empty-blob + syntax + vitest + TMA build) but only the empty-blob guardrail has anything specific to flag for a docs-only PR; the rest pass trivially.

- **[TEST]** None — docs-only. Verifier is GitHub's diff review.

- **[KNOWN GAPS]**
  - **Vault snapshot not in this PR.** Vault is a heavy operation (~163 MB / 1100+ files per the v0.61.208 / PR #711 baseline) and traditionally ships as its own PR. Tracked as Task #157 in this session; PR B follows.
  - **Register not updated.** The Register has open items that may have been resolved by v0.61.304 / v0.61.305, but no clear signal which. Operator (or a follow-up scan) to mark Completed.
  - **Older Journal entries' `[STATUS] PR-OPEN` are not retroactively edited.** Per AU-1 (must add to prior content, not edit) the v0.61.304 and v0.61.305 journals keep their pre-merge status lines unchanged; this entry's POST-MERGE CONFIRMATIONS block is the additive supersession.
  - **Mid Valley Southkey region-tag drift.** Live trace showed `set-location → 1.4912,103.7665 label="Mid Valley Southkey" SG` despite Southkey being in JB. Cause: `handleRecentPick` in `LocationField.jsx` doesn't pass the entry's stored `country` / `region` through to `onSelect`, so the server re-infers from coords (and the coarseGate currently mis-classifies that point as SG). Pre-existing in coarseGate; exposed by the new drawer. Worth a small follow-up PR — pass `country` + `region` through.

- **Confirmation Gates**

  | Gate | Resolution | Date |
  |---|---|---|
  | G1 — version bump | PATCH `0.61.305 → 0.61.306`. Docs-only; no code change. | 31-05 '26 |
  | G2 — destructive action | _n/a_. Additive: one Removed table row + one new journal + counter bumps. | 31-05 '26 |
  | G3 — new decision rule | _n/a_. Documents v0.61.305's already-shipped rule change (in-TMA drawer is the revert affordance; auto-fire-on-pick applies in every region). | 31-05 '26 |
  | G4 — paid external API call | _n/a_. | 31-05 '26 |

## Amendments

- **`doc/Feature/feature-0_61_83-21_05_26-1645.md`** — Removed Features table gains RF-16 (autoFire SG-only guard, removed in v0.61.305).
- **`package.json`** — version `0.61.305 → 0.61.306`.
- **`doc/.serial-state.yml`** — counters + last_anchor_time re-anchored 31-05 '26 20:45 SGT.

## Deleted

_None._
