(№ 150 - 15-05 '26 09:05 SGT)

# Journal — soleat v0.60.176

> Operator: *"500/200/500/500/500 per 15 min"* — single cap change on `warm-start` (80 → 200 per 15 min). The other four endpoints already at 500/15 min from v0.60.174 + v0.60.175.

## New

### [HDR] #143 | 09:05:00 SGT 15-05-26 | v0.60.176 Cuisine rate-limit: warm-start cap 80 → 200 per 15 min | 3 files, ~5 LOC net | 1 PR |

- **[INTENT]** Operator-supplied cap adjustment. `warm-start` was the tightest of the five endpoints at 80/15 min because each call triggers ~5 internal Place Details + 1 Routes-Matrix lookup (~6 paid Places-SKU calls per request). Operator's new 200/15 min keeps it tighter than the 500/15 min Places-API-only endpoints but closer to their order of magnitude.

- **[DELTA]**

  **`index.js`** — single `makeRateLimiter(redis, { endpoint: 'warm-start', cap: 80 })` → `cap: 200`. Inline comment block expanded to record the v0.60.173 → v0.60.174 → v0.60.175 → v0.60.176 cap evolution per AU-7.

  **`package.json`** — `0.60.175 → 0.60.176` (PATCH).

  **`doc/Journal/journal-0_60_176-15_05_26-0905.md`** — this entry (#143).

  **`doc/.serial-state.yml`** — `journal 142 → 143`, `commit 149 → 150`; re-anchored `15-05 '26 09:05 SGT`.

  Final cap matrix per chatId:

  | Endpoint | per 15 min | per hour (theoretical max) |
  |---|---|---|
  | `cuisine-search` | 500 | 2000 |
  | `warm-start` | **200** | **800** |
  | `place-autocomplete` | 500 | 2000 |
  | `place-resolve` | 500 | 2000 |
  | `copy-all` | 500 | 2000 |

- **[VERIFICATION (sandbox)]**

  ```bash
  $ node --check index.js
  syntax OK
  $ npm test -- --run
   Test Files  58 passed (58)
        Tests  1769 passed (1769)
  ```

- **[STATUS]** STAGED → PROD on next Railway deploy.

- **[TEST]** Post-deploy: hammer `/api/cuisine/warm-start` at machine speed → 201st call within any 15-min window returns HTTP 429 (was 81st before this PR). Normal sessions unaffected.

- **[KNOWN GAPS]**
  - **DF-55 (carried, still critical)** — cloud-console daily quotas + budget caps.
  - **DF-61 / DF-53** — Tell-Gia refactor; auth chokepoint for other TMAs.

## Confirmation Gates

| Gate | Authority | Stamp |
|---|---|---|
| G1 — version bump | _PATCH 0.60.175 → 0.60.176 (single cap value tweak)_ | auto |
| G2 — destructive action | _additive — `cap: 80` → `cap: 200` plus comment block expansion per AU-7_ | — |
| G3 — new decision rule | _n/a — same architecture; just operator-driven sizing_ | — |
| G4 — paid external API call | _n/a_ | — |

## Amendments

(None. v0.60.175 (#142) is the prior entry.)

## Deleted

(Nothing deleted. v0.60.175's cap 80 is preserved in the inline comment block per AU-7.)
