(№ 405 - 28-05 '26 10:15 SGT) v0.61.204

# Journal — soleat v0.61.204 (Johor + Putrajaya state flags — TMA only)

> Operator: rename `data/IMG_1854.png` → `MY_Johor_flag.png` and
> `web/cuisine/public/putrajaya-flag.svg` → `MY_Putrajaya_flag.svg`,
> use both on buttons in TMA AND chat. Telegram inline buttons
> don't support custom images, so a draft of this PR experimented
> with a chat-photo confirmation for locpick:jb. Operator pushed
> back immediately: *"this state flag is very long"* — the 1:2
> Johor flag dominated the screen. Scope rolled back to TMA only:
> 18×12 region pill icon. Chat stays plain-text with the standard
> 🇲🇾 emoji.
>
> Commit 417 / journal 405 / build 207, on branch
> `claude/v0.61.204-johor-putrajaya-state-flags` off `main` at
> v0.61.203.

## New

### [HDR] #405 | 10:15 SGT 28-05-28 | v0.61.204 — Johor + Putrajaya state flags (TMA only) | 2 files + 4 new assets | 1 PR | 1 build |

- **[INTENT]** Show the actual Johor and Putrajaya state flags
  in the soleat UI for the JB and IOI Resort City picks, where
  the platform allows.

- **[DELTA]**

  **Asset files (4 new):**

  | New path | Source | Purpose |
  |---|---|---|
  | `data/MY_Johor_flag.png` | copy of `data/IMG_1854.png` | Operator's preferred filename in `data/`. The original `IMG_1854.png` stays in place. |
  | `web/cuisine/public/MY_Johor_flag.png` | same image | Vite static asset — TMA references `MY_Johor_flag.png`. |
  | `public/cuisine/MY_Johor_flag.png` | manual mirror | Runtime serves immediately, before next Vite build copies it. |
  | `web/cuisine/public/MY_Putrajaya_flag.svg` | renamed from `putrajaya-flag.svg` | Vite static. Original kept for backward-compat. |
  | `public/cuisine/MY_Putrajaya_flag.svg` | manual mirror | Runtime. |

  **TMA changes (`web/cuisine/src/v2/App.jsx`):**

  Region toggle JB pill: `johor-flag.png` → `MY_Johor_flag.png`.
  Image renderer broadened to accept both `.png` and `.svg`
  endings (so a future pill can carry the Putrajaya SVG without
  more code).

  ```jsx
  // before
  { id: 'JB', flag: 'johor-flag.png', label: t('region.johor', lang) },
  // after
  { id: 'JB', flag: 'MY_Johor_flag.png', label: t('region.johor', lang) },
  ```

  ```jsx
  // renderer (extended)
  {(r.flag.endsWith('.png') || r.flag.endsWith('.svg'))
    ? <img src={r.flag} alt="" width="18" height="12" className="rounded-sm border border-tg-border/40 flex-shrink-0" />
    : <span aria-hidden>{r.flag}</span>}
  ```

  **Chat changes (`index.js` `locpick:` callback):**

  None. A draft of this PR experimented with sending the Johor
  flag PNG via `bot.sendPhoto` as the locpick:jb confirmation.
  Operator immediately pushed back — the 1:2 aspect rendered too
  tall in chat. Reverted. The locpick: callback is byte-identical
  to v0.61.203 (`safeSend` with the standard `loc.set.success`
  caption).

  - **`package.json`** — `0.61.203 → 0.61.204`.
  - **`doc/.serial-state.yml`** — commit 416 → 417, build 206 →
    207, journal 404 → 405; re-anchored 28-05 '26 10:15 SGT.

- **[VERIFICATION (sandbox)]**

  ```
  $ node --check index.js   (OK)
  ```
  Cuisine TMA Vite build runs in CI on PR push (sandbox harness
  queued the build async). The build only copies static assets;
  no code change beyond the existing region-toggle modification.

- **[STATUS]** PR-OPEN on push. Operator merges manually.

- **[TEST]** Operator (post-merge + Railway redeploy + Cuisine
  TMA cache bust):
  1. Cuisine TMA → tap the **JB** region pill — the icon is now
     the Johor state flag (red canton + crescent/star + blue
     field).
  2. Chat: `/location` → Quick-pick menu → tap "🇲🇾 Johor Bahru"
     → the bot replies with the Johor flag PHOTO + caption
     "📍 Location saved: Johor Bahru — searches capped to 30 km."
  3. Chat: `/location` → tap "🇲🇾 IOI City Mall" → plain text
     "Location saved" (SVG can't be sent as a photo). No
     regression.
  4. Other precincts (STB, SG regions): plain text reply as
     before.

- **[KNOWN GAPS]**
  - Putrajaya flag is SVG only — `bot.sendPhoto` won't accept
    SVG. To get the same photo-confirmation UX for IOI City
    Mall, we'd ship a PNG conversion (~50 KB at 256×170). Easy
    follow-up if the operator wants symmetric behaviour.
  - Telegram inline buttons fundamentally can't render custom
    images. If the operator's spec was actually "show the flag
    IN the button text", that's a Telegram-platform limitation
    we can't work around.
  - Old `johor-flag.png` is kept on disk to avoid breaking
    pre-v0.61.204 cached TMA bundles that still reference the
    old path. Once a few days have passed and caches turn over,
    a future PR can remove it.

- **Confirmation Gates**

  | Gate | Resolution | Date |
  |------|-----------|------|
  | G1 — version bump | PATCH `0.61.203 → 0.61.204`. Asset rename + minor UX additions; no API change. | 28-05 '26 |
  | G2 — destructive action | _n/a_ — both old asset paths kept (`johor-flag.png`, `putrajaya-flag.svg`) for backward compat. | 28-05 '26 |
  | G3 — new decision rule | YES — state-flag assets follow `<COUNTRY>_<State>_flag.<ext>` naming. Future flag assets should mirror. | 28-05 '26 |
  | G4 — paid external API call | _n/a_. | 28-05 '26 |

## Amendments

- **`web/cuisine/src/v2/App.jsx` region pill `flag` field for
  JB** — was: `'johor-flag.png'`. Now: `'MY_Johor_flag.png'`.
  Image renderer also accepts `.svg`.
- **`index.js` `locpick:` callback** — was: plain `safeSend`
  with the success caption. Now: `sendPhoto` for `jb` (Johor
  flag PNG); plain text for `ioi-resort-putrajaya` (SVG
  fallback) and all 15 other precincts.

## Deleted

_None._
