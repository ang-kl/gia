(№ 258 - 19-05 '26 23:59 SGT) v0.61.47

# Journal — soleat v0.61.46 → v0.61.47 (fixed high-contrast map-popup palette)

> Operator: *"The bus stop pin information background is making the text (especially
> hyperlink) cannot be readable … we should have a standard background regardless of
> light/dark mode and bright sunshine."*
>
> Sequenced after PR #535 — this entry is commit 258 / journal 251 / build 59.

## New

### [HDR] #251 | 23:59:00 SGT 19-05-26 | v0.61.47 — fixed high-contrast popup palette | ~1 kb | 4 files | 1 PR | 1 build |

- **[INTENT]** Make every map-pin popup legible regardless of the Telegram light/dark
  theme and outdoor glare.

- **[DELTA]**
  - **`mapOverlays.js`** (byte-identical ×3) —
    - `infoPalette()` no longer follows the Telegram `colorScheme`. It returns a
      single **fixed light scheme** — white card `#ffffff`, near-black text `#1c1c1f`,
      sub `#5a5a5a`, a strong underlined blue link `#1558d6`, good `#2e7d32`. A dark
      card washed out in bright sunshine and the hyperlink read poorly; a fixed
      high-contrast light card stays legible everywhere.
    - the standard `Google Map ↗` link (`gmapsLinkRow`) and the bus-stop popup's
      road-name link are now `font-weight:600` + `text-decoration:underline` so the
      hyperlink is unmistakable.
  - **`package.json`** — `0.61.46` → `0.61.47` (PATCH).

- **[VERIFICATION (sandbox)]**

  ```bash
  $ node --check web/transport/src/lib/mapOverlays.js  → OK
  $ npm run build:cuisine / build:hawker / build:transport  → all ✓ built
  $ npm test  → 65 files / 1928 tests green
  ```
  `mapOverlays.js` confirmed byte-identical ×3.

- **[STATUS]** DRAFT → PR (base `main`, branch `claude/review-gia4lunch-readme-3s798`).
  Deploys via the Railway auto-deploy on `main` once merged.

- **[TEST]** Open any map-pin popup (bus stop, clinic, hospital, police, attraction)
  in Telegram light AND dark mode — the card is always a white high-contrast panel
  with dark text and a clearly underlined blue hyperlink.

- **[KNOWN GAPS]** This reverses the v0.61.22 theme-aware popup card (operator
  decision D — fixed palette preferred). Sandbox-unverified visual change. The
  24 hours layer remains disabled; the `doc/Legal/` revision (journal #239) remains.

- **Confirmation Gates**

  | Gate | Resolution | Date |
  |------|-----------|------|
  | G1 — version bump | PATCH `0.61.46` → `0.61.47`, established 0.61.x cadence | 19-05 '26 |
  | G2 — destructive action | _n/a_ | — |
  | G3 — new decision rule | operator decision: popup card uses a fixed palette, not the Telegram theme (supersedes v0.61.22) | 19-05 '26 |
  | G4 — paid external API call | _n/a_ | — |

## Amendments

_None._

## Deleted

_None._
