(№ 65 - 01-05 '26 16:45 SGT)

# Register — soleat v0.30.7

> **Authenticity note (recipe AU-1):** Items below carry forward from `register-0_3_0-29_04_26-2125.md`. Closed items move to Completed; nothing is deleted.

## Open Items

| # | Item | Owner | Priority | Notes |
| :--- | :--- | :--- | :--- | :--- |
| 1 | Verify webhook mode is live on Railway after Service Settings → Networking → Generate Domain. | Human Lead | high | ✅ confirmed live at 22:55 SGT 29-04 with `RAILWAY_PUBLIC_DOMAIN=gia4lunch-production.up.railway.app`. Webhook log line observed. (Item kept in Open until v0.5.0 deploy logs also show `[Updates] Webhook registered`.) |
| 2 | Confirm `/eat` returns picks for the right meal at the right hour. | Human Lead | high | Try at three different SGT hours (e.g. 09:00, 13:00, 22:00) and verify period label. |
| 3 | Confirm Telegram menu button shows "🌿 Gia Map" and opens the TWA. | Human Lead | high | Requires webhook mode (Open #1 done first). |
| 4 | **Resolve `GOOGLE_MAPS_API_KEY` 403 on server-side Places calls.** Either (A) loosen the HTTP-referrer restriction (key works server-side again, but exposed to scrape) OR (B) deploy two-key split: keep referrer-restricted browser key for `/maps-key`, add new unrestricted server key for backend Places calls. | Human Lead | **CRITICAL** | Currently blocking — Railway log shows `[Vibe] Places fetch failed: 403`. Bot is in seed-fallback mode showing static listings (LeVeL33, Monti, etc.) without business_status data. Until resolved, all picks are stale and the v0.5.1 quality filter is dormant. |
| 5 | If `gemini-3-flash` 404s, set `GEMINI_MODEL=gemini-1.5-flash` in Railway Variables. | Human Lead | medium | One-line fix; no code change. |
| 6 | Phase 4 — GrabID OAuth 2.0 PKCE for live quietness signal. | Claude Code | medium | Replaces Gemini-from-reviews with real merchant occupancy. |
| 7 | Phase 4 — DryRoute B1/B2 linkway routing. | Claude Code | low | Persona feature; rainy-day path. |
| 8 | Phase 4 — Personal Vault sync to Google Drive. | Claude Code | low | Zero-Server identity store. |
| 9 | Phase 4 — Telegram Cloud Storage Favorites (replaces 30-min Redis location cache for proper Zero-Server compliance). | Claude Code | medium | Closes the v0.4.0 Mandate breach. |
| 10 | ~~Folder templates (`Builder.md`, `Persona.md`, etc.) not present in repo.~~ | — | — | **CLOSED 01-05 '26.** All 8 templates created in v0.28.1 (PR per the rollout). |
| 11 | Register a Cloud Map ID. | Human Lead | low | v0.12.1 made this env-driven. Cloud Console → Map Management → Create Map ID (Vector, JS) → set Railway env var `MAP_ID=<id>`. No code change needed. |
| 12 | **Tranche B of `02_Expand.MD` — Redis Vault (200+ curated locations).** | Human Lead | medium | Spec §5 references "curated 200+ locations (CSV/KML)". Source file not provided. Paste the data or upload to repo (e.g. `data/vault.csv`) and Claude will write the import script + Redis loader + Vault-first lookup in `pickValidated`. |
| 13 | **Tranche B — `/BusArrivalv2`.** | Human Lead | medium | LTA endpoint takes a BusStopCode. Need a UX product call: (a) bot uses cached `/eat` location → reverse-lookup nearest stop, (b) user types stop code, or (c) inline keyboard from a list of "your usual stops". Pick one path. |
| 14 | **Tranche B — `getTransitContext()` and "1 MRT stop away" expansion.** | Human Lead | medium | Needs MRT station coordinates dataset. Either pull `/MRTStations` from LTA at boot and cache (~135 stations), or hardcode the CBD subset. Confirm preference. |
| 15 | ~~Clarify "Decision Matrix API" intent.~~ | — | — | **CLOSED 30-04 '26.** Human Lead confirmed Google Distance Matrix; shipped as v0.8.0 via Routes API `computeRouteMatrix` walking-minute re-rank. |
| 16 | ~~Vault data file mismatch (Takeout audit).~~ | — | — | **CLOSED 30-04 '26.** Superseded by Architectural Spec — Human Lead provided 14 Google Maps short URLs directly; v0.9.0 ships `sync-vault.js` to import them. |
| 17 | **Run vault sync once.** v0.28.2 added the easier route: set `ADMIN_SYNC_SECRET=<random>` in Railway env, redeploy, then `curl "https://<webhookDomain>/admin/sync-vault?secret=<random>"`. Returns `{imported, skippedClosed, fenced, failed, vaultCount, samples, durationMs}` JSON. Pre-v0.28.2 path (`railway run node sync-vault.js`) still works as a CLI fallback. | Human Lead | high | One-shot. Paste back the JSON response + Railway log lines so we can audit which URLs landed and which dropped to `gia:vault:cleanup`. |

## Deferred

| # | Item | Reason | Reactivate when |
| :--- | :--- | :--- | :--- |
| 1 | Live "Popular Times" / quietness signal via Google Places. | Not exposed by official API; scraping violates ToS. **Partially mitigated by Gemini-from-reviews "Sanctuary read."** Full mitigation via GrabID per Open item 6. | Open #6 ships. |
| 2 | LTA bus arrival times (`/BusArrivalv2`). | Phase 1 limited to MRT alerts. | When `/status` evolves into a multi-mode CBD pulse. |
| 3 | RSS Vibe Sensor (SethLui, Honeycombers). | Filter quality inadequate. | Only if Google Places fails permanently. |
| 4 | Inline mode (`@gia4lunch_bot raffles`). | Not in immediate scope; Mini App now covers cross-chat sharing via deep links. | Demand observed. |
| 5 | Scheduled "11:45 AM Pulse" daily nudge. | Persona feature; deferred until user list grows. | User base reaches 5+. |

## Decisions

| # | Decision | Rationale | Date |
| :--- | :--- | :--- | :--- |
| 1 | LTA `LTA_ACCOUNT_KEY` is optional in Phase 1. | Lets Telegram + Redis verification proceed without DataMall registration. | 29-04 '26 |
| 2 | Switch from RSS to Google Places for venue discovery. | RSS returned lifestyle articles. | 29-04 '26 |
| 3 | Switch from long-polling to webhooks. | Recurring 409 Conflict. | 29-04 '26 |
| 4 | gia4lunch §6.3 rules subset = R002, R003, R004, R005 (extended), R010. | Project A lineage rules don't apply to Node/Telegram/Railway. | 29-04 '26 |
| 5 | Time anchor = `user_stated_device_required_due_to_runtime_constraint`. | Sandbox blocks live sovereign fetch. | 29-04 '26 |
| 6 | Vibe summary lazy on `/lunch`, 7-day cache. | Cheaper at low traffic. | 29-04 '26 |
| 7 | Gemini model overridable via `GEMINI_MODEL`. | Safe default + one-line escape hatch. | 29-04 '26 |
| 8 | **Pure-Gemini-then-Places validation for `/eat`.** Gemini suggests names; Places validates within 200 m. | Avoids hallucination while preserving Gemini taste. Per Human Lead choice. | 29-04 '26 |
| 9 | **Reuse `GOOGLE_MAPS_API_KEY` for the TWA frontend.** | Per Human Lead choice ("Reuse existing key — insecure"). Mitigation: HTTP-Referrer restriction in Cloud Console (Open #4). | 29-04 '26 |
| 10 | **Bot registers menu button on boot via `setChatMenuButton({type:'web_app'})`.** | Per Human Lead choice ("Bot registers it on boot"). Auto-updates when domain changes. | 29-04 '26 |
| 11 | **Ship `/eat` chat fallback alongside TWA.** Drop `/lunch` entirely. | Per Human Lead choice ("Yes, keep /eat as fallback"). | 29-04 '26 |
| 12 | **Accept Zero-Server Mandate transient breach for 30-min hashed location cache.** Mitigation: SHA-256 hashed chat ID, 30-min TTL, no append/history. | Per Human Lead choice ("Cache in Redis 30 min"). One-line rollback documented. | 29-04 '26 |
| 13 | **Pass `google_place_id` + `google_place_type` to `bot.sendVenue`** and build TWA InfoWindow links from `place_id` rather than `googleMapsUri`. | Telegram's coords-only fallback URL was opening Google Maps at raw lat/lng instead of the Place page. Reported by Human Lead with example URL. | 29-04 '26 |
| 14 | **TWA InfoWindow "Open in Maps" uses Google's documented Search-action URL Schema** `…/maps/search/?api=1&query=<name>&query_place_id=<id>` and `tg.openLink()` for external dispatch. | iPadOS Universal Links with both Google Maps + Apple Maps installed routed v0.4.1's `?q=place_id:` pattern to Apple Maps, which doesn't recognise `place_id` and degraded to coords. Documented schema with `query=<name>` keeps the name visible regardless of which Maps app handles the link. | 29-04 '26 |
| 15 | **iOS deep-link probe + install fallback for "Open in Google Maps".** Try `comgooglemaps://` URL scheme; if `visibilitychange` doesn't fire within 1500 ms the app is not installed → prompt user via `tg.showConfirm` to install from App Store or fall back to documented HTTPS URL. | Per Human Lead requirement "Google map must be installed, detect if not." Universal Links can still route HTTPS to Apple Maps; URL scheme bypasses that and reaches Google Maps app deterministically when installed. | 29-04 '26 |
| 16 | **AdvancedMarkerElement with HTML pins** showing venue name visibly + `gestureHandling: 'greedy'` + inline keyboard under each Sanctuary read with **📍 Open Map** (web_app, single-pin focus via `?placeId`) and **🚗 Directions** (Google Directions URL Schema). | Per Human Lead "Yes, all 3" approval of the strategy doc trio. Solves "anonymous pin" UX, two-finger gesture frustration, and the missing per-pick navigation affordance in one minor bump. | 30-04 '26 |
| 17 | **Quality filter — drop venues with `businessStatus !== 'OPERATIONAL'` or `currentOpeningHours.openNow === false`.** Applied in both `vibe.js fetchPlaces` (cached listings) and `vibe-suggest.js validateWithPlaces` (per-pick validation). | Per Human Lead's §103.12 "Safety Filter" instruction. Prevents permanently-closed and currently-closed venues from surfacing in `/eat` once Places API is reachable again. | 30-04 '26 |
| 18 | **Tranche A of `02_Expand.MD`:** brand rename Gia4lunch → soleat (Gia persona kept as in-message voice); new `/drink` and `/groceries`; Topic Gatekeeper for free-text via Gemini structured-JSON + 60 s SHA-256 cache; walking radius 300 m; last-30-day review filter in `vibe-summary.js`. | Per Human Lead "yes Tranche A" + (2) yes for the G4 gate on the additional Gemini call. Ships the unblocked subset of `instruction/02_Expand.MD`; Tranche B (Vault, /BusArrivalv2, MRT-stop expansion) deferred pending data inputs. | 30-04 '26 |
| 19 | **Three-path location entry on `/eat /drink /groceries` + radius 200 m.** Tap 📍 to share, or **type a place name** (geocoded via Places `searchText`), or ⛔ Raffles Place default. Free-text handler consumes pending sanctuary flow before Gatekeeper. | Per Human Lead instruction: "Funciton, include share current, default to Raffles, or enter" + revised wording "search within 200 metres in radius". | 30-04 '26 |
| 20 | **Walking-minute re-rank via Google Routes API `computeRouteMatrix`.** Picks for `/eat /drink /groceries` are sorted ascending by actual walking duration from origin. Header line shows "X min walk" alongside rating + open-now. | Per Human Lead "Ship distance matrix". Closes confirmation Open #15. ~$0.015/invocation; documented for cost monitoring. | 30-04 '26 |
| 21 | **Fail-fast on `pickValidated` (interpretation A).** First Gemini candidate is the gate; if it doesn't validate within 200 m, return `{venues: []}` immediately and let `handleNoResults` surface the new copy. Seed-fallback re-poll path removed. | Per Human Lead instruction "refactor the fetchLocations logic to include a maximum retry limit of 1. If no places are found after the first search, trigger the handleNoResults function instead of re-polling" → choice **A**. | 30-04 '26 |
| 22 | **Redis Vault** — `gia:vault` (geo) + `gia:place:<id>` (hash) + `gia:vault:cleanup` (audit list). `sync-vault.js` one-shot importer with concurrency 5 and 429/503 backoff. `runFlow` consults Vault first for `/eat` and `/drink` (300 m radius, live `open_now` verify), falls through to `pickValidated` if <3 open hits. `/groceries` skips Vault. | Per Human Lead Architectural Spec: Input Resolution → Operational Gate → Schema → Priority Routing → Performance Requirement. | 30-04 '26 |
| 23 | **Processing lock + Hidden Sanctuary consultant + review-keyword gate + TMA spinner/onerror swallow.** `runFlow` wrapped in 60 s Redis processing flag. `pickValidated` empty → `findHiddenSanctuary` (broad searchNearby + Gemini-on-reviews) before `handleNoResults`. `validateWithPlaces` final gate now includes negative-keyword review screen. TMA `#status` overlay has spinner; `window.error` + `unhandledrejection` swallow with friendly fallback. | Per Human Lead Architectural Spec ("stabilize the iPad interface") + "Yes, iPadOS loop". §1 substeps `tg.close()` and `answerCallbackQuery` skipped (don't apply to our keyboard types). | 30-04 '26 |
| 24 | **`bot.on('location', …)` immediate ack reorder.** The user-facing "📍 Got it — looking for X within 200m…" message now lands as the first reply after pending-state confirmation, before `setUserLocation` and any expensive call. Stops perceived iPadOS timeout. | Per Human Lead Architectural Spec §1 ("Immediate Ack"). §2 (personal-tag steer-back) explicitly skipped — paternalistic + no data + unclear UX value. §3 (Vault first) and §4 (`tg.expand()`) already shipped in v0.9.0 and v0.4.0 respectively. | 30-04 '26 |
| 25 | **Radial expansion (200 → 500 → 1000 m) + ack copy refresh + sharper Hidden Sanctuary keyword prompt + tea_house in afternoon scope + Vault 50 m fuzzy buffer + cheerio HTML parser in sync-vault.** | Per Human Lead's latest Architectural Spec. **Supersedes v0.8.1 fail-fast (interpretation A)**: prior choice "max retry 1, no re-polling" is reversed; latest instruction wins. AU-2 trail in Feature Removed #10 and Technical Deprecated #15. | 30-04 '26 |
| 26 | **`/ver` command — version + upstream API health probes.** New `ver.js` module runs six parallel probes (Telegram, Redis, Google Places, Routes API, Gemini, LTA DataMall) with 5 s timeouts. Replies as `<pre>` HTML block with 🟢/🔴 icons + latency + note. | Per Human Lead "Create a command /ver to check version of telegram TMA and whether API is alive to Google, AI, telegram etc". | 30-04 '26 |
| 27 | **TWA Map ID env-driven.** `/maps-key` route returns `{key, mapId}`; `public/app.js` overrides default placeholder when env `MAP_ID` is set. | Per Human Lead "How to do?" question on Open #11; makes Cloud Map ID adoption a Railway-Variable-only operation. | 30-04 '26 |
| 28 | **Universal ack copy + Location Validation Gate + radial expansion 2000 m ceiling.** All entry points (location share, default button, typed place, cached-location fast path) reply with `'🌿 Sensing the vibe…'`. Any location-handler failure restores pending state and prompts manual-input fallback. `pickValidated` retries first candidate at `[200, 500, 1000, 2000]` m. | Per Human Lead Architectural Spec ("device-agnostic and resilient to multi-map app handshake errors"). §3 typed text input + §4 intent lock-in already shipped v0.7.0; reconfirmed and consolidated. | 30-04 '26 |
| 29 | **Default Gemini model → `gemini-3-pro` across all five modules** + dual env-var support (`GEMINI_API_KEY \|\| GOOGLE_API_KEY`) + Hidden Sanctuary tightened to last 3 reviews + `afternoon` keyword + building-level navigation cue extraction (4th `Approach:` bullet in Sanctuary read; `approach` line in Hidden Sanctuary chat reply). | Per Human Lead Architectural Spec §130. §130.8 "Reformed-Theological / Christ-modeled tone" interpreted as secular virtues (polite/helpful/grounded) — explicit doctrinal content NOT embedded in prompts; rationale: religiously diverse Singapore audience. Logged in journal per AU-2. | 30-04 '26 |
| 30 | **Slash-command handlers must be one-line delegates to `run*` functions; no inline command bodies.** Applies to `/eat`, `/drink`, `/grocery`, `/cuisine`, `/weather`, `/transport`, `/carpark`, `/ver`. Single source of truth shared with TMA `web_app_data` tile routing via `routeMenuCommand`. | Per Human Lead screenshot showing `/weather` returning v0.18.0 format on a v0.20.0 deploy. Root cause was three drifted slash handlers (`/weather`, `/transport`, `/carpark`) plus a stale standalone `/status` from v0.1.x. Patch policy locked in v0.20.1 to prevent recurrence. (`/status` previously listed in this row was removed entirely in v0.21.0 — see Decision #31 and Removed Features #1.) | 30-04 '26 |
| 31 | **`/status` removed; `/transport` is the single transport surface.** Slash handler, `routeMenuCommand` case, `/start` welcome line, and `setMyCommands` registration all dropped in one patch. Internal Redis key `lta:train_status` retained — it is the LTA train alert cache, name overlap is incidental. | Per Human Lead "remove status…" after observing the v0.20.1 alias still showed the legacy `Gia CBD Pulse` reply on the unupdated Railway deploy. G2 destructive-action gate satisfied by explicit instruction. | 30-04 '26 |
| 32 | **`/transport` adds live traffic incidents.** New `transport.fetchTrafficIncidents()` calls LTA `/TrafficIncidents`; new `transport.nearestIncidents(list, lat, lng, radiusM=5000, count=3)` Haversine filter + sort. Reply gains `🚦 Traffic …` block between Network crowd and Nearest MRT stations, listing top 3 nearest with type + distance + LTA's pre-formatted message. Falls through to "no live incidents reported" or "N island-wide; none within 5 km" when appropriate. | Per Human Lead "expand the transport to include traffic, bus, mrt"; bus + MRT shipped in v0.19.0/v0.20.0, this closes the road-network pillar. ~1 LTA call/invocation, within throttle. | 30-04 '26 |
| 33 | **`/ver` reports deploy time and uptime.** `ver.js` captures `BOOT_MS = Date.now() - process.uptime() * 1000` at module load (Node start = Railway redeploy), renders trailing line `Deployed: 30/04/26 14:55 SGT (uptime 12m)` after the seven probe rows. | Per Human Lead "/ver returns 0.20.0… include time deploy at the end" — provides at-a-glance confirmation that a Railway redeploy actually swapped the running build. | 30-04 '26 |
| 34 | **Adopt Vite + React 18 + Tailwind for new TMAs.** New surface area lives under `web/<page>/` with build output emitted to `public/<page>/` (gitignored). Root `package.json` has a `build` script + `postinstall` hook so Railway runs `vite build` automatically on every deploy. Existing TMAs (`menu`, `app`) stay vanilla until proven worth migrating. | Per Human Lead "Add a Vite build pipeline" answer in plan-mode. Trade-off: ~30 s extra deploy time + ~150 kB initial TMA payload, in exchange for proper React component tree, multi-state controls, and mobile-first Tailwind. | 30-04 '26 |
| 35 | **`/cuisine` is a TMA-only command (no chat keyboard fallback).** Slash command sends a single `🍴 Open Cuisine Picker` web_app button. Picks come back via TMA `sendData({cmd:'cuisine-pick', placeId, name})` round-trip; the bot delivers a Sanctuary read for the tapped venue. Telegram clients without WebApp support cannot use `/cuisine`. | Per Human Lead "Replace with a deep-link to the TMA only" answer. Older clients are vanishingly rare on Telegram given the WebApp API has been GA since 2022. | 30-04 '26 |
| 36 | **Cuisine search returns 15 candidates** sorted (open_now first → walking minutes asc → rating desc). UI shows 5 by default with a "Show next N" affordance. Per Human Lead spec: "search results should be 15 default sorted by distance and highest rating and open now first but shows the first 5 and with option for next ten." | This is the headline UX shape of v0.22.0. | 30-04 '26 |
| 37 | **Four preset combos with concrete logic gates** (Transit Efficiency, After-Hours, Holiday/Special, Cuisine Discovery). Each pre-fills the controls and shapes the Gemini prompt + post-validate filter. After-Hours forces `meal=supper` regardless of clock; Holiday/Special enriches with current PH (or *next* PH if not today); Cuisine Discovery requires ≥1 cuisine chip. | Per Human Lead "four functional presets (Combos)" spec line. | 30-04 '26 |
| 38 | **LLM "recipe" surface = `signature_dish` per candidate.** Gemini schema for the cuisine TMA includes `{name, area, vibe, signature_dish}`. UI renders `🍴 try the <dish>` on each card. | Per Human Lead "LLM result should be proactive and based on recipe" — interpreted as actionable order suggestions (not whole recipes), so users see what to actually eat at each venue. | 30-04 '26 |
| 39 | **SG public-holiday dataset shipped** as `holidays.js`. Source: `api-open.data.gov.sg` PH dataset cached in Redis 90 days; inline 2026 fallback array (10 dates) is the offline floor. Boot warm-cache via `holidays.warmCache(redis)`. | Per Human Lead "Build the full PH dataset now" answer in plan-mode. Replaces the deferred "Phase 2 PH integration" gap from earlier holiday-special preset thinking. | 30-04 '26 |
| 40 | **70-cuisine catalogue verbatim from Human Lead.** 8 categories (Common Here / Southeast Asian / China (Regional) / South Asian Specialists / European / Middle Eastern & Central Asian / Americas / African). No relocation despite Brazilian/Australian/NZ/Cambodian/Burmese sitting under "Common Here" — kept where the Human Lead placed them. | Per Human Lead "follow my list". | 30-04 '26 |
| 41 | **Cuisine selection cap = 5.** Hard limit enforced in the reducer; over-cap chips dim with explanatory hint. | Per Claude proposal accepted in plan-mode ("OK with 5"). Keeps Gemini prompts tight and the "selected" pill row legible. | 30-04 '26 |
| 42 | **Range sliders replace toggles for radius/recency/queue.** Radius 200 m–5 km in 100 m steps; Recency 5 d–6 mo in 5-day steps; Queue 5–60 min in 5-min steps. Telegram theme accent on slider thumb. | Per Human Lead "use range bar" + "default 15 minutes but can be shorter than 5 minutes to 60 minutes". | 30-04 '26 |
| 43 | **Free-text "Another cuisine" input,** comma-tolerant; appended to cuisines array unsanitised. | Per Human Lead "Another cuisine choice at the bottom should build the search prompt as user select." | 30-04 '26 |
| 44 | **Queue estimate is Gemini-knowledge, labelled `~N min (est)`.** No real-time queue feed exists for SG; we don't pretend otherwise. The slider acts as a *cap* (drop venues whose Gemini estimate exceeds it); null estimates pass through. | Per Human Lead acceptance of "ship with estimated label". Honest framing > false precision. | 30-04 '26 |
| 45 | **Live prompt preview pane** below presets, collapsible. Mirrors `cuisine-search.js#buildPrompt` shape so users can see what's about to be sent. | Per Human Lead "build the search prompt as user select". Also doubles as a debugging surface for the dev. | 30-04 '26 |
| 46 | **`/surprise` is single-venue.** Returns ONE Markdown card, not a list. Inline buttons: 🚗 Directions, 🔍 Google. Markdown falls back to plain text on stray-char errors. | A "surprise" loses its character if it's a list of 5. The user's spec asked for single-card semantics ("highlight 1–3 special dishes"). | 30-04 '26 |
| 47 | **`/surprise` annulus geometry = 1.5 km–3 km.** Implemented via 3 km Places Nearby Search + post-filter haversine. Centred-circle approach is ~5× cheaper than 6 overlapping circles around the annulus mid-radius. | Default chosen in plan-mode after ambiguity in the original spec. | 30-04 '26 |
| 48 | **`/surprise` price-band heuristic = `priceLevel ≤ MODERATE`.** Places API doesn't expose absolute SGD; this is the closest available proxy for "$10–$30 entrée". Documented as a known imperfect signal. | Per Human Lead acceptance: ship the feature with the closest available proxy rather than block on a perfect price feed that doesn't exist. | 30-04 '26 |
| 49 | **Buddy Level 1 = share-link round-trip via t.me deep link.** No direct DM-between-users. Originating user taps 👋 → bot replies with `t.me/<bot>?start=share_<token>` link the user can paste into any messenger. Buddy taps the link → /start share_<token> → bot renders the same Sanctuary read for them. | Per Human Lead "Lastly, buddy feature is it possible?" + Claude proposal accepted in plan-mode. Avoids requiring a username registry on the bot side; works even if buddy hasn't /start'd before. | 30-04 '26 |
| 50 | **Share tokens = 7-day TTL, reusable within window, no PII.** Payload contains only the trimmed venue record (no originating user identity). Acceptable abuse surface for Phase 1 (no doxxing, no spam multiplier — buddy still has to actively tap). | Phase-1 scope explicitly defined to keep the safety surface bounded. Phase 2 (live solo-dining match) deferred. | 30-04 '26 |
| 51 | **Buddy Level 2 (live solo-dining match) is deferred.** Reason: needs explicit safety/abuse policy (women-only opt-in flag, mutual confirmation, escape hatch, moderation pipeline). | Per Human Lead acceptance — Level 2 is "weeks of work + a moderation policy" and out of scope for the current rollout. | 30-04 '26 |
| 52 | **`/cuisine` is the default chat-menu surface.** Menu button label changed from "🌿 soleat Menu" → "🍴 Cuisine Picker", URL changed from `/app/menu` → `/app/cuisine`. Slash autocomplete leads with `/cuisine`. | Per Human Lead "move /cuisine as default when loaded". The tile-grid `/app/menu` page still exists for muscle-memory navigation. | 30-04 '26 |
| 53 | **`/eat` removed from discovery surfaces only.** Dropped from `setMyCommands` and from `public/menu.html` tile grid. The `bot.onText(/^\/eat…)` handler is intentionally retained so users with muscle memory still get a response, just no longer surfaced as a recommendation. | Per Human Lead "you should remove /eat" interpreted as "hide from menu/autocomplete, don't break old links". | 30-04 '26 |
| 54 | **TMA Search has end-to-end diagnostic codes.** Client-side `D100…D900` log every step (mount → geo → initData → POST → HTTP → parse → venues), mirrored to console with `[Cuisine-Diag]` prefix. Server-side `D700…D703` tag every `/api/cuisine-search` request with elapsed-ms and outcome. UI Diagnostics panel auto-opens on first failure. | Per Human Lead "search in the TMA isn't working, create diagnostic codes". Codes are stable and grep-able across both surfaces so a screenshot of the TMA panel can be matched directly to Railway log lines. | 30-04 '26 |
| 55 | **Vault scope = aggregator across all venue-knowledge Redis caches** (`gia:vault` geo set + `gia:place:<id>` hashes + `vibe-summary:<id>` + new `place-reviews:<id>` 24 h cache). 30-venue × 5-review snapshot is loaded into Gemini's Reason prompt verbatim so cost estimates and dish recommendations are grounded in cached review text instead of training-data hallucinations. | Per Human Lead "(b)" answer: "Treat gia:vault as a new aggregated index of every prior Sanctuary read + Vibe summary + Place metadata cached in Redis." | 30-04 '26 |
| 56 | **Per-cluster contextual fetch via 500 m grid.** Pipeline groups picks by `floor(lat/Δlat):floor(lng/Δlng)` cells (Δlat = gridM/111320, Δlng scaled by `cos(lat)`). Each cluster runs ONE `weather.summary` + `transport.fetchTrafficIncidents` + `carpark.nearest` Promise.allSettled. Refine sees the cluster summary per venue. | Per Human Lead "per-cluster contextual fetch" answer. ~5× cheaper than per-venue context for typical 15-pick searches. | 30-04 '26 |
| 57 | **PIPELINE_ENABLED env flag (default true).** Pipeline can be killed instantly by setting `PIPELINE_ENABLED=false` in Railway, reverting `/cuisine` and `/surprise` to v0.25.1 behaviour exactly. Used as the safety valve while the new layer beds in. | Architectural caution: a brand-new middleware that calls Gemini twice per request needs an off-switch reachable without a redeploy. | 30-04 '26 |
| 58 | **Refine layer adds ~1 Gemini call (~1.5–3 s) to every `/cuisine` and `/surprise`.** Total `/cuisine` latency: ~6 s → ~9–11 s. `/surprise`: ~7 s → ~9 s. Cost delta: +~$0.002/call. Trade accepted as the cost of "operationally sound" per Human Lead spec. | The Reason–Fetch–Refine sequence is explicitly two-phase Gemini by spec; latency cost is intrinsic. | 30-04 '26 |
| 59 | **TMA↔backend bridge has a pre-flight ping** at `GET /api/diag/cuisine` (auth-free, exposes `{version, pipelineEnabled, envPresent.*, vaultIndexLoaded, webhookDomain, timestamp}`). The TMA pings on mount, paints a green/red connectivity badge under the Header, and records D050–D053 codes. | Per Human Lead bridge audit. Ensures the user sees "online vN" *before* they tap Search, not delayed-failure after. | 01-05 '26 |
| 60 | **Permissive CORS on `/api/*`** (wildcard origin). Safe because auth flows via the `X-Telegram-Init-Data` header — no cookies, no credentials. If we ever introduce cookie-based auth, this MUST be tightened to a specific origin allowlist. | Pre-emptive defense for future preview deploys hosted on different origins. | 01-05 '26 |
| 61 | **Backend chat receipt** — `POST /api/cuisine-search` fires `safeSend(tgUserId, '🌿 Sensing the vibe…')` on entry, fire-and-forget, throttled by `isProcessing` flag and cleared in `finally`. | Per Human Lead "implement a Visual Feedback Loop … the very first action after receiving the data is to send the 'Sensing the vibe...' status message back to the Telegram chat." | 01-05 '26 |
| 62 | **Single 📍 Google Maps button per Sanctuary card.** Every per-pick action row across `deliverPicks` / `deliverSurprise` / TMA `VenueCard` is reduced to one outbound link, defaulting to the Google Maps **place page** (not directions). URL-resolution waterfall: `venue.url` → `https://www.google.com/maps/place/?q=place_id:<pid>` → `directionsUri`. | Per Human Lead "default should be google map, and listed only 1 link instead of 3". Trade: directions now require one extra tap inside Google Maps; place page is the more general affordance (photos, reviews, hours, directions all reachable from there). | 01-05 '26 |
| 63 | **Dual-channel TMA bridge (HTTPS primary, sendData fallback).** Telegram WebApp `sendData()` works only from KeyboardButton / chat-menu launches — fetch+initData works in every launch context but can be silently blocked by some in-app webviews. v0.26.3 always tries fetch first; on any HTTP failure (network / 4xx / 5xx) the front-end retries via `sendData({cmd:'cuisine-search', ...payload})`, which the bot routes through `web_app_data` to the same `searchCuisine` pipeline and delivers picks to the chat. Two channels = redundancy. | Per Human Lead "verify the launch context of the TMA … if KeyboardButton, use sendData; if inline button, use POST /api/search". Implemented as adaptive dual-channel rather than launch-context guessing because Telegram's API doesn't expose the launch context cleanly. | 01-05 '26 |
| 64 | **Full simulation logging across the bridge.** Every step of the Search flow emits a stable D-code prefixed `[Cuisine-Diag]` to both browser console (TMA) and Railway server log: D050–D053 (pre-flight bridge ping), D060 (launch context), D061–D063 (sendData fallback), D100–D502 (front-end Search flow), D601–D640 (pipeline), D700–D704 (HTTPS handler), D710 (diag endpoint), D720–D723 (sendData fallback handler), D730–D732 (web_app_data dispatcher). Codes are stable and grep-able; a TMA screenshot can be matched directly to Railway log lines. | Per Human Lead "every step of this process … is logged in the Railway console for simulation and debugging". | 01-05 '26 |
| 65 | **Search never hard-blocks on missing location.** When `state.loc` is null at Search time, the TMA proceeds with a CBD-centre default (`{lat: 1.2839, lng: 103.8517}`) and surfaces this in Diagnostics as `D202 Geolocation unavailable — proceeding with CBD default`. The earlier "Tap 📍 first" alert was invisible in some Telegram clients, leaving the user with no feedback at all. | Per Human Lead "I still cannot fire the prompt in TMA" — root-cause analysis identified a silent early-return as the most common failure mode, not the post-tap dual-channel logic. | 01-05 '26 |
| 66 | **Search button paints synchronous feedback before any async work.** First line of `onSearch` is `setBannerText('🌿 Sensing the vibe…')`, rendered as a high-contrast `tg-accent`-bordered banner above the bottom button row. Tap counter `🔍 Search · N` appears on the button label so the user has unambiguous proof their click registered, even if everything downstream fails silently. | Same root cause as #65 — visual feedback was async-gated; now it's synchronous. | 01-05 '26 |
| 67 | **6-second fetch timeout via AbortController.** `searchCuisine(payload, {timeoutMs: 6000})` aborts the HTTPS path after 6 s and returns `{timedOut: true}` so the dual-channel fallback triggers. Reason+Refine pipeline typically takes 5–11 s end-to-end; the 6 s bound deliberately favours the fallback path on slow links since chat delivery succeeds even when in-TMA fetch hangs. | Per Human Lead "I still cannot fire the prompt" — long-tail latency was a previously-untouched failure mode. | 01-05 '26 |
| 68 | **`💬 Chat` escape-hatch button** rendered next to Search, less prominent (tg-card colour vs accent). One-tap bypass that calls `sendData()` directly without trying fetch. For users on hostile in-app webviews (corporate MDM, content-filter proxy, older Android Telegram). | Pragmatic last-resort surface so even when both auto-fallback heuristics misfire, the user has a manual override. | 01-05 '26 |
| 69 | **Map TMA (`/app/map`) intentionally NOT migrated to React/Vite.** v0.28.0 migrated the menu but kept the map vanilla. Rationale: (a) the existing `public/app.js` is mature working code with no known bugs; (b) Google Maps integration is fundamentally imperative DOM (markers, AdvancedMarkerElement, map instance), so the React tree is mostly a wrapper around `useEffect(() => new google.maps.Map(...))` — no real composition benefit; (c) pulling in `@vis.gl/react-google-maps` or similar adds ~80 KB gzip to the bundle for a TMA used a fraction as often as `/cuisine`. Migration remains opt-in if the cost-benefit ever flips (e.g. we want shared theme tokens, shared Sanctuary-card components between map and TMA). | Honest scope decision documented for future selves who'll wonder why menu was migrated and map wasn't. | 01-05 '26 |

## Completed

| # | Item | Closed in | Date |
| :--- | :--- | :--- | :--- |
| 1 | Phase 1 — LTA Pulse `/status` end-to-end. | PR #1 | 29-04 '26 |
| 2 | Phase 2.5 — Google Places `/lunch` end-to-end. | PR #6 | 29-04 '26 |
| 3 | Phase 2.6 — Webhook switch + venue cards + README refresh. | PR #7 | 29-04 '26 |
| 4 | Orchestrator bootstrap. | PR #8 | 29-04 '26 |
| 5 | Phase 3a — Gemini Sanctuary Summary. | PR #9 | 29-04 '26 |
| 6 | Phase 3b + 3c — `/eat` (time-aware, location-aware) + Telegram Web App. | PR #10 (squashed `1c50f96`) | 29-04 '26 |
| 7 | v0.4.1 patch — venue-card and TWA-marker Google Maps links use Place ID, not raw coords. | PR #11 (squashed `408d386`) | 29-04 '26 |
| 8 | v0.4.2 patch — TWA InfoWindow Maps link upgraded to documented Search-action URL Schema; resolves iPadOS Universal-Link → Apple Maps routing that was still showing coords. | PR #12 (squashed `1c8ca21`) | 29-04 '26 |
| 9 | v0.4.3 patch — iOS Google Maps app deep-link probe (`comgooglemaps://`) with App Store install prompt fallback. | PR #13 (squashed `1cded99`) | 29-04 '26 |
| 10 | v0.5.0 minor — AdvancedMarkerElement name-on-pin + greedy gestures + chat inline keyboard (Open Map + Directions). | PR #14 (squashed `860889e`) | 30-04 '26 |
| 11 | v0.5.1 patch — quality filter (`businessStatus`, `openNow`) drops closed/non-operational venues from live picks. | PR #15 (squashed `cca25ad`) | 30-04 '26 |
| 12 | v0.6.0 minor — Tranche A of `02_Expand.MD` (brand → soleat, /drink, /groceries, Topic Gatekeeper, 300 m walk, 30-day review filter). | PR #16 (squashed `78af562`) | 30-04 '26 |
| 13 | v0.7.0 minor — three-path location entry (tap-share / type-place geocoding / Raffles default), radius 200 m, prompt copy refresh. | PR #17 (squashed `1c09d2e`) | 30-04 '26 |
| 14 | v0.8.0 minor — Routes API `computeRouteMatrix` walking-minute re-rank + chat header annotation. | PR #18 (squashed `4a6dc6a`) | 30-04 '26 |
| 15 | v0.8.1 patch — fail-fast `pickValidated` (interpretation A); seed-fallback removed; `handleNoResults` helper extracted. | PR #19 (squashed `26fad04`) | 30-04 '26 |
| 16 | v0.9.0 minor — Redis Vault: `vault.js` runtime, `sync-vault.js` importer, `/eat`+`/drink` Vault-first decision tree. | PR #20 (squashed `25b2852`) | 30-04 '26 |
| 17 | v0.10.0 minor — processing lock + Hidden Sanctuary consultant + review-keyword gate + TMA spinner/onerror swallow. | PR #21 (squashed `292ed9d`) | 30-04 '26 |
| 18 | v0.10.1 patch — `bot.on('location')` immediate-ack reorder; first reply lands before any expensive call. | PR #22 (squashed `1113dcc`) | 30-04 '26 |
| 19 | v0.11.0 minor — radial expansion + sharper keyword prompt + tea_house scope + Vault 50 m buffer + cheerio HTML importer; supersedes v0.8.1 fail-fast. | PR #23 (squashed `acbcd22`) | 30-04 '26 |
| 20 | v0.12.0 minor — `/ver` command (Telegram + Redis + Google Places + Routes + Gemini + LTA health probes). | PR #24 (squashed `3e166ff`) | 30-04 '26 |
| 21 | v0.12.1 patch — TWA Map ID env-driven via `/maps-key` payload. | PR #25 (squashed `a4c3183`) | 30-04 '26 |
| 22 | v0.13.0 minor — universal ack copy + Location Validation Gate + radial expansion ceiling 2000 m. | PR #26 (squashed `852d885`) | 30-04 '26 |
| 23 | v0.14.0 minor — Gemini 3 Pro default + GOOGLE_API_KEY support + 3-review Hidden Sanctuary + afternoon keyword + building-level approach cues. | PR #27 (squashed `40d5603`) | 30-04 '26 |
| 24 | v0.14.1 patch — code default model `'gemini-3-pro'` → `'gemini-2.5-pro'`; `gemini-3-pro` not yet a public ID. | PR #28 (squashed `0503c1d`) | 30-04 '26 |
| 25 | v0.15.0 minor — Vault import: JSON + KMZ parsers (cheerio + adm-zip), Singapore geo-fence, migration_audit.log; runtime Vault radius 300 → 500 m; Hidden Sanctuary radius 300 → 2000 m; ver.js error-note slice 80 → 250 chars to surface real Gemini error. | PR #29 (squashed `bfb84cc`) | 30-04 '26 |
| 26 | v0.15.1 patch — code default Gemini model `'gemini-2.5-pro'` → `'gemini-2.5-flash'`; resolves HTTP 429 prepayment-credits-depleted error reported on Pro. | PR #30 (squashed `64646f3`) | 30-04 '26 |
| 27 | v0.16.0 minor — adopt Places API (New) `googleMapsLinks` structured field (placeUri / directionsUri / reviewsUri / photosUri) across vibe-suggest, vault, consultant, sync-vault. Inline Directions button now uses Google's authoritative `directionsUri` when present. | PR #31 (squashed `5c1c9f0`) | 30-04 '26 |
| 28 | v0.17.0 minor — adopt Places API (New) `generativeSummary` ("Summarized with Gemini" overview) across vibe-suggest, vault, consultant, sync-vault. Surface 💡 line in chat reply with required attribution. Region-restricted to en/IN/US — null for SG today; auto-upgrades when Google extends coverage. | PR #32 (squashed `1c9d696`) | 30-04 '26 |
| 29 | v0.18.0 minor — Durger-King-style TMA menu (8 tiles + cuisine bottom-sheet) at `/app/menu`; new commands `/cuisine <type>`, `/weather`, `/transport`, `/carpark`, `/grocery` (alias). New modules `weather.js` (NEA) and `carpark.js` (LTA). `routeMenuCommand` dispatcher unifies slash commands, `/start <cmd>` deep links, and `web_app_data` tile taps. Menu button now reads "🌿 soleat Menu". Map moved to `/app/map`. | PR #33 (squashed `ccf1b31`) | 30-04 '26 |
| 30 | v0.18.1 patch — `DATA_GOV_SG_API_KEY` env support; `weather.js` sends `x-api-key` header when set; `/ver` adds 7th probe row for data.gov.sg. | PR #34 (squashed `55164dd`) | 30-04 '26 |
| 31 | v0.18.2 patch — switch real-time weather to data.gov.sg v2 (`api-open.data.gov.sg/v2/real-time/api`); 5 endpoints (temp/rainfall/humidity/wind-dir/wind-speed) in parallel; `/weather` reply enriched with humidity / rain / wind lines; 2-hour forecast still on v1 pending v2 spec. | PR #36 (squashed `5881c4f`) | 30-04 '26 |
| 32 | v0.19.0 minor — location TTL 30 min → 24 h; pickValidated tries all candidates at 200 m before expanding (better 200 m fidelity); new `transport.js` (LTA bus stops geo cache + arrivals + crowd levels); `/transport` upgraded with nearest 3 bus stops × up to 4 services each. | PR #38 (squashed `03b8366`) | 30-04 '26 |
| 33 | v0.19.1 patch — `weather.js` v1 fallback orchestrator (v2 → v1 when v2 empty/errors); `/ver checkDataGov` reports which source actually serves data; warn-level logs reveal v2 failure cause. | PR #39 (squashed `2618ea6`) | 30-04 '26 |
| 34 | v0.20.0 minor — `transport.js` adds `nearestMrtStations` (Places `subway_station` searchNearby), `fetchPlatformCrowdAll` (LTA PCDRealTime × 10 lines), `networkCrowdSummary`. `/transport` reply gains MRT stations + network crowd line + 🔄 Refresh inline button. `/cuisine` (no arg) sends 3×3 inline keyboard. New `bot.on('callback_query')` handler routes `refresh:transport` and `cuisine:<Type>`. | PR #40 (squashed `080481e`) | 30-04 '26 |
| 35 | v0.20.1 patch — slash-command handler dedup. `/weather`, `/transport`, `/status`, `/carpark` now one-line delegates to the same `run*` functions used by TMA tile routing. Removes ~70 lines of v0.18.0-vintage inline code that was masking the v0.18.2/v0.19.0/v0.20.0 reply format upgrades when users typed slash commands instead of tapping menu tiles. Stale standalone `/status` ("Gia CBD Pulse") deleted. | PR #41 commit `b43a903` | 30-04 '26 |
| 36 | v0.21.0 minor — `/transport` adds live traffic incidents (LTA `/TrafficIncidents` + Haversine proximity filter, top 3 within 5 km). `/ver` ends with `Deployed: <SGT> (uptime <duration>)`. `/status` command surface removed entirely (slash handler, routing case, /start helper, setMyCommands). | PR #41 (squashed `1f4c332`) | 30-04 '26 |
| 37 | v0.21.1 patch — per-pick **📍 Open Map** inline button removed; **🚗 Directions** is now the sole per-pick affordance. TMA at `/app` still reachable via the menu button "🌿 soleat Menu" tile. | PR #42 (squashed `540b1bd`) | 30-04 '26 |
| 38 | v0.21.2 patch — `gemini-retry.js` shared `withRetry` helper. All four production Gemini call sites (gatekeeper, vibe-summary, vibe-suggest, consultant) wrap `generateContent` with exponential backoff (1s/2s/4s) on 429/503. `ver.js` intentionally NOT wrapped — health probe must report live state. | PR #43 (squashed `3ff57ec`) | 30-04 '26 |
| 39 | v0.22.0 minor — Cuisine Picker TMA. New Vite + React + Tailwind project at `web/cuisine/`, builds into `public/cuisine/` via root `npm run build` postinstall. New `cuisine-search.js` (count=15, single-radius, preset-shaped Gemini prompt, signature_dish per candidate, sort = open_now first → walk asc → rating desc). New `holidays.js` (data.gov.sg PH dataset + 2026 fallback array). `/cuisine` slash command rewired to a single `🍴 Open Cuisine Picker` web_app button; legacy 3×3 keyboard + `cuisine:<Type>` callback_query + menu bottom-sheet retired. New `cuisine-pick` web_app_data round-trip delivers a Sanctuary read for the tapped venue. | PR #44 (squashed `87e709b`) | 30-04 '26 |
| 40 | v0.22.1 patch — Search button hotfix. Auto-detects location on TMA mount via `useEffect` + `requestLocation()`. On rejection sets `locDenied=true` and surfaces inline "Tap 📍 above…" notice; Search button is no longer hard-disabled by missing location (the legacy onSearch alert handles the empty case). | PR #45 (squashed `2edf222`) | 30-04 '26 |
| 41 | v0.23.0 minor — Cuisine TMA major redesign. Three RangeSliders (radius 200 m–5 km, recency 5 d–6 mo, queue 5–60 min). 70-cuisine accordion across 8 categories (Common Here / Southeast Asian / China (Regional) / South Asian / European / Middle Eastern / Americas / African) with 5-cuisine selection cap. Free-text "Another cuisine" input. Live prompt preview pane. Backend `cuisine-search.js` accepts `recencyDays` + `queueMaxMin`, asks Gemini for `queue_min_estimate` + `booking_required`, post-filters venues exceeding queue tolerance. Cards now show ⏱ queue est + 📅 booking flag. | PR #46 (squashed `10dd186`) | 30-04 '26 |
| 42 | v0.24.0 minor — `/surprise` command. New `surprise.js` pipeline: Places Nearby Search 3 km → cheap pre-filter (rating ≥ 4.3, reviews < 50, priceLevel ≤ MODERATE) → haversine annulus gate (1.5–3 km) → up to 6 Place Details fetches → opening gate (open now and not within 30 min of close, or opens within 2 h) → recent-review gate (≥ 1 review in last 4 days with rating ≥ 4) → Gemini enrich for dishes / why-ordered / booking flag. Single Markdown card output with 🚗 Directions + 🔍 Google buttons. | PR #47 (squashed `a5ce7d3`) | 30-04 '26 |
| 43 | v0.25.0 minor — Buddy Level 1 (share-a-pick). New `share.js` (Redis-backed token store, 7-day TTL, base64url ~10 char tokens). `deliverPicks` and `deliverSurprise` now append a 👋 Send to buddy callback button per pick. Tap → bot replies with `t.me/<bot>?start=share_<token>` deep link. Buddy taps → bot's /start handler loads payload and renders the Sanctuary read for them. | PR #48 (squashed `f6a4043`) | 30-04 '26 |
| 44 | v0.25.1 patch — Menu default is now `/app/cuisine` ("🍴 Cuisine Picker"). Slash autocomplete leads with `/cuisine`, then `/surprise`. `/eat` dropped from setMyCommands and from `public/menu.html` tile grid (handler retained for muscle memory). Tile grid adds 🎲 Surprise tile. Cuisine TMA Search instrumented end-to-end: client codes D100–D900 (mount/geo/initData/POST/HTTP/parse/venues), server codes D700–D703 (request/validate/success/error). Diagnostics panel auto-opens on any failed step; logs mirror to console with grep-able `[Cuisine-Diag]` prefix shared between TMA + Railway. | PR #49 (squashed `29df47c`) | 30-04 '26 |
| 45 | v0.26.0 minor — Reason–Fetch–Refine pipeline. New `vault-index.js` aggregator across `gia:vault` + `gia:place:<id>` + `vibe-summary:<id>` + new `place-reviews:<id>` (24 h TTL, populated fire-and-forget by `vibe-suggest.hasNegativeRecentReview`). New `pipeline.js` with three pure phases (`reason` Gemini Flash with vault-grounded review snippets, `fetchContext` per-cluster 500 m grid, `refine` Gemini Flash second pass) + `runPipeline` orchestrator. `cuisine-search.searchCuisine` runs Reason first, validates via Places, then Refines under `PIPELINE_ENABLED=true` (default on); legacy path preserved as fallback. `surprise.findSurprise` adds Refine pass for travel advice + shelter notes on a single venue. New diagnostic codes D601, D610–D612, D620–D623, D630–D632, D640. VenueCard surfaces `💵 cost band` + 🧭 travelAdvice + ☂️ shelterNote. `deliverSurprise` renders the same. | PR #50 (squashed `dc74ed3`) | 30-04 '26 |
| 46 | v0.26.1 patch — TMA↔backend bridge audit. Permissive CORS on `/api/*` (`*` origin, methods GET/POST/OPTIONS, allowed header `X-Telegram-Init-Data`). New auth-free `GET /api/diag/cuisine` returning `{ok, version, pipelineEnabled, envPresent.*, vaultIndexLoaded, webhookDomain, timestamp}`. TMA pings on mount via `diagPing()`, records D050–D053, paints a green/red connectivity badge under the Header. Backend `POST /api/cuisine-search` now fires `safeSend(tgUserId, '🌿 Sensing the vibe…')` chat receipt on entry, throttled by `isProcessing`, cleared in `finally`. Console-side log on TMA mount: `[Cuisine-Diag] API base = <origin>/api/cuisine-search`. | PR #51 (squashed `4c442c6`) | 01-05 '26 |
| 47 | v0.26.2 patch — Single 📍 Google Maps button per Sanctuary card across `deliverPicks` (chat reads), `deliverSurprise` (single-card surprise), and TMA `VenueCard`. Removed: `🚗 Directions`, `🔍 Google`, `👋 Send to buddy` per-pick buttons. URL waterfall: `venue.url` → place_id-encoded search URL → `directionsUri`. Underlying share machinery (`share.js`, `/start share_<token>` deep-link handler, `share:<token>` callback handler) preserved on disk so existing share links still work. | PR #52 (squashed `1a51cf6`) | 01-05 '26 |
| 48 | v0.26.3 patch — Dual-channel TMA bridge. Front-end `App.jsx#onSearch` now always tries fetch first; on any HTTP failure (network/4xx/5xx) records D063 and retries via `Telegram.WebApp.sendData({cmd:'cuisine-search', ...payload})`. Auto-closes TMA 800 ms after sendData success. New `routeMenuCommand` `cuisine-search` case runs the full pipeline server-side and delivers picks to the chat. New diag codes D060 (launch context), D061–D063 (sendData fallback), D720–D723 (server fallback handler), D730–D732 (web_app_data dispatcher). New `tg.js#launchContext()` snapshot for diagnostics. Every step logs `[Cuisine-Diag]` to both browser console and Railway. | PR #53 (squashed `bf0f2bc`) | 01-05 '26 |
| 49 | v0.26.4 patch — TMA force-fire. Search no longer blocks on missing geolocation (CBD default). Synchronous "🌿 Sensing the vibe…" banner paints before any async. Tap counter `🔍 Search · N` on the button. 6 s fetch timeout via AbortController (D406 fires on abort, dual-channel fallback triggers). New `💬 Chat` escape-hatch button next to Search calls `sendData()` immediately. Build mark `D040 bundle loaded version=0.26.4` on bundle init. | PR #54 (squashed `ec5c23c`) | 01-05 '26 |
| 50 | v0.26.5 patch — Diagnostics panel hidden by default; toggled via 600 ms long-press on the connectivity badge with haptic feedback. Auto-open on failed step preserved. Badge shows `· diag ON` suffix when forced visible. | PR #55 (squashed `385d185`) | 01-05 '26 |
| 51 | v0.27.0 minor — 60-second per-chat pick cache. New `pick-cache.js` (Redis-backed; key folds chat-id + 100 m lat/lng grid + radius/recency/queue/mode/when/preset + sorted lower-cased cuisines). Cache lookup before pipeline dispatch; write-through after success. Diag codes D705 (cache hit, ~50 ms) and D706 (cache store, ttl=60 s). Tap-spam cost cut ~80% on the same-controls-within-60s pattern; full pipeline still runs on slider/cuisine changes or location drift ≥100 m. | PR #56 (squashed `c0489fd`) | 01-05 '26 |
| 52 | v0.27.1 patch — `/share` slash command + recent-picks tracker. New `recent-picks.js` (Redis LIST per chat, LPUSH+LTRIM 0 4, 24 h TTL). `deliverPicks` and `deliverSurprise` push fire-and-forget. `/share` mints share tokens via existing `saveShare()` for each recent pick and renders an inline keyboard (one row per pick, `👋 <Name>` truncated to 30 chars). Tap → existing `share:<token>` callback hands the user the `t.me/<bot>?start=share_<token>` deep link. New setMyCommands row `share — Forward a recent pick to a buddy`. Re-exposes the buddy-share feature removed from per-pick UI in v0.26.2. | PR #57 (squashed `ea49c2e`) | 01-05 '26 |
| 53 | v0.27.2 patch — Localised cuisine labels (zh / ms / ta) on the 70-cuisine accordion. New `cuisine-i18n.js` with `CATEGORY_LABELS` (8 × 4 langs, full) and `CUISINE_LABELS` (Common Here + SE Asian + China Regional + South Asian fully covered; European/Middle Eastern/Americas/African high-frequency only, long-tail falls back to English). New `tg.js#getLanguage()` reads `Telegram.WebApp.initDataUnsafe.user.language_code`. State + Gemini payload remain canonical English; only rendered labels translate. | PR #58 (squashed `a71ea0b`) | 01-05 '26 |
| 54 | v0.28.0 minor — Migrated the Menu TMA from hand-rolled `public/menu.html` + `public/menu.js` to a Vite + React 18 + Tailwind project at `web/menu/`. Same 8-tile inventory + behaviour as v0.25.1; tile dispatcher unchanged (cuisine/map → navigate; everything else → `tg.sendData`). Build pipeline: root `npm run build` now chains `build:cuisine` and `build:menu`. Map TMA (`/app/map`) intentionally retained as vanilla JS (Decision #69). | PR #59 (squashed `02571b9`) | 01-05 '26 |
| 55 | v0.28.1 patch — Created all 8 CLAUDE.md folder template skeletons (`Builder.md`, `Persona.md`, `Feature.md`, `Technical.md`, `Legal.md`, `Journal.md`, `Chat.md`, `Register.md`). Each template carries the AU-rule references, table schemas, and seeded content where appropriate (Technical Stack, Legal Sources, Technical Budgets pre-filled with current values). Closes Open #10. | PR #60 (squashed `b5ce2ae`) | 01-05 '26 |
| 56 | v0.28.2 patch — `/admin/sync-vault?secret=<ADMIN_SYNC_SECRET>` HTTPS endpoint runs the vault import in-process using the bot's existing Redis + Maps key. Refactored `sync-vault.js` to expose `runSync({redis, items?, fenceDisabled?})` while preserving the CLI entrypoint via `if (require.main === module)`. Auth: timing-safe compare. On success, calls `vaultIndex.refreshIndex` so new venues land in the in-memory snapshot immediately. Replaces the need for `railway run node sync-vault.js` from a workstation. Re-runnable any time the source list grows. | PR #61 (squashed `9a6afba`) | 01-05 '26 |
| 57 | v0.29.0 minor — (a) Nearest-MRT enrichment in the Refine-pipeline per-cluster context: `pipeline.fetchClusterContext` adds `transport.nearestMrtStations(lat, lng, 1500, 2)` via `Promise.allSettled`; `summariseClusterCtx` adds `[nearest MRT: <station>(s)]` segment to refine prompt. (b) **Cache-bust headers** on all TMA HTML routes (`Cache-Control: no-store, no-cache, must-revalidate, max-age=0` + `Pragma` + `Expires`) so Telegram's in-app webview can't pin a stale bundle after Railway redeploys. (c) **Bundle version stamped in TMA Header** via Vite `define` of `__BUILD_VERSION__`; user confirms at-a-glance they're on the right build. | PR #62 (squashed `36facd6`) | 01-05 '26 |
| 58 | v0.29.1 patch — Diagnostic surfaces. (a) `GET /admin/test-pipeline?secret=<ADMIN_SYNC_SECRET>&lat=&lng=&cuisines=…` runs the full pipeline server-side (bypassing TMA + initData) and returns a step-by-step trace `{version, env, params, steps: {vault, pipeline}, totalMs, error?}`. Definitively answers "is the bridge broken or is the pipeline broken?" when /ver shows green. (b) 5-tap-in-2-seconds fallback for opening the Diagnostics panel — preserves the 600 ms long-press path while adding a click-counter alternative for clients where PointerEvents long-press doesn't fire reliably. | PR #63 (squashed `f1011b9`) | 01-05 '26 |
| 59 | v0.29.2 patch — Visible 🔧 Debug button in Header next to 📍. Diagnostics panel relocated to render directly under the Header (was buried below 3 sliders + accordion). Empty-state copy "no D-codes yet (tap Search)" so toggle proves it works even before any action. Long-press + 5-tap badge paths retained as fallback. | PR #64 (squashed `a686ea2`) | 01-05 '26 |
| 60 | v0.29.3 patch — TMA fetch timeout raised from 6 s → 25 s. Root cause from v0.29.2 Diagnostics screenshot: D406 was firing at 6 s into a request that legitimately needs 8–15 s (Reason + Validate + Refine = 2 Gemini + Place validates + Refine). Below-p50 timeout was triggering sendData fallback unnecessarily, which auto-closed the TMA per Telegram spec — visible to the user as "/cuisine screen closes so fast". Banner copy updated to "Sensing the vibe… (8–15 s typical — Reason → Fetch → Refine)" so user mental-models the actual duration. | PR #65 (squashed `290b27d`) | 01-05 '26 |
| 61 | v0.30.0 minor — Free-text chat search (any language) via Gemini intent classifier. New `nl-intent.js` (Gemini Flash + 60s Redis cache + 73-cuisine catalogue + special_request extraction + ack_text in user's language). New free-text branch in `bot.on('message')` between pending-meal consume and gatekeeper. New `runNLFlow` invokes searchCuisine with extracted hints. Pending-state extension `nl:<json>` resumes after location-share. `cuisine-search.js` + `pipeline.js` now thread `specialRequest` into Gemini reason prompt as "Distinctive user qualifier (HONOUR THIS): <qualifier>". Diag codes D750-D753 (Railway-side). Confidence threshold 0.6; below falls through to existing topic gatekeeper. | PR #66 (squashed `f0d7787`) | 01-05 '26 |
| 62 | v0.30.1 patch — `/admin/test-pipeline` accepts `nl_text` (free-text query, runs `classifyIntent` first, merges extracted hints into the pipeline call), `specialRequest`/`special_request` (alias for direct injection without classifying), and `lang` (language hint). Single curl now exercises the full chat NL flow end-to-end including the classifier, vault snapshot, reason, validate, fetch context, refine, and final venues. Trace JSON includes new `steps.nl` block with `{intent, confidence, cuisines, special_request, lang, ack_text}` so the user sees the classifier's full output. | PR #67 (squashed `4806d2e`) | 01-05 '26 |
| 63 | v0.30.2 patch — Five-strand resilience + UX bundle. (a) **Pro→Flash auto-fallback** in `gemini-retry.js` — `withRetry` accepts `fallbackFn`; new `makeFlashFallback(genAI, prompt, generationConfig)` helper. All 6 Gemini call sites wired (nl-intent, pipeline.reason, pipeline.refine, surprise, cuisine-search, vibe-suggest). Pro-503 outage no longer bricks bot. (b) **/surprise gate relaxation** — RECENT_REVIEW_DAYS 4→30; soft-fallback returns top-rated annulus venue with `isFallback: true` flag and transparent footer. (c) **TMA Header shows resolved location** — `📍 lat, lng` line under title; user always knows what location Gia is using. (d) **NL classifier `update-location` intent** — typed "My location change" / "我换地方了" / etc. now routes to `LOCATION_REQUEST_KEYBOARD` instead of gatekeeper. New diag D754. (e) **15-min staleness reminder** — `location-cache.setUserLocation` stamps `setAt`; new `getLocationAgeMinutes`; `deliverPicks` appends "📍 Heads up — your location is N min old…" footer when ≥15 min since last share. | PR #68 (squashed `3373888`) | 01-05 '26 |
| 64 | v0.30.3 patch — Six-strand performance + UX + canonical-prompt bundle. (a) **Place-validate parallelisation** — `Promise.allSettled` over 15 candidates, ~15s → ~3s phase. (b) **TMA→Redis location sync** — `/api/cuisine-search` writes `setUserLocation(redis, tgUserId, lat, lng)` so chat-side flow doesn't re-prompt. New diag D707. Fixes "asks twice for location". (c) **D406 label** updated `(>6s) → (>25s)`. (d) **NEW `prompt-templates/geospatial-culinary-analyst.md`** — verbatim Human Lead source + parameterised template + substitution glossary, "Keep in GitHub" per directive. (e) **`pipeline.js#buildReasonPrompt` adopts `[ACT: GEOSPATIAL_CULINARY_ANALYST]` framing** — adds `verified_opening_date`, `verified_google_maps_url` to schema; fast-food chain negative constraints enumerated; cross-reference instruction names IG/TikTok/Reddit/Time Out/SethLui/Honeycombers/MICHELIN. (f) **Google Search grounding** enabled in `pipeline.js#reason` via `tools: [{googleSearch: {}}]` — model can hit live web for "newly opened" claims. Flash-fallback wrapper rebuilt to retain tools. UI surfaces `🆕 Opened YYYY-MM-DD (web-grounded)` in both chat reads and TMA cards. Adds ~$0.0035/call. | PR #69 (squashed `ec37b89`) | 01-05 '26 |
| 65 | v0.30.4 patch — `/log on` verbose mode + `GROUNDING_ENABLED` env flag. NEW `verbose-log.js` (Redis-backed per-chat flag, 24h TTL, `say(redis, chatId, sendFn, text)` helper). `bot.on('message')` NL branch + `runNLFlow` now mirror every step to chat when verbose is on (D750/D751 received+intent, runNLFlow start, searchCuisine duration + venue count, exception messages). New `/log on|off|status` slash + setMyCommands row. **`pipeline.js#reason` GROUNDING_ENABLED gate** (default ON; `=false` reverts to no-tools behaviour); JSON-parse hardening with explicit head/tail snippets in D612 diag on parse error. | PR #70 (squashed `3548430`) | 01-05 '26 |
| 66 | v0.30.5 patch — Two diagnostics-driven fixes after v0.30.4 trace screenshot showed (a) wrong-location anchor (user GPS used instead of "Tanjong Pagar MRT" mentioned in prompt) and (b) Reason returned 0 candidates in 60s with no fallback. (a) **`nl-intent.js` schema adds `location_override`** field; classifier extracts "Tanjong Pagar MRT", "Raffles Place", "Bishan" etc. when user names an anchor in their prompt. (b) **`bot.on('message')` NL branch geocodes `location_override`** via `geocodeQuery` and uses resolved lat/lng instead of cached GPS for the search. Diag codes D708 (geocode start), D709 (geocode ok), D710 (fail → fallback). Verbose-log surfaces "Search anchored at <override → place / cached GPS>". (c) **`pipeline.js#runPipeline` Reason 0-candidate fallback** — when initial draft empty AND `recencyDays` or `specialRequest` set, retries once with both relaxed; diag D613 (retry trigger) + D614 (retry yield). Avoids "0 venues in 60s" dead-end. | PR #71 (squashed `85d794d`) | 01-05 '26 |
| 67 | v0.30.6 patch — Off-cuisine + chain-leakage fix after v0.30.5 returned coffee shops + Coffee Bean for a Peranakan/Indonesian/Korean query. (a) **`pipeline.js#buildReasonPrompt`** — when `cuisines.length > 0`, period framing softened ("Time of day (informational only — DO NOT bias toward generic snack/café venues)") and AUTHORITATIVE CUISINE CONSTRAINT clause appended: "Every venue you return MUST primarily serve one of [<cuisines>]. Coffee shops, dessert specialists, generic cafés, kopitiams, and bakeries DO NOT QUALIFY". Pairs with v0.30.5 relaxed-retry fallback. (b) **`cuisine-search.js`** — server-side `excludeChains(venues)` defense-in-depth filter. `FAST_FOOD_CHAIN_PATTERNS` regex array covers McDonald's/KFC/Subway/Burger King/Starbucks/Coffee Bean/Domino's/Pizza Hut/Jollibee/Texas Chicken/Wendy's/Popeye's/Shake Shack/Five Guys/Taco Bell/Long John Silver's/Carl's Jr. Conservative — does NOT touch SG-local chains. Logs `D709 chain-filter dropped "<name>"` per drop. | PR #72 (squashed `9c39b89`) | 01-05 '26 |
| 68 | v0.30.7 patch — Documentation-only. NEW `prompt-templates/test-prompts.md` covers the cuisine pipeline's test surface across 16 categories (A–P): single cuisine, multi-cuisine, cuisine+qualifier, location override, drinks, groceries, multi-language, recency, compound prompts, location-update intent, off-topic gatekeeper, chain-leakage defense, empty/0-result paths, max-cap edge, ambiguous/typo input, voice (deferred). Each row carries verbatim prompt, expected classifier output, expected venue shape, and diag codes to look for. Includes 5-prompt smoke test (A1, D1, G1, I1, K1) for release readiness. Coverage matrix maps tests to code paths. | this PR | 01-05 '26 |

## Removed Features

| # | Item | Original Version | Removed in Version | Removal Date (DD-MM 'YY) | Reason | Replacement | Rule Reference | Serial № |
|---|------|------------------|--------------------|--------------------------|--------|-------------|----------------|----------|
| 1 | `/status` slash command (originally LTA train alert pulse "Gia CBD Pulse"; aliased to `runTransportCommand` in v0.20.1) | 0.1.x | 0.21.0 | 30-04 '26 | Per Human Lead "Transport and status are the same.. remove status". The v0.20.1 alias collapsed it to a duplicate `/transport`; no behavioural distinction remained. | `/transport` (now MRT pulse + crowd + traffic + nearest bus stops) | AU-2, G2 | (№ 33 - 30-04 '26 15:00 SGT) |
| 2 | `📍 Open Map` per-pick inline button (TMA `web_app:` link to `/app?placeId=<id>`) | 0.5.0 | 0.21.1 | 30-04 '26 | Per Human Lead "Click 'open map' after searching the places, resulted in TMA pop-up instead of the map. I think can remove this option". The TMA pop-up was the wrong affordance under per-pick reads; users want directions, not browsing. | `🚗 Directions` retained as sole per-pick button; menu button "🌿 soleat Menu" tile UI continues to expose the TMA map | AU-2, G2 | (№ 34 - 30-04 '26 15:30 SGT) |
| 3 | `CUISINE_KEYBOARD` (3×3 inline cuisine keyboard) + `cuisine:<Type>` `callback_query` case + `/cuisine` chat-side location prompt | 0.18.0 | 0.22.0 | 30-04 '26 | Per Human Lead "Replace with a deep-link to the TMA only". The TMA owns multi-select cuisines, dual radius, transport mode, time, and four preset combos — the chat keyboard couldn't carry that surface area. | TMA Cuisine Picker at `/app/cuisine` (Vite + React + Tailwind) reachable via single `🍴 Open Cuisine Picker` web_app button on `/cuisine` | AU-2, G2 | (№ 36 - 30-04 '26 16:00 SGT) |
| 4 | `cuisine-modal` bottom-sheet in `public/menu.html` + matching tile/cancel handlers in `public/menu.js` | 0.18.0 | 0.22.0 | 30-04 '26 | Same as Removed #3: the TMA Cuisine Picker fully replaces the bottom-sheet's role. | Menu Cuisine tile now navigates internally to `/app/cuisine` | AU-2, G2 | (№ 36 - 30-04 '26 16:00 SGT) |
| 5 | `CuisineChips.jsx` (9-chip Japanese/Korean/Chinese/Italian/Indian/Thai/Vietnamese/Malay/Western single-row picker) + the `ALL_CUISINES` 9-element constant | 0.22.0 | 0.23.0 | 30-04 '26 | Replaced by the 70-cuisine accordion across 8 categories from the Human Lead spec. The new catalogue is too large for a single chip row. | `CuisineAccordion.jsx` + `state/cuisines.js` | AU-2, G2 | (№ 38 - 30-04 '26 17:00 SGT) |
| 6 | `RadiusToggle.jsx` (1 km · Transit / 250 m · Walking two-button selector) + the `ALLOWED_RADIUS = new Set([250, 1000])` server-side gate | 0.22.0 | 0.23.0 | 30-04 '26 | Replaced by a free-form slider 200 m–5 km in 100 m steps per Human Lead "range bar from Walk to train or drive should be from 200m to 5 km". | `RangeSlider.jsx` + server-side `clamp(200..5000)` | AU-2, G2 | (№ 38 - 30-04 '26 17:00 SGT) |
| 7 | `/eat` `setMyCommands` row + `public/menu.html` Eat tile (`data-cmd="eat"`, 🍽 icon, "Solo-diner sanctuary picks for now") | 0.5.0 | 0.25.1 | 30-04 '26 | Per Human Lead "you should remove /eat". Hidden from discovery surfaces only; `bot.onText(/^\/eat…)` handler retained so existing usage doesn't 404. | `/cuisine` (TMA Cuisine Picker, now the default chat-menu surface) | AU-2, G2 | (№ 41 - 30-04 '26 18:30 SGT) |
| 8 | Chat menu button "🌿 soleat Menu" → `/app/menu` (8-tile grid) as the default landing surface | 0.18.0 | 0.25.1 | 30-04 '26 | Per Human Lead "move /cuisine as default when loaded". Menu button now lands directly on the Cuisine Picker. | "🍴 Cuisine Picker" → `/app/cuisine` (`public/menu.html` tile grid still reachable manually) | AU-2, G2 | (№ 41 - 30-04 '26 18:30 SGT) |
| 9 | `🚗 Directions` per-pick inline button on `deliverPicks` (chat-side Sanctuary reads) — used `directionsUri` from googleMapsLinks or a constructed `dir/?api=1&destination_place_id=…` URL | 0.5.0 | 0.26.2 | 01-05 '26 | Per Human Lead "default should be google map, and listed only 1 link instead of 3". Consolidated into a single `📍 Google Maps` button. | `📍 Google Maps` (place page; user can tap "Directions" within Google Maps) | AU-2, G2 | (№ 44 - 01-05 '26 11:00 SGT) |
| 10 | `🚗 Directions` + `🔍 Google` two-button row on `deliverSurprise` (single-card surprise reads) | 0.24.0 | 0.26.2 | 01-05 '26 | Same — collapsed to one outbound link per card. | `📍 Google Maps` | AU-2, G2 | (№ 44 - 01-05 '26 11:00 SGT) |
| 11 | `👋 Send to buddy` per-pick callback button on `deliverPicks` and `deliverSurprise` (Buddy Level 1 share-link minter, v0.25.0) | 0.25.0 | 0.26.2 | 01-05 '26 | Per Human Lead "1 link instead of 3". `share.js` + `/start share_<token>` deep-link handler + `share:<token>` callback handler all preserved on disk so existing share links in users' chat history continue to work — only the *minter button* is gone. | none in default UI; if needed, candidate re-surfaces are `/share` slash command, long-press in TMA, or one-button-per-message instead of one-per-pick | AU-2, G2 | (№ 44 - 01-05 '26 11:00 SGT) |
| 12 | `🚗 Go` per-card link button in TMA `VenueCard.jsx` (used `venue.directionsUri` direct) | 0.22.0 | 0.26.2 | 01-05 '26 | Same as Removed #9 / #10 — TMA card now matches chat-card UX with a single `📍 Google Maps` link. | `📍 Google Maps` opening `venue.url` (place page) | AU-2, G2 | (№ 44 - 01-05 '26 11:00 SGT) |
| 13 | `public/menu.html` (147 lines) + `public/menu.js` (28 lines) — hand-rolled tile-grid Menu TMA | 0.18.0 | 0.28.0 | 01-05 '26 | Stack consistency: migrated to a Vite + React 18 + Tailwind project at `web/menu/`. Same 8-tile inventory + dispatch behaviour. | `web/menu/src/App.jsx` + `web/menu/src/components/Tile.jsx` (built to `public/menu/`) | AU-2, G2 | (№ 51 - 01-05 '26 13:15 SGT) |
