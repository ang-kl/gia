(№ 360 - 26-05 '26 22:00 SGT) v0.61.159

# Journal — soleat v0.61.159 (PR 5 of 5 — Putrajaya UI button + train-line default + scope-guard tests; FINAL of the 10-rule phased build)

> Operator: *"Merge"* on PR #659 (v0.61.158, PR 4/5), then continue
> to PR 5. PR 5 is the FINAL phase of the 10-rule location-
> classification spec. It ships:
>
>   1. The operator's original ask — **a Putrajaya region pill in
>      the Cuisine TMA**, as the UI surface on top of the
>      v0.61.155-158 foundation (answer 1).
>   2. **Rule §2.10** — train-line default OFF outside SG. Verified
>      via the SG_ONLY membership of `TRANSPORT_TRAIN` +
>      `TMA_TRAINLINE` + the MapPanel's `isNonSg` force-zero on the
>      effective-layers object.
>   3. **Rule §2.11** — scope-guard tests. Cuisine / free-text /
>      Michelin / carpark / transport-drive / location MUST be in
>      `ALWAYS_ALLOWED` and NEVER in `SG_ONLY` for ANY mode.
>
> Commit 372 / journal 360 / build 162, on branch
> `claude/v0.61.159-putrajaya-button-scope-guard` off `main` at
> v0.61.158 (commit `e1bc856`, PR #659).

## New

### [HDR] #360 | 22:00 SGT 26-05-26 | v0.61.159 — Putrajaya UI pill + scope-guard tests + train-line default verification (FINAL PR 5/5) | 8 files | 1 PR | 1 build |

- **[INTENT]** Close the 10-rule location-classification phased
  build. PR 1 shipped the classifier (rules §2.2 + §2.3); PR 2
  the set_location + feature gate + no-nag (§2.4 + §2.5 + §2.6);
  PR 3 the boundary drift + single re-prompt (§2.7); PR 4 the
  carpark-outside-SG dispatcher (§2.8 + backend half of §2.9).
  PR 5 ships:
  - **The Putrajaya UI button** — a 3rd region pill in the
    Cuisine TMA (alongside SG + JB), with the Wikipedia Commons
    SVG (Flag_of_Putrajaya.svg, 188 KB). Selecting it sets
    `state.region = 'MY-PUT'` which the backend recognises as the
    IOI Resort City anchor (15 km cap from `precincts.js`).
  - **Rule §2.10 verification** — train-line toggle remains
    SG-gated (it was already gated in PR 2's `SG_ONLY` set, and
    the MapPanel force-zeroes the layer for any non-SG region).
    Default ON-OR-OFF behaviour is unchanged; the test asserts
    the gate membership so future refactors can't silently flip
    the train toggle to an always-on default.
  - **Rule §2.11 scope-guard tests** — 25 tests across the three
    spec-architectural invariants:
      1. `isFeatureAllowed(mode, ALWAYS_KEY) === true` for every
         mode × every always-allowed key (3 × 6 = 18 cases).
      2. `cuisine-search / freetext-search / michelin-search /
         carpark / transport-drive / location` are NEVER in the
         `SG_ONLY` deny-set.
      3. The carpark dispatcher's API shape (positional mode arg,
         no chatId / redis) ENFORCES the decoupling — cuisine /
         free-text / carpark cannot reach into the persisted
         locale by construction.
      4. The classifier reads `country` + `adminAreaLevel1`, not
         the TMA region toggle — clicking Putrajaya in the
         Cuisine TMA can't change a SG-coordinated user's
         classified mode.

- **[DELTA]**
  - **`web/cuisine/public/putrajaya-flag.svg`** (NEW, 188 KB).
    Source: https://upload.wikimedia.org/wikipedia/commons/9/9f/Flag_of_Putrajaya.svg
    (public domain — Wikipedia Commons file). The SVG is full-
    fidelity; the TMA renders it at 18 × 12 px in the region
    pill, matching the existing `johor-flag.png` dimensions.
  - **`web/cuisine/src/v2/App.jsx`** (region toggle row, ~25
    lines):
    - 3rd pill `{ id: 'MY-PUT', flag: 'putrajaya-flag.svg',
      label: t('region.putrajaya', lang) }` added to the array.
    - The image branch now accepts `.svg` in addition to `.png`.
    - `isMy = r.id === 'JB' || r.id === 'MY-PUT'` controls the
      Michelin chip strip-out (Michelin SG-only).
    - Auto-flip from cached anchor preserves the MY-PUT region
      (was: collapsed to JB).
    - Footer region label gains the Putrajaya branch.
  - **`web/cuisine/src/v2/lib/state.js`**: `REGIONS` array
    extended to `['SG', 'JB', 'MY-PUT']`. The URL-hash
    deep-link contract accepts `region=MY-PUT` now too.
  - **`web/cuisine/src/v2/lib/i18n.js`**: new
    `'region.putrajaya'` key (EN + FR both `'Putrajaya'` —
    proper noun, no translation).
  - **`web/cuisine/src/v2/components/MapPanel.jsx`**:
    - `isNonSg = jb || region === 'MY-PUT'` replaces every
      `disabled: jb` use in `rowToggles` + `menuToggles` and
      the `effectiveLayers` SG-only force-zero. Train toggle
      gated for MY-PUT users (rule §2.10).
  - **`__tests__/scope-guard.test.js`** (NEW, 25 tests, 4
    `describe` groups). Asserts rules §2.10 + §2.11.
  - **`package.json`** — `0.61.158 → 0.61.159`.
  - **`doc/.serial-state.yml`** — `commit 371 → 372`, `build
    161 → 162`, `journal 359 → 360`; re-anchored 26-05 '26
    22:00 SGT.
  - **`doc/Journal/journal-0_61_159-26_05_26-2200.md`** —
    this entry.

- **[VERIFICATION (sandbox)]**

  ```
  $ node --check state-v159.js                               (OK)
  $ node --check i18n-v159.js                                (OK)
  $ esbuild --loader:.jsx=jsx app-v159.jsx                   (OK)
  $ esbuild --loader:.jsx=jsx mp-v159.jsx                    (OK)
  $ vitest run __tests__/scope-guard.test.js \
       __tests__/location-mode.test.js \
       __tests__/location-locale.test.js \
       __tests__/location-classify.test.js \
       __tests__/location-boundary.test.js \
       __tests__/carpark.test.js
       ✓ scope-guard      (25 tests)
       ✓ location-mode    (30 tests)
       ✓ location-locale  (28 tests)
       ✓ location-classify (12 tests)
       ✓ location-boundary (19 tests)
       ✓ carpark          (14 tests)
       Tests  128 passed (128)
  ```

  `vite build` skipped — same O-22 reason as prior PRs (Railway
  postinstall rebuilds the Cuisine TMA bundle).

- **[STATUS]** PR-OPEN on push. Operator merges manually.

- **[TEST]** Operator (post-merge):
  - **Cuisine TMA region toggle row** — now shows three pills:
    🇸🇬 Singapore · 🟦 Johor Bahru · 🟦 Putrajaya (the SVG
    renders Putrajaya's navy-and-yellow flag).
  - **Tap Putrajaya** — region state goes to `MY-PUT`; the
    backend cuisine-search recognises the MY-PUT anchor (IOI
    Resort City, 15 km cap); the search uses Places only
    (Michelin chip stripped; LTA layers disabled).
  - **Map toolbar** — Train / Carpark / BusStop / Exits /
    Taxis / Attractions / Parks / Police / Clinics / Hospitals
    all greyed out (matches existing JB behaviour).
  - **Cached MY-PUT anchor** (chat /location → "IOI Resort
    City Putrajaya" picks the precinct) — Cuisine TMA opens
    on the Putrajaya pill (no more collapse-to-JB).
  - **SG user, fresh load** — unchanged. SG pill selected by
    default; all toggles functional.

- **[KNOWN GAPS]**
  - **Cuisine TMA Carpark MAP LAYER for non-SG** still
    deferred. Backend `/api/geo/carpark?mode=…&lat=&lng=`
    accepts non-SG params (PR 4); the TMA's `createOverlayController`
    + `fetchCarpark` are NOT yet wired to pass them. This is
    purely a wiring task in `mapOverlays.js` + `MapPanel.jsx`;
    no blockers, just out-of-scope for the location-classification
    spec which finishes here. Filed for a future follow-up
    (no PR planned in the current phase).
  - **Menu TMA `isMy` flag** still keys on
    `region === 'JB' || region === 'MY-PUT'`. The new
    location-mode classifier output (`userlocale.mode`) is the
    canonical "non-SG" signal; the Menu TMA could be migrated
    to read `mode !== 'SG'` directly. Behaviour is identical
    today (anchor's `region` is set by the same precinct picks
    that drive the mode); migration is purely a cleanup.
  - **Putrajaya SVG raw size**: 188 KB. Acceptable for a 18×12
    pill (browser cache + 1-time load) but a 5 KB optimised
    variant would be a nice-to-have. Operator can replace with
    an optimised export at any time — the filename stays
    `putrajaya-flag.svg`.

- **Confirmation Gates**

  | Gate | Resolution | Date |
  |------|-----------|------|
  | G1 — version bump | PATCH `0.61.158 → 0.61.159`. Final PR of the 5-PR phased build. New UI surface (Putrajaya pill) + scope-guard tests; backwards-compatible. | 26-05 '26 |
  | G2 — destructive action | _n/a_ — pure additions: 1 new SVG asset, 25 new tests, 1 new region pill (existing pills unchanged), `jb` flag widened to `isNonSg` (additive). | 26-05 '26 |
  | G3 — new decision rule | YES — Putrajaya is now a first-class TMA region; SG_ONLY+ALWAYS_ALLOWED contract pinned by tests; auto-flip preserves MY-PUT distinction. | 26-05 '26 |
  | G4 — paid external API call | _n/a_ — UI + tests only. The MY-PUT region already had backend support via `precincts.js`. | 26-05 '26 |

## Amendments

- **Auto-flip behaviour for cached MY-PUT anchor.** v0.61.124
  introduced the auto-flip from server-cache MY-PUT anchor to
  TMA region 'JB' (because MY-PUT had no UI pill). v0.61.159
  preserves the MY-PUT distinction now that the pill exists.
  The auto-flip line at App.jsx:407 changes from
  `setState({ region: 'JB' })` to
  `setState({ region: r.region })`.

## Deleted

_None._

---

## Phase summary (10-rule location-classification phased build)

| Rule | Status | Shipped in |
|------|--------|------------|
| §2.2 Coarse radius gate (120 km from SG centroid) | ✅ | v0.61.155 (#656) |
| §2.3 Country / admin-area classification (SG / JB / OTHER) | ✅ | v0.61.155 (#656) |
| §2.4 set_location with placeName (never raw lat/long) | ✅ | v0.61.156 (#657) |
| §2.5 Feature gate for non-SG | ✅ chat path | v0.61.156 (#657) |
|      &nbsp; &nbsp; (TMA toggles) | ✅ extended to MY-PUT | v0.61.159 (this PR) |
| §2.6 Per-chatId silence on same locale | ✅ | v0.61.156 (#657) |
| §2.7 Boundary drift + single re-prompt | ✅ | v0.61.157 (#658) |
| §2.8 Carpark outside SG (Places, 5 km) | ✅ | v0.61.158 (#659) |
| §2.9 Cuisine + carpark overlay (backend half) | ✅ | v0.61.158 (#659) |
| §2.10 Train-line default OFF outside SG | ✅ verified | v0.61.159 (this PR) |
| §2.11 Scope guard (location-mode never affects cuisine/freetext/carpark) | ✅ tested | v0.61.159 (this PR) |
| _Putrajaya UI button_ (operator's original ask, now on top of the foundation) | ✅ | v0.61.159 (this PR) |

The 10-rule build is complete.
