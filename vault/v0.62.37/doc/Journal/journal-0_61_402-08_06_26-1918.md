(№ 602 - 08-06 '26 19:18 SGT) v0.61.402

# Journal — soleat v0.61.402 (Unlock the Fruits pill everywhere; keep durian + durian-pastry belt-gated)

> Operator: *"fruits unlock"*.
>
> Commit 615 / journal 602 / **build held 386** (server-only).

## New

### [HDR] #602 | 19:18 SGT 08-06-26 | v0.61.402 — fruits unlocked globally (mode-aware belt gate) | special-mode.js · index.js · __tests__ | 1 PR | 0 build |

- **[INTENT]** The **v0.61.397** belt gate blocked **all three** special-mode pills (fruits / durian / durian-pastry) outside **SG/MY/ID/TH/PH/BN**. The operator wants **Fruits available everywhere** — fruit sellers exist worldwide (Japan's premium fruit parlours, etc.) and returned sensible results; only **durian + durian-pastry** are SE-Asian-specific and gave wrong answers.

- **[DELTA]**
  - **`special-mode.js`** — the gate is now **mode-aware**. New `BELT_GATED_MODES = {durian, durian-pastry}`; **`specialModeAllowed(country, mode)`** returns `true` immediately when `mode` is given **and not belt-gated** (i.e. fruits → allowed globally), else falls back to the `SPECIAL_MODE_COUNTRIES` belt check. **Mode omitted → conservative belt check** (preserves the v0.61.397 contract / existing callers).
  - **`index.js`** — passes the mode: `sm.specialModeAllowed(smCountry, specialMode)`. The **Fruits pill** now fires its `buildSeeds` search in JP/KR/HK/…; **durian + durian-pastry pills** stay belt-gated (still return the "only available in …" banner outside the belt).
  - **`__tests__/special-mode.test.js`** (+3).
  - **`package.json`** `0.61.401 → 0.61.402`; **`doc/.serial-state.yml`** commit 614 → 615, journal 601 → 602, **build held 386**; re-anchored 19:18 SGT.

- **[VERIFICATION (sandbox)]** `node --check` clean (index.js + special-mode.js). Full suite **114 files / 3107 tests** (+3).

- **[STATUS]** PR-**OPEN**, **auto-merge armed (squash)**. Railway redeploys on merge (server only — no rebuild).

- **[TEST]** Operator: in **Japan / Korea / Hong Kong**, tap the **Fruits** pill → a fruit-seller list loads (was the "only available in SG/MY/ID/TH/PH/BN" banner). The **Durian** + **Durian-pastry** pills still show that banner outside the belt.

- **[KNOWN GAPS / SAFETY]**
  - The **fruits-as-cuisine-chip** path was never gated (separate from the pill) — unchanged.
  - The `special.fruits.blocked` i18n key is now **dead** (fruits is never blocked). Left in place so this stays **server-only** (no `web/` rebuild); harmless. Can be pruned in a future web PR.
  - **Revert-ready.**

- **Confirmation Gates**

  | Gate | Resolution | Date |
  |---|---|---|
  | G1 — version bump | PATCH `0.61.401 → 0.61.402`. | 08-06 '26 |
  | G2 — destructive action | Additive — loosens one gate; durian/durian-pastry behaviour preserved. | 08-06 '26 |
  | G3 — new decision rule | Belt gate is **mode-aware**: only durian + durian-pastry are belt-gated; fruits is global. | 08-06 '26 |
  | G4 — paid external API call | Fruits pill now runs its Places search where it was previously short-circuited — intended. | 08-06 '26 |

## Amendments

- **`special-mode.js`** — `specialModeAllowed(country, mode)` + `BELT_GATED_MODES`; fruits unlocked.
- **`index.js`** — passes `specialMode` to `specialModeAllowed`.
- **`__tests__/special-mode.test.js`** — +3 mode-aware cases.
- **`package.json`** — `0.61.401 → 0.61.402`.
- **`doc/.serial-state.yml`** — counters (commit 615 / journal 602 / build held 386) + anchor 19:18 SGT 08-06-26.

## Deleted

- _None_ (the all-mode block is superseded by the mode-aware gate).
