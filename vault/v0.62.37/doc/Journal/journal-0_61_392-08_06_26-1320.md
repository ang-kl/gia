(№ 592 - 08-06 '26 13:20 SGT) v0.61.392

# Journal — soleat v0.61.392 (Drop the build date/time from the Cuisine footer)

> Operator (IMG_2333 footer): *"your build still show date and time in Cuisine, this was handled previous PR."*
>
> **Left OPEN for review — auto-merge to be armed on the operator's word.** Commit 605 / journal 592 / build 382. web/cuisine.

## New

### [HDR] #592 | 13:20 SGT 08-06-26 | v0.61.392 — remove footer "built … UTC" chip | App.jsx | 1 PR | 1 build |

- **[INTENT]** The Cuisine footer still showed `· built 2026-06-08 04:28 UTC`. The operator believed a previous PR removed it.

- **[SOURCE-OF-TRUTH CHECK]** The build-timestamp chip was **added** in **v0.61.182 (#684)** (`App.jsx:3185`), and `git log -S` confirms **no later PR removed it** — so it was **never actually handled** (the prior intent/request didn't land). Code wins over memory; removing it now.

- **[DELTA]** (`web/cuisine/src/v2/App.jsx`)
  - Removed the `· built ${_formatBuildTimeShort(BUILD_TIME)} UTC` segment from the footer render (~`:3185`), keeping `· v{BUILD_VERSION}` — the version alone identifies a deploy.
  - Removed the now-dead `BUILD_TIME` const + `_formatBuildTimeShort` helper (~`:95–102`). Left the `__BUILD_TIME__` vite define in place (harmless) so the chip can be reinstated trivially if ever wanted.
  - **`package.json`** `0.61.391 → 0.61.392`; **`doc/.serial-state.yml`** commit 604 → 605, journal 591 → 592, **build 381 → 382**; re-anchored 13:20 SGT.

- **[VERIFICATION (sandbox)]** Cuisine build green. Full suite **113 files / 3088 tests**.

- **[STATUS]** PR-**OPEN**, **auto-merge to arm on the operator's word**. Railway redeploys on merge.

- **[TEST]** Operator: the Cuisine footer now reads `Experimental · Singapore · v0.61.392` — no date/time.

- **[KNOWN GAPS]**
  - The build timestamp is no longer shown anywhere in the Cuisine TMA; to verify a fresh deploy, use the **version** (which bumps every PR) or the Railway build log.
  - Not live-E2E tested.

- **Confirmation Gates**

  | Gate | Resolution | Date |
  |---|---|---|
  | G1 — version bump | PATCH `0.61.391 → 0.61.392`. | 08-06 '26 |
  | G2 — destructive action | Removed a footer chip + its dead helper; the `__BUILD_TIME__` define is preserved. | 08-06 '26 |
  | G3 — new decision rule | The Cuisine footer shows the version only, no build timestamp. | 08-06 '26 |
  | G4 — paid external API call | None. | 08-06 '26 |

## Amendments

- **`web/cuisine/src/v2/App.jsx`** — removed the footer `built … UTC` chip + the dead `BUILD_TIME` / `_formatBuildTimeShort`.
- **`package.json`** — `0.61.391 → 0.61.392`.
- **`doc/.serial-state.yml`** — counters (commit 605 / journal 592 / build 382) + anchor 13:20 SGT 08-06-26.

## Deleted

- The footer build-timestamp chip (v0.61.182) + its `_formatBuildTimeShort` helper — superseded; version-only footer.
