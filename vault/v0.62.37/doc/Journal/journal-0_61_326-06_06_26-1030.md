(№ 527 - 06-06 '26 10:30 SGT) v0.61.326

# Journal — soleat v0.61.326 (HK cities: add 4 New Territories centres)

> Operator (06-06 '26, approved data): add four New Territories city centres — **Tuen Mun, Yuen Long, Tai Po, Tseung Kwan O** — to the OTHER-mode HK cities tables, so a New-Territories location pick anchors to a nearby centre instead of only Sha Tin. Mirror the same four into the gia-web browser port.
>
> Before this patch the HK block carried 8 entries (Tsim Sha Tsui, Central, Causeway Bay, Mong Kok, Wan Chai, Sha Tin, Aberdeen, Tung Chung) — Sha Tin was the only New-Territories representative. The four new centres bring HK to 12 and give the NT its own anchors. Commit 540 / journal 527 / build 327, branch off main @ v0.61.325 (sha 2f49314).

## New

### [HDR] #527 | 10:30 SGT 06-06-26 | v0.61.326 — HK New-Territories cities (Tuen Mun, Yuen Long, Tai Po, Tseung Kwan O) | 4 files | 1 PR | 1 build |

- **[INTENT]** HK New-Territories coverage in the OTHER-mode cities tables was just Sha Tin; add Tuen Mun, Yuen Long, Tai Po and Tseung Kwan O (all `HKG`) to both parallel cities tables so a New-Territories pick anchors to a nearby centre. Pure data — no logic, no UI, no API change.

- **[DELTA]**

  **1. `web/cuisine/src/v2/lib/cities.js`** — HK array: appended four rows after the existing last entry `{ name: 'Tung Chung', code: 'HKG', lat: 22.2914, lng: 113.9434 }` (added the previously-missing trailing comma to it), aligned to the existing `code:`-at-column-32 style:
    - `{ name: 'Tuen Mun',      code: 'HKG', lat: 22.3908, lng: 113.9725 }`
    - `{ name: 'Yuen Long',     code: 'HKG', lat: 22.4445, lng: 114.0225 }`
    - `{ name: 'Tai Po',        code: 'HKG', lat: 22.4501, lng: 114.1644 }`
    - `{ name: 'Tseung Kwan O', code: 'HKG', lat: 22.3076, lng: 114.2590 }`

  HK: **8 → 12** entries.

  **2. `web/menu/src/cities.js`** — the parallel copy (the cuisine file's header says "Keep the two in sync"). Same four rows, byte-identical alignment (its HK block matched the cuisine file's whitespace exactly — same Tung Chung last-entry, same column-32 `code:`). HK: **8 → 12**.

  **3. `package.json`** — `0.61.325 → 0.61.326`.

  **4. `doc/.serial-state.yml`** — commit 540 / build 327 / journal 527; re-anchored 10:30 SGT 06-06-26 with `last_anchor_source` describing the HK NT-cities add.

  `code` stays the shared IATA `HKG` for all four (per the v0.61.242 non-inventive-code rule — the IATA code groups a metro, it is not per-city/inventive); only `name` + `lat`/`lng` are per-city.

- **[VERIFICATION (sandbox)]**
  - `node --check web/cuisine/src/v2/lib/cities.js` → **clean**.
  - `node --check web/menu/src/cities.js` → **clean**.
  - Post-edit assertion: each file's `HK: [ … ]` block now holds exactly 12 `code: 'HKG'` rows; all four new names present exactly once.
  - Non-HK region diff vs origin/main: every country block OUTSIDE `HK: [ … ]` is byte-identical in both files — only the HK block changed.
  - Coordinates are the operator-approved real centres: Tuen Mun (22.3908, 113.9725), Yuen Long (22.4445, 114.0225), Tai Po (22.4501, 114.1644), Tseung Kwan O (22.3076, 114.2590).

- **[STATUS]** PR-OPEN; auto-merge squash armed (bot repo auto-merges on green CI). Railway redeploys on merge.

- **[TEST]** Operator (post-merge): in OTHER mode, a location pick in Tuen Mun / Yuen Long / Tai Po / Tseung Kwan O now snaps to that NT centre rather than to Sha Tin or an HK-Island district.

- **[KNOWN GAPS]**
  - **Shared IATA `code: 'HKG'`** across all 12 HK rows is intentional (one metro code); not per-city.
  - **gia-web mirror** shipped as a SEPARATE, review-first PR (`lib/cities.ts`, version 0.1.x) — see that repo; it does NOT use this journal protocol.
  - Other NT towns (e.g. Tin Shui Wai, Ma On Shan, Fanling/Sheung Shui) not added — only the four operator-approved centres. Can extend later if requested.

- **Confirmation Gates**

  | Gate | Resolution | Date |
  |---|---|---|
  | G1 — version bump | PATCH `0.61.325 → 0.61.326`. Pure data addition. | 06-06 '26 |
  | G2 — destructive action | _n/a_. Additive — 4 new rows + 1 trailing comma; no removal, no other block touched. | 06-06 '26 |
  | G3 — new decision rule | _n/a_. Follows the existing OTHER-mode cities-table + v0.61.242 shared-IATA-code conventions. | 06-06 '26 |
  | G4 — paid external API call | _n/a_. No API surface; static table data only. | 06-06 '26 |

## Amendments

- **`web/cuisine/src/v2/lib/cities.js`** — HK array 8 → 12 (added Tuen Mun, Yuen Long, Tai Po, Tseung Kwan O; trailing comma added to Tung Chung).
- **`web/menu/src/cities.js`** — same four rows (parallel sync copy).
- **`package.json`** — `0.61.325 → 0.61.326`.
- **`doc/.serial-state.yml`** — counters + anchor re-set 10:30 SGT 06-06-26.

## Deleted

_None._
