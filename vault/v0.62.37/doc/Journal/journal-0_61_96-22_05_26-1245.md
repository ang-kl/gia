(№ 303 - 22-05 '26 12:45 SGT) v0.61.96

# Journal — soleat v0.61.96 (shrink the embedded-map zoom indicator)

> Operator — a large map spec (~8 items). This entry is the first,
> smallest piece: *"the zoom indicator in Google Maps embedded is too
> large, make it smaller by 2 size for all TMA."* The remaining items
> (amenity zoom tiers; bringing the `/app/map` page — `public/app.js`,
> opened by the "View/Compare/Show all" buttons — to parity with the
> React TMA maps) are scoped as follow-up PRs.
>
> This entry is commit 303 / journal 296 / build 102, off `main` at
> v0.61.95 (#588).

## New

### [HDR] #296 | 12:45 SGT 22-05-26 | v0.61.96 — zoom indicator size | ~1 kb | 4 files | 1 PR | 1 build |

- **[INTENT]** The embedded-map zoom-level indicator reads too large;
  shrink it by two size steps on all three TMA maps.

- **[DELTA]**
  - **`web/cuisine/src/v2/components/MapPanel.jsx`**,
    **`web/hawker/src/components/HawkerMapPanel.jsx`**,
    **`web/transport/src/components/MrtMapPanel.jsx`** — the white
    circular zoom readout: `w-10 h-10` → `w-8 h-8` (40 px → 32 px) and
    `text-sm` → `text-xs`.
  - **`package.json`** — `0.61.95` → `0.61.96` (PATCH).

- **[VERIFICATION (sandbox)]**

  ```
  npm run build (5 TMAs)   all green
  npm test                 1944/1944 passing, 67 files
  ```

- **[STATUS]** DRAFT — PR #589 on `claude/v0.61.96-zoom-indicator-size`
  (off `main` at v0.61.95). On merge deploys via the Railway
  auto-deploy on `main`.

- **[TEST]** The zoom circle on each of the three maps is visibly
  smaller; the integer stays legible.

- **[KNOWN GAPS]**
  - **Remaining operator spec items, scoped as follow-ups:**
    - Amenity overlays (Attractions / Parks / Clinic / Police /
      Hospital) need per-zoom-band rendering (tiny at z10-11,
      glyph/icon at z12-13, glyph+text at z14+ with overlap demotion)
      — a `trainTier`-style change in `mapOverlays.js`.
    - The "View *N* Train Status / Compare all *N* carpark / Show all
      *N* bus stop" buttons open `/app/map` (`public/app.js`) — a
      separate, older non-React map page. Its station taps, dropdown,
      top-edge toggle row, location pin, carpark-name casing and
      train-toggle behaviour all need to be brought to parity with the
      React TMA maps. The largest chunk of the spec.
  - No in-sandbox visual verification (no Maps webview).
  - `doc/VibeCodingRecord/` regen still due (carried forward).

- **Confirmation Gates**

  | Gate | Resolution | Date |
  |------|-----------|------|
  | G1 — version bump | PATCH `0.61.95` → `0.61.96` | 22-05 '26 |
  | G2 — destructive action | _n/a_ — two CSS size classes per file | 22-05 '26 |
  | G3 — new decision rule | _n/a_ | 22-05 '26 |
  | G4 — paid external API call | _n/a_ | 22-05 '26 |

## Amendments

_None._

## Deleted

_None._
