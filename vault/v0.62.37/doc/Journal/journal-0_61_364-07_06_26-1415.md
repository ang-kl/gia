(№ 564 - 07-06 '26 14:15 SGT) v0.61.364

# Journal — soleat v0.61.364 (LocationField OTHER-picker pill polish — font −2 + city flush to country)

> Operator (multi-part request, screenshots VN/PQC + JP/OSA). **This PR = the low-risk UI half**: shrink the closed country/city pill fonts by 2 px and butt the city pill flush against the country pill. The behavioural half (mismatch / auto-load gating + street+building+city persistence) is a **follow-up PR**.
>
> **Left OPEN for review — auto-merge to be armed on the operator's word.** Commit 577 / journal 564 / build 362. Cuisine TMA.

## New

### [HDR] #564 | 14:15 SGT 07-06-26 | v0.61.364 — OTHER-picker pill font −2 + city-flush layout | 1 code file | 1 PR | 1 build |

- **[INTENT]** Tighten the Cuisine OTHER picker's location row: the closed country/city pills were a touch large, and a gap sat between them.

- **[MECHANISM]**
  - **Pill font −2** — the **closed** `CountryDropdown` and `CityDropdown` buttons drop `text-sm` → `text-xs` (14 px → 12 px). The **open** picker-list items are untouched (country list keeps `text-sm`; city list keeps `text-[13px]`), so legibility while choosing is preserved. Both lists already scroll (`max-h-72 overflow-y-auto`).
  - **City flush to country** — `CountryDropdown` + `CityDropdown` are wrapped in a `flex items-center flex-shrink-0` (gap-0) group, so the two pills butt together against the country's right border; the outer row keeps its `gap-1.5` before the free-text input.

- **[DELTA]**
  - **`web/cuisine/src/v2/components/LocationField.jsx`** — CountryDropdown button `text-sm → text-xs`; CityDropdown button `text-sm → text-xs`; wrapped both pills in a gap-0 group.
  - **`package.json`** — `0.61.363 → 0.61.364`. **`doc/.serial-state.yml`** — commit 576 → 577, journal 563 → 564, build 361 → 362; re-anchored 14:15 SGT.

- **[VERIFICATION (sandbox)]** `npm run build` (cuisine) clean; full `npx vitest run` → **106 files / 3007 tests green**.

- **[STATUS]** PR-**OPEN**, **auto-merge to arm on the operator's word**. Cuisine TMA. Field test: open the OTHER picker (e.g. VN / PQC) → the country + city pills are slightly smaller and sit flush together; the picker lists are unchanged and still scroll.

- **[TEST]** Operator: the country + city dropdowns read 2 px smaller and the city now hugs the country's right edge; tapping either still opens a full-size, scrollable list.

- **[KNOWN GAPS / FOLLOW-UP]**
  - **Mismatch / auto-load gating (NEXT PR)** — root-caused: the v0.61.322 splash gate opens + fires `runInitialLoad` **before** `state.countryPref` finishes loading async, so the v0.61.274 `coherenceMismatch` modal pops up **after** "⏳ loading random eateries…" already started (operator's Image 1 double-state). Fix planned: a **0.3 s settle** before opening the gate + a `modalPendingRef` re-check; then resolve **street + building + city**, write it to the **per-device** location table (v0.61.363), and only then auto-load.
  - **Device-ID question** — answered: yes, the resolved location is written to the **per-device** slot from v0.61.363.
  - **Not live-E2E tested** — no Telegram WebView here.

- **Confirmation Gates**

  | Gate | Resolution | Date |
  |---|---|---|
  | G1 — version bump | PATCH `0.61.363 → 0.61.364`. | 07-06 '26 |
  | G2 — destructive action | Two Tailwind class tweaks + a wrapper div; no logic change; full suite green. | 07-06 '26 |
  | G3 — new decision rule | Closed location pills render 2 px smaller; the city pill butts flush against the country pill. | 07-06 '26 |
  | G4 — paid external API call | None. | 07-06 '26 |

## Amendments

- **`web/cuisine/src/v2/components/LocationField.jsx`** — pill font −2 + city-flush layout.
- **`package.json`** — `0.61.363 → 0.61.364`.
- **`doc/.serial-state.yml`** — counters (commit 577 / journal 564 / build 362) + anchor 14:15 SGT 07-06-26.

## Deleted

- _None._
