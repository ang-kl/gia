(№ 547 - 06-06 '26 20:55 SGT) v0.61.347

# Journal — soleat v0.61.347 (Remove Laos / Cambodia / Myanmar from the OTHER-mode picker — no Michelin guide there)

> Operator: *"remove: Laos, Cambodia, Myanmar"*. None of the three has a Michelin guide, so they're dropped from the OTHER-region location picker (country list + city lists), in both the cuisine and menu Mini-Apps.
>
> **Left OPEN for review — auto-merge NOT armed.** Commit 560 / journal 547 / build 346. TMA source-only (Railway rebuilds the bundles).

## New

### [HDR] #547 | 20:55 SGT 06-06-26 | v0.61.347 — drop KH / LA / MM from the OTHER picker (countries + cities), both cuisine + menu | 4 code files | 1 PR | 1 build |

- **[INTENT]** Prune the three ASEAN countries with no Michelin guide from the OTHER-mode picker so users only see countries the app meaningfully serves. (Part 2 of the operator's 3-part request; part 1 = the v0.61.346 search wiring; part 3 = expand the remaining countries' picker cities to the Michelin-listed ones — still due.)

- **[MECHANISM]** The OTHER picker reads two frozen lists: `countries.js` (`OTHER_COUNTRIES`) and `cities.js` (per-country city arrays). Removed the `KH`/`LA`/`MM` entries from `OTHER_COUNTRIES` (16 → 13) and their Cambodia/Laos/Myanmar city blocks from `cities.js`, in **both** copies (`web/cuisine/src/v2/lib/` + `web/menu/src/`). **Deliberately left untouched:** `cuisine-i18n.js` (where "Burmese"/"Laotian" are cuisine *types*, not locations), and `iata-cities.js` + `coords-to-country.js` (the geo-detection reference table — used to recognise where the user physically *is*, independent of the curated picker). The cities.js→iata-cities invariant still holds (removing picker cities only shrinks the set being checked).

- **[DELTA]**
  - **`web/cuisine/src/v2/lib/countries.js`** / **`web/menu/src/countries.js`** — removed the `KH` / `LA` / `MM` rows from `OTHER_COUNTRIES`.
  - **`web/cuisine/src/v2/lib/cities.js`** / **`web/menu/src/cities.js`** — removed the Cambodia / Laos / Myanmar city blocks (bounded by the `// Cambodia.` … `// Australia.` comments; the four blocks were byte-identical across copies).
  - **`package.json`** — `0.61.346 → 0.61.347`.
  - **`doc/.serial-state.yml`** — commit 559 → 560, journal 546 → 547, build 345 → 346; re-anchored 20:55 SGT.

- **[VERIFICATION (sandbox)]**
  - `npm run build` (all 5 TMAs) — clean (the cities.js→iata-cities invariant test still passes).
  - `npx vitest run` → **105 files / 2985 tests green** (3 fewer than 2988: the suite parametrises over the country list, so dropping 3 countries dropped 3 per-country iterations; **0 failures** — no test asserted KH/LA/MM presence).
  - Grep confirms no `Cambodia`/`Laos`/`Myanmar` references remain in the 4 picker files.

- **[STATUS]** PR-**OPEN**, **auto-merge NOT armed**. TMA source-only; Railway rebuilds the cuisine + menu bundles on merge. Eyes-on-live: OTHER picker no longer lists Laos / Cambodia / Myanmar.

- **[TEST]** Operator: the OTHER-mode country dropdown now shows 13 countries (was 16); Laos, Cambodia, Myanmar are gone. Cuisine *types* (Burmese, Laotian food) are unaffected.

- **[KNOWN GAPS]**
  - **Part 3 — expand picker cities to the Michelin-listed ones** — still due (CN's 9 extra cities, TW New Taipei, HK "Hong Kong", a Macau block, PH cities, etc.). Needs real city centroids; will source from the existing `iata-cities.js` reference (no invented coordinates). Separate PR.
  - **Macau (`MO`) still not in the OTHER country picker** — folds into part 3.
  - A `doc/Feature/` / `doc/Technical/` / `doc/Register/` update + a `vault/v0.61.347/` snapshot are still due.

- **Confirmation Gates**

  | Gate | Resolution | Date |
  |---|---|---|
  | G1 — version bump | PATCH `0.61.346 → 0.61.347`. | 06-06 '26 |
  | G2 — destructive action | Removal of 3 country + 3 city blocks from the picker — operator-requested. Both Mini-App copies kept in sync; geo-detection + cuisine-type data untouched; full suite green. | 06-06 '26 |
  | G3 — new decision rule | The OTHER-mode picker lists only countries the app serves; Laos / Cambodia / Myanmar (no Michelin guide) are excluded. | 06-06 '26 |
  | G4 — paid external API call | _n/a_. | 06-06 '26 |

## Amendments

- **`web/cuisine/src/v2/lib/countries.js`** / **`web/menu/src/countries.js`** — `OTHER_COUNTRIES` − KH/LA/MM (16 → 13).
- **`web/cuisine/src/v2/lib/cities.js`** / **`web/menu/src/cities.js`** — − Cambodia/Laos/Myanmar city blocks.
- **`package.json`** — `0.61.346 → 0.61.347`.
- **`doc/.serial-state.yml`** — counters (commit 560 / journal 547 / build 346) + anchor 20:55 SGT.

## Deleted

- OTHER-picker entries for **Cambodia (KH)**, **Laos (LA)**, **Myanmar (MM)** — country rows + city blocks, both Mini-App copies. (Geo-detection `iata-cities.js`/`coords-to-country.js` retain their airports — those recognise physical location, not the picker.)
