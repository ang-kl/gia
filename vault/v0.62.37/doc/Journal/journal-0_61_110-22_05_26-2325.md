(№ 317 - 22-05 '26 23:25 SGT) v0.61.110

# Journal — soleat v0.61.110 (Cuisine map zoom ceiling + info-card text)

> Operator map-polish feedback (with two screenshots — /app/map and the
> Transport station popup), four points:
> 1. *"The zoom for Cuisine TMA for loading results and search criteria
>    - ceiling is 11 level and not beyond 10 unless the results are
>    extreme both ends of Singapore Island."*
> 2. *"The attractions zoom effects in google map embedded in Cuisine
>    TMA, Hawker TMA, and Train TMA should follow the other TMA (View 3
>    Train Stations, Compare all 5 carparks, show all 5 bus stops, view
>    all on map)."*
> 3. *"The result info card text should be one size larger, or same
>    throughout the different TMA (Claude to propose first)."*
> 4. *"no 🗺 for View all # picks on map."*
>
> Points 1, 3, 4 are this PR. Point 2 is deferred — see [KNOWN GAPS].
> This entry is commit 317 / journal 310 / build 113, off `main` at
> v0.61.109 (#602).

## New

### [HDR] #310 | 23:25 SGT 22-05-26 | v0.61.110 — Cuisine zoom ceiling + info-card text | ~9 kb | 6 files | 1 PR | 1 build |

- **[INTENT]** Operator feedback on the embedded-map polish: cap the
  Cuisine result auto-zoom, enlarge + unify the InfoWindow text across
  the three TMA maps, and drop the 🗺 glyph from the Cuisine results
  footer button.

- **[DELTA]**
  - **`web/cuisine/src/v2/components/MapPanel.jsx`** — the result
    auto-fit `maxZoom` lowered **16 → 11**. A tight venue cluster now
    settles at zoom 11; spread-out picks zoom out naturally and drop
    below 10 only when results span opposite ends of the island. The
    post-fit `idle` listener still restores `maxZoom: null` for manual
    zoom-in. Operator confirmed "zoom 11 ceiling" via AskUserQuestion.
  - **Info-card text — one size larger + unified** (operator approved
    the "13/12 unified" proposal via AskUserQuestion):
    - `web/{cuisine,hawker,transport}/.../mapOverlays.js` — the shared
      `infoCard` base `font-size:12px → 13px`; the two overlay
      sub-lines at `11px → 12px` (attraction-exits line + the hospital
      category label). cuisine edited, copied byte-identical to hawker
      + transport.
    - `MapPanel.jsx` — the Cuisine venue popup body lines `11px → 12px`
      (×6) and the "you are here" anchor caption `10.5px → 12px`. Every
      TMA-map popup is now 13px heading / 12px body.
  - **`web/cuisine/src/components/ResultsGrid.jsx`** — removed the 🗺
    glyph from the "View all N picks on map" results footer button.
  - **`package.json`** — `0.61.109 → 0.61.110` (PATCH).

- **[VERIFICATION (sandbox)]**

  ```
  npm run build    all 5 TMAs build clean
  npm test         1944/1944 passing, 67 files
  ```

- **[STATUS]** DRAFT — PR #603 on `claude/v0.61.110-map-polish` (off
  `main` at v0.61.109, `65e5969`). On merge deploys via the Railway
  auto-deploy on `main`.

- **[TEST]** Operator: run a Cuisine search — the map should frame at
  ≤ zoom 11; a venue / overlay popup should read one size larger; the
  results footer button should no longer show 🗺.

- **[KNOWN GAPS]**
  - **Point 2 deferred** — the operator asked attraction maps to
    "follow the other TMA (View 3 Train Stations …)". That reference is
    the bot-rendered `View {n} Train Stations` / `Compare all {n}
    carparks` / `Show all {n} bus stops` buttons (`i18n.js` lines
    302/135/307, `index.js` ~3281/3422/3817) which open `/app/map` and
    `fitBounds(bounds, 80)` to frame the features. There is no
    attractions search flow in the bot, so where a "View {n}
    Attractions" equivalent should live (a TMA overlay affordance? a
    new bot flow?) is unresolved — needs one more operator decision.
  - No in-sandbox UI verification (no Maps webview).
  - `doc/VibeCodingRecord/` regen still due — ~50 PRs behind.

- **Confirmation Gates**

  | Gate | Resolution | Date |
  |------|-----------|------|
  | G1 — version bump | PATCH `0.61.109` → `0.61.110` | 22-05 '26 |
  | G2 — destructive action | _n/a_ — additive UI tweaks | 22-05 '26 |
  | G3 — new decision rule | _n/a_ — operator chose "zoom 11 ceiling" + "13/12 unified" via AskUserQuestion | 22-05 '26 |
  | G4 — paid external API call | _n/a_ | 22-05 '26 |

## Amendments

_None._

## Deleted

_None._
