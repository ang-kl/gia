(№ 605 - 08-06 '26 23:01 SGT) v0.61.405

# Journal — soleat v0.61.405 (Loading indicator: drop the bottom toast, just an animated hourglass)

> Operator: *"my mistake to move the loading message to the bottom, remove it instead animate the hourglass."*
>
> Commit 618 / journal 605 / **build 389**. web/cuisine. Revert-ready (one branch in ResultPanel).

## New

### [HDR] #605 | 23:01 SGT 08-06-26 | v0.61.405 — animated hourglass replaces the v0.61.403 bottom toast | ResultPanel.jsx | 1 PR | 1 build |

- **[INTENT]** The **v0.61.403** bottom-anchored loading toast was the wrong call. The operator wants the relocated message **removed** — just an **animated hourglass**.

- **[DELTA]** (`web/cuisine/src/v2/components/ResultPanel.jsx` — the `loading` branch only)
  - Dropped the bottom toast (message + spinner pill). The loading state is now a **no-text, centred, inline animated hourglass** (`⏳` `animate-spin`, `text-2xl`; `aria-label` from `loadingHint`), back where the loading indicator always sat.
  - The v0.61.403 **progressive streaming reveal is kept** — the complaint was the relocated *message*, not the one-by-one card reveal (which renders fine).
  - **`package.json`** `0.61.404 → 0.61.405`; **`doc/.serial-state.yml`** commit 617 → 618, journal 604 → 605, **build 388 → 389**; re-anchored 23:01 SGT.

- **[VERIFICATION (sandbox)]** Cuisine build green. Full suite green.

- **[STATUS]** PR-**OPEN**, **auto-merge armed (squash)**. Railway redeploys on merge.

- **[TEST]** Operator: open the Cuisine TMA → during a search the loading state is a small **spinning hourglass** (no bottom toast, no message), then cards stream in.

- **[KNOWN GAPS / the other two points this turn]**
  - *"why didn't build the result"* + *"why still ask keep/use — I thought a previous PR is not to ask"* → diagnosed as the **same issue**: the **anchorMismatch keep/use modal** is a full-screen overlay (`z-40 bg-black/50`) that **blocks the results until the user picks**. The shown log is the **v0.61.403** container (14:18→14:51) — it **predates** v0.61.404's mismatch-sequence redeploy.
  - Whether to **remove the keep/use modal entirely** (auto-use the current device location, never ask) — which **reverses** the v0.61.404 *"go"* spec and touches the **hang-prone gate** — is **deferred to operator confirmation** (asked this turn). Not changed here.

- **Confirmation Gates**

  | Gate | Resolution | Date |
  |---|---|---|
  | G1 — version bump | PATCH `0.61.404 → 0.61.405`. | 08-06 '26 |
  | G2 — destructive action | Swaps the loading-branch UI; streaming + everything else untouched. | 08-06 '26 |
  | G3 — new decision rule | Loading state = a plain animated hourglass (no relocated message / toast). | 08-06 '26 |
  | G4 — paid external API call | None. | 08-06 '26 |

## Amendments

- **`web/cuisine/src/v2/components/ResultPanel.jsx`** — loading branch: bottom toast → inline animated hourglass.
- **`package.json`** — `0.61.404 → 0.61.405`.
- **`doc/.serial-state.yml`** — counters (commit 618 / journal 605 / build 389) + anchor 23:01 SGT 08-06-26.

## Deleted

- The v0.61.403 bottom-anchored loading toast (superseded by the hourglass).
