(№ 198 - 16-05 '26 16:40 SGT) v0.60.223

# Journal — soleat v0.60.223

> Operator recreated the Copy-button result template and reported *"Also missing are some of the fields"* — the `/api/cuisine/copy-one` card was missing the 🍽️ restaurant type, the price-range / ♿️ / 🐾 row and the ✳️ Michelin badge, and the operator's template re-ordered the `🌿` block to sit **below** the `🍲 Try` line.

## New

### [HDR] #191 | 16:40:00 SGT 16-05-26 | v0.60.223 — Copy-card field order + the missing fields | 4 files | 1 PR | 1 build |

- **[INTENT]** Make the `/api/cuisine/copy-one` card match the operator's recreated template — both the **row order** and the **fields present**.

- **[DELTA]**
  - **Missing fields — `web/cuisine/src/v2/components/ResultCard.jsx` `copy()`.** Root cause: `copy()` POSTs a hand-listed subset of venue fields, and `/api/cuisine/copy-one` passes that body **whole** to `formatVenueBlock` — so any field `copy()` omits simply never renders. Added to the POST body: `restaurantType`, `priceRangeDisplay`, `allowsDogs`, `wheelchairAccessible`, `signatureDish`, `vibe`, `recentReview`, `michelinCategory`, `michelinYear`. No server change needed — `formatVenueBlock` already renders each of these when present.
  - **Row order — `venue-templates.js` `formatVenueBlock`.** Re-sequenced the middle of the block to the operator's template: name → 🍽️ type → 📇 address → 🕰️ hours → 🌐/📞 contact → **🌟 stats → price/♿️/🐾 → 🍲 Try → 🌿 Quiet/Seating** → footfall → rain → 🚊/🚘 → 📍 → ✳️ Michelin. The `🌿` Sanctuary block moved from *above* stats to *below* the `🍲 Try` line; the price/pet row moved from after-rain to right after stats.
  - **♿️ on the price row — `formatPriceAndPetLine`.** The line now also carries `♿️` when `p.wheelchairAccessible === true` (parity with the TMA ResultCard's combined price·♿️·🐾 row). Order: `price-range · ♿️ · 🐾 Pet allowed`.
  - `package.json` `0.60.222 → 0.60.223`; this Journal entry (#191); `doc/.serial-state.yml` (`journal 190→191`, `commit 197→198`, `build 6→7`; re-anchored).

- **[VERIFICATION (sandbox)]**

  ```bash
  $ node --check venue-templates.js     → OK
  $ (cd web/cuisine && npm run build)   → built in 3.00s
  $ npm test -- --run                   → 1889 passed (1889)
  ```

- **[STATUS]** STAGED → PROD on next Railway deploy. The Cuisine TMA bundle is rebuilt on deploy.

- **[TEST]** Post-deploy: Cuisine TMA → tap a venue's 📋 Copy → the chat card carries, in order — name, 🍽️ type, 📇 address, 🕰️ hours, 🌐 website, 📞 phone, 🌟 stats, price-range · ♿️ · 🐾, 🍲 Try · dishes, 🌿 Quiet / 🌿 Seating, 🚊/🚘, 📍, ✳️ Michelin (each row only when its data exists).

- **[KNOWN GAPS]**
  - The 🌿 reorder is in `formatVenueBlock` — it applies to **every** `detail-with-sanctuary` consumer (Copy-one, Copy-all, free-text deliverPicks), so all those cards now show the Sanctuary block below the Try line. Consistent and intended.
  - Remaining operator areas: D (map pins — 4a/4b), E (Train TMA polylines 5a-5d, then the 178-station exit dataset 5e).

## Confirmation Gates

| Gate | Authority | Stamp |
|---|---|---|
| G1 — version bump | _PATCH 0.60.222 → 0.60.223._ | auto |
| G2 — destructive action | _None._ | n/a |
| G3 — new decision rule | _Operator: the detail-with-sanctuary venue card row order is name → type → address → hours → contact → stats → price/♿️/🐾 → 🍲 Try → 🌿 Quiet/Seating → travel → maps → Michelin; the TMA Copy button forwards the full venue field set._ | operator |
| G4 — paid external API call | _None._ | n/a |

## Amendments

| # | Item | Prior (verbatim) | Amended to | Date | Rule | Serial № |
|---|---|---|---|---|---|---|
| O-4 | `venue-templates.js` `formatVenueBlock` row order | `… contact → 🌿 sanctuary → 🌟 stats → 🍲 Try → footfall → rain → price/pet → travel …` | `… contact → 🌟 stats → price/♿️/pet → 🍲 Try → 🌿 sanctuary → footfall → rain → travel …` | 16-05 '26 | AU-7 | (№ 198 — 16-05 '26 16:40 SGT) |
| O-5 | `venue-templates.js` `formatPriceAndPetLine` | `price-range` + `🐾 Pet` only | adds `♿️` when `wheelchairAccessible === true` | 16-05 '26 | AU-7 | (№ 198 — 16-05 '26 16:40 SGT) |

## Deleted

(Nothing deleted. `formatVenueBlock`'s middle rows are re-ordered in place; the prior order is recorded in the Amendments table above.)
