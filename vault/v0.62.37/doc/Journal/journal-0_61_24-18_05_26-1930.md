(№ 233 - 18-05 '26 19:30 SGT) v0.61.24

# Journal — soleat v0.61.23 → v0.61.24 (exit feature, Part A)

> Operator: train-station exits should show their letter/number, not an icon; tapping an
> exit opens an Exit Template (exit code, station name, colour-coded station codes).

## New

### [HDR] #226 | 19:30:00 SGT 18-05-26 | v0.61.24 — exit feature Part A: code pins + Exit Template | ~9 kb | 5 files | 1 PR | 1 build |

- **[INTENT]** Operator review of the TMA maps: *"All train stations exit are either shown
  in alphabets or numbers, replace icons with this criteria. When i click on the exit, it
  should show: <Exit Code> [bg colour per station code] / <Station Name> / <Station codes,
  colour-coded per line> / ♿️ handicap lift."* Scoped (operator decision) into **Part A**
  — the rendering, derivable from existing data — and **Part B** — lift availability +
  surrounding buildings, which needs a build-time scrape (`doc/observability-expansion-plan.md`
  is a separate, parallel ask). This entry is Part A. The ♿ lift line is Part B.

- **[DELTA]**
  - **`scripts/build-geo-overlays.js`** — `convertPoint` now enriches each `exits` feature
    with `exitCode` (bare letter/number), `station` (name) and `codes` (the station's line
    codes, joined from `data/mrt-coords.json` via the existing `nearestStation`).
  - **`data/geo-exits.json`** — regenerated: 597 exits now carry `exitCode` / `station` /
    `codes`. (`/api/geo/overlays` passes feature fields through verbatim — no endpoint
    change.)
  - **`web/{cuisine,hawker,transport}/src/.../lib/mapOverlays.js`** (byte-identical ×3) —
    new module helpers `codeHex` (code → line colour), `codePill`, `exitTemplateHtml`.
    New controller `buildExitMarkers` draws each exit as an `amenityLabelNode` pill of the
    exit letter/number coloured by the station's primary line (replacing the `🚆` `dotNode`
    via `buildMarkers`); `exitInfo` opens the Exit Template. `attachAmenityPins`' exit pins
    (station-detail tap view) are unified to the same line colour + Exit Template.
  - **`doc/observability-expansion-plan.md`** — separate plan for the operator's analytics
    ask (latency / request volume / venue popularity / UX funnel), per operator request.

- **[VERIFICATION (sandbox)]**

  ```bash
  $ node scripts/build-geo-overlays.js   → exits: 597 features (0 missing station/codes)
  $ node --check mapOverlays.js          → OK (byte-identical ×3)
  $ npx vite build  (cuisine/hawker/transport)  → all green
  $ npm test -- --run                    → 1924 passed (1924)
  ```

- **[STATUS]** DRAFT → PR (base `main`, branch `claude/review-gia4lunch-readme-3s798`).

- **[TEST]** Each TMA map: the Exits layer draws letter/number pills coloured by line; a
  tap opens the Exit Template (exit-code header, station name, colour-coded code pills);
  station-detail exit pins match.

- **[KNOWN GAPS]** Part B still due — ♿ lift availability + surrounding buildings/
  attractions, scraped (build-time data file) from journey.smrt.com.sg (NSL/EWL/CCL/TEL/
  BPLRT) and sbstransit.com.sg (DTL/NEL/SLRT/PLRT); SMRT site is a JS SPA — scrape
  feasibility to be verified first. Observability expansion (`doc/observability-expansion-plan.md`)
  is planned but not built.

## Amendments

(none)

## Deleted

Superseded by v0.61.24: the `exits` overlay layer's `buildMarkers(d.exits, '#5E35B1',
'🚆', nameInfo)` call (purple `🚆` emoji dot + name-only popup) is replaced by
`buildExitMarkers` (line-coloured exit-code pill) + `exitInfo` (Exit Template). The
station-detail exit pin's mini "🚪 Exit A / Station MRT" card is replaced by the shared
`exitTemplateHtml`. Preserved here per Recipe AU-5.

## Confirmation Gates Satisfied

| Gate | Authority | Stamp |
| :--- | :--- | :--- |
| G1 — version bump (PATCH: 0.61.23 → 0.61.24) | _operator: 0.61.x PATCH sequence_ | 18-05 '26 |
| G2 — destructive action | _code-only: exit emoji-dot rendering superseded; logged in Deleted_ | 18-05 '26 |
| G3 — new decision rule | _operator scoped the exit feature into Part A / Part B; build-time data file for scraped data_ | 18-05 '26 |
| G4 — paid external API call | _n/a_ | — |
