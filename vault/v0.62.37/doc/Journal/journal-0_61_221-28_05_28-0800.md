(№ 422 - 28-05 '26 08:00 SGT) v0.61.221

# Journal — soleat v0.61.221 (Add Singapore to Menu TMA country dropdown)

> Operator (28-05 '26): *"Add Singapore in part of the dropdown
> list at the bottom for Menu TMA only, as Cuisine TMA we can
> toggle Singapore, Johor Bahru. When Select Singapore in the Menu
> TMA, and user type in the location it should behavior like what
> has been coded and not change. But if user select Malaysia in
> Menu TMA, it should have the cities to select."*
>
> Scope of this PR is just the SG-in-dropdown half. The
> Malaysia-cities child dropdown is part of the v0.61.222+ arc
> tracked separately.
>
> Commit 434 / journal 422 / build 224, on branch
> `claude/v0.61.221-sg-menu-dropdown` off `main` at v0.61.220.

## New

### [HDR] #422 | 08:00 SGT 28-05-28 | v0.61.221 — SG at bottom of Menu TMA dropdown | 4 files | 1 PR | 1 build |

- **[INTENT]** Let a user anchored in OTHER (Bangkok / Tokyo / KL / …) flip back to SG via the existing Menu TMA flag dropdown without leaving the picker.

- **[DELTA]**

  **1. `web/menu/src/countries.js`** — appended one entry:

  ```js
  { code: 'SG', flag: '🇸🇬', name: 'Singapore' }
  ```

  at the bottom of `OTHER_COUNTRIES`. Updated the file header
  comment to record the intentional divergence from
  `web/cuisine/src/v2/lib/countries.js` (the Cuisine TMA still uses
  a SG/JB pill outside its dropdown, so its list stays SG-less).

  **2. `web/menu/src/components/LocationFieldMenu.jsx`** — removed
  the cross-TMA seed-skip:

  ```js
  // Was (v0.61.209):
  if (body.countryCode === 'SG') return;
  ```

  'SG' is now a valid `OTHER_COUNTRIES` code so it flows through
  the `country-pref` sync like any other entry. If the user
  previously toggled the Cuisine TMA's SG pill, the Menu TMA's
  flag dropdown now reflects that selection when in OTHER mode.

  **3. `package.json`** — `0.61.220 → 0.61.221`.

  **4. `doc/.serial-state.yml`** — commit 433→434, build 223→224, journal 421→422; re-anchored 28-05 '26 08:00 SGT.

- **[VERIFICATION (sandbox)]**

  Local Vite build hit the same iCloud / Node-24 ESM PostCSS
  parser quirk that bit us during v0.61.219 vitest (file resolves
  via `JSON.parse` but Node's internal `package_json_reader` is
  flaky for files under `~/Library/Mobile Documents/`). CI runs on
  a clean Linux box, so the TMA-build job will validate.
  Code change is one frozen-array entry + one conditional removal;
  zero functional risk to existing paths.

- **[STATUS]** PR-OPEN on push.

- **[TEST]** Operator (post-merge + Railway redeploy):
  1. From any non-SG anchor (e.g. `/location Bangkok`), open Menu TMA.
  2. The flag dropdown should now list 🇸🇬 Singapore at the bottom (after 🇹🇼 Taiwan).
  3. Pick 🇸🇬 → type `Marina Bay` → 🔍 → see SG results → tap one → form auto-flips back into SG mode (precinct dropdown reappears, autocomplete works), anchor region = `SG`.
  4. Picking 🇸🇬 then submitting an SG place that geocodes inside SG should NOT trigger any non-SG behaviour (no JB / MY-PUT tile-disable banner). This is the "behaviour like what has been coded and not change" guarantee.

- **[KNOWN GAPS]**
  - Malaysia state-capitals child dropdown is part of the v0.61.222+ arc, NOT this PR.
  - Cuisine TMA's flag dropdown still excludes SG (Cuisine has the SG/JB pill pair instead). Operator scope is "Menu TMA only".
  - The `place-search-by-country` endpoint with `countryCode='SG'` hasn't been audited; assumed to work because Google Places (New) `regionCode='SG'` is valid. Real validation comes from operator's post-deploy test.

- **Confirmation Gates**

  | Gate | Resolution | Date |
  |---|---|---|
  | G1 — version bump | PATCH `0.61.220 → 0.61.221`. Pure additive dropdown entry + one stale conditional removed. | 28-05 '26 |
  | G2 — destructive action | _n/a_. | 28-05 '26 |
  | G3 — new decision rule | YES (mild) — the two `countries.js` files (cuisine / menu) intentionally diverge from v0.61.221 onward; the byte-identical-duplicate invariant is dropped. | 28-05 '26 |
  | G4 — paid external API call | _n/a in this PR_. | 28-05 '26 |

## Amendments

- **`web/menu/src/countries.js`** — `OTHER_COUNTRIES` was 16 entries (ASEAN-9 minus SG, Oceania-2, North Asia-5). Now 17 with SG appended.
- **`LocationFieldMenu` seed effect** — was `if (body.countryCode === 'SG') return;`. Now lets SG through.

## Deleted

_None._
