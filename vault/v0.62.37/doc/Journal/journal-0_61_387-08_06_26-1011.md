(№ 587 - 08-06 '26 10:11 SGT) v0.61.387

# Journal — soleat v0.61.387 (First load: keep the wait message, never a fact card)

> Operator: *"first load cross 20 seconds threshold in cuisine tma"* → *"first load just ask user to wait, dont need fact card — only subsequent results"* → *"keep to current display message and not use fact card"*.
>
> **Left OPEN for review — auto-merge to be armed on the operator's word.** Commit 600 / journal 587 / build 379. web/cuisine.

## New

### [HDR] #587 | 10:11 SGT 08-06-26 | v0.61.387 — hold the 20s follow-sync until the boot load lands; no fact card on first load | location-follow · App | 1 PR | 1 build |

- **[INTENT]** The first load must show **only** the "Please wait while loading list…" banner — **no fact card** (those are for genuine subsequent searches).

- **[ROOT (traced)]** The "20-second threshold" is the **follow-device location sync** (`location-sync.js:58`, `durationMs = 20000`; polls GPS every 4s; re-centres + re-searches on a >1500m move). Its decision (`shouldFollowDevice`, `location-follow.js`) was gated only on `initialResolveDone` (location resolved, `App.jsx:810`) — **not** on `firstLoadPending` (the actual boot venue fetch, cleared at `App.jsx:1493/1704/1773/1947`). So a **slow first load** that crosses the 20s window could be **re-centred / re-searched mid-flight**; that re-search counts as **'rotating'**, which **pops a fact card** on the first load.

- **[DELTA]**
  - **`web/cuisine/src/v2/lib/location-follow.js`** — `shouldFollowDevice` gains a `firstLoadPending` param → returns `false` while it's true (the follow-sync **holds off** until the boot load lands).
  - **`web/cuisine/src/v2/App.jsx`** — NEW `firstLoadPendingRef` (mirrors `firstLoadPending` via a small effect), passed into the sync's `shouldFollowDevice` call. **Belt-and-suspenders:** a guard at the top of the fun-fact effect — `if (firstLoadPendingRef.current) → setFunFact(null)` — so a fact card can **never** show during the first load, regardless of `loadingReason`.
  - **`__tests__/location-follow.test.js`** — +2 (pending → `false`, landed → `true`); 12 there.
  - **`package.json`** `0.61.386 → 0.61.387`; **`doc/.serial-state.yml`** commit 599 → 600, journal 586 → 587, **build 378 → 379**; re-anchored 10:11 SGT.

- **[VERIFICATION (sandbox)]** Cuisine build green. Full suite **113 files / 3094 tests** (+2).

- **[STATUS]** PR-**OPEN**, **auto-merge to arm on the operator's word**. Railway redeploys on merge.

- **[TEST]** Operator: on first load the Cuisine TMA now stays on the "Please wait…" banner through the 20s window — no fact card, no mid-load re-centre. Fact cards still appear on later searches.

- **[KNOWN GAPS]**
  - The follow-sync now **defers** entirely if the first load outlasts its 20s window (it never re-fires after that) — acceptable per the operator (first load should just wait; subsequent searches handle location normally).
  - Mindful of this area's prior hang history: the change only **suppresses** the sync / fact card during first load (no new blocking dependency); the boot load itself is untouched.
  - Not live-E2E tested.

- **Confirmation Gates**

  | Gate | Resolution | Date |
  |---|---|---|
  | G1 — version bump | PATCH `0.61.386 → 0.61.387`. | 08-06 '26 |
  | G2 — destructive action | None — additive guards; the follow-sync + fact card are only *suppressed* during first load. | 08-06 '26 |
  | G3 — new decision rule | The first load shows only the wait banner — no follow-sync re-search and no fact card until it lands. | 08-06 '26 |
  | G4 — paid external API call | None. | 08-06 '26 |

## Amendments

- **`web/cuisine/src/v2/lib/location-follow.js`** — `firstLoadPending` guard in `shouldFollowDevice`.
- **`web/cuisine/src/v2/App.jsx`** — `firstLoadPendingRef` + sync guard + fact-card guard.
- **`__tests__/location-follow.test.js`** — +2 tests.
- **`package.json`** — `0.61.386 → 0.61.387`.
- **`doc/.serial-state.yml`** — counters (commit 600 / journal 587 / build 379) + anchor 10:11 SGT 08-06-26.

## Deleted

- _None_ (additive).
