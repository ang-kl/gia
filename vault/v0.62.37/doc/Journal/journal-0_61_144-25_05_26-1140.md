(№ 345 - 25-05 '26 11:40 SGT) v0.61.144

# Journal — soleat v0.61.144 (Register increment — logs D-61.140 / D-61.141 / D-61.142)

> Operator: *"do the 2"* — item 2 from the next-up shortlist after
> PR #644's ledger catch-up. The v0.61.140 (wake 2-step), v0.61.141
> (special-mode chips + mutex), and v0.61.142 (per-slug imgFlag
> override) PRs all landed today; each was decision-bearing but
> the Register hadn't reflected those decisions yet. Pure docs.
>
> Commit 357 / journal 345 / build 147 / register 122, on branch
> `claude/v0.61.144-register-d140-d141-d142` off `main` at v0.61.143
> (commit `dec357e`, PR #644 — ledger catch-up).

## New

### [HDR] #345 | 11:40 SGT 25-05-26 | v0.61.144 — Register logs D-61.140 / D-61.141 / D-61.142 (docs only) | 3 files | 1 PR | 1 build |

- **[INTENT]** Append three new Decision rows to the Register so
  the v0.61.140-142 arc has the per-decision rationale captured
  alongside the per-PR journal entries. Carry-forward the existing
  Open Items + Retracted O-24 status unchanged.

- **[DELTA]**
  - **`doc/Register/register-0_61_144-25_05_26-1140.md`** (NEW) —
    incremental Register entry per AU-1. Three new Decision rows:
    - **D-61.140** — Wake-from-idle 2-step flow
      (`promptLocationOnWake` → `wake:pending` flag →
      `bot.on('location')` branch → `handleWakeLocationResponse`
      → rich `wake2.body` HTML message with 3 inline buttons +
      `/l` helper text). The v0.61.84 keep/new strings + callbacks
      retained for back-compat. Rationale cites the operator
      screenshot of the stale wake message and the bot's inability
      to read GPS unsolicited.
    - **D-61.141** — Special-mode toggles are catalogue chips, not
      amber pills. Fruits / Durian / Durian Pastry live as regular
      chips inside the "Dessert, Fruits" group; symmetric mutex
      enforced by `applyChipToggle`; Durian narrowed to fruit-only
      (NAME_REJECT_PATTERNS regex list); NEW DURIAN_PASTRY mode
      with its own seeds + keywords. Rationale cites the operator
      screenshot + written spec; codifies the recipe so future
      special-mode additions follow the same pattern.
    - **D-61.142** — Per-slug image-asset override for catalogue
      chips. `IMG_FLAG_BY_SLUG` map in `cuisines-vault.js`; chip
      renderer renders `<img>` when set, falls back to the emoji
      `flag` (or default 🍽️) on load error. First registered slug:
      `'durian' → 'durian.png'`. Rationale cites the operator's
      PNG upload + establishes the recipe for future per-slug
      assets.
  - Carry-forward Open Items section: O-21 (vitest+Node24,
    unchanged), O-22 (iCloud throttle, unchanged + documented),
    O-24 (retracted in v0.61.137, status row for visibility),
    plus the closed O-20 / O-23 from v0.61.131 (visibility rows).
  - Removed Features table: empty (no items deleted in this arc).
  - **`package.json`** — `0.61.143 → 0.61.144`.
  - **`doc/.serial-state.yml`** — `commit 356 → 357`, `build 146
    → 147`, `journal 344 → 345`, `register 121 → 122`; re-anchored
    25-05 '26 11:40 SGT (same SG-API fetch as v0.61.143, rounded
    forward 5 min); new comment-history block prepended.
  - **`doc/Journal/journal-0_61_144-25_05_26-1140.md`** — this
    entry.

- **[VERIFICATION (sandbox)]**

  ```
  $ grep -nE '^\| D-61\.14[012] ' \
        doc/Register/register-0_61_144-25_05_26-1140.md
       (3 matches — one row per new decision)
  $ grep -c 'register: 122' doc/.serial-state.yml
       1
  ```

  No code change → `node --check` / `vitest` / `npm run build`
  not applicable.

- **[STATUS]** PROD-bound on squash-merge to `main`. Docs only;
  no Railway build behaviour change.

- **[TEST]** Operator (post-deploy): `cat doc/Register/register-
  0_61_144-25_05_26-1140.md`. Decisions table reads D-61.140 →
  D-61.141 → D-61.142, each with a 25-05 '26 date stamp. Open
  Items section preserves O-21 / O-22 carry-forward state.

- **[KNOWN GAPS]**
  - **No new completion rows** in this entry — the v0.61.134-142
    PRs were operator-driven feature work + UX redesigns + asset
    adds, not Open-item closures. The Completed section refers
    out to `register-0_61_132-…` for C-BA / C-BB.
  - **Vault snapshot** for the v0.61.129-143 arc still operator-
    triggered.
  - **CLAUDE-FULL.md §17.8 elevation** of the plumbing-commit
    pattern carries forward — see PR #636's KNOWN GAPS.
  - **Durian Pastry icon** carry-forward: the operator said
    *"already in the system"* (presumably referring to the
    default emoji fallback being acceptable). If they later want
    a dedicated PNG, the recipe in D-61.142 covers it.

- **Confirmation Gates**

  | Gate | Resolution | Date |
  |------|-----------|------|
  | G1 — version bump | PATCH `0.61.143 → 0.61.144`. Docs-only PR, no behaviour change. | 25-05 '26 |
  | G2 — destructive action | _n/a_ — pure addition (new register file, append-only per AU-1). Prior register increments untouched. | 25-05 '26 |
  | G3 — new decision rule | _n/a_ — D-61.140 / .141 / .142 are LOGGING decisions already taken in PRs #641 / #642 / #643, not proposing new ones. | 25-05 '26 |
  | G4 — paid external API call | _n/a_ — docs. | 25-05 '26 |

## Amendments

_None — pure follow-up._

## Deleted

_None._
