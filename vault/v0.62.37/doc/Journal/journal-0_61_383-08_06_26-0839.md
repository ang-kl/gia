(№ 583 - 08-06 '26 08:39 SGT) v0.61.383

# Journal — soleat v0.61.383 (Global, device-localized fact cards + 15s rotation)

> Operator Task 1: the up-to-60s fetch should show an educational fact card, localized to the device language, re-triggering every 15s.
>
> **Left OPEN for review — auto-merge to be armed on the operator's word.** Commit 596 / journal 583 / build 376. web/cuisine. (Part **C** of 3 — completes the operator's 2-task spec.)

## New

### [HDR] #583 | 08:39 SGT 08-06-26 | v0.61.383 — global facts (8 langs) + device-lang body + 15s rotation | data/fun-facts · lib/fun-facts · FunFactModal · App | 1 PR | 1 build |

- **[INTENT]** During the long search wait, the fact-card modal showed **Singapore** food trivia in **English only**, as a **single** fact. A Tokyo/Seoul/Bangkok user got irrelevant, non-localized content. Make it **global**, **device-localized**, and **rotate every 15s**.

- **[DECISION]** Operator AskUserQuestion (after I flagged the translation-accuracy risk): **"Author all 6 languages now"** — provide en/fr (already) + zh/ms/ta/ja/ko/th, machine-authored and **provisional** (native-review flagged).

- **[PRIOR ART]** The mechanism already existed (v0.61.285 `FunFactModal` + `lib/fun-facts.js` + `data/fun-facts.js`) — so this **enhances**, not rebuilds. **Critical finding:** the app UI locale (`i18n.js SUPPORTED_LOCALES`) is **en|fr only**, so `useLocale()` can never be ja/ko/th. To localize the fact body to the device language the modal must read `navigator.language` **separately** — exactly Task 1's spec.

- **[DELTA]**
  - **`web/cuisine/src/v2/data/fun-facts.js`** — +8 **global** facts (ramen / sushi / kimchi / chilli / tea / noodles / coffee / umami), each with all **8 languages**, tagged `global` + `other` (the non-SG region fallback) + a country code (jp/kr/th/cn). Facts are well-known truths; source Wikipedia. **64 → 72** facts.
  - **`web/cuisine/src/v2/lib/fun-facts.js`** — `SUPPORTED_LANGS` en/fr → **en/fr/zh/ms/ta/ja/ko/th**; new **`deviceFactLang()`** = `navigator.language` primary subtag if supported, else `en` (independent of the en/fr app UI).
  - **`web/cuisine/src/v2/components/FunFactModal.jsx`** — the fact **body** now `factBody(fact, deviceFactLang())` (device language); the header / "curating" / source **chrome stays on the app locale** (en/fr).
  - **`web/cuisine/src/v2/App.jsx`** — the fun-fact effect was a **single** pick 1.5s after a rotating search; now picks the first at 1.5s, then **`setInterval` re-picks every 15 000 ms** until the search lands (operator's "re-trigger every 15s"). Cleanup clears both timers.
  - **`__tests__/fun-facts.test.js`** — +10 (→ 29): count 64→72, global facts carry all 8 langs + the `other` tag, `factBody` ja/ko/th resolution + EN fallback for SG facts, `deviceFactLang` (ja-JP→ja / ko / th / de→en via `vi.stubGlobal`), `_pickFact` surfaces jp/`other` global facts for non-SG context.
  - **`package.json`** `0.61.382 → 0.61.383`; **`doc/.serial-state.yml`** commit 595 → 596, journal 582 → 583, **build 375 → 376** (web rebuilt); re-anchored 08:39 SGT.

- **[VERIFICATION (sandbox)]** Cuisine build green (App bundle 259.94 → 266.65 kB from the facts data). Full suite **113 files / 3092 tests** (+11).

- **[STATUS]** PR-**OPEN**, **auto-merge to arm on the operator's word**. Railway redeploys on merge.

- **[TEST]** Operator: change the search to an overseas city — during the wait the floating card now shows a **global** food fact in your **device language**, swapping to a new one every **15 s**.

- **[KNOWN GAPS]**
  - **Provisional translations** — the zh/ms/ta/ja/ko/th fact phrasing is machine-authored. The *facts* are true; the *wording* should get a native-speaker review pass before it's fully trusted live.
  - **Scope:** only the `rotating` search wait shows facts. The fragile boot `initial` gate is **deliberately untouched** (past hang lessons this session). The up-to-60s **foreign** search (changing country/city) *is* a rotating search, so it's covered.
  - **Chrome:** the modal header / source labels stay en/fr (the app i18n is en/fr); only the fact body is device-localized.
  - **Webapp (`gia-web`)** mirrors this in its own repo as a follow-up.
  - Not live-E2E tested (no Telegram WebView).

- **Confirmation Gates**

  | Gate | Resolution | Date |
  |---|---|---|
  | G1 — version bump | PATCH `0.61.382 → 0.61.383`. | 08-06 '26 |
  | G2 — destructive action | None — additive (8 new facts, expanded lang set, rotation); existing SG facts + en/fr untouched. | 08-06 '26 |
  | G3 — new decision rule | Fact bodies localize to the **device** language (navigator.language), not the en/fr app UI; global facts tagged `global`+`other` are the non-SG fallback; rotate every 15s. | 08-06 '26 |
  | G4 — paid external API call | None — curated static content; no LLM, no API. | 08-06 '26 |

## Amendments

- **`web/cuisine/src/v2/data/fun-facts.js`** — +8 global facts × 8 languages (64 → 72).
- **`web/cuisine/src/v2/lib/fun-facts.js`** — `SUPPORTED_LANGS` expanded; `deviceFactLang()`.
- **`web/cuisine/src/v2/components/FunFactModal.jsx`** — device-language fact body.
- **`web/cuisine/src/v2/App.jsx`** — 15s rotation.
- **`__tests__/fun-facts.test.js`** — +10 tests.
- **`package.json`** — `0.61.382 → 0.61.383`.
- **`doc/.serial-state.yml`** — counters (commit 596 / journal 583 / build 376) + anchor 08:39 SGT 08-06-26.

## Deleted

- _None_ (additive).
