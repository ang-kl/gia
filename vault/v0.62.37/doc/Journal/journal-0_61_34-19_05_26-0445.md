(№ 245 - 19-05 '26 04:45 SGT) v0.61.34

# Journal — soleat v0.61.33 → v0.61.34 (/ver admin-command choices)

> Operator: *"Create other admin (ChatID) choices after typing '/ver', showing
> /ftlog, /admin, /log on, /log off."* — and, on the ambiguous "/admin" label:
> the "/admin" choice launches the Oversight admin TMA.
>
> Sequenced after PR #518 (v0.61.33 / commit 244) — this PR is commit 245 /
> journal 238.

## New

### [HDR] #238 | 04:45:00 SGT 19-05-26 | v0.61.34 — /ver admin-command choices keyboard | ~0.7 kb | 2 files | 1 PR | 0 build |

- **[INTENT]** After the owner types `/ver` (owner-only, `TELEGRAM_OWNER_CHAT_ID`-gated)
  and gets the health-check report, also surface the other owner-only commands as
  tappable inline-keyboard choices, so the operator does not have to remember / type
  the hidden commands.

- **[DELTA]**
  - **`index.js`** — the `/ver` `bot.onText` handler sends a follow-up
    `🛠 Admin commands` message after `ownerHealthCheck()`, carrying an inline keyboard
    of four choices: `📋 /ftlog` → `v:ftlog20`, `🛡 /admin` → `v:oversight`,
    `🔍 /log on` → `v:logon`, `🔍 /log off` → `v:logoff`. These reuse the existing
    `/v` builder-menu callback handlers (index.js:1971-1975) verbatim — no new callback
    routes. `/admin` maps to the Oversight admin TMA launcher per the operator's
    clarification. The keyboard send is best-effort (`.catch`) so a Telegram hiccup
    can't break the `/ver` path.
  - **`package.json`** — `0.61.33` → `0.61.34` (PATCH).

- **[VERIFICATION (sandbox)]**

  ```bash
  $ node --check index.js  → OK
  $ npm test  → 65 files / 1928 tests passed
  ```
  No `web/` change → no TMA build. The follow-up message text is a static string
  (`🛠 <b>Admin commands</b>`) — no user input interpolated into the `parse_mode:'HTML'`
  payload, so no escaping exposure.

- **[STATUS]** DRAFT → PR (base `main`, branch `claude/review-gia4lunch-readme-3s798`).

- **[TEST]** As the owner chat, send `/ver`: the health-check `<pre>` report arrives,
  followed by a `🛠 Admin commands` message with four buttons. Tapping `📋 /ftlog`
  dumps the free-text log; `🛡 /admin` opens the Oversight Mini App; `🔍 /log on` /
  `🔍 /log off` toggle verbose mode. Non-owner chats get nothing (silent no-op, unchanged).

- **[KNOWN GAPS]** None for this change. The `/v` builder menu still also lists
  `/ver` + `/oversight`; `/ver`'s own keyboard intentionally omits a self-referential
  `/ver` button. Map-controls redesign Phases C / D / E remain open.

## Amendments

(none)

## Deleted

(none)

## Confirmation Gates Satisfied

| Gate | Authority | Stamp |
| :--- | :--- | :--- |
| G1 — version bump (PATCH: 0.61.33 → 0.61.34) | _operator: 0.61.x PATCH sequence_ | 19-05 '26 |
| G2 — destructive action | _n/a — additive keyboard_ | — |
| G3 — new decision rule | _n/a_ | — |
| G4 — paid external API call | _n/a_ | — |
