(№ 634 - 10-06 '26 06:16 SGT) v0.61.434

# Journal — soleat v0.61.434 (Claude Opus tier migrated 4.7 → 4.8)

> Operator: `/claude-api migrate` → confirmed scope **"Opus → 4.8, live files only"** (offered: Opus-only / also re-alias Haiku / different target).

> Commit 647 / journal 634 / **build 409 (holds — server-only)**. llm-client.js + its test. Revert-ready.

## New

### [HDR] #634 | 06:16 SGT 10-06-26 | v0.61.434 — OPUS_MODEL default claude-opus-4-7 → claude-opus-4-8 | llm-client.js + test | 1 PR | 0 build |

- **[SURVEY (migration guide Step 0/1)]** The bot's **single Claude integration point** is `llm-client.js` (all callers go through this shim). Tiers: Sonnet `claude-sonnet-4-6` (current), Haiku `claude-haiku-4-5-20251001` (current), **Opus `claude-opus-4-7` (one generation behind)**. Other matches classified: `gemini-client.js:466-467` = telemetry labels reading the same `ANTHROPIC_MODEL` env default (current — no change); `tell-gia.js:220` `'claude-haiku'` = a log/source label, not a model ID (no change); `vault/**` = frozen historical snapshots (excluded — never edited).

- **[BREAKING-CHANGE SCAN]** Clean. No `temperature`/`top_p`/`top_k`/`budget_tokens` anywhere in `llm-client.js`. The `role: 'assistant'` at `:111` is the **documented `pause_turn` resume pattern** (re-send with the trailing `server_tool_use` assistant turn appended) — supported on 4.7/4.8, not a last-turn prefill. Per Anthropic's migration guide, **4.7 → 4.8 has no new breaking changes** — the migration is the model-ID swap.

- **[DELTA]** (server-only)
  - **`llm-client.js`** — `OPUS_MODEL` fallback `'claude-opus-4-7'` → `'claude-opus-4-8'` + the Defaults doc-comment line. The v0.40.1 historical changelog comment ("default changed *from* claude-opus-4-7 *to* sonnet…") is retained — it's accurate history. The `ANTHROPIC_OPUS_MODEL` env override still wins when set on Railway.
  - **`__tests__/llm-client.test.js`** — the fallback assertion updated in lockstep (`claude-opus-4-8`).
  - **`package.json`** `0.61.433 → 0.61.434`; **`doc/.serial-state.yml`** commit 646 → 647, journal 633 → 634, **build 409 holds**; re-anchored 06:16 SGT 10-06-26.

- **[VERIFICATION (sandbox)]** `node --check llm-client.js` clean. Full suite **3175/3175 green** (the updated assertion passes). No live API call possible in the sandbox (no key) — runtime spot-check is the operator's deployed step.

- **[STATUS]** PR-**OPEN**, **auto-merge armed (squash)**. Railway redeploys (server) on merge; the Opus tier takes effect on next boot unless `ANTHROPIC_OPUS_MODEL` is set in Railway env (then the env wins — unchanged behaviour).

- **[TEST — operator, deployed]** Any feature routed through the Opus tier (heavy-reasoning calls via `llm.generate({model: OPUS_MODEL,…})`) now runs on Opus 4.8. If `ANTHROPIC_OPUS_MODEL` is set in Railway, clear it (or set it to `claude-opus-4-8`) for the new default to apply.

- **[KNOWN GAPS]**
  - **Check Railway env:** if `ANTHROPIC_OPUS_MODEL=claude-opus-4-7` is explicitly set there, the code change is shadowed until the env var is updated/removed. [UNVERIFIED: I can't read Railway env from the sandbox.]
  - Haiku stays on the dated full ID `claude-haiku-4-5-20251001` (operator chose Opus-only; the bare alias swap was offered and declined).
  - 4.8 behavioral re-tuning ([TUNE] items — narration, ask-rate, tool-triggering) not applied: the shim's prompts are small/JSON-extraction-shaped and Gemini handles most NL work in this repo; revisit only if Opus-tier outputs read differently post-deploy.

- **Confirmation Gates**

  | Gate | Resolution | Date |
  |---|---|---|
  | G1 — version bump | PATCH `0.61.433 → 0.61.434`. | 10-06 '26 |
  | G2 — destructive action | ID swap with env-override escape hatch; vault untouched. Reversible. | 10-06 '26 |
  | G3 — new decision rule | Opus tier tracks `claude-opus-4-8` (env override still wins). | 10-06 '26 |
  | G4 — paid external API call | None in-sandbox; deployed calls bill at Opus 4.8 = same $5/$25 per-1M rate as 4.7. | 10-06 '26 |

## Amendments

- **`llm-client.js`** — `OPUS_MODEL` default → `claude-opus-4-8` (+ Defaults comment).
- **`__tests__/llm-client.test.js`** — fallback assertion → `claude-opus-4-8`.
- **`package.json`** — `0.61.433 → 0.61.434`.
- **`doc/.serial-state.yml`** — counters (commit 647 / journal 634 / build 409 holds) + anchor 06:16 SGT 10-06-26 + `last_anchor_source` prepend.

## Deleted

- The `claude-opus-4-7` Opus-tier default (superseded by `claude-opus-4-8`; 4.7 remains reachable via the `ANTHROPIC_OPUS_MODEL` env override).
