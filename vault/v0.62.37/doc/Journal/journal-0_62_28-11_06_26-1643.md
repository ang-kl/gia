(№ 681 - 11-06 '26 16:43 SGT) v0.62.28

# Journal — soleat v0.62.28 (Vibe Coding Record refresh — PRs #965-#989, 951 → 976 records)

> Operator: *"vibe-journal + journal"* — the per-PR doc-upkeep step from the standing rule. The Vibe
> Coding Record (generated ledger + hosted `/doc/vibe-journal.html`) was current only through #964
> (last refresh v0.62.4 / PR #966); main has since advanced to #989. Brought it current: appended
> the 25 PRs #965-#989 to the committed data snapshots and re-ran the generator. The per-PR Journal
> entries themselves were already written + merge-stamped this session (#975, #983-#985, #987-#989);
> this entry closes the loop on the GENERATED cross-section.

> Commit 698 / journal 681 / build 426 (doc-only; no app build). PATCH 0.62.27 → 0.62.28.

## New

### [HDR] #681 | 16:43 SGT 11-06-26 | v0.62.28 — Vibe Coding Record refresh #965-#989 | doc/VibeCodingRecord/* + public/doc/vibe-journal.* | 1 PR | 0 build |

- **[INTENT]** Operator: keep the Vibe Journal + hosted page tracking main per the standing rule.

- **[DELTA]**
  - **`doc/VibeCodingRecord/data/prs.ndjson`** — appended 25 rows (#965-#989), each
    `{ n, title, state:"MERGED", merged:<ISO>, body:<first 360 chars of the GitHub PR description> }`.
    Bodies + merged timestamps pulled from the GitHub API (`list_pull_requests`, state=closed);
    file lists from `git log --name-only origin/main` keyed on the squash "(#NNN)" tag.
  - **`doc/VibeCodingRecord/data/pr-files.tsv`** — appended the 25 squash-commit file lists.
  - **`doc/VibeCodingRecord/generate.mjs`** — `GEN_DATE` 2026-06-10 → 2026-06-11 with the
    #965-#989 arc summary in the inline note.
  - **Regenerated (committed outputs):** `records.tsv` (951 → **976 rows**), `vibe-coding-record.md`,
    `public/doc/vibe-journal.json`, `public/doc/vibe-journal.html` (hosted at /doc/vibe-journal.html).
    Generator category tally: feature 644 · fix 129 · docs 66 · copy 43 · prompt-tune 42 ·
    refactor 27 · test 15 · infra 10 | rework-flagged 84.

- **[VERIFICATION (sandbox)]** Generator ran clean (`node doc/VibeCodingRecord/generate.mjs` →
  "wrote records.tsv (976 rows) …"); prs.ndjson max PR# = 989, pr-files.tsv max = 989, 0 missing in
  [965..989]. No code path touched → full suite unaffected (not re-run; doc-only change).

- **[STATUS]** PR (rides the session branch). The hosted page redeploys with the static `public/`
  assets on merge.

- **[KNOWN GAPS]**
  - Several #965-#989 entries are OTHER contributors' PRs (#966 #968-#974 #976-#982 #986) merged to
    main outside this session; their per-PR Journal entries live in their own merge commits, not
    re-authored here (the standing rule scopes Journal authorship to PRs I open).
  - The deferred Places-cost levers (discover Atmosphere split, autocomplete session tokens, per-SKU
    telemetry) remain open from v0.62.27.

- **Confirmation Gates**

  | Gate | Resolution | Date |
  |---|---|---|
  | G1 — version bump | PATCH `0.62.27 → 0.62.28` (doc artifact refresh, matching #966 precedent). | 11-06 '26 |
  | G2 — destructive action | None — generated outputs overwritten by their own generator (by design); data files append-only. | 11-06 '26 |
  | G3 — new decision rule | None. | 11-06 '26 |
  | G4 — paid external API call | None (GitHub API reads only). | 11-06 '26 |

## Amendments

- **`doc/VibeCodingRecord/data/prs.ndjson`** (+25) · **`…/data/pr-files.tsv`** (+25) ·
  **`…/generate.mjs`** (GEN_DATE) · **`…/records.tsv`** + **`…/vibe-coding-record.md`** +
  **`public/doc/vibe-journal.{json,html}`** (regenerated) · **`package.json`** `0.62.27 → 0.62.28` ·
  **`doc/.serial-state.yml`** counters + anchor.

## Deleted

- (none) — data snapshots are append-only; the four generated outputs are regenerated in place.
