(№ 308 - 22-05 '26 14:50 SGT) v0.61.101

# Journal — soleat v0.61.101 (/app/map — wire the ⋯ dropdown)

> Operator — *"the dropdown isn't working"* on `/app/map`, and *"just
> work to get done"*. This entry completes the `/app/map` parity
> overhaul (operator spec items #3–#7).
>
> This entry is commit 308 / journal 301 / no new build (public/app.js
> is served raw), off `main` at v0.61.100 (#593).

## New

### [HDR] #301 | 14:50 SGT 22-05-26 | v0.61.101 — /app/map dropdown | ~3 kb | 2 files | 1 PR | 0 builds |

- **[INTENT]** The `⋯` overflow button on `/app/map` was a visual-
  parity placeholder with no behaviour. Wire it to a working dropdown
  carrying the extra overlay layers.

- **[DELTA]**
  - **`public/app.js`** —
    - New `MENU_LAYERS` config + `menuState` — the five extra layers
      Parks / Attractions / Clinics / Police / Hospitals.
    - `ensureOverlaysData` fetches `/api/geo/overlays` once;
      `renderMenuLayer` draws a layer (parks as green polygons, the
      rest as glyph pins with a name popup), viewport-clipped +
      zoom-gated z≥13 like the bus / carpark layers; `clearMenuLayer`
      tears it down; `refreshMenuLayers` re-clips on map idle (added
      to `refreshClippedLayers`); `toggleMenuLayer` flips a layer.
    - `buildToggleRow` — the `⋯` button now opens a dropdown panel of
      checkbox rows, one per menu layer; the panel closes on an
      outside click.
  - **`package.json`** — `0.61.100` → `0.61.101` (PATCH).

- **[VERIFICATION (sandbox)]**

  ```
  node --check public/app.js   clean
  npm test                     1944/1944 passing, 67 files
  ```
  (No Vite build — `public/app.js` is served raw.)

- **[STATUS]** DRAFT — PR #594 on `claude/v0.61.101-appmap-dropdown`
  (off `main` at v0.61.100, `f8eaf8d`). On merge deploys via the
  Railway auto-deploy on `main`.

- **[TEST]** `/app/map`: tap `⋯` → the dropdown opens; toggling Parks /
  Attractions / Clinics / Police / Hospitals draws / clears that
  layer; a tap outside closes the panel.

- **[KNOWN GAPS]**
  - The `/app/map` menu-layer popups show only the feature name — they
    are not the rich React-side info cards (clinic hours, hospital
    phones, etc.). A first-pass parity; richer popups can follow if
    the operator wants them.
  - No in-sandbox visual verification (no Maps webview).
  - `doc/VibeCodingRecord/` regen still due (carried forward).

- **Confirmation Gates**

  | Gate | Resolution | Date |
  |------|-----------|------|
  | G1 — version bump | PATCH `0.61.100` → `0.61.101` | 22-05 '26 |
  | G2 — destructive action | _code-only_ — additive dropdown machinery; no deletion | 22-05 '26 |
  | G3 — new decision rule | _n/a_ — operator UI spec | 22-05 '26 |
  | G4 — paid external API call | _n/a_ — reuses the existing `/api/geo/overlays` endpoint | 22-05 '26 |

## Amendments

_None._

## Deleted

_None._
