(№ 126 - 31-05 '26 09:15 SGT)

# Register — soleat / Gia4lunch v0.61.300 (outstanding-snapshot)

> **Document type:** Register (per `doc/CLAUDE.md` §3 — *open items, known issues, deferred, decisions, completed*; §4.1 — items move between sections, never deleted).
> **Status:** **Incremental outstanding snapshot** (not a master refresh). Continues the v0.61.276 incremental snapshot (`register-0_61_276-30_05_30-1230.md`) that itself continued the v0.61.147 master. This file records the open backlog + new decisions accumulated through the v0.61.277 → v0.61.300 arc (24 versions over ~21 hours, 30-05 '26 12:15 SGT → 31-05 '26 09:00 SGT). The v0.61.147 + v0.61.276 ledgers remain the canonical references for items O-1 → O-37 + D-61.120 → D-61.276; this file appends D-61.277 → D-61.300 and moves resolved Open items into the Completed section.
> **Reflects:** project state at **v0.61.300, 31-05 '26 09:15 SGT**, immediately after PR #806 auto-merge-queued. Live deployment on Railway is converging on the v0.61.300 bundle. Operator-facing TMAs: Cuisine + Menu + Hawker + Transport + Oversight. Branch protection on `main` enabled at v0.61.296 (4 required checks; enforce_admins=true).

---

## Open Items (live as of v0.61.300)

| # | Item | Owner | Priority | Notes |
| :--- | :--- | :--- | :--- | :--- |
| O-33 | **Gemini comparison variance** | agent | low | Operator's 29-05 '26 ask: *"use google, gemini and compare their results."* Run a side-by-side: Google Places `searchText` vs. Gemini-grounded search for the same prompt; measure overlap, unique-to-each, and quality. Could inform whether the v0.61.275 Gemini post-filter generalises beyond the current 10 cached cuisines. |
| O-34 | **Telegram inline-keyboard cache invalidation** | operator + agent | low | Operator hit this 30-05 '26: `/ver` panel was showing pre-v0.61.264 inline keyboard rows. Currently the user-side fix is to close the message and re-fire `/ver`. Code-side fix would embed a version marker in callback_data so stale keyboards get rejected. Low priority — manual close-and-resend works. |
| O-35 | **JB inference from coords-only is impossible** | agent | low | v0.61.273 coarseGate falls back: SG bbox → 'SG'; elsewhere → 'OTHER'. JB cannot be distinguished from SG by coords alone (bbox-adjacent). JB requires an explicit pill pick OR a cached anchor with `region='JB'`. v0.61.276's `isJbCoords` helper goes the other way (rules JB out when coords aren't JB-shaped). True bidirectional inference would need a `location-mode.js` JB-bbox secondary check. Not blocking; current behaviour is correct. |
| O-36 | **Country-only resting label** for OTHER picker compact pill on cold load | agent | low | If the cached `anchor.label` is just a bare country name (e.g. "Singapore" / "Malaysia") or `'Unnamed'`, v0.61.265's display guards correctly suppress it from the compact pill, but the picker then stays expanded. UX-acceptable but slightly noisier than ideal. Could fall back to a "Tap to set location" pill instead of the expanded picker. |
| O-37 | **iCloud-stale-checkout silently writes empty blobs** | agent + operator | medium → partially mitigated | First flagged 30-05 '26 in v0.61.287. **CI guardrail** added in v0.61.289 (`Empty-blob guardrail` job) catches the **0-byte case** at the merge boundary. **Remaining risk**: silent *partial-payload* writes (e.g. the v0.61.260 → v0.61.287 `durian-variance-runner.js` placeId loss — non-empty file with one missing field) are NOT caught by the size check. Mitigation options: (a) field-shape assertions in vitest, (b) move project off iCloud Drive, (c) accept the residual risk now that the variance modules are unified (v0.61.293). |
| O-38 | **Multi-cuisine search bypasses the Gemini cache** | agent | low | The v0.61.299 D703e/D703f gate fires only when `cuisineMetas.length === 1`. A search like "Russian + Polish" doesn't get the precision uplift. Acceptable for now — multi-cuisine intent is intentionally looser — but a future PR could apply the cache per-cuisine and merge. |
| O-39 | **Re-run scripts/slavic-ee-variance.js post-v0.61.300 seed-tuning** | operator | low | Operator action: validate the v0.61.298 + v0.61.300 seed changes lift Romanian 68 → ~120+ and Czech 90 → ~170+. Cost ~$1.40. |
| O-40 | **No metric / log of fact-send success** in `bot-fun-facts.js` | agent | low | A `console.log('[bot-funfact] sent', factId, 'to', chatId)` would help debug rate-limit / pick-failure cases. Held back in v0.61.296 for noise reduction; revisit if a real issue surfaces. |
| O-41 | **`scripts/durian-variance.js` had no unit tests** | agent | low | Flagged in v0.61.293 [KNOWN GAPS]. Adding `__tests__/durian-variance-runner.test.js` with a mocked `axios` would catch the v0.61.283/.288 placeId divergence class at write-time. Sized ~45 min. |

---

## Deferred (explicitly queued for follow-up PRs)

> Carried over from `register-0_61_276-30_05_30-1230.md`. Status updates appended at the right edge.

| # | Item | Target version | Notes | Status as of v0.61.300 |
| :--- | :--- | :--- | :--- | :--- |
| DEF-1 | First-paint OTHER → GPS revert (full) | v0.61.277+ | v0.61.268 shipped only the explicit-clear branch of operator's audit #5. | Still pending — superseded in concept by v0.61.276/v0.61.277 region-coherence work; explicit revert path still TBD. |
| DEF-2 | Menu TMA SG-mode autocomplete migration | v0.61.277+ | SG mode still uses v0.61.124 inline-fetch autocomplete in `LocationFieldMenu.jsx`. | Still pending. v0.61.279 closed the related O-27 (first-paint defaults) but didn't touch SG-mode autocomplete. |
| DEF-3 | JB Southkey alternate persistence | v0.61.277+ | v0.61.268 shipped `jbFocusKey` as session-only state. | Still pending. v0.61.281 generalised the JB chip row to 5 anchors; persistence still session-only. |
| DEF-4 | Cuisines-vault country-agnostic seeds | v0.61.278+ | Many `cuisines-vault.js` entries still hardcode `' Singapore'` in their `searchQuery`. | Still pending. |
| DEF-5 | Verifier auto-retry on transient 429 | v0.61.278+ | `durian-gemini-verifier.js` defaults failed-batch venues to `unrelated` / `low`. | Still pending. The v0.61.299 Slavic/EE extension exposes 10 cuisines to verify; transient failures hurt more now. |
| DEF-6 | Per-country chip availability hints | v0.61.279+ | After v0.61.272 made all chips selectable everywhere, some combinations (e.g. Durian Pastry in Tokyo) will return ~0 results. | Still pending. The v0.61.282 inline D703f auto-warming mitigates the underlying noise problem but doesn't pre-empt the user's tap. |
| DEF-7 | `LocationField` JSX integration tests | v0.61.280+ | All UX paths currently rely on operator manual testing. | Still pending. v0.61.281 + v0.61.284 + v0.61.297 all touched LocationField; the test gap remains. |

---

## Decisions (append-only, v0.61.277 → v0.61.300)

> Continues from D-61.276 in `register-0_61_276-30_05_30-1230.md`.

| # | Decision | Rationale | Date |
|---|---|---|---|
| D-61.277 | **JB chip + pill commit the anchor, not just toggle state.** Tapping a Southkey / JB CBD chip or the JB region pill now calls `onLocationSelect({ noAutoFire: true })`. The 🔍 button is no longer the only path to commit the anchor. | Operator: *"i switch to Johor Bahru, and confirm my new loc is JB south key as spec. why my loc still in south SG."* The v0.61.268 chip only toggled UI state. | 30-05 '26 |
| D-61.278 | **JB→OTHER server fallback gets a TMA banner.** When the v0.61.276 graceful-exit fires (JB pill at non-JB coords → server returns OTHER-country results), `payload.jbFallbackToOther: true` carries to the TMA which renders an amber notice. | Operator's Register O-25: silent server overrides leave the user confused. | 30-05 '26 |
| D-61.279 | **Shared `location-mode.js` constants + Menu TMA `__NONE__` parity + `place-search-by-country` deletion.** Three Register closures in one patch (O-26, O-27, O-28). 150 km threshold extracted to `isFarFromJB`; Menu TMA's `\|\| 'SG'` fallback replaced with `\|\| null` + `anchorLoading` gate; deprecated route deleted (183 lines). | Operator selection 3/4/5 from v0.61.278 backlog. | 30-05 '26 |
| D-61.280 | **Michelin chip strip generalises to all non-SG regions + sparse notice in drawer.** v0.60.199 stripped on JB only; v0.61.280 widens to `r.id !== 'SG'` AND adds an italic "Michelin is Singapore-only" caption when `region !== 'SG'`. | Register O-31. | 30-05 '26 |
| D-61.281 | **5-chip JB anchor row + 8 s auto-hide on suffix line + SG-bbox west carve-out.** JB chips become `Legoland · Bukit Indah · CBD · Southkey · Mt Austin` (data-driven from `jb-focus-points.js`). The `{N} places nearby · tap to change 🔝` auto-hides after 8 s. `coords-to-country.js` gets `SG_LNG_WEST_THRESHOLD=103.70 / SG_LAT_MAX_WEST=1.42` so Legoland (1.4296, 103.6321) classifies as JB, not SG. | Operator screenshot annotation + the v0.61.276 modal regression at Legoland coords. | 30-05 '26 |
| D-61.282 | **Auto-warming Gemini cache (D703f online verify-then-cache).** When a cuisine-search special-mode hits an uncached venue, fire one `_processBatch` call inline, drop `unrelated`, write verdicts back to `dgv:labels:<mode>`. Subsequent searches anywhere globally on the same placeIds are free. | Operator's durian-pastry-in-Surabaya bug (steak house in result list); pre-cached labels only covered SG/JB/Putrajaya/KL. | 30-05 '26 |
| D-61.283 | **`scripts/durian-variance.js` placeId carry-through.** `placeToVenue` returns `placeId: p?.id`; per-query `kept[]` row includes it. v0.61.275 Plan B cache had been silently empty in prod through 8 patches. | Operator JSON dump showed every venue had `placeId: ""`. | 30-05 '26 |
| D-61.284 | **Halal-chip active-state contrast + tap-to-search hint immediate-show.** Halal Arabic glyph swaps `text-emerald-600` → `text-tg-accent-text` when active. The v0.61.244 6 s idle-hint timer is dropped; bubble shows immediately on type and persists until pick/clear/submit/blur. | Operator screenshot of green-on-cyan halal text + *"i thought when i type in the location ... the message ... will be below the 🔍 until the user click."* | 30-05 '26 |
| D-61.285 | **NLB-sourced fun-facts modal during cuisine-search rotating-titles window** (Phase 1). 40 facts curated from NLB Infopedia / curiocity digital stories / BiblioAsia. Tag-keyed selector (cuisine / community / region). Modal renders 1.5 s into a `rotating` search; 3 s minimum on-screen; anti-repeat via localStorage. | Operator brief: *"replace the current wait messages while curating results … a food-wiki on the city … floating and fun to read."* | 30-05 '26 |
| D-61.286 | **FunFactModal dark-mode border contrast.** `border-2 border-tg-accent` + `ring-1 ring-tg-accent/30` + `shadow-2xl`. | Operator: *"for dark mode, the contrast in border for this wait message must be obvious."* | 30-05 '26 |
| D-61.287 | **Register O-37 logged + stale `register-outstanding-v0.61.276` branch deleted.** Documents the iCloud-stale-checkout failure mode (smart-place-label.js + durian-variance-runner.js silent placeId loss). | Pattern observed twice; warranted formal capture. | 30-05 '26 |
| D-61.288 | **`durian-variance-runner.js` placeId carry-through.** Same fix as D-61.283 but in the bot's in-process runner (which v0.61.283 missed). 28 patches of `dgv:labels:<mode>` silent no-op in prod. | Operator JSON dumps from v0.61.286 deploy still showed empty placeId. | 30-05 '26 |
| D-61.289 | **CI empty-blob guardrail** + `scripts/slavic-ee-variance.js` investigation tool. New CI job fails the merge if any tracked `*.js / *.jsx / *.json / *.mjs` is 0 bytes. Variance script tests Slavic/EE coverage across 7 cuisines × 8 cities × 4-5 seeds. | Register O-37 mitigation + Register O-32 investigation. | 30-05 '26 |
| D-61.290 | **Data files imported by both Vite browser bundles AND Node-ESM (vitest) MUST be JS modules with `export default`, not bare JSON.** `fun-facts.json` → `fun-facts.js`. Bare JSON imports require import attributes that differ across Node versions and Vite versions; the JS module form is portable. | v0.61.285's vitest test had been failing silently on Node 20 ESM for 5 patches (auto-merge bypassed because vitest wasn't a required check). | 30-05 '26 |
| D-61.291 | **Cuisine-tag priority in fun-fact selector.** Tiered match: cuisine/community tags (Tier 1) → region tags (Tier 2) → full pool (Tier 3). Earlier "any-tag-overlap" treated `'sg'` and `'laksa'` as equally weighted, so SG-tagged facts diluted cuisine-targeted picks 25:1. | v0.61.290 vitest run caught the assertion failure. | 30-05 '26 |
| D-61.292 | **Removed Features RF-12 → RF-15 appended to `feature-0_61_83-21_05_26-1645.md`** (NOT to `cuisines_js.MD` as prior journals incorrectly referenced). 4 rows covering v0.61.84 → v0.61.291 removals. | Course-correction on prior [KNOWN GAPS] target file. | 30-05 '26 |
| D-61.293 | **Variance pipeline has a single source of truth.** `scripts/durian-variance.js` becomes a 71-line CLI wrapper that delegates to `durian-variance-runner.js`'s `runVariance({...})`. Both consumers (CLI + bot `/ver`) share the same SEEDS / `_placeToVenue` / loop. | Eliminates the silent-divergence-between-duplicates risk that caused v0.61.275–287 silent placeId loss. | 30-05 '26 |
| D-61.294 | **D703e/D703f Gemini cache pattern generalises across special modes via a `PROMPT_CONFIG` map + `GEMINI_CACHE_MODES` Set.** Adding a new mode is one entry in each. `fruits` added; pattern documented. | Operator: *"5"* — extend D703f beyond durian/durian-pastry. | 30-05 '26 |
| D-61.295 | **Fun-facts Phase 2A: 12 MY/regional facts (40 → 52).** Wikipedia-sourced (NLB skews Singapore-only). Tagged for selector targeting. | Operator: *"5"* — Phase 2 of fun-facts. Phase 2B (chat surface) deferred to next sub-PR. | 30-05 '26 |
| D-61.296 | **Chat-surface fun-fact: separate `bot.sendMessage` AFTER the venue result lands.** Per-chat-id Redis anti-repeat (10 IDs, 1-day TTL). Server-side selector reuses cuisine TMA `_pickFact` via dynamic `import()` — no logic duplication. `runCuisineFlow` (`/c <cuisine>`) is the first wire point. | Operator chose surface option B from v0.61.295 design proposal. | 30-05 '26 |
| D-61.297 | **FunFactModal exclusively replaces the rotating-titles overlay when a fact is picked.** v0.61.285 brief was *"replace the current wait messages while curating results"*; v0.61.285 built the modal but didn't gate the older overlay. v0.61.297 fixes the stacking via `loading && !funFact`. Also adds 12 anti-repeat-variety facts (52 → 64). | Operator screenshot of overlap. | 31-05 '26 |
| D-61.298 | **Romanian seed expansion in `slavic-ee-variance.js`.** 4 → 6 seeds (drop weak `bucătărie românească`, add `ciorbă` / `papanași` / `restaurant românesc`). | v0.61.297 variance analysis: Romanian was worst-served (68 total across 8 cities). | 31-05 '26 |
| D-61.299 | **D703e/D703f extended to 7 Slavic/EE cuisines via single-cuisine slug match.** `geminiCacheKey` is `specialMode` OR `cuisineMetas[0].slug` when `cuisineMetas.length === 1`. 7 new `PROMPT_CONFIG` entries with "not a generic <neighbour>" caveats to handle Russian/Ukrainian/Czech-Hungarian dish overlap. | Variance analysis showed 20–30 % cross-cuisine false-positive rate. | 31-05 '26 |
| D-61.300 | **Native-language generic phrases dominate dish-name seeds 2-5× in variance.** Hungarian `magyar étterem` 80 hits vs `gulyás` 17. v0.61.300 adds the same form to Russian / Polish / Ukrainian / Czech. Drops `holubtsi` (4 hits) from Ukrainian. | v0.61.297 per-seed productivity analysis. | 31-05 '26 |

---

## Completed in this arc (v0.61.277 → v0.61.300, 24 versions)

> Headline shifts only; per-PR detail lives in `doc/Journal/journal-0_61_27{7..9}-*.md` + `doc/Journal/journal-0_61_2{80..99}-*.md` + `doc/Journal/journal-0_61_300-*.md`.

- **JB region surface polish** — v0.61.277 (chip + pill anchor commit) → v0.61.281 (5-chip row + SG-bbox west carve-out). The full Mid Valley Southkey screenshot loop closed.
- **Fun-facts feature** — v0.61.285 (Phase 1: 40 NLB-sourced facts + modal) → v0.61.286 (dark-mode border) → v0.61.295 (Phase 2A: +12 MY/regional → 52) → v0.61.296 (Phase 2B: chat-surface) → v0.61.297 (variety pass + stacking fix → 64) → v0.61.291 (cuisine-tag priority). End state: 64 facts, TMA modal + bot DM chat surface, tier-1 cuisine prioritisation.
- **Gemini-cache architecture generalisation** — v0.61.282 (auto-warming D703f for durian/pastry) → v0.61.294 (fruits added) → v0.61.299 (7 Slavic/EE cuisines). End state: 10 cached cuisines/modes, mode-agnostic mechanism, single PROMPT_CONFIG map.
- **Variance script + module unification** — v0.61.283 (CLI placeId fix) → v0.61.288 (runner placeId fix — the v0.61.283 follow-up that closed 28 patches of silent loss) → v0.61.293 (single source of truth: CLI delegates to runner). End state: 542 → 356 LOC, no duplication risk.
- **Slavic/EE variance arc** — v0.61.289 (script + CI guardrail) → v0.61.297 (variance run) → v0.61.298 (Romanian seeds) → v0.61.299 (Gemini cache extension) → v0.61.300 (generic-phrase seeds). End state: 7 cuisines instrumented + cached; coverage projection ~3× the v0.61.297 baseline.
- **CI + branch protection** — v0.61.289 empty-blob guardrail; v0.61.290 fixed silent vitest fail; v0.61.296 enabled branch protection on `main` (4 required checks, enforce_admins=true) and `allow_auto_merge=true` on the repo. End state: PRs require all 4 jobs green; no admin bypass for protected merges.
- **Bug-class learnings captured** — Register O-37 (iCloud-stale-checkout) added v0.61.287; mitigated by CI guardrail v0.61.289 (0-byte case) + variance module unification v0.61.293 (silent-divergence case).
- **Resolved Open items from v0.61.276 ledger**:
  - O-25 → D-61.278 (v0.61.278 JB-fallback banner)
  - O-26 → D-61.279 (v0.61.279 `isFarFromJB` extraction)
  - O-27 → D-61.279 (v0.61.279 Menu TMA `__NONE__` parity)
  - O-28 → D-61.279 (v0.61.279 `place-search-by-country` deletion)
  - O-29 → D-61.282 + D-61.288 (cache cold-start solved by auto-warming + placeId-loss fix)
  - O-30 → D-61.285 + D-61.295 + D-61.296 + D-61.297 (fun-facts JSON, four-PR arc)
  - O-31 → D-61.280 (v0.61.280 Michelin OTHER strip + sparse notice)
  - O-32 → D-61.289 + D-61.298 + D-61.299 + D-61.300 (Slavic/EE variance + Gemini cache, four-PR arc)

---

## Amendments

- **`register-0_61_276-30_05_30-1230.md`** — appended Open item O-37 in v0.61.287 (still present there; the entry above carries its v0.61.300 status forward).

---

## Reference

- v0.61.147 full master: `doc/Register/register-0_61_147-25_05_26-1225.md`
- v0.61.276 incremental: `doc/Register/register-0_61_276-30_05_30-1230.md`
- Recent journals: `doc/Journal/journal-0_61_2{77..99}-*.md` + `doc/Journal/journal-0_61_300-31_05_31-0900.md`
- Slavic/EE variance JSON: `/tmp/slavic-ee-variance.json` (not committed; reproducible via `GOOGLE_MAPS_API_KEY=… node scripts/slavic-ee-variance.js`)
- Gemini-verified durian + durian-pastry data: live in Redis hashes `dgv:labels:durian` + `dgv:labels:durian-pastry` (populated by /ver workflow on v0.61.288+ deploys)
- Branch protection state: 4 required checks (Vitest, Syntax, TMA build, Empty-blob); `enforce_admins=true`; `allow_auto_merge=true`
