(№ 237 - 18-05 '26 23:00 SGT) v0.61.28

# Journal — soleat v0.61.27 → v0.61.28 (Exit Template Part B: nearby attractions)

> Operator: exit feature Part B — surrounding buildings / attractions per exit. (The ♿
> lift half is shelved — see [KNOWN GAPS].)

## New

### [HDR] #230 | 23:00:00 SGT 18-05-26 | v0.61.28 — Exit Template shows nearby attractions | ~6 kb | 5 files | 1 PR | 3 builds |

- **[INTENT]** Exit feature **Part B** — the operator's original Exit Template asked for
  surrounding buildings / attractions per exit (and a ♿ lift line). A spike this session
  established: (a) no LTA dataset carries lift-per-exit data, and the SMRT site that does
  (`journey.smrt.com.sg/.../exits/`) returns 403 to bots / is unreachable from the build
  sandbox; (b) the URA building-footprint dataset has no names. Operator decision: **build
  the buildings/attractions half** from the named data we do have (STB tourist
  attractions); shelve the ♿ lift half pending a scrape run elsewhere.

- **[DELTA]**
  - **`scripts/build-geo-overlays.js`** — new `loadAttractions()` (reads
    `geoloc/TouristAttractions.geojson`, 109 named STB landmarks) + `cleanAttractionName()`
    (reduces the verbose marketing PAGETITLE — "Sri Mariamman Temple: Hindu Temple in
    Singapore" → "Sri Mariamman Temple"; fixes the `â€™` mojibake) + `nearbyAttractions()`
    (≤400 m, nearest-first, de-duped, max 3). `convertPoint`'s exits branch now sets
    `rec.nearby` on each exit feature.
  - **`data/geo-exits.json`** — regenerated: 125 of 597 exits now carry a `nearby` array.
  - **`web/{transport,hawker,cuisine}/src/.../lib/mapOverlays.js`** (byte-identical ×3) —
    `exitTemplateHtml` takes a `nearby` arg and renders a `🎡 …` line of attraction names;
    `exitInfo` passes `f.nearby` from the geo-exits feature.

- **[VERIFICATION (sandbox)]**

  ```bash
  $ node scripts/build-geo-overlays.js   → exits: 597 features, 125 with `nearby`
  $ node --check mapOverlays.js          → OK (byte-identical ×3)
  $ npx vite build  (cuisine / hawker / transport)  → all green
  $ npm test -- --run                    → 1928 passed (1928)
  # spot-check: Esplanade·Exit E → ["The Civilian War Memorial park",
  #   "MINT Museum of Toys…","Esplanade Theatre"]; Orchard exits → ["Orchard Road"]
  ```

- **[STATUS]** DRAFT → PR (base `main`, branch `claude/review-gia4lunch-readme-3s798`).

- **[TEST]** Any TMA, Google-map view: turn on the 🚆 Exits layer, tap an exit near a
  landmark (e.g. an Esplanade or Bras Basah exit) → the Exit Template popup shows a
  `🎡 …` line under the station-code pills.

- **[KNOWN GAPS]** Exit feature Part B **♿ lift half — shelved.** No LTA dataset has
  lift-per-exit accessibility; the SMRT `journey.smrt.com.sg` station pages have it but
  the site 403s non-browser clients and is unreachable from the build sandbox. To build
  it: a scraper (browser headers, possibly headless if the page is a JS SPA) must run in
  an open-network environment and commit the result as a static data file — or the
  operator supplies the data. "Surrounding buildings" by name is also not possible: the
  URA building-footprint dataset is unnamed; attractions are the named-place signal used
  here. The doc-system catch-up (VibeCodingRecord #498–#512 + Feature/Technical/Register)
  is still due — a separate pass.

## Amendments

(none)

## Deleted

(none)

## Confirmation Gates Satisfied

| Gate | Authority | Stamp |
| :--- | :--- | :--- |
| G1 — version bump (PATCH: 0.61.27 → 0.61.28) | _operator: 0.61.x PATCH sequence_ | 18-05 '26 |
| G2 — destructive action | _n/a — additive_ | — |
| G3 — new decision rule | _operator: build the attractions half of Part B, shelve the ♿ lift half_ | 18-05 '26 |
| G4 — paid external API call | _n/a_ | — |
