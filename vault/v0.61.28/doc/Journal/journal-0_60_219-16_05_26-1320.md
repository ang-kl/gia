(№ 194 - 16-05 '26 13:20 SGT) v0.60.219

# Journal — soleat v0.60.219

> Operator multi-area request — weather slice 2. Item 3a (drop the Cuisine title `Nc · Nf` badge) + item 3d (a live weather summary in the TMAs). This PR covers the three **web** TMAs the operator named — Cuisine, Train, Hawker. The 3d chat-flow surfaces (nearest bus stops / carpark / nearest stations are Telegram message flows, not web TMAs) fold into the upcoming transport-chat PR alongside items 2 / 2b.

## New

### [HDR] #187 | 13:20:00 SGT 16-05-26 | v0.60.219 — TMA weather summary (web TMAs) + Cuisine-title swap | 6 files | 1 PR | 3 TMA builds |

- **[INTENT]** Surface a live Singapore weather emoji in the Mini Apps. The Cuisine title's `Nc · Nf` count badge (operator: *"meaningless"*, flagged since v0.60.80) is replaced with it.

- **[DELTA]**
  - **`index.js`** — new public endpoint `GET /api/weather/summary` (no auth / no PII — same posture as `/maps-key`). Returns `{ ok, tempC, tempF, humidityPct, condition, emoji }` from `weather.summary` at the SG centroid, mapped through `weather-emoji.js`'s `forecastEmoji` / `toFahrenheit`. Redis-cached 5 min (`weather:tma-summary`) to spare the NEA feeds.
  - **`WeatherBadge.jsx`** (NEW ×3 — `web/cuisine/src/v2/components/`, `web/transport/src/components/`, `web/hawker/src/components/`) — a self-contained chip: fetches `/api/weather/summary` once on mount, renders `<emoji> <tempC>°C`, renders nothing until/unless data arrives. Best-effort — a fetch failure never blocks or errors the header.
  - **`web/cuisine/src/v2/App.jsx`** — the `Nc · Nf` count `<div>` beside the locale toggle is replaced with `<WeatherBadge>` (item 3a).
  - **`web/transport/src/App.jsx`** — `<WeatherBadge>` added beside the `timestampSGT` line in the header.
  - **`web/hawker/src/App.jsx`** — the header build-version `<p>` is replaced with `<WeatherBadge>` (operator: *"replace version number with weather icon"*). `BUILD_VERSION` is still used by the hawker footer; the `header.versionCount` / `header.versionOnly` i18n keys are now unused but retained.
  - `package.json` `0.60.218 → 0.60.219`; this Journal entry (#187); `doc/.serial-state.yml` (`journal 186→187`, `commit 193→194`, `build 4→5`; re-anchored).

- **[VERIFICATION (sandbox)]**

  ```bash
  $ node --check index.js                              → OK
  $ (cd web/{cuisine,transport,hawker} && npm run build) → all 3 built clean
  $ npm test -- --run                                   → 1889 passed (1889)
  ```

- **[STATUS]** STAGED → PROD on next Railway deploy.

- **[TEST]** Post-deploy: open the Cuisine TMA → a live weather emoji + temp sits where `Nc · Nf` was; Train TMA → a weather emoji beside the timestamp; Hawker TMA → a weather emoji where the version line was. `/api/weather/summary` returns the cached SG summary.

- **[KNOWN GAPS]**
  - **3d — chat-flow surfaces deferred.** The operator's 3d list also names "View nearest bus stop / carpark / nearest 3 stations" — these are Telegram chat message flows, not web TMAs. Their weather line + items 2 ("Gia is thinking" → "Soleat is thinking") + 2b ("3 stations" → "3 train stations") fold into the next PR (transport-chat polish).
  - The Hawker `header.versionCount` / `header.versionOnly` i18n keys are now unreferenced — retained, harmless.

## Confirmation Gates

| Gate | Authority | Stamp |
|---|---|---|
| G1 — version bump | _PATCH 0.60.218 → 0.60.219._ | auto |
| G2 — destructive action | _None._ | n/a |
| G3 — new decision rule | _Operator: the Mini Apps carry a live Singapore weather emoji (the Cuisine title's count badge is replaced by it; the Hawker version line becomes it)._ | operator |
| G4 — paid external API call | _None new — `/api/weather/summary` reuses the NEA feeds `weather.js` already polls; Redis-cached 5 min._ | n/a |

## Amendments

(None. v0.60.218 (#186) is the prior entry.)

## Deleted

(Nothing deleted. The Cuisine `Nc · Nf` badge and the Hawker version-line content are replaced in place; the underlying i18n keys are retained.)
