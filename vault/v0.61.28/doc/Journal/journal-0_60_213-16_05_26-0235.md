(№ 188 - 16-05 '26 02:35 SGT) v0.60.213

# Journal — soleat v0.60.213

> Operator: *(1)* the Cuisine TMA footer (`v0.60.4 · Singapore · 💬 Tell me or 🔍 Search`) → two lines — a how-to line `📍 Enter location or 💬 type dish · Tap 🔍 to search` and a tag line `Experimental · Singapore · v#.##.###`. *(2)* Append the same `Experimental · Singapore · v#.##.###` tag line to the Train, Hawker and Menu TMAs.

## New

### [HDR] #181 | 02:35:00 SGT 16-05-26 | v0.60.213 — TMA footers: standardised "Experimental" tag line + Cuisine how-to line | 9 files | 1 PR | 4 TMA builds |

- **[INTENT]** Give all four Mini Apps a consistent footer tag line (`Experimental · Singapore · v<build>`), and add a how-to line to the Cuisine TMA so first-time users see how to drive it. Replaces the Cuisine TMA's single mixed footer line.

- **[DELTA]**

  **Cuisine TMA** (`web/cuisine/`):
  - `src/v2/lib/i18n.js` — two keys: `footer.howto` (`📍 Enter location or 💬 type dish · Tap 🔍 to search` / FR `📍 Saisir un lieu ou 💬 taper un plat · 🔍 pour rechercher`), `footer.experimental` (`Experimental` / `Expérimental`).
  - `src/v2/App.jsx` — added `const BUILD_VERSION` (the `__BUILD_VERSION__` guard, replacing a hardcoded `v0.60.4`). Footer ~1443 rewritten from one line to two: `<div>{footer.howto}</div>` + `<div>{footer.experimental} · {region} · v{BUILD_VERSION}</div>`. Region stays dynamic (the TMA toggles SG / Johor Bahru).

  **Train / Hawker / Menu TMAs** — each gets a `footer.tag` i18n key (`Experimental · Singapore` / `Expérimental · Singapour`) and renders `{t('footer.tag', lang)} · v{BUILD_VERSION}`:
  - `web/transport/` — `src/i18n.js` + `src/App.jsx` (added `BUILD_VERSION` const; the existing `Source: LTA …` footer gains a second line).
  - `web/hawker/` — `src/i18n.js` + `src/App.jsx` (no footer existed — a new `<footer>` added at the end of the scroll-content `<div>`; `BUILD_VERSION` const already present).
  - `web/menu/` — `src/i18n.js` + `src/App.jsx` (the existing `Soleat <ver> · 2026` brand line kept; the tag line added below it; `BUILD_VERSION` const already present).

  **`package.json`** — `0.60.212 → 0.60.213` (PATCH). **`doc/Journal/journal-0_60_213-16_05_26-0235.md`** — this entry (#181). **`doc/.serial-state.yml`** — `journal 180→181`, `commit 187→188`, `build 1→2`; re-anchored.

- **[VERIFICATION (sandbox)]**

  ```bash
  $ (cd web/cuisine && npm run build)     → built in 3.42s
  $ (cd web/transport && npm run build)   → built in 2.16s
  $ (cd web/hawker && npm run build)      → built in 2.46s
  $ (cd web/menu && npm run build)        → built in 2.12s
  $ npm test -- --run                     → 1865 passed (1865)   # sanity — no server code touched
  ```

- **[STATUS]** STAGED → PROD on next Railway deploy. The `public/<tma>/` bundles are gitignored and rebuilt on deploy; the local builds here were verification.

- **[TEST]** Post-deploy, open each TMA:
  1. **Cuisine** — footer is two lines: `📍 Enter location or 💬 type dish · Tap 🔍 to search` then `Experimental · Singapore · v0.60.4`. In Johor-Bahru mode the middle term flips to `Johor Bahru`.
  2. **Train / Hawker / Menu** — a footer line `Experimental · Singapore · v<build>` at the bottom (Menu keeps `Soleat … · 2026` above it).
  3. FR locale → `Expérimental · Singapour · …` and the FR how-to line.

- **[KNOWN GAPS]**
  - The cuisine / transport / hawker TMA `package.json` versions are stale at `0.60.4`, so their footers display `v0.60.4`; the Menu TMA injects the **root** version, so it shows `v0.60.213`. The operator asked only for the layout (`v#.##.###` was a placeholder), so the per-TMA version values are left untouched — flag if a unified version display is wanted.
  - `header.tagline` in the Cuisine v2 i18n is now unreferenced (the footer no longer uses it); left in place — removing it is out of scope.
  - DF backlog remains empty (closed across v0.60.210–212).

## Confirmation Gates

| Gate | Authority | Stamp |
|---|---|---|
| G1 — version bump | _PATCH 0.60.212 → 0.60.213._ | auto |
| G2 — destructive action | _None._ | n/a |
| G3 — new decision rule | _Operator: every TMA footer carries a standardised `Experimental · Singapore · v<build>` tag line; the Cuisine TMA additionally leads with a how-to line._ | operator |
| G4 — paid external API call | _None._ | n/a |

## Amendments

(None. v0.60.212 (#180) is the prior entry.)

## Deleted

(Nothing deleted. The Cuisine TMA's former single footer line — `v0.60.4 · <region> · 💬 Tell me or 🔍 Search` — is replaced in place by the two-line footer.)
