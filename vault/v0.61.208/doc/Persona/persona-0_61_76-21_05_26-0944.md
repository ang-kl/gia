(№ 4 - 21-05 '26 09:44 SGT)

# Persona — soleat Human Lead v0.61.76 (fresh master)

> **Document type:** Persona (per `doc/CLAUDE.md` §3 — *living capability profile of the Human Lead*; §4.1 — add + amendment, no deletion).
> **Status:** Fresh, non-incremental master, produced under operator instruction (21-05 '26) — the v0.61.76 vault pass, following the v0.61.28 precedent (decision D-1028). Supersedes `persona-0_61_28-18_05_26-2330.md` as the current-state reference; the prior file remains on disk as the authenticity trace.
> **Reflects:** project state at **v0.61.76, 21 May 2026 09:44 SGT**, captured into `vault/v0.61.76/`.
> **Reading note:** standalone master — readable cold. Source evidence: the 275-entry Journal arc, the Register, the `.serial-state.yml` per-version log, and operator-quoted INTENT blocks.

---

## 0. Profile in brief

The Human Lead is the **sole operator, product owner, and director** of Soleat / Gia4lunch — a production Telegram bot + five Mini Apps serving Singapore F&B discovery, transport, and logistics. Across ~285 numbered versions and ~574 pull requests the operator has carried the project end-to-end: domain dataset curation, product and UX direction, architecture decisions, security hardening, a self-designed eight-folder documentation contract, bilingual (EN/FR) content, and release operations on Railway — working through AI-assisted development as the directing intelligence.

Since the v0.61.28 master the operator has driven a sustained ~48-version transit-domain arc: the multi-phase **CR6 station-info programme** (Phase 1 data layer → Phase 2 bus services + first/last-train → Phase 3 station info card), a full **map-controls / map-UI redesign** (overlay layers, unified `MapControls`, theme-aware greyscale toggle, two-colour station pins, zoom-aware layers), a curated **bus-stop / clinic / hospital / 24-hour-places data-ingestion** strand, and a chat-template rework of the `/transport` replies — all in the operator's now-established phased "CR" (change-request) idiom.

Every rating below is anchored to a verifiable artifact in the repository.

---

## 1. Domain Strengths

| Domain | Confidence (1-5) | Observed evidence |
|---|---|---|
| Singapore F&B + venue curation | **5** | Curated the Saved Places dataset, `michelin-2025.js`, `hawker-vault.js`, `cuisines-vault.js`; the curated dish-type dictionary; the pet-friendly allow-list; country-currency price display. |
| Singapore transport + geospatial domain | **5** | Drove the entire v0.61 map arc: MRT/LRT line geometry, station-code knowledge (NS21 / DT11 interchange pairing), exit-letter vs exit-number conventions, station-detail neighbour logic, the Exit Template design; this arc — caught that `mrt-coords.json` station lat/lng was coarse (median 37 m, worst 1.5 km off) and that LTA exit GeoJSON was authoritative, motivating the `exit_centroid` field; specified the CR6 station-info card per SMRT/SBS conventions; knew that VivoCity sits >1.5 km from any MRT (HarbourFront only) and corrected the Train search radius accordingly. |
| Telegram bot UX (button hierarchies, callback patterns, force-reply flows) | **5** | The `/clipboard` row, the `/s` assistance sub-menu, hidden owner commands, the `/v` builder-command inline menu; this arc — the `📍 Refresh location` callback rows on the Bus menu and `/carpark` result, count-aware map buttons, the `/ver` "🛠 Admin commands" inline keyboard. |
| TMA / WebApp client-server contracts | **5** | Repeatedly caught the "payload ≠ render" gap and insisted on cross-handler render parity; drove the three-TMA Google-Map overlay system on a shared controller; this arc — extended the contract through the station-info data layer (`/api/geo/stations`, `/api/transport/stations`) and the bus-stop / overlay routes. |
| Map / interaction UX | **5** | Directed the overlay-chip system, the station-detail focus view, the theme-aware map popup cards, the line-coloured exit pins; this arc — the full map-controls redesign (unified `MapControls`, ⋯/⋮ overflow menu, quick-toggle pills), the theme-aware greyscale `🎨 Colour` toggle, two-colour station pins, zoom-aware train layers, `flashPin` pulse feedback, numbered Cuisine result pins, and consistently corrected pin legibility/contrast. |
| Doc protocol design (the AU Recipe, eight templates, serial numbers, time anchors, vault) | **5** | Authored `doc/CLAUDE.md` + `doc/CLAUDE-FULL.md` (contract v0.0.4); codified the per-PR Journal cadence; commands deliberate fresh-master + vault passes — the v0.61.28 pass and this v0.61.76 pass are both operator-instructed consolidations. |
| LLM prompt engineering + cost-aware Gemini integration | **5** | Directed the cooking-method explainer, the dish-name semantic guard, and best-effort LLM scoping. |
| Performance / cost budget reasoning (Telegram 30 s ceiling, Places quotas, Redis TTLs) | **5** | Commissioned the TMA load-speed audit; approved immutable cache headers; reasons clearly about Places-cost trade-offs and prefers build-time geometric precompute over per-request cost; this arc — the build-time station-info join pipeline (`build-station-info.js`), Redis-cached overlay routes (24 h TTL), and the offline Haversine bus-stop triangulation before any paid OneMap pass. |
| Open-data ingestion + curated-dataset pipeline design | **5** | This arc — directed the LTA DataMall (`/BusStops`, `/BusRoutes`), NEA, OneMap, and CHAS ingestion strands: `fetch-clinic-hours.js`, `fetch-hospitals.js`, `fetch-bus-services-by-stop.js`, `enrich-nearest-bus-stops.js`, `fetch-24hr-places.js`; understood and accepted source ceilings (clinic-hours coverage capped ~89 %; exit GeoJSON has no street name); held a non-invention rule — fields copied verbatim from the API, missing → `null`. |
| Security & operational hardening | **4** | Directed the `initData` HMAC auth chokepoint; specced the per-chatId rate-limit layer — *"caps catch bugs, not money."* |

---

## 2. Capability Inventory

| Capability | Strength (1-5) | Observed evidence |
|---|---|---|
| Spotting recurrent bugs from screenshots | **5** | The `[object Object]` arc; the Michelin walk-through regression; the `/transport` line-vs-station-code screenshot; this arc — diagnosed the v0.61.62 hotfix chain from blank Mini Apps (the `/healthz` `pkg` ReferenceError → 500 → webhook-domain probe → fallback host → 3 blank TMAs), and caught the menu-hub FAB showing the wrong label after a navigate-away. |
| Q&A clarifying language (verbatim quotes, timestamped screenshots, option framing) | **5** | Treats `AskUserQuestion` as a first-class tool; answers structured forks decisively and often picks the scope-expanding option (e.g. "unify the station-detail exit pins too"). |
| Scoping a feature against reality | **5** | Split the exit feature into Part A / Part B and shelved the unbuildable lift half; this arc — ran CR6 as an explicitly phased programme (Phase 1 data → Phase 2a bus services → Phase 2b first/last train → Phase 3 card UI), each phase shippable on its own, with later phases stubbed in the data layer ahead of the UI. |
| Multi-phase change-request ("CR") direction | **5** | This arc — issued numbered change requests (CR1–CR10) and ran them as tracked, multi-PR programmes: CR4 bus-stop de-emphasis, CR5 selective polyline emphasis (v1 → v2), CR6 station info (5+ PRs), CR7/CR8/CR9 map-control tidy; deferred individual CRs explicitly when scope demanded and resumed them later. |
| Holding the doc protocol invariants | **5** | Codified the per-PR Journal cadence; commands deliberate consolidation + fresh-master + vault passes; never asks to skip a Gate; logs doc-system catch-up as a `[KNOWN GAPS]` item when a code pass runs ahead of the docs. |
| Sequencing destructive vs reversible actions | **5** | "Report first, don't fix or build" before authorising change; diagnose infrastructure before retrying; pivots cleanly when a shipped decision proves wrong; sequences concurrent-PR counters so two in-flight branches don't collide. |
| Naming + UI copy decisions in two locales (EN + FR) | **5** | Supplies exact replacement copy verbatim; this arc — supplied the full `/transport` nearest-bus-stops message template (weather leads, live count in header, road-name lead, per-stop distance sentence, "🚌 Bus №" rows, "minutes" ETA bands), the station-info card template, and precise pill/button relabels ("☑️ Grey" → "☑️ Monochrome", "Show all {n} bus stops"). |
| Diagnosing infrastructure vs code failures | **5** | The GitHub Actions billing-block diagnosis; recognises an MCP/connectivity drop as environment, not code; this arc — traced the v0.61.62 blank-TMA incident to a single mis-named identifier (`pkg` → `pkgJson`) cascading through the health probe. |
| Backlog discipline | **5** | A `DF-NN` deferred backlog with reactivation conditions; deliberate closure passes; tracks known gaps across sessions; this arc — carried deferred CRs (CR4/CR5 deferred at v0.61.51, resumed v0.61.52–.58; CR6 deferred entirely then run as the central programme). |
| Iterative review-driven direction | **5** | Drives long feature arcs by reviewing the running product and feeding back precise, incremental corrections — the whole v0.61 map arc, and the ~48-version v0.61.29 → v0.61.76 arc, is review-then-refine. |
| Operating a build-time data pipeline | **5** | This arc — directs offline fetch/enrich/join scripts that he runs locally with his own API keys (GOOGLE_PLACES_API_KEY, LTA_ACCOUNT_KEY, ONEMAP_API_KEY), with the repo shipping scaffolds/empty data files and graceful fallback when a data file is absent. |

---

## 3. Working Style

| # | Observation | Status |
|---|---|---|
| 1 | **Terse, quote-forward.** Provides operator-INTENT verbatim, often with screenshot context; expects the response to lead with *what* changed. | stable |
| 2 | **Recurring-bug stamina.** Re-surfaces a defect until the upstream source is found; rejects "we added more guards". | stable |
| 3 | **Decisive at decision-points.** Given a 2–4-option question, picks one (often the scope-expanding option) and moves on. | stable |
| 4 | **Per-PR doc cadence enforcer.** The Journal is updated after opening *and* after merging every substantive PR. | stable |
| 5 | **Owner-only / hidden-command preference.** Diagnostic + admin surfaces stay off `setMyCommands` and behind `isOwnerChat`. | stable |
| 6 | **Two-locale parity expected on user-facing copy.** | stable |
| 7 | **Accepts mid-arc deferrals if logged.** Treats `[KNOWN GAPS]` + `DF-NN` entries as legitimate work breaks. | stable |
| 8 | **`Status cold!` — clean session boundaries.** An explicit cue marks a session logically closed. | stable |
| 9 | **Rapid merge-then-next cadence.** Reviews each PR, says "Merge", immediately directs the next change — a fast, sustained iteration loop. | stable |
| 10 | **Pivots cleanly when a shipped decision proves wrong.** Reverses course explicitly and without ego. | stable |
| 11 | **Investigate-before-act on risk.** Diagnose before changing code; default to the reversible path. | stable |
| 12 | **Opinionated on UX; rejects generic.** Rejects un-intuitive layouts, generic wording, silent auto-behaviour. | stable |
| 13 | **Accessibility- and inclusivity-minded.** Added the ♿️ accessibility marker; asked for per-exit lift info; caught a dark-mode legibility bug. | stable |
| 14 | **Reviews the live product surface.** Direction comes from using the running bot/TMA and reporting what looks wrong, with screenshots — not from spec documents. | stable |
| 15 | **Phased "CR" change-request idiom.** New (this arc): issues numbered change requests (CR1…CR10) and runs each as a tracked, multi-PR programme — phase by phase, shippable increments, later phases stubbed ahead of UI. The CR6 station-info programme spanned Phases 1→2a→2b→3 across 5+ PRs. | new — v0.61.32 onward |
| 16 | **Hands over operator-supplied UI copy verbatim, as a full template.** New (this arc): for the station-info card and the `/transport` nearest-bus-stops reply the operator supplied the *entire* message layout — line order, glyphs, phrasing, count placeholders — to be reproduced exactly. The agent transcribes, it does not draft. | new — v0.61.60 / v0.61.73 |
| 17 | **Runs the paid/keyed data fetches himself.** New (this arc): for ingestion scripts the operator's expressed pattern is "ship the script + an empty/scaffold data file; I run the fetch locally with my key". Keeps paid API calls off the build box and out of CI (consistent with Gate G4). | new — v0.61.39 onward |
| 18 | **Treats vault passes as a recurring discipline, not a one-off.** New (this arc): the v0.61.76 fresh-master + vault pass deliberately repeats the v0.61.28 pass — same shape, AU Recipe overridden for the pass, no code change, no version bump — establishing vaulting as a periodic, operator-scheduled checkpoint. | new — v0.61.28 → v0.61.76 |

---

## 4. Builder Mode

| Mode | Trigger | Artifact form |
|---|---|---|
| **Director** | "What should we build next?"; bug screenshots; quoted feedback; a numbered "CR" list | A numbered option / CR list; operator picks; the Journal records the decision. |
| **Architect** | Cross-module / cross-TMA refactors; contract changes; multi-phase data-layer programmes | Plan → phased implementation; a Technical-doc delta + a Deprecated-decisions row; later phases stubbed ahead of UI. |
| **Operator** | Tactical bug fix; UI polish; "the screenshot still shows X"; a production hotfix | Direct edit, version bump, preflight, PR + Journal in one pass. The v0.61.62 blank-TMA hotfix is the type case. |
| **Consolidator** | "Create a fresh set of documents"; end-of-arc catch-up; vault | A deliberate docs-only pass — fresh masters across the folders + a `vault/<v>/` snapshot, no code change. The v0.61.28 and v0.61.76 passes are the type cases. |

---

## 5. Standing preferences (recorded for AU-7 traceability)

| # | Preference | Rationale |
| :--- | :--- | :--- |
| P-1 | `VIBE_JOURNAL_KEY` is a short numeric value (4-8 digits) | Tap-typed on mobile. |
| P-2 | `Status cold!` closes a session; `Status warm` keeps it open | Improves Journal `[HDR]` boundaries. |
| P-3 | Hidden owner-only commands route through `isOwnerChat()` | Production gating tight; local dev open. |
| P-4 | Per-PR Journal update **after open AND after merge** | "After open" captures intent; "after merge" the squash SHA. |
| P-5 | Recurring bug fixes warrant a root-cause sweep, not another guard layer | The `[object Object]` precedent. |
| P-6 | Zero-results UX = silent auto-retry + visible CTA, never a silent empty-state | D-130. |
| P-7 | Venue-card price uses a country-currency prefix (S$ / M$ / US$), never a 💲 emoji | D-183. |
| P-8 | New payload fields must reach **every** render surface in the same change | D-183. |
| P-9 | Curated-dataset dedup keys derive from stable fields (`name`\|`address`) | D-201. |
| P-10 | User-facing surfaces are labelled "Experimental", not "Beta" | Honest framing. |
| P-11 | Rate-limit caps are sized to "catch bugs, not money" | Operational philosophy. |
| P-12 | `mapOverlays.js` stays byte-identical across the three map TMAs | No shared package across separate Vite apps; edit all copies together. |
| P-13 | A new map-overlay layer prioritises the station/feature in focus, then falls back to vicinity | The station-scoped Exits/Taxis chips; the building-anchored search direction. |
| P-14 | `MapControls.jsx` (and `mapOverlays.js`) stay byte-identical across the three map TMAs | New (this arc): the unified `MapControls` component was added to all three Vite apps; every map-control change touches all three copies together — the same no-shared-package constraint as P-12, extended to the controls layer. |
| P-15 | Ingestion data fields are copied verbatim from the source API; a missing field is `null`, never invented | New (this arc): the explicit non-invention rule on the bus-stop / clinic / hospital / 24-hour-places fetchers. |
| P-16 | Ingestion scripts ship with an empty/scaffold data file; the operator runs the keyed fetch locally | New (this arc): keeps paid API calls off the build box and out of CI; the build degrades gracefully when the data file is absent. |
| P-17 | A header that promises a count ("Nearest 3 …") must be backed by logic that actually delivers that count | New (this arc): D-rationale for the v0.61.74 Train search-radius widen (1500 m → 5000 m) so MRT-sparse spots like VivoCity still return 3 stations — a "broken promise" in copy is a bug. |
| P-18 | Map-control labels carry state and count in their text, not in a colour flip or a decorative icon | New (this arc): "🎨 Colour mode" / "🎨 Greyscale mode" state-in-text pills; "☑️ Monochrome" shows the action; count-aware "Show all {n} bus stops" / "Compare all {n} carparks" with the 🗺/🚏 icons dropped. |

---

## 6. Professional & commercial contribution profile

> This section profiles where the Human Lead can contribute professionally or commercially.

**Demonstrated, repo-verifiable competencies**

- **Full-lifecycle product ownership** — took a real product from concept to a deployed, versioned, documented v0.61.76 across ~574 PRs, single-handed, as the directing intelligence over an AI-assisted workflow.
- **AI-assisted engineering leadership** — fluent at directing LLM-based development: framing intent precisely, surfacing decision forks, budgeting LLM cost and latency, keeping AI output best-effort and gracefully-degrading; this arc — the phased "CR" change-request idiom shows mature programme-level direction of an AI workflow.
- **Systems & architecture thinking** — a layered design, a five-app client family on a shared contract, a shared map-overlay + map-controls controller, a build-time geo-data pipeline (station-info join, bus-stop triangulation, multi-source ingestion), and a bespoke documentation-governance system.
- **Product & UX judgement** — evidence-driven decisions, explicit trade-off framing, bilingual release discipline, designed-not-accidental empty/error/degradation handling, strong interactive-map UX taste, and operator-authored UI copy templates reproduced verbatim.
- **Operational & security competence** — Redis TTL design, third-party-API quota awareness, open-data ingestion across LTA / NEA / OneMap / CHAS / Google Places, CI/CD on Railway, fast production-hotfix diagnosis (the v0.61.62 blank-TMA incident), infrastructure diagnosis, and a sequenced security-hardening programme.
- **Documentation & governance** — every change traced, every decision recorded with authority, a system explicitly built to survive hand-off, with periodic vault snapshots for reproducibility and rollback (the v0.61.28 and v0.61.76 passes).

**Opportunity fit**

| Opportunity | Why the evidence supports it |
|---|---|
| **Employment / leadership** — product owner, founding/senior engineer, technical lead, head of product for an AI-enabled product | End-to-end ownership, architecture decisions, release discipline, team-ready documentation. |
| **Consulting & strategy advisory** — product strategy, AI-adoption advisory, engineering-process design | The doc-governance system, the confirmation-gate model, the phased-CR programme idiom, and the operational patterns are transferable methodology. |
| **AI-related projects & collaborations** — applied-LLM product work, AI-assisted-development practice design | Demonstrated cost-aware, latency-bounded, degradation-safe LLM integration in production. |
| **Technical partnerships** — integration-heavy products (maps, transit, messaging-platform apps) | Working integrations across Google Places/Routes, LTA DataMall, NEA, OneMap, data.gov.sg, CHAS, Telegram, Gemini. |
| **Product development** — 0→1 builds, Mini-App / conversational-interface products | A live, multi-surface product family shipped solo. |
| **Research, ecosystem & innovation** — AI-assisted-development methodology, agentic-workflow practice | The repo is itself a documented case study of disciplined AI-assisted engineering at scale. |

---

## Rule reference

- **AU-3:** Persona is a permanent record; no deletions. This fresh master supersedes `persona-0_61_28` as the current-state reference under explicit operator instruction (the v0.61.76 vault pass, following decision D-1028); the prior file remains on disk.
- **AU-7:** future re-ratings quote the prior rating and append rationale + serial number.

## Amendments

(None — fresh master. The Persona evolution trace is `persona-0_60_157` → `persona-0_60_213` → `persona-0_61_28` → this file. New content in this master is the v0.61.29 → v0.61.76 arc: domain rows on open-data ingestion; capability rows on multi-phase "CR" direction and build-time pipelines; Working-Style observations #15–#18; standing preferences P-14–P-18 — all added per AU-1, none deleting prior content.)
