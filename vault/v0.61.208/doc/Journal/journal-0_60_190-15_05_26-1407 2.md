(№ 157 - 15-05 '26 14:07 SGT)

# Journal — soleat v0.60.190

> Operator: *"Create a plan in MD file (about creating a similar project like this but in WhatsApp), I would create another Claude Code session point to the different github repository - gia-wa. Be extensive in the plan"*.

Single delivery: a comprehensive, self-contained build plan for the WhatsApp port (Gia-WA), persisted into this repo so a fresh Claude Code session pointing at `ang-kl/Gia-WA` can read it cold and execute without any chat-context handoff.

## New

### [HDR] #157 | 14:07:00 SGT 15-05-26 | v0.60.190 Gia-WA build-plan handoff document committed to repo | 3 files (1 new BUILD-PLAN + journal + serial state) | 1 PR |

- **[INTENT]**
  1. Per the operator's 2026-05-15 pivot, Gia-WA is no longer a subdirectory of `ang-kl/gia` — it becomes its own repository (`ang-kl/Gia-WA`). Source-code work in that repo is currently blocked on sandbox access (MCP allowlist + git proxy + GitHub permissions); see the access checklist in §16.3 of the new plan.
  2. While access is being provisioned, persist the full architectural plan as a durable artifact inside `ang-kl/gia` so the receiving session can fetch it via raw URL (or operator paste) without needing this session's chat history. The operator explicitly asked for "extensive" — the document is structured to be read cold, in order, by an agent who has never seen this conversation.

- **[DELTA]**

  **`Gia-WA/BUILD-PLAN.md`** — NEW, 720 lines, ~30 KB. Sixteen sections plus two appendices. Structure:

  - **§0 How to use this document** — protocol for the receiving session (read in full first, don't silently deviate, never touch `ang-kl/gia`, branch + draft PR per phase, `gia-preflight` carries over, standing Journal rule applies).
  - **§1 Mission** — what Gia-WA is, what success looks like.
  - **§2 Operator directives** (non-negotiable until revised) — D-1 Meta Llama only / D-2 zero edits to root / D-3 separate repo / D-4 Journal per PR / D-5 `gia-preflight` / D-6 hold until access lands.
  - **§3 Source-project overview** — Telegram surface audit (40 inline-keyboard shapes, 8 HTML `parse_mode` sites, 5 Mini Apps, `twa-auth.js`, webhook secret at `index.js:7726`), AI integration audit (`@anthropic-ai/sdk@^0.40.0` at `llm-client.js` — Claude usage is competent but at no frontier feature: zero caching, zero thinking, zero streaming, zero Batch/Files/Citations/Memory, `claude-opus-4-7` referenced but never called), data layer (`data/cuisines.js`, `vault/<version>/`), what does NOT need porting.
  - **§4 WhatsApp surface — what's possible and what's not** — Cloud API basics, message types matrix, **Flows in depth** (component vocabulary, encrypted Data Channel contract with RSA-OAEP + AES-GCM-128 and the flipped-IV gotcha, publishing lifecycle DRAFT→PUBLISHED + versioning), 24-hour customer-care window + templates, formatting/escaping (translates `gia-preflight`'s HTML-escape rule to WhatsApp-markup-escape: `*` `_` `~` `` ` ``), E.164 identity vs Telegram user IDs.
  - **§5 Feature mapping** — per Mini App: Cuisine → Flow (good fit, build); Menu → List Message (skip Flow); Hawker → Flow (marginal, conditional Phase 5); Transport → keep chat-driven (poor fit); Oversight → don't port.
  - **§6 Architecture — `ang-kl/Gia-WA` target shape** — full repo layout (§6.1), module-by-module responsibilities (§6.2), happy-path data flow diagram (§6.3, ASCII), dependencies (§6.4 — `axios`, `express`, `ioredis`, `vitest`; **explicit non-dependencies** including `@anthropic-ai/sdk`).
  - **§7 AI layer — Meta Llama only** — provider strategy (Groq primary + Together AI fallback), model router table (3.1-8B classify / 3.3-70B converse+Stage-A+sanctuary / 3.2-11B-vision), prompt structure (token-trimmed, JSON schema strict), function calling (web-search via Tavily, google-reviews, vault-lookup) with the **tool-arg hallucination guard**, JSON-mode / strict schema, honest losses vs Anthropic (no caching, no extended thinking, no server-side `web_search_20260209`, weaker tool-use determinism), mitigations (Redis dedup, per-call telemetry, streaming-aware delivery).
  - **§8 Data layer** — read-only bridge from `ang-kl/gia` with three options (npm package vs submodule vs HTTP fetch; default submodule for Phase 0), Redis keyspace `wa:` prefix (separate DB number), user identity (E.164 → tenant key).
  - **§9 Security & secrets** — full env-var list, Flow encryption keypair lifecycle (RSA-2048, quarterly rotation, 24h dual-acceptance window), provider API key redaction in telemetry, Telegram-bot-token isolation (must never appear in Gia-WA env).
  - **§10 Testing strategy** — unit (vitest, ≥80 % coverage on transport/crypto/router/identity), Meta-published Flow encryption test vectors (non-negotiable), integration on WA Business test number (per-phase smoke), pre-commit checklist (translated from `gia-preflight`), CI shape.
  - **§11 Deployment** — Railway parallel project recommended (separate blast radius from Soleat); Flow publish lifecycle in CI; domain/phone provisioning sequence (1–7 days for business verification).
  - **§12 Execution order** — seven phases, each one branch + one draft PR: Phase 0 bootstrap → Phase 1 skeleton+transport+format → Phase 2 Llama wrapper+router → Phase 3 inbound text path → Phase 4 Cuisine Flow (the big one) → Phase 5 Hawker Flow (conditional, **hard stop for operator approval**) → Phase 6 telemetry+soak → Phase 7 ADRs.
  - **§13 Success criteria per phase** — table.
  - **§14 Open questions to confirm with operator before Phase 4** — vault bridge option, Llama 4 availability, web-search provider, Hawker phase gate metric, cross-transport identity linking, test-number recipient limits.
  - **§15 Reference links** — Meta WhatsApp Cloud API, Flows reference, encryption spec, component spec, templates, webhook signatures, Groq/Together docs, Tavily, Llama model cards.
  - **§16 Hygiene rules for the receiving session** — branch/commit/PR style, what never to touch, **access verification on session startup** (4-step check before any code: `git remote -v`, `git ls-remote origin`, MCP test call, GitHub permission set), when to ask vs when not to ask the operator.
  - **Appendix A** — full file inventory (40+ files under `ang-kl/Gia-WA/`; zero edits to `ang-kl/gia`).
  - **Appendix B** — glossary (TMA, Flow, MSISDN, R.E.D, AU recipe, Vault).

  Source of the document content: the approved plan at `/root/.claude/plans/explain-depth-about-whatsapp-happy-rainbow.md` (the session's plan-mode artifact). The plan file was approved by the operator after a single revision cycle (initial plan denied; rewrite as a handoff document approved).

  **`doc/Journal/journal-0_60_190-15_05_26-1407.md`** — this entry (#157), authored under the standing rule D-4.

  **`doc/.serial-state.yml`** — counters bumped: `journal 156 → 157`, `commit 163 → 164`, `current_chat_counter 21 → 22`. Anchor re-stamped: `15-05 '26 14:07 SGT` / source `claude_v0_60_190_gia_wa_build_plan_handoff_doc`.

  **`package.json`** — `0.60.189 → 0.60.190` (PATCH; docs-only).

- **[VERIFICATION (sandbox)]**

  ```bash
  $ wc -l Gia-WA/BUILD-PLAN.md
  720 Gia-WA/BUILD-PLAN.md

  $ head -3 Gia-WA/BUILD-PLAN.md
  # Gia-WA — WhatsApp Port of Soleat ("Gia4lunch") — Build Plan

  **Handoff document for a fresh Claude Code session targeting `ang-kl/Gia-WA`.**

  $ grep -c '^## ' Gia-WA/BUILD-PLAN.md
  18    # §0–§16 plus Appendix A + Appendix B
  ```

  No `.js` files touched — `node --check` n/a. No runtime contract change — vitest unchanged from green-on-main baseline.

- **[STATUS]** DRAFT PR opened on `ang-kl/gia` against `main`. Merging is at operator's discretion (likely after a glance through the plan to confirm it matches their intent). Once merged, the file lives at `https://github.com/ang-kl/gia/blob/main/Gia-WA/BUILD-PLAN.md` and `https://raw.githubusercontent.com/ang-kl/gia/main/Gia-WA/BUILD-PLAN.md` — both routes the receiving Claude Code session can fetch.

- **[TEST]**

  - Operator opens the draft PR diff: 720 lines of new doc visible at `Gia-WA/BUILD-PLAN.md`; the journal entry visible at `doc/Journal/journal-0_60_190-15_05_26-1407.md`; serial-state + `package.json` deltas obvious.
  - Operator opens the rendered markdown in GitHub: §0 reads as a clean briefing for a cold-start session; §6.1 file-tree renders as a fenced code block; §6.3 ASCII diagram readable; tables in §5, §7.2, §13 render correctly.
  - (After merge) Operator points a new Claude Code session at `ang-kl/Gia-WA` and instructs it to read `https://raw.githubusercontent.com/ang-kl/gia/main/Gia-WA/BUILD-PLAN.md` as the first action; the new session can identify Phase 0 from §12 and report back the access-verification checklist from §16.3.

- **[KNOWN GAPS]**
  - **PR #426** (`claude/gia-wa-readme-pointer` branch) — opened earlier today as a draft to collapse `Gia-WA/README.md` to a one-line pointer. Operator subsequently restored the README to the full 41-line scope marker, signalling that the pointer revision is not wanted. PR #426 should be closed without merging; flagged here as a follow-up housekeeping item.
  - **CI on PR #426** — three failing jobs (syntax / vitest / TMA build) in the same 3-second infra pattern as PR #419; root cause not yet diagnosed. Hypothesis (per the session's pre-revision diagnostic plan): lockfile drift (`package-lock.json` at `0.58.27` vs `package.json` at `0.60.x`) causing `actions/setup-node@v4 cache: npm` to fast-fail. Action when bandwidth permits: regenerate the lockfile via `npm install --package-lock-only` in a small dedicated PR. **Not done in this PR** — keeps this delivery docs-only and one-purpose.
  - **Gia-WA access provisioning** — sandbox MCP allowlist + local git proxy + GitHub App / PAT permissions on `ang-kl/Gia-WA` not yet open. The receiving session will hit `repository not authorized` on first push until the operator provisions all three layers (full checklist in §16.3 of the new plan).
  - **VibeCodingRecord row for this PR** — still due (carried from prior gaps). When convenient: append a row to `doc/VibeCodingRecord/data/prs.ndjson` + its file list to `data/pr-files.tsv`, bump `GEN_DATE`, run the generator. Doing it inline here would broaden the PR scope.
  - **DF-62 / DF-63 (carried)** — PR-tab data source via `doc/VibeCodingRecord/data/prs.ndjson` for the hosted Vibe Journal; CI auto-regen on push to main.

## Confirmation Gates

| Gate | Authority | Stamp |
|---|---|---|
| G1 — version bump | _PATCH 0.60.189 → 0.60.190 (docs-only; no runtime contract change)_ | auto |
| G2 — destructive action | _additive — single new doc file `Gia-WA/BUILD-PLAN.md`; no existing content overwritten; `Gia-WA/README.md` untouched (operator's earlier revert preserved)._ | auto |
| G3 — new decision rule | _No new rule. The plan codifies operator directives D-1 through D-6 that already exist in chat; this file makes them durable but doesn't introduce them._ | auto |
| G4 — paid external API call | _n/a — no API call from this PR. The plan describes future Groq/Together/Tavily/Google calls that will land inside `ang-kl/Gia-WA`._ | — |

## Amendments

(None. v0.60.189 (#156) is the prior entry.)

## Deleted

(Nothing deleted from the AU-protected trail. PR #426 (separate branch, still draft) proposed a destructive replacement of `Gia-WA/README.md`; the operator's manual restoration of the full scope-marker README has effectively reverted that proposal — once PR #426 is closed, the AU-1 / AU-6 chain is preserved without active edit on this branch.)
