(№ 333 - 25-05 '26 07:00 SGT) v0.61.132

# Journal — soleat v0.61.132 (Register increment — O-20 + O-23 closed; D-61.129 + D-61.130 logged)

> Operator (from the v0.61.131 KNOWN GAPS follow-up): *"Register
> increment for O-20 / O-23 / D-61.129 / D-61.130"*. The three-PR
> Cuisine-TMA arc (v0.61.129 backend → v0.61.130 TMA UI → v0.61.131
> pill-reorder + helper extract + 33 inline tests) closed both
> Open Items but the Register hadn't reflected that yet. Pure
> docs PR — no code change.
>
> Commit 345 / journal 333 / build 135 / register 119, on branch
> `claude/v0.61.132-register-o20-o23` off `main` at v0.61.131
> (commit `4d061d0`, PR #632).

## New

### [HDR] #333 | 07:00 SGT 25-05-26 | v0.61.132 — Register increment closes O-20 / O-23 + logs D-61.129 / D-61.130 (docs only) | 3 files | 1 PR | 1 build |

- **[INTENT]** Bring `doc/Register/` current with the v0.61.129-131
  arc. Specifically:
  - **O-20** *"Cuisine TMA 'Tell me' box does not run place
    detection"* (from `register-0_61_124-24_05_26-1636.md`) is
    moved Open → Completed (`C-BA` arc).
  - **O-23** *"Progressive radius widening on weak Fruits/Durian
    results"* (from `register-0_61_127-24_05_26-1754.md`) is
    moved Open → Completed (`C-BB` arc).
  - **D-61.129** decision row logged — `/api/cuisine/search` is
    now place-aware (Tell-me text → search-centre pivot) and
    special-mode searches widen progressively per the
    `scripts/Create_2_buttons.MD` "Fallback behaviour" clause.
  - **D-61.130** decision row logged — TMA layer renders the
    new payload fields as a 📍 pill + a *"· widened to N km"*
    caption; v0.61.131's pill-first reorder is folded into the
    same row (single decision, two implementation phases).

- **[DELTA]**
  - **`doc/Register/register-0_61_132-25_05_26-0700.md`** (NEW)
    — incremental Register entry on top of the v0.61.83 master
    + v0.61.124 / v0.61.127 increments. Per AU-1, items move
    between sections rather than getting deleted: O-20 / O-23
    annotated as **→ Completed (C-BA / C-BB)** with refs to the
    three PRs that landed each closure; the original Open-item
    text remains in the prior register increments (this file
    only annotates the status change). O-21, O-22, O-24 carry
    forward unchanged.
  - **`package.json`** — `0.61.131 → 0.61.132`.
  - **`doc/.serial-state.yml`** — `commit 344 → 345`, `build
    134 → 135`, `journal 332 → 333`, `register 118 → 119`;
    re-anchored 25-05 '26 07:00 SGT (SG air-temperature
    sovereign source); new comment-history block prepended.
  - **`doc/Journal/journal-0_61_132-25_05_26-0700.md`** — this
    entry.

- **[VERIFICATION (sandbox)]**

  ```
  $ grep -c '^| O-2[0234] \|^| C-B[AB] \|^| D-61\.13[01] ' \
        doc/Register/register-0_61_132-25_05_26-0700.md
       6  (O-20 + O-21 + O-22 + O-23 + O-24 carry-forward row,
            C-BA + C-BB completed rows, D-61.129 + D-61.130
            decisions = 6 expected new ledger lines + per-cell
            references; actual matches are higher because grep
            also picks each cell.)
  ```

  No code change; `node --check` / `vitest` / `npm run build`
  not applicable for a doc-only PR (matches the v0.61.127 docs-
  only spec correction precedent).

- **[STATUS]** PROD-bound on squash-merge to `main`. Docs only;
  no Railway build behaviour change.

- **[TEST]** Operator (post-deploy): `cat
  doc/Register/register-0_61_132-25_05_26-0700.md` (or open in
  any markdown viewer). O-20 + O-23 status row reads **→
  Completed (C-BA / C-BB)**; the Decisions table has fresh
  D-61.129 + D-61.130 entries that reference the three PRs by
  number and the spec doc by path.

- **[KNOWN GAPS]**
  - **D-61.131 not logged** — the operator's directive named
    only D-61.129 + D-61.130, so the v0.61.131 pill-reorder +
    helper-extract + inline-tests work is folded into the
    D-61.130 row (since it's the implementation phase of the
    same operator-visible decision) rather than getting its own
    row. If a separate D-61.131 row is wanted (e.g. to record
    the "extract helpers when adding tests" pattern as a
    re-usable decision), it can land in the next register
    increment.
  - **VibeCodingRecord ledger refresh** — the hosted Vibe
    Journal at `/doc/vibe-journal.html` hasn't been refreshed
    since PR #629 (v0.61.128 covered #553–#627). PRs #630-#633
    are now 4 PRs deep; due for a refresh once a couple more
    PRs land or when convenient.
  - **Vault snapshot** for the v0.61.129-131 arc still not
    cut — operator-triggered.
  - **Michelin branch raw `freeText`** — carries forward
    (Michelin + place-anchor remains a rare combination).
  - **Full vitest suite** still not run (~3 h on Node 24 per
    O-21); the per-file fast path is the working pattern.

- **Confirmation Gates**

  | Gate | Resolution | Date |
  |------|-----------|------|
  | G1 — version bump | PATCH `0.61.131 → 0.61.132`. Docs-only PR, no behaviour change. | 25-05 '26 |
  | G2 — destructive action | _n/a_ — pure addition (new register file, append-only per AU-1). Prior register increments untouched; the original Open-item text remains in `register-0_61_124-…` / `register-0_61_127-…` per AU-7 (this file only annotates the status change). | 25-05 '26 |
  | G3 — new decision rule | _n/a_ — D-61.129 + D-61.130 are LOGGING decisions already taken in PRs #630 / #631 / #632, not proposing new ones. | 25-05 '26 |
  | G4 — paid external API call | _n/a_ — docs. | 25-05 '26 |

## Amendments

_None — pure follow-up._

## Deleted

_None._
