(№ 335 - 25-05 '26 07:40 SGT) v0.61.134

# Journal — soleat v0.61.134 (Cuisine TMA — Michelin + place-anchor follow-up)

> Operator (from the v0.61.131 KNOWN GAPS shortlist surfaced after
> PR #634 closed the ledger catch-up): *"Michelin-branch raw-freeText
> follow-up"*. The v0.61.129 PR (#630) routed the post-strip `ftRawIn`
> through misrep + cookMethod + chipDisambig probes and through the
> freetext-log payload, but the `isMichelinSearch` early-return at the
> top of `/api/cuisine/search` still passed `req.body?.freeText` (raw,
> pre-strip) to `handleMichelinSearch`. Logged in the v0.61.129 journal:
> *"Michelin + place-anchor remains a rare combination — logged as
> future work."* This PR closes it.
>
> Commit 347 / journal 335 / build 137, on branch
> `claude/v0.61.134-michelin-place-anchor` off `main` at v0.61.133
> (commit `b6f577f`, PR #634 — VibeCodingRecord catch-up #628-#633).

## New

### [HDR] #335 | 07:40 SGT 25-05-26 | v0.61.134 — Michelin path threads v0.61.129 place anchor + post-strip freeText | 4 files | 1 PR | 1 build |

- **[INTENT]** Route the v0.61.129 O-20 place-anchor pivot through
  the Michelin search branch too. Currently:
  - When a user has the Michelin chip ON and types
    *"ramen in Tiong Bahru"*, the place-anchor detection still fires
    (it runs BEFORE the `isMichelinSearch` check), so `searchCenter`
    + `searchRadius` are correctly pivoted. ✓
  - But the freeText passed to `handleMichelinSearch` is the RAW
    *"ramen in Tiong Bahru"*, not the stripped *"ramen"* — so the
    name-substring filter applied to the curated Michelin list
    drops matches like *"Hashida"* (a Tiong Bahru ramen-adjacent
    starred eatery) because its name doesn't contain *"in Tiong
    Bahru"*. ✗
  - And the response payload `handleMichelinSearch` writes doesn't
    carry `placeAnchor` / `placeAnchorQueryRemainder` because those
    are set BELOW the Michelin early-return in `/api/cuisine/search`.
    So the TMA's 📍 pill never renders on Michelin searches even
    when a place was detected. ✗

- **[DELTA]**
  - **`index.js`** — three line-level edits:
      - **Call site at `~10787`** (inside `isMichelinSearch` branch):
        `freeText: req.body?.freeText || ''` → `freeText: ftRawIn`.
        Plus a new `detectedPlaceAnchor` parameter threaded through
        from the outer scope.
      - **`handleMichelinSearch` signature at `~6593`** — added
        optional `detectedPlaceAnchor = null` to the destructured
        params; existing call sites that don't pass it (none other
        than `/api/cuisine/search`'s call site — confirmed via
        `grep -n handleMichelinSearch /tmp/work-134/index.js`)
        get the default.
      - **`handleMichelinSearch` payload at `~7359`** — when
        `detectedPlaceAnchor` is set, spread
        `{ placeAnchor, placeAnchorQueryRemainder }` onto the
        response, mirroring the non-Michelin path's
        `payload.placeAnchor` write at the bottom of
        `/api/cuisine/search`.
  - **`package.json`** — `0.61.133 → 0.61.134`.
  - **`doc/.serial-state.yml`** — `commit 346 → 347`, `build
    136 → 137`, `journal 334 → 335`; re-anchored 25-05 '26 07:40
    SGT (SG air-temperature sovereign source); new comment-history
    block prepended.
  - **`doc/Journal/journal-0_61_134-25_05_26-0740.md`** — this
    entry.

- **[VERIFICATION (sandbox)]**

  ```
  $ node --check index.js
       (no output → OK)
  $ grep -n 'handleMichelinSearch' index.js | head -5
       1898:     /api/cuisine/search + /copy-one + /copy-all + handleMichelinSearch
       6593: async function handleMichelinSearch({ … detectedPlaceAnchor = null })
       9218:        // 'michelin' slug and branches to handleMichelinSearch which
      10777:          // handleMichelinSearch so it can filter the curated list by
      10784:          return await handleMichelinSearch({
       (function signature updated, sole call site updated)
  ```

  Behavioural walkthrough:
  - **Michelin + no Tell-me text** → `ftRawIn=''`,
    `detectedPlaceAnchor=null`; behaviour identical to v0.61.133.
  - **Michelin + "ramen"** → `ftRawIn='ramen'`,
    `detectedPlaceAnchor=null`; behaviour identical to v0.61.133.
  - **Michelin + "ramen in Tiong Bahru"** → place-detector hits Tiong
    Bahru MRT, `searchCenter` pivots, `searchRadius` → 1500 m,
    `ftRawIn='ramen'` (stripped), `detectedPlaceAnchor` populated.
    `handleMichelinSearch` filters curated list by name-substring
    *"ramen"* (not *"ramen in Tiong Bahru"*) AND surfaces
    `payload.placeAnchor` so the TMA renders the 📍 pill.
  - **Michelin + "Tiong Bahru"** → similar; `ftRawIn=''` (no
    qualifier), `detectedPlaceAnchor` populated. Filter is by-name
    against the empty string (matches everything → behaves as
    Michelin-list-near-Tiong-Bahru-MRT).

- **[STATUS]** PROD-bound on squash-merge to `main`. Auto-deploys
  via Railway. No web build needed — TMA already accepts
  `payload.placeAnchor` since v0.61.130 (the field shape this PR
  emits is identical).

- **[TEST]** Operator (post-deploy):
  - Open Cuisine TMA, toggle the ✳️ Michelin chip + type
    *"ramen in Tiong Bahru"* in the Tell-me box, tap →. Expect
    the 📍 *"Showing 'ramen' near Tiong Bahru MRT"* pill at the
    top of the result-list info-card stack (now also on Michelin
    responses, not just non-Michelin). The list should be Michelin
    entries clustered around Tiong Bahru MRT.
  - Negative — toggle ✳️ Michelin + type *"laksa"* (no place).
    No pill (no anchor detected), behaves exactly as v0.61.133.

- **[KNOWN GAPS]**
  - **No inline unit test** for the Michelin path — the handler is
    ~700 lines and tightly coupled to Places + Redis + michelin-
    walk; no clean unit surface without integration infra. The
    v0.61.131 `cuisine-tellme-anchor.test.js` covers the helper
    that produces `detectedPlaceAnchor` + `ftRawIn`; this PR is a
    routing fix on those values. Verified via `node --check` +
    spec walkthrough.
  - **Vault snapshot** for the v0.61.129-134 arc still
    operator-triggered.
  - **O-21 / O-22 / O-24** carry forward unchanged. O-21's
    per-file fast path remains the working pattern.

- **Confirmation Gates**

  | Gate | Resolution | Date |
  |------|-----------|------|
  | G1 — version bump | PATCH `0.61.133 → 0.61.134`. Three line-level edits, behaviour-preserving for the no-anchor case. | 25-05 '26 |
  | G2 — destructive action | _n/a_ — pure additions (new param with default; new payload fields via conditional spread). The prior `req.body?.freeText` value remains accessible to the Michelin handler via `req.body` if any internal code path still wants it. | 25-05 '26 |
  | G3 — new decision rule | _n/a_ — closes the v0.61.129 KNOWN GAP. | 25-05 '26 |
  | G4 — paid external API call | _n/a_ — no new outbound traffic; the stripped freeText reduces the chance that a per-entry Places searchText returns 0 hits when the user typed a multi-word query. | 25-05 '26 |

## Amendments

_None — pure follow-up._

## Deleted

_None._
