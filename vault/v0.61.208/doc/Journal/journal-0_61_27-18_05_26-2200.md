(№ 236 - 18-05 '26 22:00 SGT) v0.61.27

# Journal — soleat v0.61.26 → v0.61.27 (/transport train: station codes)

> Operator: in /transport's Train reply, the nearest-3-stations list must show each
> station's code (NS21, DT11, …), not just the line code.

## New

### [HDR] #229 | 22:00:00 SGT 18-05-26 | v0.61.27 — /transport train nearest-stations show the station code | ~3 kb | 3 files | 1 PR | 0 build |

- **[INTENT]** Operator, reviewing the `/transport` → Train reply: *"the <station code> must
  be included and not just the station line."* The current rows read
  `🟥 NSL · 🟦 DTL Newton` — line codes only. They should carry the actual station code:
  `🟥 NS21 · 🟦 DT11 Newton`.

- **[DELTA]**
  - **`mrt-lines.js`** — new `codedLinesForStation(name)`: returns `[{ code, line }]` pairs
    (e.g. Newton → `[{NS21,NSL},{DT11,DTL}]`), sourced from `data/mrt-coords.json`, where
    each station's `codes` and `lines` arrays pair index-for-index. Lazily loaded + cached,
    keyed by the existing `normaliseStationName`. Exported.
  - **`index.js`** — the `/transport` Train flow's nearest-3-stations renderer now builds
    the line prefix from `codedLinesForStation`: each station code is shown with its line's
    colour emoji (`🟥 NS21 · 🟦 DT11 `). Falls back to the old line-code prefix
    (`linesForStation`) for any station absent from `mrt-coords.json`. Station codes are
    HTML-escaped, consistent with the prior code.
  - **`__tests__/mrt-lines.test.js`** — new `codedLinesForStation` suite (4 tests:
    interchange pair, single-line station, suffix/casing tolerance, unknown → `[]`).

- **[VERIFICATION (sandbox)]**

  ```bash
  $ node --check index.js mrt-lines.js   → OK
  $ node -e "…codedLinesForStation('Newton')…"
      → [{code:NS21,line:NSL},{code:DT11,line:DTL}]   (Orchard → NS22/TE14, Somerset → NS23)
  $ npm test -- --run                    → 1928 passed (1928)  (+4)
  ```

  `gia-preflight`: §0 syntax + tests green; §1 `codedLinesForStation` is exported and
  imported in the test; §2 the station code is run through `escapeHtmlForTelegram` before
  it enters the HTML `stationRow` message; §3 the renderer's fallback path keeps an empty
  prefix for unknown stations — no silent break.

- **[STATUS]** DRAFT → PR (base `main`, branch `claude/review-gia4lunch-readme-3s798`).
  Server-only — deploys with the next Railway push of `index.js`.

- **[TEST]** `/transport` → Train, with a location set: the "Nearest 3 stations" rows show
  `🟥 NS21 · 🟦 DT11 Newton`-style codes; interchanges list every code; a station missing
  from `mrt-coords.json` still renders (line-code fallback or bare name).

- **[KNOWN GAPS]** Exit feature Part B (♿ lift availability + surrounding buildings) still
  due. `/api/transport/station-context` remains unused (noted in #228).

## Amendments

(none)

## Deleted

(none — the line-code prefix is retained as the fallback path.)

## Confirmation Gates Satisfied

| Gate | Authority | Stamp |
| :--- | :--- | :--- |
| G1 — version bump (PATCH: 0.61.26 → 0.61.27) | _operator: 0.61.x PATCH sequence_ | 18-05 '26 |
| G2 — destructive action | _n/a — additive; old prefix kept as fallback_ | — |
| G3 — new decision rule | _n/a_ | — |
| G4 — paid external API call | _n/a_ | — |
