(№ 379 - 27-05 '26 23:15 SGT) v0.61.178

# Journal — soleat v0.61.178 ({cuisine-venues} placeholder in /start marketing copy)

> Item #3 from the operator's "1, 2, 3" shortlist. v0.61.176 +
> v0.61.177 (running-version chip + Redis persistence + chat
> trigger) shipped earlier in the session. v0.61.178 closes the
> loop: surface the cumulative SG venue count across the 48-cuisine
> subset in the `/start` marketing intro.
>
> Commit 391 / journal 379 / build 181, on branch
> `claude/v0.61.178-cuisine-venues-marketing-copy` off `main` at
> v0.61.177 (PR #679).

## New

### [HDR] #379 | 23:15 SGT 27-05-27 | v0.61.178 — {cuisine-venues} marketing-copy placeholder | 4 files | 1 PR | 1 build |

- **[INTENT]** Surface the cumulative venue count (from
  v0.61.177's Redis persistence layer) in the chat-side marketing
  copy without coupling the cvc module to count-history. Keep the
  data flow one-directional: cvc writes to its own Redis key →
  count-display reads it on /start render.

- **[DELTA]**
  - **`count-display.js`** (3 changes):
    - `FALLBACK` gains a `cuisine-venues: 600` entry (rough
      estimate for the 48-cuisine subset when the Redis blob is
      empty — most non-saturating cuisines × ~10–20 venues each).
    - **NEW `_getCuisineVenuesCount(redis)`** — calls
      `cvc.loadFromRedis(redis)`, returns `latest.total` when
      finite and > 0, else FALLBACK. Wrapped in try/catch so a
      circular-require / missing-module never throws.
    - **`getDisplayCount(redis, item)`** — special-cases
      `item === 'cuisine-venues'` BEFORE the count-history lookup,
      so the cuisine-venues path doesn't pollute the Periodical
      ZSET schema.
    - **`getDisplayCounts(redis)`** + **`formatAllCountsPlus(redis)`**
      both append the `'cuisine-venues'` entry to their output
      maps (since `count-history.ITEMS` doesn't include it).
  - **`__tests__/count-display.test.js`** (8 new + 2 updated):
    - NEW describe block `cuisine-venues (v0.61.178)` with 8
      tests: FALLBACK baseline; empty Redis path; populated blob
      path; total=null / total=0 fallbacks; corrupt-JSON fallback;
      getDisplayCounts includes; formatAllCountsPlus includes;
      substituteCounts replaces the hyphenated placeholder.
    - 2 existing tests updated to expect 15-item map (was 14).
    - Total 17 → **25 tests, all passing**.
  - **`i18n.js`** — `start.intro` (EN + FR) extended:
    - EN: "Singapore's {cuisines} cuisine melting pot, **{cuisine-venues}
      curated venues**, hawkers, Michelin Star picks, Bib Gourmand
      favourites…" + same placeholder in the `/cuisine` slash-menu
      description line.
    - FR: equivalent — "{cuisines} cuisines à Singapour, **{cuisine-venues}
      adresses sélectionnées**, hawkers, adresses Michelin…"
    - Substitution happens at /start render via the existing
      `count-display.substituteCounts(template, counts)` call
      (no index.js wiring change needed — the count map now
      includes the `'cuisine-venues'` key).
  - **`package.json`** — `0.61.177 → 0.61.178`.
  - **`doc/.serial-state.yml`** — `commit 390 → 391`, `build 180
    → 181`, `journal 378 → 379`; re-anchored 27-05 '26 23:15 SGT.
  - **`doc/Journal/journal-0_61_178-27_05_27-2315.md`** — this
    entry.

- **[VERIFICATION (sandbox)]**

  ```
  $ node --check count-display.js                            (OK)
  $ node --check i18n.js                                     (OK)
  $ npx vitest run __tests__/count-display.test.js
       ✓ count-display.test.js   (25 tests)  ← was 17
       Test Files  1 passed (1)
            Tests  25 passed (25)
  $ npx vitest run __tests__/cuisine-venue-counts.test.js
       ✓ cuisine-venue-counts.test.js   (28 tests, unchanged)
  ```

- **[STATUS]** PR-OPEN on push. Operator merges manually.

- **[TEST]** Operator (post-merge):
  - Send `/start` — intro reads:
    > Hungry for something beyond the usual? Soleat … helps you
    > explore Singapore's **55+ cuisine melting pot, 600+ curated
    > venues**, hawkers, Michelin Star picks, Bib Gourmand
    > favourites…
    (600+ is the FALLBACK; actual number flips after the operator
    runs `/v` → 🍽 Cuisine venues → 🔄 Run now → ~$3.45 sweep.)
  - Send `/start` AFTER running the sweep — intro reads the live
    rounded total instead of 600+ (e.g. "615+ curated venues" →
    "610+" after the round-down).
  - Toggle `/language fr` → /start — FR copy uses "adresses
    sélectionnées" for the same placeholder.

- **[KNOWN GAPS]**
  - **No TMA-side banner** — cuisine-venues only surfaces in chat,
    not in the Cuisine TMA header / footer. Operator can add an
    inline read of `/api/cuisine/counts` (which now surfaces the
    cuisine-venues entry too, via getDisplayCounts) once they want
    a "600+ venues" badge in the TMA UI.
  - **No subtotal per region or category** — `{cuisine-venues}` is
    the global sum across all 48 cuisines. Breakdowns (e.g. "200+
    European, 80+ African") would require either more placeholders
    or a structured-content panel. Filed.
  - **600 baseline is a guess** — the real cumulative total
    depends on Places' SG coverage at sweep time. Once the
    operator runs a real sweep, we could replace the FALLBACK
    with the LAST KNOWN value committed to data/ for new
    deployments to use as a more accurate cold-start baseline.

- **Confirmation Gates**

  | Gate | Resolution | Date |
  |------|-----------|------|
  | G1 — version bump | PATCH `0.61.177 → 0.61.178`. Marketing-copy + helper extension only; no API change, no schema migration. | 27-05 '26 |
  | G2 — destructive action | _n/a_ — pure additions. The existing FALLBACK values + 14 Periodical items are untouched. | 27-05 '26 |
  | G3 — new decision rule | YES — count-display.getDisplayCount routes `cuisine-venues` to the cvc module instead of count-history. Future per-cuisine breakdowns would either follow the same pattern (new special-case routes) or get a proper "data source" registry. | 27-05 '26 |
  | G4 — paid external API call | _n/a_ — count-display reads from Redis only; the Places sweep itself is still gated by the v0.61.177 chat button. | 27-05 '26 |

## Amendments

- **`count-display.FALLBACK`** — was 14 items (the count-history
  ITEMS set). Now 15 items: + `'cuisine-venues': 600`. The 14
  Periodical items are unchanged.
- **`count-display.getDisplayCount(redis, item)`** — was
  "always reads `count-history.getCurrentCount`". Now: special-
  cases `item === 'cuisine-venues'` to read from
  `cuisine-venue-counts.loadFromRedis` instead.
- **`getDisplayCounts` + `formatAllCountsPlus`** return shape —
  was `Map<14 keys>`. Now `Map<15 keys>` (+ cuisine-venues).
- **`start.intro` template** — was "{cuisines} cuisine melting
  pot, hawkers, Michelin…" (v0.61.169). Now "{cuisines} cuisine
  melting pot, {cuisine-venues} curated venues, hawkers, Michelin…".
  Same change in the `/cuisine` slash-menu description line in
  both EN + FR.

## Deleted

_None._
