(№ 385 - 28-05 '26 00:45 SGT) v0.61.184

# Journal — soleat v0.61.184 (data quality: resync mrt-coords.json _meta)

> Closes v0.61.172 known gap. v0.61.172 fixed recountTrainStations
> reading the wrong shape from data/mrt-coords.json; it ALSO noted
> that the file's `_meta` header had its station counts stale
> (143 + 41 = 184 in header vs 209 actual). v0.61.184 syncs the
> header to the body.
>
> Commit 397 / journal 385 / build 187, on branch
> `claude/v0.61.184-mrt-meta-resync` off `main` at v0.61.183.

## New

### [HDR] #385 | 00:45 SGT 28-05-28 | v0.61.184 — mrt-coords _meta resync | 3 files | 1 PR | 1 build |

- **[DELTA]**
  - **`data/mrt-coords.json`** (5 _meta edits, 209 station rows
    untouched):
    - `operationalCount`: **143 → 180**
    - `futureCount`: **41 → 29**
    - NEW `linesOperational`: **10**
    - NEW `linesTotal`: **12**
    - `lastUpdated`: `2026-05-10 → 2026-05-27`
    - `comment` appended with v0.61.184 note explaining the rot.
  - **`package.json`** — `0.61.183 → 0.61.184`.
  - **`doc/.serial-state.yml`** — commit 396 → 397, build 186 →
    187, journal 384 → 385.
  - **`doc/Journal/journal-0_61_184-28_05_28-0045.md`**.

- **[VERIFICATION (sandbox)]**

  ```
  $ node -e "const j = require('./data/mrt-coords.json');
             console.log(j._meta);"   (valid JSON, sane numbers)
  ```

  The 5 numbers in _meta (180 operational, 29 future, 10/12 lines,
  date) all match what `recountTrainStations` /
  `recountTrainLines` (v0.61.172) compute against the live body.

- **[STATUS]** PR-OPEN on push. Operator merges manually.

- **[TEST]** Operator:
  - Tap `/v` → 📊 Periodical → 🔄 Re-check on the train-stations
    item — history reports `n=180` (was `n=null,
    source='unavailable'` before v0.61.172; `n=209` after v0.61.172
    because the recount counts BOTH operational + future stations).
    The new `_meta.operationalCount=180` matches the actual
    operational subset.
  - The recount fns still count `Object.keys(j).filter(k => k !==
    '_meta').length` (all stations) — so the bot's display still
    reads `train-stations: 209`, which matches the body cardinality.
    The _meta header is now ALSO accurate.

- **[KNOWN GAPS]**
  - **No automated tooling** to keep _meta + body in sync going
    forward. If someone adds a new station entry, they have to
    remember to bump _meta. A future test could validate
    `_meta.operationalCount === Object.values(j).filter(v =>
    v.status === 'operational').length` etc. Filed but not added
    in this PR.
  - **Other data files** may have similar drift (e.g. carpark
    counts, geo overlays). A bulk audit could catch them, but the
    operator-facing pain is small enough to defer.

- **Confirmation Gates**

  | Gate | Resolution | Date |
  |------|-----------|------|
  | G1 — version bump | PATCH `0.61.183 → 0.61.184`. Pure data-file fix. | 28-05 '26 |
  | G2 — destructive action | _n/a_ — only _meta values changed; the 209 station entries are byte-identical. | 28-05 '26 |
  | G3 — new decision rule | _n/a_. | 28-05 '26 |
  | G4 — paid external API call | _n/a_. | 28-05 '26 |

## Amendments

- **`data/mrt-coords.json:_meta.operationalCount`** — was 143
  (v0.60.85 baseline). Now 180. Reason: station entries with
  `status: "operational"` increased as later edits added newer
  stations without bumping the header.
- **`data/mrt-coords.json:_meta.futureCount`** — was 41 (v0.60.85
  baseline, anticipated future stations). Now 29. Reason: some
  "future" stations have since opened and were flipped to
  `status: "operational"` in the body, but `_meta` wasn't updated.
- **`data/mrt-coords.json:_meta.lastUpdated`** — was `2026-05-10`.
  Now `2026-05-27` (v0.61.184 sync date).

## Deleted

_None._
