(№ 599 - 08-06 '26 17:35 SGT) v0.61.399

# Journal — soleat v0.61.399 (New filter over-compresses to 0 in established markets: floor the review-time refute)

> Operator (**URGENT**): *"Are we facing an overcompress — i cannot select fruits AND desserts in TYO/JP? Now, even Fruit in OSA/JP is 0 results. Is it a custom search problem? Revert if it helps. Last time, I didn't have this issue."* + *"is the custom search giving poor results in other cuisines?"* (evidence: `log/logs.1780910707621.json`).
>
> Commit 612 / journal 599 / **build held 386** (server-only). Revert-ready.

## New

### [HDR] #599 | 17:35 SGT 08-06-26 | v0.61.399 — review-time refute demotes, never empties | index.js | 1 PR | 0 build |

- **[INTENT]** With the **New** pill on in **Osaka / Tokyo**, **Fruits returns 0** — and the operator's log shows it's **every cuisine**, not just fruits (`american` also 0). Over-compression.

- **[ROOT (traced from the operator's log, `index.js ~15084`)]** Every logged search has `newlyOpened=true`. The **New** pill (since **v0.61.349**) uses `/hidden`'s **review-time refute**: drop any venue whose **oldest Google review is > 3 months** (a review can only exist after opening, so it *refutes* newness). In an **established market** (Osaka) **every** candidate has reviews older than 3 months, so the **hard** cut zeroes the page:
  - `fruits`: discover 58 → fruit-label 28 → **review-refute 28 → 0** → 0 venues.
  - `american`: **review-refute 2 → 0** → 0 venues. (Confirms it's general, answering *"poor results in other cuisines?"* — **yes**.)
  - Pre-v0.61.349 used a softer **≤150-review proxy** → *"last time I didn't have this issue."*
  - **Not** the durian-belt block (v0.61.397): the log shows fruits/durian on the **cuisine** path (`cuisines=[…]`), **no** `D778-BLOCK`.

- **[DELTA]** (`index.js`)
  - Review-time can only **REFUTE**, never **CONFIRM**, newness → it must **DEMOTE**, not **EMPTY**. New code computes `refuted = filter(≤3mo OR no-reviews)` and sets **`venues = refuted.length ? refuted : preRefute`** — when refuting would zero the page, keep the **"newly opened"-biased** recall pool (floor). Logs `; floor — kept biased pool, none proven-new` when it fires. Markets with genuinely-new venues (SG) are **unchanged** (refuted is non-empty → used as before).
  - **`package.json`** `0.61.398 → 0.61.399`; **`doc/.serial-state.yml`** commit 611 → 612, journal 598 → 599, **build held 386** (server-only); re-anchored 17:35 SGT.

- **[VERIFICATION (sandbox)]** `node --check index.js` clean. Full suite **113 files / 3097 tests** — **no regression**.

- **[STATUS]** PR-**OPEN**, **auto-merge armed (squash)**. Railway redeploys on merge (server only — no rebuild).

- **[TEST]** Operator: **Osaka / Tokyo**, **New** on, tap **Fruits** (or any cuisine) → a list now loads (best-effort newest) instead of 0. **SG New** unchanged — proven-new venues still rank first.

- **[KNOWN GAPS / SAFETY]**
  - The floor only triggers when the refute would reach **0** — it never *adds* proven-old venues ahead of proven-new ones; it only prevents an **empty** page.
  - Still **server-only**, contained to the `if (filters.newlyOpened)` block. **Not** unit-tested (inline express-route logic, no harness seam — same gap class as v0.61.396/398).
  - Separate, not in scope here: the **Fruits/Durian *pill*** (special-mode path) is still belt-gated by v0.61.397 — if the operator wants the Fruits *pill* to work in Japan too, that's a one-line change to exclude `fruits` from the gate. Flagged for the operator.
  - **Revert-ready.**

- **Confirmation Gates**

  | Gate | Resolution | Date |
  |---|---|---|
  | G1 — version bump | PATCH `0.61.398 → 0.61.399`. | 08-06 '26 |
  | G2 — destructive action | Additive floor; the refute still applies whenever it leaves ≥1 venue. | 08-06 '26 |
  | G3 — new decision rule | The New-pill review-time refute **demotes, never empties** — falls back to the biased pool at 0. | 08-06 '26 |
  | G4 — paid external API call | None new — same per-candidate Places Details reviews fetch as v0.61.349. | 08-06 '26 |

## Amendments

- **`index.js`** — review-time refute floors to the pre-refute biased pool when it would zero the list.
- **`package.json`** — `0.61.398 → 0.61.399`.
- **`doc/.serial-state.yml`** — counters (commit 612 / journal 599 / build held 386) + anchor 17:35 SGT 08-06-26.

## Deleted

- _None_ (additive floor).
