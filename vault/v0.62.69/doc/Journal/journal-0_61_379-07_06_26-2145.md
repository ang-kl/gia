(№ 579 - 07-06 '26 21:45 SGT) v0.61.379

# Journal — soleat v0.61.379 (Vault snapshot — vault/v0.61.378/)

> Operator: **"vault"** — the periodic frozen source snapshot was due (last was v0.61.308, ~69 versions back).
>
> **Left OPEN for review — auto-merge to be armed on the operator's word.** Commit 592 / journal 579 / build 373 (held). Docs/artifact.

## New

### [HDR] #579 | 21:45 SGT 07-06-26 | v0.61.379 — vault/v0.61.378/ snapshot | snapshot + README | 1 PR | 0 build |

- **[INTENT]** Capture an append-only, bootable, point-in-time mirror of the source at v0.61.378 (Rule V-1).

- **[MECHANISM]** `git archive HEAD | tar -x -C vault/v0.61.378 --exclude='vault'` — the **tracked tree only**, so `node_modules/` · `.git/` · `dist/` · `public/{cuisine,menu,…}/` build artefacts · `.env` · `tmp/` are inherently excluded (all gitignored), and the nested `vault/` is dropped (no recursion). The snapshot's own `package.json` stays frozen at **0.61.378**.

- **[CONTENTS]** 1369 files, ~180 MB — `index.js` + ~100 root modules, `web/<app>/src`, `scripts/`, `data/`, `data_realtime/`, `geoloc/`, `public/app.js`, `__tests__/` (3055 tests), and `doc/` through journal #578. NEW `vault/v0.61.378/VAULT_README.md` (modelled on the v0.61.308 one): capture metadata, exclusion table, boot instructions, and the **v0.61.309 → v0.61.377 arc** (Michelin multi-country · native-script names · location refactor · FX engine + Option B + iOS-Region currency · the location-coherence decompression + search-centre single source · Michelin-SG hint + 2025-drop · durian local locales · durian city-coverage probe).

- **[DELTA]**
  - **`vault/v0.61.378/`** (NEW — 1369 files) + **`vault/v0.61.378/VAULT_README.md`** (NEW).
  - **`package.json`** — `0.61.378 → 0.61.379` (the PR adding the vault; the snapshot stays 0.61.378). **`doc/.serial-state.yml`** — commit 591 → 592, journal 578 → 579 (**build held 373**); re-anchored 21:45 SGT.

- **[VERIFICATION (sandbox)]** Snapshot sanity-checked: no `node_modules`, no nested `vault/`, `package.json` = 0.61.378. `git archive` is reproducible from the committed tree.

- **[STATUS]** PR-**OPEN**, **auto-merge to arm on the operator's word**. Append-only — prior vaults untouched (Rule V-1).

- **[TEST]** Operator: a frozen, bootable copy of v0.61.378 now lives at `vault/v0.61.378/`, alongside the earlier vaults.

- **[KNOWN GAPS]**
  - Large commit (~180 MB) — consistent with the established vault cadence.
  - A `doc/Feature/` / `doc/Technical/` / `doc/Register/` refresh for the v0.61.360–378 arc is still due (the journals + this vault carry the record meanwhile).

- **Confirmation Gates**

  | Gate | Resolution | Date |
  |---|---|---|
  | G1 — version bump | PATCH `0.61.378 → 0.61.379`. | 07-06 '26 |
  | G2 — destructive action | Append-only — a new `vault/v0.61.378/` directory; no prior vault or source touched (Rule V-1). | 07-06 '26 |
  | G3 — new decision rule | Vault snapshot taken at v0.61.378 via `git archive` (tracked tree minus nested vault). | 07-06 '26 |
  | G4 — paid external API call | None. | 07-06 '26 |

## Amendments

- **`vault/v0.61.378/`** (NEW) — frozen source mirror + `VAULT_README.md`.
- **`package.json`** — `0.61.378 → 0.61.379`.
- **`doc/.serial-state.yml`** — counters (commit 592 / journal 579 / build held 373) + anchor 21:45 SGT 07-06-26.

## Deleted

- _None_ (append-only).
