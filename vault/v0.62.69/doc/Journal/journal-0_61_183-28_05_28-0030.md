(№ 384 - 28-05 '26 00:30 SGT) v0.61.183

# Journal — soleat v0.61.183 (per-cuisine detail dump for cv:menu)

> Closes v0.61.177 known gap: the chat /v → 🍽 Cuisine venues menu
> showed total + capped slugs only; the per-slug breakdown was in
> Redis but inaccessible from chat. v0.61.183 adds a "📋 Per-cuisine
> details" button.
>
> Commit 396 / journal 384 / build 186, on branch
> `claude/v0.61.183-cv-per-slug-details` off `main` at v0.61.182.

## New

### [HDR] #384 | 00:30 SGT 28-05-28 | v0.61.183 — Per-cuisine detail dump | 3 files | 1 PR | 1 build |

- **[INTENT]** Surface the per-slug count data already stored in
  the v0.61.177 Redis blob via a chat button. Sorted (count desc,
  errors last) Markdown code block.

- **[DELTA]**
  - **`index.js`** (3 changes):
    - cv: callback dispatcher gains a `cv:details` branch.
    - `ownerCuisineVenueMenu` adds a `[📋 Per-cuisine details]`
      button row, conditionally rendered only when
      `latest.perSlug` is populated (hidden in the no-data
      first-tap state).
    - NEW `ownerCuisineVenueDetails(chatId)` — loads the Redis
      blob, sorts entries by:
      1. errors (n=null) last
      2. count descending
      3. alpha for ties
      Each row tagged with `60+` (capped single-query),
      `(tiled)` (15-tile saturating), or `err`. Renders as a
      single ```code block``` per Telegram message, chunked
      at 3500 chars (safe under the 4096 limit).
  - **`package.json`** — `0.61.182 → 0.61.183`.
  - **`doc/.serial-state.yml`** — commit 395 → 396, build 185 →
    186, journal 383 → 384.
  - **`doc/Journal/journal-0_61_183-28_05_28-0030.md`**.

- **[VERIFICATION (sandbox)]** `node --check index.js` (OK).

- **[STATUS]** PR-OPEN on push. Operator merges manually.

- **[TEST]** Operator (post-merge + sweep populated):
  - `/v` → 🍽 Cuisine venues → tap **📋 Per-cuisine details**.
  - Receive a sorted dump:
    ```
    Cuisine venue counts (48 cuisines) — total: 1247
      italian.............  180 (tiled)
      japanese............  450 (tiled)
      ...
      czech...............    5
      polish..............    3
      egyptian............ —   err
    ```

- **[KNOWN GAPS]**
  - Markdown-mode escaping is minimal — slug names don't contain
    backticks but if a future cuisine slug had one, it could
    break the code block. Not a concern at 48 known slugs.
  - No CSV export. If operator ever wants a spreadsheet, that's
    a separate format.

- **Confirmation Gates**

  | Gate | Resolution | Date |
  |------|-----------|------|
  | G1 — version bump | PATCH `0.61.182 → 0.61.183`. Pure additive UI surface. | 28-05 '26 |
  | G2 — destructive action | _n/a_. | 28-05 '26 |
  | G3 — new decision rule | _n/a_. | 28-05 '26 |
  | G4 — paid external API call | _n/a_ — reads from Redis only. | 28-05 '26 |

## Amendments

_None._

## Deleted

_None._
