(№ 589 - 08-06 '26 11:31 SGT) v0.61.389

# Journal — soleat v0.61.389 (Social links: scrape the website, not Gemini)

> Operator: *"why search via Gemini for social links — I mentioned in a previous PR to use Places/Google Search; and I don't want to manage more api keys"* → chose **Go** (scrape-first + Custom-Search fallback, **no new key**).
>
> **Left OPEN for review — auto-merge to be armed on the operator's word.** Commit 602 / journal 589 / build 380 (held). Server-only.

## New

### [HDR] #589 | 11:31 SGT 08-06-26 | v0.61.389 — social-profiles.js: scrape + Custom Search, Gemini removed | social-profiles.js | 1 PR | 0 build |

- **[INTENT]** The cuisine social row (📱) looked up Instagram/etc. via **Gemini grounded Google Search** — the 30s `social-profiles` latency + token cost the Railway logs showed (`ms:29981`). The operator's earlier instruction was to **use the venue's website (from Places) / a plain Google Search**, no Gemini.

- **[VERIFIED — operator's memory was right]** Their **v0.61.109** attractions PR (`scripts/fetch-attraction-details.js` `scrapeInstagram`) gets socials by **scraping the venue's website** (Places gives `websiteUri`; *"Google Places has no social field"*). The cuisine social row (`social-profiles.js`, v0.61.225 — a **separate, later** build) had **diverged** to Gemini.

- **[DELTA]** (`social-profiles.js` — **Gemini removed**)
  - **Scrape** the venue `websiteUri` HTML for all six platforms via `DOMAIN_SCAN` + reserved-path filtering + the existing strict `URL_PATTERNS` (ports + extends the attraction `instagramFromHtml` to all 6).
  - **Custom Search fallback** for the gaps — **reuses the existing `GOOGLE_MAPS_API_KEY`** (no new key; just enable "Custom Search API" on it) and runs **only** when **`GOOGLE_CSE_ID`** (a free, non-secret Programmable-Search id) is set; one query `<name> <area>` per venue, scanning the result links. **Without `GOOGLE_CSE_ID` it's pure-scrape — zero new config.**
  - Redis cache `social:<placeId>` (30d) kept; an empty result is cached **only** once a complete (CSE-configured) lookup ran, so scrape-only blanks re-try if the id is added later.
  - Public API unchanged (`getSocialProfiles` / `fetchSocialProfilesForVenues` / `attachSocialsToVenues` / `pickTopProfiles`); the 5 `index.js` `attachSocialsToVenues` sites + copy-one are untouched (they never passed `_genAIFactory`). Test seam `_genAIFactory → _getFn` (axios.get).
  - **`__tests__/social-profiles.test.js`** rewritten — 18 tests, mocked `_getFn`, no network.
  - **`package.json`** `0.61.388 → 0.61.389`; **`doc/.serial-state.yml`** commit 601 → 602, journal 588 → 589 (**build held 380**); re-anchored 11:31 SGT.

- **[VERIFICATION (sandbox)]** `node --check` clean. Full suite **113 files / 3088 tests** (social suite 18, mocked). Server-only — no web build.

- **[STATUS]** PR-**OPEN**, **auto-merge to arm on the operator's word**. Railway redeploys on merge.

- **[TEST]** Operator: the 📱 social buttons now come from the venue's **website** (free, fast) — no Gemini, no 30s wait, **no new key**. Optionally add a `GOOGLE_CSE_ID` later for wider coverage via your existing Maps key.

- **[KNOWN GAPS]**
  - **Coverage:** scraping finds only socials a venue **links on its own site**. The Custom Search fallback widens this — but needs the optional `GOOGLE_CSE_ID` (and the existing key to have "Custom Search API" enabled).
  - The `/cost` tracker no longer shows social Gemini spend (there is none now).
  - Not live-E2E tested (no live website fetch / CSE call in sandbox).

- **Confirmation Gates**

  | Gate | Resolution | Date |
  |---|---|---|
  | G1 — version bump | PATCH `0.61.388 → 0.61.389`. | 08-06 '26 |
  | G2 — destructive action | Removed the Gemini lookup path; the public API + Redis cache shape are preserved. | 08-06 '26 |
  | G3 — new decision rule | Social links come from the venue **website** (Places `websiteUri`), then **Google Custom Search** (existing key) — **never Gemini**. | 08-06 '26 |
  | G4 — paid external API call | None new — scraping is free; the optional Custom Search reuses the existing `GOOGLE_MAPS_API_KEY`. | 08-06 '26 |

## Amendments

- **`social-profiles.js`** — rewritten: scrape `websiteUri` + Custom Search fallback; Gemini path removed.
- **`__tests__/social-profiles.test.js`** — rewritten (18 mocked tests).
- **`package.json`** — `0.61.388 → 0.61.389`.
- **`doc/.serial-state.yml`** — counters (commit 602 / journal 589 / build held 380) + anchor 11:31 SGT 08-06-26.

## Deleted

- The Gemini lookup (`buildPrompt` / `parseJsonResponse` / `callGemini` / model fallback chain) — superseded by the scrape + Custom-Search path.
