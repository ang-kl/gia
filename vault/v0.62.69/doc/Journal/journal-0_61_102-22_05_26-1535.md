(№ 309 - 22-05 '26 15:35 SGT) v0.61.102

# Journal — soleat v0.61.102 (telescope zoom readout + bus-stop zoom tiers)

> Operator — a two-part map spec: a restyled zoom display, and a
> per-zoom-band bus-stop marker.
>
> This entry is commit 309 / journal 302 / build 106, off `main` at
> v0.61.101 (#594).

## New

### [HDR] #302 | 15:35 SGT 22-05-26 | v0.61.102 — zoom readout + bus tiers | ~5 kb | 8 files | 1 PR | 1 build |

- **[INTENT]** Restyle the embedded-map zoom display to a faint
  "🔭 <zoom>" readout, and render bus-stop overlay markers by zoom
  band, across all the embedded maps.

- **[DELTA]**
  - **Zoom display** — `web/cuisine/src/v2/components/MapPanel.jsx`,
    `web/hawker/src/components/HawkerMapPanel.jsx`,
    `web/transport/src/components/MrtMapPanel.jsx`, and
    `public/app.js` (`buildZoomPin`): the bottom-right readout drops
    the white circle for a faint `🔭 <zoom>` text — 30 % opacity, 2 px
    smaller (`text-xs` → `text-[10px]`; the `/app/map` pin 12 px →
    10 px). ("transparent 30 %" read as 30 % opacity.)
  - **Bus-stop zoom tiers** —
    `web/cuisine/src/v2/lib/mapOverlays.js` (+ the byte-identical
    Hawker / Transport copies): new `busTier(zoom)` + `busTierNode` —
    z17+ `🚏 Bus Stop № <code>`, z15-16 `🚏 № <code>`, z13-14 `🚏`,
    z11-12 `🚏` smaller, z≤10 a light-yellow square with a red `b`.
    `buildBusMarkers` builds the current-tier node; `applyVisibility`
    re-renders it (via a `_busTier` cache) as the zoom band changes
    (the exit pins keep their own compact/full swap).
    `public/app.js` gets the same ladder (`busTier` / `busTierPin`);
    its bus-layer zoom gate drops from z14 to z10 so the low-zoom
    tiers can show.
  - **`package.json`** — `0.61.101` → `0.61.102` (PATCH).

- **[VERIFICATION (sandbox)]**

  ```
  node --check public/app.js   clean
  npm run build (5 TMAs)       all green
  npm test                     1944/1944 passing, 67 files
  ```

- **[STATUS]** DRAFT — PR #595 on `claude/v0.61.102-zoom-bus-tiers`
  (off `main` at v0.61.101, `99997b8`). On merge deploys via the
  Railway auto-deploy on `main`.

- **[TEST]** The zoom readout shows a faint "🔭 14"; toggle Bus Stop
  and zoom through the bands — the marker steps full label → short
  label → glyph → small glyph → yellow square.

- **[KNOWN GAPS]**
  - The bus-stop tier px sizes are picked to a descending ladder; the
    operator's "by 1 px / 2 px" deltas were taken as approximate.
  - No in-sandbox visual verification (no Maps webview).
  - `doc/VibeCodingRecord/` regen still due (carried forward).

- **Confirmation Gates**

  | Gate | Resolution | Date |
  |------|-----------|------|
  | G1 — version bump | PATCH `0.61.101` → `0.61.102` | 22-05 '26 |
  | G2 — destructive action | _code-only_ — the zoom readout restyled, bus markers re-tiered in place; no deletion | 22-05 '26 |
  | G3 — new decision rule | _n/a_ — operator UI spec | 22-05 '26 |
  | G4 — paid external API call | _n/a_ — pure client-side rendering | 22-05 '26 |

## Amendments

_None._

## Deleted

_None._
