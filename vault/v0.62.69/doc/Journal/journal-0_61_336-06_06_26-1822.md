(№ 536 - 06-06 '26 18:22 SGT) v0.61.336

# Journal — soleat v0.61.336 (Japan Michelin — load the curated `instruction/Japan.js` into `JP-michelin.js`; 589 venues + native kanji name/address; JP manifest)

> Operator pointed to `instruction/Japan.js`: *"mindful of the japanese resturant name and addresses to be added."* Two decisions taken via AskUserQuestion: **(1)** Japan 2025 is **partial** (85 venues, **no Bib Gourmand**) vs 2026's full 588 → **add as-is + flag the gap** (manifest matches the file verbatim); **(2)** the native `nameJa`/`addressJa` on every row → **stored verbatim now, displayed in a later follow-up**.
>
> **Left OPEN for review — auto-merge NOT armed.** Commit 549 / journal 536 / build 335, landed via the GitHub Git Data API on origin/main @ v0.61.335.

## New

### [HDR] #536 | 18:22 SGT 06-06-26 | v0.61.336 — Japan Michelin live: `JP-michelin.js` = 589 curated venues (kanji name + address verbatim); JP manifest (85/588) | 3 code files | 1 PR | 1 build |

- **[INTENT]** v0.61.333 shipped `JP-michelin.js` as the empty stub. The operator's curated `instruction/Japan.js` holds the full venue-centric Japan dataset (**589 venues** across Tokyo/Kyoto/Osaka/Nara, both 2025 + 2026 editions, **plus** a native `nameJa` + `addressJa` on every row). This PR loads that file **verbatim** into `JP-michelin.js`, registers the JP manifest, and leaves `instruction/Japan.js` as the source-of-record. No UI surfacing of the Japanese fields yet (operator decision — see KNOWN GAPS).

- **[MECHANISM]** `instruction/Japan.js` is a bare array literal of 589 venue objects already in venue-award-schema.v0_1 shape + two locale fields (`{id, city, country:'JP', name, nameJa, address, addressJa, postal?, cuisine?, vegetarian, halal, status?, awards:[{year,category}]}`). The 589 rows are copied **byte-for-byte** into `JP-michelin.js` — no re-serialisation, so kanji (`祇園 さゝ木`, `京都市東山区小松町`), diacritics (`SÉZANNE`, `Ryō-shō`, `Joël Robuchon`) and apostrophes (`L'Effervescence`, `L'OSIER`) survive untouched. The loader (`michelin-data.js`) uses each `entry.id` verbatim and validates that every venue's `city` resolves in `CITY_IATA`. **All 4 JP cities (Tokyo→TYO, Osaka→OSA, Kyoto→OSA, Nara→OSA) were already mapped** in both `cities.js` and the loader — so, unlike Thailand, **no city / IATA / `CITY_IATA` additions were needed**. The loader's `venueToVenue` normaliser projects the venue down to the canonical Venue shape and **deliberately drops `nameJa`/`addressJa`** (they are not yet consumed) — the native fields remain in the source table for the display follow-up.

- **[DELTA]**

  **1. `JP-michelin.js`** (root) — was the empty v0.61.333 stub (`ENTRIES:[]`); now `const COUNTRY='JP'; const ENTRIES=[ …589 venues verbatim… ]; module.exports={COUNTRY,ENTRIES}`. Header updated to v0.61.336 (source = `instruction/Japan.js`; 4 cities; the JP-locale fields documented; the 2025-partial manifest noted). 589 unique ids, 673 awards.

  **2. `michelin-data.js`** — `COUNTRY_MANIFEST`: added `JP: { 2025:{three:20,two:57,one:8,total:85}, 2026:{three:21,two:61,one:278,bib:228,total:588} }`. 2025 omits `bib-gourmand` (the source 2025 edition has none) — same omit-absent-tier convention MY uses for `three-star`.

  **3. `__tests__/michelin-data.test.js`** — removed `JP` from the empty-tables loop + the `hasMichelinData` false-list (Tokyo/JP were asserted false — now true; swapped to still-empty Seoul/KR); added a full JP load suite (589 venues / 673 awards, 85@2025 / 588@2026, per-tier manifest `{20,57,8}` + `{21,61,278,228}`, unique ids, every JP `city` curated, Gion Sasaki 3★ both editions, SÉZANNE diacritic + closed, **and a source-table assertion that `nameJa` `祇園 さゝ木` / `addressJa` kanji are preserved verbatim**), a `JP-michelin.js is venue-centric with 589 rows` require test, and a `hasMichelinData` JP-true case (JP / jp / Tokyo / Kyoto / Nara).

  **4. `package.json`** — `0.61.335 → 0.61.336`.

  **`doc/.serial-state.yml`** — commit 548 → 549, journal 535 → 536, build 334 → 335; re-anchored 18:22 SGT 06-06-26.

- **[VERIFICATION (sandbox)]**
  - `node --check` clean: `JP-michelin.js`, `michelin-data.js`, `__tests__/michelin-data.test.js`.
  - Real-loader temp-harness (all 8 `{CC}-michelin.js` + the patched `michelin-data.js` in `/tmp/jptest`): `require('./michelin-data.js')` loads **without throwing**. `venuesForCountry('JP')===589`; global unique ids **839/839** (MY 70 + TH 180 + JP 589, no dup); JP 2025=85 / 2026=588; tiers `{three:20,two:57,one:8}` (2025) + `{three:21,two:61,one:278,bib:228}` (2026); `sum(JP awards)===673`. JP manifest passes (85/588, cross-year award total 673). `hasMichelinData` true for JP/Tokyo/Kyoto/Nara; false for Seoul/KR.
  - **19/19** explicit assertions green in the harness (counts, tiers, unique ids, jp- prefix, curated-city gate, Gion Sasaki 3★ both years, SÉZANNE closed+diacritic, source kanji `祇園 さゝ木` + `京都市東山区`, every-row `nameJa`/`addressJa` present, global 839 dedup).
  - Full `npm test` runs in CI on merge (the TH integration's 2931-green baseline + the new JP suite).

- **[STATUS]** PR-**OPEN**, **auto-merge NOT armed** — awaits operator review/merge. Landed via the GitHub Git Data API on a branch off origin/main @ v0.61.335. Railway redeploys on merge.

- **[TEST]** Operator: Japan is now in the venue loader — 589 venues across Tokyo / Kyoto / Osaka / Nara, the full 2026 edition (588) + the partial 2025 upper-tiers (85). The native Japanese name + address are stored on every row (kanji preserved byte-for-byte) ready for the display follow-up. `instruction/Japan.js` stays the source-of-record; `JP-michelin.js` is the loaded copy.

- **[KNOWN GAPS]**
  - **2025 is PARTIAL** — 85 venues, **no Bib Gourmand** and only 8 one-star (vs 2026's 588 @ 278 one-star / 228 Bib). The source 2025 edition captured only the upper tiers; loaded as-is per operator decision. A fuller 2025 Japan list is **still due** when the operator supplies it; the manifest will be revised then.
  - **`nameJa` / `addressJa` not surfaced** — stored in `JP-michelin.js` but `venueToVenue` projects them out, so they do **not** reach the card yet. The display follow-up must (a) extend `venueToVenue` to pass them through and (b) render them on JP venue cards. Tracked as the operator's "display later" decision.
  - **Unified loader still off the live search path** — `michelin-data.js` (MY/TH/JP) is not yet wired into the bot's Michelin search (SG runs standalone via `SG-michelin.js`). Per-city Michelin gating for OTHER-mode cities remains the larger follow-up.
  - A `doc/Feature/` / `doc/Technical/` / `doc/Register/` update + a `vault/v0.61.336/` snapshot are still due — logged here and in `doc/Register/`.

- **Confirmation Gates**

  | Gate | Resolution | Date |
  |---|---|---|
  | G1 — version bump | PATCH `0.61.335 → 0.61.336`. | 06-06 '26 |
  | G2 — destructive action | None. Empty `JP-michelin.js` stub filled (no deletes); append-only manifest + test edits. Curated `instruction/Japan.js` untouched. Loader loads clean (839/839 unique). | 06-06 '26 |
  | G3 — new decision rule | Japan Michelin loads from the operator's curated `instruction/Japan.js` (589 venues) VERBATIM; `nameJa`/`addressJa` stored now, displayed later; 2025 partial (85, no Bib) loaded as-is + flagged; JP manifest 2025 85 / 2026 588. | 06-06 '26 |
  | G4 — paid external API call | _n/a_. Data load + manifest registration only. | 06-06 '26 |

## Amendments

- **`JP-michelin.js`** — empty stub → 589 curated venues from `instruction/Japan.js` (verbatim, kanji + diacritics intact); header → v0.61.336.
- **`michelin-data.js`** — `COUNTRY_MANIFEST` +`JP` (2025 85 / 2026 588; 2025 omits Bib Gourmand).
- **`__tests__/michelin-data.test.js`** — JP removed from empty-tables loop + false-gate list; +JP load suite (589/673/85/588, manifest, unique ids, curated cities, kanji-preservation source assertion) + `JP-michelin.js` 589-row require test + `hasMichelinData` JP case.
- **`package.json`** — `0.61.335 → 0.61.336`.
- **`doc/.serial-state.yml`** — counters (commit 549 / journal 536 / build 335) + anchor re-set 18:22 SGT 06-06-26.

## Deleted

- _None._
