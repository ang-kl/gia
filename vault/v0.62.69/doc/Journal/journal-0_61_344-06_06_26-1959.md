(№ 544 - 06-06 '26 19:59 SGT) v0.61.344

# Journal — soleat v0.61.344 (Philippines Michelin — NEW country table `PH-michelin.js`; 34 venues; loader registration + 7 CITY_IATA keys)

> Operator: *"philippines"* → `instruction/Philippines.js`. Philippines is a **new ISO-2 country (`PH`)** — the second brand-new country table (after Macau). 34 venues, 7 city labels (Metro Manila districts + Cebu + Cavite). **2026-only** — the Philippines guide launched with the 2026 edition (no 2025).
>
> **Left OPEN for review — auto-merge NOT armed.** Commit 557 / journal 544 / build 343, landed via the GitHub Git Data API on origin/main @ v0.61.343.

## New

### [HDR] #544 | 19:59 SGT 06-06-26 | v0.61.344 — Philippines Michelin live: NEW `PH-michelin.js` (34 venues, 2026-only); +COUNTRY_TABLES entry; +7 CITY_IATA keys; PH manifest | 3 code files | 1 PR | 1 build |

- **[INTENT]** Load the operator's curated `instruction/Philippines.js` (the Philippines' inaugural Michelin edition) into the loader as its own country. PH has no pre-existing stub, so the table is created and registered (same pattern as Macau).

- **[MECHANISM]** Bare array literal of 34 venue objects (venue-award-schema.v0_1; has `postal`, no locale fields). Copied **byte-for-byte** into a new `PH-michelin.js`. Three loader edits: (1) `require('./PH-michelin')` appended to `COUNTRY_TABLES`; (2) **7 city keys** added to `CITY_IATA` — the source labels Metro-Manila districts as compound strings (`"Makati - Metro Manila"`, `"Taguig - Metro Manila"`, `"Quezon - Metro Manila"`, `"Parañaque - Metro Manila"` (note the **ñ**), `"Manila - Metro Manila"`) → all → `MNL`; `"Cavite"` → `MNL`; `"Cebu"` → `CEB`. The loader's `_cityIata` lowercases the raw city string, so the keys are stored exactly as the lowercased labels (the ñ key is `'parañaque - metro manila'`). (3) PH manifest registered. `MNL`/`CEB` already exist in `iata-cities.js`.

- **[DELTA]**

  **1. `PH-michelin.js`** (root, **NEW**) — `const COUNTRY='PH'; const ENTRIES=[ …34 venues verbatim… ]; module.exports={COUNTRY,ENTRIES}`. Header documents the source, the 7 city labels, the "- Metro Manila" suffix convention, and the 2026-only manifest. 34 unique ids, 34 awards.

  **2. `michelin-data.js`** —
  - `COUNTRY_TABLES`: `require('./PH-michelin')` after `require('./MO-michelin')`.
  - `CITY_IATA`: a "// Philippines" block with the 7 keys (5 Metro-Manila districts + Cavite → `MNL`; Cebu → `CEB`).
  - `COUNTRY_MANIFEST` + `PH: { 2026: { 'two-star':1,'one-star':8,'bib-gourmand':25, total:34 } }`. **2026-only** (no 2025 key); no three-star.

  **3. `__tests__/michelin-data.test.js`** — added a full PH load suite (34 venues / 34 awards, **0@2025** / 34@2026, per-tier 2026 manifest `{two:1,one:8,bib:25}`, unique ids, every `city` curated incl. the compound + ñ labels, Helm 2★ 2026, `CITY_IATA['parañaque - metro manila']==='MNL'`, Cebu resolves), a `PH-michelin.js is venue-centric with 34 rows` require test, and a `hasMichelinData` PH-true case (PH / ph / Makati / Cebu / Parañaque). The false-gate (Shanghai/CN) is unchanged — CN remains the only empty stub.

  **4. `package.json`** — `0.61.343 → 0.61.344`.

  **`doc/.serial-state.yml`** — commit 556 → 557, journal 543 → 544, build 342 → 343; re-anchored 19:59 SGT 06-06-26.

- **[VERIFICATION (sandbox)]**
  - `node --check` clean: `PH-michelin.js`, `michelin-data.js`, `__tests__/michelin-data.test.js`.
  - Real-loader temp-harness (all 10 `{CC}-michelin.js` incl. the new PH + patched `michelin-data.js` in `/tmp/phtest`): loads **without throwing**. `venuesForCountry('PH')===34`; global unique ids **1450/1450** (MY 70 + TH 180 + JP 589 + KR 117 + TW 196 + VN 83 + HK 147 + MO 34 + PH 34, no dup); PH 2025=0 / 2026=34; tiers `{two:1,one:8,bib:25}`; `sum(PH awards)===34`. PH manifest passes. The ñ key + compound Metro-Manila labels resolve. `hasMichelinData` true for PH/Makati/Cebu/Parañaque; Shanghai/CN still false.
  - **14/14** explicit assertions green.
  - Full `npm test` runs in CI on merge.

- **[STATUS]** PR-**OPEN**, **auto-merge NOT armed**. Landed via the GitHub Git Data API on a branch off origin/main @ v0.61.343. Railway redeploys on merge.

- **[TEST]** Operator: the Philippines is now in the venue loader as its own country (`PH`) — 34 venues across Metro Manila (Makati/Taguig/Quezon/Parañaque/Manila), Cebu, Cavite; the inaugural **2026** edition. With the Philippines done, only **China** remains an empty stub. `instruction/Philippines.js` stays source-of-record.

- **[KNOWN GAPS]**
  - **2026-only** — no 2025 (the PH guide debuted in 2026; nothing due — this is the first edition).
  - **No native-name field** — none stored (source has none; PH venue names are already in Latin script).
  - **PH not in the OTHER-mode picker** — `cities.js` has no `PH` block; PH cities are loader-side (`CITY_IATA`) only. The compound "- Metro Manila" labels would need normalising if added to the picker later.
  - **Unified loader still off the live search path** — MY/TH/JP/KR/TW/VN/HK/MO/PH via `michelin-data.js` not yet wired into the bot's Michelin search (SG standalone).
  - A `doc/Feature/` / `doc/Technical/` / `doc/Register/` update + a `vault/v0.61.344/` snapshot are still due.

- **Confirmation Gates**

  | Gate | Resolution | Date |
  |---|---|---|
  | G1 — version bump | PATCH `0.61.343 → 0.61.344`. | 06-06 '26 |
  | G2 — destructive action | None. New `PH-michelin.js` created; append-only COUNTRY_TABLES + CITY_IATA + manifest + test edits. Curated `instruction/Philippines.js` untouched. Loader clean (1450/1450 unique). | 06-06 '26 |
  | G3 — new decision rule | Philippines loads as its own ISO-2 country table `PH-michelin.js`; registered in COUNTRY_TABLES + 7 CITY_IATA keys (Metro-Manila districts + Cavite → MNL, Cebu → CEB); PH manifest 2026-only (34, no 2025). | 06-06 '26 |
  | G4 — paid external API call | _n/a_. Data load + loader registration only. | 06-06 '26 |

## Amendments

- **`PH-michelin.js`** (NEW) — 34 curated venues from `instruction/Philippines.js` (verbatim, ñ preserved); header v0.61.344.
- **`michelin-data.js`** — `COUNTRY_TABLES` +`require('./PH-michelin')`; `CITY_IATA` +7 PH keys; `COUNTRY_MANIFEST` +`PH` (2026-only, 34).
- **`__tests__/michelin-data.test.js`** — +PH load suite + `PH-michelin.js` 34-row require test + `hasMichelinData` PH case.
- **`package.json`** — `0.61.343 → 0.61.344`.
- **`doc/.serial-state.yml`** — counters (commit 557 / journal 544 / build 343) + anchor re-set 19:59 SGT 06-06-26.

## Deleted

- _None._
