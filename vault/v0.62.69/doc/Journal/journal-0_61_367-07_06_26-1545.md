(№ 567 - 07-06 '26 15:45 SGT) v0.61.367

# Journal — soleat v0.61.367 (HOTFIX: gate-settle hang on conflict-resolve → new centroid)

> Operator: *"i select a new country+city in Menu TMA, an launch the Cuisine TMA, if location conflict is resolved it should go to the new place centroid. currently is hang as it tries to load 5 after resolve the location (based on the last PR)."*
>
> Regression from **v0.61.365**. **Left OPEN for review — auto-merge to be armed on the operator's word.** Commit 580 / journal 567 / build 365. Cuisine TMA.

## New

### [HDR] #567 | 15:45 SGT 07-06-26 | v0.61.367 — gate-settle hang hotfix | 1 code file | 1 PR | 1 build |

- **[INTENT]** Un-hang the post-conflict boot: after a location mismatch is resolved, the Cuisine TMA must open and show the **new place centroid**, not hang on the "Confirming your location…" splash.

- **[ROOT CAUSE]** The v0.61.365 0.3 s settle timer had a `return () => clearTimeout(settleTimer)` cleanup **and** `state.region` / `state.countryPref` in its deps. On the conflict-**resolve** path the `apply*`'s `runSearch` mutates `state.region` / `countryPref` **while it loads** → the effect re-ran on every churn, **cleared the just-started timer**, and rescheduled → the gate **never opened** → the splash hung over an already-running search ("hang as it tries to load 5").

- **[MECHANISM]** (`web/cuisine/src/v2/App.jsx`, gate-opener)
  - **One-shot guard** — a new `gateSettleScheduledRef` schedules the 300 ms settle **exactly once** per clean window; dep churn can't restart it. If a modal raises mid-settle, the timer **bails and clears the ref** so the post-dismiss re-run reschedules.
  - **No churning cleanup/deps** — dropped the `clearTimeout` cleanup and removed `state.region` / `state.countryPref` from the deps (those were the churn source; the mismatch-state deps already re-run the effect on modal dismiss).
  - The v0.61.365 Image-1 protection (0.3 s settle + `modalPendingRef` re-check) and the persist-before-load are **preserved**.

- **[DELTA]**
  - **`web/cuisine/src/v2/App.jsx`** — `gateSettleScheduledRef` one-shot guard; removed the timer cleanup + the two churning state deps.
  - **`package.json`** — `0.61.366 → 0.61.367`. **`doc/.serial-state.yml`** — commit 579 → 580, journal 566 → 567, build 364 → 365; re-anchored 15:45 SGT.

- **[VERIFICATION (sandbox)]** `npm run build` (cuisine) clean; full `npx vitest run` → **106 files / 3007 tests green**.

- **[STATUS]** PR-**OPEN**, **auto-merge to arm on the operator's word**. Cuisine TMA. Field test: pick a new country+city in Menu → open Cuisine → resolve the mismatch → it should open at the **new centroid** and load, no hang.

- **[TEST]** Operator: after resolving the location conflict, the Cuisine TMA opens at the new place and loads the 5 — no more stuck "Confirming your location…".

- **[KNOWN GAPS]**
  - **300 ms is still a fixed settle** (operator-specified). Slower-than-300 ms `countryPref` loads can pop the modal late (unchanged from v0.61.365).
  - **Not live-E2E tested** — no Telegram WebView here.

- **Confirmation Gates**

  | Gate | Resolution | Date |
  |---|---|---|
  | G1 — version bump | PATCH `0.61.366 → 0.61.367`. | 07-06 '26 |
  | G2 — destructive action | Replaces a churn-prone timer with a one-shot ref-guarded one + fewer deps; behaviour on the happy path unchanged; full suite green. | 07-06 '26 |
  | G3 — new decision rule | The gate settle is scheduled once and survives mid-load state churn, so the conflict-resolve path opens at the new centroid instead of hanging. | 07-06 '26 |
  | G4 — paid external API call | None added (the v0.61.365 reverse-geocode persist is unchanged). | 07-06 '26 |

## Amendments

- **`web/cuisine/src/v2/App.jsx`** — one-shot gate-settle guard; removed churning cleanup + deps.
- **`package.json`** — `0.61.366 → 0.61.367`.
- **`doc/.serial-state.yml`** — counters (commit 580 / journal 567 / build 365) + anchor 15:45 SGT 07-06-26.

## Deleted

- _None._
