(№ 558 - 07-06 '26 10:30 SGT) v0.61.358

# Journal — soleat v0.61.358 (Location-state safeguard — debounce the city-preview map fly)

> Operator: *"PR 5"* (declined the clarify prompt → I proceeded with the most-likely, low-risk remaining spec item). The location-state spec's last open safeguard: *"Debounce rapid city changes so the map does not jump unpredictably."*
>
> **Left OPEN for review — auto-merge NOT armed.** Commit 571 / journal 558 / build 357. TMA.

## New

### [HDR] #558 | 10:30 SGT 07-06-26 | v0.61.358 — debounce the city-preview fly (350 ms) so rapid switches don't jump the map | 1 code file | 1 PR | 1 build |

- **[INTENT]** Complete the location-state spec's safeguards. Rapid OTHER city switches each fired an immediate `setFlyTarget`, so the map could `panTo` repeatedly and jump. Debounce so only the latest pick flies.

- **[MECHANISM]** A `cityFlyDebounceRef` timer in `App`. In the `onLocationSelect` `cityPreview` branch, `selectedCityLocation` (and therefore the ↩ Back button) still update **immediately**; only the **map fly** is deferred — each pick clears the prior timer and schedules `setFlyTarget` after **350 ms** of quiet, so a burst of city switches results in a single smooth fly to the **last** one. `🔍` still commits the latest `selectedCityLocation`.

- **[DELTA]**
  - **`web/cuisine/src/v2/App.jsx`** — `cityFlyDebounceRef` + debounced fly in the preview branch.
  - **`package.json`** — `0.61.357 → 0.61.358`.
  - **`doc/.serial-state.yml`** — commit 570 → 571, journal 557 → 558, build 356 → 357; re-anchored 10:30 SGT 07-06-26.

- **[VERIFICATION (sandbox)]** `npm run build` clean; `npx vitest run` → **106 files / 2992 tests green**.

- **[STATUS]** PR-**OPEN**, **auto-merge NOT armed**. TMA; Railway rebuilds the cuisine bundle on merge. Eyes-on-live: flick through several OTHER cities quickly → the map settles once on the last, no jump.

- **[TEST]** Operator: switching cities quickly no longer makes the map jump around — it eases to the last one you landed on.

- **[KNOWN GAPS]**
  - Other spec safeguards (persist-previous, stale-centroid guard, labels reflect real search location) were already satisfied by the v0.61.354 decouple.
  - SG-only TMA location-sync + the TH picker-dupe cleanup remain optional follow-ups.
  - A `doc/Feature/` / `doc/Technical/` / `doc/Register/` update + a `vault/v0.61.358/` snapshot are still due.

- **Confirmation Gates**

  | Gate | Resolution | Date |
  |---|---|---|
  | G1 — version bump | PATCH `0.61.357 → 0.61.358`. | 07-06 '26 |
  | G2 — destructive action | Additive — one ref + a 350 ms debounce on the preview fly; immediate state unchanged; full suite green. | 07-06 '26 |
  | G3 — new decision rule | The city-preview map fly is debounced (350 ms) — rapid city switches fly only to the last pick; selection/Back update immediately. | 07-06 '26 |
  | G4 — paid external API call | _n/a_. | 07-06 '26 |

## Amendments

- **`web/cuisine/src/v2/App.jsx`** — `cityFlyDebounceRef` + 350 ms debounce on the city-preview fly.
- **`package.json`** — `0.61.357 → 0.61.358`.
- **`doc/.serial-state.yml`** — counters (commit 571 / journal 558 / build 357) + anchor 10:30 SGT 07-06-26.

## Deleted

- _None._
