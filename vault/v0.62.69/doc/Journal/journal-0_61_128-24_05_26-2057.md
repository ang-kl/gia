(№ 329 - 24-05 '26 20:57 SGT) v0.61.128

# Journal — soleat v0.61.128 (docs — VibeCodingRecord ledger catch-up PRs #553–#627)

> Operator: *"list of work to be done"* → *"1"* (VibeCodingRecord
> ledger refresh, from a two-item next-step shortlist) → *"merge"*.
> The hosted Vibe Journal at `/doc/vibe-journal.html` was 75 PRs
> behind: last entry was PR #552 (merged 2026-05-20T02:25Z); origin/
> main is at PR #628 with v0.61.127 in production. This PR catches the
> ledger up to PR #627 inclusive — pure docs/data, no runtime change.
>
> Commit 341 / journal 329 / build 131, on branch
> `claude/v0.61.128-vibe-ledger-catchup` off `main` at v0.61.127
> (commit `69956b3`, PR #628 — the v0.61.125-127 journal rollup).

## New

### [HDR] #329 | 20:57 SGT 24-05-26 | v0.61.128 — VibeCodingRecord ledger catch-up #553–#627 (docs/data only) | ~75 PRs | 10 files | 1 PR | 1 build |

- **[INTENT]** Catch the VibeCodingRecord hosted ledger
  (`/doc/vibe-journal.html`) up to PR #627. The last catch-up was
  PR #553 ("docs: VibeCodingRecord catch-up (PRs #542–#552)") on
  20-05 '26; the v0.61.57 → v0.61.127 arc landed 75 PRs that had no
  ledger row. Per `CLAUDE.md` root standing rule ("Regenerate
  `doc/VibeCodingRecord/` when convenient … so the ledger + hosted
  page track main").

- **[DELTA]**
  - **`doc/VibeCodingRecord/data/prs.ndjson`** — 75 rows appended
    (#553-#627), 545 → 620 lines. Each row matches the existing
    `{n, title, state:"closed", merged, body}` schema with `body`
    trimmed to ~360 chars; fetched via `gh pr list --state all --json
    number,title,state,mergedAt,body --search "is:pr"` then `jq` to
    normalise the body (collapse whitespace, slice 0..360).
  - **`doc/VibeCodingRecord/data/pr-files.tsv`** — 37 rows appended
    (the 37 PRs in #553-#627 whose squash-merge to `main` carries the
    `(#NNN)` tag in the commit subject). The other 38 PRs in the
    range have **no file list** — they either landed via the
    `48ee865 Add files via upload` bulk commit (most of #553-#587)
    or were closed-unmerged (#605, #609, #618). This matches the
    existing convention used for PR #1-#77, which predate the
    `(#NNN)` squash-commit convention.
  - **`doc/VibeCodingRecord/generate.mjs`** — `GEN_DATE`
    `'2026-05-20' → '2026-05-24'`. No logic change.
  - **`doc/VibeCodingRecord/records.tsv`,
    `doc/VibeCodingRecord/vibe-coding-record.md`,
    `public/doc/vibe-journal.html`,
    `public/doc/vibe-journal.json`** — all four regenerated by
    `node doc/VibeCodingRecord/generate.mjs`. 619 records total
    (1 closed-unmerged PR is filtered out by the generator);
    categories: feature=383, fix=85, docs=57, copy=35,
    prompt-tune=31, refactor=17, test=6, infra=5; rework-flagged=61.
  - **`package.json`** — `0.61.127 → 0.61.128`.
  - **`doc/.serial-state.yml`** — `commit 340 → 341`, `build 130 →
    131`, `journal 328 → 329`; re-anchored to 24-05 '26 20:57 SGT
    (source: SG air-temperature API per Rule TF-9); new
    comment-history block prepended.

- **[VERIFICATION (sandbox)]**

  ```
  $ wc -l doc/VibeCodingRecord/data/prs.ndjson
       620
  $ awk -F'\t' '$1>=553 && $1<=627 {n++} END {print n}' \
        doc/VibeCodingRecord/records.tsv
       75
  $ tail -1 doc/VibeCodingRecord/records.tsv | cut -f1-3
       627    merged    2026-05-24 09:54:38
  $ head -2 public/doc/vibe-journal.json
       { "generated": "2026-05-24",
         "count": 619, …
  $ node --check doc/VibeCodingRecord/generate.mjs
       (no output → OK)
  $ node doc/VibeCodingRecord/generate.mjs
       Vibe-Coding Record: wrote records.tsv (619 rows), …
       Categories: feature=383 fix=85 docs=57 copy=35 prompt-tune=31
                   refactor=17 test=6 infra=5 | rework-flagged=61
  ```

- **[STATUS]** PROD-bound on squash-merge to `main`. Auto-deploys via
  Railway; the hosted ledger updates as soon as the build finishes.

- **[TEST]** Operator (post-deploy): open
  `https://soleat.net/doc/vibe-journal.html` (supply `?key=<value>`
  if `VIBE_JOURNAL_KEY` is set). The header should read "generated
  2026-05-24, 619 records". The newest row at the top of the ledger
  is PR #627 ("v0.61.127: docs — fix Create_2_buttons.MD spec,
  Putrajaya not Sarawak"). The Insights panel's *PRs per release*
  bar list should now show v0.61.57-v0.61.127 entries.

- **[KNOWN GAPS]**
  - PR #628 (the v0.61.125-127 journal catch-up that landed at
    17:54 SGT) is **not** in this ledger refresh — it post-dates the
    last metadata pull window. Likewise PR #629 (this PR itself).
    Both will appear in the next catch-up.
  - **O-22 (iCloud Drive throttle)** — `git diff/status` against
    `origin/main` stalled during this session; built the commit via
    the operator's standard plumbing workaround (`git read-tree
    origin/main` + per-file `git update-index --cacheinfo` +
    `git commit-tree -p origin/main`), then pushed the branch.
  - Progressive radius widening for Fruits/Durian (O-23) and the
    real-Sarawak anchor (O-24) carry forward unchanged.

- **Confirmation Gates**

  | Gate | Resolution | Date |
  |------|-----------|------|
  | G1 — version bump | PATCH `0.61.127 → 0.61.128`. | 24-05 '26 |
  | G2 — destructive action | _n/a_ — pure append (ndjson, tsv) + regenerate (records.tsv, vibe-coding-record.md, vibe-journal.{html,json}). No removal. | 24-05 '26 |
  | G3 — new decision rule | _n/a_ — recurring upkeep per the standing rule in `CLAUDE.md` root §"keep `doc/Journal/` current per-PR". | 24-05 '26 |
  | G4 — paid external API call | _n/a_ — `gh` CLI hits GitHub's authenticated free-tier; no paid call. | 24-05 '26 |

## Amendments

_None — this PR is a ledger refresh, not a content correction._

## Deleted

_None._
