(№ 635 - 10-06 '26 06:25 SGT) v0.61.435

# Journal — soleat v0.61.435 (Michelin: an in-Singapore anchor always returns the Singapore list)

> Operator: *"Why my Michelin get corrupted, I selected Singapore and ought to get from Singapore Michelin, check the latest PR on change to Michelin listing."* Operator confirmed the selection was made via the **SG region pill**.
>
> Commit 648 / journal 635 / **no build (server-only)**. index.js (1 guard). Revert-ready. (Version bumped 0.61.433 → **0.61.435** at merge time: #950 (v0.61.433 Vibe Journal catch-up) and #952 (v0.61.434 Opus 4.8 migration) landed on main concurrently — see [KNOWN GAPS].)

## New

### [HDR] #635 | 06:25 SGT 10-06-26 | v0.61.435 — `insideSG` anchor forces SG Michelin (unless an explicit non-SG country is sent) — defeats region-drift / stale cached country | index.js | 1 PR | 0 build |

- **[INTENT]** Operator picked **Singapore** (SG pill) + **Michelin** and got a *corrupted* (wrong-country) Michelin list. Asked to check the latest Michelin PR.

- **[INVESTIGATION — log + diff, two sub-agents]** The latest Michelin PR is **#949 (v0.61.432)** — "Michelin abroad: country resolution + country-wide primary + cuisine pinned to top". It rewrote the `michelinCountry` resolution at the single `handleMichelinSearch` caller (`index.js` ~14291):
  ```js
  } else if (isOther) {                                  // region === 'OTHER' || 'MY-PUT'
    michelinCountry = requestCountry || null;            // explicit body countryCode
    if (!michelinCountry && csChatId) {
      const cachedLoc = await getUserLocation(...);       // ← stale cache
      const lc = cachedLoc?.country?.toUpperCase();
      if (/^[A-Z]{2}$/.test(lc) && lc !== 'SG') michelinCountry = lc;
    }
  } else { michelinCountry = 'SG'; }
  ```
  and the handler now returns **"country unresolved" → empty** for an empty `michCC` (no SG leak).
  - **The clean SG-pill path is correct** (`region:'SG'` → `else` branch → `'SG'`).
  - **The corruption fires when the SG-intended request reaches the server as `region=OTHER`** (region drift / stale app state) **without `countryCode:'SG'`.** Then #949's new fallback reads the **cached `location.country`** — which, after the operator had been testing MY/VN, is `MY`/`VN` → **foreign Michelin despite picking Singapore**. (Pre-#949 the same drift leaked to the **SG** list, which *accidentally* matched SG intent; #949 deliberately removed that SG fallback to fix MY/VN abroad — turning *accidental-SG* into *accidental-foreign-or-empty*.) The resolution never consulted the **anchor coordinates**, which were inside Singapore the whole time.

- **[DELTA]**
  - **`index.js` (single `handleMichelinSearch` caller, ~14291):** added an **`insideSG && !explicitForeign` guard** *before* the `isOther` branch — when the search anchor is physically inside the SG bbox (`insideSG`, already computed at ~14134) **and no explicit non-SG `requestCountry` was sent**, force `michelinCountry = 'SG'`, regardless of a drifted `OTHER` region or a stale cached country. Picking a foreign **guide-city** sets foreign coords (`insideSG=false`), so this never blocks browsing Michelin abroad. `isJB` still bails to `null` first (no JB list); the existing `OTHER` / `else` branches are unchanged for genuinely-abroad anchors.
  - **`!explicitForeign` (Codex P1 review):** the SG bbox (`lat ≤ 1.50`) overlaps Johor (JB CBD `1.4927`). A genuine **MY/JB** pick that fires as `region=OTHER` + `countryCode=MY` with JB coords (an App.jsx stale-closure on a Johor autocomplete pick) must **not** be coerced to SG — an **explicit** non-SG `requestCountry` is now honoured by the `OTHER` branch. The override fires only when there's no explicit foreign country (the real drift / stale-cache case), so it still fixes the operator's bug.
  - **`package.json`** `0.61.432 → 0.61.435` (collision-avoid vs #950/#952); **`doc/.serial-state.yml`** commit 647 → 648, journal 634 → 635, build unchanged (409); re-anchored 06:25 SGT 10-06-26.

- **[VERIFICATION (sandbox)]** `node --check index.js` clean. Single `handleMichelinSearch` caller confirmed (`grep -c` = 1). Full suite **3175/3175 green**. Server-only change → no `web/` build. `/gia-preflight`: the guard only *adds* an earlier SG resolution gated on `!explicitForeign`; the clean `region:'SG'` path already returned `'SG'`, so no regression for in-SG searches; the abroad / explicit-country branches are byte-identical.

- **[STATUS]** PR **#951 OPEN**, rebased onto `origin/main` @ v0.61.434 (post-#949/#950/#952). Auto-merge armed (squash). Railway redeploys (server only — no bundle change) on merge.

- **[TEST — operator, deployed]** Pick **Singapore** (SG pill) + **Michelin** → the **Singapore** Michelin/Bib list returns even if a prior MY/VN pick is still cached. Then pick **Malaysia** / **Vietnam** (their guide cities) + Michelin → still the **MY/VN** national list (unchanged from #949 — those anchors are not `insideSG`). Browsing a foreign guide-city from a phone physically in SG: the picker sets the foreign city's coordinates, so it still returns the foreign list.

- **[KNOWN GAPS]**
  - The deeper question — *why does the SG pill sometimes reach the server as `region=OTHER`?* — is a **client/state-drift** issue (App.jsx region reset + the 20s device-follow, partially addressed by v0.61.430 "explicit pick wins"). The Codex review pinpointed a related App.jsx **stale-closure** on a Johor autocomplete pick (`App.jsx:2313-2324, 2379-2388`) that can emit `region=OTHER` + `countryCode=MY` + JB coords. This server guard makes the **anchor authoritative** while honouring an explicit foreign country, so the symptom can't surface either way; a follow-up could still tighten the App.jsx region reset / stale-closure. Logged in Register.
  - The `insideSG` bbox (`lat ≤ 1.50`) is the repo's existing coarse SG box and overlaps Johor; `coords-to-country.js` has a tighter carved-out SG boundary. This fix sidesteps the overlap by gating on `!explicitForeign` rather than tightening the bbox (minimal, no blast radius); a future pass could reuse the accurate boundary.
  - **Concurrent collisions:** branched off main @ v0.61.432; while open, **#950 (v0.61.433 Vibe Journal catch-up)** and **#952 (v0.61.434 Opus 4.8 migration)** merged. Both touched only `package.json` + `doc/.serial-state.yml` (+ their own docs) — no `index.js` overlap. Resolved by merging main and renumbering this to **0.61.435 / № 635 / commit 648**.
  - Country/pin logic still lives in the large `handleMichelinSearch` route + its caller; not unit-tested (relied on `node --check` + suite + operator deploy check). A future `resolveMichelinCountry({ isJB, isOther, insideSG, requestCountry, cachedCountry })` extract would be unit-testable and would let this guard get a regression test.
  - VibeCodingRecord ledger refreshed through #949 by #950; this PR (#951) not yet in it — log/do post-merge.

- **Confirmation Gates**

  | Gate | Resolution | Date |
  |---|---|---|
  | G1 — version bump | PATCH `0.61.432 → 0.61.435` (collision-avoid vs #950/#952). | 10-06 '26 |
  | G2 — destructive action | None — adds one resolution branch (gated on `!explicitForeign`); abroad / explicit-country paths unchanged. Reversible. | 10-06 '26 |
  | G3 — new decision rule | An in-Singapore search anchor is authoritative for Michelin → the SG list, over any region/country drift — **unless** an explicit non-SG country is sent (then that wins). | 10-06 '26 |
  | G4 — paid external API call | None. | 10-06 '26 |

## Amendments

- **`index.js`** — `handleMichelinSearch` caller: new `insideSG && !explicitForeign` → `michelinCountry = 'SG'` guard ahead of the `isOther` (cached-country) branch; an explicit non-SG `requestCountry` is honoured by the `OTHER` branch (Codex P1).
- **`package.json`** — `0.61.432 → 0.61.435`.
- **`doc/.serial-state.yml`** — counters (commit 648 / journal 635 / build 409) + anchor 06:25 SGT 10-06-26 + `last_anchor_source` prepend (on top of #952/#950/#949's chain).

## Deleted

- (none — no prior behaviour removed; the abroad `OTHER` / `else` branches from #949 are preserved verbatim, the SG-anchor guard is inserted ahead of them.)
