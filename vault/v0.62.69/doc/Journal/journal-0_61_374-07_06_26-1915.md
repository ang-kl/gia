(№ 574 - 07-06 '26 19:15 SGT) v0.61.374

# Journal — soleat v0.61.374 (Singapore Michelin gets the new city-aware hint, de-duped)

> Operator investigated the stale Michelin strings, then chose **fix A**: give Singapore Michelin the new display hint, de-duped.
>
> **Left OPEN for review — auto-merge to be armed on the operator's word.** Commit 587 / journal 574 / build 372. Server + Cuisine TMA.

## New

### [HDR] #574 | 19:15 SGT 07-06-26 | v0.61.374 — SG Michelin new hint | 2 code files | 1 PR | 1 build |

- **[INTENT]** Replace the legacy "📚 Curated Michelin list — N more to explore (X in total). Tap 🔍 for the next batch of 12." with the new format for **Singapore** Michelin.

- **[ROOT — investigated]** The new "📚 Explore N … across `<flag>` `<Country>`" hint was **gated to non-Singapore**: `index.js:9083` computes `michSelectedCity` only when `!isSGMich`, so SG never set `michCityName` → `michelinSummary` omitted the city/country fields → the client (`App.jsx`) fell to the legacy national string. (Not "after 2 taps" — it was the legacy string on **every** SG page once `remaining > 0`.)

- **[MECHANISM]**
  - **Server (`index.js`, `handleMichelinSearch`)** — `michCountryName` / `michCountryFlag` are now computed **unconditionally** (`isSGMich` → `'Singapore'` + 🇸🇬; else `MICH_CC_NAME[cc]` + flag). The **city** block stays only for multi-city countries (so SG, where city == country, doesn't read "Singapore" twice). `michelinSummary` now ships `countryName`/`countryFlag` always; `city`/`cityRemaining` stay city-only.
  - **Client (`App.jsx`, `michelinHint`)** — added a **country-only NEW-format** branch: when there's country info but no city (Singapore), render **`📚 Explore 69 more · 117 across 🇸🇬 Singapore`** (no redundant "in `<City>`"). The legacy "Curated Michelin list …" string is now only a last-resort when no country info resolves at all.

- **[SCOPE]** Operator chose **A only** (not B): the hard-coded **"Michelin Singapore 2025"** edition label (`index.js:8942`, shown in the criteria line `App.jsx:2894`) is **left as-is**.

- **[DELTA]**
  - **`index.js`** — unconditional country fields + `michelinSummary` country/city split.
  - **`web/cuisine/src/v2/App.jsx`** — country-only hint branch.
  - **`package.json`** — `0.61.373 → 0.61.374`. **`doc/.serial-state.yml`** — commit 586 → 587, journal 573 → 574, build 371 → 372; re-anchored 19:15 SGT.

- **[VERIFICATION (sandbox)]** `node --check index.js` clean; `npm run build` (cuisine) clean; full `npx vitest run` → **106 files / 3029 tests green**.

- **[STATUS]** PR-**OPEN**, **auto-merge to arm on the operator's word**. Server + Cuisine TMA. Field test: open SG Michelin, page past the first batch → the hint reads "📚 Explore N more · 117 across 🇸🇬 Singapore", not the old "Curated Michelin list …".

- **[TEST]** Operator: Singapore Michelin now shows the same new-style hint the other countries get, without repeating "Singapore".

- **[KNOWN GAPS]**
  - **"Michelin Singapore 2025"** label untouched (fix B, not chosen).
  - **Not live-E2E tested** — no Telegram WebView here.

- **Confirmation Gates**

  | Gate | Resolution | Date |
  |---|---|---|
  | G1 — version bump | PATCH `0.61.373 → 0.61.374`. | 07-06 '26 |
  | G2 — destructive action | Additive — country fields computed always + a new client branch; the city path + legacy fallback are preserved; full suite green. | 07-06 '26 |
  | G3 — new decision rule | Every Michelin search (incl. Singapore) ships country fields; the hint uses the new "Explore N … across `<flag>` `<Country>`" format, de-duped for city-states. | 07-06 '26 |
  | G4 — paid external API call | None. | 07-06 '26 |

## Amendments

- **`index.js`** — `handleMichelinSearch` country fields unconditional; `michelinSummary` country/city split.
- **`web/cuisine/src/v2/App.jsx`** — country-only Michelin hint branch.
- **`package.json`** — `0.61.373 → 0.61.374`.
- **`doc/.serial-state.yml`** — counters (commit 587 / journal 574 / build 372) + anchor 19:15 SGT 07-06-26.

## Deleted

- _None_ (the legacy "Curated Michelin list" string is retained as a last-resort fallback only).
