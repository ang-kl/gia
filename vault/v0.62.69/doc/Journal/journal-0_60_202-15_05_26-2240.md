(№ 177 - 15-05 '26 22:40 SGT) v0.60.202

# Journal — soleat v0.60.202

> Operator: *"Missing /l or /location in the /start result."* + *"Remove /hidden in the following current location result… 📍 Location saved … Ready for /cuisine, /hidden, /s, /carpark, /transport."*. Two copy fixes; no logic change.

## New

### [HDR] #170 | 22:40:00 SGT 15-05-26 | v0.60.202 — /start intro adds /l /location · location-saved hint drops /hidden | 3 files | 1 PR |

- **[INTENT]** Operator-spotted text inconsistencies. The /start command listed every other top-level command except `/l` / `/location`; and the location-saved confirmation still listed `/hidden` even though `/hidden` is the Telegram sanctuary path the operator is moving away from in favour of `/cuisine` + `/s`.

- **[DELTA]**

  **`i18n.js`** — `start.intro`:
  - EN: inserted `/l /location — share or set your current location` between `/recognised` and `/weather` in the command list. The shortcut `/l` pattern mirrors the existing `/c /cuisine` and `/m /menu` shortcuts already in the intro paragraph.
  - FR: same insertion with FR copy `/l /location — partager ou définir votre position actuelle`.

  **`index.js`** — location-saved confirmation block (lines 2313-2314):
  - EN: `_Ready for /cuisine, /hidden, /s, /carpark, /transport._` → `_Ready for /cuisine, /s, /carpark, /transport._`
  - FR: `_Prête pour /cuisine, /hidden, /s, /carpark, /transport._` → `_Prête pour /cuisine, /s, /carpark, /transport._`

  **`package.json`** — `0.60.201 → 0.60.202` (PATCH; copy-only).

  **`doc/Journal/journal-0_60_202-15_05_26-2240.md`** — this entry (#170).

  **`doc/.serial-state.yml`** — `journal 169 → 170`, `commit 176 → 177`; re-anchored.

- **[VERIFICATION (sandbox)]**

  ```bash
  $ node --check index.js && node --check i18n.js
  syntax OK
  $ npm test -- --run
   Tests  1815 passed (1815)
  ```

- **[STATUS]** STAGED → PROD on next Railway deploy.

- **[TEST]** Post-deploy:
  1. `/start` in Telegram → command list now includes the `/l /location` row in both EN and FR.
  2. Share location (or `/l 102 Telok Blangah Dr`) → confirmation `📍 Location saved … Ready for /cuisine, /s, /carpark, /transport.` (no `/hidden`).
  3. `/hidden` is still a working command for users who type it explicitly — only the suggestion in the location-saved message changed.

- **[KNOWN GAPS]**
  - **DF-104 NEW** — other "ready for X" copy sites may still list `/hidden`. The location-saved confirmation is the most-trafficked surface; if operator wants `/hidden` dropped from /start's intro too, that's a separate decision (intro lists `/hidden`? — actually it doesn't, so this is one-off). No follow-up unless operator reports another stale mention.
  - DF-84, DF-101, DF-103 carried.

## Confirmation Gates

| Gate | Authority | Stamp |
|---|---|---|
| G1 — version bump | _PATCH 0.60.201 → 0.60.202 (copy-only)._ | auto |
| G2 — destructive action | _None._ | n/a |
| G3 — new decision rule | _Operator: the location-saved confirmation's "ready for X" list is curated copy; future menu changes should sync it. /hidden is being phased out of the recommended-commands surface but remains a working command._ | operator |
| G4 — paid external API call | _None._ | n/a |

## Amendments

(None. v0.60.201 (#169) is the prior entry.)

## Deleted

(None physically deleted.)
